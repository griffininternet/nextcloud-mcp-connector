---
phase: 13-cimd-nachmessung-und-release-0-1-9
plan: 05
type: execute
wave: 5
depends_on: ["13-04"]
files_modified:
  - CHANGELOG.md
  - docs/store-submission.md
autonomous: false
requirements: [EXAPP-09]

must_haves:
  truths:
    - "Der Branch main ist gepusht, bevor irgendein Tag existiert"
    - "Der Tag v0.1.9 entsteht erst nach einer ausdrücklichen Owner-Freigabe"
    - "Der Tag heißt genau v0.1.9 und entspricht der Version des Manifests"
    - "Der Release-Workflow ist in jedem Schritt grün und hängt das Store-Archiv an das GitHub-Release"
    - "Die Runbook-Schritte 4 und 5 tragen eine Proof-Zeile mit Datum, Behauptung und Befehl"
  artifacts:
    - path: "docs/store-submission.md"
      provides: "Proof-Zeile zu Push, Tag und Workflow-Lauf"
      contains: "v0.1.9"
  key_links:
    - from: "Tag v0.1.9"
      to: "appinfo/info.xml <version>"
      via: "Versions-Gleichheitsprüfung in .github/workflows/release.yml, Exit 1 bei Ungleichheit"
      pattern: "v0\\.1\\.9"
    - from: ".github/workflows/release.yml"
      to: "GitHub Release Asset mcp_connector-0.1.9.tar.gz"
      via: "build_store_release.sh im Job publish"
      pattern: "mcp_connector-0\\.1\\.9\\.tar\\.gz"
---

<objective>
Runbook Schritt 4 und 5: der Push vor dem Tag, das menschliche Tor, der Tag und der Workflow.
Dieser Plan überschreitet die einzige irreversible Grenze der Phase und ist deshalb nicht
autonom.

Purpose: D-01. Der Tag löst den Release-Workflow aus, ein Asset ist danach unveränderlich
und darf nie gelöscht werden, weil AppAPI von unserer URL installiert und nicht aus dem
Store. Der Push kommt zuerst: bei 0.1.8 lagen an dieser Stelle 42 ungepushte Commits, und ein
Tag auf einem nicht gepushten Stand hätte auf einen Commit gezeigt, den niemand abrufen kann.
Output: gepushter Branch, Tag `v0.1.9`, grüner Workflow-Lauf, eine Proof-Zeile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-cimd-nachmessung-und-release-0-1-9/13-CONTEXT.md
@.planning/phases/13-cimd-nachmessung-und-release-0-1-9/13-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Branch pushen, Datum prüfen, Abwesenheit des Tags belegen</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - docs/store-submission.md, Zeile 204-219 (Runbook Schritt 4, der Push vor dem Tag)
    - docs/store-submission.md, Zeile 125 (die 0.1.8-Proof-Zeile zu Push, Tag und Workflow als Formvorbild)
    - .github/workflows/release.yml: der Trigger `tags: - "v*"` und die Versions-Gleichheitsprüfung, die bei Ungleichheit mit Exit 1 abbricht
    - CHANGELOG.md, die Kopfzeile des 0.1.9-Blocks (`## [0.1.9] - ...`)
  </read_first>
  <action>
    Prüfe zuerst die Datumszeile des 0.1.9-Blocks in `CHANGELOG.md` gegen den heutigen
    Kalendertag in UTC. Stimmt sie nicht, korrigiere ausschließlich diese eine Zeile, damit
    das Release-Datum nicht lügt. Ändere keinen anderen Teil des Blocks.

    Pushe dann alle Commits der Phase: `git push origin main`. Belege danach mit
    `git log origin/main..HEAD`, dass nichts mehr ungepusht ist, und mit
    `git status --short`, dass der Arbeitsbaum sauber ist.

    Belege außerdem, dass noch kein Tag existiert (`git tag --list v0.1.9` leer) und dass es
    noch keinen Release-Workflow-Lauf zu diesem Tag gibt
    (`gh run list --workflow release.yml --limit 5`). Erzeuge in diesem Task keinen Tag.

    Setze keinen Milestone-Tag. Ein Tag `v1.3` würde `release.yml` auslösen und an der
    Versions-Gleichheitsprüfung mit Exit 1 abbrechen; Milestone-Tags heißen `milestone-v*`
    und triggern nichts.
  </action>
  <verify>
    <automated>git log origin/main..HEAD --oneline | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `git log origin/main..HEAD --oneline | wc -l` gibt 0
    - `git status --short` gibt keine Zeile aus
    - Die Kopfzeile des 0.1.9-Blocks in `CHANGELOG.md` trägt den heutigen Kalendertag in der Form `## [0.1.9] - YYYY-MM-DD`
    - `git tag --list v0.1.9` gibt keine Zeile aus
    - `gh run list --workflow release.yml --limit 5` nennt keinen Lauf zu `v0.1.9`
  </acceptance_criteria>
  <done>main ist gepusht, der Arbeitsbaum ist sauber, das Changelog-Datum stimmt, kein Tag und kein Workflow-Lauf existieren.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Owner-Freigabe für den Tag v0.1.9 und die Store-Sitzung</name>
  <files>keine</files>
  <read_first>
    - .planning/phases/13-cimd-nachmessung-und-release-0-1-9/13-01-SUMMARY.md bis 13-04-SUMMARY.md (was bis Runbook-Schritt 3 geliefert und gemessen wurde)
    - docs/store-submission.md, Zeile 249-261 (was die Store-Sitzung in Schritt 7 verlangt und welche drei Wege es gibt)
  </read_first>
  <action>
    Halte hier an und lege dem Owner den Stand aus <what-built> vor, zusammen mit den fünf
    Schritten aus <how-to-verify>. Führe in diesem Task kein Kommando aus, das einen Tag
    erzeugt oder einen Push auslöst: `git tag`, `git push origin v0.1.9` und jede
    Store-Einreichung sind hier verboten. Nimm die Antwort wörtlich auf (`freigeben` oder
    `warten`) samt dem genannten Weg für Schritt 7 und halte sie mit Zeitpunkt fest. Ohne die
    Antwort `freigeben` endet der Plan hier und Task 3 bleibt ungestartet.
  </action>
  <what-built>
    Alles bis Runbook-Schritt 3 ist erledigt und belegt: sechs Versionsstellen auf 0.1.9,
    Changelog-Block 0.1.9 mit `message_truncated` als Formatänderung, Enterprise-Abschnitt in
    drei READMEs und drei Store-Beschreibungen, Issue-Entwurf als Datei ohne Veröffentlichung,
    CIMD-Nachmessung gegen den 0.1.9-Kandidaten mit Proof-Zeile in `docs/oauth-setup.md`, sechs
    Gates lokal grün, Archiv-Probelauf bestanden, `main` gepusht, kein Tag vorhanden.
  </what-built>
  <how-to-verify>
    1. `git log origin/main..HEAD` ist leer und `git status --short` gibt nichts aus.
    2. `git tag --list v0.1.9` ist leer: es existiert noch kein Tag.
    3. Lies den 0.1.9-Block in `CHANGELOG.md` und den Abschnitt `## Enterprise` in `README.md`
       gegen: sagen beide, was du veröffentlichen willst?
    4. Lies die drei neuen Zeilen in der Nachweistabelle von `docs/store-submission.md` und
       die neue datierte Zeile im Kapitel "Client ID Metadata Documents" von
       `docs/oauth-setup.md`.
    5. Entscheide dann zwei Dinge:
       Erstens die Freigabe. `freigeben` bedeutet: `git tag v0.1.9` und
       `git push origin v0.1.9` starten `release.yml`, bauen das Multi-Arch-Image nach
       ghcr.io und hängen das Store-Archiv an ein GitHub-Release. Das ist irreversibel; ein
       Asset darf danach nie gelöscht und ein Tag nie umgeschrieben werden, weil AppAPI von
       dieser URL installiert. Eine spätere Korrektur kostet eine neue Patch-Version.
       `warten` bedeutet: nichts ist veröffentlicht, die Phase bleibt bis Schritt 3
       vollständig und in diesem Zustand prüfbar, und Erfolgskriterium 4 bleibt offen.
       Zweitens den Weg für Schritt 7 in Plan 13-06: eine angemeldete Store-Sitzung im
       Browser, ein `NC_STORE_TOKEN` in der Umgebung, oder das Formular unter
       `https://apps.nextcloud.com/developer/apps/releases/new`. Ohne einen dieser Wege endet
       die Phase nach Schritt 6.
  </how-to-verify>
  <acceptance_criteria>
    - Die Antwort lautet wörtlich `freigeben` oder `warten`
    - Bei `freigeben` ist außerdem benannt, welcher der drei Wege für Schritt 7 zur Verfügung steht
    - Die Antwort ist im SUMMARY dieses Plans festgehalten, mit Zeitpunkt
    - Ohne die Antwort `freigeben` wird Task 3 nicht ausgeführt und kein Tag erzeugt
  </acceptance_criteria>
  <resume-signal>Antworte mit `freigeben` oder `warten`. Nenne bei `freigeben` außerdem, ob eine angemeldete Store-Sitzung, ein `NC_STORE_TOKEN` oder das Formular für Schritt 7 der Weg ist.</resume-signal>
  <verify>
    <automated>test -z "$(git tag --list v0.1.9)" && echo "kein Tag, korrekt"</automated>
  </verify>
  <done>Die Antwort des Owners liegt wörtlich vor und ist mit Zeitpunkt festgehalten; kein Tag ist entstanden.</done>
</task>

<task type="auto">
  <name>Task 3: Tag v0.1.9, Workflow beobachten, Proof-Zeile der Schritte 4 und 5</name>
  <files>docs/store-submission.md</files>
  <read_first>
    - docs/store-submission.md, Zeile 204-226 (Runbook Schritte 4 und 5) und Zeile 125 (die 0.1.8-Zeile als Formvorbild, sie nennt Run-Id, Job-Name und Laufzeit)
    - .github/workflows/release.yml: die Jobs, die Versions-Gleichheitsprüfung, buildx für amd64 und arm64, der Aufruf von `scripts/build_store_release.sh` und das Anhängen des Assets
    - .planning/phases/13-cimd-nachmessung-und-release-0-1-9/13-04-SUMMARY.md (die Bytegröße des lokal gebauten Archivs, die Plan 13-06 gegen das veröffentlichte Asset stellt)
  </read_first>
  <action>
    Nur ausführen, wenn die Antwort aus Task 2 `freigeben` lautet.

    Setze den Tag exakt `v0.1.9`, kein anderer Name: `git tag v0.1.9`, dann
    `git push origin v0.1.9`. Ein Tippfehler wie `v0.19` triggert den Workflow ebenfalls und
    bricht erst an der Versions-Gleichheitsprüfung ab.

    Beobachte den Lauf bis zum Ende: `gh run list --workflow release.yml --limit 1`, um die
    Run-Id zu bekommen, dann `gh run watch <run-id> --exit-status`. Erwartet werden ein
    grüner Job `publish`, das gebaute und nach ghcr.io gepushte Multi-Arch-Image und ein an
    das GitHub-Release angehängtes `mcp_connector-0.1.9.tar.gz`.

    Schreibe danach eine Proof-Zeile in die Nachweistabelle von `docs/store-submission.md`,
    hinter die drei Zeilen aus Plan 13-04. Sie deckt die Schritte 4 und 5 zusammen ab, wie
    die 0.1.8-Zeile es tat: Datum in UTC, die Behauptung, dass der Workflow des Tags `v0.1.9`
    in jedem Schritt grün ist, mit Run-Id, Job-Name und Laufzeit, und die Behauptung, dass
    die Commits der Phase unmittelbar vor dem Tag nach `main` gepusht wurden. Dritte Spalte:
    `git push origin main`, `git tag v0.1.9`, `git push origin v0.1.9`,
    `gh run watch <run-id> --exit-status` mit dem Exit-Code.

    Schreibe die Zeile erst NACH dem grünen Lauf. Keine Zeile für die Schritte 6 bis 8: die
    entstehen in Plan 13-06, nachdem sie passiert sind.

    Lösche kein Release-Asset und schreibe keinen Tag um, auch nicht bei einem Fehlerbefund.
    Ein Fehler kostet eine neue Patch-Version und wird als Befund gemeldet.
  </action>
  <verify>
    <automated>gh release view v0.1.9 --json assets --jq '.assets[].name' | grep -c '^mcp_connector-0.1.9.tar.gz$'</automated>
  </verify>
  <acceptance_criteria>
    - `git tag --list v0.1.9` gibt genau `v0.1.9`
    - `git ls-remote --tags origin v0.1.9` nennt den Tag
    - `gh run watch <run-id> --exit-status` endete mit Exit 0, und `gh run view <run-id> --json conclusion --jq .conclusion` gibt `success`
    - `gh release view v0.1.9 --json assets --jq '.assets[].name'` nennt `mcp_connector-0.1.9.tar.gz`
    - `docs/store-submission.md` trägt eine neue Zeile mit Datum, der Run-Id des Laufs und den vier Kommandos in der dritten Spalte
    - `docs/store-submission.md` trägt weiterhin keine Zeile zu den Schritten 6 bis 8 für 0.1.9
    - `uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -q` endet mit Exit 0
  </acceptance_criteria>
  <done>Der Tag v0.1.9 existiert lokal und remote, der Workflow-Lauf ist grün, das Asset hängt am Release, und eine datierte Proof-Zeile belegt die Schritte 4 und 5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Arbeitsbaum zu öffentlichem Tag | Der Tag ist irreversibel und startet den Release-Workflow |
| Automat zu Mensch | Die Freigabe ist die eine Entscheidung dieser Phase, die kein Kommando ersetzen darf |
| GitHub Release Asset zu AppAPI-Installation | Administratoren installieren von dieser URL, nicht aus dem Store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-22 | Denial of Service | Tag v0.1.9 | mitigate | Der Tag entsteht ausschließlich in Task 3 und nur, wenn die Antwort des blockierenden Checkpoints `freigeben` lautet; der Plan ist `autonomous: false`, und `gate="blocking-human"` heißt, dass `auto_advance` hier nicht gilt (D-01) |
| T-13-23 | Tampering | Tag-Name | mitigate | Der Name ist wörtlich `v0.1.9`; `release.yml` triggert auf `v*` und prüft die Gleichheit gegen `<version>`; Milestone-Tags heißen `milestone-v*` und werden hier nicht gesetzt |
| T-13-24 | Denial of Service | Release-Asset und Tag-Historie | mitigate | Kein `gh release delete-asset`, kein `git tag -f`, kein Force-Push; ein Fehlerbefund wird gemeldet und kostet eine neue Patch-Version |
| T-13-25 | Repudiation | Proof-Zeile zu Schritt 4 und 5 | mitigate | Die Zeile entsteht erst nach dem grünen Lauf und trägt Run-Id, Job-Name, Laufzeit und Exit-Code; ein Akzeptanzkriterium verbietet Zeilen zu den Schritten 6 bis 8 |
| T-13-26 | Tampering | Nicht gepushter Stand unter einem Tag | mitigate | Task 1 belegt mit `git log origin/main..HEAD` die Zahl 0, bevor der Tag überhaupt zur Entscheidung steht (Runbook Schritt 4, Erfahrung 0.1.8 mit 42 ungepushten Commits) |
| T-13-27 | Repudiation | Changelog-Datum | mitigate | Task 1 prüft die Datumszeile des 0.1.9-Blocks gegen den heutigen Tag und korrigiert nur diese eine Zeile, bevor gepusht wird |
| T-13-SC | Tampering | Build im Workflow | transfer | Das Image wird im GitHub-Workflow aus dem getaggten Quellstand gebaut, nicht lokal hochgeladen; die Lieferkette liegt bei GitHub Actions und ghcr.io, ihre Nachweise erbringt Plan 13-06 mit OCI-Index und Tagliste |
</threat_model>

<verification>
- `git log origin/main..HEAD` leer, Arbeitsbaum sauber
- Antwort des Checkpoints im SUMMARY festgehalten
- `git tag --list v0.1.9` genau ein Treffer, remote vorhanden
- Workflow-Lauf `success`, Asset am Release
- Eine neue Proof-Zeile, keine für die Schritte 6 bis 8
</verification>

<success_criteria>
Roadmap-Erfolgskriterium 3 ist vollständig erfüllt: der Branch war gepusht, bevor irgendein
Tag existierte, und der Tag `v0.1.9` entstand erst nach ausdrücklicher Owner-Freigabe. Der
Workflow hat das Multi-Arch-Image und das Store-Archiv erzeugt.
</success_criteria>

<output>
Create `.planning/phases/13-cimd-nachmessung-und-release-0-1-9/13-05-SUMMARY.md` when done.
Nenne darin die Antwort des Checkpoints mit Zeitpunkt, die Run-Id des Workflow-Laufs und die
Download-URL des Assets, damit Plan 13-06 ohne Suche signieren kann. Lautete die Antwort
`warten`, halte das als Ergebnis fest und lasse Plan 13-06 ungestartet.
</output>
