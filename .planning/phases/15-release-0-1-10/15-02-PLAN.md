---
phase: 15-release-0-1-10
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - docs/store-submission.md
autonomous: true
requirements: [EXAPP-10]

must_haves:
  truths:
    - "Alle sechs Gates aus Runbook-Schritt 3 laufen lokal grün, ohne Anhebung eines Grenzwerts"
    - "Die Werkzeugoberfläche misst weiter 15711 Bytes über 21 Werkzeuge gegen ein Budget von 18000"
    - "Das gebaute Store-Archiv hat genau einen Top-Level-Ordner und das darin liegende README.md trägt die Statuszeile 0.1.10"
    - "Das darin liegende CHANGELOG.md trägt den Block 0.1.10 und den korrigierten 0.1.9-Wortlaut"
    - "Der Archiv-Probelauf ist im Nachweis ausdrücklich als Strukturprüfung benannt und die Skript-Signatur wird nicht eingereicht"
    - "Die Runbook-Schritte 1 bis 3 tragen je eine Proof-Zeile mit Datum, Behauptung und Befehl"
    - "Kein Tag v0.1.10 existiert"
  artifacts:
    - path: "docs/store-submission.md"
      provides: "Proof-Zeilen der Schritte 1 bis 3 für 0.1.10"
      contains: "0.1.10"
  key_links:
    - from: "dist/mcp_connector-0.1.10.tar.gz"
      to: "README.md Statuszeile"
      via: "das Archiv trägt README.md mit, die Statuszeile reist unveränderlich mit und kein Gate hält sie"
      pattern: "^Version 0\\.1\\.10\\."
    - from: "dist/mcp_connector-0.1.10.tar.gz"
      to: "CHANGELOG.md Block 0.1.10"
      via: "das Archiv trägt CHANGELOG.md mit, damit fährt auch die WR-02-Korrektur am 0.1.9-Block erstmals mit"
      pattern: "## \\[0\\.1\\.10\\]"
---

<objective>
Runbook Schritt 3: die sechs Gates lokal grün, der Archiv-Probelauf als Strukturprüfung, und
die Proof-Zeilen der Schritte 1 bis 3. Dieser Plan ist der letzte, der ohne Owner-Freigabe
vollständig abschließbar ist.

Purpose: Die Gates bleiben auf der v1.2-Messung (BUDGET_BYTES 18000, MAX_TOOL_BYTES 1400,
21 Werkzeuge); eine Anhebung ist laut ROADMAP ausdrücklich Out of Scope. Der Archiv-Probelauf
belegt die fünfte Versionsstelle VOR dem Tag: bei 0.1.8 lag der Fix hinter dem Tag, und weil
ein Asset unveränderlich ist, blieb im veröffentlichten Tarball für immer "Version 0.1.7"
stehen.

Diesmal trägt der Probelauf einen zweiten Beleg: das `CHANGELOG.md` im Archiv muss den
0.1.10-Block enthalten, denn genau darüber fährt auch die WR-02-Korrektur am 0.1.9-Block
erstmals in ein veröffentlichtes Asset und beendet den Drift W-1.

Eine CIMD-Nachmessung gehört ausdrücklich nicht in diese Phase, anders als bei 0.1.9:
0.1.10 ändert keine Zeile Code, nur Texte. Der CIMD-Weg ist mit der datierten Zeile in
`docs/oauth-setup.md` gegen den 0.1.9-Kandidaten belegt, und in dieser Phase gibt es nichts,
was diesen Beweis entkräften könnte. Ein neuer Lauf schriebe denselben Befund zum zweiten
Mal auf.

Output: sechs grüne Gates, ein geprüftes Archiv in `dist/` (nicht committet), drei
Proof-Zeilen in `docs/store-submission.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-release-0-1-10/15-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Die sechs Gates in der Runbook-Reihenfolge und der Archiv-Probelauf</name>
  <files>dist/ (gitignored, nicht committen)</files>
  <read_first>
    - docs/store-submission.md, Zeile 204-221 (Runbook Schritt 3: die sechs Kommandos wörtlich und die Aufzählung der Manifest-Gates)
    - docs/store-submission.md, Zeile 135 (die 0.1.9-Proof-Zeile zu Schritt 3: sie ist das Formvorbild und nennt Herkunft und Bedeutung der 15711 Bytes)
    - scripts/build_store_release.sh, Zeile 5-23 (der Kommentar "diagnosis only" samt der 0.1.8-Messung) und Zeile 42-50 (die vier Mitglieder des Archivs: appinfo/info.xml, CHANGELOG.md, LICENSE, README.md)
    - scripts/check_tool_budget.py (der Gate der Werkzeugoberfläche und seine Grenzwerte)
    - .planning/milestones/v1.3-phases/13-cimd-nachmessung-und-release-0-1-9/13-04-PLAN.md, Task 1 (derselbe Lauf für 0.1.9, inklusive der Regel, bei Abweichung zu stoppen statt anzuheben)
    - .planning/phases/15-release-0-1-10/15-01-SUMMARY.md (welche sieben Zeilen Plan 15-01 geändert hat)
  </read_first>
  <action>
    Führe die sechs Gates in genau der Reihenfolge aus, die Runbook Schritt 3 nennt, jedes mit
    dem Präfix `uv run --no-sync`: `pytest -q`, `ruff check .`, `ruff format --check .`,
    `pyright`, `vulture src scripts vulture_whitelist.py`, und
    `python scripts/check_tool_budget.py`. Halte jede Ausgabe fest, die die Proof-Zeile
    braucht: die Zahl der bestandenen und deselektierten Tests, die Zahl der formatierten
    Dateien, und die Messung der Werkzeugoberfläche.

    Erwartungswert der Werkzeugoberfläche: 15711 Bytes über 21 Werkzeuge gegen ein Budget von
    18000, dieselbe Zahl, die der 0.1.9-Lauf gemessen hat. Sie darf sich nicht geändert
    haben, weil seit 0.1.9 kein Werkzeug und kein Docstring angefasst wurde. Weicht sie ab,
    ist das ein Befund und kein Rundungsfehler: dann stoppe und melde die Abweichung, statt
    einen Grenzwert anzuheben. Eine Anhebung von BUDGET_BYTES oder MAX_TOOL_BYTES ist
    ausdrücklich Out of Scope.

    Erwartungswert der Testzahl: der 0.1.9-Lauf zählte 2812 bestandene und 163 deselektierte
    Tests, und Phase 14 hat genau einen Test hinzugefügt
    (`test_the_vocabulary_gate_stops_at_the_internal_planning_area`). Notiere die tatsächliche
    Zahl, rate sie nicht.

    Baue danach das Store-Archiv als Strukturprüfung: `bash scripts/build_store_release.sh`,
    dann `tar -tzf dist/mcp_connector-0.1.10.tar.gz`. Erwartet wird genau ein
    Top-Level-Ordner `mcp_connector/` mit den vier Mitgliedern `appinfo/info.xml`,
    `CHANGELOG.md`, `LICENSE` und `README.md`. Die vom Skript ausgegebene Signatur ist
    Diagnose und wird nirgends eingereicht: signiert wird in Plan 15-04 ausschließlich das
    heruntergeladene Asset, weil `tar.gz` nicht bytereproduzierbar ist (bei 0.1.9: 47546
    Bytes lokal gegen 47264 veröffentlicht, verschiedene sha256).

    Prüfe im gebauten Archiv zwei Inhalte ausdrücklich, denn beide reisen unveränderlich mit
    und kein Gate hält sie:

    Erstens die fünfte Versionsstelle:
    `tar -xzOf dist/mcp_connector-0.1.10.tar.gz mcp_connector/README.md | grep -c '^Version 0\.1\.10\.'`
    muss 1 geben.

    Zweitens den Changelog-Stand:
    `tar -xzOf dist/mcp_connector-0.1.10.tar.gz mcp_connector/CHANGELOG.md` muss den Block
    `## [0.1.10]` enthalten und im 0.1.9-Block den korrigierten Wortlaut aus `9ac0a3c`
    ("only at upload time"), nicht den vor der Korrektur veröffentlichten. Das ist der
    Beleg, dass der Drift W-1 mit diesem Asset endet.

    Committe nichts aus `dist/`; das Verzeichnis ist gitignored und hält bereits die Archive
    früherer Versionen. Erzeuge keinen Tag.
  </action>
  <verify>
    <automated>uv run --no-sync pytest -q && uv run --no-sync ruff check . && uv run --no-sync ruff format --check . && uv run --no-sync pyright && uv run --no-sync vulture src scripts vulture_whitelist.py && uv run --no-sync python scripts/check_tool_budget.py</automated>
  </verify>
  <acceptance_criteria>
    - Jedes der sechs Kommandos endet mit Exit 0, und die Ausgaben von `pytest -q`, `ruff format --check .` und `check_tool_budget.py` sind mit ihren Zahlen notiert
    - `uv run --no-sync python scripts/check_tool_budget.py` nennt 21 Werkzeuge, 15711 Bytes und ein Budget von 18000; jede Abweichung ist als Befund gemeldet und kein Grenzwert wurde angehoben
    - `git diff -- scripts/check_tool_budget.py tests/contract/test_tool_surface.py` ist leer
    - `tar -tzf dist/mcp_connector-0.1.10.tar.gz | cut -d/ -f1 | sort -u` gibt genau eine Zeile, `mcp_connector`
    - `tar -tzf dist/mcp_connector-0.1.10.tar.gz` nennt `mcp_connector/appinfo/info.xml`, `mcp_connector/CHANGELOG.md`, `mcp_connector/LICENSE` und `mcp_connector/README.md`
    - `tar -xzOf dist/mcp_connector-0.1.10.tar.gz mcp_connector/README.md | grep -c '^Version 0\.1\.10\.'` gibt 1
    - `tar -xzOf dist/mcp_connector-0.1.10.tar.gz mcp_connector/CHANGELOG.md | grep -c '^## \[0\.1\.10\]'` gibt 1
    - `tar -xzOf dist/mcp_connector-0.1.10.tar.gz mcp_connector/CHANGELOG.md | grep -c 'only at upload time'` gibt mindestens 1
    - `tar -xzOf dist/mcp_connector-0.1.10.tar.gz mcp_connector/appinfo/info.xml | grep -c 'admin@infranode.dev'` gibt mindestens 1
    - Die Bytegröße des lokal gebauten Archivs ist notiert
    - `git status --short dist` gibt keine Zeile aus
    - `git tag --list v0.1.10` gibt keine Zeile aus
  </acceptance_criteria>
  <done>Sechs Gates grün ohne Anhebung, das Archiv hat einen Top-Level-Ordner und trägt Statuszeile 0.1.10 sowie den 0.1.10-Changelog-Block, nichts aus dist ist committet, kein Tag existiert.</done>
</task>

<task type="auto">
  <name>Task 2: Proof-Zeilen der Runbook-Schritte 1 bis 3</name>
  <files>docs/store-submission.md</files>
  <read_first>
    - docs/store-submission.md, Zeile 76-81 (der Satz "Every line was measured, not assumed. No fact without its check." und der Tabellenkopf `| Date | Fact | Checked with |`)
    - docs/store-submission.md, Zeile 135-137 (die drei 0.1.9-Zeilen zu den Schritten 3, 1 und 2 als Formvorbild) und Zeile 145 (die letzte Zeile der Tabelle, 18:46Z) sowie Zeile 147 (`### The update keeps the connections`, die Grenze, vor der die neuen Zeilen stehen)
    - docs/store-submission.md, Zeile 187-221 (Runbook Schritte 1 bis 3, damit jede Behauptung ihren Schritt trifft)
    - .planning/phases/14-doku-reste-und-gate-entscheid/14-02-SUMMARY.md (die Tabelle läuft seit Phase 14 in der Uhrzeit vorwärts; neue Zeilen gehören deshalb an das Ende) und die Begründung der Vokabular-Ausnahme dieser Datei
    - tests/unit/test_exapp_env_setup.py, die Zeile mit `VOCABULARY_EXCEPTION` samt Gate-Docstring: warum genau diese Datei ausgenommen ist
  </read_first>
  <action>
    Schreibe drei neue Zeilen in die Nachweistabelle, direkt nach der letzten 0.1.9-Zeile
    (heute Zeile 145, 2026-08-25 18:46Z) und vor `### The update keeps the connections`.
    Form wie die Vorbilder: Datum in UTC mit `Z`, dann die Behauptung im Präsens, dann der
    Befehl, mit dem sie geprüft wurde. Sprache Englisch wie die übrige Datei. Die Tabelle
    läuft seit Phase 14 in der Uhrzeit vorwärts, deshalb stehen die neuen Zeilen am Ende und
    in der Reihenfolge ihrer Uhrzeit; bewege keine bestehende Zeile.

    Zeile zu Schritt 1: die sechs Versionsstellen tragen 0.1.10, mit den drei
    README-Statuszeilen ausdrücklich genannt, geprüft per grep je Stelle plus dem
    Manifest-Gate; nenne auch, dass `uv.lock` als sechste Stelle per Texteditierung
    mitgezogen wurde, ohne Lock-Lauf, und dass die vier CRLF-Dateien byte-exakt gepatcht
    wurden, sodass der Diff je Datei eine geänderte Zeile ist.

    Zeile zu Schritt 2: der Changelog-Block 0.1.10 nennt die Kürzung des
    Enterprise-Abschnitts in der Store-Beschreibung und den drei READMEs samt dem
    Kontaktwechsel zu admin@infranode.dev unter `### Changed` und die Übersetzungs-Korrekturen
    in README.fr.md und README.de.md unter `### Fixed`; die Linkdefinition `[0.1.10]` steht
    und ist mit ihrem Abschnitt gepaart, eine `[Unreleased]`-Definition existiert weiter
    nicht.

    Zeile zu Schritt 3: alle sechs Gates sind lokal grün, mit den gemessenen Zahlen aus
    Task 1 (bestandene und deselektierte Tests, formatierte Dateien, keine Lint- und
    Typbefunde, kein toter Code, Werkzeugoberfläche in Bytes über 21 Werkzeuge gegen 18000,
    unverändert gegenüber dem 0.1.9-Lauf und ohne Anhebung), und das Store-Archiv hat genau
    einen Top-Level-Ordner, trägt die Statuszeile 0.1.10 in seinem README.md und den Block
    0.1.10 in seinem CHANGELOG.md. Nenne in dieser Zeile ausdrücklich, dass mit dem
    mitreisenden CHANGELOG.md auch die nach dem 0.1.9-Tag geschriebene Korrektur am
    0.1.9-Block erstmals veröffentlicht wird, und dass der Archiv-Probelauf eine
    Strukturprüfung ist: das lokal gebaute Archiv ist nicht das Artefakt, das signiert wird.
    Nenne keine Signatur und keine Bytezahl als eingereichten Wert; die Bytegröße des lokalen
    Archivs darf als Messwert der Strukturprüfung stehen.

    Schreibe keine Zeile für ein Ereignis, das noch nicht eingetreten ist. Die Schritte 4
    bis 8 bekommen ihre Zeilen in den Plänen 15-03 und 15-04, nachdem sie passiert sind: eine
    vorab geschriebene Proof-Zeile dreht die Beweisrichtung um und macht aus einem Protokoll
    eine Behauptung. Diese Datei ist die einzige mit einer Vokabular-Gate-Ausnahme, und genau
    diese Beweisrichtung ist ihre Begründung.

    `docs/store-submission.md` liegt mit LF im Arbeitsbaum; die Zeilenenden bleiben LF. Keine
    Em-Dashes. Behaupte am Ende dieses Tasks ausdrücklich, dass noch kein Tag existiert.
  </action>
  <verify>
    <automated>uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `docs/store-submission.md` enthält drei neue Tabellenzeilen, jede beginnend mit einem Datum der Form `| YYYY-MM-DD HH:MMZ |`
    - Die drei Zeilen stehen zwischen der bisher letzten Tabellenzeile (18:46Z vom 2026-08-25) und der Überschrift `### The update keeps the connections`, belegt per `grep -n`
    - Die Datumsspalte der Tabelle ist von der ersten bis zur letzten Zeile aufsteigend; ein sortierter Vergleich der extrahierten Datumsspalte mit ihrer Originalreihenfolge zeigt keine Abweichung
    - `git diff --numstat docs/store-submission.md` nennt 3 hinzugefügte und 0 entfernte Zeilen
    - Jede der drei Zeilen nennt `0.1.10` und trägt in der dritten Spalte mindestens ein Kommando
    - Die Zeile zu Schritt 3 enthält die Worte structure check, sagt, dass das lokal gebaute Archiv nicht das signierte Artefakt ist, und nennt das mitreisende CHANGELOG.md
    - `docs/store-submission.md` enthält keine Zeile zu den Schritten 4 bis 8 für 0.1.10
    - `python -c "d=open('docs/store-submission.md','rb').read(); print(d.count(b'\r\n'))"` gibt 0
    - `git tag --list v0.1.10` gibt keine Zeile aus
    - `uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -q` endet mit Exit 0
  </acceptance_criteria>
  <done>Drei datierte Proof-Zeilen belegen die Schritte 1 bis 3, der Probelauf ist als Strukturprüfung benannt, die Tabelle läuft weiter vorwärts, keine Zeile läuft ihrem Ereignis voraus, kein Tag existiert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lokal gebautes Archiv zu veröffentlichtem Asset | Zwei verschiedene Bytefolgen, die leicht verwechselt werden; nur die zweite darf signiert werden |
| Nachweistabelle zu Leser | Die Tabelle ist der einzige Beleg für die Sorgfalt eines Releases; ihre Richtung ist Ereignis, dann Zeile |
| Gate-Grenzwerte zu Release-Kandidat | Ein angehobener Grenzwert macht einen roten Lauf grün, ohne etwas zu verbessern |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07 | Tampering | dist/mcp_connector-0.1.10.tar.gz | mitigate | Der Probelauf ist in der Proof-Zeile ausdrücklich als Strukturprüfung benannt; die Skript-Signatur ist Diagnose (Kommentar in `build_store_release.sh` Zeile 18-23); signiert wird ausschließlich in Plan 15-04 über das heruntergeladene Asset |
| T-15-08 | Tampering | Gate-Grenzwerte | mitigate | Grenzwerte bleiben auf der v1.2-Messung; bei Abweichung der 15711 Bytes stoppt der Task und meldet den Befund; ein Akzeptanzkriterium verlangt einen leeren Diff über `check_tool_budget.py` und `test_tool_surface.py` |
| T-15-09 | Repudiation | docs/store-submission.md | mitigate | Nur Zeilen für eingetretene Ereignisse; Form ist Datum in UTC plus Behauptung plus Befehl; ein Akzeptanzkriterium verbietet Zeilen zu den Schritten 4 bis 8 und verlangt 3 hinzugefügte und 0 entfernte Zeilen, sodass keine bestehende Zeile umgeschrieben wird |
| T-15-10 | Denial of Service | Git-Tag v0.1.10 | mitigate | Dieser Plan erzeugt keinen Tag und behauptet in beiden Tasks als Akzeptanzkriterium, dass `git tag --list v0.1.10` leer ist |
| T-15-11 | Tampering | Die fünfte Versionsstelle im Tarball | mitigate | Der Probelauf prüft `README.md` im Archiv gegen `^Version 0\.1\.10\.` VOR dem Tag; bei 0.1.8 lag dieser Fix hinter dem Tag und blieb als 0.1.7 für immer veröffentlicht |
| T-15-12 | Information Disclosure | dist/ im Arbeitsbaum | accept | `dist/` ist gitignored und hält nur gebaute Archive aus öffentlichem Quellcode; ein Akzeptanzkriterium prüft, dass nichts daraus committet wird |
| T-15-SC | Tampering | Paketinstallation | mitigate | Alle Gate-Läufe mit `uv run --no-sync`, also ohne Sync und ohne Auflösung; kein `uv add`, `pip install` oder `npm install` |
</threat_model>

<verification>
- Sechs Gates mit Exit 0, Zahlen notiert, kein Grenzwert angehoben
- Archiv mit genau einem Top-Level-Ordner, Statuszeile 0.1.10 und Block 0.1.10 darin
- Drei Proof-Zeilen am Tabellenende, Datumsspalte weiter aufsteigend, keine Zeile für die Schritte 4 bis 8
- `git tag --list v0.1.10` leer
</verification>

<success_criteria>
Roadmap-Erfolgskriterium 3 ist erfüllt: alle Gates laufen lokal grün auf dem
Release-Kandidaten, ohne Anhebung, bei 18000 und 1400 über 21 Werkzeuge. Die Phase ist damit
ohne Owner-Freigabe bis Runbook-Schritt 3 vollständig abgeschlossen und in diesem Zustand
prüfbar.
</success_criteria>

<output>
Create `.planning/phases/15-release-0-1-10/15-02-SUMMARY.md` when done.
Nenne darin die gemessenen Gate-Zahlen und die Bytegröße des lokal gebauten Archivs, damit
Plan 15-04 die Differenz zum veröffentlichten Asset belegen kann.
</output>
