---
phase: 15-release-0-1-10
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/mcp_connector/__init__.py
  - appinfo/info.xml
  - uv.lock
  - README.md
  - README.de.md
  - README.fr.md
  - CHANGELOG.md
autonomous: true
requirements: [EXAPP-10]

must_haves:
  truths:
    - "Die Zeichenkette 0.1.10 steht als dieselbe Version an allen sechs Versionsstellen"
    - "Der Changelog-Block 0.1.10 nennt den gekürzten Enterprise-Abschnitt und den Kontaktwechsel zu admin@infranode.dev als nutzersichtbare Änderung"
    - "Der Changelog-Block 0.1.10 nennt die Übersetzungs-Korrekturen in README.fr.md und README.de.md als Doku-Korrektur"
    - "Die Linkdefinition [0.1.10] existiert und referenziert einen Abschnitt, der existiert; eine [Unreleased]-Definition entsteht nicht"
    - "Nach diesem Plan existiert kein Tag v0.1.10"
  artifacts:
    - path: "pyproject.toml"
      provides: "Paketversion 0.1.10"
      contains: "version = \"0.1.10\""
    - path: "src/mcp_connector/__init__.py"
      provides: "__version__ 0.1.10"
      contains: "__version__ = \"0.1.10\""
    - path: "appinfo/info.xml"
      provides: "Manifest-Version und image-tag 0.1.10"
      contains: "<version>0.1.10</version>"
    - path: "README.md"
      provides: "Statuszeile 0.1.10, die fünfte Stelle, die im signierten Tarball mitreist"
      contains: "Version 0.1.10."
    - path: "CHANGELOG.md"
      provides: "Release-Notes-Block 0.1.10 plus eine Link-Referenz"
      contains: "## [0.1.10]"
  key_links:
    - from: "appinfo/info.xml"
      to: "src/mcp_connector/__init__.py"
      via: "Versions-Gleichheit, gehalten von tests/unit/test_exapp_env_setup.py"
      pattern: "0\\.1\\.10"
    - from: "appinfo/info.xml <image-tag>"
      to: "appinfo/info.xml <version>"
      via: "image-tag folgt der Version, gehalten vom selben Gate und ein zweites Mal von release.yml beim Tag"
      pattern: "<image-tag>0\\.1\\.10</image-tag>"
    - from: "CHANGELOG.md Linkdefinition [0.1.10]"
      to: "GitHub-Compare-Bereich v0.1.9...v0.1.10"
      via: "Markdown-Referenzdefinition am Dateiende, gepaart mit dem Abschnitt ## [0.1.10]"
      pattern: "compare/v0\\.1\\.9\\.\\.\\.v0\\.1\\.10"
---

<objective>
Dieser Plan hebt die Version des Repositories auf 0.1.10 und schreibt den
Release-Notes-Block dazu. Er erzeugt keinen Tag und stößt nichts an.

Purpose: Runbook Schritt 1 und 2 aus `docs/store-submission.md`. Ohne diesen Plan gibt es
keinen 0.1.10-Kandidaten: `scripts/build_store_release.sh` liest die Version aus
`appinfo/info.xml` (Zeile 30) und benennt das Archiv danach, und `release.yml` verweigert
einen Tag, dessen Name nicht der Manifest-Version gleicht.

0.1.10 existiert als Release, weil der Store die Manifest-Texte ausschließlich zur
Uploadzeit liest: der gekürzte Enterprise-Abschnitt und der Kontaktwechsel zu
admin@infranode.dev liegen seit Commit `55a5822` im Repository und erreichen bis zu diesem
Release keinen einzigen Leser der Store-Seite. Genau diese Begründung tragen die Blöcke
0.1.5, 0.1.6 und 0.1.9 schon.

Output: sechs geänderte Versionsstellen, ein Changelog-Block, eine Link-Referenz.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-doku-reste-und-gate-entscheid/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sechs Versionsstellen auf 0.1.10</name>
  <files>pyproject.toml, src/mcp_connector/__init__.py, appinfo/info.xml, uv.lock, README.md, README.de.md, README.fr.md</files>
  <read_first>
    - pyproject.toml, Zeile 1-5 (`version = "0.1.9"` steht auf Zeile 3)
    - src/mcp_connector/__init__.py, Zeile 1-9 (`__version__ = "0.1.9"` steht auf Zeile 7)
    - appinfo/info.xml, Zeile 183 (`<version>`) und Zeile 258 (`<image-tag>`)
    - uv.lock, Zeile 470-476 (der Block mit `name = "nextcloud-mcp-connector"`, die Versionszeile ist 472)
    - README.md, Zeile 25-29 (Abschnitt `## Status`, Statuszeile auf 27)
    - README.de.md, Zeile 27-31 (Statuszeile auf 29)
    - README.fr.md, Zeile 29-33 (Statuszeile auf 31)
    - docs/store-submission.md, Runbook Schritt 1 (Zeile 187-200): dort stehen die Stellen wörtlich, samt der Begründung, warum die drei Statuszeilen von Hand gehoben werden
    - tests/unit/test_exapp_env_setup.py, der Manifest-Gate, der `<version>` gegen die Paketversion und `<image-tag>` gegen `<version>` hält
    - .planning/milestones/v1.3-phases/13-cimd-nachmessung-und-release-0-1-9/13-01-PLAN.md, Task 1 (dasselbe Vorgehen für 0.1.9, inklusive der uv.lock-Begründung)
    - .planning/phases/14-doku-reste-und-gate-entscheid/14-01-SUMMARY.md, Abschnitt `patterns` (CRLF-Dateien byte-exakt per Python rb/wb patchen)
  </read_first>
  <action>
    Ersetze die Zeichenkette 0.1.9 durch 0.1.10 an genau sechs Stellen und an keiner
    weiteren. Ein Grep über die sieben Dateien nennt heute genau sieben Zeilen mit 0.1.9,
    und genau diese sieben sind die Arbeitsmenge (die drei README-Statuszeilen zählen als
    eine Stelle, die fünfte):

    `pyproject.toml:3` zu `version = "0.1.10"`, `src/mcp_connector/__init__.py:7` zu
    `__version__ = "0.1.10"`, `appinfo/info.xml:183` zu `<version>0.1.10</version>`,
    `appinfo/info.xml:258` zu `<image-tag>0.1.10</image-tag>`, die drei Statuszeilen
    (`Version 0.1.10.` als Zeilenanfang in README.md:27, README.de.md:29 und
    README.fr.md:31) und `uv.lock:472` zu `version = "0.1.10"`.

    Die uv.lock-Zeile ist die sechste Stelle und wird bewusst mitgezogen: Commit `8392680`
    hat sie beim 0.1.8-Bump ebenso geändert, obwohl kein Gate und kein Runbook-Schritt sie
    hält, und 0.1.9 ist ihr genauso gefahren. Fasse sie per Texteditierung an, rufe NICHT
    `uv lock` und NICHT `uv sync` auf: ein Lock-Lauf würde Dependency-Zeilen bewegen. Kein
    `uv add`, kein `pip install`, kein Dependency-Block in `pyproject.toml` oder `uv.lock`.

    README.md, README.de.md, README.fr.md und appinfo/info.xml liegen mit CRLF im
    Arbeitsbaum (536, 551, 570 und 540 CRLF-Zeilen, keine reine LF-Zeile). Patche diese
    vier byte-exakt, per Python im Binärmodus (`rb` lesen, ersetzen, `wb` schreiben), wie
    Phase 14 es getan hat. Sonst entsteht ein Massen-Diff aus Zeilenenden und der Diff
    verliert seine Aussage. `pyproject.toml`, `src/mcp_connector/__init__.py` und `uv.lock`
    liegen mit LF und werden normal editiert.

    In den drei READMEs ändert sich ausschließlich die Versionszahl der Statuszeile. Die
    Zahl 21 in "All 21 tools", "Alle 21 Tools" und "Les 21 outils" bleibt unangetastet: sie
    steht unter `tests/contract/test_tool_surface.py`, und in dieser Phase wird kein
    Werkzeug angefasst. Keine Em-Dashes, echte Umlaute in der deutschen und echte Accents
    in der französischen Fassung.

    In `appinfo/info.xml` werden `<summary>`, die `<description>`-Blöcke, die
    Enterprise-Abschnitte und `<environment-variables>` in diesem Task NICHT angefasst. Ein
    leer gemachtes Element lässt den Store-Upload später mit HTTP 500 sterben, und der
    gekürzte Enterprise-Text ist die Nutzlast dieses Releases: er ist schon fertig und darf
    nicht neu formuliert werden.

    Fasse `CHANGELOG.md` in diesem Task nicht an. Die Linkdefinitionen auf Zeile 480 und
    darunter enthalten die Zeichenkette 0.1.9 in Compare-URLs, die zur Historie gehören
    und bleiben.

    Erzeuge keinen Tag. `git tag` bleibt in diesem Plan unbenutzt.
  </action>
  <verify>
    <automated>uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^version = "0.1.10"' pyproject.toml` gibt 1
    - `grep -c '^__version__ = "0.1.10"' src/mcp_connector/__init__.py` gibt 1
    - `grep -o '<version>[^<]*' appinfo/info.xml` gibt genau `<version>0.1.10`
    - `grep -o '<image-tag>[^<]*' appinfo/info.xml` gibt genau `<image-tag>0.1.10`
    - `grep -n '^Version 0\.1\.10\.' README.md README.de.md README.fr.md` gibt drei Treffer, einen je Datei
    - `grep -n 'version = "0.1.10"' uv.lock` nennt Zeile 472
    - `grep -rn '0\.1\.9' pyproject.toml src/mcp_connector/__init__.py uv.lock README.md README.de.md README.fr.md appinfo/info.xml` gibt keinen Treffer
    - `git diff --numstat` nennt für jede der sieben Dateien genau `1	1`, also keine Zeilenenden-Umschreibung
    - `git diff -- pyproject.toml uv.lock` enthält keine Zeile mit `dependencies`, `requires-dist` oder einem Paketnamen außer `nextcloud-mcp-connector`
    - `git diff -- appinfo/info.xml` enthält keine Zeile mit `<summary`, `<description`, `Enterprise`, `admin@infranode.dev` oder `<default`
    - `python -c "import xml.etree.ElementTree as E; E.parse('appinfo/info.xml')"` läuft ohne Fehler
    - `git status --short CHANGELOG.md` gibt keine Zeile aus
    - `uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -q` endet mit Exit 0
    - `git tag --list v0.1.10` gibt keine Zeile aus
  </acceptance_criteria>
  <done>Sechs Stellen tragen dieselbe Zeichenkette 0.1.10, kein Zeilenenden-Massen-Diff, das Manifest-Gate ist grün, kein Tag existiert.</done>
</task>

<task type="auto">
  <name>Task 2: Changelog-Block 0.1.10 mit Enterprise-Kürzung und Kontaktwechsel</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md, Zeile 1-12 (Kopf, SPDX-Kommentar, Keep-a-Changelog-Absatz, dann direkt `## [0.1.9] - 2026-08-25`)
    - CHANGELOG.md, Zeile 12-45 (der 0.1.9-Block: der Einleitungsabsatz als Tonvorbild, der `### Added`-Eintrag zum Enterprise-Abschnitt und seine Begründung über die Uploadzeit, der `### Changed`-Eintrag zu `message_truncated`)
    - CHANGELOG.md, Zeile 476-489 (die zehn Linkdefinitionen; `[0.1.9]:` steht auf 480, eine `[Unreleased]`-Definition gibt es bewusst nicht mehr)
    - .planning/phases/14-doku-reste-und-gate-entscheid/14-01-SUMMARY.md, Abschnitt "DOC-01b: gewählter Weg und Regel für Phase 15" (die verbindliche Paarungsregel und die exakte Form der 0.1.10-Definition) sowie die Tabelle der vier ersetzten Wortstellen
    - `git show 55a5822` (der gekürzte Enterprise-Abschnitt und der Kontaktwechsel in README.md, README.de.md, README.fr.md und appinfo/info.xml: die Nutzlast dieses Releases im Wortlaut)
    - `git show 9ac0a3c` (die WR-02-Korrektur am 0.1.9-Block, die mit diesem Asset erstmals veröffentlicht wird; sie bekommt KEINEN eigenen Eintrag)
    - .planning/milestones/v1.3-phases/13-cimd-nachmessung-und-release-0-1-9/13-01-PLAN.md, Task 2 (Form und Ja/Nein-Disziplin des Vorgänger-Blocks)
  </read_first>
  <action>
    Setze einen neuen Block `## [0.1.10] - 2026-08-28` direkt oberhalb von
    `## [0.1.9] - 2026-08-25` (heute Zeile 12) ein. Das Datum ist der Kalendertag des
    Schreibens; Plan 15-03 prüft es vor dem Tag noch einmal nach und korrigiert es, falls
    der Tag an einem anderen Tag entsteht.

    Lege KEINEN `## [Unreleased]`-Abschnitt und KEINE `[Unreleased]`-Linkdefinition an.
    Phase 14 hat die hängende Definition entfernt und die Regel übergeben: eine Definition
    entsteht nur zusammen mit dem Abschnitt, der sie referenziert. Dieser Plan schreibt
    genau ein Paar, `## [0.1.10]` und `[0.1.10]:`.

    Der Block beginnt mit einem Einleitungsabsatz aus Prosa ohne Aufzählung, im Ton des
    0.1.9-Blocks: dieses Release trägt keinen Codewechsel, dieselben einundzwanzig
    Werkzeuge beantworten dieselben Fragen, und was sich ändert, ist Text, den nur ein
    Release zu den Lesern bringt.

    Danach genau diese zwei Rubriken:

    `### Changed` mit einem Eintrag zur Enterprise-Kürzung und zum Kontaktwechsel: der
    Enterprise-Abschnitt in der Store-Beschreibung und in den drei READMEs ist auf wenige
    Sätze gekürzt, und die Kontaktadresse ist von k.cherif@outlook.de auf
    admin@infranode.dev gewechselt. Nenne, dass keines der drei genannten Vorhaben (Audit-Log,
    Gruppen-Policies, SSO über den Identitätsanbieter der Organisation) in dieser Version
    existiert, in keiner Einstellung: der Abschnitt beschreibt weiter ein Vorhaben und kein
    Feature. Nenne die Begründung für das Release ausdrücklich und wortgleich in der Sache
    mit dem 0.1.9-Eintrag nach der WR-02-Korrektur: der Store liest die Manifest-Texte nur
    zur Uploadzeit, ein korrigierter Text im Repository erreicht bis zum nächsten Release
    niemanden, und 0.1.5, 0.1.6 und 0.1.9 waren aus genau diesem Grund Releases. Baue
    keinen Widerspruch zu diesem Satz.

    `### Fixed` mit einem Eintrag zu den Übersetzungs-Korrekturen: die französische
    Hauptüberschrift ist Französisch statt Englisch, das Scheinwort "confidemment" ist an
    seinen beiden Fundstellen in README.fr.md durch eine echte Formulierung ersetzt, und
    README.de.md schreibt den Produktbegriff so, wie die deutsche Store-Summary ihn
    schreibt. Halte fest, dass dies Wortkorrekturen in den Lesefassungen sind und kein
    Verhalten ändern.

    NICHT in den Block aufnehmen, weil nicht nutzersichtbar: die Entscheidung über die
    Reichweite des Vokabular-Gates und der Test, der sie hält; der neu formulierte
    Ampersand-Kommentar in `appinfo/info.xml`; die chronologische Sortierung der
    Nachweistabelle in `docs/store-submission.md` und der datierte Nachtrag am abgelegten
    Verifikationsstand; die Anhebung der GitHub-Actions auf ihre node24-Majors. Nenne auch
    die WR-02-Korrektur am 0.1.9-Block NICHT als eigenen Eintrag: sie ist Prosa im Block der
    Vorversion, sie fährt mit diesem Asset zum ersten Mal mit, und ein Eintrag darüber wäre
    ein Changelog-Eintrag über den Changelog.

    Am Dateiende: ergänze über `[0.1.9]:` (heute Zeile 480) die Definition `[0.1.10]:` mit
    `https://github.com/street1983nk/nextcloud-mcp-connector/compare/v0.1.9...v0.1.10`.
    Keine weitere Definition, keine entfernte Definition.

    Sprache Englisch wie die übrigen Blöcke. Keine Em-Dashes. `CHANGELOG.md` liegt im
    Vokabular-Gate und mit LF im Arbeitsbaum: das Wort `archiv` darf im neuen Block in
    keiner Sprache und in keinem Kompositum vorkommen, und die Zeilenenden bleiben LF.
  </action>
  <verify>
    <automated>uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '^## \[' CHANGELOG.md | head -2` zeigt `## [0.1.10] - 2026-08-28` vor `## [0.1.9] - 2026-08-25`
    - `grep -c 'admin@infranode.dev' CHANGELOG.md` gibt mindestens 1, und der Treffer liegt oberhalb der Zeile mit `## [0.1.9]`
    - Der 0.1.10-Block nennt `k.cherif@outlook.de` als bisherige Adresse und `### Changed` sowie `### Fixed`, und keine weitere Rubrik
    - Der 0.1.10-Block nennt "upload time" oder eine gleichbedeutende Aussage über den Zeitpunkt, zu dem der Store die Manifest-Texte liest, samt der Versionen 0.1.5 und 0.1.6
    - Der 0.1.10-Block nennt README.fr.md und README.de.md im `### Fixed`-Eintrag
    - Der 0.1.10-Block nennt weder `Unreleased` noch `store-submission` noch `node24` noch `FORBIDDEN_VOCABULARY` noch `WR-02`
    - `grep -c 'Unreleased' CHANGELOG.md` gibt 0
    - `grep -c 'compare/v0.1.9...v0.1.10' CHANGELOG.md` gibt 1
    - Die Mengen aus `grep -oE '^## \[[0-9.]+\]' CHANGELOG.md` und `grep -oE '^\[[0-9.]+\]:' CHANGELOG.md` sind nach Versionen sortiert identisch, 11 gegen 11
    - `grep -in 'archiv' CHANGELOG.md` gibt keinen Treffer
    - `grep -c '—' CHANGELOG.md` gibt 0
    - `python -c "d=open('CHANGELOG.md','rb').read(); print(d.count(b'\r\n'))"` gibt 0
    - `uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -q` endet mit Exit 0
    - `git tag --list v0.1.10` gibt keine Zeile aus
  </acceptance_criteria>
  <done>Der 0.1.10-Block steht über dem 0.1.9-Block, nennt die Enterprise-Kürzung mit dem Kontaktwechsel und die Übersetzungs-Korrekturen, die Linkdefinition ist mit ihrem Abschnitt gepaart, das Vokabular-Gate ist grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Arbeitsbaum zu Git-Tag | Ein Tag ist der Auslöser des Release-Workflows und irreversibel; dieser Plan darf die Grenze nicht überschreiten |
| Repo-Text zu veröffentlichtem Artefakt | Was hier als Zeichenkette steht, reist unveränderlich im signierten Tarball und in den Store-Metadaten mit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | Die sechs Versionsstellen | mitigate | Ein Akzeptanzkriterium je Stelle mit exaktem grep-Muster; `tests/unit/test_exapp_env_setup.py` hält Manifest gegen `__version__` und `image-tag` gegen `<version>`; `release.yml` prüft die Gleichheit beim Tag ein zweites Mal (Zeile 48-52) |
| T-15-02 | Denial of Service | Git-Tag v0.1.10 | mitigate | Dieser Plan erzeugt keinen Tag und behauptet in beiden Tasks als Akzeptanzkriterium, dass `git tag --list v0.1.10` leer ist; der Tag entsteht in Plan 15-03 nach ausdrücklicher Owner-Freigabe |
| T-15-03 | Tampering | Nutzlast des Releases in appinfo/info.xml und den READMEs | mitigate | Der gekürzte Enterprise-Text aus `55a5822` wird nicht neu formuliert; `git diff -- appinfo/info.xml` darf keine Zeile mit `<summary`, `<description`, `Enterprise` oder `<default` enthalten, und der Diff jeder Datei ist auf `1	1` begrenzt |
| T-15-04 | Denial of Service | appinfo/info.xml, leeres Element | mitigate | Beschreibungs- und Variablenblöcke bleiben unangetastet; zusätzlich parst ein Akzeptanzkriterium die Datei mit ElementTree, denn ein leeres Element beantwortet der Store-Upload mit HTTP 500 |
| T-15-05 | Repudiation | CHANGELOG.md | mitigate | Nur die zwei nutzersichtbaren Einträge, die die Phase-14-Summaries und `55a5822` belegen; fünf intern gebliebene Änderungen sind namentlich ausgeschlossen und per Akzeptanzkriterium geprüft; die Definition-Abschnitt-Paarung wird als Mengengleichheit 11 gegen 11 geprüft |
| T-15-06 | Tampering | Zeilenenden der vier CRLF-Dateien | mitigate | Byte-exaktes Patchen per Python rb/wb wie in Phase 14; `git diff --numstat` erlaubt genau eine geänderte Zeile je Datei, ein Massen-Diff aus Zeilenenden fällt damit auf |
| T-15-SC | Tampering | uv.lock, pyproject.toml Dependencies | mitigate | Kein `uv add`, `uv lock`, `uv sync`, `pip install` oder `npm install`; der Diff darf keinen Fremdpaketnamen und keine Dependency-Zeile enthalten; alle Prüfläufe mit `uv run --no-sync` |
</threat_model>

<verification>
- Alle sechs Versionsstellen tragen dieselbe Zeichenkette (grep je Stelle), kein 0.1.9-Rest in den sieben Dateien
- `uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -q` grün (Manifest-Gate, Beschreibungs-Gate, Variablen-Gate, Vokabular-Gate)
- `git diff --numstat` nennt genau acht Dateien, jede mit `1	1` außer CHANGELOG.md
- `git tag --list v0.1.10` leer
</verification>

<success_criteria>
Roadmap-Erfolgskriterium 1 ist erfüllt und Erfolgskriterium 2 ist geschrieben: die Version
0.1.10 steht als derselbe String an allen sechs Stellen, und der Changelog-Block 0.1.10
nennt den gekürzten Enterprise-Abschnitt samt dem Kontaktwechsel zu admin@infranode.dev als
nutzersichtbare Änderung sowie die Übersetzungs-Korrekturen als Doku-Korrektur. Kein Tag
existiert.
</success_criteria>

<output>
Create `.planning/phases/15-release-0-1-10/15-01-SUMMARY.md` when done.
Nenne darin ausdrücklich, welches Datum die Kopfzeile des 0.1.10-Blocks trägt, damit Plan
15-03 es vor dem Tag prüfen kann, und halte fest, dass mit diesem Block auch die
WR-02-Korrektur am 0.1.9-Block erstmals in ein Asset fährt (Tech-Debt-Punkt W-1 aus
`.planning/milestones/v1.3-MILESTONE-AUDIT.md`), ohne dass sie einen eigenen
Changelog-Eintrag bekommen hat.
</output>
