---
phase: 15-release-0-1-10
plan: 04
type: execute
wave: 4
depends_on: ["15-03"]
files_modified:
  - docs/store-submission.md
autonomous: false
requirements: [EXAPP-10]

must_haves:
  truths:
    - "Signiert wurde das heruntergeladene Release-Asset, nicht das lokal gebaute Archiv"
    - "Die Signatur verifiziert gegen das Zertifikat über genau die veröffentlichten Bytes"
    - "Der Store hat das Release 0.1.10 mit 201 angenommen"
    - "Vier Nachweise belegen Release-Liste, Erreichbarkeit des Assets, OCI-Index und Tagliste"
    - "Die Runbook-Schritte 6, 7 und 8 tragen je eine Proof-Zeile mit Datum, Behauptung und Befehl"
    - "Weder privater Schlüssel noch Store-Token noch Signatur stehen in einem Dokument oder Commit"
  artifacts:
    - path: "docs/store-submission.md"
      provides: "Proof-Zeilen der Schritte 6 bis 8 für 0.1.10"
      contains: "Verified OK"
  key_links:
    - from: "heruntergeladenes mcp_connector-0.1.10.tar.gz"
      to: "Signatur"
      via: "openssl dgst -sha512 -sign über genau dieses Artefakt"
      pattern: "Verified OK"
    - from: "apps.nextcloud.com Katalog"
      to: "GitHub Release Asset URL"
      via: "der Store hält nur URL und Metadaten, AppAPI installiert von der URL"
      pattern: "releases/download/v0\\.1\\.10"
---

<objective>
Runbook Schritt 6 bis 8: signieren, einreichen, nachweisen. Der letzte Plan der Phase.

Purpose: `tar.gz` ist nicht bytereproduzierbar, und der Store prüft die Signatur gegen die
Bytes, die er von der URL herunterlädt. Bei 0.1.9 lag das lokal gebaute Archiv bei 47546
Bytes gegen 47264 veröffentlichte, mit verschiedenen sha256, und bei 0.1.8 und 0.1.2 dasselbe
Muster. Die Signatur ist eine reine Funktion aus veröffentlichtem Asset und Schlüssel und
wird deshalb neu berechnet, wo sie gebraucht wird, statt weitergegeben zu werden.

Mit der Annahme dieses Releases erreicht der gekürzte Enterprise-Text samt der Adresse
admin@infranode.dev zum ersten Mal die Store-Seite, und das mitreisende `CHANGELOG.md`
beendet den Drift W-1 zwischen Repo und signiertem Asset.

Output: ein angenommenes Store-Release, vier Nachweise, drei Proof-Zeilen, EXAPP-10
geschlossen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-release-0-1-10/15-02-SUMMARY.md
@.planning/phases/15-release-0-1-10/15-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Das heruntergeladene Asset signieren und die Signatur gegenprüfen</name>
  <files>docs/store-submission.md</files>
  <read_first>
    - docs/store-submission.md, Zeile 240-247 (Runbook Schritt 6 mit den zwei Kommandos wörtlich)
    - docs/store-submission.md, Zeile 140 (die 0.1.9-Proof-Zeile zu Schritt 6: sie nennt beide Größen und beide sha256-Präfixe) und Zeile 276-278 (die Signatur ist eine reine Funktion aus Asset und Schlüssel und wird nie weitergegeben)
    - scripts/build_store_release.sh, Zeile 18-23 (der Kommentar "diagnosis only")
    - .planning/phases/15-release-0-1-10/15-02-SUMMARY.md (die Bytegröße des lokal gebauten Archivs)
    - .planning/phases/15-release-0-1-10/15-03-SUMMARY.md (die Download-URL und die im Release angehängte Bytegröße)
  </read_first>
  <action>
    Lade zuerst das veröffentlichte Asset in ein Arbeitsverzeichnis außerhalb von `dist/`:
    `curl -sSLO https://github.com/street1983nk/nextcloud-mcp-connector/releases/download/v0.1.10/mcp_connector-0.1.10.tar.gz`.
    Halte die Antwortkette fest: `curl -sSIL` auf dieselbe URL gibt 302, dann 200, und die
    Größe in Bytes.

    Signiere ausschließlich diese Datei, mit den zwei Kommandos aus Runbook Schritt 6:
    `openssl dgst -sha512 -sign ~/.nextcloud/certificates/mcp_connector.key mcp_connector-0.1.10.tar.gz`,
    die Rohsignatur durch `openssl base64 -A` für die Einreichung. Signiere nicht das Archiv
    aus `dist/` und nicht die Ausgabe von `scripts/build_store_release.sh`: dessen Signatur
    ist Diagnose und sagt es selbst.

    Prüfe die Signatur gegen, bevor sie eingereicht wird: den öffentlichen Schlüssel aus dem
    Zertifikat ziehen mit
    `openssl x509 -in ~/.nextcloud/certificates/mcp_connector.crt -pubkey -noout`, dann
    `openssl dgst -sha512 -verify` mit der Rohsignatur über das heruntergeladene Asset.
    Erwartet wird wörtlich `Verified OK`.

    Belege die Differenz zum lokal gebauten Artefakt, denn sie ist der Beweis, dass das
    richtige Artefakt signiert wurde: `sha256sum` beider Dateien und beide Bytegrößen, die
    des veröffentlichten Assets und die aus dem Probelauf von Plan 15-02. Stimmen sie
    ausnahmsweise überein, notiere das als Befund und nicht als Fehler.

    Prüfe zusätzlich im heruntergeladenen Asset, dass die Nutzlast dieses Releases darin
    steht: `tar -xzOf mcp_connector-0.1.10.tar.gz mcp_connector/appinfo/info.xml` enthält
    `admin@infranode.dev`, und
    `tar -xzOf mcp_connector-0.1.10.tar.gz mcp_connector/CHANGELOG.md` enthält
    `## [0.1.10]`. Damit ist belegt, dass die veröffentlichten Bytes den gekürzten
    Enterprise-Text und den korrigierten Changelog tragen und nicht nur der lokale Probelauf.

    Schreibe dann eine Proof-Zeile zu Schritt 6 an das Ende der Nachweistabelle von
    `docs/store-submission.md`, hinter die Zeile aus Plan 15-03, im Muster der 0.1.9-Zeile:
    Datum in UTC, die Behauptung mit beiden Größen und beiden sha256-Präfixen und dem
    Ergebnis `Verified OK`, plus der Beleg, dass das Asset `admin@infranode.dev` und den
    0.1.10-Block trägt, und in der dritten Spalte die Kommandos.

    Schreibe die Signatur selbst in kein Dokument, in keine Zusammenfassung und in kein
    Commit: sie wird neu berechnet, wo sie gebraucht wird. Der private Schlüssel wird nicht
    kopiert, nicht ausgegeben und nicht in das Repository gelegt.
  </action>
  <verify>
    <automated>openssl x509 -in ~/.nextcloud/certificates/mcp_connector.crt -pubkey -noout > mcp_pub.pem && openssl dgst -sha512 -verify mcp_pub.pem -signature mcp_connector-0.1.10.tar.gz.sig mcp_connector-0.1.10.tar.gz</automated>
  </verify>
  <acceptance_criteria>
    - `curl -sSIL https://github.com/street1983nk/nextcloud-mcp-connector/releases/download/v0.1.10/mcp_connector-0.1.10.tar.gz` gibt 302 dann 200 und eine Content-Length
    - `openssl dgst -sha512 -verify` über das heruntergeladene Asset gibt wörtlich `Verified OK`
    - `sha256sum` des heruntergeladenen Assets und des Archivs aus `dist/` sind notiert, beide Bytegrößen ebenfalls
    - `tar -xzOf mcp_connector-0.1.10.tar.gz mcp_connector/appinfo/info.xml | grep -c 'admin@infranode.dev'` gibt mindestens 1
    - `tar -xzOf mcp_connector-0.1.10.tar.gz mcp_connector/CHANGELOG.md | grep -c '^## \[0\.1\.10\]'` gibt 1
    - Die neue Proof-Zeile nennt beide Größen, beide sha256-Präfixe und `Verified OK`
    - `grep -rn 'BEGIN PRIVATE KEY\|BEGIN RSA PRIVATE KEY' docs .planning` gibt keinen Treffer
    - `git status --short` nennt keine `.sig`-, `.pem`- oder `.tar.gz`-Datei als unversionierte Neuerung im Repo-Wurzelverzeichnis, oder sie ist vor dem Commit entfernt
    - Die Base64-Signatur kommt in keinem Commit dieses Plans vor (`git show --format= -p HEAD` enthält keine Zeile mit einer Base64-Kette von mehr als 100 Zeichen)
  </acceptance_criteria>
  <done>Das veröffentlichte Asset ist signiert und die Signatur verifiziert; die Differenz zum lokalen Archiv und die Nutzlast im Asset sind belegt; eine datierte Proof-Zeile steht in der Tabelle.</done>
</task>

<task type="auto">
  <name>Task 2: Einreichung beim Store und die Annahme mit 201</name>
  <files>docs/store-submission.md</files>
  <read_first>
    - docs/store-submission.md, Zeile 248-278 (Runbook Schritt 7: der POST, die drei Wege und der Absatz zur Store-Sitzung)
    - docs/store-submission.md, Zeile 141 (die 0.1.9-Proof-Zeile zu Schritt 7 als Formvorbild) und Zeile 145 (die gemessenen Cache-Versätze)
    - docs/store-submission.md, der Abschnitt über den Cache (warum eine Änderung Minuten braucht und niemals nachreleast wird)
    - .planning/phases/15-release-0-1-10/15-03-SUMMARY.md (welcher Weg für die Store-Sitzung zugesagt wurde)
  </read_first>
  <action>
    Reiche die Download-URL und die Base64-Signatur beim Store ein, auf dem Weg, den die
    Checkpoint-Antwort aus Plan 15-03 genannt hat: entweder als POST auf
    `https://apps.nextcloud.com/api/v1/apps/releases` mit dem Header
    `Authorization: Token $NC_STORE_TOKEN` und dem JSON-Körper mit den Feldern `download` und
    `signature`, oder aus dem Seitenkontext einer angemeldeten Browser-Sitzung, damit das
    Token den Browser nicht verlässt, oder über das Formular unter
    `https://apps.nextcloud.com/developer/apps/releases/new`.

    Steht keiner dieser Wege zur Verfügung, halte hier an und melde den Zustand: alles bis
    Schritt 6 ist erledigt und die Signatur ist jederzeit mit den zwei Kommandos aus Schritt 6
    reproduzierbar. Erfinde keinen Weg und lege kein Token in das Repository oder in eine
    Umgebungsdatei des Arbeitsbaums.

    `201` ist die Annahme. Der Store lädt das Archiv selbst herunter, prüft die Signatur gegen
    das Zertifikat, prüft die Ordnerstruktur und validiert die Metadaten nach `pre-info.xslt`
    gegen `info.xsd`. Ein `500` ohne verwertbare Meldung deutet auf ein leeres XML-Element;
    in diesem Fall ist der Befund zu melden, nicht das Manifest im Nachhinein zu verändern,
    denn das Asset ist unveränderlich.

    Erscheint der gekürzte Enterprise-Text eine Minute später noch nicht im Katalog, ist das
    kein Fehlschlag: Detailseite, Katalog-Endpunkt und Suchindex laufen aus getrennten Caches
    mit gemessenen zwei bis zwölf Minuten Versatz (0.1.9: sechs Minuten am Katalog-Endpunkt,
    Detailseite nach zwei), Instanzen liegen bis zu einer Stunde zurück. Warte und frage
    erneut. Baue unter keinen Umständen ein zweites Release, um eine Änderung durchzudrücken.

    Schreibe danach eine Proof-Zeile zu Schritt 7 an das Tabellenende: Datum in UTC, die
    Behauptung, dass der Store das Release 0.1.10 mit 201 angenommen hat, und in der dritten
    Spalte der benutzte Weg samt Statuscode. Nenne kein Token, keinen Header-Wert und keine
    Signatur.
  </action>
  <verify>
    <automated>curl -sS https://apps.nextcloud.com/api/v1/appapi_apps.json | jq -e '.[] | select(.id=="mcp_connector") | [.releases[].version] | index("0.1.10")'</automated>
  </verify>
  <acceptance_criteria>
    - Die Antwort des Stores auf die Einreichung war `201`, und der Statuscode ist notiert
    - `curl -sS https://apps.nextcloud.com/api/v1/appapi_apps.json | jq -r '.[] | select(.id=="mcp_connector") | .releases[].version' | grep -cx '0.1.10'` gibt 1
    - Die neue Proof-Zeile nennt Datum, die Annahme mit 201 und den benutzten Weg
    - `grep -rniE '(NC_STORE_TOKEN=|Authorization: Token [A-Za-z0-9])' docs .planning` gibt keinen Treffer
    - Es existiert genau ein Release 0.1.10; `gh release list --limit 5` nennt keinen zweiten Versuch
    - `git diff --numstat docs/store-submission.md` nennt in diesem Task 1 hinzugefügte und 0 entfernte Zeilen
  </acceptance_criteria>
  <done>Der Store hat 0.1.10 mit 201 angenommen, die Release-Liste nennt die Version, eine datierte Proof-Zeile belegt es, und kein Token steht in einem Dokument.</done>
</task>

<task type="auto">
  <name>Task 3: Die vier Nachweise aus Schritt 8 und ihre Proof-Zeilen</name>
  <files>docs/store-submission.md</files>
  <read_first>
    - docs/store-submission.md, Zeile 279-289 (Runbook Schritt 8: die vier Kommandos) und Zeile 142-145 (die vier 0.1.9-Zeilen als Formvorbild; die Asset-Zeile sagt ausdrücklich "This row is not optional")
    - .planning/milestones/v1.3-phases/13-cimd-nachmessung-und-release-0-1-9/13-06-PLAN.md, Task 3 (dieselben vier Nachweise für 0.1.9)
  </read_first>
  <action>
    Erbringe die vier Nachweise mit `V=0.1.10`:

    Erstens die Release-Liste des Stores:
    `curl -sS https://apps.nextcloud.com/api/v1/appapi_apps.json | jq '.[] | select(.id=="mcp_connector") | [.releases[].version]'`.
    Erwartet werden die zehn bisherigen Versionen plus 0.1.10, also elf.

    Zweitens die Erreichbarkeit des Assets:
    `curl -I https://github.com/street1983nk/nextcloud-mcp-connector/releases/download/v0.1.10/mcp_connector-0.1.10.tar.gz`.
    Dieser Nachweis ist nicht optional: AppAPI installiert von dieser URL und nicht aus dem
    Store, ein gelöschtes Asset bricht jede spätere Installation mit
    "Failed to get app info ... from the Appstore".

    Drittens der OCI-Index des Images: Token holen von
    `https://ghcr.io/token?scope=repository:street1983nk/mcp_connector:pull&service=ghcr.io`,
    dann `https://ghcr.io/v2/street1983nk/mcp_connector/manifests/0.1.10` mit dem Header
    `Accept: application/vnd.oci.image.index.v1+json`. Erwartet werden beide Plattformen,
    `linux/amd64` und `linux/arm64`.

    Viertens die vollständige Tagliste:
    `https://ghcr.io/v2/street1983nk/mcp_connector/tags/list`. Erwartet werden die zehn
    bisherigen Tags plus 0.1.10, also elf.

    Achte bei allen Zählungen auf verankerte Muster. `0.1.1` ist ein Präfix von `0.1.10`:
    ein `grep -c '0.1.1'` zählt beide, `grep -cx '0.1.10'` oder `grep -c '^0\.1\.10$'` zählt
    genau eine. Dieselbe Falle gilt für die Sortierung: `0.1.10` steht in einer
    Zeichenketten-Sortierung vor `0.1.9`, das ist kein Fehler der Liste.

    Schreibe je eine Proof-Zeile an das Tabellenende, in dieser Reihenfolge und im Muster der
    vier 0.1.9-Zeilen: Datum in UTC, die Behauptung mit der konkreten Zahl oder dem konkreten
    Statuscode, dann der Befehl. Erwähne in der Zeile zur Release-Liste, ob und mit welchem
    Versatz der Katalog-Endpunkt die neue Version trug, statt einen Versatz zu verschweigen.
    Lösche nichts und schreibe keinen Tag um.

    Führe danach das Vokabular-Gate ein letztes Mal aus, prüfe, dass die Nachweistabelle
    keine Zeile für ein nicht eingetretenes Ereignis trägt und dass ihre Datumsspalte weiter
    aufsteigend läuft.
  </action>
  <verify>
    <automated>uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `curl -sS https://apps.nextcloud.com/api/v1/appapi_apps.json | jq -r '.[] | select(.id=="mcp_connector") | .releases[].version' | grep -cx '0.1.10'` gibt 1, und die Liste zählt elf Releases
    - `curl -sSI https://github.com/street1983nk/nextcloud-mcp-connector/releases/download/v0.1.10/mcp_connector-0.1.10.tar.gz` endet mit 200 nach einer 302-Weiterleitung
    - Der OCI-Index zu `0.1.10` nennt `linux/amd64` und `linux/arm64`
    - `curl -sS -H "Authorization: Bearer $TOKEN" https://ghcr.io/v2/street1983nk/mcp_connector/tags/list | jq -r '.tags[]' | grep -cx '0.1.10'` gibt 1, und die Liste zählt elf Tags
    - `docs/store-submission.md` trägt vier neue Zeilen, eine je Nachweis, jede mit Datum, konkreter Zahl oder Statuscode und Befehl
    - Die Zeilen der Schritte 6 bis 8 stehen in der Tabelle nach den Zeilen der Schritte 1 bis 5, und die Datumsspalte läuft von der ersten bis zur letzten Zeile aufsteigend
    - `git diff --numstat docs/store-submission.md` nennt über die drei Tasks dieses Plans insgesamt 6 hinzugefügte und 0 entfernte Zeilen
    - `uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -q` endet mit Exit 0
  </acceptance_criteria>
  <done>Vier Nachweise sind erbracht und als datierte Zeilen festgehalten; das Asset ist erreichbar, das Image liegt für beide Plattformen bereit, die Tagliste ist vollständig, EXAPP-10 ist damit geschlossen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lokales Artefakt zu veröffentlichtem Asset | Nur die heruntergeladenen Bytes dürfen signiert werden |
| Privater Signierschlüssel zu Repo und Dokumenten | Der Schlüssel liegt außerhalb des Arbeitsbaums und bleibt dort |
| Store-Token zu Arbeitsbaum | Das Token gehört der Store-Sitzung und ist bewusst nicht im Repository |
| Veröffentlichtes Asset zu AppAPI-Installationen | Administratoren installieren von dieser URL; sie muss dauerhaft antworten |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-19 | Tampering | Signatur über das falsche Artefakt | mitigate | Signiert wird ausschließlich die per `curl -sSLO` geholte Datei; Gegenprobe mit `openssl x509 -pubkey -noout` und `openssl dgst -sha512 -verify` vor der Einreichung, Ergebnis wörtlich `Verified OK`; `sha256sum` beider Dateien belegt die Differenz zum lokalen Archiv |
| T-15-20 | Information Disclosure | Privater Schlüssel und Store-Token | mitigate | Der Schlüssel wird nicht kopiert und nicht ausgegeben; Token und Header-Wert stehen in keinem Dokument; Akzeptanzkriterien sind greps nach `BEGIN PRIVATE KEY`, `NC_STORE_TOKEN=` und `Authorization: Token` ohne Treffer |
| T-15-21 | Information Disclosure | Base64-Signatur in einem Commit | mitigate | Die Signatur wird nie weitergegeben oder aufgeschrieben, sondern mit den zwei Kommandos aus Schritt 6 neu berechnet; ein Akzeptanzkriterium verbietet lange Base64-Ketten im Commit, und Arbeitsdateien (`.sig`, `.pem`, `.tar.gz`) landen nicht im Repo |
| T-15-22 | Denial of Service | Store-Upload antwortet 500 | mitigate | Ursache ist ein leeres XML-Element; die Beschreibungs- und Variablen-Gates liefen in Plan 15-01 und 15-02, und ElementTree hat die Datei geparst; im Fehlerfall wird der Befund gemeldet und kein Manifest nachträglich verändert, weil das Asset unveränderlich ist |
| T-15-23 | Denial of Service | Zweites Release wegen Cache-Versatz | mitigate | 201 ist die Annahme; Detailseite, Katalog und Suchindex laufen aus getrennten Caches mit zwei bis zwölf Minuten Versatz; der Plan verbietet ein Nachrelease ausdrücklich und prüft per `gh release list`, dass es nur einen Versuch gab |
| T-15-24 | Denial of Service | Gelöschtes Asset, umgeschriebener Tag | mitigate | Der `curl -I`-Nachweis ist Pflicht und nicht optional; kein Löschen, kein Force-Tag, kein Force-Push in diesem Plan |
| T-15-25 | Repudiation | Proof-Zeilen der Schritte 6 bis 8 | mitigate | Jede Zeile entsteht nach ihrem Ereignis und trägt Datum, konkrete Zahl oder Statuscode und Befehl; die Datumsspalte bleibt aufsteigend, und der Diff der Datei ist auf 6 hinzugefügte und 0 entfernte Zeilen begrenzt |
| T-15-26 | Tampering | Fehlzählung durch das Präfix 0.1.1 | mitigate | Alle Zählungen laufen mit verankerten Mustern (`grep -cx '0.1.10'`); die Zeichenketten-Sortierung, die `0.1.10` vor `0.1.9` stellt, ist im Plan als erwartetes Verhalten benannt |
| T-15-SC | Tampering | Herkunft des veröffentlichten Images | transfer | Image und Archiv entstehen im GitHub-Workflow aus dem getaggten Quellstand; die Nachweise drei und vier belegen Index und Tagliste bei ghcr.io, ein eigener Build wird nicht hochgeladen |
</threat_model>

<verification>
- `openssl dgst -sha512 -verify` gibt `Verified OK` über das heruntergeladene Asset
- Das Asset trägt `admin@infranode.dev` in info.xml und den Block 0.1.10 in CHANGELOG.md
- Store-Antwort 201, `appapi_apps.json` nennt 0.1.10 unter elf Releases
- Vier Nachweise erbracht, vier Zeilen geschrieben, Tabelle weiter aufsteigend
- `uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -q` grün
- Kein Token, kein Schlüssel, keine Signatur in einem Dokument
</verification>

<success_criteria>
Roadmap-Erfolgskriterium 4 ist vollständig und Erfolgskriterium 5 ist erfüllt: die Signatur
lief über das heruntergeladene Asset, Release 0.1.10 ist im Store gelistet, und die
Runbook-Schritte 1 bis 8 tragen je eine datierte Proof-Zeile mit Befehl und Ergebnis in
`docs/store-submission.md`. Damit ist EXAPP-10 vollständig und die Phase abgeschlossen.
</success_criteria>

<output>
Create `.planning/phases/15-release-0-1-10/15-04-SUMMARY.md` when done.
Nenne darin die Bytegröße und den sha256 des veröffentlichten Assets, nicht die Signatur, und
halte fest, dass der Changelog-Drift W-1 aus `.planning/milestones/v1.3-MILESTONE-AUDIT.md`
mit diesem Asset erledigt ist.
</output>
