---
phase: 10-mail-strikt-lesend-und-die-trifecta-grenze
plan: 06
type: execute
wave: 4
depends_on: [10-04, 10-05]
files_modified:
  - src/mcp_connector/server/reg_mail.py
  - scripts/check_tool_budget.py
  - scripts/acceptance_all_tools.py
  - tests/contract/test_tool_surface.py
  - tests/contract/test_no_destructive_calls.py
  - src/mcp_connector/vulture_whitelist.py
  - README.md
autonomous: true
requirements: [MAIL-01, MAIL-03]

must_haves:
  truths:
    - "tools/list antwortet mit 21 Werkzeugen, mail_browse ist eines davon, und die Werkzeugliste hängt weiterhin nicht davon ab, welche Apps eine Instanz hat"
    - "mail_browse trägt eine Nur-Lesen-Annotation, kein Output-Schema und ein Enum mit drei Ebenen"
    - "Die Zahl der Schreibwerkzeuge bleibt sechs: Mail fügt keines hinzu"
    - "Das Budget-Gate steht auf einer neuen, datierten Messung plus 15 Prozent und ist als Zwischenstand für Phase 11 markiert"
    - "Es existiert kein Weg, über diese App eine Mail zu senden, als Entwurf anzulegen, zu verschieben, zu markieren oder zu löschen, und das Gate belegt es mit Gegenprobe, obwohl die Mail-App selbst eine Sende-Route anbietet"
    - "Die vier erlaubten Mail-Leseformen passieren das Gate, und der Client enthält nur GETs"
  artifacts:
    - path: "src/mcp_connector/server/reg_mail.py"
      provides: "Ein Werkzeug mail_browse: Literal-Enum für level, READ_ONLY, structured_output=False, graceful, compact, leere Strings als Defaults"
      contains: "Literal"
      min_lines: 40
    - path: "scripts/check_tool_budget.py"
      provides: "Neue datierte Messzeile mit 21 Werkzeugen, angehobenes BUDGET_BYTES, Vermerk Zwischenstand"
      contains: "21"
    - path: "tests/contract/test_no_destructive_calls.py"
      provides: "Mail-Nadeln mit Gegenprobe je Nadel, ALLOWED_MAIL_ROUTES als Positivliste, der Nur-GET-Test über clients/mail.py"
      contains: "ALLOWED_MAIL_ROUTES"
    - path: "tests/contract/test_tool_surface.py"
      provides: "EXPECTED_TOOLS plus mail_browse, Zahl 21 in Assertion und Docstring, familienspezifischer Test"
      contains: "mail_browse"
    - path: "scripts/acceptance_all_tools.py"
      provides: "EXPECTED_TOOLS 21, ein Mail-Aufrufblock mit ehrlichem SKIP, mail_browse in der zweiten Namensliste"
      contains: "mail_browse"
  key_links:
    - from: "src/mcp_connector/server/reg_mail.py"
      to: "src/mcp_connector/tools/mail.py"
      via: "browse, DEFAULT_LIMIT und MAX_LIMIT kommen aus dem Tools-Modul, damit jede Zahl an einer Stelle steht"
      pattern: "mail_tools\\.(browse|DEFAULT_LIMIT|MAX_LIMIT)"
    - from: "tests/contract/test_no_destructive_calls.py"
      to: "/apps/mail/message/send"
      via: "Nadel plus Gegenprobe-Zeile durch den echten Prüfpfad _violations"
      pattern: "/message/send"
---

<objective>
Das Werkzeug registrieren und im selben Commit alles nachziehen, was daran hängt: die vier
eingefrorenen Zahlen an fünf Stellen, das Budget-Gate und das erweiterte Schreibverbot.

Purpose: Registrierung geschieht in diesem Projekt durch die Existenz einer `reg_*`-Datei, und der
Preis dafür sind die Zahlen, die an mehreren Stellen wiederholt sind. Sie müssen in **demselben**
Commit steigen wie die Registrierung, sonst ist der Contract-Test rot. Das Budget-Gate ist hier
nicht Formsache: gemessen sind 14358 von 15000 Bytes belegt, also 642 frei, und `talk_browse`, das
nächstliegende Vorbild, kostet allein 886. Der Kommentar im Skript sagt seit Phase 8 wörtlich "a
twenty-first tool ... is supposed to trip this gate", und das ist genau dieser Plan. Und MAIL-01
SC4 verlangt eine Gegenprobe, obwohl die Mail-App selbst eine Sende-Route anbietet: die liegt
direkt neben der Leseroute, beide teilen das Pfadsegment `/message/`, und nur `send`
unterscheidet sie. Deshalb bekommt Mail nicht nur Nadeln, sondern zusätzlich eine Positivliste
der vier erlaubten Formen.

Output: `server/reg_mail.py`, das angehobene Budget mit neuer Messzeile, die Zahl 21 an allen
Stellen, und das erweiterte Destruktiv-Gate mit Gegenproben.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-mail-strikt-lesend-und-die-trifecta-grenze/10-RESEARCH.md
@.planning/phases/10-mail-strikt-lesend-und-die-trifecta-grenze/10-PATTERNS.md
@.planning/phases/10-mail-strikt-lesend-und-die-trifecta-grenze/10-04-SUMMARY.md
@.planning/phases/10-mail-strikt-lesend-und-die-trifecta-grenze/10-05-SUMMARY.md

Die Vorlage für die Registrierungsdatei, vollständig:
@src/mcp_connector/server/reg_talk.py
@src/mcp_connector/server/__init__.py

Die fünf Pflegeorte:
@scripts/check_tool_budget.py
@scripts/acceptance_all_tools.py
@tests/contract/test_tool_surface.py
@tests/contract/test_no_destructive_calls.py
@src/mcp_connector/vulture_whitelist.py

Was registriert wird:
@src/mcp_connector/tools/mail.py
@src/mcp_connector/nextcloud/clients/mail.py

<interfaces>
Aus src/mcp_connector/server/__init__.py (heute im Repo gemessen):
  READ_ONLY, CREATE_ONLY, compact, graceful      Zeile 54 bis 99
  def _load_registrations() -> None              Zeile 101  (pkgutil über reg_*; keine zentrale Liste)
  deps.resolve_clients(ctx)

Aus src/mcp_connector/server/reg_talk.py (die Formvorlage, kurz genug, um sie als Ganzes zu lesen):
  @mcp.tool(annotations=READ_ONLY, structured_output=False)
  @graceful
  async def talk_browse(level: Annotated[Literal[...], Field(description=...)] = "conversations",
                        token: Annotated[str, Field(description=...)] = "",
                        limit: Annotated[int, Field(ge=1, le=talk_tools.MAX_LIMIT, ...)] = talk_tools.DEFAULT_LIMIT,
                        cursor: Annotated[str, Field(description=...)] = "",
                        ctx: Context | None = None) -> str
  Leere Strings als Defaults statt None, damit kein anyOf aus string und null ins Schema kommt;
  der Rumpf macht sie vor dem Aufruf wieder zu None.

Die vier eingefrorenen Zahlen an fünf Stellen (heute im Repo gemessen):
  tests/contract/test_tool_surface.py:36-57   EXPECTED_TOOLS (Menge, 20 Namen)
  tests/contract/test_tool_surface.py:429-434 Docstring "20 tools ... 18 of them" plus
                                              assert len(tools) == 20
  tests/contract/test_tool_surface.py:531     Docstring "six create-only tools, fourteen pure reads"
  scripts/acceptance_all_tools.py:54          EXPECTED_TOOLS = 20
  scripts/acceptance_all_tools.py:~370-395    zweite Namensliste im Abschlusstest
  scripts/check_tool_budget.py:46             BUDGET_BYTES = 15_000  (MAX_TOOL_BYTES = 1400 bleibt)
  Ausserdem: tests/contract/test_tool_surface.py:59-60 CREATE_TOOLS (bleibt bei sechs) und
             der Doku-Zählwächter (Zeile ~600 bis 640), der README.md und docs/ gegen
             len(EXPECTED_TOOLS) prüft.

Messung 2026-08-24, 20 Werkzeuge: 14358 Bytes bei BUDGET_BYTES 15000, also 642 frei.
Die teuersten: calendar_create_event 1351, calendar_list_events 951, search 924,
talk_browse 886, deck_create_card 877. MAX_TOOL_BYTES ist 1400.

Die vier erlaubten Mail-Pfadformen (aus clients/mail.py, Plan 10-02) und die eine verbotene:
  GET  /apps/mail/account/list
  GET  /apps/mail/ocs/mailboxes
  GET  /apps/mail/ocs/mailboxes/{mailbox}/messages
  GET  /apps/mail/message/{message}
  POST /apps/mail/message/send        <- die Gegenprobe, die MAIL-01 SC4 wörtlich verlangt
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: reg_mail.py und die Budget-Zwischenanhebung</name>
  <files>src/mcp_connector/server/reg_mail.py, scripts/check_tool_budget.py, src/mcp_connector/vulture_whitelist.py</files>
  <read_first>
    - src/mcp_connector/server/reg_talk.py (ganze Datei; sie ist die vollständige Vorlage)
    - src/mcp_connector/server/__init__.py (READ_ONLY, compact, graceful Zeile 54 bis 99, _load_registrations Zeile 101 bis 110)
    - scripts/check_tool_budget.py (ganze Datei; der Messzeilen-Block Zeile 16 bis 46 mit den drei bisherigen Anhebungen und dem Absatz über den einundzwanzigsten Werkzeug, MAX_TOOL_BYTES Zeile 48 bis 63)
    - src/mcp_connector/tools/mail.py (aus Plan 10-04) und 10-04-SUMMARY.md (Signatur von browse, FILTER_TYPES, die erlaubten is-Werte)
    - src/mcp_connector/vulture_whitelist.py (die Blockform am Dateiende)
    - .planning/phases/10-mail-strikt-lesend-und-die-trifecta-grenze/10-RESEARCH.md, Abschnitt "Das Token-Budget" vollständig, Falle 14, A5
    - .planning/phases/10-mail-strikt-lesend-und-die-trifecta-grenze/10-PATTERNS.md, Abschnitte "server/reg_mail.py" und "scripts/check_tool_budget.py"
  </read_first>
  <action>
    Neue Datei `src/mcp_connector/server/reg_mail.py` nach der Vorlage `reg_talk.py`. Der
    Modul-Docstring nennt die drei Entscheidungen der Registrierungsschicht in derselben
    Reihenfolge: `level` ist ein `Literal` und damit ein Enum im Input-Schema statt eines freien
    Strings, damit das Modell die drei gültigen Werte sieht statt "inbox" oder "folders" zu raten
    und eine Runde für die Korrektur zu zahlen (D-06, D-14); das Werkzeug wird bedingungslos
    gelistet, auch auf einer Instanz ohne Mail-App, weil eine credential-abhängige `tools/list`
    nicht cachebar ist, das Budget-Gate bricht und Clients überrascht, die Werkzeuglisten
    speichern (SRV-04); leere Strings sind die Defaults statt `None`, damit kein `anyOf` aus
    string und null ins Schema kommt, und der Rumpf macht sie vor dem Aufruf wieder zu `None`.

    Genau ein Werkzeug, `mail_browse`, mit `@mcp.tool(annotations=READ_ONLY,
    structured_output=False)` und `@graceful`, sechs Parametern plus `ctx`:
    `level` als `Literal["accounts", "mailboxes", "messages"]` mit Default `"accounts"`;
    `account_id` als `str` mit Default `""`; `mailbox_id` als `str` mit Default `""`;
    `filter` als `str` mit Default `""`; `limit` als `int` mit `ge=1`, `le=mail_tools.MAX_LIMIT`
    und Default `mail_tools.DEFAULT_LIMIT`; `cursor` als `str` mit Default `""`. Die Zahlen kommen
    aus dem Tools-Modul, damit sie an einer Stelle stehen. Der Rumpf ruft
    `compact(await mail_tools.browse(...))` und übergibt leere Strings als `None`.

    Die Feldbeschreibungen sind die Budget-Grenze dieser Phase: jedes Wort in
    `Field(description=...)` ist ein Byte in jedem `tools/list` jeder Sitzung, und
    `MAX_TOOL_BYTES` von 1400 ist der eigentliche Wächter. Die Beschreibungen müssen trotzdem die
    zwei Dinge tragen, die ein Modell nicht erraten kann: dass `mailboxes` eine `account_id` und
    `messages` eine `mailbox_id` braucht, und dass `filter` eine Grammatik hat. Für den Filter
    steht die vollständige Grammatik **nicht** im Schema, sondern im Docstring des Werkzeugs
    beziehungsweise in der README; die Feldbeschreibung nennt die erlaubten Typen in kürzester
    Form und den Hinweis, dass `start:` und `end:` Unix-Sekunden nehmen. Reicht `mail_browse`
    dabei über 1400 Bytes, wird die Beschreibung kürzer und **nicht** die Grenze höher (A5).

    In `scripts/check_tool_budget.py` das Budget einmal anheben, nach der Regel, die dort dreimal
    vorgeführt ist. Zuerst messen: `uv run python scripts/check_tool_budget.py` mit registriertem
    `mail_browse` liefert die neue Zahl. Dann eine neue Messzeile im Block ergänzen, mit Datum,
    der Werkzeugzahl 21, der Messung, der Rechnung (Messung plus 15 Prozent, aufgerundet auf die
    nächsten 500) und dem Vermerk, dass dies ein **Zwischenstand** ist und TOOL-15 in Phase 11 auf
    die Endmessung neu verankert. Die älteren Zeilen bleiben, wo sie sind: eine Regression ist nur
    zuordenbar, wenn die Zahl, von der sie abwich, noch lesbar ist. `BUDGET_BYTES` wird auf das
    Ergebnis der Rechnung gesetzt. `MAX_TOOL_BYTES` bleibt bei 1400. Der Absatz, der von einem
    einundzwanzigsten Werkzeug spricht, wird nachgezogen, damit er weiter auf das nächste zeigt
    und nicht auf dieses.

    `vulture_whitelist.py`: sollte ein Eintrag für einen Namen aus `clients/mail.py` oder
    `tools/mail.py` als Durchgangsstation gesetzt worden sein, kommt er hier heraus, weil die
    Registrierung die Namen jetzt aufruft. Am Phasenende ist das Gate ohne Mail-Eintrag grün; ist
    keiner nötig gewesen, bleibt die Datei unberührt, und das gehört in die Zusammenfassung.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run pyright</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run python scripts/check_tool_budget.py</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; python - &lt;&lt;'PY'
import re
from pathlib import Path
text = Path("scripts/check_tool_budget.py").read_text(encoding="utf-8")
budget = int(re.search(r"^BUDGET_BYTES = ([0-9_]+)", text, re.M).group(1).replace("_", ""))
assert budget > 15000, f"the gate has to rise for a twenty-first tool, is {budget}"
assert re.search(r"^MAX_TOOL_BYTES = 1400", text, re.M), "MAX_TOOL_BYTES stays the real guard"
lines = [l for l in text.splitlines() if l.lstrip().startswith("#")]
measurement = [l for l in lines if "21" in l and "2026-08" in l]
assert measurement, "a new dated measurement line naming 21 tools is required"
for old in ("10643", "12801", "14312", "14358"):
    assert old in text, f"the older measurement {old} must stay readable"
assert any("wischenstand" in l or "intermediate" in l.lower() for l in lines), "mark it as intermediate for TOOL-15"
print("budget line ok:", budget)
PY</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run python -c "
import asyncio
from mcp import Client
from mcp_connector.server import mcp
async def main():
    async with Client(mcp, raise_exceptions=True) as client:
        tools = {t.name: t for t in (await client.list_tools()).tools}
    assert 'mail_browse' in tools, sorted(tools)
    assert len(tools) == 21, len(tools)
    browse = tools['mail_browse']
    assert browse.output_schema is None, 'structured_output=False (schema diet)'
    assert browse.annotations is not None
    assert browse.annotations.read_only_hint is True
    assert browse.annotations.open_world_hint is False
    props = browse.input_schema['properties']
    assert props['level']['enum'] == ['accounts', 'mailboxes', 'messages'], props['level']
    for name in ('account_id', 'mailbox_id', 'filter', 'cursor'):
        assert props[name].get('type') == 'string', (name, props[name])
        assert 'anyOf' not in props[name], name
    print('mail_browse registered')
asyncio.run(main())
"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run vulture src/mcp_connector vulture_whitelist.py</automated>
  </verify>
  <acceptance_criteria>
    - `tools/list` antwortet mit 21 Werkzeugen, und `mail_browse` ist eines davon
    - `mail_browse` hat kein Output-Schema, `read_only_hint` ist `True` und `open_world_hint` ist `False`
    - Das `level`-Schema ist ein Enum mit genau `["accounts", "mailboxes", "messages"]`
    - `account_id`, `mailbox_id`, `filter` und `cursor` sind im Schema reine Strings ohne `anyOf`
    - `limit` trägt `ge=1` und `le` gleich `mail_tools.MAX_LIMIT`, und der Default ist `mail_tools.DEFAULT_LIMIT`
    - `uv run python scripts/check_tool_budget.py` endet mit Exit-Code 0, und `mail_browse` liegt unter `MAX_TOOL_BYTES` 1400
    - `BUDGET_BYTES` ist grösser als 15000 und gleich Messung plus 15 Prozent, aufgerundet auf die nächsten 500
    - Eine neue Kommentarzeile trägt Datum, die Werkzeugzahl 21, die Messung und die Rechnung, und die vier älteren Messungen 10643, 12801, 14312 und 14358 stehen weiterhin in der Datei
    - Eine Kommentarzeile markiert die Anhebung als Zwischenstand, den TOOL-15 in Phase 11 neu verankert
    - `MAX_TOOL_BYTES` ist unverändert 1400
    - `uv run vulture src/mcp_connector vulture_whitelist.py` ist grün, und `vulture_whitelist.py` enthält keinen Mail-Eintrag mehr
  </acceptance_criteria>
  <done>Das Werkzeug ist registriert, das Enum zeigt dem Modell die drei Ebenen, und das Budget-Gate steht auf einer neuen Messung, die als Zwischenstand markiert ist.</done>
</task>

<task type="auto">
  <name>Task 2: Die vier eingefrorenen Zahlen an fünf Stellen, im selben Commit</name>
  <files>tests/contract/test_tool_surface.py, scripts/acceptance_all_tools.py</files>
  <read_first>
    - tests/contract/test_tool_surface.py (ganze Datei; EXPECTED_TOOLS 36 bis 57, CREATE_TOOLS 59 bis 60, der Talk-Block 315 bis 358, der Vollständigkeitstest 427 bis 440, der Annotationstest 529 bis 540, der README-Tabellentest ~595 bis 615, der Doku-Zählwächter ~617 bis 640)
    - scripts/acceptance_all_tools.py (ganze Datei; EXPECTED_TOOLS Zeile 50 bis 54, der Talk-Aufrufblock 237 bis 268 mit dem ehrlichen SKIP, die zweite Namensliste ~365 bis 398)
    - src/mcp_connector/server/reg_mail.py (aus Task 1)
    - .planning/phases/10-mail-strikt-lesend-und-die-trifecta-grenze/10-RESEARCH.md, Abschnitt "Das Token-Budget", die zwei weiteren eingefrorenen Zahlen
    - .planning/phases/10-mail-strikt-lesend-und-die-trifecta-grenze/10-PATTERNS.md, Abschnitte "tests/contract/test_tool_surface.py" und "scripts/acceptance_all_tools.py"
  </read_first>
  <action>
    Alle Stellen nachziehen, an denen eine Zahl oder ein Name eingefroren ist. Die Regel dieser
    Phase: jede dieser Änderungen liegt im **selben** Commit wie `reg_mail.py`, weil eine Zahl an
    zwei Orten sonst driftet.

    In `tests/contract/test_tool_surface.py`:
    - `EXPECTED_TOOLS` bekommt `"mail_browse"`.
    - Der Vollständigkeitstest: `assert len(tools) == 21`, und der Docstring wird mitgezogen (er
      sagt heute "20 tools, and the diet holds for 18 of them"; die zweite Zahl ist die der
      Werkzeuge ohne Output-Schema und steigt auf 19).
    - Der Annotationstest: der Docstring sagt heute "six create-only tools, fourteen pure reads";
      die Schreibzahl bleibt sechs, die Leserzahl steigt auf fünfzehn. `CREATE_TOOLS` bleibt
      unverändert, und das ist eine prüfbare Aussage dieser Phase: Mail fügt kein
      Schreibwerkzeug hinzu. Der Kommentar über `CREATE_TOOLS`, der von sechs Schreibwegen
      spricht, bleibt richtig und braucht nur den Hinweis, dass die Zahl der reinen Leser
      gewachsen ist, falls er sie nennt.
    - Ein familienspezifischer Mail-Test nach dem Muster des Talk-Blocks: `mail_browse` ist
      vorhanden, hat kein Output-Schema, trägt `read_only_hint` True und `open_world_hint` False,
      das `level`-Enum ist genau die drei Ebenen in dieser Reihenfolge, `limit` trägt Minimum 1
      und Maximum `MAX_LIMIT`, die vier Stringparameter tragen kein `anyOf`, und eine verbotene
      Namensmenge existiert **nicht**: `mail_send`, `mail_list_messages`, `mail_read_message`,
      `mail_search`, `mail_fetch`, `mail_create_draft`. Der Docstring des Tests sagt, dass
      `mail_browse` und das bestehende `fetch` diese Menge abdecken und dass die Abwesenheit einer
      Sende-Funktion hier ein Vertrag ist und keine Lücke.

    In `scripts/acceptance_all_tools.py`:
    - `EXPECTED_TOOLS = 21`.
    - Ein Mail-Aufrufblock nach dem Muster des Talk-Blocks, mit dem ehrlichen SKIP, der für Mail
      der häufigere Fall ist: `mail_browse(level="accounts")` aufrufen; ist die Liste leer, ein
      `SKIP` mit dem Satz, dass dieses Konto kein Mail-Konto hat und es deshalb keine Postfächer
      zu lesen gibt; sonst mit der ersten Konto-Id `level="mailboxes"` aufrufen, und mit der
      `databaseId` des ersten Postfachs (bevorzugt dem mit `special_role` gleich `inbox`)
      `level="messages"`. Ist auch die Postfachliste leer, ein zweites, eigenes `SKIP` mit
      eigenem Satz. Kein `fetch("mail:...")` im Abnahmelauf ohne eine Nachrichten-Id aus dem
      vorigen Schritt; ist eine da, wird sie benutzt, sonst `SKIP`.
    - Die zweite Namensliste am Dateiende bekommt `"mail_browse"`. Sie ist der Pflegeort, der
      leicht übersehen wird, und der Grund, warum dieser Task existiert.

    Zum Schluss der Doku-Zählwächter, und der ist bereits vermessen. Er sucht mit
    `re.compile(r"tools=(\d+)|(\d+)\s+tools")` in `docs/*.md` und `README.md` und meldet jede
    Zahl, die nicht die aktuelle ist, **es sei denn** die Seite verweist auf
    `tests/contract/test_tool_surface.py`. Gemessen am 2026-08-24: die fünf Seiten
    `docs/client-setup.md`, `docs/conference-demo.md`, `docs/oauth-setup.md`,
    `docs/spike-discovery.md` und `docs/store-submission.md` verweisen alle auf den Halter, sind
    also erklärt und brechen nicht. `README.md` verweist **nicht** darauf und trägt genau zwei
    numerische Aussagen, Zeile 19 ("The 20 tools are curated so that ...") und Zeile 30 ("All 20
    tools of the v1 set are implemented ..."). Diese zwei Zahlen werden hier auf 21 gesetzt, und
    zwar nur die Zahlen: Werkzeugtabelle, Mail-Abschnitt, Grenzen, Trifecta-Absatz und die
    Übersetzungen bleiben Plan 10-07. Vor dem Schreiben ist der Grep zu wiederholen
    (`grep -rnE "tools=[0-9]+|[0-9]+ tools" README.md docs/*.md`), damit eine zwischenzeitlich
    dazugekommene Zeile nicht übersehen wird.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run pyright &amp;&amp; uv run pytest tests/contract/test_tool_surface.py -q</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run pytest -q</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; python - &lt;&lt;'PY'
import re
from pathlib import Path
surface = Path("tests/contract/test_tool_surface.py").read_text(encoding="utf-8")
assert '"mail_browse"' in surface
assert "len(tools) == 21" in surface, "the frozen count has to rise in the assertion"
assert " 20 tools" not in surface, "the docstring count has to rise with the assertion"
for forbidden in ("mail_send", "mail_list_messages", "mail_read_message", "mail_search"):
    assert forbidden in surface, f"{forbidden} belongs in the forbidden set of the family test"
acc = Path("scripts/acceptance_all_tools.py").read_text(encoding="utf-8")
assert re.search(r"^EXPECTED_TOOLS = 21", acc, re.M), "acceptance count"
assert acc.count("mail_browse") >= 3, acc.count("mail_browse")
assert "SKIP" in acc
print("frozen numbers pulled")
PY</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run python scripts/check_tool_budget.py</automated>
  </verify>
  <acceptance_criteria>
    - `EXPECTED_TOOLS` in `test_tool_surface.py` enthält `mail_browse`, und `assert len(tools) == 21` steht in der Assertion
    - Kein Docstring in `test_tool_surface.py` nennt noch 20 Werkzeuge; die Zahl der Werkzeuge ohne Output-Schema und die Zahl der reinen Leser sind mitgezogen
    - `CREATE_TOOLS` ist unverändert, und die Zahl der Schreibwerkzeuge bleibt sechs
    - Ein familienspezifischer Test prüft Annotation, Enum, Parameterschemata und die Abwesenheit von `mail_send`, `mail_list_messages`, `mail_read_message`, `mail_search`, `mail_fetch` und `mail_create_draft`
    - `scripts/acceptance_all_tools.py` hat `EXPECTED_TOOLS = 21`, einen Mail-Aufrufblock mit mindestens zwei eigenen SKIP-Sätzen und `mail_browse` in der zweiten Namensliste
    - `README.md` nennt in Zeile 19 und Zeile 30 nicht mehr 20, sondern 21 Werkzeuge, und `grep -rnE "tools=[0-9]+|[0-9]+ tools" README.md` meldet keine andere Zahl als 21
    - `uv run pytest tests/contract/test_tool_surface.py -q` ist grün, inklusive des README-Tabellentests und des Doku-Zählwächters
    - `uv run pytest -q` ist über die ganze Default-Auswahl grün
    - `uv run python scripts/check_tool_budget.py` endet mit Exit-Code 0
  </acceptance_criteria>
  <done>Die Zahl 21 steht überall gleich, die Zahl der Schreibwerkzeuge ist nachweislich nicht gewachsen, und der Abnahmelauf kann ein Konto ohne Mail ehrlich überspringen.</done>
</task>

<task type="auto">
  <name>Task 3: Das erweiterte Schreibverbot, mit Gegenprobe je Nadel und Positivliste</name>
  <files>tests/contract/test_no_destructive_calls.py</files>
  <read_first>
    - tests/contract/test_no_destructive_calls.py (ganze Datei; der Kommentarblock über FORBIDDEN 32 bis 63, FORBIDDEN 64 bis 89, TALK_ROUTES 104 bis 130, ALLOWED_TALK_ROUTES 121 bis 147, die Tables-Ausnahmeform 140 bis 154 und 298 bis 302, _violations 250 bis 270, die drei Talk-Tests 407 bis 450, der Ausnahme-Gegenprobetest 453 bis 473, ALLOWED_MODULE_STATE 199 bis 202 und sein Zähltest 517 bis 522)
    - src/mcp_connector/nextcloud/clients/mail.py (aus Plan 10-02): die vier Pfadformen in genau der f-String-Schreibweise, die die Datei benutzt
    - src/mcp_connector/tools/mail.py (aus Plan 10-04)
    - .planning/phases/10-mail-strikt-lesend-und-die-trifecta-grenze/10-RESEARCH.md, Abschnitt "Das erweiterte Gate (MAIL-01 SC4)" vollständig, inklusive der Nadeltabelle und der Kollisionsprüfung
    - .planning/phases/10-mail-strikt-lesend-und-die-trifecta-grenze/10-PATTERNS.md, Abschnitt "tests/contract/test_no_destructive_calls.py"
  </read_first>
  <action>
    Das Gate um die Mail-Familie erweitern, in den vier Bausteinen, die der Talk-Block schon
    vorführt.

    (1) **Neue Nadeln in `FORBIDDEN`**, jede mit ihrer Begründung im Wert:
    `/message/send` (die eine deklarierte Sende-Route der Mail-App; sie liegt direkt neben der
    Leseroute, beide teilen `/message/`, nur `send` unterscheidet sie);
    `/api/messages` (interne Resource-Route: POST legt einen Entwurf an, PUT ändert, DELETE
    löscht, und alle drei tragen denselben Pfad wie ein Lesen);
    `/api/mailboxes` (interne Resource-Route plus `/sync`, `/clear`, `/read`, `/repair`);
    `/api/accounts` (interne Resource-Route plus `/draft` und `/signature`);
    `/api/drafts`; `/api/outbox` (die zweite Sendemöglichkeit der App, ohne das Wort send im
    Pfad); `/api/thread`; `/api/tags`; `/api/trustedsenders` (Absender-Vertrauen setzen und
    entziehen ist eine Sicherheitsentscheidung des Nutzers).
    Der Kommentarblock über `FORBIDDEN` bekommt einen Absatz nach dem Vorbild der Zeilen 61 bis
    63: zwei **bestehende** Nadeln greifen schon in die Mail-Familie, `/attachment` verbietet die
    Mail-Anhangsroute und `/read` verbietet `/api/mailboxes/{id}/read` ein zweites Mal, und
    deshalb bekommen die zwei keinen neuen Eintrag daneben; eine zusätzliche Nadel würde nur wie
    mehr Sicherheit aussehen.
    Vor dem Schreiben ist die Kollisionsfreiheit zu prüfen: keine der neun Nadeln darf eine
    bestehende Zeile in `clients/tables.py`, `clients/deck.py`, `clients/notes.py`,
    `clients/talk.py` oder `clients/mail.py` treffen. Der Lauf des ganzen Gates ist der Beweis.

    (2) **`MAIL_ROUTES` als Gegenproben-Dict**, eine Zeile je Nadel, in der f-String-Schreibweise,
    die dieses Projekt für einen Pfad benutzt, nach dem Vorbild von `TALK_ROUTES`. Der
    `#:`-Kommentar sagt denselben Satz wie dort: eine Nadel, die nie getroffen hat, ist von keiner
    Nadel nicht zu unterscheiden.

    (3) **`ALLOWED_MAIL_ROUTES` als Positivliste** mit genau den vier Formen, die
    `clients/mail.py` wirklich baut, wörtlich in der Schreibweise des Clients. Der Kommentar sagt,
    warum sie für diese Familie wichtiger ist als für Talk: hier teilen eine Leseroute und eine
    Schreibroute das Pfadpräfix `/message/`, und eine Nadel kann nicht zwischen Verben
    unterscheiden, weil `POST` und `PUT` in diesem Projekt erlaubte Verben sind.

    (4) **Die vier Tests**, nach dem Vorbild der Zeilen 407 bis 450:
    - Ein parametrisierter Test je Mail-Nadel, der über den **echten** Prüfpfad `_violations`
      läuft: `clients/mail.py` ist ohne die Zeile sauber, und mit der angehängten Zeile wird die
      Nadel gemeldet. Das ist die Gegenprobe, die MAIL-01 SC4 wörtlich verlangt, und für
      `/message/send` lautet die Zeile eine `ocs_post`-Zeile auf `/apps/mail/message/send`.
    - Ein Test, der behauptet, dass jede Mail-Nadel in `FORBIDDEN` eine Gegenprobe-Zeile hat und
      dass die Zahl stimmt (neun), mit demselben Satz wie das Talk-Analog: eine Nadel ohne
      Gegenprobe ist eine Behauptung, und diese Datei macht keine Behauptungen.
    - Ein Test, der behauptet, dass die vier erlaubten Mail-Leseformen das Gate passieren.
    - Der zusätzliche **Nur-GET-Test** über `clients/mail.py`: die Datei enthält kein `ocs_post`,
      kein `.post(`, kein `.put(`, kein `.patch(`, kein `.delete(` und kein `client.request`. Der
      Docstring sagt, dass diese Familie die erste ohne jeden Schreibpfad ist und dass "es gibt
      nur GETs in dieser Datei" eine prüfbare, wörtliche Aussage ist; das Muster existiert schon
      für `contacts.py` (D-07). Derselbe Test in seiner kurzen Form gilt für `tools/mail.py`.
    Trifft eine Mail-Nadel doch eine erlaubte Leseroute, ist die Ausnahmeform vorgegeben und eng:
    exakte Literale, eine Datei, plus ein Gegenprobe-Test, nach dem Vorbild von
    `FILES_WITH_THE_TABLES_READS` und `TABLES_READ_FORMS`.

    `ALLOWED_MODULE_STATE` bleibt bei zwei Einträgen: der Nachfüllpfad von Plan 10-02 schreibt in
    dieselbe Cache-Zeile, und dieser Plan legt keinen dritten Zustand an.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run pyright &amp;&amp; uv run pytest tests/contract/test_no_destructive_calls.py -q</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run pytest -q</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; python - &lt;&lt;'PY'
import importlib.util, sys
from pathlib import Path
spec = importlib.util.spec_from_file_location("gate", Path("tests/contract/test_no_destructive_calls.py"))
gate = importlib.util.module_from_spec(spec)
sys.modules["gate"] = gate
spec.loader.exec_module(gate)
needles = ["/message/send", "/api/messages", "/api/mailboxes", "/api/accounts", "/api/drafts", "/api/outbox", "/api/thread", "/api/tags", "/api/trustedsenders"]
for needle in needles:
    assert needle in gate.FORBIDDEN, needle
    assert needle in gate.MAIL_ROUTES, f"no counter proof line for {needle}"
assert len(gate.MAIL_ROUTES) == 9, len(gate.MAIL_ROUTES)
assert len(gate.ALLOWED_MAIL_ROUTES) == 4, len(gate.ALLOWED_MAIL_ROUTES)
assert len(gate.ALLOWED_MODULE_STATE) == 2, len(gate.ALLOWED_MODULE_STATE)
rel = "nextcloud/clients/mail.py"
real = gate._code_lines(gate.SRC / rel)
assert gate._violations(rel, real) == [], "the real mail client must be clean"
line = '    await ocs.ocs_post(client, creds, "/apps/mail/message/send", body)'
found = gate._violations(rel, [*real, (len(real) + 1, line)])
assert any("/message/send" in f for f in found), found
for allowed in gate.ALLOWED_MAIL_ROUTES:
    assert gate._violations(rel, [(1, allowed)]) == [], allowed
print("mail gate armed with counter proofs")
PY</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; python - &lt;&lt;'PY'
from pathlib import Path
for module in ("src/mcp_connector/nextcloud/clients/mail.py", "src/mcp_connector/tools/mail.py"):
    text = Path(module).read_text(encoding="utf-8")
    for call in ("ocs_post", ".post(", ".put(", ".patch(", ".delete(", "client.request"):
        assert call not in text, f"{call} has no place in {module}"
print("mail modules are read only in source")
PY</automated>
  </verify>
  <acceptance_criteria>
    - `FORBIDDEN` enthält die neun neuen Mail-Nadeln, jede mit einer Begründung im Wert
    - `MAIL_ROUTES` hat genau neun Gegenprobe-Zeilen, eine je Nadel, in der f-String-Schreibweise des Projekts
    - Ein parametrisierter Test läuft je Nadel über `_violations` und behauptet beide Richtungen: die echte Datei ist sauber, die angehängte Zeile wird gemeldet
    - Für `/message/send` ist die Gegenprobe eine `ocs_post`-Zeile auf `/apps/mail/message/send`, und sie wird gemeldet
    - `ALLOWED_MAIL_ROUTES` hat genau vier Einträge, und alle vier passieren das Gate
    - Ein Test behauptet, dass `clients/mail.py` kein `ocs_post`, `.post(`, `.put(`, `.patch(`, `.delete(` und `client.request` enthält; derselbe Test in kurzer Form gilt für `tools/mail.py`
    - Der Kommentarblock über `FORBIDDEN` nennt `/attachment` und `/read` als bestehende Nadeln, die schon in die Mail-Familie greifen, und sagt, warum sie keinen zweiten Eintrag bekommen
    - `ALLOWED_MODULE_STATE` hat weiterhin genau zwei Einträge
    - `uv run pytest tests/contract/test_no_destructive_calls.py -q` und `uv run pytest -q` sind grün
  </acceptance_criteria>
  <done>Es gibt keinen Weg, über diese App eine Mail zu senden, als Entwurf anzulegen, zu verschieben, zu markieren oder zu löschen, und jede der neun Nadeln hat bewiesen, dass sie greift, obwohl die Mail-App selbst eine Sende-Route anbietet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Werkzeugoberfläche zu Client | `tools/list` ist ein Vertrag, den Clients zwischenspeichern; jedes Byte kostet in jeder Sitzung |
| Nächster Commit zu Schreibverbot | Ein Gate ist eine Aussage über künftigen Code, ein Docstring nur über den heutigen |
| Mail-App zu diesem Server | Die Mail-App bietet 59 schreibende Routen an, darunter eine deklarierte Sende-Route direkt neben der benutzten Leseroute |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-37 | Elevation of Privilege | `POST /apps/mail/message/send` in einem künftigen Commit | mitigate | Nadel `/message/send` in `FORBIDDEN` plus eine Gegenprobe, die durch den echten Prüfpfad `_violations` läuft; das ist die Gegenprobe, die MAIL-01 SC4 wörtlich verlangt |
| T-10-38 | Elevation of Privilege | Rückfall auf die internen Resource-Routen | mitigate | Acht weitere Nadeln (`/api/messages`, `/api/mailboxes`, `/api/accounts`, `/api/drafts`, `/api/outbox`, `/api/thread`, `/api/tags`, `/api/trustedsenders`), jede mit Gegenprobe, plus die Positivliste `ALLOWED_MAIL_ROUTES` als die Hälfte des Beweises, die eine Nadel nicht liefern kann, weil POST und PUT hier erlaubte Verben sind |
| T-10-39 | Tampering | Ein Schreibaufruf, den keine Nadel kennt | mitigate | Der Nur-GET-Test über `clients/mail.py` und `tools/mail.py`: die Aussage "es gibt nur GETs in dieser Datei" ist wörtlich prüfbar, unabhängig von der Nadelliste (Muster von `contacts.py`, D-07) |
| T-10-40 | Denial of Service | Kontextkosten der Werkzeugoberfläche | mitigate | Budget-Gate auf Messung plus 15 Prozent mit datierter Messzeile, `MAX_TOOL_BYTES` unverändert 1400 als eigentlicher Wächter; die Anhebung ist als Zwischenstand markiert, damit Phase 11 nicht eine schon grosszügige Zahl noch einmal grosszügig anhebt (Falle 14) |
| T-10-41 | Repudiation | Zahlen, die an mehreren Orten stehen | mitigate | Alle fünf Pflegeorte im selben Commit; die älteren Messzeilen bleiben lesbar, damit eine Regression zuordenbar bleibt |
| T-10-42 | Spoofing | Eine credential-abhängige `tools/list` | accept | `mail_browse` wird bedingungslos gelistet, auch ohne Mail-App; die ehrliche Antwort auf eine fehlende App ist der Satz, den das Werkzeug zurückgibt (SRV-04). Eine gefilterte Liste wäre nicht cachebar und würde das Budget-Gate wertlos machen |
| T-10-SC | Tampering | Paketinstallation | mitigate | Keine neue Sprachabhängigkeit: `pyproject.toml` und `uv.lock` bleiben unangetastet, es gibt kein `uv add`, kein `pip install` und kein `npm install` in diesem Plan |
</threat_model>

<verification>
1. `uv run pytest -q` (Default-Auswahl) und `uv run pytest tests/contract -q` sind grün.
2. `uv run ruff check .`, `uv run ruff format --check .`, `uv run pyright` und `uv run vulture src/mcp_connector vulture_whitelist.py` sind grün.
3. `uv run python scripts/check_tool_budget.py` endet mit Exit-Code 0, meldet 21 Werkzeuge und `mail_browse` unter 1400 Bytes.
4. `ALLOWED_MODULE_STATE` hat genau zwei Einträge, und `CREATE_TOOLS` ist unverändert.
5. `git diff --stat` zeigt keine Änderung an `pyproject.toml`, `uv.lock` und `appinfo/info.xml`, und `git diff -- README.md` besteht ausschliesslich aus den zwei Zahlkorrekturen.
</verification>

<success_criteria>
- `tools/list` antwortet mit 21 Werkzeugen, `mail_browse` ist schema-diätet, hat die Nur-Lesen-Annotation und ein Enum mit drei Ebenen.
- Die Zahl 21 steht identisch in Registry, Contract-Test und Abnahme-Skript.
- Das Budget-Gate steht auf einer neuen, datierten Messung und ist als Zwischenstand für TOOL-15 markiert.
- Es existiert kein Weg zum Senden, Entwerfen, Verschieben, Markieren oder Löschen einer Mail, und das Gate belegt es mit einer Gegenprobe je Nadel und einer Positivliste der vier erlaubten Formen (MAIL-01 SC4).
</success_criteria>

<output>
Create `.planning/phases/10-mail-strikt-lesend-und-die-trifecta-grenze/10-06-SUMMARY.md` when done.
Die Zusammenfassung nennt die gemessene Byte-Zahl von `tools/list` bei 21 Werkzeugen, die Kosten
von `mail_browse` allein, den neuen Wert von `BUDGET_BYTES` samt Rechnung, die endgültigen
Feldbeschreibungen von `mail_browse` (weil Plan 10-07 die README-Tabellenzeile und die
Store-Beschreibung daraus baut) und die Liste der neun Mail-Nadeln, weil die Zahl in einem Test
eingefroren ist.
</output>
