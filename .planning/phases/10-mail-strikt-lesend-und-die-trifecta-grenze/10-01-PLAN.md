---
phase: 10-mail-strikt-lesend-und-die-trifecta-grenze
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - compose.exapp.yml
  - scripts/bootstrap_exapp.sh
  - docs/spike-mail.md
autonomous: true
requirements: [MAIL-01, MAIL-02, MAIL-03]

must_haves:
  truths:
    - "Die HaRP-Topologie hat einen erreichbaren IMAP- und SMTP-Server, ohne einen einzigen veröffentlichten Port dazuzugewinnen"
    - "Das Mail-Konto von alice zeigt auf den GreenMail-Dienst und nicht mehr auf imap.invalid, und ein zweiter Bootstrap-Lauf richtet dieselbe Lage wieder ein"
    - "Die Postfachliste und die Nachrichtenliste antworten mit echten Daten statt mit 500 und 403, und die Feldwerte von specialRole, previewText, unread und dateInt sind gemessen statt erschlossen"
    - "Die Filtergrammatik is:unread, from:, subject:, start: und tags: ist an echten Nachrichten gemessen, inklusive der stillen Verwerfung eines Tippfehlers"
    - "Die Byte-Kappe des Volltexts ist eine Zahl mit einer Messung daneben und keine Schätzung"
    - "docs/spike-mail.md behauptet nicht mehr, die drei Listen-Wege seien nur als SCOPE_IGNORE-Interna erreichbar"
  artifacts:
    - path: "compose.exapp.yml"
      provides: "GreenMail als vierter Dienst im Netz nc-mcp-exapp-net, ohne veröffentlichten Port"
      contains: "greenmail"
    - path: "scripts/bootstrap_exapp.sh"
      provides: "Mail-Konto auf dem GreenMail-Host, Einlieferung der Testmails, Zwangssynchronisation"
      contains: "greenmail"
    - path: "docs/spike-mail.md"
      provides: "Stufe-2-Protokoll mit den vier gemessenen Annahmen und die Korrektur K1"
      contains: "Stufe 2"
      min_lines: 200
  key_links:
    - from: "scripts/bootstrap_exapp.sh"
      to: "greenmail:3143"
      via: "occ mail:account:create-imap mit dem Dienstnamen aus dem Compose-Netz statt imap.invalid"
      pattern: "greenmail"
    - from: "docs/spike-mail.md"
      to: "GET /ocs/v2.php/apps/mail/ocs/mailboxes"
      via: "Das Stufe-2-Protokoll nennt die vier deklarierten OCS-Routen als die benutzten Wege"
      pattern: "ocs/mailboxes"
---

<objective>
Der Topologie einen echten IMAP-Server geben und aus den vier offenen Annahmen dieser Phase vier
Messwerte machen, bevor irgendein Werkzeug auf ihnen aufbaut.

Purpose: Die Erreichbarkeit von Mail ist seit Phase 8 bewiesen, die Antwortformen sind es nicht.
`specialRole`, `previewText`, das Filterverhalten und die realistische Länge einer gewandelten
HTML-Mail (A1 bis A4 in `10-RESEARCH.md`) sind ohne echte IMAP-Daten nicht entscheidbar, und
MAIL-03 verlangt wörtlich "dokumentiert wie getestet". Ohne diesen Plan sind die Kappenzahl in
Plan 10-04, die Feldprojektion in Plan 10-04 und die Byte-Kappe in Plan 10-05 Schätzungen, die
erst im Live-Lauf von Plan 10-08 auffallen, also nach dem Bau. GreenMail ist billig: ein
Compose-Dienst, ein Konto, ein paar Testmails, ein Sync-Kommando, und die Vorlage dafür steht
seit Phase 8 wörtlich fertig in `docs/spike-mail.md`.

Output: GreenMail in `compose.exapp.yml`, das Mail-Konto von alice auf diesem Dienst plus die
Testmails in `scripts/bootstrap_exapp.sh`, und ein Stufe-2-Abschnitt in `docs/spike-mail.md`, der
die vier Annahmen mit Messwerten beantwortet und die falsche Prämisse des Dokuments (Korrektur
K1) richtigstellt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-mail-strikt-lesend-und-die-trifecta-grenze/10-RESEARCH.md
@.planning/phases/10-mail-strikt-lesend-und-die-trifecta-grenze/10-PATTERNS.md

Die drei geänderten Dateien:
@compose.exapp.yml
@scripts/bootstrap_exapp.sh
@docs/spike-mail.md

Der Rahmen eines Integrationslaufs gegen diese Topologie:
@tests/integration/test_exapp_mail_reach.py

<interfaces>
Aus scripts/bootstrap_exapp.sh (heute im Repo gemessen):
  ALICE_IMAP_PASSWORD="${NC_EXAPP_ALICE_IMAP_PASSWORD:-alice-spike-imap-pw}"   Zeile 102
  ensure_mail_account() { local uid name email password ... }                  Zeile 299
    Existenzprüfung: occ mail:account:export "$uid" | grep "${email}"          Zeile 304
    Anlage:          occ mail:account:create-imap "$uid" "$name" "$email" \
                       imap.invalid 143 none "$uid" "$password" ...            Zeile 310
  ensure_app mail                                                             Zeile 954
  ensure_mail_account alice "Alice Spike" alice@example.test "${ALICE_IMAP_PASSWORD}"   Zeile 969

Aus compose.exapp.yml (heute im Repo gemessen):
  name: nc-mcp-exapp
  services: caddy, nextcloud, appapi-harp, registry
  networks.default.name: nc-mcp-exapp-net, subnet 172.29.42.0/24, ip_range 172.29.42.128/25
  Der Dienst `registry` ist das Vorbild für einen Dienst ohne veröffentlichten Port.

Die vier deklarierten OCS-Leseformen der Mail-App (Mail 5.11.1, gemessen):
  GET /ocs/v2.php/apps/mail/account/list
  GET /ocs/v2.php/apps/mail/ocs/mailboxes?accountId=<int>
  GET /ocs/v2.php/apps/mail/ocs/mailboxes/{mailboxId}/messages?limit=..&view=singleton[&filter=..][&cursor=..]
  GET /ocs/v2.php/apps/mail/message/{id}

Der Aufrufweg der Messung, wie in der Recherche benutzt: reine AppAPI-Impersonation, also die
Header AUTHORIZATION-APP-API (base64 von "<user>:<APP_SECRET>") und EX-APP-ID plus EX-APP-VERSION
aus .env.exapp, gegen die öffentliche Nextcloud-URL aus .env.exapp.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GreenMail als vierter Dienst und das Mail-Konto auf diesem Dienst</name>
  <files>compose.exapp.yml, scripts/bootstrap_exapp.sh</files>
  <read_first>
    - compose.exapp.yml (ganze Datei; besonders der Dienst `registry` als Vorbild für einen Dienst ohne veröffentlichten Port und der `networks`-Block am Ende)
    - scripts/bootstrap_exapp.sh (ganze Datei; besonders ALICE_IMAP_PASSWORD Zeile 99 bis 102, ensure_mail_account Zeile 287 bis 318, der Aufrufblock Zeile 950 bis 995)
    - docs/spike-mail.md, Abschnitt "Eskalationsregel und Stufe 2" (Zeile 119 bis 148): die YAML-Vorlage und die vier Schritte stehen dort wörtlich
    - .planning/phases/10-mail-strikt-lesend-und-die-trifecta-grenze/10-RESEARCH.md, Abschnitt "Environment Availability" und "Offene Fragen" Punkt 1
  </read_first>
  <action>
    In `compose.exapp.yml` den Dienst `greenmail` aufnehmen, wörtlich nach der Vorlage aus
    `docs/spike-mail.md` Zeile 125 bis 136: Image `greenmail/standalone:2.1.12`, Containername
    `nc-mcp-exapp-greenmail`, `GREENMAIL_OPTS` mit `-Dgreenmail.setup.test.all`,
    `-Dgreenmail.hostname=0.0.0.0`, `-Dgreenmail.auth.disabled` und
    `-Dgreenmail.users=alice:<ALICE_IMAP_PASSWORD>@example.test`. Kein `ports`-Block, aus
    demselben Grund wie beim Dienst `registry`: Nextcloud erreicht GreenMail über das Netz
    `nc-mcp-exapp-net`, ein gebundener Port wäre Reichweite ohne Gegenwert (T-08-05). Der
    Kommentar über dem Dienst sagt, warum er existiert (die Feldformen von Mail sind ohne echte
    IMAP-Daten nicht messbar) und dass er nur zu dieser Wegwerf-Topologie gehört, nie zu einer
    Installation. Das Nutzerpasswort kommt aus der Compose-Variable, die dieselbe Vorgabe hat wie
    `ALICE_IMAP_PASSWORD` im Bootstrap (`NC_EXAPP_ALICE_IMAP_PASSWORD`, Vorgabe
    `alice-spike-imap-pw`), damit die zwei Stellen nicht auseinanderlaufen können; ein
    Healthcheck ist erlaubt, aber kein Muss, und wenn er kommt, prüft er den IMAP-Port 3143.

    In `scripts/bootstrap_exapp.sh` die drei Anpassungen:

    (1) `ensure_mail_account` bekommt Host und Port als Parameter statt der eingebauten Literale
    `imap.invalid 143`. Der bestehende Kommentarblock über der Funktion (Zeile 287 bis 298), der
    erklärt, warum `imap.invalid` gewählt war, wird ersetzt durch die neue Begründung: die
    Topologie hat jetzt einen erreichbaren Server, weil die Feldformen von `specialRole`,
    `previewText` und der Filterlauf ohne echte Daten nicht messbar sind, und `imapSslMode` ist
    `none`, weil GreenMail im Docker-Netz kein Zertifikat hat.

    (2) Die Existenzprüfung wird schärfer. Sie prüft heute nur die Mailadresse, also würde ein
    Konto, das noch auf `imap.invalid` zeigt, als vorhanden durchgehen und der Bootstrap wäre
    grün, ohne dass ein einziger IMAP-Abruf möglich ist. Neu: der Export wird zusätzlich gegen den
    Zielhost geprüft. Passt die Adresse, aber nicht der Host, wird das Konto verworfen und neu
    angelegt. Welches `occ`-Kommando ein Mail-Konto entfernt, ist vor dem Schreiben mit
    `occ list mail` beziehungsweise `occ mail:account:delete --help` festzustellen und nicht zu
    erraten; existiert kein solches Kommando, ist der Ausweg eine laute Fehlermeldung mit dem
    Satz "delete the mail account of alice in the Mail app once, then run this script again",
    also ein benannter Handgriff und kein stiller Weiterlauf. Die gewählte Auflösung gehört als
    Kommentar an die Stelle.

    (3) Der Aufrufblock am Dateiende ruft `ensure_mail_account` mit Host `greenmail` und Port
    `3143`. `ensure_app mail` bleibt, wo es steht.

    Der Dienstname `greenmail` ist der Hostname im Compose-Netz und damit derselbe String in
    beiden Dateien; er wird an keiner Stelle aus einer Variablen zusammengesetzt, damit ein Grep
    ihn findet.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; docker compose -p nc-mcp-exapp -f compose.exapp.yml config --quiet &amp;&amp; bash -n scripts/bootstrap_exapp.sh</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; docker compose -p nc-mcp-exapp -f compose.exapp.yml config | grep -v "^ *#" | grep -q "greenmail/standalone" &amp;&amp; test "$(docker compose -p nc-mcp-exapp -f compose.exapp.yml config --format json | python -c "import json,sys; d=json.load(sys.stdin); print(len(d['services']['greenmail'].get('ports') or []))")" = "0" &amp;&amp; echo "greenmail service ok, no published port"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; grep -v "^ *#" scripts/bootstrap_exapp.sh | grep -q "greenmail" &amp;&amp; grep -v "^ *#" scripts/bootstrap_exapp.sh | grep -q "3143" &amp;&amp; ! grep -v "^ *#" scripts/bootstrap_exapp.sh | grep -q "imap.invalid" &amp;&amp; echo "bootstrap points at greenmail"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; export HP_SHARED_KEY="$(cat .env.exapp 2>/dev/null | grep -m1 '^HP_SHARED_KEY=' | cut -d= -f2)" &amp;&amp; docker compose -p nc-mcp-exapp -f compose.exapp.yml up -d --wait &amp;&amp; bash scripts/bootstrap_exapp.sh &amp;&amp; MSYS_NO_PATHCONV=1 docker compose -p nc-mcp-exapp -f compose.exapp.yml exec -T --user www-data nextcloud php /var/www/html/occ mail:account:export alice</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose -p nc-mcp-exapp -f compose.exapp.yml config --quiet` endet mit Exit-Code 0
    - Der Dienst `greenmail` existiert in der aufgelösten Compose-Konfiguration und hat eine leere `ports`-Liste
    - `scripts/bootstrap_exapp.sh` nennt in nicht kommentierten Zeilen `greenmail` und `3143` und nirgends mehr `imap.invalid`
    - `bash -n scripts/bootstrap_exapp.sh` ist grün
    - Ein Bootstrap-Lauf auf einer Topologie, deren alices-Konto noch auf `imap.invalid` zeigt, endet entweder mit einem neu angelegten Konto auf `greenmail` oder mit einer lauten Fehlermeldung, die den Handgriff nennt; ein stilles "exists" auf dem alten Host ist ausgeschlossen
    - `occ mail:account:export alice` nennt den Host `greenmail` und den Port `3143`
    - Ein zweiter Bootstrap-Lauf ist idempotent: er endet mit Exit-Code 0 und meldet das Konto als vorhanden
  </acceptance_criteria>
  <done>Die Topologie hat einen erreichbaren IMAP- und SMTP-Server, alices Konto zeigt darauf, kein Port ist dazugekommen, und ein zweiter Bootstrap-Lauf richtet dieselbe Lage wieder ein.</done>
</task>

<task type="auto">
  <name>Task 2: Fünf Testmails einliefern und die vier Annahmen messen</name>
  <files>scripts/bootstrap_exapp.sh</files>
  <read_first>
    - scripts/bootstrap_exapp.sh (nach Task 1; besonders ensure_mail_account und der Aufrufblock am Dateiende)
    - docs/spike-mail.md, Abschnitt "Eskalationsregel und Stufe 2" (Zeile 119 bis 148) und "Reproduktion" (Zeile 150 bis 161)
    - tests/integration/test_exapp_mail_reach.py (ganze Datei; der Messrahmen, die Kappung auf 120 Zeichen und der Satz über die gemessene Frage)
    - .planning/phases/10-mail-strikt-lesend-und-die-trifecta-grenze/10-RESEARCH.md, Abschnitte "Ebene mailboxes", "Ebene messages", "Volltext", "Die Filtergrammatik", "Assumptions Log" (A1 bis A4), "Offene Fragen" Punkt 3
  </read_first>
  <action>
    In `scripts/bootstrap_exapp.sh` eine Funktion ergänzen, die die Testmails über SMTP an
    GreenMail einliefert, und sie im Aufrufblock nach `ensure_mail_account` rufen. Die Einlieferung
    läuft aus einem Wegwerf-Container im Netz `nc-mcp-exapp-net`, weil GreenMail keinen
    veröffentlichten Port hat: `docker run --rm --network nc-mcp-exapp-net python:3.13-alpine
    python -c ...` mit `smtplib.SMTP("greenmail", 3025)`. Die Funktion ist idempotent zu machen,
    indem sie vorher die Nachrichtenzahl des Kontos prüft oder eine Marker-Datei im
    Nextcloud-Volume setzt; welcher der beiden Wege genommen wird, gehört als Kommentar dazu, und
    ein zweiter Lauf darf die Postfächer nicht verdoppeln.

    Fünf Nachrichten an `alice@example.test`, jede mit einem Zweck, der genau eine Annahme
    messbar macht:
    1. Eine reine Textmail mit echten Umlauten im Betreff und im Körper (`Grüße`, `Maße`), ohne
       jedes HTML. Sie belegt Korrektur K2: der `body` der Volltextroute kommt trotzdem als HTML
       an.
    2. Eine HTML-Mail in realistischer Newsletter-Grösse (Grössenordnung 30 bis 60 KB HTML) mit
       `<style>`- und `<script>`-Block, verschachtelten Tabellen, `<p>`, `<div>`, `<br>`, `<li>`
       und HTML-Entities. Sie liefert die Zahl für A3, also die Länge nach der Wandlung.
    3. Eine sehr grosse HTML-Mail (Grössenordnung 400 KB) als Obergrenzfall.
    4. Eine Mail mit dem Betreff-Wort `Rechnung` und einer zweiten mit `Rechnung Mai`, für
       `subject:` und für die Leerzeichenregel der Filtergrammatik.
    5. Eine Mail ohne Textkörper, nur mit einem kleinen Anhang, für den Fall "Mail ohne Body".
    Alle Absender liegen unter `example.test`, alle Inhalte sind erfunden. Genau deshalb darf das
    Messprotokoll dieses Plans Feldwerte drucken, anders als die Messung aus Phase 8, deren
    Kontoantwort eine echte Adresse trug (T-08-01); der Unterschied gehört als Satz in den
    Kommentar der Funktion.

    Danach `occ mail:account:sync <id> -f` im Bootstrap rufen, mit der Konto-Id aus
    `mail:account:export`, damit die Postfächer und Nachrichten in der Datenbank der App stehen.
    Ohne diesen Schritt antwortet `listMessages` mit `MailboxNotCachedException`, also einem 500
    (K6), und die Messung würde den Fehlerfall statt der Feldformen sehen.

    Anschliessend die Messung selbst durchführen, ad hoc und ohne neue Datei im Repo: mit
    `uv run python -c ...` unter reiner AppAPI-Impersonation die vier Leseformen abrufen und
    protokollieren. Zu messen und wörtlich mitzuschreiben sind:
    - `account/list`: die Feldnamen und die Werte für alice.
    - `ocs/mailboxes?accountId=<id>`: für jedes Postfach `databaseId`, `name`, `displayName`,
      `specialRole` samt Typ (Zahl oder String), `specialUse`, `unread`, `delimiter`. Das
      beantwortet A1: die tatsächliche Wertemenge von `specialRole` statt der aus Horde
      erschlossenen.
    - `ocs/mailboxes/{id}/messages?limit=5&view=singleton`: pro Nachricht ob `previewText`
      gesetzt ist und wie lang, plus `dateInt`, `flags.seen`, `subject`. Das beantwortet A2. Dazu
      der Gegenversuch **ohne** `limit`, der genau eine Nachricht liefern muss (K3), und der
      Gegenversuch ohne `view`, dessen Ergebnis als Thread-Ansicht zu erkennen ist (Falle 12).
    - `message/{databaseId}` für die Textmail, die Newsletter-Mail, die grosse Mail und die Mail
      ohne Body: Länge von `body` in Bytes, `hasHtmlBody`, das Vorhandensein von
      `isSenderTrusted`, `hasDkimSignature`, `dkimValid`, `phishingDetails`, `smime`, und der
      Statuscode. Das beantwortet A3.
    - Die Filterläufe für A4, jeder mit der Trefferzahl daneben: `is:unread`, `from:alice`,
      `subject:Rechnung`, `subject:Rechnung%20Mai`, `subject:Rechnung Mai` (die stille
      Verwerfung), `start:<unix>`, `start:2026-08-01` (der wirkungslose ISO-Wert, K4),
      `tags:<label>` und ein bewusster Tippfehler wie `is:ungelesen`, der die ungefilterte Liste
      liefern muss.
    - Zusätzlich der `\Seen`-Nachweis in seiner Rohform: `flags.seen` einer Nachricht vor und
      nach einem `message/{id}`-Abruf. Der ausgebaute Nachweis gehört zu Plan 10-08; hier zählt
      die Zahl, die dort erwartet wird.
    Aus der gemessenen Länge der gewandelten Newsletter-Mail wird die Byte-Kappe des Volltexts
    entschieden: Startwert der Grössenordnung 16 KiB, und die gewählte Zahl trägt die Messung als
    Begründung neben sich. Sie wird hier **nur entschieden und aufgeschrieben**, gesetzt wird sie
    in Plan 10-05.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; bash -n scripts/bootstrap_exapp.sh &amp;&amp; grep -v "^ *#" scripts/bootstrap_exapp.sh | grep -q "3025" &amp;&amp; grep -v "^ *#" scripts/bootstrap_exapp.sh | grep -q "mail:account:sync"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; export HP_SHARED_KEY="$(grep -m1 '^HP_SHARED_KEY=' .env.exapp | cut -d= -f2)" &amp;&amp; docker compose -p nc-mcp-exapp -f compose.exapp.yml up -d --wait &amp;&amp; bash scripts/bootstrap_exapp.sh &amp;&amp; bash scripts/bootstrap_exapp.sh</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; set -a &amp;&amp; . ./.env.exapp &amp;&amp; set +a &amp;&amp; uv run python -c "
import base64, json, os, urllib.request
base = os.environ['NC_EXAPP_PUBLIC_URL'].rstrip('/') if os.environ.get('NC_EXAPP_PUBLIC_URL') else 'http://127.0.0.1:8081'
auth = base64.b64encode(f\"alice:{os.environ['APP_SECRET']}\".encode()).decode()
def get(path):
    req = urllib.request.Request(base + path, headers={'AUTHORIZATION-APP-API': auth, 'EX-APP-ID': os.environ['APP_ID'], 'EX-APP-VERSION': os.environ['APP_VERSION'], 'OCS-APIRequest': 'true', 'Accept': 'application/json'})
    with urllib.request.urlopen(req) as resp:
        return resp.status, json.loads(resp.read())
status, payload = get('/ocs/v2.php/apps/mail/account/list')
accounts = payload['ocs']['data']
assert status == 200 and accounts, (status, accounts)
account = accounts[0]['id']
status, payload = get(f'/ocs/v2.php/apps/mail/ocs/mailboxes?accountId={account}')
boxes = payload['ocs']['data']
assert status == 200 and boxes, (status, payload)
print('mailboxes', [(b.get('databaseId'), b.get('name'), b.get('specialRole'), b.get('unread')) for b in boxes])
inbox = next(b['databaseId'] for b in boxes if str(b.get('specialRole') or '').lower() == 'inbox')
status, payload = get(f'/ocs/v2.php/apps/mail/ocs/mailboxes/{inbox}/messages?limit=10&amp;view=singleton')
messages = payload['ocs']['data']
assert status == 200 and len(messages) &gt;= 5, (status, len(messages))
print('previewText set on', sum(1 for m in messages if m.get('previewText')), 'of', len(messages))
status, one = get(f'/ocs/v2.php/apps/mail/ocs/mailboxes/{inbox}/messages?view=singleton')
assert len(one['ocs']['data']) == 1, 'K3: a limit without a value must answer exactly one message'
print('K3 confirmed')
"</automated>
  </verify>
  <acceptance_criteria>
    - Ein Bootstrap-Lauf liefert fünf Nachrichten ein und synchronisiert das Konto; ein zweiter Lauf verdoppelt nichts (die gemessene Nachrichtenzahl bleibt gleich)
    - `ocs/mailboxes?accountId=<id>` antwortet 200 mit mindestens einem Postfach, und mindestens ein Postfach trägt `specialRole` mit dem Wert `inbox`
    - `ocs/mailboxes/{id}/messages?limit=10&view=singleton` antwortet 200 mit mindestens fünf Nachrichten
    - Ein Aufruf derselben Route **ohne** `limit` antwortet mit genau einer Nachricht (K3 an echten Daten belegt)
    - Für jede der fünf Testmails ist gemessen und notiert, ob `previewText` gesetzt ist und wie lang es ist (A2)
    - Für die Newsletter-Mail, die grosse Mail, die Textmail und die Mail ohne Body ist die Byte-Länge von `body` und der Statuscode notiert (A3), und die gewählte Byte-Kappe des Volltexts steht mit dieser Messung als Begründung daneben
    - Für jeden der zehn Filterläufe steht die Trefferzahl im Protokoll, inklusive des Belegs, dass `is:ungelesen` und `subject:Rechnung Mai` still verwerfen (A4, Falle 3)
    - Der Wert von `flags.seen` einer Nachricht ist vor und nach einem `message/{id}`-Abruf notiert
    - `bash -n scripts/bootstrap_exapp.sh` ist grün, und die Einlieferungsfunktion nennt Port 3025 und `mail:account:sync`
  </acceptance_criteria>
  <done>Die vier Annahmen A1 bis A4 sind Messwerte, die Byte-Kappe des Volltexts ist entschieden statt geschätzt, und die Postfächer der Topologie tragen Daten, gegen die Plan 10-08 später prüfen kann.</done>
</task>

<task type="auto">
  <name>Task 3: Das Stufe-2-Protokoll und die Korrektur K1 in docs/spike-mail.md</name>
  <files>docs/spike-mail.md</files>
  <read_first>
    - docs/spike-mail.md (ganze Datei; besonders "Replaceability" Zeile 101 bis 117, "Eskalationsregel und Stufe 2" Zeile 119 bis 148, "Reproduktion" Zeile 150 bis 161, "Was diese Messung nicht beweist" Zeile 163 bis 177)
    - .planning/phases/10-mail-strikt-lesend-und-die-trifecta-grenze/10-RESEARCH.md, Abschnitte "K1", "K5", "Stand der Technik" (die drei veralteten Aussagen) und "Das Live-Protokoll dieser Recherche"
    - Die Messwerte aus Task 2
  </read_first>
  <action>
    `docs/spike-mail.md` in drei Punkten in Ordnung bringen, ohne die bestehende Stufe-1-Tabelle
    umzuschreiben: sie ist ein Protokoll eines Laufs und bleibt, wie sie aufgeschrieben wurde.

    (1) Der Abschnitt "Replaceability" bekommt die Korrektur K1 als eigenen, vorangestellten
    Absatz. Er sagt: Mail 5.11.1 liefert in der mit der App ausgelieferten `openapi.json` sieben
    deklarierte OCS-Routen, davon vier lesende, und diese vier deckten alle drei Leseebenen plus
    den Volltext ab. Die drei `SCOPE_IGNORE`-Routen aus der Stufe-1-Messung werden deshalb
    **nicht** benutzt. Die vier benutzten Formen stehen wörtlich als Liste, und dazu der Grund,
    warum ein Rückfall auf die internen Routen eine Verschlechterung wäre: `/api/messages` ist
    eine Resource-Route, POST legt an, PUT ändert, DELETE löscht, alles auf demselben Pfad, den
    ein Lesen benutzt; ein pfadbasiertes Schreibverbot ist dort nicht mehr formulierbar. Der
    bestehende Satz, der die OCS-Volltextroute als "die einzige der vier" mit deklarierter Route
    bezeichnet, wird als für die vier **gemessenen Wege** richtig und für die Familie falsch
    gekennzeichnet, nicht gelöscht.

    (2) Der Abschnitt "Eskalationsregel und Stufe 2" bekommt das Protokoll von Task 2. Der
    bestehende Satz "Alle vier Messzeilen sind eindeutig, deshalb bleibt compose.exapp.yml
    unverändert" wird durch die neue Lage ersetzt: Stufe 2 ist ausgeführt worden, und zwar nicht
    weil Stufe 1 unklar war, sondern weil Phase 10 die Feldformen braucht und MAIL-03
    "dokumentiert wie getestet" verlangt. Darunter das Protokoll mit Datum: die Wertemenge von
    `specialRole`, das Füllverhalten und die Länge von `previewText`, die Byte-Längen der
    gewandelten Bodies samt der gewählten Kappe, die zehn Filterläufe mit Trefferzahlen und der
    `\Seen`-Wert vor und nach dem Volltextabruf. Jede Zeile ist eine Messung mit Datum, keine
    Empfehlung. Feldwerte dürfen hier stehen, und der Satz, warum das anders ist als in Stufe 1
    (GreenMail-Daten sind erfunden, die Stufe-1-Kontoantwort trug eine echte Adresse), gehört
    dazu.

    (3) Der Abschnitt "Was diese Messung nicht beweist" wird nachgezogen: die Zeile "Sie sagt
    nichts über die Feldformen der Antworten" verweist jetzt auf das Stufe-2-Protokoll, und der
    Brute-Force-Punkt bekommt die Messung aus Korrektur K5 daneben, dass `throttle()` im
    `lib/`-Baum von Mail 5.11.1 nirgends gerufen wird, der Zähler also nicht zählt und der Grund
    gegen eine Schleife über `message/{id}` die IMAP-Sitzung ist und nicht der Zähler. Der
    Reproduktionsblock bekommt die zwei zusätzlichen Schritte (GreenMail läuft mit, Konto zeigt
    auf `greenmail:3143`).

    Deutsch, echte Umlaute, keine Em-Dashes. Das Wort "archiv" darf in diesem Dokument stehen (das
    Vokabular-Gate prüft nur den Manifest-Text), aber ein Postfachname im Protokoll ist ein
    Messwert und keine Wortwahl.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run pytest tests/contract/test_tool_surface.py -q</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; python - &lt;&lt;'PY'
from pathlib import Path
text = Path("docs/spike-mail.md").read_text(encoding="utf-8")
for needle in ("Stufe 2", "openapi.json", "/ocs/v2.php/apps/mail/account/list", "/ocs/v2.php/apps/mail/ocs/mailboxes", "/ocs/v2.php/apps/mail/message/", "specialRole", "previewText", "throttle"):
    assert needle in text, needle
for dash in ("—", "–"):
    assert dash not in text, f"em dash {dash!r} in the document"
print("spike-mail.md carries the correction and the stage 2 protocol")
PY</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - Der Abschnitt "Replaceability" nennt die vier benutzten OCS-Leseformen wörtlich und den Grund, warum die internen Routen nicht benutzt werden (Resource-Route, POST/PUT/DELETE auf dem Leseweg)
    - Die alte Aussage über "die einzige der vier deklarierten Routen" ist als für die gemessenen Wege richtig und für die Familie falsch gekennzeichnet und nicht gelöscht
    - Der Stufe-2-Abschnitt trägt ein Datum und die Messwerte zu `specialRole`, `previewText`, den Body-Längen samt gewählter Byte-Kappe, den zehn Filterläufen mit Trefferzahlen und `flags.seen` vor und nach dem Volltextabruf
    - Der Satz steht dort, warum Stufe 2 Feldwerte drucken darf und Stufe 1 nicht
    - Der Brute-Force-Punkt nennt die Messung, dass `throttle()` in Mail 5.11.1 nie gerufen wird
    - Der Reproduktionsblock nennt GreenMail und das Konto auf `greenmail:3143`
    - Das Dokument enthält kein `—` und kein `–`
    - `uv run pytest -q` und die beiden ruff-Läufe sind grün; `test_tool_surface.py` bleibt grün, also nennt das Dokument keine Werkzeugzahl ohne Verweis auf den Halter
  </acceptance_criteria>
  <done>Das Dokument, das die nächsten sechs Pläne zitieren, behauptet nichts Falsches mehr und trägt die Messwerte, auf denen sie aufbauen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GreenMail zu Nextcloud | Ein neuer Dienst im Compose-Netz, der IMAP- und SMTP-Verkehr ohne Authentifizierung annimmt |
| Messprotokoll zu öffentlichem Repo | `docs/spike-mail.md` liegt in einem öffentlichen Repository, und die Messung berührt Kontodaten und ein `APP_SECRET` |
| Testmail zu Modellkontext | Der Inhalt einer eingelieferten Mail ist fremder Text, der in späteren Phasen durch Werkzeuge dieses Servers läuft |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | GreenMail ohne Authentifizierung | mitigate | Kein `ports`-Block, also kein Weg von aussen; erreichbar nur im Netz `nc-mcp-exapp-net`, genau wie der Dienst `registry` (T-08-05). Die Compose-Konfiguration wird mit einer leeren `ports`-Liste geprüft |
| T-10-02 | Information Disclosure | Messprotokoll in `docs/spike-mail.md` | mitigate | Alle Adressen liegen unter `example.test` und alle Inhalte sind erfunden, deshalb dürfen Feldwerte gedruckt werden; ein `APP_SECRET` und jeder Header, der es trägt, erscheint nie, auch nicht gekappt (T-08-01) |
| T-10-03 | Spoofing | Existenzprüfung des Mail-Kontos | mitigate | Die Prüfung vergleicht Adresse **und** Host; ein Konto auf dem alten Host gilt nicht als vorhanden, sonst wäre ein grüner Bootstrap ohne IMAP-Zugang möglich |
| T-10-04 | Tampering | Wegwerf-Container für die SMTP-Einlieferung | accept | `docker run --rm` in ein Netz, das ohnehin nur diese Topologie enthält; das Image ist ein offizielles Python-Image, und es entsteht kein Volume und kein veröffentlichter Port |
| T-10-05 | Denial of Service | Die 400-KB-Testmail | accept | Sie ist der Zweck der Messung: sie soll die Kappe auslösen. Die Topologie ist eine Wegwerf-Instanz auf Loopback |
| T-10-SC | Tampering | Paketinstallation | mitigate | Keine neue Sprachabhängigkeit: `pyproject.toml` und `uv.lock` bleiben unangetastet, es gibt kein `uv add`, kein `pip install` und kein `npm install` in diesem Plan. Die zwei neuen Container-Images (`greenmail/standalone:2.1.12`, `python:3.13-alpine`) sind auf eine Fassung gepinnt und laufen nur in der Wegwerf-Topologie |
</threat_model>

<verification>
1. `docker compose -p nc-mcp-exapp -f compose.exapp.yml config --quiet` und `bash -n scripts/bootstrap_exapp.sh` sind grün.
2. Zwei aufeinanderfolgende Bootstrap-Läufe enden beide mit Exit-Code 0 und liefern dieselbe Nachrichtenzahl.
3. Die vier deklarierten OCS-Leseformen antworten unter reiner AppAPI-Impersonation mit 200 und echten Daten.
4. `uv run pytest -q`, `uv run ruff check .` und `uv run ruff format --check .` sind grün; dieser Plan ändert keinen Produktionscode.
5. `git diff --stat` zeigt keine Änderung an `pyproject.toml`, `uv.lock`, `src/` oder `appinfo/info.xml`.
</verification>

<success_criteria>
- Die Topologie hat einen erreichbaren IMAP- und SMTP-Server, ohne einen veröffentlichten Port dazuzugewinnen.
- A1 bis A4 aus `10-RESEARCH.md` sind Messwerte mit Datum, und die Byte-Kappe des Volltexts ist entschieden.
- `docs/spike-mail.md` behauptet nicht mehr, das Rückgrat der Familie seien die `SCOPE_IGNORE`-Routen.
- Kein Produktionscode und keine Abhängigkeit ist angefasst.
</success_criteria>

<output>
Create `.planning/phases/10-mail-strikt-lesend-und-die-trifecta-grenze/10-01-SUMMARY.md` when done.
Die Zusammenfassung nennt ausdrücklich und in Zahlen: die gemessene Wertemenge von `specialRole`,
das Füllverhalten und die typische Länge von `previewText`, die gewählte Byte-Kappe des Volltexts
samt der Messung, aus der sie kommt, das Ergebnis jedes der zehn Filterläufe und den `flags.seen`
-Wert vor und nach dem Volltextabruf. Plan 10-04 setzt die Vorschau-Kappe und die
Filter-Positivliste gegen diese Zahlen, Plan 10-05 die Volltext-Kappe, und Plan 10-08 erwartet
genau diese Werte im Live-Lauf.
</output>
