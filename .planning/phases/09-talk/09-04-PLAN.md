---
phase: 09-talk
plan: 04
type: execute
wave: 3
depends_on: ["09-03"]
files_modified:
  - src/mcp_connector/server/reg_talk.py
  - tests/contract/test_tool_surface.py
  - tests/contract/test_no_destructive_calls.py
  - scripts/check_tool_budget.py
  - scripts/acceptance_all_tools.py
  - README.md
  - README.de.md
  - README.fr.md
  - docs/oauth-setup.md
  - docs/client-setup.md
  - docs/conference-demo.md
  - appinfo/info.xml
  - CHANGELOG.md
autonomous: true
requirements: [TALK-01, TALK-02, TALK-03, TALK-04]

must_haves:
  truths:
    - "tools/list nennt 20 Werkzeuge, darunter talk_browse und talk_send, und die Zahl steht identisch in Registry, Contract-Test, beiden Skripten, den drei READMEs und der Doku"
    - "level ist im Input-Schema von talk_browse ein enum mit genau conversations und messages, und kein Schema trägt $defs"
    - "Die Annotationen sagen die Wahrheit: talk_browse liest nur, talk_send legt an und ist weder destruktiv noch idempotent"
    - "Es existiert kein Pfad zum Bearbeiten, Löschen, geplanten Senden, Zusammenfassen, Anheften, Erinnern, Anhängen oder Lesemarker-Setzen: das Gate hält das mit Nadeln fest, jede mit Gegenprobe, und die genau drei erlaubten Talk-Pfadformen sind positiv behauptet"
    - "Das Budget-Gate ist auf eine datierte Messung verankert und kein einzelnes Werkzeug überschreitet 1400 Bytes"
    - "Wer die Store-Beschreibung in einer der drei Sprachen liest, findet Talk als Familie genannt, und keine der drei nennt zur Seite gelegte Konversationen mit dem verbotenen Wort"
  artifacts:
    - path: "src/mcp_connector/server/reg_talk.py"
      provides: "Registrierung beider Talk-Werkzeuge mit Literal-Enum, READ_ONLY und CREATE_ONLY"
      contains: "Literal"
      min_lines: 55
    - path: "tests/contract/test_tool_surface.py"
      provides: "Eingefrorene Mengen auf 20, Enum-Test und Verbotsliste für Talk"
      contains: "talk_send"
    - path: "tests/contract/test_no_destructive_calls.py"
      provides: "Zehn Talk-Nadeln mit Gegenproben plus ALLOWED_TALK_ROUTES als positive Behauptung"
      contains: "ALLOWED_TALK_ROUTES"
    - path: "scripts/acceptance_all_tools.py"
      provides: "EXPECTED_TOOLS 20, die zwei Namen in der Erwartungsmenge und ein Talk-Abschnitt im Lauf"
      contains: "talk_browse"
    - path: "appinfo/info.xml"
      provides: "Talk in allen drei Store-Beschreibungen, ohne das verbotene Wort"
      contains: "Talk"
  key_links:
    - from: "src/mcp_connector/server/reg_talk.py"
      to: "src/mcp_connector/tools/talk.py"
      via: "talk_tools.browse und talk_tools.send hinter compact und graceful"
      pattern: "talk_tools\\.(browse|send)"
    - from: "src/mcp_connector/server/__init__.py"
      to: "src/mcp_connector/server/reg_talk.py"
      via: "_load_registrations() importiert jedes reg_*-Modul per pkgutil, kein Eintrag nötig"
      pattern: "_load_registrations"
    - from: "tests/contract/test_tool_surface.py"
      to: "README.md, README.de.md, README.fr.md"
      via: "test_the_readme_permission_table_matches_the_live_registry parst die Tabellenzellen"
      pattern: "readme"
---

<objective>
Die zwei Werkzeuge sichtbar machen und alle eingefrorenen Zahlen, Gates und Texte in einem Zug
mitziehen, so dass die Registry nie ein rotes Fenster hat.

Purpose: Sobald `reg_talk.py` existiert, sind fünf Stellen im Oberflächen-Test, zwei Skripte,
drei READMEs, drei Doku-Seiten und der Changelog mit ihrer alten Zahl falsch. Genau deshalb
gehören Registrierung und Nachzug in denselben Plan, wie in Phase 8 bewährt: kein Plan hat ein
rotes Gate als Zwischenzustand. Dazu die tragfähige Absicherung dieser Familie: Talk bietet
Schreibwege an, die **kein verbotenes Verb** brauchen (`/schedule` legt eine Nachricht an, die
die App später selbst sendet, und **PUT** ist in diesem Projekt kein verbotenes Verb, weil
`files_upload` es benutzt). Ein Bearbeiten-Pfad fällt also durch keine bestehende Nadel. Die
Absicherung ist deshalb zweiteilig: zehn neue Nadeln mit je einer Gegenprobe, plus die positive
Behauptung über die genau drei Pfadformen, die `clients/talk.py` baut. Und der Owner-Entscheid
vom 2026-08-21 gilt: die neue Familie gehört in die drei Store-Beschreibungen von
`appinfo/info.xml`, zusammen mit den übrigen Doku-Nachzügen.

Output: `reg_talk.py`, die angepassten Contract-Tests, das verankerte Budget-Gate, das
Abnahmeskript mit Talk-Abschnitt, die drei READMEs, die sechste Zeile der Feldtabelle in
`docs/oauth-setup.md`, die Doku-Zahlen, die drei Store-Beschreibungen und ein Changelog-Eintrag.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-talk/09-RESEARCH.md
@.planning/phases/09-talk/09-PATTERNS.md
@.planning/phases/09-talk/09-02-SUMMARY.md
@.planning/phases/09-talk/09-03-SUMMARY.md

Vorbild für die Registrierung, die ganze Datei:
@src/mcp_connector/server/reg_tables.py

Annotationen, compact, graceful und der Auto-Import:
@src/mcp_connector/server/__init__.py

Die zwei Contract-Tests, die angepasst werden:
@tests/contract/test_tool_surface.py
@tests/contract/test_no_destructive_calls.py

<interfaces>
Aus src/mcp_connector/server/__init__.py:
  READ_ONLY = ToolAnnotations(read_only_hint=True, open_world_hint=False)
  CREATE_ONLY = ToolAnnotations(read_only_hint=False, destructive_hint=False,
                               idempotent_hint=False, open_world_hint=False)
  def compact(payload: object) -> str
  graceful                     Dekorator, steht direkt unter @mcp.tool(...)
  mcp                          das MCPServer-Objekt
  _load_registrations()        importiert jedes reg_*-Modul per pkgutil, kein Eintrag nötig

Aus src/mcp_connector/tools/talk.py (Plan 09-03):
  LEVELS = ("conversations", "messages"); DEFAULT_LIMIT = 20; MAX_LIMIT = 50
  async def browse(clients, level="conversations", token=None, limit=DEFAULT_LIMIT, cursor=None) -> dict
  async def send(clients, token, message) -> dict

Die genau drei Pfadformen, die src/mcp_connector/nextcloud/clients/talk.py baut (Plan 09-01):
  ocs.ocs_url bzw. ocs_get auf ROOM_PREFIX
  ocs_get  auf f"{CHAT_PREFIX}/{conversation}"
  ocs_post auf f"{CHAT_PREFIX}/{conversation}"

Stand der eingefrorenen Zahlen (heute im Repo gemessen):
  tests/contract/test_tool_surface.py: EXPECTED_TOOLS Zeile 35, CREATE_TOOLS Zeile 57 (fünf
    Einträge), Docstring "18 tools, and the diet holds for 16 of them" Zeile 370,
    assert len(tools) == 18 Zeile 375, Assertion-Meldung "all 18 schemas" Zeile 520,
    Kommentar und Zellenlogik Zeile 553 bis 560
  scripts/acceptance_all_tools.py: Docstring Zeile 1 und 16, EXPECTED_TOOLS = 18 Zeile 52, die
    Erwartungsmenge der 18 Namen in main() (Zeile 320 ff.), Tables-Muster für einen Write mit
    SKIP (Zeile 262 bis 282)
  scripts/check_tool_budget.py: Messzeilen Zeile 19 und 22, BUDGET_BYTES = 15_000 Zeile 35,
    MAX_TOOL_BYTES = 1400 Zeile 42, zwei Fehlerzweige Zeile 64 bis 74
  README.md: "The 18 tools" Zeile 19, "All 18 tools" Zeile 30, Tables-Zeilen 198 und 199
  README.de.md: Zeile 21 und 32, Tables-Zeilen 205 und 206
  README.fr.md: Zeile 21 und 34, Tables-Zeilen 208 und 209
  docs/oauth-setup.md: "carries five fields" Zeile 110, Feldtabelle Zeile 113 bis 119
  docs/client-setup.md: "18 tools" Zeile 11 und 74 (Zeile 431 und 714 sind datierte Messwerte
    alter Läufe mit 16 und dürfen nach der Regel des Wächters stehenbleiben)
  docs/conference-demo.md: "tools=18" Zeile 140, "eighteen tools" Zeile 267, die zitierte
    Fehlermeldung des Abnahmeskripts Zeile 271 ff.
  appinfo/info.xml: die drei <description>-Blöcke Zeile 26 bis 64; der zweite Absatz jeder
    Sprache nennt die Familien namentlich (Zeile 29, 42, 55)

Gemessene Kollisionsfreiheit der zehn geplanten Nadeln (heute im Repo, jeweils null Treffer in
src/mcp_connector): /schedule, /summarize, /reminder, /pin, /attachment, /read, /favorite,
/notify, /participants, /archive. Achtung: /share ist bereits eine Nadel und deckt
chat/{token}/share mit ab.

Zielzahl dieser Phase: 20 Werkzeuge, davon sechs create-only (files_upload,
calendar_create_event, notes_create, deck_create_card, tables_create_row, talk_send) und
vierzehn rein lesende.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: reg_talk.py und die eingefrorenen Stellen im Oberflächen-Test</name>
  <files>src/mcp_connector/server/reg_talk.py, tests/contract/test_tool_surface.py</files>
  <read_first>
    - src/mcp_connector/server/reg_tables.py (ganze Datei, 71 Zeilen)
    - src/mcp_connector/server/__init__.py (READ_ONLY und CREATE_ONLY Zeile 53 bis 60, graceful 68 bis 99, _load_registrations 102 bis 114)
    - src/mcp_connector/tools/talk.py (Signaturen und Konstanten aus Plan 09-03)
    - tests/contract/test_tool_surface.py (ganze Datei; Mengen 28 bis 63, Tables-Block 248 bis 299, die Zahl 18 an Zeile 370, 375, 520, 553 bis 560, der README-Wächter 471 bis 560)
    - .planning/phases/09-talk/09-PATTERNS.md, Abschnitte "server/reg_talk.py" und "tests/contract/test_tool_surface.py"
    - .planning/phases/09-talk/09-RESEARCH.md, Abschnitt "Falle 8" und "Falle 9"
  </read_first>
  <action>
    Neue Datei `src/mcp_connector/server/reg_talk.py`, eins zu eins nach `reg_tables.py`. Kein
    Eintrag in `server/__init__.py`: `_load_registrations()` importiert jedes `reg_*`-Modul per
    pkgutil, genau damit parallel geschriebene Pläne keine gemeinsame Datei anfassen.

    Modul-Docstring nach dem Vorbild: die Logik liegt in `mcp_connector.tools.talk`, `level` ist
    ein `Literal` und damit ein `enum` im Input-Schema statt eines freien Strings, und beide
    Werkzeuge werden unbedingt gelistet, auch auf einer Instanz ohne Talk-App, weil ein
    credential-abhängiges `tools/list` nicht cachebar ist, das Budget-Gate bricht und Clients
    überrascht, die Werkzeuglisten speichern (SRV-04).

    `talk_browse` mit `@mcp.tool(annotations=READ_ONLY, structured_output=False)` und `@graceful`
    darunter. Parameter mit `Annotated` und `Field`: `level` als
    `Literal["conversations", "messages"]` mit Default `"conversations"`, `token` als `str` mit
    Default leerer String und einer Beschreibung, die sagt, dass das Token aus
    `level="conversations"` kommt, `limit` als `int` mit `ge=1`, `le=talk_tools.MAX_LIMIT` und
    Default `talk_tools.DEFAULT_LIMIT`, `cursor` als `str` mit Default leerer String, `ctx` als
    `Context | None = None`. Leere Strings statt `None`, damit kein `anyOf` aus String und Null ins
    Schema kommt; der Rumpf holt `deps.resolve_clients(ctx)` und wandelt leere Strings vor der
    Übergabe in `None`. Die Werkzeug-Beschreibung nennt in einem Satz, dass die Nachrichtenebene
    neueste zuerst liefert und die Fortsetzung in die Vergangenheit läuft, weil die App genau so
    antwortet und ein Umdrehen im Werkzeug eine Antwort erzeugen würde, deren Reihenfolge der
    Bedeutung von `next` widerspricht.

    `talk_send` mit `@mcp.tool(annotations=CREATE_ONLY, structured_output=False)` und `@graceful`.
    Parameter: `token` als `str` mit der Beschreibung, dass es aus `talk_browse` kommt, `message`
    als `str`, und `ctx`. Die Beschreibung ist der Ort, an dem das Budget wirklich reißen kann
    (Pro-Werkzeug-Deckel 1400 Bytes, heutiger Ausreißer `calendar_create_event` mit 1351), also
    genau zwei Sätze und keinen Absatz: einer sagt, dass nur gesendet und nie bearbeitet oder
    gelöscht wird, einer sagt, dass ein Timeout nicht bedeutet, dass nichts gesendet wurde, und
    dass der nächste Schritt ein Nachlesen mit `talk_browse(level="messages")` ist und kein
    zweiter Aufruf.

    Danach `tests/contract/test_tool_surface.py` an genau fünf Stellen nachziehen:
    1. `EXPECTED_TOOLS` um `talk_browse` und `talk_send` erweitern; `CREATE_TOOLS` um `talk_send`,
       und den Kommentar darüber von fünf auf sechs Schreibwege ändern.
    2. `assert len(tools) == 18` auf 20 setzen samt Meldung, und den Docstring derselben Funktion
       von "18 tools, and the diet holds for 16 of them" auf 20 beziehungsweise 18 korrigieren.
    3. Die Assertion-Meldung "all 18 schemas" auf 20 ziehen und jede weitere Aussage in dieser
       Datei, die eine Anzahl create-only gegen eine Anzahl reine Lesewerkzeuge stellt, auf sechs
       gegen vierzehn.
    4. Einen neuen Test nach dem Vorbild
       `test_the_two_tables_tools_are_listed_and_browse_takes_an_enum_level`: beide
       Talk-Werkzeuge sind gelistet, beide haben `output_schema is None`, `talk_browse` hat
       `read_only_hint` True und `open_world_hint` False, das Level-Enum im Schema ist genau die
       Liste conversations, messages, `$defs` kommt im Schema nicht vor, und `talk_send` hat
       `read_only_hint` False, `destructive_hint` False, `idempotent_hint` False und
       `open_world_hint` False.
    5. Einen neuen Test nach dem Vorbild `test_there_is_no_tool_per_tables_level` mit der
       Verbotsliste `talk_list_conversations`, `talk_list_messages`, `talk_read_message`,
       `talk_send_message`.

    Nach der Änderung mit grep prüfen, dass in dieser Datei außerhalb von Kommentaren keine
    Aussage mit der alten Zahl übrig ist.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run pyright &amp;&amp; uv run pytest tests/contract/test_tool_surface.py -q</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run python -c "
import asyncio, json
from mcp import Client
from mcp_connector.server import mcp
async def main():
    async with Client(mcp, raise_exceptions=True) as client:
        tools = {t.name: t for t in (await client.list_tools()).tools}
    assert len(tools) == 20, sorted(tools)
    for name in ('talk_browse', 'talk_send'):
        assert name in tools, sorted(tools)
        assert tools[name].output_schema is None, name
    schema = tools['talk_browse'].input_schema
    assert schema['properties']['level']['enum'] == ['conversations', 'messages'], schema['properties']['level']
    assert '\$defs' not in json.dumps(schema)
    browse, send = tools['talk_browse'].annotations, tools['talk_send'].annotations
    assert browse.read_only_hint is True and browse.open_world_hint is False
    assert send.read_only_hint is False and send.destructive_hint is False and send.idempotent_hint is False
    print('registry ok', len(tools))
asyncio.run(main())
"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; grep -v "^\s*#" tests/contract/test_tool_surface.py | grep -c "== 18\|all 18\|18 tools" | grep -qx 0 &amp;&amp; grep -c "talk_browse\|talk_send" tests/contract/test_tool_surface.py</automated>
  </verify>
  <acceptance_criteria>
    - `reg_talk.py` existiert und wird ohne Eintrag in `server/__init__.py` geladen: ein In-Memory-Client listet `talk_browse` und `talk_send`
    - Die Registry meldet 20 Werkzeuge; `uv run pytest tests/contract/test_tool_surface.py -q` ist grün
    - Das Level-Enum von `talk_browse` ist genau die Liste conversations, messages
    - Keines der beiden neuen Schemata enthält `$defs`, und beide Werkzeuge haben `output_schema is None`
    - `talk_browse` hat `read_only_hint` True; `talk_send` hat `read_only_hint` False, `destructive_hint` False und `idempotent_hint` False, jeweils `open_world_hint` False
    - `CREATE_TOOLS` hat sechs Einträge und ist eine echte Teilmenge von `EXPECTED_TOOLS`
    - Ein Test verbietet `talk_list_conversations`, `talk_list_messages`, `talk_read_message` und `talk_send_message`
    - `tests/contract/test_tool_surface.py` enthält außerhalb von Kommentaren keine Aussage mehr mit der Zahl 18
  </acceptance_criteria>
  <done>Die zwei Werkzeuge sind Teil des kuratierten Satzes, ihr Level ist ein Enum, ihre Annotationen sagen die Wahrheit, und die fünf eingefrorenen Stellen nennen 20.</done>
</task>

<task type="auto">
  <name>Task 2: Destruktiv-Gate um die verblosen Talk-Schreibwege, Budget-Gate und Abnahmeskript</name>
  <files>tests/contract/test_no_destructive_calls.py, scripts/check_tool_budget.py, scripts/acceptance_all_tools.py</files>
  <read_first>
    - tests/contract/test_no_destructive_calls.py (ganze Datei; der Kommentar 32 bis 49, FORBIDDEN 50 bis 63, TABLES_ROUTES 65 bis 76, ALLOWED_TABLES_ROUTES 78 bis 84, die Lese-Ausnahme 86 bis 100, ALLOWED_MODULE_STATE 145 bis 148, die Gegenproben 316 bis 373)
    - src/mcp_connector/nextcloud/clients/talk.py (welche Pfadformen das Modul wirklich baut, im Wortlaut)
    - scripts/check_tool_budget.py (ganze Datei: Messkommentar 15 bis 35, MAX_TOOL_BYTES 42, per_tool und beide Fehlerzweige 55 bis 78)
    - scripts/acceptance_all_tools.py (Docstring 1 bis 31, EXPECTED_TOOLS 52, der Tables-Abschnitt mit SKIP-Logik 230 bis 290, die Erwartungsmenge in main() ab Zeile 320)
    - .planning/phases/09-talk/09-RESEARCH.md, Abschnitte T14, "Was die App an Schreibwegen sonst noch anbietet", "Falle 8"
    - .planning/phases/09-talk/09-PATTERNS.md, Abschnitte "tests/contract/test_no_destructive_calls.py", "scripts/check_tool_budget.py", "scripts/acceptance_all_tools.py"
  </read_first>
  <action>
    In `tests/contract/test_no_destructive_calls.py` das `FORBIDDEN`-Dictionary um die
    Talk-Schreibwege erweitern, die kein verbotenes Verb brauchen. Zehn Nadeln, jede mit einem
    Begründungssatz als Wert, jede auf einem **Pfadsegment** verankert und nicht auf dem
    öffnenden Anführungszeichen: `/schedule` (geplanter Versand, der genau die Umgehung des
    TALK-04-Schalters wäre), `/summarize` (schickt Inhalt an einen KI-Anbieter der Instanz),
    `/reminder`, `/pin`, `/attachment`, `/read` (Lesemarker setzen und auf ungelesen
    zurücksetzen), `/favorite`, `/notify`, `/participants`, `/archive`. `/share` ist bereits eine
    Nadel und deckt `chat/{token}/share` mit ab; das gehört als Kommentar dazu, damit niemand
    eine elfte Nadel danebenlegt. Der Kommentarblock erklärt außerdem, warum diese Familie
    beide Hälften braucht: **PUT** ist in diesem Projekt kein verbotenes Verb, weil
    `files_upload` es benutzt, also fällt ein Bearbeiten-Pfad durch keine Verb-Nadel.

    Je Nadel eine Gegenprobe nach dem Vorbild von `TABLES_ROUTES` und
    `test_each_tables_needle_trips_on_its_route_and_leaves_the_real_module_alone`: ein
    `TALK_ROUTES`-Dictionary mit einer Zeile je Nadel, geschrieben in der f-String-Form, die
    dieses Projekt für Pfade benutzt, und ein parametrisierter Test, der zweierlei behauptet: die
    Nadel schlägt auf der angehängten Zeile an, und der echte Quellcode von
    `nextcloud/clients/talk.py` ist vorher sauber. Vor dem Commit gilt: ist eine Nadel zu breit
    und trifft bestehenden Code, wird die Nadel präziser gefasst und nicht das Modul umgebaut.
    Die zehn Segmente haben heute je null Treffer im gesamten `src/mcp_connector`, das ist
    gemessen; eine Kollision würde also aus dem neuen Talk-Client kommen und wäre ein echter
    Fund.

    Dann die positive Hälfte: `ALLOWED_TALK_ROUTES` mit **genau drei** Zeilen im Wortlaut, den
    `clients/talk.py` schreibt, plus ein Test nach dem Vorbild
    `test_the_three_routes_the_tables_client_really_builds_stay_allowed`, der behauptet, dass
    keine der drei das Gate auslöst. Das ist der Ersatz für die fehlende Verb-Nadel bei PUT: die
    Familie sagt positiv, welche Pfadformen es gibt, statt nur negativ, welche es nicht geben
    darf.

    `ALLOWED_MODULE_STATE` bleibt bei genau zwei Einträgen: die neuen Module dürfen keinen
    eigenen Cache und keine modulweite Liste haben. Wird der Test hier rot, liegt der Fehler im
    Modul und nicht im Gate.

    Dann `scripts/check_tool_budget.py`: erst das Skript laufen lassen und den gemessenen Wert
    für 20 Werkzeuge sowie die Größe der zwei neuen Schemata notieren. Reißt das Gate (heute
    12801 Bytes bei 18 Werkzeugen, Gate 15000, geschätzt 14300 bis 14500 bei 20), dann eine neue
    Zeile in derselben Schreibweise wie die bestehenden zwei ergänzen, also "Measurement
    <heutiges Datum>, all 20 curated tools registered: <gemessen> bytes" plus die Rechnung mit 15
    Prozent Zuschlag, aufgerundet auf die nächsten 500, und `BUDGET_BYTES` auf diesen Wert
    setzen. Die alten Messzeilen bleiben stehen, damit eine Regression zuordenbar bleibt.
    Reißt es nicht, wird **nichts** angehoben und die Messung wandert in die
    Plan-Zusammenfassung: die Regel ist, dass eine Anhebung nur gegen eine Messung erfolgt, nicht
    dass jede Phase anhebt. `MAX_TOOL_BYTES` bleibt bei 1400; überschreitet die Beschreibung von
    `talk_send` diesen Deckel, wird die Beschreibung gekürzt und nicht der Deckel gehoben.

    Dann `scripts/acceptance_all_tools.py`: `EXPECTED_TOOLS` von 18 auf 20, die Zahl im
    Modul-Docstring (Zeile 1) und in Zeile 16 mitziehen, und die Erwartungsmenge in `main()` um
    `talk_browse` und `talk_send` erweitern. Dazu ein Talk-Abschnitt im Lauf, nach dem Muster des
    Tables-Abschnitts: `talk_browse` mit `level="conversations"`, dann `talk_browse` mit
    `level="messages"` für die erste Konversation der Antwort, dann `talk_send` in die erste
    Konversation, die `can_send` meldet und nicht Typ 4 ist, mit einer Markierung, die den Lauf
    kennzeichnet und echte Umlaute trägt. Gibt es keine schreibbare Konversation, ist das ein
    SKIP und kein FAIL, genau wie bei Deck und Tables, weil eine Konversation keine Fähigkeit
    dieses Connectors ist. Der Modul-Docstring bekommt einen Satz dazu, damit die Ausnahme
    dokumentiert ist wie die beiden bestehenden.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run pytest tests/contract/ -q &amp;&amp; uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run pyright</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run python scripts/check_tool_budget.py &amp;&amp; uv run python -c "
import re, pathlib
src = pathlib.Path('scripts/check_tool_budget.py').read_text(encoding='utf-8')
budget = int(re.search(r'BUDGET_BYTES = ([0-9_]+)', src).group(1).replace('_', ''))
assert budget % 500 == 0, budget
assert 'MAX_TOOL_BYTES = 1400' in src, 'the per tool cap is not raised in this phase'
measurements = [l for l in src.splitlines() if 'Measurement' in l]
assert len(measurements) >= 2, measurements
if budget != 15000:
    assert any('20 curated tools' in l for l in measurements), 'a raise needs a dated measurement line for 20 tools'
print('budget', budget, len(measurements), 'measurement lines')
"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run python -c "
import pathlib
src = pathlib.Path('scripts/acceptance_all_tools.py').read_text(encoding='utf-8')
assert 'EXPECTED_TOOLS = 20' in src
assert src.count('talk_browse') >= 2 and src.count('talk_send') >= 2
assert '18 tools' not in src and 'all 18' not in src
print('acceptance script ok')
"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run python -c "
import pathlib
gate = pathlib.Path('tests/contract/test_no_destructive_calls.py').read_text(encoding='utf-8')
for needle in ('/schedule', '/summarize', '/reminder', '/pin', '/attachment', '/read', '/favorite', '/notify', '/participants', '/archive'):
    assert repr(needle)[1:-1] in gate, needle
assert 'ALLOWED_TALK_ROUTES' in gate
assert gate.count('ALLOWED_MODULE_STATE') >= 1
print('gate needles ok')
"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/contract/ -q` ist grün, inklusive aller neuen Gegenproben
    - `FORBIDDEN` trägt zehn neue Talk-Nadeln, jede mit einem Begründungssatz als Wert, und ein Kommentar sagt, dass `/share` schon existiert und dass PUT kein verbotenes Verb ist
    - Für jede der zehn Nadeln existiert eine Gegenprobe, die belegt, dass eine angehängte Zeile mit dieser Route erkannt wird und der echte Quellcode nicht getroffen wird
    - `ALLOWED_TALK_ROUTES` behauptet genau drei Pfadformen, und keine davon löst das Gate aus
    - `ALLOWED_MODULE_STATE` hat weiterhin genau zwei Einträge
    - `uv run python scripts/check_tool_budget.py` endet mit Exit-Code 0; `MAX_TOOL_BYTES` ist unverändert 1400; wurde `BUDGET_BYTES` angehoben, gibt es eine neue datierte Messzeile mit "20 curated tools", und die alten Zeilen stehen noch da
    - `scripts/acceptance_all_tools.py` hat `EXPECTED_TOOLS = 20`, nennt beide Talk-Werkzeuge in der Erwartungsmenge und im Lauf, und enthält keine Aussage mit 18 mehr
    - Der Talk-Abschnitt des Abnahmeskripts meldet SKIP statt FAIL, wenn keine schreibbare Konversation existiert
  </acceptance_criteria>
  <done>Kein Werkzeug kann eine Nachricht bearbeiten, löschen, planen, zusammenfassen, anheften oder als gelesen markieren, ohne dass ein Gate mit Gegenprobe rot wird, und das Budget bleibt an einer Messung verankert statt an einer Gewohnheit.</done>
</task>

<task type="auto">
  <name>Task 3: Dreisprachige READMEs, Store-Beschreibungen, Doku-Zahlen und Changelog</name>
  <files>README.md, README.de.md, README.fr.md, docs/oauth-setup.md, docs/client-setup.md, docs/conference-demo.md, appinfo/info.xml, CHANGELOG.md</files>
  <read_first>
    - README.md (Zeile 15 bis 35 und die Tool-Tabelle 185 bis 205)
    - README.de.md (Zeile 17 bis 36 und die Tool-Tabelle 195 bis 210)
    - README.fr.md (Zeile 17 bis 38 und die Tool-Tabelle 198 bis 212)
    - docs/oauth-setup.md (Zeile 100 bis 130: "carries five fields" und die Feldtabelle)
    - docs/client-setup.md (Zeile 8 bis 14, 70 bis 78, und die datierten Messwerte 425 bis 435 und 710 bis 718)
    - docs/conference-demo.md (Zeile 135 bis 145 und 263 bis 280)
    - appinfo/info.xml (die drei description-Blöcke Zeile 26 bis 64 samt dem Format-Kommentar darüber)
    - CHANGELOG.md (der oberste Block)
    - tests/contract/test_tool_surface.py (der README- und Doku-Wächter Zeile 471 bis 560)
    - tests/unit/test_exapp_env_setup.py (die Manifest-Gates: FORBIDDEN_VOCABULARY 1675, Absatzregel und Markup-Regeln 1750 bis 1785)
    - .planning/phases/09-talk/09-RESEARCH.md, Abschnitt "Projekt-Constraints" (Vokabular-Gate und Store-Beschreibung-Regel)
  </read_first>
  <action>
    In allen drei READMEs je zwei Zeilen in die Tool-Tabelle direkt unter die Tables-Zeilen
    einfügen. Das Format ist bindend, weil
    `test_the_readme_permission_table_matches_the_live_registry` die Zellen parst: erste Zelle der
    Werkzeugname in Backticks, zweite Zelle wörtlich `read` beziehungsweise `create-only`, dritte
    Zelle die Beschreibung. Englisch: `talk_browse` mit `read` und einer Beschreibung über
    Konversationen und deren Verlauf, `talk_send` mit `create-only` und einer Beschreibung, die
    sagt, dass nur gesendet und nie bearbeitet oder gelöscht wird und dass ein Administrator das
    Senden instanzweit abschalten kann. Dieselben zwei Zeilen übersetzt in `README.de.md` und
    `README.fr.md`, mit echten Umlauten beziehungsweise Akzenten und Cedille.

    Die Werkzeugzahl in allen drei READMEs von 18 auf 20 ziehen: `README.md` Zeile 19 und 30,
    `README.de.md` Zeile 21 und 32, `README.fr.md` Zeile 21 und 34. Danach mit grep prüfen, dass
    in keiner der drei Dateien eine Produktaussage mit 18 übrig ist.

    `docs/oauth-setup.md`: "carries five fields" auf sechs ziehen, eine sechste Tabellenzeile für
    `talk_send` und `NC_MCP_TALK_SEND` ergänzen, und einen Satz dazuschreiben, dass nicht jedes
    Feld dieser Form OAuth betrifft: dieses eine schaltet den Talk-Ausgangskanal instanzweit ab
    und wirkt wie die anderen erst nach einem Deaktivieren und Aktivieren der App.

    `docs/client-setup.md`: die Zahlen in Zeile 11 und 74 auf 20 ziehen. Die datierten Messwerte
    alter Läufe (16 Werkzeuge) in Zeile 431 und 714 bleiben nach der Regel des Wächters stehen,
    solange die Seite auf `tests/contract/test_tool_surface.py` zeigt; steht der Zeiger nicht da,
    wird er ergänzt statt die Zahl gefälscht.

    `docs/conference-demo.md`: `tools=20` in Zeile 140, das ausgeschriebene Zahlwort in Zeile 267
    auf zwanzig, und die wörtlich zitierte Fehlermeldung des Abnahmeskripts an die neue Zahl
    anpassen. Wenn der Talk-Abschnitt aus Task 2 eine dritte Familie mit möglichem SKIP
    einführt, den Satz über die zwei SKIP-Fälle entsprechend richtigstellen.

    `appinfo/info.xml`: die drei `<description>`-Blöcke. Der zweite Absatz jeder Sprache nennt
    die Familien namentlich, dort kommt Talk dazu (englisch bei den bestehenden Namen, deutsch und
    französisch in der jeweiligen Fassung). Formatregeln des Datei-Kommentars beachten:
    Leerzeile als Absatztrenner, mindestens zwei Absätze je Sprache, kein Backtick, keine
    Tabelle, kein Bild, keine horizontale Linie, kein HTML-Element. **Vokabular-Gate:** das Gate
    prüft den gesamten Elementtext des Manifests casefold auf das verbotene Wort, also darf keine
    der drei Beschreibungen zur Seite gelegte Konversationen mit diesem Wort benennen; die
    empfohlene Lösung ist, sie im Store-Text gar nicht zu erwähnen. Ein Satz zum Ausgangskanal
    gehört dazu, ohne die Exfiltrationskette zu benennen: deren Benennung ist SEC-01 in Phase 10
    und darf hier nicht vorweggenommen werden, weil Mail-Lesen noch nicht existiert. `<version>`
    und `<image-tag>` werden nicht angefasst.

    `CHANGELOG.md`: unter der noch nicht veröffentlichten Fassung einen `### Added`-Abschnitt
    nach dem Muster des Tables-Eintrags anlegen, also ganze Sätze aus Nutzersicht, keine Tickets,
    keine Dateinamen, keine Emojis: Konversationen und Verlauf lesen, ohne dass dieses Lesen etwas
    am eigenen Zustand ändert; eine Nachricht in eine Konversation senden, adressiert nur mit
    einem Token aus dem Lesewerkzeug; und ein Administrator kann das Senden instanzweit
    abschalten. Das Release selbst ist Phase 11; dieser Plan schreibt keine Version und setzt
    keinen Tag.

    Danach den Doku-Wächter laufen lassen und jede Seite behandeln, die er meldet: entweder den
    Zeiger auf die Contract-Testdatei ergänzen oder die Aussage aktualisieren. Der Test
    entscheidet, welche Seite welchen Weg braucht, nicht die Vermutung.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run pytest -q &amp;&amp; uv run ruff check . &amp;&amp; uv run ruff format --check .</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run python -c "
import io
for name in ('README.md', 'README.de.md', 'README.fr.md'):
    text = io.open(name, encoding='utf-8').read()
    assert text.count('\`talk_browse\`') >= 1 and text.count('\`talk_send\`') >= 1, name
    assert '| read |' in text and '| create-only |' in text, name
    head = '
'.join(text.splitlines()[:40])
    assert '18' not in head, (name, 'a product statement still says 18')
    print(name, 'ok')
"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run python -c "
from lxml import etree
root = etree.parse('appinfo/info.xml').getroot()
langs = {d.get('lang'): (d.text or '') for d in root.findall('description')}
assert set(langs) == {None, 'de', 'fr'}, sorted(str(k) for k in langs)
for lang, text in langs.items():
    label = lang or 'en'
    assert 'Talk' in text, label
    assert '\`' not in text and '|' not in text, label
    paragraphs = [b for b in text.strip().split('\n\n') if b.strip()]
    assert len(paragraphs) >= 2, (label, len(paragraphs))
whole = ' '.join(t for t in root.itertext())
assert 'archiv' not in whole.casefold(), 'forbidden vocabulary in the manifest text'
print('descriptions ok')
"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run python -c "
import io, sys
bad = []
files = ('README.md','README.de.md','README.fr.md','docs/oauth-setup.md','docs/client-setup.md','docs/conference-demo.md','CHANGELOG.md','appinfo/info.xml')
for name in files:
    text = io.open(name, encoding='utf-8').read()
    if chr(0x2014) in text or chr(0x2013) in text:
        bad.append((name, 'dash'))
for name in ('README.de.md','README.fr.md','CHANGELOG.md'):
    if 'archiv' in io.open(name, encoding='utf-8').read().casefold():
        bad.append((name, 'vocabulary'))
print(bad)
sys.exit(1 if bad else 0)
"</automated>
  </verify>
  <acceptance_criteria>
    - Jede der drei READMEs enthält genau eine Tabellenzeile für `talk_browse` mit der zweiten Zelle `read` und genau eine für `talk_send` mit der zweiten Zelle `create-only`
    - Keine der drei READMEs enthält noch eine Produktaussage mit der Zahl 18; beide Stellen je Datei nennen 20
    - `test_the_readme_permission_table_matches_the_live_registry` und der Doku-Zahlen-Wächter sind grün
    - `docs/oauth-setup.md` nennt sechs Felder, hat eine sechste Tabellenzeile für `talk_send` und `NC_MCP_TALK_SEND` und sagt, dass nicht jedes Feld dieser Form OAuth betrifft
    - `docs/conference-demo.md` nennt `tools=20`, das Zahlwort zwanzig und eine zur neuen Zahl passende Fehlermeldung
    - Alle drei `<description>`-Blöcke in `appinfo/info.xml` nennen Talk, haben mindestens zwei Absätze, keinen Backtick, keine Tabelle und kein HTML-Element; `<version>` und `<image-tag>` sind unverändert
    - Der gesamte Manifest-Elementtext enthält das verbotene Wort nicht, und die Store-Beschreibung nimmt SEC-01 nicht vorweg
    - `CHANGELOG.md` hat einen `### Added`-Abschnitt mit drei Sätzen aus Nutzersicht; es wird keine Versionsnummer geändert und kein Tag gesetzt
    - Keine der geänderten Textdateien enthält ein Em-Dash oder En-Dash oder ein Emoji, und die deutschen und französischen Texte tragen echte Umlaute beziehungsweise Akzente
    - `uv run pytest -q` ist über die ganze Default-Auswahl grün
  </acceptance_criteria>
  <done>Die drei Sprachfassungen, die Doku, die Store-Beschreibung und der Changelog sagen dasselbe wie die Registry, und keine Zahl ist geraten.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Registry zu MCP-Client | `tools/list` ist die Zusage, die jeder Client cacht; Annotationen und Schemata sind Teil dieser Zusage |
| Quellcode zu Gate | Das Destruktiv-Gate ist die einzige Instanz, die verhindert, dass eine schreibende Talk-Route unbemerkt dazukommt |
| Store-Text zu Administrator | Eine Store-Beschreibung ist eine Sicherheitsaussage vor der Installation, keine Werbung |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-30 | Tampering | Neue Talk-Schreibroute im Client | mitigate | Zehn `FORBIDDEN`-Nadeln auf Pfadsegmenten, jede mit eigener Gegenprobe, plus `ALLOWED_TALK_ROUTES` als positive Behauptung über genau drei Pfadformen, weil PUT in diesem Projekt kein verbotenes Verb ist |
| T-09-31 | Elevation of Privilege | Geplanter Versand als Umgehung des Admin-Schalters | mitigate | `/schedule` ist eine eigene Nadel mit Gegenprobe: ein Sendeweg, den die App später selbst ausführt, würde den Schalter von TALK-04 umlaufen |
| T-09-32 | Information Disclosure | `/summarize` schickt Inhalt an einen KI-Anbieter der Instanz | mitigate | Eigene Nadel mit Gegenprobe; dieser Server leitet keinen Chatinhalt an einen dritten Dienst weiter |
| T-09-33 | Repudiation | Annotationen von `talk_send` | mitigate | Der Contract-Test behauptet `read_only_hint` False, `destructive_hint` False und `idempotent_hint` False; die Annotation ist ehrlich, weil im Client kein Update- und kein Delete-Pfad existiert |
| T-09-34 | Denial of Service | Größe von `tools/list` | mitigate | Messung vor jeder Anhebung, `MAX_TOOL_BYTES` bleibt bei 1400, und eine zu lange Beschreibung von `talk_send` wird gekürzt statt der Deckel gehoben |
| T-09-35 | Tampering | Modulweiter veränderlicher Zustand in den neuen Modulen | mitigate | `ALLOWED_MODULE_STATE` bleibt bei genau zwei namentlich gelisteten Einträgen |
| T-09-36 | Information Disclosure | Store-Beschreibung nimmt SEC-01 vorweg | accept | Die Benennung der Exfiltrationskette gehört zu Phase 10, weil Mail-Lesen noch nicht existiert; eine Sicherheitsaussage, die eine Phase zu früh steht, wäre in der Zwischenzeit unvollständig. Hier steht nur der Satz zum Ausgangskanal und zum Schalter |
| T-09-37 | Repudiation | Datierte Doku-Messwerte alter Läufe | accept | Sie dürfen stehenbleiben, wenn die Seite auf `tests/contract/test_tool_surface.py` zeigt; das ist die bestehende Regel des Wächters und wird nicht neu verhandelt |
| T-09-SC | Tampering | Paketinstallation | accept | Keine neue Sprachabhängigkeit; `pyproject.toml` und `uv.lock` bleiben unangetastet |
</threat_model>

<verification>
1. `uv run pytest -q` (Default-Auswahl) ist grün, inklusive beider Contract-Test-Dateien.
2. `uv run python scripts/check_tool_budget.py` endet mit Exit-Code 0 und gibt 20 Werkzeuge aus.
3. `uv run ruff check .`, `uv run ruff format --check .`, `uv run pyright` und `uv run vulture src/mcp_connector vulture_whitelist.py` sind grün.
4. Die Zahl 20 steht identisch in Registry, `tests/contract/test_tool_surface.py`, beiden Skripten, den drei READMEs und `docs/`.
5. Kein Em-Dash, kein En-Dash, kein Emoji in den geänderten Textdateien; kein verbotenes Vokabular im Manifest-Elementtext.
6. `git diff appinfo/info.xml` enthält keine Zeile mit `<version>` oder `<image-tag>`.
</verification>

<success_criteria>
- Die zwei Werkzeuge sind aufrufbar, damit sind TALK-01 bis TALK-03 auf Oberflächenebene erfüllt und TALK-04 hat sein Werkzeug, an dem der Schalter wirkt.
- Es existiert kein Pfad zum Bearbeiten, Löschen, geplanten Senden, Zusammenfassen oder Lesemarker-Setzen, das Gate hält das mit zehn Nadeln, jede mit Gegenprobe, und die drei erlaubten Pfadformen sind positiv behauptet.
- Werkzeugzahl 20 steht identisch in Registry, Contract-Test, beiden Skripten, den drei READMEs und der Doku.
- Wer die Store-Beschreibung in einer der drei Sprachen liest, findet Talk genannt, ohne dass das verbotene Wort auftaucht und ohne dass SEC-01 vorweggenommen wird.
</success_criteria>

<output>
Create `.planning/phases/09-talk/09-04-SUMMARY.md` when done.
Die Zusammenfassung nennt den gemessenen Byte-Wert bei 20 Werkzeugen, die Größe der zwei neuen
Schemata einzeln und ob `BUDGET_BYTES` angehoben wurde, weil TOOL-15 in Phase 11 das Gate auf
dieser Messung fortschreibt.
</output>
