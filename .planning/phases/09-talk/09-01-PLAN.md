---
phase: 09-talk
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp_connector/nextcloud/clients/ocs.py
  - src/mcp_connector/nextcloud/capabilities.py
  - src/mcp_connector/nextcloud/clients/talk.py
  - tests/unit/test_ocs_capabilities.py
  - tests/unit/test_talk_client.py
  - tests/fixtures/talk_rooms.json
  - tests/fixtures/talk_messages.json
autonomous: true
requirements: [TALK-01, TALK-02, TALK-03]

must_haves:
  truths:
    - "Ein erfolgreich gesendeter OCS-Aufruf mit Statuscode 201 gilt als Erfolg und nicht als unerwarteter Status, und die sechs bestehenden Familien sehen dieselben Antworten wie vorher"
    - "Ein leerer oder durchpaginierter Verlauf (HTTP 304) ergibt eine leere Nachrichtenliste ohne Cursor und nie den Hinweis auf eine falsche Basis-URL"
    - "Die Fortsetzung der Verlaufsseite kommt aus dem Antwort-Header X-Chat-Last-Given und nie aus den Nachrichten-Ids, also auch bei einem Fenster mit Status 200 und leerer Liste"
    - "Ein Konversations-Token, das nicht dem Muster aus vier bis dreißig Kleinbuchstaben und Ziffern entspricht, erreicht Nextcloud nie"
    - "Jeder Verlauf-Aufruf trägt lookIntoFuture=0, setReadMarker=0, markNotificationsAsRead=0 und noStatusUpdate=1 in der gebauten URL"
    - "Die App-Erkennung kennt den Schlüssel spreed, liest ihn aus der Präsenz der Sektion und liefert die Chat-Höchstlänge der Instanz mit"
  artifacts:
    - path: "src/mcp_connector/nextcloud/clients/talk.py"
      provides: "Talk-Client: v4-Räume, v1-Chat lesen und senden, Token-Wächter, 304-Behandlung, Header-Rückgabe, Web-URL"
      contains: "READ_ONLY_PARAMS"
      min_lines: 150
    - path: "src/mcp_connector/nextcloud/clients/ocs.py"
      provides: "201 im Erfolgsraum des gemeinsamen OCS-Parsers, mit Begründung"
      contains: "201"
    - path: "src/mcp_connector/nextcloud/capabilities.py"
      provides: "spreed_available, spreed_features, spreed_chat_max_length und der _MISSING-Satz für spreed"
      contains: "spreed"
    - path: "tests/unit/test_talk_client.py"
      provides: "Alle Pfade des Clients, behauptet an der gebauten Anfrage"
      contains: "lookIntoFuture=0"
    - path: "tests/fixtures/talk_rooms.json"
      provides: "Echte Antwortform der Konversationsliste mit absichtlich unpassender Server-Reihenfolge"
      contains: "lastActivity"
  key_links:
    - from: "src/mcp_connector/nextcloud/clients/talk.py"
      to: "src/mcp_connector/nextcloud/clients/ocs.py"
      via: "ocs_get, ocs_post, ocs_url und parse_ocs; kein eigener Header-Bau, keine eigene Statuszuordnung"
      pattern: "ocs\\.(ocs_get|ocs_post|parse_ocs|ocs_url)"
    - from: "src/mcp_connector/nextcloud/capabilities.py"
      to: "GET /ocs/v2.php/cloud/capabilities"
      via: "parse() liest die Sektion spreed aus derselben gecachten Antwort wie notes, deck und tables"
      pattern: "spreed_available"
---

<objective>
Die zwei blockierenden Transport-Befunde dieser Phase aus dem Weg räumen und den Talk-Client
bauen, bevor irgendein Werkzeug ihn benutzt.

Purpose: `POST /chat/{token}` antwortet 201, und `parse_ocs` lehnt heute alles ab, was nicht
100 oder 200 ist; ein leerer Verlauf antwortet 304, und `_check_transport` verwandelt jedes 3xx
in "Nextcloud hat mit einer Weiterleitung geantwortet, prüfe die Basis-URL". Beide Fälle
treten beim ersten echten Aufruf auf, nicht in einem Randfall: die erste gesendete Nachricht
und jede frische Konversation (T1 und T2 in 09-RESEARCH.md). Wer sie erst im Integrationslauf
findet, baut die Envelope-Auswertung ein zweites Mal. Dazu gehört die App-Erkennung, weil ein
Werkzeug ohne sie einen 404 auf einer HTML-Seite produziert statt eines Satzes.

Output: `clients/talk.py`, die 201-Erweiterung von `clients/ocs.py`, die spreed-Felder in
`capabilities.py`, zwei Fixtures und die vollständige Unit-Abdeckung des Clients.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-talk/09-RESEARCH.md
@.planning/phases/09-talk/09-PATTERNS.md

Das Vorbild für die ganze Familie, zwölf Tage alt:
@src/mcp_connector/nextcloud/clients/tables.py
@tests/unit/test_tables_client.py

Die zwei geänderten Dateien:
@src/mcp_connector/nextcloud/clients/ocs.py
@src/mcp_connector/nextcloud/capabilities.py
@tests/unit/test_ocs_capabilities.py

<interfaces>
Aus src/mcp_connector/nextcloud/clients/ocs.py (heute im Repo gemessen):
  OCS_HEADERS: Mapping[str, str]                     Zeile 41, OCS-APIRequest plus Accept
  _OK_STATUS = frozenset({100, 200})                 Zeile 50, wird zu {100, 200, 201}
  def ocs_url(creds: Credentials, path: str) -> str  Zeile 58
  async def ocs_get(client, creds, path, params=None) -> httpx.Response      Zeile 65
  async def ocs_post(client, creds, path, json_body) -> httpx.Response       Zeile 87
  def parse_ocs(response, what: str) -> Any          Zeile 151, prüft _OK_STATUS in Zeile 170
  def _check_transport(response, what)               Zeile 196, 3xx wird REDIRECT_HINT
  def _status_error(...)                             die eine Stelle für Statussätze

Aus src/mcp_connector/nextcloud/capabilities.py (heute im Repo gemessen):
  _MISSING: dict[str, tuple[str, str]]               Zeile 41, Satz plus eine Handlung je App
  class Capabilities                                 Zeile 67, notes_/deck_/tables_-Felder
  def has(self, app: str) -> bool                    Zeile 78, Dictionary in Zeile 81 bis 83
  def clear_cache() -> None                          Zeile 109
  async def require_app(clients, app) -> Capabilities  Zeile 114
  def app_missing(app: str) -> AppMissingError       Zeile 126
  def parse(data: Any) -> Capabilities               Zeile 132, tables in Zeile 154 bis 155
  def _versions(section, key) -> tuple[str, ...]     Zeile 159, Vorlage für den Feature-Leser

Aus src/mcp_connector/errors.py:
  ToolError(message=..., hint=...)

Die drei Pfadformen, die dieser Client bauen darf, und keine vierte:
  GET  /ocs/v2.php/apps/spreed/api/v4/room
  GET  /ocs/v2.php/apps/spreed/api/v1/chat/{token}
  POST /ocs/v2.php/apps/spreed/api/v1/chat/{token}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 201 im Erfolgsraum des OCS-Parsers und spreed in der App-Erkennung</name>
  <files>src/mcp_connector/nextcloud/clients/ocs.py, src/mcp_connector/nextcloud/capabilities.py, tests/unit/test_ocs_capabilities.py</files>
  <read_first>
    - src/mcp_connector/nextcloud/clients/ocs.py (ganze Datei; besonders _OK_STATUS Zeile 50, parse_ocs Zeile 151 bis 195, _check_transport Zeile 196 bis 203, _status_error)
    - src/mcp_connector/nextcloud/capabilities.py (ganze Datei; Modul-Docstring Zeile 1 bis 15, _MISSING Zeile 41 bis 63, Capabilities Zeile 67 bis 90, parse Zeile 132 bis 158, _versions Zeile 159 bis 168)
    - tests/unit/test_ocs_capabilities.py (ganze Datei; besonders die tables-Fälle Zeile 118 bis 205)
    - .planning/phases/09-talk/09-RESEARCH.md, Abschnitte T1, T2, Capabilities
    - .planning/phases/09-talk/09-PATTERNS.md, Abschnitte "nextcloud/clients/ocs.py" und "nextcloud/capabilities.py"
  </read_first>
  <action>
    In `clients/ocs.py` `_OK_STATUS` von `frozenset({100, 200})` auf `frozenset({100, 200, 201})`
    erweitern und den bestehenden `#:`-Kommentar um einen Satz ergänzen, der sagt warum: OCS v2
    schreibt den rohen Status in `ocs.meta.statuscode` (`V2Response::render`), und
    `POST /apps/spreed/api/v1/chat/{token}` dokumentiert 201 als einzigen Erfolg. Heute liefert
    keine benutzte Route 201, die Erweiterung kann also keine bestehende Prüfung umdrehen. Die
    Alternative wäre eine zweite Envelope-Auswertung im Talk-Client, und die ist genau die
    Doppelung, die dieses Projekt vermeidet.

    `_check_transport` bleibt unangetastet. Die 3xx-Behandlung ist richtig, und die 304 des
    Verlaufs wird in Task 2 im Talk-Client vor `parse_ocs` abgefangen, weil 304 nur an dieser
    einen Route eine Bedeutung hat. Diese Begründung gehört in den Docstring von
    `get_messages` und nicht in `ocs.py`.

    In `capabilities.py` die vierte optionale App aufnehmen. Der Schlüssel heißt `spreed` und
    nicht `talk`, weil das Capabilities-Dokument ihn so nennt. Talk verhält sich wie Notes und
    Deck, also Präsenz der Sektion und kein `enabled`-Feld; zusätzlich gibt eine für den
    Nutzer abgeschaltete Talk-Instanz ein leeres Array zurück, deshalb lautet die Bedingung
    "Sektion vorhanden und nicht leer". Neue Felder an `Capabilities`:
    `spreed_available: bool = False`, `spreed_features: tuple[str, ...] = ()` und
    `spreed_chat_max_length: int = 32000`. `spreed_features` liest `features` nach dem Vorbild
    von `_versions` (Liste akzeptieren, einzelnen String tolerieren, alles andere ignorieren);
    `spreed_chat_max_length` liest `config.chat.max-length` und fällt auf 32000 zurück, weil
    die Zahl der Instanz gehört und nicht uns. `has()` bekommt den Eintrag `"spreed"`, und
    `_MISSING` einen Eintrag mit einem Satz plus genau einer Sache, die der Nutzer tun kann
    (einen Administrator um die Talk-App bitten). Den Modul-Docstring nachziehen: er sagt heute
    "Notes, Deck and Tables" und "Only these three optional apps are checked here".

    In `tests/unit/test_ocs_capabilities.py` nach dem Muster der tables-Fälle ergänzen, ohne
    die Fixture `ocs_capabilities.json` anzufassen (die tables-Tests bauen ihre Payload inline,
    Talk macht es genauso): eine vorhandene spreed-Sektion mit `features` und
    `config.chat.max-length` ergibt `spreed_available` True, das Feature-Tupel und die Zahl der
    Instanz; eine fehlende Sektion ergibt False und leere Werte plus 32000; eine Sektion, die
    ein leeres Array ist, ergibt ebenfalls False (das ist der Fall, den Tables nicht braucht);
    ein einzelner Feature-String wird toleriert; `require_app` gegen eine Instanz ohne Talk
    nennt die App und den nächsten Schritt. Dazu ein Test, der einen OCS-Envelope mit
    `statuscode` 201 durch `parse_ocs` schickt und die Nutzlast zurückbekommt, mit einem
    Docstring, der T1 benennt, plus die Gegenprobe, dass ein Envelope mit 401 weiterhin ein
    ToolError ist.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run pyright &amp;&amp; uv run pytest tests/unit/test_ocs_capabilities.py -q</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run pytest -q</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run python -c "
from mcp_connector.nextcloud import capabilities
from mcp_connector.nextcloud.clients import ocs
assert ocs._OK_STATUS == frozenset({100, 200, 201}), ocs._OK_STATUS
caps = capabilities.parse({'capabilities': {'spreed': {'features': ['chat-v2'], 'config': {'chat': {'max-length': 32000}}}}})
assert caps.spreed_available is True and caps.has('spreed') is True
assert caps.spreed_features == ('chat-v2',) and caps.spreed_chat_max_length == 32000
empty = capabilities.parse({'capabilities': {'spreed': []}})
assert empty.spreed_available is False and empty.has('spreed') is False
assert empty.spreed_chat_max_length == 32000
print('capabilities ok')
"</automated>
  </verify>
  <acceptance_criteria>
    - `ocs._OK_STATUS` ist genau `frozenset({100, 200, 201})`, und der `#:`-Kommentar darüber nennt OCS v2 und die Talk-Sende-Route als Grund
    - `_check_transport` ist unverändert: der 3xx-Zweig und `REDIRECT_HINT` stehen wie vorher
    - Ein OCS-Envelope mit `statuscode` 201 gibt seine Nutzlast durch `parse_ocs` zurück; ein Envelope mit 401 löst weiterhin einen ToolError aus
    - `Capabilities` hat `spreed_available`, `spreed_features` und `spreed_chat_max_length`; `has("spreed")` antwortet ohne ValueError
    - Eine leere spreed-Sektion (leeres Array) und eine fehlende Sektion ergeben beide `spreed_available` False
    - `spreed_chat_max_length` ist ohne Angabe der Instanz 32000
    - `_MISSING["spreed"]` trägt einen Satz plus genau eine Handlung für den Nutzer
    - Der Modul-Docstring von `capabilities.py` nennt vier optionale Apps statt drei
    - `uv run pytest -q` ist grün: die sechs bestehenden Familien sehen dieselben Antworten wie vorher
  </acceptance_criteria>
  <done>Der gemeinsame Parser kennt 201, der 304-Weg bleibt bewusst dem Talk-Client vorbehalten, und ein Werkzeug kann eine fehlende Talk-App erklären, bevor es den ersten Talk-Request baut.</done>
</task>

<task type="auto">
  <name>Task 2: Der Talk-Client mit den vier Leseparametern, der 304 und dem Header-Cursor</name>
  <files>src/mcp_connector/nextcloud/clients/talk.py, tests/fixtures/talk_rooms.json, tests/fixtures/talk_messages.json</files>
  <read_first>
    - src/mcp_connector/nextcloud/clients/tables.py (ganze Datei; Modul-Docstring 1 bis 42, Imports 44 bis 51, Konstanten 53 bis 98, get_rows_simple 149 bis 177, create_row 180 bis 207, _path_id 210 bis 218, _as_list und _as_dict 221 bis 236)
    - src/mcp_connector/nextcloud/clients/ocs.py (ocs_url, ocs_get, ocs_post, parse_ocs nach Task 1)
    - tests/fixtures/deck_boards.json und tests/fixtures/tables_tables.json (Größe und Unaufgeräumtheit einer echten Antwort)
    - .planning/phases/09-talk/09-RESEARCH.md, Abschnitte "Muster 1", "Muster 2", "Muster 3", "Talk-API-Referenz" (beide Parametertabellen, Antwort-Header, Feldauswahl), T7, T8, T9, T14
    - .planning/phases/09-talk/09-PATTERNS.md, Abschnitt "nextcloud/clients/talk.py"
  </read_first>
  <action>
    Neue Datei `src/mcp_connector/nextcloud/clients/talk.py` nach dem Bauplan von `tables.py`.
    Modul-Docstring mit einer Aussage je Absatz: (1) Räume sind API-Version v4, Chat ist v1, und
    diese Trennlinie ist eine Eigenschaft der App und landet nie beim Aufrufer; (2) die zwei
    Pflichtheader nach D-18 setzt `ocs_get` und `ocs_post` schon, deshalb hat dieses Modul
    bewusst keine eigene Header-Konstante (anders als Tables, das eine Generation-1-Route direkt
    baut); (3) die Falle der Familie sind die vier Leseparameter; (4) beide Routen stehen in der
    veröffentlichten `openapi.json` der App, sind also zugesagte API und kein
    Frontend-Innenleben; (5) `MAX_MESSAGES` ist eine erzwungene Grenze und kein Angebot; (6) es
    gibt bewusst keinen Edit-, Delete-, Schedule-, Summarize-, Pin-, Reminder-, Attachment- und
    Share-Pfad und das Wort `silent` steht nirgends in dieser Datei; (7) es gibt kein Retry, auf
    keiner Schicht, weil eine doppelte Nachricht für Dritte sichtbar und von keinem Werkzeug
    dieses Servers entfernbar ist.

    Konstanten, jede mit einem `#:`-Kommentar, der die Begründung trägt und nicht den Namen
    wiederholt: `ROOM_PREFIX = "/apps/spreed/api/v4/room"`,
    `CHAT_PREFIX = "/apps/spreed/api/v1/chat"`, `TALK_WEB_PREFIX = "/index.php/call"`,
    `MAX_MESSAGES = 50` und `READ_ONLY_PARAMS` als Mapping mit genau
    `lookIntoFuture` 0, `setReadMarker` 0, `markNotificationsAsRead` 0, `noStatusUpdate` 1. Der
    Kommentar von `READ_ONLY_PARAMS` ist die wichtigste Prosa der Datei und sagt: das sind keine
    Argumente, weil ein Argument etwas ist, das ein Aufrufer falsch machen kann, und ein falscher
    Wert hier in den Zustand des Nutzers schreibt; `lookIntoFuture` ist in der API ein
    Pflichtparameter ohne Vorgabewert und 0 bedeutet Verlauf statt Long Poll; die anderen drei
    haben in Talk die schreibende Seite als Vorgabe; in spreed 24.0.4 können sie mit diesen
    Werten ohnehin nicht feuern, aber der Code, der sie überspringt, trägt selbst einen
    Kommentar, der sich vorläufig nennt, und dieses Projekt verspricht die Eigenschaft und nicht
    die Version.

    `_path_token(value)` wortwörtlich nach dem Muster von `_path_id`, nur mit
    `re.fullmatch(r"[a-z0-9]{4,30}", text)` statt `.isdigit()`: jede Talk-Route deklariert dieses
    `requirements`-Muster, also ist der Wächter dieser Familie genauso scharf wie der numerische
    von Tables, nur mit anderem Zeichenvorrat. Der ToolError nennt den abgelehnten Wert und sagt
    im Hint, dass ein Token vier bis dreißig Kleinbuchstaben und Ziffern hat und aus
    `talk_browse` kommt.

    `get_rooms(client, creds, *, include_last_message: bool)`: GET auf `ROOM_PREFIX` mit
    `noStatusUpdate` 1 und `includeLastMessage` als einzigem weiteren Parameter. `includeStatus`,
    `modifiedSince` und `threadId` werden nie gesetzt. `include_last_message` ist ein Keyword
    ohne Vorgabewert, damit der Aufrufer die Entscheidung sichtbar trifft: die Liste braucht die
    Vorschau, die Sende-Vorprüfung nicht, und die letzte Nachricht ist der größte Einzelposten
    der Antwort. Rückgabe ist `_as_list(parse_ocs(...))`.

    `get_messages(client, creds, token, *, limit, last_known_message_id=0)` nach Muster 3 aus
    09-RESEARCH.md: `_path_token`, `limit` als Keyword ohne Vorgabewert und im Client auf 1 bis
    `MAX_MESSAGES` gekappt, `lastKnownMessageId` auf mindestens 0 gehoben, dann `ocs_get` mit
    `READ_ONLY_PARAMS` plus diesen zwei Werten. **Vor** `parse_ocs` auf
    `httpx.codes.NOT_MODIFIED` prüfen und `([], None)` zurückgeben. Sonst Nachrichten **und**
    die Id aus dem Antwort-Header `X-Chat-Last-Given` als Tupel zurückgeben, wobei ein fehlender
    oder nicht numerischer Header `None` ergibt. Der Docstring trägt beide Begründungen: 304 ist
    ein Erfolg und keine Weiterleitung, und die Fortsetzung kommt aus dem Header und nie aus den
    Nachrichten-Ids, weil die App den Header aus der ältesten gelesenen Nachricht setzt und erst
    danach unsichtbare und abgelaufene Nachrichten wegfiltert, ein Fenster also Status 200 mit
    leerer Liste und brauchbarem Header sein kann.

    `send_message(client, creds, token, *, message)`: `_path_token`, dann `ocs_post` auf
    `CHAT_PREFIX` mit dem Token und dem Körper, der genau den Schlüssel `message` trägt, dann
    `_as_dict(parse_ocs(...))`. Der Docstring sagt, dass die Antwort 201 ist und nicht 200, dass
    das Body-Schema außerdem `actorDisplayName`, `referenceId`, `replyTo`, `replyToToken`,
    `threadTitle` und `threadId` kennt und keines davon gesetzt wird, und dass es kein Retry gibt.
    Das Wort für stilles Senden wird in diesem Modul nicht buchstabiert, damit ein Gate es
    fernhalten kann.

    `web_url(creds, token)` gibt `f"{creds.base_url}{TALK_WEB_PREFIX}/{token}"` zurück, immer aus
    der konfigurierten Basis-URL und nie aus einer Antwort (SSRF-Grenze).

    `_SHAPE_HINT`, `_as_list` und `_as_dict` eins zu eins aus `tables.py` übernehmen, den
    Hinweistext auf Talk umtexten.

    Dazu die zwei Fixtures. `tests/fixtures/talk_rooms.json` ist die echte Antwortform mit allen
    Beifeldern (Anruf, Lobby, Signaling, Avatar, Aufzeichnung), nicht die Projektion, und trägt
    mindestens fünf Konversationen mit diesen absichtlichen Eigenschaften: die Server-Reihenfolge
    widerspricht der `lastActivity`-Reihenfolge, ein Eintrag hat `isArchived` true, einer
    `readOnly` 1, einer `type` 4, einer `permissions` 0 zusammen mit `attendeePermissions` 0, und
    ein weiterer trägt `permissions` 128 zusammen mit `attendeePermissions` 0 als Regressionsfall
    zu T3. Anzeigenamen mit echten Umlauten. `tests/fixtures/talk_messages.json` trägt eine
    Verlaufsantwort mit Systemnachrichten, einer Nachricht mit `messageParameters` (mindestens ein
    `{actor}` und eine Erwähnung), einer bearbeiteten Nachricht mit `lastEditTimestamp`, einer
    Nachricht über 800 Zeichen und den Pflichtfeldern `reactions`, `parent`, `markdown`,
    `isReplyable`, die die Projektion wegwerfen muss.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run pyright</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run python -c "
import inspect, json, pathlib
from mcp_connector.nextcloud.clients import talk
assert talk.READ_ONLY_PARAMS == {'lookIntoFuture': 0, 'setReadMarker': 0, 'markNotificationsAsRead': 0, 'noStatusUpdate': 1}, talk.READ_ONLY_PARAMS
assert talk.MAX_MESSAGES == 50
for name in ('get_messages', 'get_rooms'):
    p = inspect.signature(getattr(talk, name)).parameters
    key = 'limit' if name == 'get_messages' else 'include_last_message'
    assert p[key].default is inspect.Parameter.empty and p[key].kind is inspect.Parameter.KEYWORD_ONLY, name
src = pathlib.Path(talk.__file__).read_text(encoding='utf-8')
for forbidden in ('.put(', '.patch(', '.delete(', '/schedule', '/summarize', '/reminder', '/pin', '/attachment', '/share', '/read', 'silent'):
    assert forbidden not in src, forbidden
for name in ('talk_rooms.json', 'talk_messages.json'):
    data = json.loads((pathlib.Path('tests/fixtures') / name).read_text(encoding='utf-8'))
    assert data, name
print('client shape ok')
"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run python -c "
import json, pathlib
rooms = json.loads(pathlib.Path('tests/fixtures/talk_rooms.json').read_text(encoding='utf-8'))
assert isinstance(rooms, list) and len(rooms) >= 5, len(rooms)
activity = [r['lastActivity'] for r in rooms]
assert activity != sorted(activity, reverse=True), 'the server order must contradict lastActivity (trap 5)'
assert any(r.get('isArchived') for r in rooms)
assert any(int(r.get('readOnly') or 0) == 1 for r in rooms)
assert any(int(r.get('type') or 0) == 4 for r in rooms)
assert any(int(r.get('permissions') or 0) == 0 and int(r.get('attendeePermissions') or 0) == 0 for r in rooms)
assert any(int(r.get('permissions') or 0) &amp; 128 and int(r.get('attendeePermissions') or 0) == 0 for r in rooms)
print('rooms fixture ok', len(rooms))
"</automated>
  </verify>
  <acceptance_criteria>
    - `talk.READ_ONLY_PARAMS` ist genau das Mapping mit `lookIntoFuture` 0, `setReadMarker` 0, `markNotificationsAsRead` 0 und `noStatusUpdate` 1, und sein `#:`-Kommentar begründet jeden der vier Werte
    - `get_messages` nimmt `limit` als Keyword ohne Vorgabewert; `get_rooms` nimmt `include_last_message` als Keyword ohne Vorgabewert
    - `get_messages` gibt bei HTTP 304 `([], None)` zurück, ohne `parse_ocs` aufzurufen
    - `get_messages` liest die Fortsetzung aus dem Header `X-Chat-Last-Given` und nie aus den Nachrichten
    - `_path_token` lehnt `ABC`, `ab-cd`, `abc` und einen 31 Zeichen langen Wert mit einem ToolError samt Hint ab und baut in diesen Fällen keine URL
    - Der Modultext enthält keines der Zeichenfolgen `.put(`, `.patch(`, `.delete(`, `/schedule`, `/summarize`, `/reminder`, `/pin`, `/attachment`, `/share`, `/read` oder `silent`
    - `web_url` baut die Adresse aus `creds.base_url` und `TALK_WEB_PREFIX`
    - `talk_rooms.json` hat mindestens fünf Einträge, eine Server-Reihenfolge, die der `lastActivity`-Reihenfolge widerspricht, und je einen Eintrag mit `isArchived`, `readOnly` 1, `type` 4, `permissions` 0 bei `attendeePermissions` 0 und `permissions` 128 bei `attendeePermissions` 0
    - `talk_messages.json` enthält Systemnachrichten, `messageParameters`, eine bearbeitete Nachricht, eine Nachricht über 800 Zeichen und die Pflichtfelder, die die Projektion wegwirft
    - `ruff check .`, `ruff format --check .` und `pyright` sind grün
  </acceptance_criteria>
  <done>Der Client kennt die zwei API-Versionen, hält die vier Leseparameter fest, behandelt 304 als leere Antwort, gibt den Header-Cursor mit heraus und hat keinen Pfad, den er nicht haben soll.</done>
</task>

<task type="auto">
  <name>Task 3: Unit-Abdeckung des Clients, behauptet an der gebauten Anfrage</name>
  <files>tests/unit/test_talk_client.py</files>
  <read_first>
    - tests/unit/test_tables_client.py (ganze Datei; Docstring 1 bis 23, eingefrorene URL-Literale 41 bis 48, Envelope-Helfer und Fixtures 50 bis 86, URL-Behauptungen 106 bis 149, Origin-Verbot 187 bis 201, Kein-Request-Muster 270 bis 282, Quellcode-Behauptungen 319 bis 332)
    - src/mcp_connector/nextcloud/clients/talk.py (aus Task 2)
    - tests/fixtures/talk_rooms.json und tests/fixtures/talk_messages.json (aus Task 2)
    - .planning/phases/09-talk/09-RESEARCH.md, Abschnitt "Mechanische Checkliste, Tests", Zeile zu test_talk_client.py
    - .planning/phases/09-talk/09-PATTERNS.md, Abschnitt "tests/unit/test_talk_client.py"
  </read_first>
  <action>
    Neue Datei `tests/unit/test_talk_client.py` mit `respx`, nach dem Bauplan von
    `test_tables_client.py`. Der Modul-Docstring nennt die zwei Eigenschaften, die Vertrag und
    nicht Implementierungsdetail sind und die in einer geparsten Antwort nicht sichtbar wären,
    weshalb sie gegen die gebaute URL geprüft werden: die vier Leseparameter (T6) und die
    Paginierung über den Antwort-Header (T8).

    Eingefrorene URL-Literale oben in der Datei, als Wächter gegen ein Verwechseln der beiden
    API-Versionen: `ROOM_URL` als `{BASE}/ocs/v2.php/apps/spreed/api/v4/room` und `CHAT_URL` als
    `{BASE}/ocs/v2.php/apps/spreed/api/v1/chat/abcd1234`. Envelope-Helfer, `creds`- und
    `client`-Fixture eins zu eins aus dem Analog; `envelope(..., statuscode=201)` ist damit der
    Test für T1 ohne neue Hilfsfunktion.

    Alle Pfade, jeder Testname ein vollständiger Satz über das Verhalten:
    1. Die vier Leseparameter stehen wörtlich in der gebauten Verlaufs-URL, positiv behauptend
       und einzeln geprüft, `lookIntoFuture=0` ausdrücklich als eigene Behauptung (T7).
    2. Ein `limit` über `MAX_MESSAGES` erscheint in der URL als 50, ein `limit` von 0 als 1, und
       der Test nennt `talk.MAX_MESSAGES` als Zahl daneben.
    3. Ein `lastKnownMessageId` unter 0 erscheint als 0.
    4. Status 304 ergibt `([], None)` und ruft `parse_ocs` nicht auf.
    5. Status 200 mit leerer Datenliste und gesetztem `X-Chat-Last-Given` ergibt `([], id)`, also
       eine leere Seite mit brauchbarer Fortsetzung (T8).
    6. Status 200 mit Nachrichten und Header ergibt beide Teile; ein fehlender oder nicht
       numerischer Header ergibt `None` als Fortsetzung.
    7. Die Konversationsliste trägt `noStatusUpdate=1`, sendet `includeLastMessage` in beiden
       Ausprägungen und niemals `includeStatus` oder `modifiedSince`.
    8. Senden mit einem Envelope-Status 201 gibt die Nachricht zurück; der Test enthält die
       Behauptung, dass diese Antwort ein Erfolg ist und kein unerwarteter Status.
    9. Die Sende-Anfrage trägt Content-Type application/json und OCS-APIRequest true und niemals
       einen Origin-Header, und ihr Körper hat genau den Schlüssel `message`.
    10. Der Token-Wächter lehnt `ABC`, `abc`, `ab-cd` und einen 31 Zeichen langen Wert ab, und
        in allen vier Fällen ist `len(route.calls) == 0`, also erreicht nichts Nextcloud
        (`assert_all_called=False` plus Routen-Zählung, das Muster aus dem Analog).
    11. Zwei Quellcode-Behauptungen: das Modul hat keinen der verbotenen Pfade und kein `silent`,
        und die Signaturen erzwingen ihre Grenzen (`limit` und `include_last_message` als
        Keyword ohne Vorgabewert, per `inspect.signature`).

    Testdaten tragen echte Umlaute, wie im Analog (`Grüße`, `Maße`, `übergeben`).
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run pyright &amp;&amp; uv run pytest tests/unit/test_talk_client.py -q</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run pytest -q &amp;&amp; uv run vulture src/mcp_connector vulture_whitelist.py</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; grep -v "^\s*#" tests/unit/test_talk_client.py | grep -c "lookIntoFuture=0" | grep -qv "^0$" &amp;&amp; grep -c "X-Chat-Last-Given" tests/unit/test_talk_client.py &amp;&amp; grep -c "apps/spreed/api/v4/room" tests/unit/test_talk_client.py &amp;&amp; grep -c "apps/spreed/api/v1/chat" tests/unit/test_talk_client.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_talk_client.py -q` ist grün und deckt Happy Path, Fehler, Edge und `no_data` ab
    - Ein Test behauptet alle vier Leseparameter wörtlich in der gebauten URL, `lookIntoFuture=0` zusätzlich einzeln
    - Ein Test belegt Status 304 als `([], None)`, ein zweiter Status 200 mit leerer Liste und gesetztem Header als `([], id)`
    - Ein Test belegt, dass ein Envelope mit Status 201 beim Senden ein Erfolg ist
    - Ein Test belegt, dass die Sende-Anfrage nie einen Origin-Header trägt und ihr Körper genau den Schlüssel `message` hat
    - Vier Testfälle belegen, dass ein ungültiges Token keinen einzigen Request erzeugt (`len(route.calls) == 0`)
    - Die eingefrorenen URL-Literale beider API-Versionen stehen wörtlich in der Datei
    - `uv run vulture src/mcp_connector vulture_whitelist.py` ist grün, ohne neuen Whitelist-Eintrag
    - `uv run pytest -q` ist über die ganze Default-Auswahl grün
  </acceptance_criteria>
  <done>Die zwei Eigenschaften, die man an einer geparsten Antwort nicht sehen kann, sind an der Anfrage festgenagelt, und ein erfundenes Token kostet keinen Request.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Modelleingabe zu URL-Pfad | Das Konversations-Token kommt aus einer Modellantwort und wird Teil eines URL-Pfads |
| Talk-Instanz zu Serverzustand | Ein Lesevorgang dieser API schreibt per Voreinstellung in den Zustand des Nutzers (Lesemarker, Benachrichtigungs-Quittung, Online-Status) |
| Nextcloud-Antwort zu Parser | Statuscode und Header sind Nutzlast dieser Familie und nicht nur Transport |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | `get_messages`, Voreinstellungen der Leseroute | mitigate | `READ_ONLY_PARAMS` als Modulkonstante im Client, nicht als Argument, plus positiv behauptender Unit-Test auf die gebaute URL; ein Tool-Parameter wäre vergessbar und ein Denylist-Gate sieht ein GET auf einen Lese-Endpunkt nicht |
| T-09-02 | Spoofing | `_path_token`, Token aus einer Modellantwort | mitigate | `re.fullmatch(r"[a-z0-9]{4,30}")` vor jedem Request, Ablehnung mit ToolError, und ein Test belegt null Requests im Ablehnungsfall |
| T-09-03 | Elevation of Privilege | Pfadformen dieses Clients | mitigate | Nur drei Pfadformen existieren im Modul; kein Edit-, Delete-, Schedule-, Summarize-, Pin-, Reminder-, Attachment- und Share-Pfad, und das Wort für stilles Senden ist nicht buchstabiert, damit Plan 09-04 es per Gate fernhalten kann |
| T-09-04 | Information Disclosure | `web_url` | mitigate | Die Adresse wird immer aus `creds.base_url` gebaut und nie aus einer Antwort übernommen (SSRF-Grenze der Codebasis) |
| T-09-05 | Repudiation | 201 im Erfolgsraum von `parse_ocs` | mitigate | Erweiterung mit Begründungskommentar plus Unit-Test; heute liefert keine benutzte Route 201, die Erweiterung kann keine bestehende Prüfung umdrehen, und `uv run pytest -q` belegt das für alle sechs bestehenden Familien |
| T-09-06 | Denial of Service | Verlaufsfenster ohne Grenze | mitigate | `limit` ist ein Keyword ohne Vorgabewert und wird im Client auf 1 bis 50 gekappt; eine Verlauf-URL ohne Grenze kann konstruktiv nicht entstehen |
| T-09-07 | Tampering | Retry auf dem Sendeweg | mitigate | Kein Retry in irgendeiner Schicht; die Antwort trägt die Nachrichten-Id, und der Docstring sagt, dass ein Timeout nicht bedeutet, dass nichts gesendet wurde |
| T-09-08 | Information Disclosure | 304 als Konfigurationsfehler gemeldet | accept | Der gemeinsame `_check_transport` bleibt unverändert; die 304 wird an der einen Route abgefangen, an der sie eine Bedeutung hat. Ein zweiter Sonderfall im gemeinsamen Parser wäre teurer als diese lokale Prüfung |
| T-09-SC | Tampering | Paketinstallation | accept | Keine neue Sprachabhängigkeit: `pyproject.toml` und `uv.lock` bleiben unangetastet, es gibt kein `uv add`, kein `pip install` und kein `npm install` in dieser Phase |
</threat_model>

<verification>
1. `uv run pytest -q` (Default-Auswahl) ist grün, inklusive `test_talk_client.py` und `test_ocs_capabilities.py`.
2. `uv run ruff check .`, `uv run ruff format --check .`, `uv run pyright` und `uv run vulture src/mcp_connector vulture_whitelist.py` sind grün.
3. `uv run python scripts/check_tool_budget.py` endet mit Exit-Code 0 und meldet weiterhin 18 Werkzeuge: dieser Plan registriert nichts.
4. `git diff --stat` zeigt keine Änderung an `pyproject.toml`, `uv.lock`, `ids.py`, `provider_map.py`, `tools/chatgpt.py` oder `tools/context.py`.
</verification>

<success_criteria>
- Die zwei blockierenden Transportbefunde (201 beim Senden, 304 beim leeren Verlauf) sind erledigt, bevor irgendein Werkzeug sie treffen kann.
- Der Talk-Client baut genau drei Pfadformen und hält die vier Leseparameter, die Erfolgskriterium 3 der Phase verspricht, an der Anfrage fest.
- Die App-Erkennung kennt `spreed` und liefert die Chat-Höchstlänge der Instanz mit, damit die Kappe von TALK-03 nicht doppelt gepflegt wird.
- Keine bestehende Familie ändert ihr Verhalten.
</success_criteria>

<output>
Create `.planning/phases/09-talk/09-01-SUMMARY.md` when done.
Die Zusammenfassung nennt den gewählten Rückgabetyp von `get_messages`, die Behandlung eines
fehlenden `X-Chat-Last-Given`-Headers und die Feldnamen der drei neuen Capabilities-Felder,
weil Plan 09-03 gegen genau diese Signaturen baut.
</output>
