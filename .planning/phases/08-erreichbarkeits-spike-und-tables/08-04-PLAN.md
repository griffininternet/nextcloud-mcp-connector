---
phase: 08-erreichbarkeits-spike-und-tables
plan: 04
type: execute
wave: 3
depends_on: ["08-03"]
files_modified:
  - src/mcp_connector/server/reg_tables.py
  - tests/contract/test_tool_surface.py
  - tests/contract/test_no_destructive_calls.py
  - scripts/check_tool_budget.py
  - scripts/acceptance_all_tools.py
  - README.md
  - README.de.md
  - README.fr.md
  - docs/client-setup.md
  - docs/conference-demo.md
  - CHANGELOG.md
autonomous: true
requirements: [TABLES-01, TABLES-02]

must_haves:
  truths:
    - "tools/list nennt 18 Werkzeuge, darunter tables_browse und tables_create_row, und die Zahl steht identisch in Registry, Contract-Test, Skripten und den drei READMEs"
    - "level ist im Input-Schema ein enum mit genau den Werten tables, columns, rows, und kein Schema trägt $defs"
    - "Die Annotationen sagen die Wahrheit: tables_browse liest nur, tables_create_row legt an und ist weder destruktiv noch idempotent"
    - "Es existiert kein Pfad zum Ändern oder Löschen einer Zeile, einer Spalte oder eines Schemas: das Gate hält das mit Nadeln fest, und eine Gegenprobe zeigt, dass das Gate auslöst"
    - "Das Budget-Gate steht auf einer neuen datierten Messung plus 15 Prozent und behauptet zusätzlich, dass kein einzelnes Werkzeug 1400 Bytes überschreitet"
  artifacts:
    - path: "src/mcp_connector/server/reg_tables.py"
      provides: "Registrierung beider Tables-Werkzeuge mit Literal-Enum, READ_ONLY und CREATE_ONLY"
      contains: "Literal"
      min_lines: 60
    - path: "tests/contract/test_tool_surface.py"
      provides: "Eingefrorene Mengen auf 18, Enum-Test und Verbotsliste für Tables"
      contains: "tables_create_row"
    - path: "tests/contract/test_no_destructive_calls.py"
      provides: "Nadeln für die Tables-Schreibrouten plus Gegenproben"
      contains: "apps/tables/api/1/rows"
    - path: "scripts/check_tool_budget.py"
      provides: "Neue datierte Messzeile, angehobenes BUDGET_BYTES und die Pro-Werkzeug-Grenze"
      contains: "MAX_TOOL_BYTES"
  key_links:
    - from: "src/mcp_connector/server/reg_tables.py"
      to: "src/mcp_connector/tools/tables.py"
      via: "tables_tools.browse und tables_tools.create_row hinter compact und graceful"
      pattern: "tables_tools\\.(browse|create_row)"
    - from: "src/mcp_connector/server/__init__.py"
      to: "src/mcp_connector/server/reg_tables.py"
      via: "_load_registrations() importiert jedes reg_*-Modul automatisch"
      pattern: "_load_registrations"
    - from: "tests/contract/test_tool_surface.py"
      to: "README.md, README.de.md, README.fr.md"
      via: "test_the_readme_permission_table_matches_the_live_registry parst die Tabellenzellen"
      pattern: "readme"
---

<objective>
Die Werkzeuge sichtbar machen und alle eingefrorenen Zahlen und Gates in einem Zug mitziehen, so
dass die Registry nie ein rotes Fenster hat.

Purpose: Sobald `reg_tables.py` existiert, sind fünf Stellen im Contract-Test, zwei Skripte, drei
READMEs und mehrere Doku-Seiten mit ihrer alten Zahl falsch, und das Budget-Gate reißt
rechnerisch (Messung 11268 Bytes bei einem Gate von 12500, plus rund 1310 Bytes für die zwei
neuen Schemata). Registrierung und Nachzug gehören deshalb in denselben Plan. Zusätzlich wird
das Destruktiv-Gate um die Tables-Schreibrouten erweitert, die kein verbotenes Verb brauchen,
weil ein POST auf eine Zeilen-Route sie ändert.

Output: `reg_tables.py`, die angepassten Contract-Tests, das neu verankerte Budget-Gate, das
Acceptance-Skript, die drei READMEs, die Doku-Zahlen und ein Changelog-Eintrag.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-erreichbarkeits-spike-und-tables/08-RESEARCH.md
@.planning/phases/08-erreichbarkeits-spike-und-tables/08-PATTERNS.md
@.planning/phases/08-erreichbarkeits-spike-und-tables/08-03-SUMMARY.md

Vorbild für die Registrierung:
@src/mcp_connector/server/reg_deck.py

Annotationen, compact, graceful und der Auto-Import:
@src/mcp_connector/server/__init__.py

Die beiden Contract-Tests, die angepasst werden:
@tests/contract/test_tool_surface.py
@tests/contract/test_no_destructive_calls.py

<interfaces>
Aus src/mcp_connector/server/__init__.py:
  READ_ONLY = ToolAnnotations(read_only_hint=True, open_world_hint=False)
  CREATE_ONLY = ToolAnnotations(read_only_hint=False, destructive_hint=False,
                               idempotent_hint=False, open_world_hint=False)
  def compact(payload: object) -> str
  graceful                     Dekorator, steht direkt unter @mcp.tool(...)
  mcp                          das MCPServer-Objekt
  _load_registrations()        importiert jedes reg_*-Modul per pkgutil, kein Eintrag nötig

Aus src/mcp_connector/tools/tables.py (Plan 03):
  LEVELS = ("tables", "columns", "rows"); DEFAULT_LIMIT = 25; MAX_LIMIT = 200
  async def browse(clients, level="tables", table_id=None, limit=DEFAULT_LIMIT, cursor=None) -> dict
  async def create_row(clients, table_id, values) -> dict

Aktueller Stand der eingefrorenen Zahlen (heute im Repo gemessen):
  tests/contract/test_tool_surface.py: EXPECTED_TOOLS (Zeile 34), CREATE_TOOLS (Zeile 54, vier
    Einträge), Docstring "16 tools, and the diet holds for 14 of them" (Zeile 307),
    assert len(tools) == 16 (Zeile 312), Docstring "four create-only tools, twelve pure reads"
    (Zeile 409), Assertion-Meldung "all 16 schemas" (Zeile 457), Beispielzahl im Docstring des
    Doku-Wächters (Zeile 497)
  scripts/acceptance_all_tools.py: "call all 15 tools once" (Zeile 1), "all 15 tools answered"
    (Zeile 16), EXPECTED_TOOLS = 15 (Zeile 44), Fehlermeldung (Zeilen 103 bis 104)
  scripts/check_tool_budget.py: Messzeile 2026-08-14 (Zeile 19), BUDGET_BYTES = 12_500 (Zeile 27),
    per_tool-Auswertung (Zeilen 37 bis 47), Fehlerzweig mit return 1 (Zeilen 49 bis 52)
  README.md: "The 16 tools" (Zeile 19), "All 16 tools" (Zeile 30), Deck-Tabellenzeilen (196, 197)
  README.de.md: Zeilen 21 und 32, Deck-Tabellenzeilen (203, 204)
  README.fr.md: Zeilen 21 und 34, Deck-Tabellenzeilen (206, 207)
  docs/client-setup.md: Zeilen 11, 74 und 431 (Zeile 431 ist ein datierter Messwert)
  docs/conference-demo.md: tools=16 (Zeile 140), zitierte Fehlermeldung des Acceptance-Skripts
    (Zeile 271)

Zielzahl dieser Phase: 18 Werkzeuge, davon fünf create-only (files_upload,
calendar_create_event, notes_create, deck_create_card, tables_create_row) und dreizehn rein
lesende.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Registrierung und die fünf eingefrorenen Stellen im Oberflächen-Test</name>
  <files>src/mcp_connector/server/reg_tables.py, tests/contract/test_tool_surface.py</files>
  <read_first>
    - src/mcp_connector/server/reg_deck.py (ganze Datei, 73 Zeilen)
    - src/mcp_connector/server/__init__.py (READ_ONLY, CREATE_ONLY, compact, graceful, Zeilen 53 bis 99; _load_registrations Zeilen 102 bis 114)
    - src/mcp_connector/tools/tables.py (Signaturen aus Plan 03)
    - tests/contract/test_tool_surface.py (ganze Datei, insbesondere Zeilen 28 bis 54, 199 bis 236, 300 bis 320, 405 bis 460, 471 bis 527)
    - .planning/phases/08-erreichbarkeits-spike-und-tables/08-PATTERNS.md, Abschnitte "server/reg_tables.py" und "tests/contract/test_tool_surface.py"
    - .planning/phases/08-erreichbarkeits-spike-und-tables/08-RESEARCH.md, Abschnitt "Falle 7"
  </read_first>
  <action>
    Neue Datei `src/mcp_connector/server/reg_tables.py` eins zu eins nach `reg_deck.py`. Kein
    Eintrag in `server/__init__.py`: `_load_registrations()` importiert jedes `reg_*`-Modul per
    pkgutil automatisch, genau damit parallel laufende Pläne keine gemeinsame Datei anfassen.

    Modul-Docstring nach dem Vorbild: dass die Logik in `mcp_connector.tools.tables` liegt, dass
    `level` ein `Literal` und damit ein `enum` im Input-Schema ist statt eines freien Strings
    (D-06, D-14), und dass beide Werkzeuge unbedingt gelistet werden, auch auf einer Instanz
    ohne Tables-App, weil ein credential-abhängiges `tools/list` nicht cachebar ist, das
    Budget-Gate bricht und Clients überrascht, die Werkzeuglisten speichern (SRV-04).

    `tables_browse` mit `@mcp.tool(annotations=READ_ONLY, structured_output=False)` und
    `@graceful` darunter. Parameter mit `Annotated` und `Field`: `level` als
    `Literal["tables", "columns", "rows"]` mit Default `"tables"`, `table_id` als `str` mit
    Default leerer String, `limit` als `int` mit `ge=1`, `le=tables_tools.MAX_LIMIT` und Default
    `tables_tools.DEFAULT_LIMIT`, `cursor` als `str` mit Default leerer String, `ctx: Context |
    None = None`. Leere Strings statt `None` als Default, damit kein `anyOf` aus String und Null
    ins Schema kommt. Der Rumpf holt `deps.resolve_clients(ctx)` und gibt das Ergebnis von
    `tables_tools.browse` durch `compact` zurück, wobei leere Strings vor der Übergabe zu
    `None` werden.

    `tables_create_row` mit `@mcp.tool(annotations=CREATE_ONLY, structured_output=False)` und
    `@graceful`. Parameter: `table_id` als `str` mit der Beschreibung, dass die Id aus
    `tables_browse` kommt, `values` als `str` mit einer Beschreibung, die die Form nennt (ein
    JSON-Objekt aus Spaltentiteln und Werten, mit einem kurzen Beispiel), und `ctx`. Die
    Werkzeug-Beschreibung enthält zwei Sätze, die im Betrieb Geld wert sind: dass nur angelegt
    und niemals eine bestehende Zeile geändert oder gelöscht wird, und dass ein Timeout nicht
    bedeutet, dass nichts geschrieben wurde, weshalb der nächste Schritt ein Nachlesen mit
    `tables_browse(level="rows")` ist und kein zweiter Aufruf.

    Beide Beschreibungen bleiben knapp: das Budget-Gate erlaubt nach Task 2 kein einzelnes
    Werkzeug über 1400 Bytes, und `calendar_create_event` liegt heute mit 1351 Bytes schon
    knapp darunter.

    Danach `tests/contract/test_tool_surface.py` an genau fünf Stellen nachziehen:
    1. `EXPECTED_TOOLS` um `tables_browse` und `tables_create_row` erweitern; `CREATE_TOOLS` um
       `tables_create_row`, und den Kommentar darüber von "The four write paths" auf fünf
       ändern.
    2. `assert len(tools) == 16` auf 18 setzen, samt Meldung ("the curated set is 18 tools, no
       more and no fewer"), und den Docstring derselben Funktion von "16 tools, and the diet
       holds for 14 of them" auf 18 beziehungsweise 16 korrigieren.
    3. Den Docstring "four create-only tools, twelve pure reads" auf "five create-only tools,
       thirteen pure reads" und die Assertion-Meldung "all 16 schemas" auf "all 18 schemas"
       ändern; die Beispielzahl im Docstring des Doku-Wächters mitziehen.
    4. Einen neuen Test nach dem Vorbild
       `test_the_two_deck_tools_are_listed_and_browse_takes_an_enum_level` anlegen: beide
       Tables-Werkzeuge sind gelistet, beide haben `output_schema is None`, `tables_browse` hat
       `read_only_hint` gleich True und `open_world_hint` gleich False, das Level-Enum im Schema
       ist genau die Liste tables, columns, rows, `$defs` kommt im Schema nicht vor, und
       `tables_create_row` hat `read_only_hint` False, `destructive_hint` False,
       `idempotent_hint` False und `open_world_hint` False.
    5. Einen neuen Test nach dem Vorbild `test_there_is_no_tool_per_deck_level` mit der
       Verbotsliste aus `tables_list_tables`, `tables_list_columns`, `tables_list_rows` und
       `tables_read_row`.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector && uv run ruff check . && uv run ruff format --check . && uv run pyright src/mcp_connector/server/reg_tables.py && uv run pytest tests/contract/test_tool_surface.py -q</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector && uv run python scripts/check_tool_budget.py; uv run python -c "import json,subprocess,sys; print('budget script exit code recorded above')"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector && grep -v '^\s*#' tests/contract/test_tool_surface.py | grep -c "== 16\|all 16\|16 tools" | grep -qx 0 && grep -c "tables_browse\|tables_create_row" tests/contract/test_tool_surface.py</automated>
  </verify>
  <acceptance_criteria>
    - `reg_tables.py` existiert und wird ohne Eintrag in `server/__init__.py` geladen: ein In-Memory-Client gegen `mcp` listet `tables_browse` und `tables_create_row`
    - Die Registry meldet 18 Werkzeuge; `uv run pytest tests/contract/test_tool_surface.py -q` ist grün
    - Das Level-Enum im Input-Schema von `tables_browse` ist genau die Liste tables, columns, rows
    - Keines der beiden neuen Schemata enthält `$defs`, und beide Werkzeuge haben `output_schema is None`
    - `tables_browse` hat `read_only_hint` True, `tables_create_row` hat `read_only_hint` False, `destructive_hint` False und `idempotent_hint` False, jeweils `open_world_hint` False
    - `tests/contract/test_tool_surface.py` enthält außerhalb von Kommentaren keine Vorkommen von "== 16", "all 16" oder "16 tools" mehr
    - `CREATE_TOOLS` hat fünf Einträge und ist eine echte Teilmenge von `EXPECTED_TOOLS`
    - Ein Test verbietet `tables_list_tables`, `tables_list_columns`, `tables_list_rows` und `tables_read_row`
  </acceptance_criteria>
  <done>Die zwei Werkzeuge sind Teil des kuratierten Satzes, ihr Level ist ein Enum, ihre Annotationen sagen die Wahrheit, und die fünf eingefrorenen Stellen nennen 18.</done>
</task>

<task type="auto">
  <name>Task 2: Destruktiv-Gate erweitern und Budget-Gate neu verankern</name>
  <files>tests/contract/test_no_destructive_calls.py, scripts/check_tool_budget.py, scripts/acceptance_all_tools.py</files>
  <read_first>
    - tests/contract/test_no_destructive_calls.py (ganze Datei, insbesondere FORBIDDEN Zeilen 29 bis 48, ALLOWED_MODULE_STATE Zeilen 79 bis 87, die Gate-Gegenprobe Zeilen 170 bis 184, die Ausnahme-Gegenproben Zeilen 187 bis 232, die Konstanten-Ausnahme Zeile 256)
    - scripts/check_tool_budget.py (ganze Datei: Messkommentar 16 bis 27, per_tool 37 bis 47, Fehlerzweig 49 bis 52)
    - scripts/acceptance_all_tools.py (Zeilen 1, 16, 44 und die Fehlermeldung 103 bis 104)
    - src/mcp_connector/nextcloud/clients/tables.py (welche Routen das Modul wirklich baut)
    - .planning/phases/08-erreichbarkeits-spike-und-tables/08-RESEARCH.md, Abschnitte "Falle 6", "Mechanische Checkliste, Tests", "Offene Fragen 2"
    - .planning/phases/08-erreichbarkeits-spike-und-tables/08-PATTERNS.md, Abschnitte "tests/contract/test_no_destructive_calls.py", "scripts/check_tool_budget.py", "scripts/acceptance_all_tools.py"
  </read_first>
  <action>
    In `tests/contract/test_no_destructive_calls.py` das `FORBIDDEN`-Dictionary um die
    Tables-Schreibrouten erweitern, die kein verbotenes Verb brauchen, weil ein POST auf eine
    Zeilen-Route die Zeile ändert. Form ist "Pfadfragment als Schlüssel, Begründungssatz als
    Wert". Die Nadeln: das Fragment der v1-Einzelzeilen-Routen (lesen, ändern und löschen einer
    Zeile liegen dort), das Fragment der v1-Spalten-Routen (kein Spalten-Schreibpfad), das
    Schema-Fragment `tables/scheme` (kein Schema-Import und kein Schema-Export), das
    Transfer-Fragment (kein Eigentumsübergang) und das Share-Fragment der v2-Generation
    (Pfadform nodeCollection, nodeId, share).

    Harte Nebenbedingung bei der Wahl der Nadeln: `clients/tables.py` baut die erlaubte
    Create-Route unter `tables/{id}/rows` und die erlaubte Lese-Route
    `tables/{id}/rows/simple`. Die Nadeln müssen so gewählt sein, dass sie diese beiden
    erlaubten Formen nicht treffen. Vor dem Commit prüfen, dass das Gate gegen den aktuellen
    Quellcode grün ist und die Gegenproben trotzdem auslösen; wenn eine Nadel zu breit ist,
    wird sie präziser gefasst und nicht das Modul umgebaut.

    Je neue Nadel eine Gegenprobe nach dem Vorbild der drei bestehenden Ausnahmen-Tests: ein
    Test, der den gefilterten Quelltext der Zieldatei nimmt, eine Zeile mit der verbotenen Route
    anhängt und behauptet, dass die Prüfung darauf anschlägt, und der zugleich behauptet, dass
    der echte Quellcode nicht getroffen wird.

    `ALLOWED_MODULE_STATE` bleibt bei genau zwei Einträgen: die beiden neuen Module dürfen
    keinen eigenen Cache und keine modulweite Liste haben. Wird der Test hier rot, liegt der
    Fehler im Modul und nicht im Gate.

    Dann `scripts/check_tool_budget.py`: zuerst das Skript laufen lassen und den gemessenen Wert
    für 18 Werkzeuge notieren. Im Kommentarblock eine neue Zeile in derselben Schreibweise wie
    die bestehende ergänzen, also "Measurement <heutiges Datum>, all 18 curated tools
    registered: <gemessen> bytes" plus die Rechnung "Budget <gemessen> + 15 percent = <Wert>,
    rounded up to the next 500 = <Gate>". `BUDGET_BYTES` auf diesen Wert setzen. Die bestehende
    Zeile vom 2026-08-14 bleibt stehen, damit eine Regression zuordenbar bleibt.

    Zusätzlich die zweite Behauptung einziehen, die eine Aggregatzahl mit Luft nie meldet: eine
    Konstante `MAX_TOOL_BYTES = 1400` mit einem Kommentar, der begründet, warum sie existiert
    (der heutige Ausreißer `calendar_create_event` liegt bei 1351 Bytes; ein neues Werkzeug mit
    Absatzbeschreibung fällt darüber, und genau das ist die Regression, die die Gesamtzahl
    nicht sieht). Datenquelle ist die vorhandene `per_tool`-Liste; es fehlt nur der Vergleich und
    ein zweiter Fehlerzweig mit `return 1` nach dem Vorbild des bestehenden, dessen Ausgabe das
    überschreitende Werkzeug samt Größe nennt.

    Dann `scripts/acceptance_all_tools.py`: `EXPECTED_TOOLS` von 15 auf 18 setzen und die Zahl im
    Modul-Docstring (Zeile 1) und in Zeile 16 mitziehen. Das ist die im v1.1-Audit notierte
    Altlast: das Skript stand noch auf 15, während die Registry schon 16 lieferte. Die
    Fehlermeldung des Skripts wird in `docs/conference-demo.md` wörtlich zitiert und wandert in
    Task 3 mit.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector && uv run pytest tests/contract/ -q && uv run ruff check . && uv run ruff format --check .</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector && uv run python scripts/check_tool_budget.py && uv run python -c "
import re
src = open('scripts/check_tool_budget.py', encoding='utf-8').read()
assert 'MAX_TOOL_BYTES' in src and '1400' in src
lines = [l for l in src.splitlines() if 'Measurement' in l]
assert len(lines) >= 2, 'the old measurement line must stay so a regression is attributable'
assert any('18 curated tools' in l for l in lines), 'a dated measurement line for 18 tools is required'
budget = int(re.search(r'BUDGET_BYTES = ([0-9_]+)', src).group(1).replace('_',''))
assert budget % 500 == 0, 'the gate is rounded up to the next 500'
print('budget', budget)
"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector && grep -n "EXPECTED_TOOLS = 18" scripts/acceptance_all_tools.py && grep -c "15 tools" scripts/acceptance_all_tools.py | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/contract/ -q` ist grün, inklusive der neuen Gegenproben
    - `FORBIDDEN` trägt fünf neue Nadeln, jede mit einem Begründungssatz als Wert
    - Für jede neue Nadel existiert ein Test, der belegt, dass eine künstlich angehängte Zeile mit dieser Route erkannt wird und der echte Quellcode nicht getroffen wird
    - Die erlaubten Routen `tables/{id}/rows` und `tables/{id}/rows/simple` aus `clients/tables.py` lösen das Gate nicht aus
    - `ALLOWED_MODULE_STATE` hat weiterhin genau zwei Einträge
    - `scripts/check_tool_budget.py` endet mit Exit-Code 0; `BUDGET_BYTES` ist durch 500 teilbar, der Kommentarblock enthält mindestens zwei Messzeilen und eine davon nennt 18 Werkzeuge
    - `MAX_TOOL_BYTES` ist 1400, und ein zweiter Fehlerzweig gibt bei Überschreitung Name und Größe des Werkzeugs aus und liefert Exit-Code 1
    - `scripts/acceptance_all_tools.py` hat `EXPECTED_TOOLS = 18` und enthält nirgends mehr die Zahl 15 in einer Aussage über die Werkzeugzahl
  </acceptance_criteria>
  <done>Kein Werkzeug kann eine Zeile ändern, eine Spalte anlegen oder ein Schema importieren, ohne dass ein Gate mit Gegenprobe rot wird, und das Budget-Gate ist auf eine datierte Messung verankert statt einmalig angehoben.</done>
</task>

<task type="auto">
  <name>Task 3: Dreisprachige READMEs, Doku-Zahlen und Changelog</name>
  <files>README.md, README.de.md, README.fr.md, docs/client-setup.md, docs/conference-demo.md, CHANGELOG.md</files>
  <read_first>
    - README.md (Zeilen 15 bis 35 und die Tool-Tabelle 185 bis 205, plus der Deck-Abschnitt 275 bis 295)
    - README.de.md (Zeilen 17 bis 36 und die Tool-Tabelle 195 bis 215)
    - README.fr.md (Zeilen 17 bis 38 und die Tool-Tabelle 198 bis 215)
    - docs/client-setup.md (Zeilen 8 bis 14, 70 bis 78, 425 bis 435)
    - docs/conference-demo.md (Zeilen 135 bis 145, 265 bis 275)
    - CHANGELOG.md (Block 0.1.3, Zeilen 1 bis 40)
    - tests/contract/test_tool_surface.py (der README- und Doku-Wächter, Zeilen 471 bis 527)
    - .planning/phases/08-erreichbarkeits-spike-und-tables/08-PATTERNS.md, Abschnitte "README.md, README.de.md, README.fr.md" und "CHANGELOG.md"
  </read_first>
  <action>
    In allen drei READMEs je zwei Zeilen in die Tool-Tabelle direkt unter die Deck-Zeilen
    einfügen. Das Format ist bindend, weil
    `test_the_readme_permission_table_matches_the_live_registry` die Zellen parst: erste Zelle der
    Werkzeugname in Backticks, zweite Zelle wörtlich `read` beziehungsweise `create-only`, dritte
    Zelle die Beschreibung. Englisch: `tables_browse` mit "read" und einer Beschreibung über das
    Durchsehen von Tabellen, Spalten und Zeilen; `tables_create_row` mit "create-only" und einer
    Beschreibung, die sagt, dass Spaltentitel statt Ids benutzt werden und dass bestehende Zeilen
    nie geändert werden. Dieselben zwei Zeilen übersetzt in `README.de.md` und `README.fr.md`,
    mit echten Umlauten beziehungsweise Akzenten und Cedille.

    Die Werkzeugzahl in allen drei READMEs von 16 auf 18 ziehen: `README.md` Zeilen 19 und 30,
    `README.de.md` Zeilen 21 und 32, `README.fr.md` Zeilen 21 und 34. Danach mit grep prüfen,
    dass in keiner der drei Dateien eine Produktaussage mit 16 übrig ist.

    Vokabular-Gate: Tables hat zur Seite gelegte Tabellen. Die deutschen und französischen Texte
    dürfen das verbotene Wort nicht benutzen, also "zur Seite gelegte Tabellen" beziehungsweise
    eine gleichwertige französische Formulierung. Im englischen Code bleibt das Feld `archived`
    unberührt, weil es ein Feldname der App ist.

    `docs/client-setup.md`: die Zahlen in den Zeilen 11 und 74 auf 18 ziehen. Zeile 431 ist ein
    datierter Messwert eines Laufs und darf nach der Regel des Contract-Tests stehenbleiben, wenn
    die Seite auf `tests/contract/test_tool_surface.py` zeigt. Prüfen, ob dieser Zeiger auf der
    Seite steht; fehlt er, ergänzen statt die Zahl zu fälschen.

    `docs/conference-demo.md`: `tools=16` in Zeile 140 auf 18 ziehen und die wörtlich zitierte
    Fehlermeldung des Acceptance-Skripts in Zeile 271 an die neue Zahl anpassen, weil Task 2 das
    Skript auf 18 gesetzt hat.

    Danach den Doku-Wächter laufen lassen und jede weitere Seite behandeln, die er meldet
    (`docs/oauth-setup.md`, `docs/spike-discovery.md` und `docs/store-submission.md` enthalten
    datierte Messwerte alter Läufe): entweder den Zeiger auf die Contract-Testdatei ergänzen
    oder die Aussage aktualisieren. Der Test entscheidet, welche Seite welchen Weg braucht, nicht
    die Vermutung.

    `CHANGELOG.md`: unter einer noch nicht veröffentlichten Fassung einen `### Added`-Abschnitt
    nach dem Muster des Blocks 0.1.3 anlegen, also ganze Sätze aus Nutzersicht, keine Tickets,
    keine Dateinamen, keine Emojis: Tabellen, Spalten und Zeilen lesen mit gekappten Antworten,
    und eine Zeile mit Spaltentiteln anlegen, wobei unbekannte, mehrdeutige und fehlende
    Pflichttitel vor dem Schreiben abgelehnt werden. Das Release selbst ist Phase 11 (EXAPP-07);
    dieser Plan schreibt keine Version und setzt keinen Tag.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector && uv run pytest tests/contract/test_tool_surface.py -q && uv run pytest -q</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector && for f in README.md README.de.md README.fr.md; do grep -c "tables_browse" "$f"; grep -c "tables_create_row" "$f"; done && grep -c "18" README.md README.de.md README.fr.md</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector && python -c "
import io, sys
bad = []
for name in ('README.md','README.de.md','README.fr.md','docs/client-setup.md','docs/conference-demo.md','CHANGELOG.md'):
    text = io.open(name, encoding='utf-8').read()
    if chr(0x2014) in text or chr(0x2013) in text:
        bad.append((name, 'dash'))
for name in ('README.de.md','README.fr.md','CHANGELOG.md'):
    text = io.open(name, encoding='utf-8').read().lower()
    if 'archiv' in text:
        bad.append((name, 'forbidden vocabulary'))
print(bad)
sys.exit(1 if bad else 0)
"</automated>
  </verify>
  <acceptance_criteria>
    - Jede der drei READMEs enthält genau eine Tabellenzeile für `tables_browse` mit der zweiten Zelle `read` und genau eine für `tables_create_row` mit der zweiten Zelle `create-only`
    - Keine der drei READMEs enthält noch eine Produktaussage mit der Zahl 16; beide Stellen je Datei nennen 18
    - `test_the_readme_permission_table_matches_the_live_registry` ist grün, ebenso `test_a_documented_tool_count_is_the_current_one_or_says_which_run_it_is_from`
    - `docs/conference-demo.md` nennt `tools=18` und die zitierte Fehlermeldung passt zur neuen Zahl im Skript
    - Jede Doku-Seite, die der Wächter meldet, hat entweder die aktuelle Zahl oder den Zeiger auf `tests/contract/test_tool_surface.py`
    - `CHANGELOG.md` hat einen `### Added`-Abschnitt mit zwei Sätzen aus Nutzersicht; es wird keine Versionsnummer geändert und kein Tag gesetzt
    - Keine der geänderten Dateien enthält ein Em-Dash oder En-Dash
    - Die deutschen und französischen Texte enthalten das verbotene Wort nicht, benutzen echte Umlaute beziehungsweise Akzente und keine Emojis
    - `uv run pytest -q` ist über die ganze Default-Auswahl grün
  </acceptance_criteria>
  <done>Die drei Sprachfassungen, die Doku und der Changelog sagen dasselbe wie die Registry, und die Kappung der Zahlen ist an keiner Stelle geraten.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Registry zu MCP-Client | `tools/list` ist die Zusage, die jeder Client cacht; Annotationen und Schemata sind Teil dieser Zusage |
| Quellcode zu Gate | Das Destruktiv-Gate ist die einzige Instanz, die verhindert, dass eine schreibende Route unbemerkt dazukommt |
| Doku zu Nutzer | Eine README-Zeile über Schreibgrenzen ist eine Sicherheitsaussage, keine Kosmetik |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-20 | Tampering | Neue Tables-Schreibroute im Client | mitigate | Fünf neue `FORBIDDEN`-Nadeln für Einzelzeilen-, Spalten-, Schema-, Transfer- und Share-Routen, jede mit einer eigenen Gegenprobe, die belegt, dass die Nadel auslöst und den echten Quellcode nicht fälschlich trifft |
| T-08-21 | Repudiation | Annotationen von `tables_create_row` | mitigate | Der Contract-Test behauptet `read_only_hint` False, `destructive_hint` False und `idempotent_hint` False; die Annotation ist ehrlich, weil im Client kein Update- und kein Delete-Pfad existiert |
| T-08-22 | Denial of Service | `tools/list`-Größe | mitigate | Neue datierte Messzeile plus Gate auf Messung plus 15 Prozent, aufgerundet auf 500, und zusätzlich `MAX_TOOL_BYTES = 1400` je Werkzeug: ein Gate mit viel Luft meldet keine Regression, deshalb die zweite Behauptung |
| T-08-23 | Tampering | Modulweiter veränderlicher Zustand | mitigate | `ALLOWED_MODULE_STATE` bleibt bei genau zwei namentlich gelisteten Einträgen; ein Dictionary, das eine Anfrage überlebt, ist einen Refactor von einem Session-Store entfernt und bricht den Restart-Beweis |
| T-08-24 | Information Disclosure | Werkzeug-Beschreibungen und README-Zeilen | mitigate | Die Beschreibung von `tables_create_row` nennt ausdrücklich, dass ein Timeout nicht bedeutet, dass nichts geschrieben wurde, damit ein Modell nachliest statt einen zweiten Datensatz anzulegen |
| T-08-25 | Repudiation | Doku-Zahlen | accept | Datierte Messwerte alter Läufe dürfen stehenbleiben, wenn die Seite auf `tests/contract/test_tool_surface.py` zeigt. Das ist die bestehende Regel des Wächters und wird nicht neu verhandelt |
| T-08-SC | Tampering | Paketinstallation | accept | Keine neue Sprachabhängigkeit; `pyproject.toml` und `uv.lock` bleiben unangetastet |
</threat_model>

<verification>
1. `uv run pytest -q` (Default-Auswahl) ist grün, inklusive beider Contract-Test-Dateien.
2. `uv run python scripts/check_tool_budget.py` endet mit Exit-Code 0 und gibt 18 Werkzeuge aus.
3. `uv run ruff check .`, `uv run ruff format --check .`, `uv run pyright` und `uv run vulture src/mcp_connector vulture_whitelist.py` sind grün.
4. `uv run python scripts/acceptance_all_tools.py` erwartet 18 Werkzeuge (der volle Lauf gegen eine Instanz gehört zu Plan 05).
5. Kein Em-Dash und kein En-Dash in den geänderten Textdateien, kein verbotenes Vokabular in den deutschen und französischen Texten.
</verification>

<success_criteria>
- Der zweite Teil von Erfolgskriterium 5 der Phase ist erfüllt: es existiert kein Pfad zum Ändern oder Löschen einer Zeile oder eines Schemas, das Gate hält, und die Gegenprobe ist vorhanden.
- Werkzeugzahl 18 steht identisch in Registry, Contract-Tests, beiden Skripten, den drei READMEs und der Doku.
- Das Budget-Gate ist auf eine datierte Messung verankert und meldet zusätzlich ein einzelnes zu großes Werkzeug.
- Der Changelog nennt die nutzerrelevante Änderung, ohne eine Version zu setzen.
</success_criteria>

<output>
Create `.planning/phases/08-erreichbarkeits-spike-und-tables/08-04-SUMMARY.md` when done.
Die Zusammenfassung nennt den gemessenen Byte-Wert, das neue Gate und die Größe der zwei neuen
Werkzeuge, weil Phase 11 (TOOL-15) das Gate auf dieser Messung fortschreibt.
</output>
</content>
