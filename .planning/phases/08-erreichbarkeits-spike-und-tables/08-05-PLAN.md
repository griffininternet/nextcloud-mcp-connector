---
phase: 08-erreichbarkeits-spike-und-tables
plan: 05
type: execute
wave: 4
depends_on: ["08-01", "08-04"]
files_modified:
  - tests/integration/test_tables_roundtrip.py
  - tests/integration/test_permission_fidelity_exapp.py
autonomous: true
requirements: [TABLES-01, TABLES-02]

must_haves:
  truths:
    - "Gegen eine echte Nextcloud mit installierter Tables-App legt der Nutzer eine Zeile mit Spaltentiteln an und liest sie über tables_browse mit level=rows zurück"
    - "Die Capabilities der laufenden Instanz melden tables_available und beide API-Generationen 1.0 und 2.0"
    - "Die live gebaute Zeilen-URL trägt limit, gemessen an der echten Anfrage und nicht an einer Mock-Antwort"
    - "Eine unbekannte Tabelle wird mit einem Fehlersatz samt nächstem Schritt gemeldet statt geraten"
    - "Ein zweites Konto sieht die Tabelle des ersten Kontos nicht und kann in sie nicht schreiben, auch bei bekannter Tabellen-Id"
  artifacts:
    - path: "tests/integration/test_tables_roundtrip.py"
      provides: "Roundtrip gegen eine echte Instanz mit ausdrücklich markiertem Setup-Gerüst"
      min_lines: 150
    - path: "tests/integration/test_permission_fidelity_exapp.py"
      provides: "Zwei-Konten-Negativbeweis, um Tables erweitert"
      contains: "tables"
  key_links:
    - from: "tests/integration/test_tables_roundtrip.py"
      to: "src/mcp_connector/tools/tables.py"
      via: "browse und create_row werden über die Tool-Schicht aufgerufen, nicht über den Client"
      pattern: "tables_tools\\.(browse|create_row)"
    - from: "tests/integration/test_permission_fidelity_exapp.py"
      to: "src/mcp_connector/nextcloud/credentials.py"
      via: "zwei Credential-Objekte im AppAPI-Modus, ein Konto je Identität"
      pattern: "MODE_APPAPI"
---

<objective>
Der Nachweis gegen eine echte Instanz: die Tables-Familie funktioniert von der Werkzeugschicht bis
in die Nextcloud, und die Berechtigungstreue hält auch für Tabellen.

Purpose: Die Unit-Tests fixieren die Formen. Nur eine echte Instanz beantwortet, ob die
Pflichtheader wirklich so akzeptiert werden wie gelesen, ob `createRow` wirklich mit 200
antwortet, ob `GET /api/2/tables/{id}` wirklich `rowsCount` und `onSharePermissions` mitliefert,
und ob die Wertformen jenseits von Text und Zahl ohne eigene Umformung durchgehen (Annahme A2).
Der Zwei-Konten-Negativbeweis ist der Teil, der das Kernversprechen des Projekts für die neue
Familie einlöst: der Assistent sieht nie mehr als der angemeldete Nutzer.

Output: zwei Integrationstests, beide mit Marker `integration`, plus die Messwerte im
Plan-Summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-erreichbarkeits-spike-und-tables/08-RESEARCH.md
@.planning/phases/08-erreichbarkeits-spike-und-tables/08-PATTERNS.md
@.planning/phases/08-erreichbarkeits-spike-und-tables/08-03-SUMMARY.md
@.planning/phases/08-erreichbarkeits-spike-und-tables/08-04-SUMMARY.md

Vorbild für den Roundtrip (Docstring mit Laufanleitung, clients-Fixture mit Skip und
Admin-Sperre, Setup-Schreibaufrufe als markiertes Gerüst, die vier Testarten):
@tests/integration/test_deck_roundtrip.py

Vorbild für den Zwei-Konten-Beweis:
@tests/integration/test_permission_fidelity_exapp.py

<interfaces>
Aus src/mcp_connector/tools/tables.py (Plan 03):
  async def browse(clients, level="tables", table_id=None, limit=25, cursor=None) -> dict
  async def create_row(clients, table_id, values) -> dict     # values ist kompaktes JSON als String

Aus src/mcp_connector/nextcloud/clients/tables.py (Plan 02):
  TABLES_HEADERS, api_url, web_url, get_tables, get_table, get_columns, get_rows_simple, create_row

Fixtures in tests/conftest.py:
  live_env   für die App-Passwort-Schicht (base_url, user, secret), skippt mit dem Namen der
             fehlenden Variable
  exapp_env  für die AppAPI-Schicht (base_url, app_id, app_secret, app_version, aa_version,
             alice, bob)

Setup-Routen, die nur das Testgerüst benutzt (der Connector kann und darf das nicht):
  POST /ocs/v2.php/apps/tables/api/2/tables            Tabelle anlegen
  POST /ocs/v2.php/apps/tables/api/1/columns/...       Spalten anlegen (v1-Schreibpfad)
Diese Aufrufe laufen direkt mit httpx und derselben Basis-URL, ausdrücklich als Gerüst
markiert, so wie `_post` in test_deck_roundtrip.py es macht.

Voraussetzung aus Plan 01: `ensure_app tables` steht in beiden Bootstrap-Skripten, die App ist
auf der Testinstanz und auf der HaRP-Topologie installiert.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Roundtrip gegen eine echte Nextcloud</name>
  <files>tests/integration/test_tables_roundtrip.py</files>
  <read_first>
    - tests/integration/test_deck_roundtrip.py (ganze Datei, 174 Zeilen: Docstring 1 bis 17, clients-Fixture 43 bis 60, Setup-Aufrufe 63 bis 95, die vier Testarten 98 bis 174)
    - tests/conftest.py (Fixture live_env, Zeilen 30 bis 68)
    - src/mcp_connector/tools/tables.py und src/mcp_connector/nextcloud/clients/tables.py
    - .planning/phases/08-erreichbarkeits-spike-und-tables/08-RESEARCH.md, Abschnitte "Falle 4", "Assumptions Log A2 und A6", "Tables-API-Referenz"
    - .planning/phases/08-erreichbarkeits-spike-und-tables/08-PATTERNS.md, Abschnitt "tests/integration/test_tables_roundtrip.py"
  </read_first>
  <action>
    Neue Datei `tests/integration/test_tables_roundtrip.py` mit
    `pytestmark = [pytest.mark.integration, pytest.mark.anyio]`.

    Modul-Docstring nach dem Vorbild: was die Unit-Tests schon fixieren, welche Fragen nur eine
    echte Instanz beantwortet (akzeptierte Pflichtheader, Statuscode 200 beim Anlegen,
    `rowsCount` und `onSharePermissions` aus dem Einzeltabellenaufruf, Wertformen jenseits von
    Text und Zahl), dass Tabelle und Spalten Testgerüst sind und keine Connector-Fähigkeit
    (der Client kann sie absichtlich nicht anlegen), und die Laufanleitung mit
    `docker compose -f compose.test.yml up -d --wait`, `bash scripts/bootstrap_test_nc.sh`,
    `set -a && . ./.env.test && set +a` und
    `uv run pytest tests/integration/test_tables_roundtrip.py -m integration -q`.

    `clients`-Fixture wörtlich nach dem Deck-Vorbild: Skip mit dem Namen der fehlenden Variable,
    `assert user != "admin"` mit der Begründung, dass Integrationstests als normaler Nutzer
    laufen, `capabilities.clear_cache()`, `NcClients` mit
    `httpx.AsyncClient(follow_redirects=False, timeout=30.0)`.

    Ein `_post`-Helfer als ausdrücklich markiertes Gerüst: direkte httpx-POSTs mit
    `httpx.BasicAuth(user, secret)` und `dict(tables_client.TABLES_HEADERS)`; bei einem Status ab
    400 wird der Test mit `pytest.skip` übersprungen und nicht rot, weil ein Konto, das keine
    Tabelle anlegen darf, keine Aussage über den Connector trifft.

    Ein `_table_with_columns`-Helfer, der eine Tabelle mit festem Titel sucht und, wenn sie
    fehlt, einmal anlegt: Titel mit echten Umlauten, plus vier Spalten, darunter eine
    Pflichtspalte (Text, `mandatory: true`), eine Zahl, eine Auswahl mit `selectionOptions` und
    eine `datetime`. Die Auswahl- und Datumsspalte sind Absicht: sie machen die Lücke aus
    Annahme A2 sichtbar, statt sie zu verstecken.

    Fünf Tests:
    1. Die Capabilities der laufenden Instanz melden `tables_available` und enthalten `"1.0"`
       und `"2.0"` in `tables_api_versions`.
    2. Eine mit `tables_create_row` angelegte Zeile ist über `browse(level="rows")`
       zurückzulesen. Der Testinhalt trägt echte Umlaute (etwa "Grüße aus Hamburg, Straße 1"),
       und es wird ein Auswahl- sowie ein Datumswert mitgeschrieben. Die Antwort enthält `id`,
       `table_id`, `url` und `values_written`. Schlägt einer der beiden Werte mit einem 400
       fehl, wird die durchgereichte Meldung der App im Testbericht sichtbar gemacht und der
       Befund in der Zusammenfassung notiert; ein solcher 400 ist ein Messergebnis für Phase 10
       und kein Grund, im Client eine Typumformung zu bauen.
    3. Die live gebaute Zeilen-URL trägt `limit`: dazu `clients.client.send` wie im
       Deck-Vorbild ersetzen, die Anfragen mitschneiden und behaupten, dass jede URL zur
       `rows/simple`-Route einen `limit`-Parameter trägt. Das ist die Eigenschaft, die eine
       geparste Antwort nicht zeigt.
    4. `browse(level="tables")` liefert die Testtabelle mit `rowsCount` und `can_create` gleich
       True (eigene Tabelle, Annahme A6 und Korrektur K5 gegen die echte Instanz geprüft).
    5. Eine unbekannte Tabellen-Id wird gemeldet statt geraten: `pytest.raises(ToolError)` plus
       `assert excinfo.value.hint`.

    Danach den Test wirklich laufen lassen und die gemessenen Werte für die Zusammenfassung
    notieren: Statuscode des Create, ob `rowsCount` und `onSharePermissions` im
    Einzeltabellenaufruf enthalten waren, und wie die Instanz auf den Auswahl- und den Datumswert
    reagiert hat.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector && docker compose -f compose.test.yml up -d --wait && bash scripts/bootstrap_test_nc.sh && set -a && . ./.env.test && set +a && uv run pytest tests/integration/test_tables_roundtrip.py -m integration -q</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector && grep -c "def test_" tests/integration/test_tables_roundtrip.py && grep -n "pytest.mark.integration" tests/integration/test_tables_roundtrip.py && grep -n "limit" tests/integration/test_tables_roundtrip.py | head -5 && uv run ruff check tests/integration/test_tables_roundtrip.py && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/integration/test_tables_roundtrip.py -m integration -q` ist grün mit mindestens fünf Tests und ohne Skip, solange `.env.test` gesetzt und die Tables-App installiert ist
    - Die Datei trägt `pytestmark = [pytest.mark.integration, pytest.mark.anyio]`, damit der Default-Lauf sie nicht anfasst
    - Ein Test behauptet `tables_available` und beide API-Generationen aus den Capabilities der laufenden Instanz
    - Ein Test legt eine Zeile mit Spaltentiteln an und liest sie über `browse(level="rows")` zurück; der Inhalt enthält echte Umlaute
    - Der Create-Test schreibt zusätzlich einen Auswahl- und einen Datumswert; das Ergebnis (Erfolg oder durchgereichter 400 mit der Meldung der App) ist im Summary notiert
    - Ein Test schneidet die echten Anfragen mit und behauptet, dass jede `rows/simple`-URL einen `limit`-Parameter trägt
    - Ein Test behauptet `can_create` gleich True für die eigene Tabelle
    - Ein Test behauptet für eine unbekannte Tabelle einen `ToolError` mit gesetztem `hint`
    - Die Setup-POSTs sind im Code ausdrücklich als Gerüst markiert und überspringen den Test bei einem Status ab 400
    - `uv run pytest -q` (Default-Auswahl) bleibt grün
  </acceptance_criteria>
  <done>Die Familie ist gegen eine echte Nextcloud belegt: Anlegen mit Titeln, Zurücklesen, erzwungenes Limit in der echten URL, Eigentümer darf schreiben, unbekannte Tabelle wird gemeldet.</done>
</task>

<task type="auto">
  <name>Task 2: Zwei-Konten-Negativbeweis für Tables</name>
  <files>tests/integration/test_permission_fidelity_exapp.py</files>
  <read_first>
    - tests/integration/test_permission_fidelity_exapp.py (ganze Datei: Docstring, die Credential-Fabrik im AppAPI-Modus, die bestehenden Negativfälle für files_search, notes_search, unified_search und files_read)
    - tests/integration/test_exapp_dav_matrix.py (Zeilen 58 bis 143, Credential-Bau und beide Kontrollprüfungen)
    - src/mcp_connector/tools/tables.py
    - .planning/phases/08-erreichbarkeits-spike-und-tables/08-RESEARCH.md, Abschnitt "Bekannte Bedrohungsmuster für diesen Stack" (Zeile "Zugriff auf eine fremde Tabelle")
  </read_first>
  <action>
    `tests/integration/test_permission_fidelity_exapp.py` um die Tables-Familie erweitern, im
    Muster der bestehenden Fälle und ohne die vorhandenen zu verändern.

    Setup als ausdrückliches Gerüst: unter der Identität des ersten Kontos eine Tabelle mit
    einer Textspalte anlegen (direkter POST mit denselben Credentials wie im Roundtrip-Test,
    aber im AppAPI-Modus) und eine Zeile mit einem erkennbaren Inhalt schreiben, etwa einer
    Zeichenkette, die sonst nirgends vorkommt.

    Drei Behauptungen:
    1. Das erste Konto findet seine eigene Tabelle über `browse(level="tables")` und die Zeile
       über `browse(level="rows")` im selben Lauf. Ohne diesen positiven Teil beweist der
       Negativteil nichts, weil eine leere Antwort auch aus einem Konfigurationsfehler kommen
       kann.
    2. Das zweite Konto findet die Tabelle nicht in `browse(level="tables")`, und ein Aufruf von
       `browse(level="rows")` mit der bekannten Tabellen-Id liefert keinen Inhalt des ersten
       Kontos, sondern einen Fehler aus Nextclouds eigener Berechtigungsschicht, der als
       Fehlersatz beim Modell ankommt. Der bekannte Pfad ist der Kern: der Test benutzt
       absichtlich die echte Id und nicht eine geratene.
    3. Das zweite Konto kann in die Tabelle des ersten Kontos nicht schreiben: `create_row`
       endet mit einem Fehlersatz samt nächstem Schritt, entweder aus der eigenen
       Vorprüfung nach K5 oder aus Nextclouds Middleware, und es entsteht keine Zeile. Danach
       liest das erste Konto seine Zeilen erneut und die Anzahl ist unverändert.

    Der bestehende Kontrollcharakter der Datei bleibt erhalten: kein `httpx.BasicAuth` als
    Credential-Quelle für die Messungen, die Identität kommt allein aus dem AppAPI-Modus, und
    die vorhandenen Kontrollprüfungen der Datei werden nicht angefasst.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector && export HP_SHARED_KEY="$(openssl rand -hex 32)" && docker compose -p nc-mcp-exapp -f compose.exapp.yml up -d --wait && bash scripts/bootstrap_exapp.sh && set -a && . ./.env.exapp && set +a && uv run pytest tests/integration/test_permission_fidelity_exapp.py -m integration -q</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector && grep -c "tables" tests/integration/test_permission_fidelity_exapp.py && uv run ruff check . && uv run ruff format --check . && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/integration/test_permission_fidelity_exapp.py -m integration -q` ist grün; die bestehenden Fälle laufen unverändert mit
    - Ein Test belegt, dass das erste Konto seine eigene Tabelle und seine eigene Zeile im selben Lauf findet
    - Ein Test belegt, dass das zweite Konto die Tabelle nicht listet und mit der bekannten Tabellen-Id keinen Inhalt des ersten Kontos erhält
    - Ein Test belegt, dass ein Schreibversuch des zweiten Kontos mit einem Fehlersatz samt Hinweis endet und die Zeilenzahl des ersten Kontos danach unverändert ist
    - Die Datei liest für die Messungen weiterhin kein App-Passwort und keinen statischen Bearer als Credential-Quelle
    - `uv run ruff check .`, `uv run ruff format --check .` und `uv run pytest -q` sind grün
  </acceptance_criteria>
  <done>Das Kernversprechen gilt auch für Tables: ein zweites Konto sieht die Tabelle des ersten nicht und kann in sie nicht schreiben, auch bei bekannter Id.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Konto A zu Konto B | Zwei Identitäten auf derselben Instanz; die Impersonation trägt die Nextcloud-ACL, und genau das wird gemessen |
| Testgerüst zu Connector-Fähigkeit | Die Setup-POSTs legen Tabelle und Spalten an, was der Connector absichtlich nicht kann; die Grenze muss im Code sichtbar bleiben |
| Testinstanz zu Entwicklungsrechner | Die Tests laufen gegen lokale Wegwerf-Topologien, nie gegen eine produktive Nextcloud |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-26 | Elevation of Privilege | Zugriff auf eine fremde Tabelle | mitigate | Zwei-Konten-Negativbeweis mit bekannter Tabellen-Id: kein Inhalt, kein Schreibvorgang, und die Zeilenzahl des Eigentümers bleibt danach unverändert. Der positive Teil im selben Lauf schließt aus, dass ein Konfigurationsfehler als Sicherheit gelesen wird |
| T-08-27 | Spoofing | Credential-Quelle im Fidelity-Test | mitigate | Die Identität kommt allein aus dem AppAPI-Modus; die bestehenden Kontrollprüfungen der Datei bleiben unverändert, und die neuen Fälle benutzen dieselbe Fabrik |
| T-08-28 | Tampering | Setup-POSTs im Integrationstest | mitigate | Direkte httpx-Aufrufe, im Code ausdrücklich als Gerüst markiert, Skip statt Fehlschlag bei Status ab 400; der Connector-Client hat für diese Routen keinen Code, und das Gate aus Plan 04 hält das fest |
| T-08-29 | Information Disclosure | Testinhalte | accept | Die geschriebenen Inhalte sind erfundene Testzeichenketten mit Umlauten auf einer lokalen Wegwerf-Instanz; es entstehen keine echten personenbezogenen Daten |
| T-08-30 | Denial of Service | Wiederholte Testläufe | mitigate | Die Setup-Helfer sind idempotent: eine Tabelle mit dem festen Testtitel wird gesucht und nur beim Fehlen angelegt, damit ein zehnter Lauf nicht zehn Tabellen hinterlässt |
| T-08-SC | Tampering | Paketinstallation | accept | Keine neue Sprachabhängigkeit; `pyproject.toml` und `uv.lock` bleiben unangetastet |
</threat_model>

<verification>
1. `uv run pytest tests/integration/test_tables_roundtrip.py -m integration -q` gegen die App-Passwort-Topologie grün.
2. `uv run pytest tests/integration/test_permission_fidelity_exapp.py -m integration -q` gegen die HaRP-Topologie grün.
3. `uv run pytest -q` (Default-Auswahl) grün, `uv run ruff check .` und `uv run ruff format --check .` grün, `uv run pyright` grün.
4. `uv run python scripts/check_tool_budget.py` endet mit Exit-Code 0.
5. `uv run python scripts/acceptance_all_tools.py` gegen die laufende Instanz meldet 18 antwortende Werkzeuge.
6. Ein zweiter Lauf beider Integrationstests legt keine zweite Testtabelle an.
</verification>

<success_criteria>
- Erfolgskriterium 3 der Phase ist live belegt: Tabellen, Spalten und Zeilen über einen Aufruf, mit Kappung und `rowsCount` aus der echten Antwort.
- Erfolgskriterium 4 der Phase ist live belegt: Zeile mit Spaltentiteln, und die Ablehnungen greifen vor dem Schreiben.
- Erfolgskriterium 5 der Phase ist vollständig: Vorprüfung statt 403, kein Änderungs- oder Löschpfad (Gate aus Plan 04), und der Zwei-Konten-Negativbeweis hält.
- Die Annahme A2 zu den Wertformen ist gemessen statt angenommen, und das Ergebnis ist für Phase 10 aufgeschrieben.
</success_criteria>

<output>
Create `.planning/phases/08-erreichbarkeits-spike-und-tables/08-05-SUMMARY.md` when done.
Die Zusammenfassung nennt den gemessenen Statuscode des Create, ob `rowsCount` und
`onSharePermissions` im Einzeltabellenaufruf ankamen, und wie die Instanz auf Auswahl- und
Datumswerte reagiert hat (Annahme A2), weil Phase 10 und 11 darauf aufbauen.
</output>
</content>
