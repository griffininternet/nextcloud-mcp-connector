---
phase: 08-erreichbarkeits-spike-und-tables
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/bootstrap_exapp.sh
  - scripts/bootstrap_test_nc.sh
  - tests/integration/test_exapp_mail_reach.py
  - docs/spike-mail.md
  - compose.exapp.yml
autonomous: true
requirements: [MAIL-04]

must_haves:
  truths:
    - "Ein Integrationstest gegen die laufende HaRP-Topologie zeigt für alle vier Mail-Wege (accounts, mailboxes, messages, OCS-Volltext) Statuscode, Content-Type und Antwortform, statt eine Erreichbarkeit zu behaupten"
    - "Das Ergebnis ist eindeutig auch ohne erreichbaren IMAP-Server: nur eine HTML-Antwort oder ein 3xx auf /login gilt als nicht erreicht"
    - "Zwei Kontrollprüfungen belegen, dass die Identität allein aus AUTHORIZATION-APP-API stammt und nicht aus einem App-Passwort"
    - "Das SCOPE_IGNORE-Risiko der drei internen Listen-Routen steht im Modul-Docstring des messenden Codes und als Ersetzbarkeits-Hinweis in der Doku"
    - "Die Topologie ist reproduzierbar: Bootstrap installiert die Apps tables und mail und legt das Spike-Mail-Konto idempotent an"
  artifacts:
    - path: "tests/integration/test_exapp_mail_reach.py"
      provides: "Messung der vier Mail-Wege unter AppAPI-Impersonation plus beide Kontrollprüfungen"
      contains: "MODE_APPAPI"
      min_lines: 120
    - path: "docs/spike-mail.md"
      provides: "Messprotokoll mit Versionen, Statuscodes, Antwortformen, Entscheidung und Ersetzbarkeits-Hinweis"
      min_lines: 60
    - path: "scripts/bootstrap_exapp.sh"
      provides: "ensure_app mail, ensure_app tables und das idempotente Spike-Mail-Konto"
      contains: "ensure_app mail"
    - path: "scripts/bootstrap_test_nc.sh"
      provides: "ensure_app tables für die App-Passwort-Integrationsschicht"
      contains: "ensure_app tables"
  key_links:
    - from: "tests/integration/test_exapp_mail_reach.py"
      to: "src/mcp_connector/nextcloud/credentials.py"
      via: "Credentials(mode=MODE_APPAPI, secret=APP_SECRET)"
      pattern: "MODE_APPAPI"
    - from: "tests/integration/test_exapp_mail_reach.py"
      to: "docs/spike-mail.md"
      via: "Protokollausgabe der Messung wandert wörtlich in die Messtabelle"
      pattern: "spike-mail"
---

<objective>
Die einzige offene Unbekannte des Meilensteins messen: erreicht eine unter AppAPI-Impersonation
gestellte Anfrage die Controller der Nextcloud-Mail-App, oder endet sie auf einer Loginseite.

Purpose: MAIL-04 ist blockierend. Ein negatives Ergebnis ändert den Schnitt der Phasen 10 und 11
(MAIL-01 bis MAIL-03, CTX-02, SEC-01, Toolzahl in TOOL-15) und muss vor deren Planung auf dem
Tisch liegen. Dieser Plan liefert deshalb eine Messung mit Protokoll, keinen Produktionscode.

Output: ein Spike-Integrationstest, ein Messprotokoll in `docs/spike-mail.md`, und eine
Topologie, die die Messung reproduzierbar macht (Apps `mail` und `tables` installiert, Mail-Konto
angelegt).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-erreichbarkeits-spike-und-tables/08-RESEARCH.md
@.planning/phases/08-erreichbarkeits-spike-und-tables/08-PATTERNS.md

Vorbild für den Test (Rolle, Credential-Bau, beide Kontrollprüfungen):
@tests/integration/test_exapp_dav_matrix.py

Vorbild für das Protokoll (Kopf, Entscheidungstabelle, Satz über Geheimnisse):
@docs/spike-dav.md

<interfaces>
Aus src/mcp_connector/nextcloud/clients/ocs.py (unverändert benutzen):
  OCS_PREFIX = "/ocs/v2.php"
  OCS_HEADERS: Mapping[str, str]  ("OCS-APIRequest": "true", "Accept": "application/json")
  def ocs_url(creds: Credentials, path: str) -> str
  async def ocs_get(client, creds, path, params=None) -> httpx.Response

Aus src/mcp_connector/nextcloud/credentials.py:
  Credentials(base_url, user, secret, mode=..., app_id=..., app_version=..., aa_version=...)
  MODE_APPAPI  (Modus-Konstante, im DAV-Spike importiert)
  normalize_base_url(value) -> str

Aus tests/conftest.py:
  Fixture `exapp_env` (Zeilen 39 bis 68): liefert base_url, app_id, app_secret, app_version,
  aa_version, alice, bob; skippt mit dem Namen der fehlenden Umgebungsvariable. Unverändert
  benutzen.

Die vier zu messenden Wege (Pfade als benannte Modul-Konstanten anlegen, damit Phase 10 sie
übernehmen kann):
  GET /index.php/apps/mail/api/accounts
  GET /index.php/apps/mail/api/mailboxes?accountId=<id>
  GET /index.php/apps/mail/api/messages?mailboxId=<id>&limit=5
  GET /ocs/v2.php/apps/mail/message/<databaseId>     (kein api-Segment, Korrektur K1)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Topologie und Bootstrap für Tables und Mail</name>
  <files>scripts/bootstrap_exapp.sh, scripts/bootstrap_test_nc.sh</files>
  <read_first>
    - scripts/bootstrap_exapp.sh (ganze Datei, insbesondere `ensure_app` Zeilen 251 bis 265, `ensure_user` Zeilen 267 bis 283, die Aufrufstellen Zeilen 849 und 850, der FALLBACK-Block am Dateiende)
    - scripts/bootstrap_test_nc.sh (insbesondere `ensure_app` Zeile 76 und die Aufrufstellen Zeilen 159 und 160)
    - .planning/phases/08-erreichbarkeits-spike-und-tables/08-PATTERNS.md, Abschnitt "scripts/bootstrap_exapp.sh und scripts/bootstrap_test_nc.sh"
    - .planning/phases/08-erreichbarkeits-spike-und-tables/08-RESEARCH.md, Abschnitt "Stufe 1 (empfohlener Start, kein IMAP nötig)"
  </read_first>
  <action>
    In `scripts/bootstrap_exapp.sh` direkt unter den bestehenden Aufrufen `ensure_app notes` und
    `ensure_app deck` zwei Zeilen ergänzen: `ensure_app tables` und `ensure_app mail`. Die
    Funktion `ensure_app` selbst bleibt unverändert, sie ist schon idempotent (install, sonst
    enable, sonst Fehler mit Verweis auf den FALLBACK-Block).

    Danach eine neue idempotente Funktion `ensure_mail_account` nach dem Muster von
    `ensure_user` anlegen: erst prüfen, ob das Konto existiert (`occ mail:account:export
    <uid>` oder die Kontoliste des Nutzers; ein Kommando, dessen Nicht-Null-Exit "existiert
    nicht" bedeutet), dann anlegen mit
    `occ mail:account:create-imap <uid> "<name>" <email> imap.invalid 143 none <uid> <imap-pw>
    smtp.invalid 25 none <uid> <imap-pw> password`.
    Der IMAP-Host ist absichtlich `imap.invalid`: `CreateImapAccount` prüft die Verbindung
    nicht, und Spike-Stufe 1 braucht keinen erreichbaren IMAP-Server. Die Ausgabe des
    Kommandos wird wie in `ensure_user` in eine Variable gefangen und nur bei Fehlschlag auf
    stderr gezeigt. Aufruf für den Testnutzer aus `NC_MCP_TEST_USER` mit einer Mailadresse
    unter `example.test`, direkt nach den `ensure_app`-Zeilen.

    Ein Kommentarblock über `ensure_mail_account` begründet in zwei Sätzen, warum der Host
    ungültig ist und warum das Konto trotzdem für die Messung reicht (jede Antwort aus
    App-Code beweist, dass der Controller erreicht wurde).

    In `scripts/bootstrap_test_nc.sh` unter den bestehenden `ensure_app`-Aufrufen (Zeilen 159
    und 160) `ensure_app tables` ergänzen. Kein Mail-Konto und kein `ensure_app mail` in
    dieser Datei: die App-Passwort-Schicht braucht in dieser Phase nur Tables.

    Danach beide Bootstrap-Läufe ausführen, damit die Apps wirklich installiert sind.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector && grep -n "ensure_app tables" scripts/bootstrap_exapp.sh scripts/bootstrap_test_nc.sh && grep -n "ensure_app mail" scripts/bootstrap_exapp.sh && grep -n "ensure_mail_account" scripts/bootstrap_exapp.sh && bash -n scripts/bootstrap_exapp.sh && bash -n scripts/bootstrap_test_nc.sh</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector && bash scripts/bootstrap_exapp.sh && docker compose -p nc-mcp-exapp -f compose.exapp.yml exec -T -u www-data nextcloud php occ app:list | grep -E "^  - (mail|tables):"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ensure_app tables" scripts/bootstrap_exapp.sh` ist mindestens 1, dasselbe für `scripts/bootstrap_test_nc.sh`
    - `grep -c "ensure_app mail" scripts/bootstrap_exapp.sh` ist mindestens 1
    - `bash -n` läuft für beide Skripte ohne Syntaxfehler durch
    - `occ app:list` der Topologie `nc-mcp-exapp` nennt `mail` und `tables` im Abschnitt Enabled
    - Ein zweiter Lauf von `bash scripts/bootstrap_exapp.sh` endet mit Exit-Code 0 und meldet für App und Konto "exists" beziehungsweise "enabled", legt also nichts doppelt an
    - Das Spike-Mail-Konto existiert: `occ mail:account:export <testnutzer>` liefert Exit-Code 0
  </acceptance_criteria>
  <done>Beide Bootstrap-Skripte installieren tables und mail idempotent, das Spike-Mail-Konto mit ungültigem IMAP-Host existiert, und ein zweiter Lauf ändert nichts.</done>
</task>

<task type="auto">
  <name>Task 2: Spike-Test, der protokolliert statt zu behaupten</name>
  <files>tests/integration/test_exapp_mail_reach.py</files>
  <read_first>
    - tests/integration/test_exapp_dav_matrix.py (ganze Datei: Modul-Docstring Zeilen 1 bis 38, `_appapi_clients` Zeilen 61 bis 94, die beiden Kontrollprüfungen Zeilen 104 bis 143)
    - tests/conftest.py (Fixture `exapp_env`, Zeilen 30 bis 68)
    - src/mcp_connector/nextcloud/clients/ocs.py (OCS_HEADERS, ocs_url, ocs_get)
    - .planning/phases/08-erreichbarkeits-spike-und-tables/08-RESEARCH.md, Abschnitte "Mail-Spike: Messplan (MAIL-04)", "Entscheidungskriterium, vorab festlegen", "Spike: eine Messzeile statt einer Vermutung", "Wo das SCOPE_IGNORE-Risiko dokumentiert wird"
    - .planning/phases/08-erreichbarkeits-spike-und-tables/08-PATTERNS.md, Abschnitt "tests/integration/test_exapp_mail_reach.py"
  </read_first>
  <action>
    Neue Datei `tests/integration/test_exapp_mail_reach.py` mit
    `pytestmark = [pytest.mark.integration, pytest.mark.anyio]`.

    Der Modul-Docstring trägt vier Punkte, und er ist laut Erfolgskriterium 2 der Ort, an dem
    das SCOPE_IGNORE-Risiko benannt wird: (1) die gemessene Frage, nämlich ob die
    Impersonation Mails Controller erreicht, (2) dass die drei Listen-Routen
    `#[OpenAPI(scope: OpenAPI::SCOPE_IGNORE)]` tragen und damit keine zugesagte API sind,
    weshalb sie ersetzbar bleiben müssen (Ausweg: Suchprovider `mail` plus die OCS-Volltextroute),
    (3) dass die OCS-Route `GET /ocs/v2.php/apps/mail/message/{id}` kein `api`-Segment hat
    (Korrektur K1) und (4) die Laufanleitung mit `docker compose -p nc-mcp-exapp -f
    compose.exapp.yml up -d --wait`, `bash scripts/bootstrap_exapp.sh`,
    `set -a && . ./.env.exapp && set +a` und
    `uv run pytest tests/integration/test_exapp_mail_reach.py -m integration -q`.

    Vier benannte Modul-Konstanten für die Routen (`MAIL_ACCOUNTS_PATH`,
    `MAIL_MAILBOXES_PATH`, `MAIL_MESSAGES_PATH`, `MAIL_MESSAGE_OCS_PATH`), damit die
    Zeichenketten an einer Stelle stehen und Phase 10 sie übernehmen kann. Jede Konstante
    trägt einen `#:`-Kommentar, der sagt, ob die Route SCOPE_IGNORE oder zugesagt ist.

    `_appapi_clients(exapp_env, user)` wörtlich aus dem DAV-Spike übernehmen: `Credentials`
    mit `mode=MODE_APPAPI` und `secret=exapp_env["app_secret"]`, `httpx.AsyncClient(
    follow_redirects=False, timeout=30.0)`. Kein `httpx.BasicAuth` in dieser Datei, nirgends.

    Beide Kontrollprüfungen wörtlich übernehmen und als solche kommentieren:
    `test_the_measuring_process_holds_no_nextcloud_app_password` (löscht
    `NC_MCP_APP_PASSWORD` und `NC_MCP_STATIC_BEARER` aus dem Environment und behauptet ihre
    Abwesenheit) und `test_a_wrong_app_secret_is_refused` (`app_secret` gleich 64 Nullen,
    `ocs.ocs_get(..., "/cloud/user")` darf nicht 200 sein).

    Der neue Baustein ist `_probe(clients, method, url) -> dict[str, str]`: führt genau eine
    Anfrage mit `dict(ocs.OCS_HEADERS)` und `auth=clients.creds.auth()` aus und gibt
    `status`, `content_type`, `shape`, `location` und `head` zurück. `shape` ist `"html"`,
    wenn der linksgetrimmte Körper mit `<` beginnt, `"json"`, wenn er mit `[` oder `{`
    beginnt, sonst `"other"`. `head` ist auf 120 Zeichen begrenzt, und der Docstring sagt,
    dass das eine Sicherheitsanforderung ist und keine Kosmetik: die Kontoantwort enthält
    IMAP- und SMTP-Hostnamen. `_probe` behauptet keinen Statuscode.

    Ein Test je Weg, in dieser Reihenfolge: `accounts` (aus der Antwort die erste `accountId`
    lesen, damit die Folgewege eine echte Id benutzen), `mailboxes` mit dieser `accountId`,
    `messages` mit `limit=5` (ein fehlendes `limit` wäre serverseitig genau eine Nachricht,
    Korrektur K6), und die OCS-Volltextroute mit genau einer nicht existierenden
    `databaseId`. Die Behauptung je Weg ist `shape == "json"` plus die Zusatzbedingung, dass
    `location` nicht `/login` enthält. Kein `assert status == 200`: die Listen-Routen
    berühren IMAP, und ein IMAP-Fehler ist trotzdem antwortender App-Code.

    Die OCS-Route darf genau einen Versuch machen, niemals in einer Schleife: `messageApi#get`
    trägt `#[BruteForceProtection('mailGetMessage')]` und Nextcloud zählt pro Quell-IP, was
    für eine ExApp eine IP für alle Nutzer ist. Diese Regel gehört als Kommentar an den
    Test und nicht in die Topologie, weil sie in Phase 10 in Produktionscode wandert.

    Ein letzter Test sammelt alle vier Messzeilen und gibt sie als Tabelle mit `print` aus
    (Statuscode, Content-Type, Form, Urteil), damit die Zeilen wörtlich in
    `docs/spike-mail.md` wandern können. Er behauptet zusätzlich, dass keine Messzeile die
    Form `html` hat und keine einen Redirect auf `/login` trägt.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector && set -a && . ./.env.exapp && set +a && uv run pytest tests/integration/test_exapp_mail_reach.py -m integration -q -s</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector && grep -c "BasicAuth" tests/integration/test_exapp_mail_reach.py | grep -qx 0 && grep -n "SCOPE_IGNORE" tests/integration/test_exapp_mail_reach.py && uv run ruff check tests/integration/test_exapp_mail_reach.py && uv run ruff format --check tests/integration/test_exapp_mail_reach.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/integration/test_exapp_mail_reach.py -m integration -q` endet mit Exit-Code 0 und lässt keinen Test übrig, der übersprungen wurde, solange `.env.exapp` gesetzt ist
    - Die Datei enthält kein `httpx.BasicAuth` und liest weder `NC_MCP_APP_PASSWORD` noch `NC_MCP_STATIC_BEARER` als Credential-Quelle: `grep -c "BasicAuth" tests/integration/test_exapp_mail_reach.py` ist 0
    - `grep -c "SCOPE_IGNORE" tests/integration/test_exapp_mail_reach.py` ist mindestens 1 und liegt im Modul-Docstring
    - Vier Routen-Konstanten existieren; `grep -c "apps/mail/api" tests/integration/test_exapp_mail_reach.py` ist mindestens 3, und der OCS-Pfad enthält `apps/mail/message` ohne das Segment `api`
    - `_probe` kappt den Körper: die Quelle enthält eine Begrenzung auf 120 Zeichen
    - Der Testlauf mit `-s` gibt vier Messzeilen mit Statuscode, Content-Type und Form aus
    - Die OCS-Volltextroute wird in der ganzen Datei genau einmal aufgerufen (kein Schleifenkonstrukt um `MAIL_MESSAGE_OCS_PATH`)
    - `uv run ruff check` und `uv run ruff format --check` sind für die Datei grün
  </acceptance_criteria>
  <done>Der Spike läuft gegen die HaRP-Topologie, protokolliert vier Wege mit Statuscode, Content-Type und Form, behauptet nur "App-Code hat geantwortet", und beide Kontrollprüfungen machen die Messung beweiskräftig.</done>
</task>

<task type="auto">
  <name>Task 3: Messprotokoll, Entscheidung und Eskalationsregel</name>
  <files>docs/spike-mail.md, compose.exapp.yml</files>
  <read_first>
    - docs/spike-dav.md (Kopf Zeilen 1 bis 16, Entscheidungstabelle Zeilen 25 bis 32, Beweisabschnitt Zeilen 38 bis 75, Abschnitt "The two controls" Zeilen 61 bis 75)
    - tests/integration/test_exapp_mail_reach.py (die in Task 2 erzeugten Messzeilen)
    - compose.exapp.yml (Dienst `registry` Zeilen 124 bis 137, Netz-Block Zeilen 139 bis 152, Dienst `nextcloud` Zeilen 45 bis 86)
    - .planning/phases/08-erreichbarkeits-spike-und-tables/08-RESEARCH.md, Abschnitte "Entscheidungskriterium, vorab festlegen", "Stufe 2", "Wo das SCOPE_IGNORE-Risiko dokumentiert wird"
  </read_first>
  <action>
    Neue Datei `docs/spike-mail.md` nach dem Kopf-Muster von `docs/spike-dav.md`: `**Status:**`,
    `**Decision date:**`, `**Nextcloud version:**`, `**AppAPI version:**`, `**Mail app
    version:**`, `**Deploy daemon:**` und `**Scope:**`. Die Versionen werden vor dem Schreiben
    aus der laufenden Instanz gelesen (`occ app:list` für `mail` und `app_api`, `occ status`
    für die Serverversion) und nicht aus der Recherche übernommen.

    Die Messtabelle hat genau diese Spalten: Weg, URL, erwarteter Status, gemessener Status,
    Content-Type, Form (json, html, other), Urteil (erreicht, nicht erreicht). Vier Zeilen,
    die Werte wörtlich aus der Testausgabe von Task 2.

    Danach ein Abschnitt "Decision" mit einem Satz, ob Mail unter reiner AppAPI-Impersonation
    erreichbar ist, und was daraus für Phase 10 und 11 folgt (erreicht: MAIL-01 bis MAIL-03
    bleiben wie geschnitten; nicht erreicht: die Listen-Routen fallen weg und Phase 10
    reduziert sich auf die OCS-Volltextroute plus den Suchprovider `mail`, was vor der Planung
    von Phase 10 entschieden werden muss).

    Ein Abschnitt "The two controls" nach dem Vorbild: beide Kontrollprüfungen namentlich,
    und der Satz, dass ein falsches `APP_SECRET` abgewiesen wird und damit alle anderen Zeilen
    gültig sind. Dazu der Satz aus dem DAV-Protokoll, dass Headerwerte, die `APP_SECRET`
    tragen, nie gedruckt werden, und dass nur Statuscodes und maximal 120 Zeichen Körper im
    Protokoll stehen.

    Ein Abschnitt "Replaceability" mit dem ausdrücklichen Ersetzbarkeits-Hinweis: die drei
    Listen-Routen tragen `SCOPE_IGNORE` und sind damit keine zugesagte API; bricht eine
    Mail-Fassung sie, ist der Ausweg die Discovery über den Suchprovider `mail` plus die
    OCS-Volltextroute. Dazu ein Satz, der die Wiederholung dieses Hinweises im künftigen
    `src/mcp_connector/nextcloud/clients/mail.py` ausdrücklich an Phase 10 übergibt.

    Eskalationsregel: wenn eine der vier Messzeilen die Form `other` trägt oder ein 3xx auf
    etwas anderes als `/login` zeigt, ist Stufe 1 nicht eindeutig. Dann und nur dann kommt
    GreenMail als vierter Dienst in `compose.exapp.yml` dazu (`image:
    greenmail/standalone:2.1.12`, `container_name: nc-mcp-exapp-greenmail`, kein
    veröffentlichter Port, Netz `nc-mcp-exapp-net`, `GREENMAIL_OPTS` mit
    `-Dgreenmail.setup.test.all`, `-Dgreenmail.hostname=0.0.0.0`,
    `-Dgreenmail.auth.disabled` und `-Dgreenmail.users=<testnutzer>:<pw>@example.test`), das
    Spike-Konto wird auf Host `greenmail` Port 3143 mit `imapSslMode none` umgestellt, ein
    Testmail wird über SMTP 3025 eingeliefert, `occ mail:account:sync <id> -f` läuft, und die
    Messung wird wiederholt. Ist Stufe 1 eindeutig, bleibt `compose.exapp.yml` unverändert und
    das Protokoll notiert Stufe 2 als benannten, ausgeklammerten Folgeschritt für Phase 10.

    Zum Schluss den Reproduktionsblock mit den vier Kommandos aus dem Modul-Docstring des
    Tests, damit ein Leser die Messung wiederholen kann.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector && grep -n "^\*\*Status:\*\*" docs/spike-mail.md && grep -c "SCOPE_IGNORE" docs/spike-mail.md && grep -n "Replaceability\|Ersetzbarkeit" docs/spike-mail.md && grep -c "| accounts \?|" docs/spike-mail.md</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector && grep -v '^#' docs/spike-mail.md | grep -ci "archiv" | grep -qx 0 && python -c "import re,sys,io; t=io.open('docs/spike-mail.md',encoding='utf-8').read(); sys.exit(1 if (chr(0x2014) in t or chr(0x2013) in t) else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/spike-mail.md` existiert, hat einen Kopf mit Status, Entscheidungsdatum, Nextcloud-, AppAPI- und Mail-Version und einen Scope-Satz
    - Die Messtabelle hat vier Datenzeilen, je eine pro Weg, mit gemessenem Statuscode, Content-Type, Form und Urteil; die Werte stimmen mit der Testausgabe aus Task 2 überein
    - Ein Abschnitt "Decision" nennt in einem Satz das Ergebnis und die Folge für Phase 10 und 11
    - Ein Abschnitt nennt beide Kontrollprüfungen namentlich und den Satz, dass Headerwerte mit `APP_SECRET` nie gedruckt werden
    - Der Ersetzbarkeits-Hinweis nennt `SCOPE_IGNORE`, den Ausweg (Suchprovider `mail` plus OCS-Volltextroute) und die Übergabe an Phase 10
    - Das Dokument enthält kein Em-Dash und kein En-Dash, und außerhalb von Codeblöcken nicht das verbotene Wort "archiv"
    - `compose.exapp.yml` ist unverändert, wenn alle vier Messzeilen eindeutig sind; andernfalls enthält es den Dienst `nc-mcp-exapp-greenmail` ohne veröffentlichten Port, und das Protokoll trägt eine zweite Messtabelle aus Stufe 2
    - `git log --oneline -1` zeigt einen Conventional-Commit ohne Co-Authored-By-Trailer
  </acceptance_criteria>
  <done>MAIL-04 ist beantwortet: das Protokoll nennt vier gemessene Wege mit Statuscode und Antwortform, die Entscheidung für Phase 10 und 11 steht als Satz, und das SCOPE_IGNORE-Risiko ist im Code und in der Doku benannt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Messprozess zu Nextcloud | Der Spike stellt Anfragen mit `APP_SECRET` als einzigem Credential; die Identität entsteht serverseitig aus `AUTHORIZATION-APP-API` |
| Antwortkörper der Mail-App zu Testprotokoll | Die Kontoantwort trägt IMAP- und SMTP-Hostnamen und Kontonamen; alles, was ins Protokoll wandert, verlässt die vertrauliche Zone |
| Bootstrap-Skript zu Testinstanz | Das Skript legt Apps und ein Mail-Konto per `occ` an, also mit Administratorrechten in der lokalen Topologie |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | `_probe` in test_exapp_mail_reach.py | mitigate | Körper auf 120 Zeichen gekappt, nur Status, Content-Type, Form und Location protokolliert; Headerwerte mit `APP_SECRET` werden nie gedruckt (Regel steht wörtlich in docs/spike-mail.md) |
| T-08-02 | Denial of Service | OCS-Route `apps/mail/message/{id}` | mitigate | Genau ein Versuch auf die 404-Route, keine Schleife; `#[BruteForceProtection('mailGetMessage')]` zählt pro Quell-IP und trifft bei einer ExApp alle Nutzer. Regel als Kommentar am Test, nicht in der Topologie |
| T-08-03 | Spoofing | Credential-Bau im Spike | mitigate | Beide Kontrollprüfungen: kein `NC_MCP_APP_PASSWORD` und kein `NC_MCP_STATIC_BEARER` im Prozess, kein `httpx.BasicAuth` in der Datei, und ein `APP_SECRET` aus 64 Nullen wird abgewiesen |
| T-08-04 | Tampering | `ensure_mail_account` in bootstrap_exapp.sh | mitigate | Idempotent nach dem Muster von `ensure_user`: erst prüfen, dann anlegen; Kommandoausgabe wird gefangen und nur bei Fehlschlag gezeigt, damit ein abgelehntes Passwort nicht als stiller Exit 1 verschwindet |
| T-08-05 | Information Disclosure | GreenMail-Dienst (nur Eskalationsfall) | mitigate | Kein veröffentlichter Port; Nextcloud erreicht den Dienst über das Docker-Netz `nc-mcp-exapp-net`. Wird ein Port doch gebunden, dann ausdrücklich auf `127.0.0.1` mit derselben Begründung wie beim Dienst `registry` |
| T-08-SC | Tampering | Paketinstallation | accept | In dieser Phase wird kein Sprachpaket installiert: `pyproject.toml` und `uv.lock` bleiben unangetastet. Die zwei Nextcloud-Apps und das optionale Docker-Image sind in 08-RESEARCH.md gegen ihre offiziellen Quellen verifiziert (tables 2.2.2, mail 5.11.1, greenmail/standalone 2.1.12); kein `[ASSUMED]` und kein `[SUS]`, also kein Legitimitäts-Checkpoint nötig |
</threat_model>

<verification>
1. `bash scripts/bootstrap_exapp.sh` läuft zweimal hintereinander mit Exit-Code 0 durch und legt beim zweiten Lauf nichts neu an.
2. `occ app:list` der Topologie nennt `mail` und `tables` als enabled.
3. `set -a && . ./.env.exapp && set +a && uv run pytest tests/integration/test_exapp_mail_reach.py -m integration -q` ist grün.
4. `uv run ruff check .` und `uv run ruff format --check .` sind über das ganze Repo grün.
5. `uv run pytest -q` (Default-Auswahl ohne Integrations- und Matrix-Marker) bleibt grün: dieser Plan ändert keinen Produktionscode.
6. `docs/spike-mail.md` trägt vier Messzeilen, eine Entscheidung, beide Kontrollen und den Ersetzbarkeits-Hinweis.
</verification>

<success_criteria>
- Erfolgskriterium 1 der Phase ist erfüllt: für alle vier Wege stehen Statuscode und Antwortform im Ergebnis, nicht die Vermutung.
- Erfolgskriterium 2 der Phase ist erfüllt: das SCOPE_IGNORE-Risiko steht im Modul-Docstring des messenden Codes und als Ersetzbarkeits-Hinweis in `docs/spike-mail.md`.
- Die Entscheidung für Phase 10 und 11 ist als Satz aufgeschrieben, nicht als Stimmung.
- Die Topologie ist reproduzierbar: ein frischer Bootstrap stellt Apps und Spike-Konto wieder her.
</success_criteria>

<output>
Create `.planning/phases/08-erreichbarkeits-spike-und-tables/08-01-SUMMARY.md` when done.
Die Zusammenfassung muss die vier Messzeilen und den Entscheidungssatz wörtlich enthalten, weil
die Planung der Phasen 10 und 11 daran hängt.
</output>
</content>
