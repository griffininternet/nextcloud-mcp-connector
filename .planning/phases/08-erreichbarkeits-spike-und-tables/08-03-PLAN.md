---
phase: 08-erreichbarkeits-spike-und-tables
plan: 03
type: execute
wave: 2
depends_on: ["08-02"]
files_modified:
  - src/mcp_connector/tools/tables.py
  - tests/unit/test_tables_tools.py
  - vulture_whitelist.py
autonomous: true
requirements: [TABLES-01, TABLES-02]

must_haves:
  truths:
    - "Nutzer kann Tabellen, Spalten und die Zeilen einer Tabelle über einen Aufruf lesen, mit einem Antwortformat für alle drei Ebenen"
    - "Ein Aufruf ohne gesetztes Limit liest nie die ganze Tabelle: der Default ist 25, das Maximum 200, und eine Kappung steht beim Namen in der Antwort"
    - "Jede Zeilenantwort nennt rowsCount, count, offset und bei Kappung truncated plus ein next-Handle, das nur für dieselbe Tabelle gilt"
    - "Nutzer kann eine Zeile mit Spaltentiteln statt Spalten-Ids anlegen"
    - "Ein unbekannter, mehrdeutiger oder fehlender Pflichttitel wird mit der Liste der gültigen Titel abgelehnt, bevor irgendetwas geschrieben wird"
    - "Ein Nutzer ohne Schreibrecht auf einer geteilten Tabelle bekommt vorab einen Fehlersatz samt nächstem Schritt, und der Eigentümer seiner eigenen Tabelle wird nie abgewiesen"
  artifacts:
    - path: "src/mcp_connector/tools/tables.py"
      provides: "browse mit level-Enum, create_row mit Titel-zu-Id-Abbildung, Projektionen, Envelope, K5-Rechtevorprüfung"
      exports: ["browse", "create_row", "LEVELS", "DEFAULT_LIMIT", "MAX_LIMIT"]
      min_lines: 200
    - path: "tests/unit/test_tables_tools.py"
      provides: "Katalog der Fehler-, Edge-, Negativ- und no_data-Fälle beider Tools"
      min_lines: 220
  key_links:
    - from: "src/mcp_connector/tools/tables.py"
      to: "src/mcp_connector/nextcloud/capabilities.py"
      via: "require_app als erste Zeile beider Einstiegsfunktionen"
      pattern: "capabilities\\.require_app"
    - from: "src/mcp_connector/tools/tables.py"
      to: "src/mcp_connector/nextcloud/clients/tables.py"
      via: "get_tables, get_table, get_columns, get_rows_simple, create_row"
      pattern: "tables_client\\.(get_tables|get_table|get_columns|get_rows_simple|create_row)"
    - from: "src/mcp_connector/tools/tables.py"
      to: "src/mcp_connector/paging.py"
      via: "encode_cursor, decode_cursor, read_offset, check_scope mit der Tabellen-Id als Scope"
      pattern: "paging\\.check_scope"
    - from: "src/mcp_connector/tools/tables.py"
      to: "src/mcp_connector/tools/marks.py"
      via: "without_marks auf jeden Zellwert, der in den Modellkontext geht"
      pattern: "marks\\.without_marks"
---

<objective>
Die Fachlogik der Tables-Familie: ein Browse-Werkzeug mit drei Ebenen und ein Werkzeug, das eine
Zeile mit Spaltentiteln anlegt, samt aller Ablehnungen, die vor dem Schreiben passieren.

Purpose: TABLES-01 und TABLES-02 leben hier. Der Wortlaut der Fehlersätze, die Titel-zu-Id-
Abbildung und die Rechte-Vorprüfung sind Fachlogik und gehören nicht in den Client. Die
gefährlichste Falle der Phase ist dabei nicht eine Route, sondern das Berechtigungsfeld
`onSharePermissions`, das für eine eigene Tabelle nur `read` meldet.

Output: `tools/tables.py` und der vollständige Unit-Testkatalog. Es entsteht in diesem Plan
noch keine Registrierung: `tools/list` bleibt bei 16 Werkzeugen, damit Toolzahl, Budget-Gate und
Contract-Tests bis Plan 04 grün bleiben und die Anhebung aller eingefrorenen Zahlen dort in
einem Zug passiert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-erreichbarkeits-spike-und-tables/08-RESEARCH.md
@.planning/phases/08-erreichbarkeits-spike-und-tables/08-PATTERNS.md
@.planning/phases/08-erreichbarkeits-spike-und-tables/08-02-SUMMARY.md

Vorbild für das Tool-Modul (Docstring, Browse-Kopf, Create-Kopf, Rechte-Vorprüfung, Projektion, Envelope):
@src/mcp_connector/tools/deck.py

Vorbild für die Cursor-Benutzung:
@src/mcp_connector/tools/files.py

Vorbild für die Unit-Tests (Capabilities-Mock, Cache-Reset, envelope-Helfer):
@tests/unit/test_deck_tools.py

<interfaces>
Aus Plan 02, src/mcp_connector/nextcloud/clients/tables.py:
  TABLES_HEADERS, V1_PREFIX, V2_PREFIX, NODE_TYPE_TABLE, NODE_COLLECTION_TABLES, MAX_ROWS
  def api_url(creds, path="") -> str
  def web_url(creds, table_id) -> str
  async def get_tables(client, creds) -> list[dict[str, Any]]
  async def get_table(client, creds, table_id) -> dict[str, Any]
  async def get_columns(client, creds, table_id) -> list[dict[str, Any]]
  async def get_rows_simple(client, creds, table_id, *, limit, offset=0) -> list[list[Any]]
  async def create_row(client, creds, table_id, *, data) -> dict[str, Any]

Aus src/mcp_connector/nextcloud/capabilities.py (nach Plan 02):
  async def require_app(clients, app) -> Capabilities      # erste Zeile jedes Tools (SRV-04)
  Capabilities.tables_available, Capabilities.tables_api_versions

Aus src/mcp_connector/paging.py:
  def encode_cursor(state: dict[str, Any]) -> str
  def decode_cursor(cursor: str) -> dict[str, Any]
  def read_offset(state: dict[str, Any]) -> int
  def check_scope(state: dict[str, Any], key: str, expected: str, what: str) -> None

Aus src/mcp_connector/tools/marks.py:
  def without_marks(text: str) -> str

Aus src/mcp_connector/errors.py:
  ToolError(message=..., hint=...)

Antwortfelder, auf die die Projektion zugreift:
  Table:  id, title, emoji, ownership, archived, favorite, isShared, onSharePermissions
          ({read, create, update, delete, manage}, nullable), rowsCount, columnsCount
  Column: id, title, type, subtype, mandatory, selectionOptions, textMaxLength, numberMin,
          numberMax, numberDecimals, datetimeDefault
  rows/simple: Liste von Listen, erste Liste sind die Spaltentitel, keine Zeilen-Ids
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: browse mit drei Ebenen, Kappung und Cursor</name>
  <files>src/mcp_connector/tools/tables.py</files>
  <read_first>
    - src/mcp_connector/tools/deck.py (ganze Datei: Docstring 1 bis 23, Imports und Konstanten 25 bis 41, Browse-Kopf 44 bis 71, Projektion `_boards` 129 bis 150, Envelope 228 bis 234)
    - src/mcp_connector/tools/files.py (Cursor-Benutzung, Zeilen 95 bis 184)
    - src/mcp_connector/paging.py (ganze Datei, 91 Zeilen)
    - src/mcp_connector/tools/marks.py (without_marks, Zeile 56)
    - src/mcp_connector/nextcloud/clients/tables.py (aus Plan 02)
    - .planning/phases/08-erreichbarkeits-spike-und-tables/08-RESEARCH.md, Abschnitte "Muster 1", "Muster 2", "Muster 6", "Korrektur K8", "Falle 1", "Offene Fragen 3"
  </read_first>
  <action>
    Neue Datei `src/mcp_connector/tools/tables.py`, Aufbau wörtlich nach `tools/deck.py`.

    Modul-Docstring mit vier Absätzen: (1) ein Werkzeug mit drei Ebenen statt drei Werkzeugen
    und warum (drei Werkzeuge kosten drei Slots und drei Schemata für eine Navigation, die das
    Modell in einem Enum-Wert ausdrücken kann, D-06 und D-14); (2) das erzwungene `limit` und
    dass ein Weglassen sonst die ganze Tabelle liest; (3) die zwei Dinge, die erklärt werden,
    bevor sie scheitern können, nämlich die fehlende Tables-App am Capabilities-Gate und das
    fehlende Schreibrecht am Objekt statt an einem 403 der Nextcloud; (4) was absichtlich fehlt,
    nämlich Update, Delete, Spalten- und Schema-Anlegen und jeder Share-Pfad, und dass genau
    das die `create-only`-Annotation ehrlich macht statt zu einem Versprechen.

    Imports: `from typing import Any`, `from .. import paging`, `from ..errors import ToolError`,
    `from ..nextcloud import NcClients, capabilities`, `from ..nextcloud.clients import tables as
    tables_client`, `from . import marks`. Kein Import von `ids`: es gibt in dieser Phase keine
    adressierbare Zeile (K8), und `ids.py` bleibt unangetastet.

    Konstanten: `APP = "tables"`, `LEVELS = ("tables", "columns", "rows")`,
    `DEFAULT_LIMIT = 25`, `MAX_LIMIT = 200`, `_LEVEL_HINT` mit den drei gültigen Werten,
    `_TABLE_HINT` mit dem Satz, dass `tables_browse` mit `level=tables` die Ids liefert.

    `browse(clients, level="tables", table_id=None, limit=DEFAULT_LIMIT, cursor=None)` in dieser
    Reihenfolge: Ebene prüfen (`level not in LEVELS` ergibt `ToolError` mit `_LEVEL_HINT`),
    `capped = min(max(limit, 1), MAX_LIMIT)`, `await capabilities.require_app(clients, APP)`.

    `level="tables"`: `get_tables` holen, projizieren auf `id`, `title`, `rowsCount`,
    `columnsCount`, `isShared`, `can_create` (aus `_may_create`) und `emoji` nur wenn gesetzt;
    zur Seite gelegte Tabellen (`archived`) werden herausgefiltert wie bei Deck. Der Titel geht
    durch `marks.without_marks`, weil er fremder Text ist.

    `level="columns"` und `level="rows"` brauchen eine `table_id`; fehlt sie, ist das ein
    `ToolError` mit `_TABLE_HINT`.

    `level="columns"`: `get_columns` holen, projizieren auf `id`, `title`, `type`, `subtype`,
    `mandatory` plus die interpretationsnötigen Grenzen, wenn sie gesetzt sind
    (`selectionOptions`, `textMaxLength`, `numberMin`, `numberMax`, `numberDecimals`,
    `datetimeDefault`).

    `level="rows"`: zuerst `get_table(table_id)` für `rowsCount` und den Titel, dann den Offset
    bestimmen (`cursor` vorhanden: `paging.decode_cursor`, danach `paging.check_scope(state,
    "t", table_id, "table")`, damit ein Handle einer anderen Tabelle abgewiesen wird statt still
    die falsche Seite zu liefern, dann `paging.read_offset`), dann `get_rows_simple(table_id,
    limit=capped, offset=offset)`. Die erste Liste der Antwort sind die Spaltentitel und wird zu
    den Schlüsseln der Zeilenobjekte; sie wird nicht als Zeile mitgezählt und nicht wiederholt.
    Fehlende Werte bleiben leere Strings. Jeder Zellwert, der ein String ist, geht durch
    `marks.without_marks`, weil Zellwerte fremder Text sind und in den Modellkontext gehoben
    werden.

    Der Envelope trägt `level`, `count`, `results` für alle Ebenen, und für `level="rows"`
    zusätzlich `rowsCount` (aus der Tabellenantwort), `offset` und, wenn `rowsCount` größer
    als `offset` plus `count` ist, `truncated: True` sowie `next` als
    `paging.encode_cursor({"o": offset + count, "t": str(table_id)})`. Die Kappung wird damit
    beim Namen genannt und nie stillschweigend.

    Kein modulweiter veränderlicher Zustand, keine Caches, keine Listen auf Modulebene.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector && uv run ruff check . && uv run ruff format --check . && uv run pyright src/mcp_connector/tools/tables.py</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector && uv run python -c "
from mcp_connector.tools import tables as t
assert t.LEVELS == ('tables','columns','rows') and t.DEFAULT_LIMIT == 25 and t.MAX_LIMIT == 200
src = open('src/mcp_connector/tools/tables.py', encoding='utf-8').read()
assert 'capabilities.require_app' in src and 'paging.check_scope' in src
assert 'marks.without_marks' in src, 'cell values are foreign text and need the mark filter'
assert 'from .. import ids' not in src and 'import ids' not in src, 'ids.py stays untouched (K8)'
print('ok')
"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector && git diff --name-only src/mcp_connector/ids.py src/mcp_connector/provider_map.py src/mcp_connector/tools/context.py | wc -l | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `tables.LEVELS == ("tables", "columns", "rows")`, `DEFAULT_LIMIT == 25`, `MAX_LIMIT == 200`
    - `browse` mit einem unbekannten `level` wirft `ToolError`, dessen `hint` alle drei gültigen Werte nennt
    - `browse` mit `level="columns"` oder `level="rows"` ohne `table_id` wirft `ToolError` mit einem Hinweis auf `level=tables`
    - `capabilities.require_app(clients, "tables")` steht in `browse` vor dem ersten Tables-Request
    - Die Antwort auf `level="rows"` enthält die Schlüssel `level`, `count`, `results`, `rowsCount` und `offset`; bei mehr vorhandenen Zeilen zusätzlich `truncated` und `next`
    - Ein `next`-Handle trägt die Tabellen-Id als Scope; `paging.check_scope` wird mit dem Schlüssel der Tabellen-Id aufgerufen
    - `marks.without_marks` wird auf Zellwerte und Titel angewandt
    - Die Datei importiert `ids` nicht, und `ids.py`, `provider_map.py` und `tools/context.py` sind unverändert
    - Zur Seite gelegte Tabellen erscheinen nicht in `level="tables"`
    - `uv run pyright src/mcp_connector/tools/tables.py` ist grün, `ruff check .` und `ruff format --check .` sind über das ganze Repo grün
  </acceptance_criteria>
  <done>Ein Aufruf liest Tabellen, Spalten oder Zeilen, die Antwortform ist für alle drei Ebenen dieselbe, und eine Kappung steht immer beim Namen.</done>
</task>

<task type="auto">
  <name>Task 2: create_row mit Titel-zu-Id-Abbildung und der Eigentümer-Regel</name>
  <files>src/mcp_connector/tools/tables.py</files>
  <read_first>
    - src/mcp_connector/tools/deck.py (Create-Kopf 74 bis 126, `_require_write_permission` 195 bis 225, `_can_edit` 227 bis 232)
    - src/mcp_connector/tools/tables.py (die Datei aus Task 1)
    - src/mcp_connector/nextcloud/clients/tables.py (get_table, get_columns, create_row)
    - .planning/phases/08-erreichbarkeits-spike-und-tables/08-RESEARCH.md, Abschnitte "Korrektur K4", "Korrektur K5", "Muster 3", "Muster 4", "Titel-zu-Id-Abbildung (TABLES-02)", "Falle 2", "Falle 5", "Don't Hand-Roll"
    - .planning/phases/08-erreichbarkeits-spike-und-tables/08-PATTERNS.md, Abschnitt "Rechte-Vorprüfung mit Begründung im Docstring"
  </read_first>
  <action>
    `create_row(clients, table_id, values)` in `tools/tables.py` ergänzen. `values` ist ein
    String mit kompaktem JSON, kein Dict: ein `dict`-Parameter zieht `additionalProperties` oder
    `$defs` in das Input-Schema, und `tests/contract/test_tool_surface.py` verbietet `$defs` an
    mehreren Stellen wörtlich. Präzedenzfall in dieser Codebasis ist der Komma-String
    `unified_search.providers`. Weil die Tables-App `data` auch als JSON-String akzeptiert (K4),
    entsteht dadurch keine zweite Form.

    Reihenfolge, und die Reihenfolge ist das Muster:
    1. `await capabilities.require_app(clients, APP)`.
    2. `values` mit `json.loads` parsen. Ein `JSONDecodeError` wird zu einem `ToolError` mit
       einem Satz und einem Beispielobjekt im Hinweis, nicht zu einem `JSONDecodeError` nach
       draußen (`from None`, damit kein Traceback mitgeht). Ist das Ergebnis kein Dictionary,
       ist das ein eigener Fehlersatz ("values must be a JSON object, not a list.") mit
       Hinweis.
    3. Ein leeres Objekt wird abgelehnt, bevor irgendein Request entsteht.
    4. `get_table(table_id)` holen. Daraus die Rechte-Vorprüfung `_may_create(table)` nach der
       Korrektur K5: erlaubt, wenn `isShared` falsch ist (eigene Tabelle) oder
       `onSharePermissions.create` wahr ist oder `onSharePermissions.manage` wahr ist;
       abgelehnt nur im Rest. Der Docstring von `_may_create` begründet, warum das naive Feld
       nicht allein befragt wird: `TableService::setIsSharedState` setzt für eine eigene
       Tabelle `Permissions(read: true)` und `isShared = false`, während Nextclouds eigener
       Check in `PermissionsService::checkPermission` schon bei `userIsElementOwner` abkürzt.
       Ein wörtliches `if not onSharePermissions.create: refuse` würde jeden Nutzer auf seiner
       eigenen Tabelle abweisen. Das ist derselbe Fall wie `canCreateBoards` in Phase 1. Die
       Defensivform `permissions if isinstance(permissions, dict) else {}` bleibt wörtlich.
       Die Ablehnung nennt die Tabelle mit Id und Titel und als nächsten Schritt, den
       Eigentümer in Nextcloud um ein Schreibrecht zu bitten oder eine Tabelle zu wählen, die
       `tables_browse` mit `can_create` meldet.
    5. `get_columns(table_id)` holen und die Abbildung Titel zu Id bauen. Der Vergleich ist
       normalisiert (getrimmt und `casefold`), damit "Task" und "task " denselben Treffer
       finden.
    6. Mehrdeutig: derselbe normalisierte Titel kommt bei mehreren Spalten vor. Ablehnen und
       die betroffenen Titel samt ihrer Spalten-Ids nennen. Tables hat keine
       Unique-Bedingung auf `title`, das ist also ein realer Fall.
    7. Unbekannt: ein Schlüssel hat keine Spalte. Ablehnen mit der Liste der gültigen Titel.
    8. Fehlende Pflichtspalte: eine Spalte mit `mandatory: true` fehlt in `values`. Ablehnen mit
       ihrem Titel.
    9. Erst danach `tables_client.create_row(..., data={str(column_id): value})`. Die Schlüssel
       sind immer Spalten-Ids, niemals Titel; die App castet sie mit `(int)$key`.
    10. Die Antwort prüfen: fehlt die Id, ist das ein `ToolError` mit dem Satz, dass Nextcloud
        die Zeile angelegt, aber keine Id gemeldet hat, und dem Hinweis, in der Tables-App
        nachzusehen. Das Ergebnis trägt `id`, `table_id`, `url` (aus
        `tables_client.web_url`) und `values_written` als Abbildung Titel zu geschriebenem Wert.

    Keine clientseitige Typvalidierung der Zellwerte: die Wertform je `type` und `subtype` ist in
    der App nicht vollständig dokumentiert, und ein selbstgebauter Validator wäre eine zweite
    Wahrheit, die bei jedem neuen Spaltentyp veraltet. Ein 400 der App wird mit ihrer eigenen
    Meldung durchgereicht, weil `_status_error` "Nextcloud says: ..." anhängt. Die zwei
    Wertformen, die der Integrationstest in Plan 05 belegt, werden in der Tool-Beschreibung
    genannt.

    Neue öffentliche Funktionen, die erst Plan 04 registriert, mit einer Begründung in
    `vulture_whitelist.py` eintragen, damit `vulture` bei voller Konfidenz grün bleibt.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector && uv run ruff check . && uv run ruff format --check . && uv run pyright src/mcp_connector/tools/tables.py && uv run vulture src/mcp_connector vulture_whitelist.py</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector && uv run python -c "
from mcp_connector.tools import tables as t
own = {'id': 7, 'title': 'Aufgaben', 'isShared': False, 'onSharePermissions': {'read': True, 'create': False, 'update': False, 'delete': False, 'manage': False}}
shared_no = {'id': 8, 'title': 'Geteilt', 'isShared': True, 'onSharePermissions': {'read': True, 'create': False, 'manage': False}}
shared_yes = {'id': 9, 'title': 'Geteilt', 'isShared': True, 'onSharePermissions': {'read': True, 'create': True}}
shared_manage = {'id': 10, 'title': 'Geteilt', 'isShared': True, 'onSharePermissions': {'manage': True}}
broken = {'id': 11, 'title': 'Kaputt', 'isShared': True, 'onSharePermissions': None}
assert t._may_create(own) is True, 'the owner of a table must never be refused (K5)'
assert t._may_create(shared_no) is False
assert t._may_create(shared_yes) is True and t._may_create(shared_manage) is True
assert t._may_create(broken) is False
print('ok')
"</automated>
  </verify>
  <acceptance_criteria>
    - `_may_create` liefert `True` für `isShared: false` (eigene Tabelle) auch dann, wenn `onSharePermissions` nur `read: true` trägt
    - `_may_create` liefert `True` bei `create: true` oder `manage: true` auf einer geteilten Tabelle und `False` sonst, auch bei `onSharePermissions: null`
    - Der Docstring von `_may_create` nennt `TableService::setIsSharedState` und den Grund, warum das Feld `create` nicht allein befragt wird
    - `create_row` mit ungültigem JSON wirft `ToolError`, dessen `hint` ein Beispielobjekt enthält; kein `JSONDecodeError` verlässt die Funktion
    - `create_row` mit einer JSON-Liste statt eines Objekts wirft einen eigenen `ToolError`
    - Alle vier Ablehnungen (unbekannter Titel, mehrdeutiger Titel, fehlende Pflichtspalte, fehlendes Schreibrecht) passieren, bevor `tables_client.create_row` aufgerufen wird
    - Der an den Client übergebene `data`-Dict hat ausschließlich Spalten-Ids als Schlüssel, niemals Titel
    - Die Datei enthält keinen eigenen Typvalidator für Zellwerte und keine Liste erlaubter `column_types`
    - `uv run vulture src/mcp_connector vulture_whitelist.py` ist grün
  </acceptance_criteria>
  <done>Eine Zeile entsteht nur mit Titeln, die die Instanz wirklich hat, und der Eigentümer seiner eigenen Tabelle wird nie durch die eigene Vorprüfung ausgesperrt.</done>
</task>

<task type="auto">
  <name>Task 3: Unit-Testkatalog beider Werkzeuge</name>
  <files>tests/unit/test_tables_tools.py</files>
  <read_first>
    - tests/unit/test_deck_tools.py (Modul-Docstring 1 bis 12, Gerüst mit Capabilities-Mock und Cache-Reset 27 bis 60)
    - src/mcp_connector/tools/tables.py (die Datei aus den Tasks 1 und 2)
    - tests/fixtures/tables_tables.json, tests/fixtures/tables_columns.json, tests/fixtures/tables_rows_simple.json
    - .planning/phases/08-erreichbarkeits-spike-und-tables/08-RESEARCH.md, Abschnitt "Mechanische Checkliste, Tests" (Zeile zu test_tables_tools.py) und "Offene Fragen 4"
  </read_first>
  <action>
    Neue Datei `tests/unit/test_tables_tools.py` nach dem Gerüst von `test_deck_tools.py`:
    `CAPABILITIES_URL = f"{BASE}/ocs/v2.php/cloud/capabilities"`, die v2- und v1-Basis-URLs als
    Konstanten, ein `envelope(data)`-Helfer für die OCS-Antworten (die v1-Zeilenantwort ist
    nackt und braucht keinen), ein `capabilities_payload(*, tables=...)`-Helfer,
    `TABLES_INSTALLED = {"enabled": True, "version": "2.2.2", "apiVersions": ["1.0", "2.0"]}`,
    und `@pytest.fixture(autouse=True) def _empty_cache()` mit `capabilities.clear_cache()`.

    Der Modul-Docstring nennt zuerst die zwei Eigenschaften, die das Ein-Werkzeug-Design
    rechtfertigen, dann den Katalog der Fehlerpfade (D-15, SRV-04).

    Testkatalog, jeder Fall ein eigener Test mit sprechendem Namen:
    1. Fehlende Tables-App: `capabilities_payload(tables=None)` führt in `browse` und in
       `create_row` zu einem Fehlersatz mit gesetztem Hinweis, und zwar ohne dass ein
       Tables-Request gestellt wird (respx-Route auf die Tables-URLs mit
       `assert_all_called=False` und `len(route.calls) == 0`).
    2. Installierte, aber abgeschaltete App (`enabled: false`) verhält sich wie eine fehlende.
    3. Unbekanntes `level` wird abgelehnt, der Hinweis nennt alle drei Werte.
    4. `level="columns"` und `level="rows"` ohne `table_id` werden abgelehnt.
    5. `level="tables"` projiziert: das Ergebnis enthält `id`, `title`, `rowsCount`,
       `columnsCount`, `isShared`, `can_create` und keines der Felder `views`, `columnOrder`,
       `sort`, `ownerDisplayName`, `createdBy`, `lastEditBy`.
    6. Zur Seite gelegte Tabellen erscheinen nicht.
    7. `level="rows"` ohne `limit` benutzt den Default 25, und die gemockte Zeilen-URL trägt
       `limit=25`.
    8. `limit=5000` wird auf 200 gekappt, `limit=0` auf 1.
    9. Die Titelzeile aus `rows/simple` wird zu den Schlüsseln und erscheint nicht als Zeile:
       eine Fixture mit drei Zeilenlisten ergibt `count == 3`.
    10. `rowsCount` größer als `offset` plus `count` ergibt `truncated: True` und ein
        `next`-Handle; ein Handle einer anderen Tabelle wird abgelehnt statt still angewandt.
    11. `no_data`: eine Antwort mit nur der Titelzeile ergibt `count == 0` und keinen Fehler.
    12. `values` ist kein JSON: Fehlersatz mit Beispiel im Hinweis.
    13. `values` ist eine JSON-Liste: eigener Fehlersatz.
    14. Unbekannter Spaltentitel: Ablehnung mit der Liste der gültigen Titel im Hinweis, und
        `create_row` des Clients wurde nicht aufgerufen.
    15. Mehrdeutiger Spaltentitel (zwei Spalten mit gleichem normalisierten Titel): Ablehnung,
        die beide Spalten-Ids nennt. Der Fall wird mit einer konstruierten Spaltenliste geprüft,
        nicht am Server.
    16. Fehlende Pflichtspalte (`mandatory: true`): Ablehnung mit dem Titel der Spalte.
    17. Normalisierung: `"task "` trifft die Spalte `"Task"`.
    18. Eigene Tabelle darf schreiben, obwohl `onSharePermissions` nur `read` meldet (K5):
        der POST wird gestellt und die Antwort enthält `id`, `table_id`, `url` und
        `values_written`.
    19. Geteilte Tabelle ohne `create` und ohne `manage`: Ablehnung mit Tabellentitel und
        nächstem Schritt, und der POST wird nicht gestellt.
    20. Die Antwort der App ohne `id` ergibt einen Fehlersatz, der zum Nachsehen in der App
        rät, statt still Erfolg zu melden.
    21. Ein 400 der App wird mit ihrer eigenen Meldung durchgereicht ("Nextcloud says: ...").
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector && uv run pytest tests/unit/test_tables_tools.py -q</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector && grep -c "def test_" tests/unit/test_tables_tools.py && uv run ruff check tests/unit/test_tables_tools.py && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_tables_tools.py -q` ist grün und sammelt mindestens 21 Tests
    - Es existiert je ein Test für: fehlende App, abgeschaltete App, unbekanntes Level, fehlende table_id, Projektion, zur Seite gelegte Tabelle, Default-Limit 25 in der URL, Kappung auf 200, Titelzeile nicht als Zeile, truncated plus next, fremdes Cursor-Handle, no_data, kein JSON, JSON-Liste, unbekannter Titel, mehrdeutiger Titel, fehlende Pflichtspalte, Titel-Normalisierung, eigene Tabelle darf schreiben, geteilte Tabelle ohne create wird abgelehnt, Antwort ohne id, 400 durchgereicht
    - In den vier Ablehnungsfällen wird nachweislich kein POST gestellt (`len(create_route.calls) == 0`)
    - `uv run pytest -q` (Default-Auswahl) bleibt grün: dieser Plan registriert kein Tool, Toolzahl und Budget-Gate sind unberührt
    - `uv run python scripts/check_tool_budget.py` bleibt unter 12500 Bytes
  </acceptance_criteria>
  <done>Jeder Ablehnungspfad von TABLES-01 und TABLES-02 ist mit einem eigenen Test belegt, inklusive der beiden Fälle, die ein Happy-Path-Test nie sieht: die Eigentümer-Falle und das fremde Cursor-Handle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Modell zu Tool-Parameter | `level`, `table_id`, `values` und `cursor` kommen aus einer Modellantwort |
| Zellwerte zu Modellkontext | Tabellenzellen enthalten fremden Text, den ein anderer Nutzer geschrieben haben kann |
| Tool zu Nextcloud-ACL | Die Vorprüfung ist die bessere Fehlermeldung, nicht die Autorität; letzte Instanz bleibt Nextclouds RequirePermission-Middleware |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-13 | Elevation of Privilege | `_may_create` in tools/tables.py | mitigate | Regel nach K5: erlaubt bei `isShared == false` oder `create` oder `manage`, sonst Ablehnung mit Satz und nächstem Schritt. Zwei Unit-Tests in beide Richtungen verhindern, dass ein rot laufender Eigentümer-Test durch Lockern des Gates statt durch Korrigieren der Regel beantwortet wird |
| T-08-14 | Tampering (Prompt Injection) | Projektion der Zellwerte und Titel | mitigate | `marks.without_marks` auf jeden Stringwert, der in den Modellkontext gehoben wird; Tabellenzeilen bekommen in Phase 11 bewusst keinen Auszug in `prepare_context` |
| T-08-15 | Tampering | Titel-zu-Id-Abbildung | mitigate | Titel werden ausschließlich gegen die Spaltenliste der Instanz aufgelöst, niemals geraten; ein mehrdeutiger Titel wird abgelehnt, statt eine der beiden Spalten zu wählen und in die falsche Spalte zu schreiben |
| T-08-16 | Denial of Service | `browse(level="rows")` | mitigate | Kappung im Tool (`min(max(limit, 1), MAX_LIMIT)`) zusätzlich zur Kappung im Client, `truncated` in der Antwort; ein Aufruf ohne Limit liest 25 Zeilen und nicht die Tabelle |
| T-08-17 | Tampering | Cursor-Handle | mitigate | `paging.check_scope` mit der Tabellen-Id als Scope: ein Handle aus einer anderen Tabelle wird abgelehnt statt still auf die falsche Seite angewandt. Handles tragen kein Geheimnis und keine Autorität |
| T-08-18 | Error Handling | `values`-Parsing | mitigate | `json.JSONDecodeError` wird mit `from None` in einen Satz plus Beispielobjekt übersetzt; kein Traceback und keine Rohantwort verlassen das Tool |
| T-08-19 | Information Disclosure | Fehlersätze der Ablehnungen | accept | Die Ablehnung nennt Titel und Ids der Spalten der angefragten Tabelle. Das ist genau der Inhalt, den der Nutzer ohnehin lesen darf, weil die Vorprüfung erst nach `get_table` und `get_columns` unter seiner eigenen Identität läuft |
| T-08-SC | Tampering | Paketinstallation | accept | Keine neue Sprachabhängigkeit in dieser Phase; `pyproject.toml` und `uv.lock` bleiben unangetastet |
</threat_model>

<verification>
1. `uv run ruff check .`, `uv run ruff format --check .`, `uv run pyright` und `uv run vulture src/mcp_connector vulture_whitelist.py` sind grün.
2. `uv run pytest tests/unit/test_tables_tools.py -q` ist grün mit mindestens 21 Tests.
3. `uv run pytest -q` (Default-Auswahl) ist grün, weil dieser Plan kein Werkzeug registriert.
4. `uv run python scripts/check_tool_budget.py` bleibt unter dem bestehenden Gate von 12500 Bytes.
5. `git diff --name-only` nennt `src/mcp_connector/ids.py`, `src/mcp_connector/provider_map.py` und `src/mcp_connector/tools/context.py` nicht.
</verification>

<success_criteria>
- Erfolgskriterium 3 der Phase ist auf Unit-Ebene erfüllt: Tabellen, Spalten und Zeilen über einen Aufruf, Default 25, Max 200, `rowsCount` genannt, Kappung benannt, kein Aufruf ohne Limit.
- Erfolgskriterium 4 der Phase ist auf Unit-Ebene erfüllt: Zeile mit Titeln, und alle drei Ablehnungen passieren vor dem Schreiben.
- Der erste Teil von Erfolgskriterium 5 ist erfüllt: die Vorprüfung liefert den Fehlersatz statt eines 403 aus der Nextcloud. Das Gate mit Gegenprobe folgt in Plan 04.
- Die Registry ist unberührt: kein rotes Fenster in den bestehenden Gates.
</success_criteria>

<output>
Create `.planning/phases/08-erreichbarkeits-spike-und-tables/08-03-SUMMARY.md` when done.
Die Zusammenfassung nennt die Signaturen von `browse` und `create_row` wörtlich, weil Plan 04
das Schema daraus baut.
</output>
</content>
