---
phase: 08-erreichbarkeits-spike-und-tables
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp_connector/nextcloud/clients/tables.py
  - src/mcp_connector/nextcloud/clients/ocs.py
  - src/mcp_connector/nextcloud/capabilities.py
  - tests/unit/test_tables_client.py
  - tests/unit/test_ocs_capabilities.py
  - tests/fixtures/tables_tables.json
  - tests/fixtures/tables_columns.json
  - tests/fixtures/tables_rows_simple.json
autonomous: true
requirements: [TABLES-01]

must_haves:
  truths:
    - "Eine Zeilen-URL kann nicht ohne limit entstehen: der Client verlangt limit als Keyword ohne Default und kappt auf 200"
    - "Die zwei Schreibweisen der Tables-App liegen als benannte Konstanten vor: nodeType im Singular für den Spaltenaufruf, nodeCollection im Plural für das Zeile-Anlegen"
    - "Eine nicht numerische Pfad-Id wird vor dem Request abgelehnt, mit einem Satz und einem nächsten Schritt"
    - "Das Projekt kann über OCS schreiben: ocs_post trägt beide Pflichtheader plus Content-Type, authentifiziert pro Aufruf, folgt keinem Redirect und sendet nie einen Origin-Header"
    - "Eine Instanz mit installierter, aber abgeschalteter Tables-App gilt als nicht vorhanden: das Gate liest tables.enabled und nicht die Präsenz der Sektion"
  artifacts:
    - path: "src/mcp_connector/nextcloud/clients/tables.py"
      provides: "HTTP-Adapter der Tables-Familie über zwei API-Generationen, lesend plus ein Create"
      exports: ["get_tables", "get_table", "get_columns", "get_rows_simple", "create_row", "api_url", "web_url", "TABLES_HEADERS", "MAX_ROWS", "NODE_TYPE_TABLE", "NODE_COLLECTION_TABLES"]
      min_lines: 150
    - path: "src/mcp_connector/nextcloud/clients/ocs.py"
      provides: "ocs_post als erste OCS-Schreibnaht des Projekts"
      contains: "async def ocs_post"
    - path: "src/mcp_connector/nextcloud/capabilities.py"
      provides: "tables_available aus tables.enabled, tables_api_versions, Fehlersatz für die fehlende App"
      contains: "tables_available"
    - path: "tests/unit/test_tables_client.py"
      provides: "respx-Unit-Tests inklusive URL-Behauptungen, Status 200 beim Create und no_data"
      min_lines: 180
    - path: "tests/fixtures/tables_tables.json"
      provides: "Realistische Rohantwort mit eigener und geteilter Tabelle für die K5-Prüfung"
      contains: "onSharePermissions"
  key_links:
    - from: "src/mcp_connector/nextcloud/clients/tables.py"
      to: "src/mcp_connector/nextcloud/clients/ocs.py"
      via: "ocs.ocs_url, ocs.ocs_post, ocs.parse_ocs, ocs.parse_app_json"
      pattern: "ocs\\.(ocs_url|ocs_post|parse_ocs|parse_app_json)"
    - from: "src/mcp_connector/nextcloud/capabilities.py"
      to: "tables.enabled aus /ocs/v2.php/cloud/capabilities"
      via: "parse() liest das Feld enabled statt der Sektionspräsenz"
      pattern: "enabled"
---

<objective>
Die Transportschicht der Tables-Familie: ein Client-Modul für beide API-Generationen, die erste
OCS-Schreibnaht des Projekts, und die App-Erkennung für `tables`.

Purpose: Die Fachlogik (Plan 03) darf sich nicht mit Generationswahl, Pfad-Schreibweisen und dem
erzwungenen `limit` beschäftigen. Diese Regeln gehören dorthin, wo die URL entsteht. Der
gefährlichste Fehler der Phase, eine Zeilen-URL ohne `limit`, wird hier konstruktiv unmöglich
gemacht.

Output: `clients/tables.py`, `ocs.ocs_post`, die Tables-Zweige in `capabilities.py`, drei
Fixtures und die zugehörigen Unit-Tests. Es entsteht in diesem Plan noch kein Tool und keine
Registrierung, `tools/list` bleibt unverändert und alle bestehenden Gates bleiben grün.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-erreichbarkeits-spike-und-tables/08-RESEARCH.md
@.planning/phases/08-erreichbarkeits-spike-und-tables/08-PATTERNS.md

Vorbild für das Client-Modul (Docstring, Konstanten, URL-Bau, Wächter, Formprüfer):
@src/mcp_connector/nextcloud/clients/deck.py

Selbst-Analog für ocs_post und die Parser:
@src/mcp_connector/nextcloud/clients/ocs.py

Selbst-Analog für die vier Capability-Stellen:
@src/mcp_connector/nextcloud/capabilities.py

Vorbild für die Unit-Tests und die Fixtures:
@tests/unit/test_deck_client.py
@tests/fixtures/deck_boards.json

<interfaces>
Bestehende Naht, unverändert benutzen (src/mcp_connector/nextcloud/clients/ocs.py):
  OCS_PREFIX = "/ocs/v2.php"
  OCS_HEADERS: Mapping[str, str]
  def ocs_url(creds: Credentials, path: str) -> str
  async def ocs_get(client, creds, path, params=None) -> httpx.Response
  def parse_ocs(response, what: str) -> Any          # OCS-Envelope
  def parse_app_json(response, what: str) -> Any     # nackte App-Antwort
  def _status_error(status, detail, what) -> ToolError   # hängt "Nextcloud says: ..." an

Bestehende Capabilities-Schnittstelle (src/mcp_connector/nextcloud/capabilities.py):
  @dataclass(frozen=True, slots=True) class Capabilities:
      notes_available: bool; notes_api_versions: tuple[str, ...]
      deck_available: bool; deck_api_versions: tuple[str, ...]; can_create_boards: bool
      def has(self, app: str) -> bool
  async def load(clients) -> Capabilities        # Cache 60 s, nicht anfassen
  def clear_cache() -> None                      # nicht anfassen
  async def require_app(clients, app) -> Capabilities   # nicht anfassen
  def app_missing(app: str) -> AppMissingError   # nicht anfassen
  def parse(data: Any) -> Capabilities
  def _versions(section, key) -> tuple[str, ...]
  _MISSING: dict[str, tuple[str, str]]           # message plus hint je App

Zielrouten dieser Phase (gegen Tables 2.2.2 verifiziert):
  GET  /ocs/v2.php/apps/tables/api/2/tables                    parse_ocs
  GET  /ocs/v2.php/apps/tables/api/2/tables/{id}               parse_ocs   (rowsCount, isShared, onSharePermissions, title)
  GET  /ocs/v2.php/apps/tables/api/2/columns/table/{tableId}   parse_ocs   (nodeType Singular)
  GET  /index.php/apps/tables/api/1/tables/{id}/rows/simple?limit=&offset=  parse_app_json
  POST /ocs/v2.php/apps/tables/api/2/tables/{id}/rows          parse_ocs   (nodeCollection Plural, Antwort 200)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OCS-Schreibnaht, Tables-Client und drei Fixtures</name>
  <files>src/mcp_connector/nextcloud/clients/ocs.py, src/mcp_connector/nextcloud/clients/tables.py, tests/fixtures/tables_tables.json, tests/fixtures/tables_columns.json, tests/fixtures/tables_rows_simple.json</files>
  <read_first>
    - src/mcp_connector/nextcloud/clients/deck.py (ganze Datei: Docstring 1 bis 28, Imports 30 bis 38, Konstanten 40 bis 67, api_url und web_url 70 bis 79, get_stacks 108 bis 123, create_card 145 bis 175, _path_id 210 bis 218, _as_list 221 bis 236)
    - src/mcp_connector/nextcloud/clients/ocs.py (ocs_url 57 bis 62, ocs_get 64 bis 83, OCS_HEADERS 36 bis 47, parse_ocs 126 bis 147, parse_app_json 150 bis 168, _status_error 219 bis 263)
    - tests/fixtures/deck_boards.json
    - .planning/phases/08-erreichbarkeits-spike-und-tables/08-RESEARCH.md, Abschnitte "Korrekturen K3, K4, K8, K9, K10, K11", "Tables-API-Referenz", "Codebeispiele", "Anti-Muster in dieser Phase"
    - .planning/phases/08-erreichbarkeits-spike-und-tables/08-PATTERNS.md, Abschnitte "clients/tables.py", "clients/ocs.py (ocs_post)", "tests/fixtures/tables_*.json"
  </read_first>
  <action>
    Zuerst `ocs_post` in `src/mcp_connector/nextcloud/clients/ocs.py` direkt unter `ocs_get`
    ergänzen: Signatur `(client: httpx.AsyncClient, creds: Credentials, path: str, json_body:
    Mapping[str, Any]) -> httpx.Response`, POST auf `ocs_url(creds, path)` mit
    `json=dict(json_body)`, `headers={**OCS_HEADERS, "Content-Type": "application/json"}` und
    `auth=creds.auth()`. Der Docstring nennt drei Punkte: dass dies der erste Schreibaufruf
    dieses Projekts über OCS ist (K9), dass Redirects nie gefolgt werden (der Client ist so
    gebaut, eine umleitende Basis-URL ist ein Konfigurationsfehler), und dass niemals ein
    `Origin`-Header gesetzt wird, weil Nextclouds CORS-Middleware bei anliegendem `Origin` eine
    Basic-Reauthentifizierung verlangt, die unter Impersonation nicht existiert. `parse_ocs`,
    `parse_app_json` und `_status_error` bleiben unangetastet.

    Dann `src/mcp_connector/nextcloud/clients/tables.py` neu anlegen, Aufbau wörtlich nach
    `clients/deck.py`.

    Modul-Docstring: die zwei API-Generationen und warum sie in einem Modul liegen (eine
    Familie, ein Modul, die Trennlinie darf nicht beim Aufrufer landen); die beiden
    Pflichtheader nach D-18; die zwei Schreibweisen `nodeType` (Singular) und `nodeCollection`
    (Plural) und dass ein Verwechseln einen 404 aus der Routing-Schicht ergibt, der wie
    "Tabelle nicht gefunden" aussieht; dass die v1-Zeilenrouten `#[NoCSRFRequired]`, `#[CORS]`
    und `#[OpenAPI(scope: SCOPE_DEFAULT)]` tragen und damit eine zugesagte API sind (K10, der
    Grund, warum hier kein Ersetzbarkeits-Hinweis nötig ist); dass `limit` erzwungen ist, weil
    ein Weglassen die ganze Tabelle liest; dass es absichtlich keinen Update-, Delete-,
    Spalten-, Schema- und Share-Pfad gibt, weil der günstigste Weg, das Versprechen zu halten,
    darin besteht, den Code nie zu schreiben; und dass auf dem POST kein Retry stattfindet,
    weil eine doppelte Zeile Datenkorruption ist, die dieser Server nicht aufräumen kann.

    Imports wie im Deck-Client: `from collections.abc import Mapping`, `from typing import Any`,
    `import httpx`, `from ...errors import ToolError`, `from ..credentials import Credentials`,
    `from . import ocs`.

    Konstanten, jede mit `#:`-Kommentar, der die Entscheidung begründet:
    `V1_PREFIX = "/index.php/apps/tables/api/1"`,
    `V2_PREFIX = "/apps/tables/api/2"` (liegt unterhalb von `/ocs/v2.php` und wird durch
    `ocs.ocs_url` gebaut), `NODE_TYPE_TABLE = "table"`, `NODE_COLLECTION_TABLES = "tables"`,
    `MAX_ROWS = 200`, und `TABLES_HEADERS: Mapping[str, str]` mit `OCS-APIRequest: true`,
    `Content-Type: application/json` und `Accept: application/json`.

    `api_url(creds, path="")` für die v1-Generation mit Slash-Wächter wie im Deck-Client,
    `web_url(creds, table_id)` für den Link, den ein Mensch öffnet (`/index.php/apps/tables/#/table/{id}`),
    immer aus `creds.base_url` gebaut.

    Fünf Funktionen, alle mit der Signaturform `(client, creds, ...)` und Wächter zuerst:
    `get_tables` (GET v2 `/tables`, `parse_ocs`, `_as_list`), `get_table(table_id)` (GET v2
    `/tables/{id}`, `parse_ocs`, `_as_dict`; der Docstring nennt, dass genau dieser Aufruf
    `rowsCount`, `isShared`, `onSharePermissions` und `title` in einem Request liefert und
    damit Kappungsmarkierung und Schreibrecht-Vorprüfung zugleich bedient, K11),
    `get_columns(table_id)` (GET v2 `/columns/{NODE_TYPE_TABLE}/{id}`, `parse_ocs`, `_as_list`),
    `get_rows_simple(table_id, *, limit, offset=0)` (GET v1
    `/tables/{id}/rows/simple` mit `params={"limit": capped, "offset": max(int(offset), 0)}`,
    `capped = min(max(int(limit), 1), MAX_ROWS)`, `parse_app_json`, `_as_rows`; `limit` ist ein
    Keyword ohne Default, damit ein Weglassen ein Fehler beim Entwickler und kein
    Volltabellenlesen beim Nutzer ist), und `create_row(table_id, *, data)` (POST über
    `ocs.ocs_post` auf `f"{V2_PREFIX}/{NODE_COLLECTION_TABLES}/{table}/rows"` mit Body
    `{"data": data}`, `parse_ocs`, `_as_dict`; der Docstring sagt, dass die Antwort 200 ist und
    nicht 201, und dass es keinen Retry gibt).

    Drei private Helfer: `_path_id(value, what)` wörtlich aus dem Deck-Client, Hinweistext auf
    `tables_browse` umgeschrieben; `_as_list` und `_as_dict` nach dem Deck-Muster mit einem
    Satz plus Hinweis statt eines `TypeError`; und `_as_rows(payload)` für die Liste von
    Listen aus `rows/simple` (K8), mit einem Docstring, der festhält, dass die erste Liste die
    Spaltentitel trägt, dass es keine Zeilen-Ids gibt und dass `limit=25` deshalb 26 Listen
    liefert.

    Kein modulweiter veränderlicher Zustand, kein eigener Cache, keine modulweite Liste:
    `tests/contract/test_no_destructive_calls.py::ALLOWED_MODULE_STATE` hat genau zwei
    Einträge und darf nicht wachsen. Großgeschriebene Konstanten sind ausgenommen.

    Zuletzt drei Fixtures nach dem Muster von `tests/fixtures/deck_boards.json`, also
    realistische vollständige Rohantworten mit deutschen Titeln und echten Umlauten:
    `tables_tables.json` mit mindestens zwei Tabellen, davon eine eigene (`isShared: false`,
    `onSharePermissions: {read: true, create: false, update: false, delete: false, manage:
    false}`, `ownership` gleich dem Testnutzer) und eine geteilte ohne `create`
    (`isShared: true`, `onSharePermissions.create: false`), beide mit den Feldern, die die
    Projektion wegwerfen muss (`views`, `columnOrder`, `sort`, `ownerDisplayName`, `createdBy`,
    `lastEditBy`), plus je `rowsCount`, `columnsCount`, `archived` und `favorite`;
    `tables_columns.json` mit mindestens vier Spalten, darunter eine mit `mandatory: true`,
    eine `selection` mit `selectionOptions`, eine `datetime` und zwei mit demselben Titel in
    unterschiedlicher Schreibweise, damit der Mehrdeutigkeitsfall ohne Server prüfbar ist;
    `tables_rows_simple.json` als Liste von Listen, erste Liste die Titel, danach drei
    Zeilenlisten mit einem leeren String für einen fehlenden Wert.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector && uv run ruff check . && uv run ruff format --check . && uv run pyright src/mcp_connector/nextcloud/clients/tables.py src/mcp_connector/nextcloud/clients/ocs.py</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector && uv run python -c "
import json, inspect
from mcp_connector.nextcloud.clients import tables as t, ocs
from mcp_connector.errors import ToolError
assert t.MAX_ROWS == 200 and t.NODE_TYPE_TABLE == 'table' and t.NODE_COLLECTION_TABLES == 'tables'
assert inspect.signature(t.get_rows_simple).parameters['limit'].default is inspect.Parameter.empty
assert hasattr(ocs, 'ocs_post')
try:
    t._path_id('7; DROP', 'table id'); raise SystemExit('non numeric id was accepted')
except ToolError as err:
    assert err.hint
for name in ('tables_tables','tables_columns','tables_rows_simple'):
    json.load(open(f'tests/fixtures/{name}.json', encoding='utf-8'))
print('ok')
"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector && grep -c "Origin" src/mcp_connector/nextcloud/clients/tables.py src/mcp_connector/nextcloud/clients/ocs.py | grep -v ':0' | grep -qv 'tables.py' && grep -vn '^\s*#' src/mcp_connector/nextcloud/clients/tables.py | grep -c "DELETE\|PROPPATCH\|\.delete(" | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pyright` meldet für beide geänderten Module keine Fehler, `ruff check .` und `ruff format --check .` sind über das ganze Repo grün
    - `ocs.ocs_post` existiert, setzt `Content-Type: application/json` zusätzlich zu `OCS_HEADERS`, übergibt `auth=creds.auth()` pro Aufruf und enthält kein `follow_redirects=True`
    - `inspect.signature(tables.get_rows_simple).parameters["limit"].default` ist `Parameter.empty`, `limit` ist also ein Keyword ohne Default
    - `tables.MAX_ROWS == 200`, `tables.NODE_TYPE_TABLE == "table"`, `tables.NODE_COLLECTION_TABLES == "tables"`
    - `tables._path_id("7; DROP", "table id")` wirft `ToolError` mit gesetztem `hint`
    - Das Modul `clients/tables.py` enthält in ausführbarem Code (Kommentare gefiltert) kein `DELETE`, kein `PROPPATCH`, kein `.delete(` und keinen `Origin`-Header
    - Das Modul enthält keinen modulweiten veränderlichen Zustand: keine Zuweisung eines `dict`, `list` oder `set` auf einen kleingeschriebenen Modulnamen
    - Alle drei Fixtures sind gültiges JSON, `tables_tables.json` enthält genau eine Tabelle mit `isShared: false` und eine mit `isShared: true` ohne `create`, `tables_columns.json` enthält mindestens eine Spalte mit `mandatory: true` und zwei Spalten mit gleichem Titel in abweichender Schreibweise
    - `tables_rows_simple.json` ist eine Liste von Listen, deren erste Liste die Spaltentitel trägt
  </acceptance_criteria>
  <done>Der Tables-Client spricht beide Generationen, kann konstruktiv keine Zeilen-URL ohne `limit` bauen, hat keinen Update-, Delete- oder Schema-Pfad, und `ocs_post` ist die einzige neue Schreibnaht.</done>
</task>

<task type="auto">
  <name>Task 2: Unit-Tests des Clients gegen die gebaute Anfrage</name>
  <files>tests/unit/test_tables_client.py</files>
  <read_first>
    - tests/unit/test_deck_client.py (Modul-Docstring 1 bis 12, Testgerüst 14 bis 49, Header-Behauptung 65 bis 79, Query-Parameter-Behauptung 98 bis 107)
    - src/mcp_connector/nextcloud/clients/tables.py (die Datei aus Task 1)
    - tests/fixtures/tables_tables.json, tests/fixtures/tables_columns.json, tests/fixtures/tables_rows_simple.json
    - .planning/phases/08-erreichbarkeits-spike-und-tables/08-RESEARCH.md, Abschnitte "Muster 5: Positive Behauptung, wo ein Denylist-Gate blind ist", "Mechanische Checkliste, Tests", "Falle 4: Auf 201 prüfen"
  </read_first>
  <action>
    Neue Datei `tests/unit/test_tables_client.py` nach dem Gerüst von `test_deck_client.py`:
    Konstanten `BASE = "http://nc.test"`, `USER = "alice"`, `SECRET = "app-password-test"`, die
    Fixtures `creds` und `client` (`httpx.AsyncClient(follow_redirects=False)`), der
    `fixture()`-Leser auf `tests/fixtures`, `@pytest.mark.anyio` je Test.

    Die Endpunkt-URLs als eingefrorene Modul-Literale, denn sie sind gleichzeitig der Schutz
    gegen die Verwechslung von `nodeType` und `nodeCollection`:
    `V2_BASE = f"{BASE}/ocs/v2.php/apps/tables/api/2"`,
    `TABLES_URL = f"{V2_BASE}/tables"`, `TABLE_URL = f"{V2_BASE}/tables/7"`,
    `COLUMNS_URL = f"{V2_BASE}/columns/table/7"` (Singular),
    `CREATE_ROW_URL = f"{V2_BASE}/tables/7/rows"` (Plural),
    `ROWS_URL = f"{BASE}/index.php/apps/tables/api/1/tables/7/rows/simple"`.

    Der Modul-Docstring nennt zuerst die zwei Eigenschaften, die Vertrag sind (eine Zeilen-URL
    trägt immer `limit`, und die Parser-Wahl hängt an der Generation), danach den Katalog der
    Fehler- und Randfälle.

    Testkatalog, jeder Fall ein eigener Test:
    1. Beide Pflichtheader liegen an jedem GET an (`OCS-APIRequest: true` und
       `Content-Type: application/json`), geprüft über `route.calls[0].request.headers`.
    2. Die Zeilen-URL trägt immer `limit`: `respx` mockt `ROWS_URL`, danach
       `assert "limit=25" in str(route.calls.last.request.url)` und
       `assert route.calls.last.request.url.params["offset"] == "0"`.
    3. `limit` wird auf `MAX_ROWS` gekappt: Aufruf mit `limit=5000` ergibt `limit=200` in der URL.
    4. `limit` unter 1 wird auf 1 gehoben, `offset` unter 0 auf 0.
    5. Der Spaltenaufruf trifft den Singular: die gemockte Route ist `COLUMNS_URL`, und ein
       Aufruf gegen eine Route mit `columns/tables/7` wird nicht bedient (respx mit
       `assert_all_called=True` und nur der Singular-Route registriert).
    6. Das Zeile-Anlegen trifft den Plural und wird mit Status 200 beantwortet, nicht 201; die
       Antwort geht durch `parse_ocs`, und der gesendete Body ist genau `{"data": {...}}`,
       geprüft über `json.loads(route.calls.last.request.content)`.
    7. Der Create-Request trägt `Content-Type: application/json` und keinen `Origin`-Header:
       `assert "origin" not in {k.lower() for k in request.headers}`.
    8. `parse_app_json` gegen eine 403-Antwort mit Körper `{"message": "..."}` ergibt einen
       `ToolError`, dessen `message` einen Satz und dessen `hint` einen nächsten Schritt
       trägt, und dessen Text die Meldung der App enthält.
    9. `no_data`: `rows/simple` antwortet mit einer Liste, die nur die Titelzeile enthält;
       `_as_rows` liefert die Titelzeile plus null Zeilen, ohne zu werfen.
    10. Eine leere Tabellenliste (`[]` im OCS-Envelope) ist kein Fehler.
    11. Eine nicht numerische `table_id` wird vor dem Request abgelehnt: `respx` mit
        `assert_all_called=False` und die Behauptung, dass keine Anfrage gestellt wurde
        (`len(route.calls) == 0`) plus `pytest.raises(ToolError)`.
    12. Eine Antwortform, die nicht passt (ein Objekt, wo eine Liste erwartet wird), ergibt
        einen `ToolError` und keinen `TypeError`.
    13. `web_url` baut den Link immer aus `creds.base_url`, nie aus einer Antwort.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector && uv run pytest tests/unit/test_tables_client.py -q</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector && uv run pytest tests/unit/test_tables_client.py -q --collect-only | tail -3 && grep -c "def test_" tests/unit/test_tables_client.py && grep -n "limit=25\|limit=200" tests/unit/test_tables_client.py && uv run ruff check tests/unit/test_tables_client.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_tables_client.py -q` ist grün und sammelt mindestens 13 Tests
    - Mindestens ein Test behauptet `"limit=25" in str(...request.url)` und mindestens einer die Kappung auf `limit=200`
    - Ein Test behauptet den Singular `columns/table/7` und ein Test den Plural `tables/7/rows`; beide URLs stehen als Modul-Literale in der Datei
    - Ein Test beantwortet das Zeile-Anlegen mit Status 200 und ist grün; es existiert kein Test, der 201 erwartet (`grep -c "201" tests/unit/test_tables_client.py` ist 0)
    - Ein Test behauptet, dass am Create-Request kein `origin`-Header anliegt
    - Ein Test belegt, dass eine nicht numerische `table_id` null Anfragen erzeugt
    - Ein Test deckt `no_data` ab (nur Titelzeile) und ein Test die leere Tabellenliste
    - Ein Test belegt, dass eine 403-Antwort mit `{"message": ...}` einen `ToolError` mit gesetztem `hint` erzeugt
    - `uv run ruff check tests/unit/test_tables_client.py` ist grün
  </acceptance_criteria>
  <done>Die Eigenschaften, die eine geparste Antwort nicht zeigt, sind an der gebauten Anfrage behauptet: erzwungenes `limit`, richtige Schreibweise je Route, kein `Origin`, kein Request bei ungültiger Id.</done>
</task>

<task type="auto">
  <name>Task 3: App-Erkennung für Tables über tables.enabled</name>
  <files>src/mcp_connector/nextcloud/capabilities.py, tests/unit/test_ocs_capabilities.py</files>
  <read_first>
    - src/mcp_connector/nextcloud/capabilities.py (ganze Datei: _MISSING 41 bis 56, Capabilities und has() 59 bis 76, load 82 bis 93, require_app 101 bis 110, app_missing 113 bis 116, parse 119 bis 147)
    - tests/unit/test_ocs_capabilities.py (die Deck-Fälle, Zeilen 80 bis 120 und 220 bis 231)
    - tests/fixtures/ocs_capabilities.json
    - .planning/phases/08-erreichbarkeits-spike-und-tables/08-RESEARCH.md, Abschnitte "Muster 2: require_app als erste Zeile", "Wichtige Antwortfelder" (capabilities.tables)
    - .planning/phases/08-erreichbarkeits-spike-und-tables/08-PATTERNS.md, Abschnitt "nextcloud/capabilities.py"
  </read_first>
  <action>
    Die vier Deck-Stellen in `capabilities.py` symmetrisch um Tables erweitern, ohne `load`,
    `clear_cache`, `require_app`, `app_missing` und den Cache `_cache` anzufassen.

    1. `_MISSING` bekommt den Eintrag `"tables"` mit einer Meldung ("The Tables app is not
       enabled on this Nextcloud.") und einem Hinweis, der genau eine Sache nennt, die der
       Nutzer tun kann (einen Administrator bitten, die Tables-App zu aktivieren, oder die
       Liste in einer Notiz mit `notes_create` führen).
    2. `Capabilities` bekommt die Felder `tables_available: bool = False` und
       `tables_api_versions: tuple[str, ...] = ()`. Die Reihenfolge folgt dem bestehenden
       Muster (erst Verfügbarkeit, dann Versionen).
    3. `has()` bekommt `"tables": self.tables_available` in das `flags`-Dictionary. Ein
       unbekannter Name bleibt ein `ValueError`.
    4. `parse()` liest die Sektion `tables` defensiv und setzt `tables_available` aus
       `bool(section_tables.get("enabled"))`, nicht aus der Präsenz der Sektion. Ein
       Kommentar begründet den Unterschied zu Deck in einem Satz: Tables veröffentlicht ein
       ausdrückliches `enabled`, und eine installierte, aber abgeschaltete App ist für diesen
       Server nicht vorhanden. `tables_api_versions` kommt aus dem bestehenden `_versions`-Helfer
       mit dem Schlüssel `apiVersions`. Kein Gate auf `version`.

    Danach `tests/unit/test_ocs_capabilities.py` nach dem Muster der Deck-Fälle erweitern:
    Tables vorhanden mit `enabled: true` und `apiVersions: ["1.0", "2.0"]` ergibt
    `tables_available is True` und beide Versionen im Tupel; Sektion vorhanden, aber
    `enabled: false` ergibt `tables_available is False` (dieser Fall ist der Grund für die
    Abweichung von Deck und braucht einen eigenen Test mit erklärendem Docstring); Sektion
    fehlt ganz ergibt `tables_available is False`; `apiVersions` als einzelner String wird
    toleriert; `has("tables")` liefert den Wert und `has("spreed")` wirft weiterhin
    `ValueError`; und `require_app(clients, "tables")` gegen eine Instanz ohne Tables ergibt den
    Fehlersatz aus `_MISSING` mit gesetztem Hinweis.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector && uv run pytest tests/unit/test_ocs_capabilities.py -q && uv run pyright src/mcp_connector/nextcloud/capabilities.py && uv run ruff check . && uv run ruff format --check .</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector && uv run python -c "
from mcp_connector.nextcloud import capabilities as c
def payload(section):
    return {'capabilities': section}
on = c.parse(payload({'tables': {'enabled': True, 'apiVersions': ['1.0','2.0']}}))
off = c.parse(payload({'tables': {'enabled': False, 'apiVersions': ['1.0','2.0']}}))
gone = c.parse(payload({'core': {}}))
assert on.tables_available is True and on.tables_api_versions == ('1.0','2.0')
assert off.tables_available is False, 'an installed but disabled Tables app must count as absent'
assert gone.tables_available is False and on.has('tables') is True
assert 'tables' in c._MISSING and all(c._MISSING['tables'])
print('ok')
"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `Capabilities` trägt die Felder `tables_available` und `tables_api_versions` mit den Defaults `False` und `()`
    - `parse()` mit `{"tables": {"enabled": false, "apiVersions": ["1.0","2.0"]}}` ergibt `tables_available is False`; mit `enabled: true` ergibt es `True` und beide Versionen im Tupel
    - `has("tables")` liefert den Wert, `has("spreed")` wirft weiterhin `ValueError`
    - `_MISSING["tables"]` existiert und beide Zeichenketten sind nicht leer; der Hinweis nennt genau eine Handlung
    - `load`, `clear_cache`, `require_app`, `app_missing` und `_cache` sind unverändert (`git diff` zeigt in diesen Funktionen keine Zeile)
    - `uv run pytest tests/unit/test_ocs_capabilities.py -q` ist grün und enthält einen Test für den Fall `enabled: false`
    - `uv run pytest -q` (Default-Auswahl) ist grün: dieser Plan registriert kein Tool, also bleiben Toolzahl, Budget-Gate und Contract-Tests unberührt
  </acceptance_criteria>
  <done>Eine Instanz ohne aktivierte Tables-App wird erkannt, bevor der erste Tables-Request entsteht, und der Nutzer liest einen Satz plus eine Handlung statt eines 404 auf einer HTML-Seite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Modell zu URL-Pfad | `table_id` kommt aus einer Modellantwort und wandert in den Pfad einer Nextcloud-URL |
| Client zu Nextcloud | Alle URLs werden aus der konfigurierten Basis-URL gebaut; die Antwort ist fremde Eingabe |
| Antwortkörper zu Tool-Schicht | Die Rohantwort der App darf nie unverarbeitet weitergegeben werden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Spoofing, Information Disclosure | `_path_id` in clients/tables.py | mitigate | Jede Pfad-Id muss `isdigit()` erfüllen, sonst `ToolError` vor dem Request (Fortschreibung von T-01-63); Unit-Test belegt null Anfragen bei ungültiger Id |
| T-08-07 | Denial of Service | `get_rows_simple` | mitigate | `limit` ist Keyword ohne Default und wird auf `MAX_ROWS = 200` gekappt; ein Unit-Test behauptet `limit=` in der gebauten URL, nicht im Ergebnis |
| T-08-08 | Information Disclosure (SSRF) | `api_url`, `web_url`, `ocs.ocs_url` | mitigate | Alle URLs entstehen aus `creds.base_url`, niemals aus einer Antwort oder einem Request-Feld; `follow_redirects=False` bleibt |
| T-08-09 | Tampering | `ocs_post` und alle Tables-GETs | mitigate | Kein `Origin`-Header, nirgends: mit `Origin` verlangt Nextclouds CORS-Middleware eine Basic-Reauthentifizierung, die unter Impersonation nicht existiert. Unit-Test behauptet die Abwesenheit |
| T-08-10 | Tampering | `create_row` | mitigate | Kein Retry auf dem POST, auf keiner Schicht: eine doppelte Zeile ist Datenkorruption, die dieser Server nicht aufräumen kann, weil DELETE per Gate verboten ist. Der Docstring sagt, dass ein Timeout nicht bedeutet, dass nichts geschrieben wurde |
| T-08-11 | Elevation of Privilege | clients/tables.py als Ganzes | mitigate | Es existiert kein Update-, Delete-, Spalten-, Schema-, Transfer- und Share-Pfad im Modul; das Gate in Plan 04 friert das mit Nadeln plus Gegenprobe fest |
| T-08-12 | Error Handling | `parse_ocs`, `parse_app_json`, `_status_error` | mitigate | Kein neuer Fehlerpfad: der 400 der App wird mit ihrer eigenen Meldung durchgereicht ("Nextcloud says: ..."); niemals Stacktrace, niemals HTML-Loginseite |
| T-08-SC | Tampering | Paketinstallation | accept | Keine neue Sprachabhängigkeit: `pyproject.toml` und `uv.lock` bleiben unangetastet, es gibt kein `uv add`. Damit entfällt der Legitimitäts-Checkpoint |
</threat_model>

<verification>
1. `uv run ruff check .` und `uv run ruff format --check .` über das ganze Repo grün.
2. `uv run pyright` grün für die drei geänderten Module.
3. `uv run pytest tests/unit/test_tables_client.py tests/unit/test_ocs_capabilities.py -q` grün.
4. `uv run pytest -q` (Default-Auswahl) grün: die Toolzahl ist unverändert, also bleiben `tests/contract/test_tool_surface.py` und `scripts/check_tool_budget.py` unberührt.
5. `uv run python scripts/check_tool_budget.py` bleibt unter dem bestehenden Gate von 12500 Bytes, weil dieser Plan kein Tool registriert.
6. `uv run vulture` bleibt grün; neue öffentliche Funktionen, die erst Plan 03 benutzt, gehören mit einer Begründung in `vulture_whitelist.py`.
</verification>

<success_criteria>
- Die Transportregeln der Familie stehen dort, wo die URL entsteht: erzwungenes `limit`, numerische Pfad-Ids, zwei Schreibweisen als benannte Konstanten, kein `Origin`, kein Retry.
- Der erste OCS-Schreibaufruf des Projekts existiert als eine Naht, die Phase 9 nur noch benutzt.
- Eine Instanz mit abgeschalteter Tables-App wird als "nicht vorhanden" erkannt.
- Toolzahl, Budget-Gate und Contract-Tests sind unberührt: dieser Plan kann keine bestehende Zusage brechen.
</success_criteria>

<output>
Create `.planning/phases/08-erreichbarkeits-spike-und-tables/08-02-SUMMARY.md` when done.
Die Zusammenfassung nennt die exportierten Funktionen von `clients/tables.py` mit ihren
Signaturen, weil Plan 03 direkt darauf aufbaut.
</output>
</content>
