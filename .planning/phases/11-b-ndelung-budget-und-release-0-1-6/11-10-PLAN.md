---
phase: 11-b-ndelung-budget-und-release-0-1-6
plan: 10
type: execute
wave: 7
depends_on: [11-09]
files_modified:
  - docs/store-submission.md
autonomous: false
requirements: [EXAPP-07]

must_haves:
  truths:
    - "Der Tag v0.1.8 entsteht erst nach einer ausdrücklichen Owner-Freigabe"
    - "Der Release-Workflow ist grün, und das Multi-Arch-Image liegt unter dem Tag 0.1.8 in der Registry"
    - "Signiert wird genau das veröffentlichte Release-Asset, nie ein lokal gebautes Archiv"
    - "Der Store hat das Release mit HTTP 201 angenommen"
    - "Alle vier Nachweise aus Runbook-Schritt 8 stehen mit Datum in der Nachweistabelle"
    - "Kein Release-Asset wurde gelöscht und kein Tag umgeschrieben"
  artifacts:
    - path: "docs/store-submission.md"
      provides: "Die Proof-Zeilen für die Runbook-Schritte 4 bis 8 von 0.1.8, je mit Datum, Behauptung und Befehl"
      contains: "0.1.8"
  key_links:
    - from: "docs/store-submission.md"
      to: ".github/workflows/release.yml"
      via: "die Proof-Zeile nennt die Run-Id des Workflows, der den Tag gebaut hat"
      pattern: "gh run"
    - from: "appinfo/info.xml"
      to: "ghcr.io/street1983nk/mcp_connector"
      via: "der image-tag im Manifest ist der Tag, den AppAPI zieht"
      pattern: "0\\.1\\.8"
---

<objective>
Der irreversible Teil: Tag `v0.1.8`, der Release-Workflow, die Signatur über das
**veröffentlichte** Asset, die Einreichung im Store und die vier Nachweise. Runbook-Schritte 4 bis
8, in dieser Reihenfolge, weil die Schritte 4 und 5 die Artefakte erzeugen, von denen 6 und 7
abhängen.

Purpose: Schritt 4 ist irreversibel in der Öffentlichkeit. AppAPI installiert nicht aus dem Store,
sondern von unserer URL, also gilt dauerhaft: niemals ein Release-Asset löschen, niemals einen Tag
umschreiben, und eine Korrektur kostet eine neue Patch-Version. Deshalb steht vor dem Push eine
ausdrückliche Owner-Freigabe, und deshalb ist die Cache-Notiz Teil dieses Plans: eine Änderung, die
eine Minute nach dem Upload nicht sichtbar ist, ist nicht verloren und darf nicht mit einem
weiteren Release verfolgt werden. Genau dieser Fehler hat 0.1.5 und 0.1.6 gekostet, und er ist im
Runbook dokumentiert.

Zwei menschliche Tore, keine technischen: die Freigabe vor Schritt 4 und die angemeldete
Store-Sitzung in Schritt 7. Alles andere ist automatisierbar und wird automatisiert. Die
Voraussetzungen sind geprüft: Schlüssel und Zertifikat liegen unter
`~/.nextcloud/certificates/mcp_connector.key` und `.crt`, `gh` 2.92.0, `openssl` 3.5.6, `curl`
8.19.0 und `tar` 1.35 sind installiert, und `release.yml` verweigert einen Tag, dessen Name nicht
der `<version>` entspricht.

Output: ein veröffentlichtes Release 0.1.8 im Nextcloud App Store und die Proof-Zeilen im Runbook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-RESEARCH.md
@.planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-PATTERNS.md
@.planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-09-SUMMARY.md

Das Runbook und der Workflow:
@docs/store-submission.md
@.github/workflows/release.yml
@appinfo/info.xml
@CHANGELOG.md

<interfaces>
Runbook-Schritt 4 (irreversibel):
  git tag v0.1.8
  git push origin v0.1.8

Runbook-Schritt 5:
  gh run list --workflow release.yml --limit 1
  gh run watch <id> --exit-status
  Der Workflow baut das Image für linux/amd64 und linux/arm64, pusht nach
  ghcr.io/street1983nk/mcp_connector:0.1.8, baut das Store-Archiv und hängt es an das
  GitHub-Release. Nicht weitermachen, solange rot.

Runbook-Schritt 6:
  curl -sSLO https://github.com/street1983nk/nextcloud-mcp-connector/releases/download/v0.1.8/mcp_connector-0.1.8.tar.gz
  openssl dgst -sha512 -sign ~/.nextcloud/certificates/mcp_connector.key mcp_connector-0.1.8.tar.gz | openssl base64 -A
  Gegenprobe:
  openssl x509 -in ~/.nextcloud/certificates/mcp_connector.crt -pubkey -noout
  openssl dgst -sha512 -verify <pubkey> -signature <sig> mcp_connector-0.1.8.tar.gz  -> "Verified OK"
  NICHT das lokal gebaute Archiv: tar.gz ist nicht byte-reproduzierbar (Nachweis 2026-08-20:
  31909 gegen 32168 Bytes, unterschiedliche sha256), und der Store prüft die Signatur gegen
  die Bytes, die er von der URL lädt.

Runbook-Schritt 7:
  Formular https://apps.nextcloud.com/developer/apps/releases/new
  ODER POST https://apps.nextcloud.com/api/v1/apps/releases mit dem Token von
  https://apps.nextcloud.com/account/token, ausgeführt im Seitenkontext der angemeldeten
  Sitzung, damit der Token den Browser nie verlässt. 201 heißt akzeptiert.
  Der Token gehört dem Store-Konto, ist ausdrücklich KEIN Repository-Secret und liegt nicht
  in dieser Arbeitskopie.

Runbook-Schritt 8, die vier Nachweise:
  curl -sS https://apps.nextcloud.com/api/v1/appapi_apps.json      Release-Zeile 0.1.8, gleicher
                                                                    Platform-Span wie im Manifest
  curl -I  https://github.com/street1983nk/nextcloud-mcp-connector/releases/download/v0.1.8/mcp_connector-0.1.8.tar.gz
  GHCR-Manifest von 0.1.8 mit dem OCI-Index-Accept-Header          echter Index, amd64 UND arm64
  GHCR-Tag-Liste                                                   0.1.8 plus jeder früherer Tag
  Der GHCR-Pull-Token kommt aus
  https://ghcr.io/token?scope=repository:street1983nk/mcp_connector:pull

Die Form einer Proof-Zeile (docs/store-submission.md, Nachweistabelle ab Zeile 78):
  | 2026-08-20 08:33Z | Behauptung als ganzer Satz | Befehl, Ergebnis |

Dauerhafte Nebenbedingungen aus derselben Datei:
  Niemals ein Release-Asset löschen, niemals einen Tag umschreiben.
  Eine Korrektur kostet eine neue Patch-Version.
  Cache-Notiz: Detailseite, Katalog-Endpunkt und Suchindex aktualisieren Minuten auseinander;
  0.1.5 und 0.1.6 wurden für genau diesen Fehler ausgegeben.
  Milestone-Tags heißen milestone-v*, niemals v*: release.yml triggert auf v*.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-approve" gate="blocking-human">
  <name>Task 1: Owner-Freigabe für den Tag v0.1.8</name>
  <read_first>
    - .planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-09-SUMMARY.md (der vollständige Zustand: vier Versionsstellen, Changelog-Block, Store-Texte, sechs grüne Gates, Archiv-Probelauf)
    - docs/store-submission.md (Runbook-Schritt 4 und die zwei dauerhaften Nebenbedingungen)
    - CHANGELOG.md (der 0.1.8-Block, so wie er veröffentlicht wird)
  </read_first>
  <what-built>
    Plan 11-09 hat alles vorbereitet, was ohne Freigabe getan werden darf: Version 0.1.8 an allen
    vier Stellen (`pyproject.toml`, `src/mcp_connector/__init__.py`, `appinfo/info.xml` in
    `version` und `image-tag`), den Changelog-Block 0.1.8, die nachgetragene Sektion 0.1.5, die
    Store-Beschreibung in EN, DE und FR, und alle sechs Gates aus Runbook-Schritt 3 grün. Der
    Spendenlink `paypal.me/KhaledCherifDev` steht unverändert im Manifest und wird mit dieser
    Version im Store sichtbar. Es existiert noch kein Tag.
  </what-built>
  <how-to-verify>
    1. Den Changelog-Block lesen: `sed -n '1,80p' CHANGELOG.md`. Stimmt der 0.1.8-Block mit dem
       überein, was veröffentlicht werden soll?
    2. Die vier Versionsstellen gegenprüfen: `grep -n '^version' pyproject.toml`,
       `grep -n '__version__' src/mcp_connector/__init__.py`,
       `grep -n 'version>\|image-tag>' appinfo/info.xml`. Alle vier müssen 0.1.8 sagen.
    3. Die Store-Beschreibung lesen, alle drei Sprachblöcke in `appinfo/info.xml`. Sagt sie, was
       diese Fassung kann, und verspricht sie nichts, was sie nicht kann?
    4. Zur Kenntnis nehmen, was mit der Freigabe passiert und nicht rückholbar ist:
       `git push origin v0.1.8` löst `release.yml` aus, baut ein Multi-Arch-Image nach
       `ghcr.io/street1983nk/mcp_connector:0.1.8` und veröffentlicht ein GitHub-Release mit Asset.
       Danach gilt dauerhaft: kein Asset löschen, keinen Tag umschreiben, und eine Korrektur
       kostet 0.1.9.
    5. Die Cache-Notiz zur Kenntnis nehmen: nach dem Store-Upload aktualisieren Detailseite,
       Katalog-Endpunkt und Suchindex Minuten auseinander. Eine Änderung, die nicht sofort
       sichtbar ist, ist nicht verloren und rechtfertigt keine weitere Version.
  </how-to-verify>
  <action>
    Diesen Checkpoint dem Owner vorlegen und auf seine Antwort warten. Vorher automatisieren, was
    zur Vorlage gehört: die vier Versionsstellen auslesen und nebeneinander zeigen
    (`pyproject.toml` `version`, `src/mcp_connector/__init__.py` `__version__`, `appinfo/info.xml`
    `version` und `image-tag`), den 0.1.8-Block aus `CHANGELOG.md` zitieren, die drei
    Beschreibungsblöcke aus `appinfo/info.xml` zitieren, und `git tag --list v0.1.8` als Beleg
    ausgeben, dass noch kein Tag existiert. Kein `git tag`, kein `git push` und keine
    Store-Interaktion in dieser Task. Die Antwort des Owners wird wörtlich notiert; ohne
    "freigegeben" endet der Plan hier und Task 2 wird nicht ausgeführt.
  </action>
  <resume-signal>
    Antworte mit "freigegeben", dann entstehen Tag und Push. Antworte mit "warten" oder beschreibe
    Änderungen, dann bleibt der Tag ungeschrieben und dieser Plan endet ohne Veröffentlichung.
  </resume-signal>
  <acceptance_criteria>
    - Der Checkpoint wurde dem Owner vorgelegt, und die Antwort ist wörtlich in der Zusammenfassung notiert
    - Ohne die Antwort "freigegeben" existiert nach diesem Plan kein Tag v0.1.8, und `git tag --list v0.1.8` ist leer
    - Der Owner hat die vier Versionsstellen, den Changelog-Block und die Store-Beschreibung vorgelegt bekommen
    - Die Unwiderruflichkeit und die Cache-Notiz waren Teil der Vorlage
  </acceptance_criteria>
  <done>Der irreversible Schritt hat eine ausdrückliche, dokumentierte Freigabe, oder er findet nicht statt.</done>
</task>

<task type="auto">
  <name>Task 2: Tag, Workflow und die Signatur über das veröffentlichte Asset</name>
  <files>docs/store-submission.md</files>
  <read_first>
    - docs/store-submission.md (Runbook-Schritte 4, 5 und 6 im Wortlaut, die Nachweistabelle mit ihrer Spaltenform, die zwei dauerhaften Nebenbedingungen)
    - .github/workflows/release.yml (der Trigger auf `tags: v*`, die Gleichheitsprüfung von Tag und `version` um Zeile 48 bis 59, die Build- und Upload-Schritte)
    - .planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-09-SUMMARY.md (der Zustand, aus dem der Tag gebaut wird)
    - .planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-RESEARCH.md, Abschnitt 8 und Runtime State Inventory (die Zeile über `dist/` mit dem Beleg 31909 gegen 32168 Bytes)
  </read_first>
  <action>
    Nur nach der Antwort "freigegeben" aus Task 1. Ohne sie endet dieser Plan hier.

    Schritt 4: `git tag v0.1.8`, dann `git push origin v0.1.8`. Der Tag heißt `v0.1.8` und nicht
    `0.1.8` und nicht `milestone-v...`: `release.yml` triggert auf `v*`, und `milestone-v*` ist für
    Meilensteine reserviert. Ein Meilenstein-Tag in der `v*`-Form würde ein Release bauen, dessen
    Version nicht im Manifest steht.

    Schritt 5: auf den Workflow warten, nicht weitermachen, solange er rot ist.
    `gh run list --workflow release.yml --limit 1` liefert die Run-Id, `gh run watch` mit
    `--exit-status` wartet und liefert den Exit-Code. Ist er rot, ist das das Ergebnis dieses
    Plans: der Tag bleibt, er wird nicht umgeschrieben und nicht gelöscht, der Fehler wird in der
    Zusammenfassung benannt, und die Korrektur ist eine neue Patch-Version. Das ist die teure, aber
    einzige richtige Reaktion.

    Schritt 6: das veröffentlichte Asset herunterladen und genau diese Bytes signieren. `curl
    -sSLO` auf die Release-URL, dann `openssl dgst -sha512 -sign` mit
    `~/.nextcloud/certificates/mcp_connector.key` über die heruntergeladene Datei, durch `openssl
    base64 -A`. Das lokal in `dist/` gebaute Archiv aus Plan 11-09 wird dafür nicht benutzt:
    `tar.gz` ist nicht byte-reproduzierbar, nachgewiesen am 2026-08-20 mit 31909 gegen 32168 Bytes
    und unterschiedlichem sha256, und der Store prüft die Signatur gegen die Bytes, die er von der
    URL lädt.

    Die Signatur wird nicht in eine Datei im Repository geschrieben und nicht in die
    Zusammenfassung kopiert: sie ist eine reine Funktion aus dem veröffentlichten Asset und dem
    Schlüssel und mit zwei Befehlen jederzeit neu berechenbar. Aufgeschrieben wird die Gegenprobe:
    Public Key aus dem Zertifikat ziehen, `openssl dgst -sha512 -verify` über das heruntergeladene
    Asset, Ergebnis wörtlich `Verified OK`.

    Zwei Proof-Zeilen in die Nachweistabelle von `docs/store-submission.md`, in ihrer Form (Datum
    mit Zeit in Z, Behauptung als ganzer Satz, Befehl und Ergebnis): eine für den grünen
    Workflow-Lauf mit der Run-Id, eine für Downloadgröße mit HTTP 200 und `Verified OK` der
    Signatur gegen das Zertifikat.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector; git tag --list v0.1.8; git ls-remote --tags origin v0.1.8</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector; gh run list --workflow release.yml --limit 1</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector; curl -sSIL https://github.com/street1983nk/nextcloud-mcp-connector/releases/download/v0.1.8/mcp_connector-0.1.8.tar.gz | grep -Ei "^HTTP/|content-length"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector; uv run python -c "
from pathlib import Path
doc = Path('docs/store-submission.md').read_text(encoding='utf-8')
rows = [l for l in doc.splitlines() if l.strip().startswith('|') and '0.1.8' in l]
assert len(rows) >= 4, rows
assert any('Verified OK' in l for l in rows), 'the signature proof row is missing'
assert any('run' in l.lower() for l in rows), 'the workflow run id proof row is missing'
for forbidden in ('—', '–'):
    assert forbidden not in doc, 'no em dash or en dash'
print('two proof rows added')
"</automated>
  </verify>
  <acceptance_criteria>
    - Der Tag heißt genau `v0.1.8` und existiert lokal und auf `origin`
    - `gh run watch` mit `--exit-status` endet mit Exit-Code 0, und die Run-Id ist notiert
    - Das veröffentlichte Asset ist heruntergeladen, `curl -sSIL` liefert 200 mit einer Größe, und genau diese Datei wurde signiert
    - Die Verifikation gegen den Public Key aus dem Zertifikat gibt wörtlich `Verified OK`
    - Das lokal gebaute Archiv aus `dist/` wurde nicht signiert
    - Die Signatur steht in keiner Datei des Repositories und in keiner Zusammenfassung
    - Zwei neue Proof-Zeilen in `docs/store-submission.md`, je mit Datum inklusive Zeit in Z, Behauptung als ganzer Satz und Befehl mit Ergebnis
    - Kein Tag wurde gelöscht oder umgeschrieben, kein Release-Asset wurde gelöscht
    - Ist der Workflow rot, existiert der Tag weiter, es gibt keine Korrektur am Tag, und die Zusammenfassung nennt den Fehler und die nächste Patch-Version als Weg
  </acceptance_criteria>
  <done>Der Tag ist gesetzt, der Workflow ist grün, und die Signatur gehört zu genau den Bytes, die der Store laden wird.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Die Store-Sitzung für die Einreichung</name>
  <read_first>
    - docs/store-submission.md (Runbook-Schritt 7 im vollen Wortlaut, inklusive des Absatzes darüber, warum ein Token aus einem laufenden Browserprofil von außen nicht lesbar ist und warum die Ausführung im Seitenkontext der angemeldeten Sitzung der Weg ist, der bei 0.1.1 und 0.1.2 funktioniert hat)
    - Das Ergebnis von Task 2: die Download-URL und die berechnete Signatur
  </read_first>
  <what-built>
    Der Tag `v0.1.8` ist gesetzt, der Release-Workflow ist grün, das Asset ist veröffentlicht, und
    die Signatur über genau dieses Asset ist berechnet und gegen das Zertifikat mit `Verified OK`
    geprüft. Was fehlt, ist die Einreichung beim Store, und die braucht die angemeldete
    Store-Sitzung. Der Token gehört dem Store-Konto, ist ausdrücklich kein Repository-Secret und
    liegt nicht in dieser Arbeitskopie.
  </what-built>
  <how-to-verify>
    Es gibt zwei Wege, und der Owner wählt einen.

    Weg A, Ausführung im Seitenkontext: eine angemeldete Sitzung auf
    `https://apps.nextcloud.com` bereitstellen. Dann wird
    `https://apps.nextcloud.com/account/token` geöffnet, der Token aus der Seite gelesen und der
    POST auf `https://apps.nextcloud.com/api/v1/apps/releases` aus dem Seitenkontext ausgeführt,
    sodass der Token den Browser nie verlässt. So wurden 0.1.1 und 0.1.2 eingereicht.

    Weg B, Formular: `https://apps.nextcloud.com/developer/apps/releases/new` öffnen und zwei
    Werte einsetzen, die Download-URL
    `https://github.com/street1983nk/nextcloud-mcp-connector/releases/download/v0.1.8/mcp_connector-0.1.8.tar.gz`
    und die Signatur aus Task 2. Die Signatur wird bei Bedarf mit den zwei Befehlen aus
    Runbook-Schritt 6 neu berechnet; sie muss nirgends aufgeschrieben und nirgends weitergegeben
    werden.

    In beiden Fällen gilt: 201 heißt akzeptiert. Der Store lädt das Archiv, prüft die Signatur
    gegen das Zertifikat, prüft die Ordnerstruktur (genau ein Verzeichnis auf oberster Ebene,
    Kleinbuchstaben und Unterstriche, enthält `appinfo/info.xml`) und validiert die Metadaten
    gegen `info.xsd`.

    Danach gilt die Cache-Notiz: Detailseite, Katalog-Endpunkt und Suchindex aktualisieren Minuten
    auseinander. Eine Änderung, die eine Minute nach dem Upload nicht sichtbar ist, ist nicht
    verloren. 0.1.5 und 0.1.6 wurden ausgegeben, weil genau darauf mit einem weiteren Release
    reagiert wurde.
  </how-to-verify>
  <action>
    Diesen Checkpoint dem Owner vorlegen und auf seine Antwort warten. Vorher automatisieren, was
    zur Vorlage gehört: die vollständige Download-URL des veröffentlichten Assets ausgeben, die
    Signatur aus Task 2 bereitstellen (im Gespräch, nicht in einer Datei), und beide Wege mit ihren
    URLs nennen. Antwortet der Owner "Sitzung steht", wird der POST auf
    `https://apps.nextcloud.com/api/v1/apps/releases` aus dem Seitenkontext der angemeldeten
    Sitzung ausgeführt und der Statuscode notiert; erwartet wird 201. Antwortet er "eingereicht",
    wird der von ihm gemeldete Statuscode notiert. Antwortet er "abbrechen", endet der Plan hier,
    und die Zusammenfassung sagt ausdrücklich, dass das Release bei GitHub veröffentlicht und im
    Store unveröffentlicht ist. Der Store-Token wird in keine Datei und in keine Zusammenfassung
    geschrieben.
  </action>
  <resume-signal>
    Antworte mit "Sitzung steht" für Weg A, dann wird die Einreichung aus dem Seitenkontext
    ausgeführt. Antworte mit "eingereicht" plus dem gemeldeten Statuscode, wenn du Weg B selbst
    gegangen bist. Antworte mit "abbrechen", dann bleibt das Release bei GitHub veröffentlicht und
    im Store unveröffentlicht, und die Zusammenfassung sagt das ausdrücklich.
  </resume-signal>
  <acceptance_criteria>
    - Der Checkpoint wurde vorgelegt, mit der Download-URL und beiden Wegen, und die Antwort ist wörtlich in der Zusammenfassung notiert
    - Der Store-Token steht in keiner Datei dieser Arbeitskopie und in keiner Zusammenfassung
    - Bei Weg A ist der POST aus dem Seitenkontext der angemeldeten Sitzung gelaufen; bei Weg B ist der vom Owner gemeldete Statuscode notiert
    - Der Store hat mit HTTP 201 geantwortet, oder der Abbruch ist mit Begründung dokumentiert
  </acceptance_criteria>
  <done>Die Einreichung ist erfolgt oder ausdrücklich abgebrochen, und der Store-Token hat den Browser nie verlassen.</done>
</task>

<task type="auto">
  <name>Task 4: Die vier Nachweise aus Runbook-Schritt 8</name>
  <files>docs/store-submission.md</files>
  <read_first>
    - docs/store-submission.md (Runbook-Schritt 8 mit den vier Befehlen im Wortlaut, die Nachweistabelle mit den zwei Zeilen aus Task 2, die zwei dauerhaften Nebenbedingungen)
    - .planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-RESEARCH.md, Abschnitt 8 (die Zeile, dass `curl -I` auf das Asset der Nachweis gegen ein gelöschtes Asset ist und ausdrücklich nicht optional)
    - appinfo/info.xml (der Platform-Span, gegen den die Release-Zeile in `appapi_apps.json` verglichen wird)
  </read_first>
  <action>
    Die vier Nachweise laufen lassen und jeden mit Datum in die Nachweistabelle schreiben, in der
    Spaltenform der vorhandenen Zeilen.

    (1) `appapi_apps.json` holen und darin die Release-Zeile für 0.1.8 finden. Behauptet wird nicht
        nur ihre Existenz, sondern auch, dass ihr Platform-Span derselbe ist wie im Manifest: ein
        abweichender Span heißt, dass der Store andere Nextcloud-Versionen bedient, als das
        Manifest sagt.
    (2) `curl -I` auf die Asset-URL. Diese Zeile ist ausdrücklich nicht optional: sie ist der
        Nachweis gegen ein gelöschtes Asset, und ein gelöschtes Asset macht jede spätere
        Installation über AppAPI kaputt, weil AppAPI von unserer URL lädt und nicht vom Store.
    (3) Das GHCR-Manifest von 0.1.8 mit dem OCI-Index-Accept-Header holen und behaupten, dass es
        ein echter Index ist und beide Architekturen enthält, `amd64` und `arm64`. ARM-Selfhoster
        sind eine relevante Zielgruppe, und ein Index mit nur einer Architektur fällt erst beim
        Nutzer auf.
    (4) Die GHCR-Tag-Liste holen und behaupten, dass sie `0.1.8` enthält und jeden früheren Tag von
        `0.1.0` bis `0.1.7`. Das ist die Gegenprobe gegen einen umgeschriebenen oder entfernten
        Tag; die Liste vorher war `0.1.0` bis `0.1.7`.

    Zusätzlich, weil es der Anlass dieses Releases ist: die Store-Detailseite auf den sichtbaren
    Spendenlink `paypal.me/KhaledCherifDev` prüfen. Ist er eine Minute nach dem Upload noch nicht
    sichtbar, ist das kein Fehler und kein Grund für 0.1.9: die Cache-Notiz sagt, dass
    Detailseite, Katalog-Endpunkt und Suchindex Minuten auseinander aktualisieren. Der Befund wird
    mit Zeitstempel notiert und, falls nötig, nach einer Wartezeit wiederholt.

    Am Ende steht die Nachweistabelle mit mindestens sieben 0.1.8-Zeilen (zwei aus Task 2, vier aus
    dieser Task, plus die Sichtbarkeitszeile), und keine Zeile behauptet etwas, das nicht gelaufen
    ist.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector; curl -sS https://apps.nextcloud.com/api/v1/appapi_apps.json | grep -o "0[.]1[.]8" | head -3</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector; curl -sSI https://github.com/street1983nk/nextcloud-mcp-connector/releases/download/v0.1.8/mcp_connector-0.1.8.tar.gz | head -5</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector; bash -c 'TOKEN=$(curl -sS "https://ghcr.io/token?scope=repository:street1983nk/mcp_connector:pull" | grep -o "\"token\":\"[^\"]*" | cut -d\" -f4); curl -sS -H "Authorization: Bearer $TOKEN" -H "Accept: application/vnd.oci.image.index.v1+json" https://ghcr.io/v2/street1983nk/mcp_connector/manifests/0.1.8 | grep -o "amd64\|arm64" | sort -u; curl -sS -H "Authorization: Bearer $TOKEN" https://ghcr.io/v2/street1983nk/mcp_connector/tags/list'</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector; uv run python -c "
from pathlib import Path
doc = Path('docs/store-submission.md').read_text(encoding='utf-8')
rows = [l for l in doc.splitlines() if l.strip().startswith('|') and '0.1.8' in l]
assert len(rows) >= 7, len(rows)
joined = '\n'.join(rows)
for needle in ('appapi_apps', 'curl -I', 'arm64', 'tags/list', 'paypal'):
    assert needle in joined, needle
for row in rows:
    assert 'Z |' in row, ('every proof row carries a date with time in Z', row)
for forbidden in ('—', '–'):
    assert forbidden not in doc, 'no em dash or en dash'
print('all proof rows present:', len(rows))
"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector; uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `appapi_apps.json` enthält eine Release-Zeile für 0.1.8, und ihr Platform-Span stimmt mit dem des Manifests überein
    - `curl -I` auf das Asset antwortet mit 200; die Zeile steht in der Tabelle und wurde nicht als optional behandelt
    - Das GHCR-Manifest von 0.1.8 ist ein OCI-Index und enthält `amd64` und `arm64`
    - Die GHCR-Tag-Liste enthält 0.1.8 und jeden Tag von 0.1.0 bis 0.1.7; keiner wurde entfernt oder umgeschrieben
    - Die Nachweistabelle in `docs/store-submission.md` enthält mindestens sieben 0.1.8-Zeilen, je mit Datum inklusive Zeit in Z
    - Eine Zeile hält den Befund zur Sichtbarkeit des Spendenlinks mit Zeitstempel; eine noch nicht sichtbare Änderung ist ausdrücklich als Cache-Verhalten notiert und nicht als Fehler
    - Keine Zeile behauptet einen Befehl, der nicht gelaufen ist
    - `docs/store-submission.md` enthält keine Em-Dashes und keine En-Dashes
    - `uv run pytest -q` ist grün
  </acceptance_criteria>
  <done>Das Release ist im Store, alle vier Nachweise stehen mit Datum in der Tabelle, und die Sichtbarkeit des Spendenlinks ist als Beobachtung notiert statt als Anlass für eine weitere Version.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository zu Öffentlichkeit | Ein gepushter Tag löst ein öffentliches Release aus und ist nicht rückholbar |
| Lokales Archiv zu signiertem Artefakt | Ein lokal gebautes `tar.gz` ist nicht byte-identisch mit dem veröffentlichten |
| Store-Token zu Arbeitskopie | Der Token gehört dem Store-Konto und darf den Browser nicht verlassen |
| Veröffentlichtes Asset zu Installation | AppAPI installiert von unserer URL, nicht vom Store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-65 | Denial of Service | Ein Tag entsteht ohne Owner-Freigabe und veröffentlicht eine Fassung, die niemand freigegeben hat | mitigate | Task 1 ist ein blockierender Checkpoint; ohne die Antwort "freigegeben" endet der Plan, und ein Prüfschritt behauptet den leeren Tag |
| T-11-66 | Tampering | Ein Tag wird umgeschrieben oder ein Asset gelöscht, um einen Fehler zu korrigieren | mitigate | Ausdrücklich verboten; die Reaktion auf einen roten Workflow ist eine neue Patch-Version, und die Tag-Liste in Nachweis 4 belegt, dass kein früherer Tag verschwunden ist |
| T-11-67 | Spoofing | Die Signatur gehört zu einem anderen Artefakt als dem, das der Store lädt | mitigate | Signiert wird ausschließlich das heruntergeladene Release-Asset, mit dem Beleg 31909 gegen 32168 Bytes im Plan; die Gegenprobe `Verified OK` steht als Proof-Zeile |
| T-11-68 | Information Disclosure | Der Store-Token landet in einer Datei, einer Zusammenfassung oder einer Umgebungsvariable des Repos | mitigate | Weg A führt den POST im Seitenkontext aus, sodass der Token den Browser nie verlässt; ein Akzeptanzkriterium behauptet die Abwesenheit in Arbeitskopie und Zusammenfassung |
| T-11-69 | Information Disclosure | Die Signatur wird aufgeschrieben und weitergegeben, statt bei Bedarf neu berechnet zu werden | mitigate | Die Signatur wird nicht in das Repository und nicht in die Zusammenfassung geschrieben; sie ist eine reine Funktion aus Asset und Schlüssel |
| T-11-70 | Denial of Service | Ein Image ohne `arm64` bricht bei ARM-Selfhostern erst nach der Installation | mitigate | Nachweis 3 behauptet einen echten OCI-Index mit `amd64` und `arm64` |
| T-11-71 | Repudiation | Eine Proof-Zeile behauptet einen Befehl, der nicht gelaufen ist | mitigate | Jede Zeile trägt Datum mit Zeit in Z und den Befehl; ein Prüfschritt behauptet die Form aller 0.1.8-Zeilen und ihre Mindestzahl |
| T-11-72 | Tampering | Der Store-Cache verleitet zu einem weiteren Release, weil die Änderung nicht sofort sichtbar ist | mitigate | Die Cache-Notiz steht im Checkpoint und in Task 4; ein noch nicht sichtbarer Spendenlink wird als Beobachtung mit Zeitstempel notiert, nie als Fehler |
| T-11-73 | Elevation of Privilege | Ein Meilenstein-Tag in der `v*`-Form löst ein Release aus, dessen Version nicht im Manifest steht | mitigate | Der Tag heißt genau `v0.1.8`; Meilensteine heißen `milestone-v*`, und `release.yml` prüft Tag gegen `version` |
| T-11-SC | Tampering | Paketinstallation | mitigate | Keine neue Sprachabhängigkeit: `pyproject.toml` und `uv.lock` bleiben unangetastet, es gibt kein `uv add`, kein `pip install` und kein `npm install` in diesem Plan |
</threat_model>

<verification>
1. `git tag --list v0.1.8` und `git ls-remote --tags origin v0.1.8` zeigen den Tag lokal und auf `origin`; kein früherer Tag fehlt.
2. Der Release-Workflow-Lauf zu diesem Tag ist grün, die Run-Id steht in der Nachweistabelle.
3. Die Verifikation der Signatur gegen das Zertifikat gibt `Verified OK`, und der Store hat mit HTTP 201 geantwortet.
4. Alle vier Nachweise aus Runbook-Schritt 8 sind gelaufen und stehen mit Datum in `docs/store-submission.md`, zusammen mit der Sichtbarkeitszeile des Spendenlinks.
5. `git diff --stat` zeigt Änderungen ausschließlich an `docs/store-submission.md`; `uv run pytest -q` ist grün.
</verification>

<success_criteria>
- Release 0.1.8 ist im Store, mit dem Spendenlink `paypal.me/KhaledCherifDev` sichtbar oder als Cache-Beobachtung notiert (EXAPP-07).
- Der Tag ist erst nach ausdrücklicher Owner-Freigabe entstanden, und die Freigabe ist dokumentiert.
- Signiert wurde genau das veröffentlichte Asset; die Signatur steht nirgends aufgeschrieben.
- Alle vier Nachweise aus Runbook-Schritt 8 stehen mit Datum in der Nachweistabelle, und kein Asset und kein Tag wurde verändert.
</success_criteria>

<output>
Create `.planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-10-SUMMARY.md` when done.
Die Zusammenfassung nennt die wörtliche Antwort des Owners auf beide Checkpoints, die Run-Id des
Release-Workflows, die Downloadgröße des Assets, das Ergebnis der Signaturverifikation, den
Statuscode des Store-Uploads, alle vier Nachweisergebnisse, den Zeitstempel und Befund zur
Sichtbarkeit des Spendenlinks, und jede im Runbook entdeckte Ungenauigkeit. Die Signatur selbst
wird ausdrücklich nicht notiert. Damit ist EXAPP-07 abgeschlossen und der Meilenstein v1.2 bereit
für seinen Abschluss.
</output>
