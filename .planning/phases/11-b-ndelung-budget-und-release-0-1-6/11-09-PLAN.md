---
phase: 11-b-ndelung-budget-und-release-0-1-6
plan: 09
type: execute
wave: 6
depends_on: [11-06, 11-08]
files_modified:
  - pyproject.toml
  - src/mcp_connector/__init__.py
  - appinfo/info.xml
  - CHANGELOG.md
autonomous: true
requirements: [EXAPP-07]

must_haves:
  truths:
    - "Die Version 0.1.8 steht an allen vier Stellen als identische Zeichenkette"
    - "Der Changelog trägt einen 0.1.8-Block, und die fehlende 0.1.5-Sektion ist nachgetragen"
    - "Die Store-Beschreibung nennt die neuen Fähigkeiten in allen drei Sprachen"
    - "Das Vokabular-Gate ist lokal grün: das verbotene Wort steht in keinem öffentlichen Artefakt"
    - "Alle sechs Gates des Runbooks sind lokal grün, bevor irgendein Tag entsteht"
    - "Der Spendenlink bleibt unangetastet, weil er schon richtig im Manifest steht"
  artifacts:
    - path: "pyproject.toml"
      provides: "version 0.1.8"
      contains: "0.1.8"
    - path: "src/mcp_connector/__init__.py"
      provides: "__version__ 0.1.8"
      contains: "0.1.8"
    - path: "appinfo/info.xml"
      provides: "version und image-tag 0.1.8, Beschreibung EN/DE/FR mit den neuen Fähigkeiten"
      contains: "0.1.8"
    - path: "CHANGELOG.md"
      provides: "Der 0.1.8-Block aus dem Unreleased-Inhalt plus den Änderungen dieser Phase, und die nachgetragene 0.1.5-Sektion"
      contains: "[0.1.8]"
  key_links:
    - from: "appinfo/info.xml"
      to: "src/mcp_connector/__init__.py"
      via: "das Manifest-Gate vergleicht version mit __version__ und image-tag mit version"
      pattern: "0\\.1\\.8"
    - from: "CHANGELOG.md"
      to: ".planning/phases/11-b-ndelung-budget-und-release-0-1-6/"
      via: "jeder Eintrag im Block stammt aus einer Zusammenfassung dieser Phase"
      pattern: "0\\.1\\.8"
---

<objective>
Die Fassung, die diese Phase trägt, bis zum Rand des irreversiblen Schritts vorbereiten: Version
0.1.8 an allen vier Stellen, ein Changelog-Block, der die Wahrheit über diese Phase sagt, die
nachgetragene Sektion 0.1.5 (IN-02, der letzte offene Info-Befund aus Phase 10), die Store-Texte
in drei Sprachen, und alle sechs Gates lokal grün.

Purpose: Die Release-Nummer ist eine Owner-Entscheidung und nicht verhandelbar: 0.1.4 (21.08.),
0.1.5, 0.1.6 und 0.1.7 (22.08.) liegen im Store, die nächste freie Nummer ist **0.1.8**. Die Zahl
0.1.6 im Phasentitel ist überholt. Mit 0.1.8 wird der Spendenlink
`https://www.paypal.com/paypalme/KhaledCherifDev` sichtbar; er steht schon im Manifest (Commit
`d36356d`) und wird **nicht** erneut angefasst, das ist ausdrücklich der Anlass des Releases und
nicht seine Aufgabe.

Der Grund, warum dieser Plan an der Tag-Grenze endet: Schritt 4 des Runbooks ist irreversibel in
der Öffentlichkeit, und ein Tag darf nur nach Owner-Freigabe entstehen. Deshalb enthält dieser
Plan keinen `git tag` und keinen `git push`; das ist Plan 11-10 mit seinem Checkpoint. Und die
Cache-Notiz gilt vorher wie nachher: 0.1.5 und 0.1.6 wurden ausgegeben, weil eine Änderung nach
dem Upload nicht sofort sichtbar war. Eine nicht sichtbare Änderung ist nicht verloren.

Output: vier identische Versionsstrings, ein Changelog mit zwei neuen Sektionen, Store-Texte in
drei Sprachen, und ein Protokoll von sechs grünen Gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-RESEARCH.md
@.planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-PATTERNS.md
@.planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-01-SUMMARY.md
@.planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-02-SUMMARY.md
@.planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-03-SUMMARY.md
@.planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-04-SUMMARY.md
@.planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-05-SUMMARY.md
@.planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-06-SUMMARY.md
@.planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-07-SUMMARY.md
@.planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-08-SUMMARY.md

Die vier geänderten Dateien:
@pyproject.toml
@src/mcp_connector/__init__.py
@appinfo/info.xml
@CHANGELOG.md

Das Runbook und die Gates:
@docs/store-submission.md
@tests/unit/test_exapp_env_setup.py
@.github/workflows/release.yml

<interfaces>
Die vier Versionsstellen, heute alle auf 0.1.7:
  pyproject.toml                     Zeile 3    version = "0.1.7"
  src/mcp_connector/__init__.py       Zeile 7    __version__ = "0.1.7"
  appinfo/info.xml                    Zeile 165  <version>0.1.7</version>
  appinfo/info.xml                    Zeile 239  <image-tag>0.1.7</image-tag>
Der Git-Tag v0.1.8 ist der fünfte identische String und entsteht erst in Plan 11-10.

Zwei Gates halten die Gleichheit:
  tests/unit/test_exapp_env_setup.py  vergleicht <version> mit mcp_connector.__version__
                                      und <image-tag> mit <version>
  .github/workflows/release.yml       Trigger tags: v*, Gleichheitsprüfung Tag gegen <version>
                                      (Zeile 48 bis 59)

Das Vokabular-Gate, in derselben Testdatei:
  FORBIDDEN_VOCABULARY = "archiv"
  Geprüft case insensitive und NUR gegen den Elementtext des Manifests, damit die
  erklärenden Kommentare des Manifests es nicht auslösen können.

Weitere Manifest-Gates in derselben Datei (Runbook Schritt 3):
  Route-Gate, Beschreibungs-Gate (kein Backtick, keine Tabelle, kein Bild, keine Regel,
  kein HTML, mindestens zwei Absätze je Sprache, summary unter 128 Zeichen, kein
  verbotenes Vokabular), Variablen-Gate (kein leeres <default>, das der Store mit 500
  beantwortet), Versionsgleichheit.

Der Spendenlink, Anlass des Releases, steht schon drin (appinfo/info.xml Zeile 208):
  <donation title="Donate with PayPal" type="paypal">https://www.paypal.com/paypalme/KhaledCherifDev</donation>
NICHT anfassen.

CHANGELOG-Zustand (IN-02 im Detail):
  Sektionen: [Unreleased] (trägt heute den vollständigen Mail-Block der Phase 10),
             dann [0.1.7], [0.1.6], [0.1.4] und weiter
  Link-Definitionen am Dateiende (Zeile 380 bis 387) existieren für [0.1.5] und [0.1.6]
  Die Sektion [0.1.5] fehlt zwischen [0.1.6] und [0.1.4]
  Der Tag v0.1.5 existiert nachweislich (Proof-Tabelle 2026-08-22 10:59Z, Run 32569019469),
  also ist die Nachtragung der Sektion der richtige Weg, nicht die Korrektur der Links
  Blockform des [0.1.7]-Blocks: Überschrift mit Datum, ein Absatz Prosa, dann
  ### Added / ### Changed / ### Security mit Aufzählungspunkten in ganzen Sätzen

Die sechs Gates aus Runbook-Schritt 3:
  uv run --no-sync pytest -q
  uv run --no-sync ruff check .
  uv run --no-sync ruff format --check .
  uv run --no-sync pyright
  uv run --no-sync vulture src scripts vulture_whitelist.py
  uv run --no-sync python scripts/check_tool_budget.py
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Version 0.1.8 an vier Stellen und der Changelog mit zwei neuen Sektionen</name>
  <files>pyproject.toml, src/mcp_connector/__init__.py, appinfo/info.xml, CHANGELOG.md</files>
  <read_first>
    - pyproject.toml (die `version`-Zeile im `[project]`-Block)
    - src/mcp_connector/__init__.py (die `__version__`-Zeile)
    - appinfo/info.xml (die `<version>`-Zeile, die `<image-tag>`-Zeile, und die `<donation>`-Zeile, die nicht angefasst wird)
    - CHANGELOG.md (ganze Datei, 387 Zeilen: der `[Unreleased]`-Block mit dem Mail-Inhalt der Phase 10, der `[0.1.7]`-Block als Formvorlage, die Stelle zwischen `[0.1.6]` und `[0.1.4]`, der Linkblock am Dateiende)
    - docs/store-submission.md (Runbook-Schritte 1 und 2 im Wortlaut, plus die Nebenbedingung "eine Korrektur kostet eine neue Patch-Version")
    - Alle acht Zusammenfassungen dieser Phase (11-01 bis 11-08): sie sind die Datenquelle des Changelog-Blocks
    - .planning/phases/10-mail-strikt-lesend-und-die-trifecta-grenze/10-REVIEW.md, Befund IN-02 im Wortlaut
    - .planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-RESEARCH.md, Owner-Vorgabe 1, Abschnitt 9 (Changelog-Zustand), Pitfall 13 und 14
  </read_first>
  <action>
    (1) Die Version wird an allen vier Stellen von `0.1.7` auf `0.1.8` gesetzt, als **identische**
        Zeichenkette. Nicht 0.1.6, obwohl der Phasentitel das sagt: 0.1.4 bis 0.1.7 liegen im
        Store, und die nächste freie Nummer ist 0.1.8. Der Spendenlink in `appinfo/info.xml` wird
        **nicht** angefasst; er ist bereits richtig, und mit dieser Version wird er sichtbar.

    (2) Der `[Unreleased]`-Block wird zu `## [0.1.8] - &lt;Datum des Releases&gt;`, in der Form des
        `[0.1.7]`-Blocks: Überschrift mit Datum, ein Absatz Prosa, der sagt, worum es in dieser
        Version geht, dann `### Added`, `### Changed` und, falls zutreffend, `### Security` mit
        Aufzählungspunkten in ganzen Sätzen. Der Inhalt kommt aus den acht Zusammenfassungen
        dieser Phase und aus dem, was heute schon unter `[Unreleased]` steht (der Mail-Block der
        Phase 10). Nutzerrelevant und damit pflichtig sind mindestens:
        - `prepare_context` liefert zusätzlich einen Talk-Digest und Mail-Ungelesen-Zähler, je mit
          eigenem Zeitbudget und eigenem `degraded`-Eintrag, und ohne Betreff oder Mailinhalt im
          Standardbündel (Pläne 11-04 und 11-05).
        - `fetch` löst zwei weitere Id-Arten auf: eine einzelne Talk-Nachricht und eine Tabelle;
          ein Mail-Suchtreffer bleibt bewusst eine URL, mit genanntem Grund (Pläne 11-01 und
          11-03).
        - Die Umbenennung `truncated` zu `preview_truncated` auf Eintragsebene der Mail-Antwort.
          Das ist eine **Antwortformat-Änderung** und gehört nach `### Changed`, ausdrücklich
          benannt, weil ein Abnehmer den alten Schlüssel gelesen haben kann (Plan 11-08).
        - Das Budget-Gate ist auf die Messung dieser Phase verankert, und die fünf Werkzeuge des
          Meilensteins sind schema-diätet; die Zahl aus 11-07-SUMMARY wird genannt (Plan 11-07).
        Ein neuer `[Unreleased]`-Block mit leerem Inhalt bleibt darüber stehen, wenn die Datei das
        bisher so gehalten hat; sonst wird er weggelassen.

    (3) IN-02: die fehlende Sektion `## [0.1.5]` wird zwischen `[0.1.6]` und `[0.1.4]`
        nachgetragen. Die Links sind in Ordnung und werden **nicht** korrigiert: der Tag `v0.1.5`
        existiert nachweislich (Proof-Zeile 2026-08-22 10:59Z, Run `32569019469`), also ist die
        fehlende Sektion der Fehler und nicht der Link. Der Inhalt der Sektion ist der Anlass
        dieses Releases von damals, so wie er in der Proof-Tabelle und in `info.xml` steht ("The
        shape since 0.1.5" verweist darauf): die neue Store-Beschreibung. Was nicht belegbar ist,
        wird nicht erfunden; eine kurze, wahre Sektion ist besser als eine ausführliche, geratene.

    (4) Der Linkblock am Dateiende bekommt die Zeile für `[0.1.8]` in der Form der vorhandenen
        (`compare/v0.1.7...v0.1.8`).

    Für den ganzen Changelog gilt: Englisch, weil die Datei englisch ist und im Store-Archiv
    mitreist; keine Em-Dashes, keine Emojis; und das verbotene Wort aus dem Vokabular-Gate kommt
    nicht vor, auch nicht in einer Formulierung, die es beiläufig enthält.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run python -c "
import re
from pathlib import Path
import xml.etree.ElementTree as ET
py = Path('pyproject.toml').read_text(encoding='utf-8')
init = Path('src/mcp_connector/__init__.py').read_text(encoding='utf-8')
xml = Path('appinfo/info.xml').read_text(encoding='utf-8')
assert re.search(r'^version = \"0\.1\.8\"$', py, re.M), 'pyproject version'
assert '__version__ = \"0.1.8\"' in init, 'package version'
root = ET.fromstring(xml)
version = root.findtext('version')
tag = root.find('.//image-tag')
assert version == '0.1.8', version
assert tag is not None and (tag.text or '').strip() == '0.1.8', tag
assert 'paypalme/KhaledCherifDev' in xml, 'the donation link stays'
print('four version places aligned')
"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run python -c "
import re
from pathlib import Path
text = Path('CHANGELOG.md').read_text(encoding='utf-8')
sections = re.findall(r'^## \[([^\]]+)\]', text, re.M)
assert '0.1.8' in sections, sections
assert '0.1.5' in sections, sections
order = [s for s in sections if re.fullmatch(r'0\.1\.\d+', s)]
assert order == sorted(order, key=lambda s: [int(p) for p in s.split('.')], reverse=True), order
assert '[0.1.8]: https://github.com/street1983nk/nextcloud-mcp-connector/compare/v0.1.7...v0.1.8' in text
assert 'preview_truncated' in text, 'the response format change has to be in the changelog'
for forbidden in ('—', '–'):
    assert forbidden not in text, 'no em dash or en dash'
assert 'archiv' not in text.lower(), 'forbidden vocabulary'
print('changelog has 0.1.8 and the missing 0.1.5')
"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run pytest tests/unit/test_exapp_env_setup.py -q</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml`, `src/mcp_connector/__init__.py`, `<version>` und `<image-tag>` tragen alle vier die identische Zeichenkette `0.1.8`
    - Die `<donation>`-Zeile mit `paypalme/KhaledCherifDev` ist unverändert
    - `CHANGELOG.md` hat eine Sektion `## [0.1.8]` mit Datum, Prosa-Absatz und mindestens `### Added` und `### Changed`
    - Der 0.1.8-Block nennt den Talk-Digest, die Mail-Zähler, die zwei neuen Id-Arten, die Verankerung des Budget-Gates mit Zahl, und die Umbenennung `preview_truncated` als Antwortformat-Änderung unter `### Changed`
    - Die Sektion `## [0.1.5]` existiert zwischen `[0.1.6]` und `[0.1.4]`; die Link-Definitionen sind unverändert
    - Der Linkblock enthält `[0.1.8]: ...compare/v0.1.7...v0.1.8`
    - Die Versionssektionen stehen in absteigender Reihenfolge
    - `CHANGELOG.md` enthält keine Em-Dashes, keine En-Dashes, keine Emojis und nicht das verbotene Wort
    - `uv run pytest tests/unit/test_exapp_env_setup.py -q` ist grün: das Manifest-Gate akzeptiert die neue Version
    - Es existiert **kein** Git-Tag aus diesem Plan: `git tag --list v0.1.8` ist leer
  </acceptance_criteria>
  <done>Vier identische Versionsstrings, ein Changelog, der die Wahrheit über diese Phase sagt, und der letzte offene Info-Befund aus Phase 10 ist geschlossen.</done>
</task>

<task type="auto">
  <name>Task 2: Die Store-Beschreibung nennt die neuen Fähigkeiten, in drei Sprachen</name>
  <files>appinfo/info.xml</files>
  <read_first>
    - appinfo/info.xml (ganze Datei, 521 Zeilen: die drei `<description>`-Blöcke für EN, DE und FR, ihre Absatzstruktur, die `<summary>`-Elemente, und die erklärenden Kommentare des Manifests, die vom Vokabular-Gate ausdrücklich nicht geprüft werden)
    - tests/unit/test_exapp_env_setup.py (das Beschreibungs-Gate: kein Backtick, keine Tabelle, kein Bild, keine Regel, kein HTML, mindestens zwei Absätze je Sprache, `summary` unter 128 Zeichen, kein verbotenes Vokabular; und `FORBIDDEN_VOCABULARY`)
    - .planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-01-SUMMARY.md, 11-03-SUMMARY.md, 11-04-SUMMARY.md, 11-05-SUMMARY.md (was in der Beschreibung neu wahr ist)
    - README.md (nach Plan 11-08: der Wortlaut, an dem sich die englische Beschreibung orientiert)
    - .planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-RESEARCH.md, Runtime State Inventory (Zeile "Live-Dienst-Konfiguration") und State of the Art (die Store-Beschreibung ist seit Commit `6eb5d05` auf dem Stand der Phase 10)
  </read_first>
  <action>
    Die Store-Beschreibung ist die Seite, die ein Nutzer im App Store liest, und sie kommt
    **beim Upload** in die Seite, nicht auf einem Zeitplan. Die Texte der Phase 10 (Commit
    `6eb5d05`) werden erst mit 0.1.8 überhaupt sichtbar; das ist der Grund, sie jetzt vollständig
    zu machen statt später.

    Ergänzt wird in **allen drei** Sprachblöcken, in derselben Aussagetiefe:
    - Das Ein-Aufruf-Bündel liefert jetzt auch einen Talk-Digest und Mail-Ungelesen-Zähler, und
      der Satz sagt ausdrücklich, dass im Standardbündel **kein** Betreff und **kein** Mailinhalt
      steht. Das ist die Aussage, die einen Datenschutzverantwortlichen interessiert, und sie ist
      wahr.
    - Ein Suchtreffer aus Talk und aus Tables lässt sich lesen, ohne die App zu wechseln; ein
      Mail-Suchtreffer bleibt eine URL, und der Grund ist genannt.
    Die bestehende ehrliche Ketten-Formulierung aus Befund CR-01 bleibt **unverändert** stehen:
    sie ist die Aussage über die Trifecta-Grenze und wurde in Phase 10 bewusst so formuliert.

    Fünf Grenzen, alle vom Beschreibungs-Gate geprüft und darum wörtlich einzuhalten: kein
    Backtick, keine Tabelle, kein Bild, keine horizontale Regel, kein HTML; mindestens zwei
    Absätze je Sprache; `summary` unter 128 Zeichen; und das verbotene Wort aus
    `FORBIDDEN_VOCABULARY` kommt im Elementtext nicht vor, auch nicht als Teil eines längeren
    Wortes.

    Sprachpflege: echte Umlaute im deutschen Block, Akzente und Cedille im französischen, keine
    Em-Dashes, keine Emojis. Und das `<version>`-Element wird in dieser Task **nicht** angefasst,
    es ist in Task 1 schon gesetzt.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run pytest tests/unit/test_exapp_env_setup.py -q</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run python -c "
import xml.etree.ElementTree as ET
from pathlib import Path
raw = Path('appinfo/info.xml').read_text(encoding='utf-8')
root = ET.fromstring(raw)
descriptions = {el.get('{http://www.w3.org/XML/1998/namespace}lang') or 'en': (el.text or '') for el in root.findall('description')}
assert set(descriptions) >= {'en', 'de', 'fr'}, sorted(descriptions)
for lang, text in descriptions.items():
    low = text.lower()
    assert 'talk' in low, lang
    assert 'mail' in low, lang
    assert 'archiv' not in low, (lang, 'forbidden vocabulary')
    for forbidden in ('\`', '|', '<', '>', '—', '–'):
        assert forbidden not in text, (lang, forbidden)
    assert len([p for p in text.split('\n\n') if p.strip()]) >= 2, lang
assert any(ch in descriptions['de'] for ch in 'äöüß'), 'german umlauts'
assert any(ch in descriptions['fr'] for ch in 'éèêàçô'), 'french accents'
for el in root.findall('summary'):
    assert len((el.text or '').strip()) < 128, el.text
print('store description covers three languages')
"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - Alle drei `<description>`-Blöcke (EN, DE, FR) nennen den Talk-Digest und die Mail-Zähler und sagen, dass kein Betreff und kein Mailinhalt im Standardbündel steht
    - Alle drei nennen, dass ein Talk- und ein Tabellen-Suchtreffer lesbar ist und ein Mail-Suchtreffer eine URL bleibt, mit Grund
    - Die Ketten-Formulierung aus Befund CR-01 ist unverändert
    - Kein Block enthält Backtick, Tabelle, Bild, horizontale Regel oder HTML; jeder hat mindestens zwei Absätze; jedes `summary` ist unter 128 Zeichen
    - Das verbotene Wort kommt in keinem Elementtext vor
    - Der deutsche Block trägt echte Umlaute, der französische seine Akzente; keiner enthält Em-Dashes, En-Dashes oder Emojis
    - Das `<version>`-Element ist gegenüber Task 1 unverändert
    - `uv run pytest tests/unit/test_exapp_env_setup.py -q` ist grün: alle Manifest-Gates akzeptieren die Texte
  </acceptance_criteria>
  <done>Die Seite, die ein Nutzer im Store liest, sagt vollständig und in drei Sprachen, was diese Fassung kann und was sie ausdrücklich nicht tut.</done>
</task>

<task type="auto">
  <name>Task 3: Runbook-Schritt 3: alle sechs Gates lokal grün, mit Protokoll</name>
  <files>docs/store-submission.md</files>
  <read_first>
    - docs/store-submission.md (Runbook-Schritt 3 im Wortlaut mit allen sechs Befehlen, der optionale Probelauf des Archivs, die Nachweistabelle ab Zeile 78 mit ihrer Spaltenform, und die zwei dauerhaften Nebenbedingungen am Ende)
    - scripts/build_store_release.sh (der Archivbau für den optionalen Probelauf)
    - .planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-RESEARCH.md, Abschnitt 8 (Runbook-Schritte 4 bis 8) und Runtime State Inventory (die Zeile über `dist/`: das lokal gebaute Archiv ist **nicht** das Artefakt, das signiert wird)
    - .planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-07-SUMMARY.md (das neue Budget, damit das Gate-Protokoll die erwartete Zahl nennt)
  </read_first>
  <action>
    Alle sechs Gates aus Runbook-Schritt 3 laufen lassen, in der Reihenfolge, in der sie dort
    stehen, und jede Ausgabe protokollieren:
    `pytest -q`, `ruff check .`, `ruff format --check .`, `pyright`,
    `vulture src scripts vulture_whitelist.py`, `python scripts/check_tool_budget.py`.
    Ist eines rot, ist das das Ergebnis dieses Plans und der Tag entsteht nicht; ein rotes Gate
    wird nicht umgangen und nicht ausgenommen.

    Danach der optionale, billige Probelauf: `scripts/build_store_release.sh` und
    `tar -tzf dist/mcp_connector-0.1.8.tar.gz`, plus ein Blick in das Archiv. Der Zweck ist die
    Ordnerstruktur (genau ein Verzeichnis auf oberster Ebene, Kleinbuchstaben und Unterstriche,
    enthält `appinfo/info.xml`) und der Nachweis, dass `CHANGELOG.md` mitreist. Der Kommentar im
    Protokoll sagt ausdrücklich, dass dieses lokale Archiv **nicht** das Artefakt ist, das später
    signiert wird: `tar.gz` ist nicht byte-reproduzierbar, nachgewiesen am 2026-08-20 mit 31909
    gegen 32168 Bytes und unterschiedlichem sha256. Signiert wird immer das heruntergeladene
    Release-Asset, und das ist Plan 11-10.

    In `docs/store-submission.md` werden **nur** die Proof-Zeilen dieses Plans in die
    Nachweistabelle eingetragen, in ihrer Form (Datum mit Zeit in Z, Behauptung als ganzer Satz,
    Befehl): eine Zeile für "alle sechs Gates von 0.1.8 sind lokal grün" mit der gemessenen
    Bytezahl und dem Budget aus 11-07-SUMMARY, und eine Zeile für den Archiv-Probelauf mit der
    Ordnerstruktur. Die Zeilen für die Schritte 4 bis 8 bleiben leer: sie gehören zu Plan 11-10
    und eine Zeile, die vor ihrem Ereignis geschrieben wird, ist eine Behauptung ohne Deckung.

    Das Runbook selbst wird **nicht** umgeschrieben. Wird beim Lauf eine Ungenauigkeit im Runbook
    entdeckt, kommt sie als eigener Satz in die Zusammenfassung, und die Korrektur ist eine
    Entscheidung und keine Nebenwirkung.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run pytest -q &amp;&amp; uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run pyright &amp;&amp; uv run vulture src scripts vulture_whitelist.py &amp;&amp; uv run python scripts/check_tool_budget.py</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; bash scripts/build_store_release.sh &amp;&amp; tar -tzf dist/mcp_connector-0.1.8.tar.gz | head -20 &amp;&amp; tar -tzf dist/mcp_connector-0.1.8.tar.gz | grep -c "^mcp_connector/appinfo/info.xml$" &amp;&amp; tar -tzf dist/mcp_connector-0.1.8.tar.gz | grep -c "^mcp_connector/CHANGELOG.md$"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run python -c "
from pathlib import Path
doc = Path('docs/store-submission.md').read_text(encoding='utf-8')
lines = [l for l in doc.splitlines() if '0.1.8' in l and l.strip().startswith('|')]
assert len(lines) >= 2, lines
for line in lines:
    assert 'Z |' in line, ('a proof row carries a date with time in Z', line)
for forbidden in ('—', '–'):
    assert forbidden not in doc, 'no em dash or en dash'
assert 'git push origin v0.1.8' not in doc.split('Release runbook')[0], 'the tag belongs to plan 11-10'
print('gate protocol recorded')
"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; git tag --list "v0.1.8" &amp;&amp; test -z "$(git tag --list v0.1.8)"</automated>
  </verify>
  <acceptance_criteria>
    - Alle sechs Gates aus Runbook-Schritt 3 sind gelaufen und grün; die Ausgaben sind protokolliert
    - `scripts/check_tool_budget.py` meldet 21 Werkzeuge und die Bytezahl aus 11-07-SUMMARY, und endet mit Exit-Code 0
    - Der Archiv-Probelauf zeigt genau ein Verzeichnis auf oberster Ebene, es heißt `mcp_connector`, und `appinfo/info.xml` sowie `CHANGELOG.md` sind darin
    - `docs/store-submission.md` trägt mindestens zwei neue Proof-Zeilen für 0.1.8, je mit Datum inklusive Zeit in Z, Behauptung als ganzer Satz und Befehl
    - Die Proof-Zeilen für die Runbook-Schritte 4 bis 8 sind **nicht** vorab geschrieben
    - Der Text der acht Runbook-Schritte selbst ist unverändert; jede entdeckte Ungenauigkeit steht als Satz in der Zusammenfassung
    - `git tag --list v0.1.8` ist leer: dieser Plan erzeugt keinen Tag
    - `docs/store-submission.md` enthält keine Em-Dashes und keine En-Dashes
  </acceptance_criteria>
  <done>Die Fassung ist an der Tag-Grenze angekommen: alles, was ohne Owner-Freigabe getan werden darf, ist getan und protokolliert, und kein Tag existiert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vier Versionsstellen zu Git-Tag | Weichen sie ab, installiert AppAPI einen Tag, der nicht existiert, oder Code, den niemand releast hat |
| Store-Beschreibung zu Nutzer | Der Text ist die einzige Aussage, die ein Nutzer vor der Installation liest |
| Changelog zu Abnehmer | Eine Antwortformat-Änderung, die nicht im Changelog steht, bricht einen Abnehmer ohne Warnung |
| Lokales Archiv zu signiertem Artefakt | Ein lokal gebautes `tar.gz` ist nicht byte-identisch mit dem veröffentlichten |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-57 | Tampering | Eine der vier Versionsstellen bleibt auf 0.1.7 | mitigate | Ein Prüfschritt liest alle vier und vergleicht sie mit `0.1.8`; das Manifest-Gate in `test_exapp_env_setup.py` und `release.yml` halten es zusätzlich |
| T-11-58 | Repudiation | Die Umbenennung `truncated` zu `preview_truncated` fehlt im Changelog | mitigate | Ein Prüfschritt behauptet `preview_truncated` in `CHANGELOG.md`; der Eintrag steht unter `### Changed` und benennt die Antwortformat-Änderung |
| T-11-59 | Information Disclosure | Das verbotene Wort erreicht ein öffentliches Artefakt | mitigate | Vokabular-Gate lokal grün, plus eigene Prüfschritte gegen `CHANGELOG.md` und jeden Beschreibungsblock; case insensitiv |
| T-11-60 | Spoofing | Die Store-Beschreibung verspricht mehr, als die Fassung liefert | mitigate | Jede neue Aussage stammt aus einer Zusammenfassung dieser Phase; die Ketten-Formulierung aus CR-01 bleibt unverändert |
| T-11-61 | Tampering | Ein lokal gebautes Archiv wird für das signierte Artefakt gehalten | mitigate | Der Probelauf ist ausdrücklich als Strukturprüfung markiert, mit dem Beleg 31909 gegen 32168 Bytes; signiert wird erst in Plan 11-10 und nur das heruntergeladene Asset |
| T-11-62 | Denial of Service | Ein Tag entsteht ohne Owner-Freigabe und löst ein Release aus | mitigate | Dieser Plan erzeugt keinen Tag; ein Prüfschritt behauptet, dass `git tag --list v0.1.8` leer ist; der Checkpoint liegt in Plan 11-10 |
| T-11-63 | Repudiation | Eine Proof-Zeile wird vor ihrem Ereignis geschrieben | mitigate | Nur die zwei Zeilen dieses Plans werden eingetragen; die Zeilen für Schritt 4 bis 8 bleiben leer und ein Prüfschritt behauptet die Form der geschriebenen |
| T-11-64 | Tampering | Der Spendenlink wird beim Editieren des Manifests verändert | mitigate | Ein Prüfschritt behauptet `paypalme/KhaledCherifDev` unverändert; die Zeile ist ausdrücklich vom Umfang ausgenommen |
| T-11-SC | Tampering | Paketinstallation | mitigate | Keine neue Sprachabhängigkeit: `uv.lock` bleibt unangetastet, `pyproject.toml` wird ausschließlich in der `version`-Zeile geändert, es gibt kein `uv add`, kein `pip install` und kein `npm install` in diesem Plan |
</threat_model>

<verification>
1. Alle vier Versionsstellen tragen `0.1.8` als identische Zeichenkette, und `git tag --list v0.1.8` ist leer.
2. `uv run pytest -q`, `uv run ruff check .`, `uv run ruff format --check .`, `uv run pyright`, `uv run vulture src scripts vulture_whitelist.py` und `uv run python scripts/check_tool_budget.py` sind alle grün.
3. `uv run pytest tests/unit/test_exapp_env_setup.py -q` ist grün: Versions-, Beschreibungs-, Variablen- und Vokabular-Gate akzeptieren die Fassung.
4. `CHANGELOG.md` hat die Sektionen `[0.1.8]` und `[0.1.5]`, absteigend sortiert, mit vollständigem Linkblock.
5. `git diff --stat` zeigt Änderungen ausschließlich an `pyproject.toml`, `src/mcp_connector/__init__.py`, `appinfo/info.xml`, `CHANGELOG.md` und `docs/store-submission.md`; `uv.lock` ist unangetastet.
</verification>

<success_criteria>
- Version 0.1.8 steht an allen vier Stellen, der Changelog trägt den 0.1.8-Block, und die fehlende 0.1.5-Sektion ist nachgetragen (EXAPP-07, IN-02).
- Die Store-Texte nennen die neuen Fähigkeiten in drei Sprachen, und alle Manifest-Gates inklusive Vokabular-Gate sind grün.
- Alle sechs Gates aus Runbook-Schritt 3 sind lokal grün und protokolliert, mit zwei Proof-Zeilen in der Nachweistabelle.
- Kein Tag existiert: der irreversible Schritt beginnt erst nach der Owner-Freigabe in Plan 11-10.
</success_criteria>

<output>
Create `.planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-09-SUMMARY.md` when done.
Die Zusammenfassung nennt die vier geänderten Versionsstellen mit ihren Zeilennummern, den
vollständigen Inhalt des 0.1.8-Changelog-Blocks, den Inhalt der nachgetragenen 0.1.5-Sektion samt
Belegquelle, die Ausgaben aller sechs Gates mit der Bytezahl von `check_tool_budget.py`, das
Ergebnis des Archiv-Probelaufs, und jede im Runbook entdeckte Ungenauigkeit, weil Plan 11-10 auf
genau diesem Zustand aufsetzt und den Tag daraus baut.
</output>
