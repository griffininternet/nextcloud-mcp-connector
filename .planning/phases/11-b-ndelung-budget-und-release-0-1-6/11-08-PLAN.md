---
phase: 11-b-ndelung-budget-und-release-0-1-6
plan: 08
type: execute
wave: 5
depends_on: [11-03, 11-07]
files_modified:
  - scripts/acceptance_all_tools.py
  - src/mcp_connector/tools/mail.py
  - tests/unit/test_mail_tools.py
  - tests/unit/test_exapp_env_setup.py
  - README.md
  - README.de.md
  - README.fr.md
autonomous: true
requirements: [TOOL-15, TOOL-16]

must_haves:
  truths:
    - "Das Abnahmeskript prüft gegen die Registry statt gegen eine zweite Namensliste"
    - "Ein fehlgeschlagener Aufruf im Abnahmeskript erzeugt keine SKIP-Zeile mit einer Begründung, die nicht stimmt"
    - "Das Abnahmeskript ruft fetch für beide neuen Id-Arten, jedes Mal mit einer Id aus einem echten Lesevorgang"
    - "Der Schlüssel truncated hat in einer Mail-Antwort nur noch eine Bedeutung je Ebene"
    - "Alle drei READMEs nennen die gleiche Zahl von Id-Arten und die gleiche Werkzeugtabelle"
    - "Der Docstring über die Umgebungsvariablen nennt die Zahl, die das Gate derselben Datei pinnt"
  artifacts:
    - path: "scripts/acceptance_all_tools.py"
      provides: "Werkzeugliste aus client.list_tools statt zweiter Kopie, korrigierte SKIP-Kette, fetch für message und table"
      contains: "list_tools"
    - path: "src/mcp_connector/tools/mail.py"
      provides: "Eindeutige Benennung der Vorschau-Kappung auf Eintragsebene"
      contains: "preview_truncated"
    - path: "README.md"
      provides: "Sieben Id-Arten, aktualisierte fetch-Zeile in der Werkzeugtabelle, konsistente Werkzeugzahl"
      contains: "message:"
    - path: "README.de.md"
      provides: "Dieselben zwei Stellen auf Deutsch, mit echten Umlauten"
      contains: "message:"
    - path: "README.fr.md"
      provides: "Dieselben zwei Stellen auf Französisch, mit Akzenten und Cedille"
      contains: "message:"
  key_links:
    - from: "scripts/acceptance_all_tools.py"
      to: "src/mcp_connector/server/"
      via: "die erwartete Werkzeugmenge kommt aus der laufenden Registry, nicht aus einer Liste im Skript"
      pattern: "list_tools"
    - from: "README.md"
      to: "tests/contract/test_tool_surface.py"
      via: "die Werkzeugtabelle wird gegen die Registry geprüft, die Übersetzungen nicht"
      pattern: "test_tool_surface"
---

<objective>
TOOL-15, zweite Hälfte, plus die drei verbliebenen Info-Befunde aus `10-REVIEW.md`: das
Abnahmeskript verliert seine zweite Namensliste und seine irreführende SKIP-Begründung (IN-04 und
der Tech-Debt-Eintrag "acceptance_all_tools-Zählung"), der Schlüssel `truncated` in der
Mail-Antwort verliert seine Doppelbedeutung (IN-01), der Docstring über die Umgebungsvariablen
nennt wieder die richtige Zahl (IN-03), und die drei READMEs ziehen die zwei neuen Id-Arten nach.

Purpose: Die zweite Namensliste in `scripts/acceptance_all_tools.py` ist eine zweite Wahrheit über
die Werkzeugoberfläche. Die Regel dieses Projekts ist eindeutig: die Zahl lebt in einem Test, nie
in einem Dokument. TOOL-15 ist die Gelegenheit, sie zu ersetzen statt sie zu aktualisieren.

Bei den READMEs ist die Driftstelle bekannt und benannt: der Contract-Test
`test_the_readme_permission_table_matches_the_live_registry` liest **nur** `README.md`. Die zwei
Übersetzungen tragen dieselbe Tabelle mit denselben englischen Stufenwörtern und werden **nicht**
geprüft, das ist der Befund WR-05 aus Phase 10 und der Grund, alle drei in derselben Task
anzufassen.

Output: ein Abnahmeskript mit einer Wahrheit, eine Mail-Antwort mit eindeutigen Schlüsseln, ein
korrigierter Docstring, drei READMEs auf demselben Stand.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-RESEARCH.md
@.planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-PATTERNS.md
@.planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-01-SUMMARY.md
@.planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-03-SUMMARY.md
@.planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-07-SUMMARY.md
@.planning/phases/10-mail-strikt-lesend-und-die-trifecta-grenze/10-REVIEW.md

Die sieben geänderten Dateien:
@scripts/acceptance_all_tools.py
@src/mcp_connector/tools/mail.py
@tests/unit/test_mail_tools.py
@tests/unit/test_exapp_env_setup.py
@README.md
@README.de.md
@README.fr.md

Die Wahrheit, gegen die geprüft wird:
@tests/contract/test_tool_surface.py

<interfaces>
Die vier Info-Befunde aus 10-REVIEW.md, alle als deferred markiert:
  IN-01  src/mcp_connector/tools/mail.py:477-481 (Eintrag: Vorschau gekappt) und :434-436
         (Antwort: Seite gekappt). In level=messages heißt truncated: true auf Antwortebene
         "die Seite wurde geschnitten, es gibt ggf. next", auf Eintragsebene "die Vorschau wurde
         bei 400 Bytes geschnitten". Ein Modell, das _CURSOR_HINT liest ("the answer says with
         truncated that it was cut"), kann die beiden verwechseln.
         Fix: Eintragsfeld umbenennen, z. B. preview_truncated, ODER die Doppelnutzung im
         Tool-Docstring benennen.
  IN-02  CHANGELOG-Sektion [0.1.5] fehlt. Gehört zu Plan 11-09, NICHT hierher.
  IN-03  tests/unit/test_exapp_env_setup.py:1796-1797. Der Docstring von variable_problems
         spricht von vier Variablen; das Set-Equality-Gate derselben Datei (:1960-1967) pinnt
         sechs (NC_MCP_PUBLIC_URL, DCR, CIMD, Allowlist x2, NC_MCP_TALK_SEND).
  IN-04  scripts/acceptance_all_tools.py:292-303. Schlägt mail_browse level=mailboxes fehl,
         gibt call einen leeren String zurück, loads("") wird {}, _preferred_mailbox({}) wird "",
         und das Skript schreibt zusätzlich zur FAIL-Zeile eine SKIP-Zeile mit dem Grund
         "that mail account lists no mailbox", der nicht stimmt.
         Fix: den Rückgabewert von call prüfen (leer = bereits als FAIL verbucht, keine
         SKIP-Zeile), analog für den Messages-Schritt.

Aus scripts/acceptance_all_tools.py (481 Zeilen):
  expected = {...}          Zeile 443, die zweite Kopie der 21 Namen
  never_called = expected - report.called()
  print(f"\nOK: all {len(expected)} tools answered over stdio.")     Zeile 476
  Die Regel für ein neues Id-Kind steht schon in derselben Datei (Z. 325-330):
      "The sixth id kind, and only ever with an id that came out of the read above: a
       fetch(\"mail:<number>\") on a guessed number would be a request about somebody's mail."

Aus tests/contract/test_tool_surface.py (720 Zeilen):
  test_the_readme_permission_table_matches_the_live_registry
      liest AUSSCHLIESSLICH README.md, Zeilen die mit "| `" beginnen, Spalte 2 gleich
      "read" oder "create-only", und vergleicht Name und Stufe mit der Registry
  test_a_documented_tool_count_is_the_current_one_or_says_which_run_it_is_from
      jede Seite in docs/ und README.md, die eine Tool-Zahl nennt, muss entweder die aktuelle
      nennen oder auf tests/contract/test_tool_surface.py verweisen
      Regex: tools=(\d+)|(\d+)\s+tools\b
      Betroffene Zeilen heute: README.md:19 ("The 21 tools are curated") und README.md:30
      ("All 21 tools of the v1 set")

Die Id-Kind-Zeile, heute in drei Sprachen (fünf Arten):
  README.md:402-405, README.de.md:417, README.fr.md:427
Die fetch-Zeile der Werkzeugtabelle:
  README.md:216, README.de.md:224, README.fr.md:228
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Das Abnahmeskript bekommt eine Wahrheit statt einer Kopie</name>
  <files>scripts/acceptance_all_tools.py</files>
  <read_first>
    - scripts/acceptance_all_tools.py (ganze Datei, 481 Zeilen: der Modul-Docstring, der Aufbau von `call` und `report`, die Mail-Kette mit den drei SKIP-Zweigen um Zeile 285 bis 303, der Kommentar zur Regel für ein neues Id-Kind um Zeile 325 bis 330, die `expected`-Menge um Zeile 443 und die Ausgabezeile um Zeile 476)
    - tests/contract/test_tool_surface.py (`EXPECTED_TOOLS` als die eingefrorene Wahrheit und der Grund, warum die Zahl in einem Test lebt)
    - .planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-01-SUMMARY.md und 11-03-SUMMARY.md (die endgültigen Id-Formen `message:` und `table:` und die Ablehnungssätze)
    - .planning/phases/10-mail-strikt-lesend-und-die-trifecta-grenze/10-REVIEW.md, Befund IN-04 im Wortlaut
    - .planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-RESEARCH.md, Ist-Zustand 7 und Open Question 4
  </read_first>
  <action>
    Drei Änderungen in `scripts/acceptance_all_tools.py`.

    (1) Die zweite Namensliste verschwindet. Statt `expected` als literale Menge im Skript wird
        die erwartete Menge aus der laufenden Verbindung geholt: die Namen aus
        `client.list_tools()` sind die Wahrheit, gegen die `report.called()` verglichen wird.
        Die Ausgabezeile nennt danach die Zahl aus der Registry und nicht die Länge einer Liste
        im Skript. Ein Kommentar sagt, warum: eine zweite Kopie der 21 Namen ist eine zweite
        Wahrheit über die Werkzeugoberfläche, und dieses Projekt hält die Zahl in einem Test
        (`tests/contract/test_tool_surface.py`), nie in einem Dokument oder Skript. Die
        Fehlermeldung bei einem nie gerufenen Werkzeug wird dadurch aussagekräftiger, nicht
        schwächer: sie nennt jetzt Werkzeuge, die die Registry führt und die Abnahme nicht
        angefasst hat, also genau den Fall, den ein handgepflegtes `expected` verpasst hätte.

    (2) IN-04. Die drei SKIP-Zweige der Mail-Kette prüfen ab jetzt den Rückgabewert von `call`:
        ein leerer String heißt, dass der Aufruf schon als FAIL verbucht ist, und dann wird
        **keine** SKIP-Zeile geschrieben. Nur wenn der Aufruf erfolgreich war und die Antwort
        wirklich kein Postfach beziehungsweise keine Nachricht enthält, entsteht eine SKIP-Zeile,
        und ihr Grund ist dann wahr. Derselbe Griff gilt für den Messages-Schritt. Der Lauf
        schlägt in beiden Fällen weiterhin über die FAIL-Zeile fehl; was sich ändert, ist, dass
        die Matrix keine Begründung mehr trägt, die nicht stimmt.

    (3) Die zwei neuen Id-Arten kommen in die Abnahme, wörtlich nach der Regel, die schon in
        derselben Datei steht: eine Id **nur** aus einem Lesevorgang desselben Laufs, nie eine
        geratene. Konkret: aus `talk_browse(level="messages")` mit einem Token aus
        `talk_browse(level="conversations")` fällt eine Nachrichten-Id, daraus wird
        `fetch("message:&lt;token&gt;:&lt;id&gt;")`; aus `tables_browse` fällt eine Tabellen-Id,
        daraus wird `fetch("table:&lt;id&gt;")`. Liefert der Lesevorgang nichts, ist das eine
        SKIP-Zeile mit einem wahren Grund und keine geratene Id, und der Grund unterscheidet
        "die Instanz hat keine Konversation mit Nachrichten" von "der Aufruf ist
        fehlgeschlagen".
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run pyright</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run python -c "
from pathlib import Path
src = Path('scripts/acceptance_all_tools.py').read_text(encoding='utf-8')
code = '\n'.join(l for l in src.splitlines() if not l.lstrip().startswith('#'))
assert 'list_tools' in code, 'the expected set comes from the registry'
for name in ('files_search', 'notes_read', 'mail_browse', 'prepare_context'):
    assert code.count(f'\"{name}\"') <= 2, (name, 'the second copy of the tool list has to go')
assert 'message:' in code and 'table:' in code
assert 'lists no mailbox' in src, 'the honest skip reason stays, but behind a success check'
print('acceptance script has one truth')
"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run vulture src scripts vulture_whitelist.py &amp;&amp; uv run pytest -q</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; docker compose -p nc-mcp-exapp -f compose.exapp.yml up -d --wait &amp;&amp; set -a &amp;&amp; . ./.env.exapp &amp;&amp; set +a &amp;&amp; uv run python scripts/acceptance_all_tools.py</automated>
  </verify>
  <acceptance_criteria>
    - Die literale `expected`-Menge mit den 21 Namen existiert nicht mehr; die erwartete Menge kommt aus `client.list_tools()`
    - Die Abschlusszeile nennt die Zahl aus der Registry, und ein Kommentar begründet den Verzicht auf die zweite Liste mit dem Verweis auf `tests/contract/test_tool_surface.py`
    - Eine SKIP-Zeile entsteht in der Mail-Kette nur nach einem erfolgreichen Aufruf; ein leerer Rückgabewert von `call` erzeugt keine SKIP-Zeile (IN-04)
    - Der Lauf schlägt bei einem fehlgeschlagenen `mail_browse` weiterhin über die FAIL-Zeile fehl
    - Das Skript ruft `fetch` für `message:` und für `table:`, jedes Mal mit einer Id aus einem Lesevorgang desselben Laufs
    - Fehlt ein Objekt, entsteht eine SKIP-Zeile, deren Grund "keine Daten" von "Aufruf fehlgeschlagen" unterscheidet
    - `uv run python scripts/acceptance_all_tools.py` gegen die laufende Topologie endet mit Exit-Code 0, und die Matrix enthält keine SKIP-Zeile neben einer FAIL-Zeile desselben Aufrufs
    - `uv run vulture src scripts vulture_whitelist.py` ist grün, ohne neuen Whitelist-Eintrag
  </acceptance_criteria>
  <done>Die Werkzeugoberfläche hat wieder eine Wahrheit, die Abnahme deckt beide neuen Id-Arten mit echten Ids ab, und keine SKIP-Zeile behauptet mehr etwas Falsches.</done>
</task>

<task type="auto">
  <name>Task 2: IN-01 und IN-03: ein Schlüssel, eine Bedeutung, und eine richtige Zahl im Docstring</name>
  <files>src/mcp_connector/tools/mail.py, tests/unit/test_mail_tools.py, tests/unit/test_exapp_env_setup.py</files>
  <read_first>
    - src/mcp_connector/tools/mail.py (die Stellen aus IN-01: `_CURSOR_HINT` um Zeile 89 mit dem Satz "the answer says with truncated that it was cut", die Antwortebene um Zeile 434 bis 440 mit dem Kommentar über `truncated` ohne `next`, die Eintragsebene um Zeile 489, und die zweite Antwortebene um Zeile 576 bis 586)
    - tests/unit/test_mail_tools.py (ganze Datei, 918 Zeilen: alle Stellen, die `truncated` auf Eintragsebene behaupten)
    - tests/unit/test_exapp_env_setup.py (der Docstring von `variable_problems` um Zeile 1796 und das Set-Equality-Gate derselben Datei um Zeile 1960 bis 1967, das sechs Variablen pinnt)
    - .planning/phases/10-mail-strikt-lesend-und-die-trifecta-grenze/10-REVIEW.md, Befunde IN-01 und IN-03 im Wortlaut
  </read_first>
  <action>
    IN-01. Von den zwei im Review genannten Wegen wird der klarere gewählt: das Feld auf
    **Eintragsebene** wird umbenannt, auf `preview_truncated`. Der Grund gehört als Kommentar
    dazu: `_CURSOR_HINT` sagt einem Modell wörtlich, dass die Antwort mit `truncated` mitteilt,
    geschnitten worden zu sein, und dieselbe Zeichenkette auf Eintragsebene mit einer anderen
    Bedeutung ist genau die Verwechslung, die ein Modell nicht auflösen kann, weil beide Aussagen
    plausibel sind. Die Antwortebene behält `truncated`, weil dort der Hinweistext hängt und weil
    ein `next` daneben steht.

    Der Vertrag ändert sich damit sichtbar, also gehört die Umbenennung in den Changelog von
    0.1.8 (Plan 11-09) und die Zusammenfassung nennt sie ausdrücklich als Antwortformat-Änderung.
    Der Tool-Docstring von `mail_browse` nennt beide Schlüssel mit ihrer je eigenen Bedeutung, und
    wenn `README.md` die Antwortform beschreibt, wird sie in derselben Task mitgezogen (die
    Beispielzeile mit `truncated` in allen drei READMEs beschreibt die Antwortebene und bleibt
    damit richtig; das ist zu prüfen, nicht anzunehmen).

    In `tests/unit/test_mail_tools.py` werden alle Behauptungen über `truncated` auf
    Eintragsebene auf den neuen Namen gezogen, und ein neuer Test behauptet ausdrücklich, dass
    beide Schlüssel in einer Antwort gleichzeitig auftreten können und Verschiedenes bedeuten:
    eine gekappte Seite mit einem Eintrag, dessen Vorschau ebenfalls gekappt ist. Das ist der
    Fall, der IN-01 überhaupt erst zu einem Problem gemacht hat.

    IN-03. Im Docstring von `variable_problems` wird die Zahl korrigiert: das Set-Equality-Gate
    derselben Datei pinnt sechs Variablen (`NC_MCP_PUBLIC_URL`, DCR, CIMD, zwei Allowlist-Namen,
    `NC_MCP_TALK_SEND`). Statt einer Zahl, die beim nächsten Schalter wieder falsch wird, nennt
    der Docstring die Menge als das, was sie ist: die Variablen, die das Gate derselben Datei
    pinnt, mit Verweis auf die Gate-Funktion. Eine Zahl im Prosatext, die ein Test daneben pinnt,
    ist eine zweite Wahrheit; dieselbe Regel wie bei der Werkzeugzahl.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run pyright &amp;&amp; uv run pytest tests/unit/test_mail_tools.py tests/unit/test_exapp_env_setup.py -q</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run python -c "
from pathlib import Path
mail = Path('src/mcp_connector/tools/mail.py').read_text(encoding='utf-8')
assert 'preview_truncated' in mail
entry_lines = [l for l in mail.splitlines() if 'entry[' in l and 'truncated' in l]
assert entry_lines and all('preview_truncated' in l for l in entry_lines), entry_lines
answer_lines = [l for l in mail.splitlines() if 'answer[' in l and 'truncated' in l]
assert answer_lines and all('preview_truncated' not in l for l in answer_lines), answer_lines
env = Path('tests/unit/test_exapp_env_setup.py').read_text(encoding='utf-8')
assert 'four variables' not in env, 'the stale number has to go'
print('IN-01 and IN-03 closed')
"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run pytest -q &amp;&amp; uv run pytest tests/contract -q</automated>
  </verify>
  <acceptance_criteria>
    - Das Feld auf Eintragsebene heißt `preview_truncated`; jede `entry[...]`-Zuweisung mit dem Wort `truncated` benutzt den neuen Namen
    - Das Feld auf Antwortebene heißt weiter `truncated`; keine `answer[...]`-Zuweisung benutzt `preview_truncated`
    - Der Tool-Docstring von `mail_browse` nennt beide Schlüssel mit ihrer je eigenen Bedeutung
    - Ein neuer Test in `tests/unit/test_mail_tools.py` belegt eine Antwort, in der beide Schlüssel gleichzeitig gesetzt sind, und behauptet ihre unterschiedliche Bedeutung
    - Die Zeichenkette "four variables" kommt in `tests/unit/test_exapp_env_setup.py` nicht mehr vor; der Docstring verweist auf die Gate-Funktion statt auf eine Zahl
    - Die Beispielzeile mit `truncated` in `README.md`, `README.de.md` und `README.fr.md` ist geprüft und beschreibt weiterhin die Antwortebene
    - `uv run pytest -q` und `uv run pytest tests/contract -q` sind grün
  </acceptance_criteria>
  <done>`truncated` bedeutet auf jeder Ebene genau eine Sache, der Fall, in dem beide gleichzeitig gelten, hat einen Test, und keine Zahl im Prosatext widerspricht mehr einem Test derselben Datei.</done>
</task>

<task type="auto">
  <name>Task 3: Drei READMEs auf denselben Stand, sieben Id-Arten</name>
  <files>README.md, README.de.md, README.fr.md</files>
  <read_first>
    - README.md (die Zeilen mit der Werkzeugzahl um Zeile 19 und 30, die Werkzeugtabelle mit der `fetch`-Zeile um Zeile 216, die Beispielzeile mit `truncated` um Zeile 243, die Filtergrammatik ab Zeile 330, die Id-Kind-Zeile um Zeile 402 bis 405)
    - README.de.md (die entsprechenden Stellen: Werkzeugtabelle um Zeile 224, Beispielzeile um Zeile 251, Id-Kind-Zeile um Zeile 417)
    - README.fr.md (die entsprechenden Stellen: Werkzeugtabelle um Zeile 228, Beispielzeile um Zeile 256, Id-Kind-Zeile um Zeile 427)
    - tests/contract/test_tool_surface.py (`test_the_readme_permission_table_matches_the_live_registry`: welche Zeilenform gelesen wird und dass nur `README.md` geprüft wird; `test_a_documented_tool_count_is_the_current_one_or_says_which_run_it_is_from` mit seinem Regex)
    - .planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-01-SUMMARY.md und 11-03-SUMMARY.md (die endgültigen Id-Formen und was `fetch` je Art liefert)
    - .planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-04-SUMMARY.md und 11-05-SUMMARY.md (die neue Antwortform von `prepare_context` mit `talk` und `mail`)
    - .planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-07-SUMMARY.md (das neue Budget und die Werkzeugzahl)
    - .planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-RESEARCH.md, Pitfall 12
  </read_first>
  <action>
    Vier Stellen, in **drei** Sprachen, in einer Task, weil das Gate nur `README.md` prüft und
    die zwei Übersetzungen genau deshalb die Driftstelle sind.

    (1) Die Id-Kind-Zeile: heute "the five id kinds", nach dieser Phase sieben. Die zwei neuen
        Formen kommen dazu, jede mit dem Zusatz, der sie unverwechselbar macht:
        `message:&lt;token&gt;:&lt;messageId&gt;` (eine einzelne Talk-Nachricht, gelesen über die
        nebenwirkungsfreie Kontextroute; die Nachricht muss in dieser Konversation lesbar sein,
        sonst gibt es eine Ablehnung und keine Nachbarnachricht) und `table:&lt;tableId&gt;`
        (Titel, Zeilenzahl und die ersten Zeilen einer Tabelle; eine View ist keine Tabelle und
        bleibt eine URL). Und der Satz, warum ein Mail-Suchtreffer weiter eine URL bleibt: die
        Auflösung einer RFC-Message-Id auf die `databaseId` ist ungemessen.
    (2) Die `fetch`-Zeile der Werkzeugtabelle: heute "resolves an id to a file, note, card, event
        or mail", danach mit Talk-Nachricht und Tabelle. **Nur** die Beschreibungsspalte ändert
        sich; Name und Stufe bleiben, weil `test_the_readme_permission_table_matches_the_live_registry`
        genau diese zwei Spalten gegen die Registry vergleicht.
    (3) Die Werkzeugzahl-Zeilen: die Zahl bleibt 21, also ist hier nur zu **prüfen**, dass beide
        Zeilen weiter die aktuelle Zahl nennen, und dass keine neue Zahl in einen Text gerutscht
        ist. Das Gate `test_a_documented_tool_count_is_the_current_one_or_says_which_run_it_is_from`
        deckt `docs/` und `README.md`, nicht die Übersetzungen: die zwei Übersetzungen werden von
        Hand gegengelesen.
    (4) Der Abschnitt, der `prepare_context` beschreibt: das Bündel trägt jetzt einen Talk-Digest
        und Mail-Ungelesen-Zähler, je mit eigenem Zeitbudget und eigenem `degraded`-Eintrag. Die
        Beschreibung nennt beide, nennt die Kappungen (höchstens drei Konversationen, Vorschau in
        Bytes, höchstens drei Konten) und sagt ausdrücklich, dass im Standardbündel **kein**
        Betreff und **kein** Mailinhalt steht. Wenn die README die Antwortform als Beispiel zeigt,
        kommen die zwei neuen Schlüssel dazu.

    Für alle drei Sprachen gilt: echte Umlaute in der deutschen Fassung, Akzente und Cedille in
    der französischen, keine Em-Dashes, keine Emojis, und die englischen Stufenwörter in den
    Tabellen der Übersetzungen bleiben englisch (`| read |`, `| create-only |`), weil sie dort
    dieselbe Registry beschreiben.

    Am Ende ein Abgleich der drei Dateien gegeneinander: jede der vier Stellen sagt in allen drei
    Sprachen dasselbe. Ein Diff der Zeilenzahlen ist dafür kein Beweis, ein Lesen der vier Stellen
    schon.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run pytest tests/contract -q</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run python -c "
from pathlib import Path
files = {name: Path(name).read_text(encoding='utf-8') for name in ('README.md', 'README.de.md', 'README.fr.md')}
for name, text in files.items():
    for needle in ('message:', 'table:', 'mail:', 'event:', 'card:', 'note:', 'file:', 'url:'):
        assert needle in text, (name, needle)
    assert 'five id kinds' not in text, (name, 'the count of id kinds moved on')
    for forbidden in ('—', '–'):
        assert forbidden not in text, (name, 'no em dash or en dash')
    low = text.lower()
    assert 'talk' in low and 'mail' in low
de = files['README.de.md']
assert any(ch in de for ch in 'äöüß'), 'the german readme carries real umlauts'
fr = files['README.fr.md']
assert any(ch in fr for ch in 'éèêàçô'), 'the french readme carries its accents'
for name in ('README.de.md', 'README.fr.md'):
    assert '| read |' in files[name] or '| read ' in files[name], (name, 'level words stay english')
print('three readmes aligned')
"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run python -c "
import re
from pathlib import Path
current = None
import importlib
mod = importlib.import_module('tests.contract.test_tool_surface') if False else None
text = Path('tests/contract/test_tool_surface.py').read_text(encoding='utf-8')
names = re.findall(r'\"([a-z_]+)\",', text.split('EXPECTED_TOOLS')[1].split('}')[0])
current = len(set(names))
print('expected tools', current)
for name in ('README.md', 'README.de.md', 'README.fr.md'):
    body = Path(name).read_text(encoding='utf-8')
    for match in re.finditer(r'(\d+)\s+(?:tools|Werkzeuge|outils)\b', body):
        assert int(match.group(1)) == current, (name, match.group(0), current)
print('tool counts consistent in three languages')
"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - Alle drei READMEs nennen sieben Id-Arten, inklusive `message:<token>:<messageId>` und `table:<tableId>`, jede mit ihrem unverwechselbaren Zusatz
    - Alle drei READMEs sagen, warum ein Mail-Suchtreffer eine URL bleibt
    - Die `fetch`-Zeile der Werkzeugtabelle nennt in allen drei Sprachen Talk-Nachricht und Tabelle; Name und Stufe der Zeile sind unverändert
    - `test_the_readme_permission_table_matches_the_live_registry` ist grün
    - Jede Werkzeugzahl in allen drei READMEs ist gleich `len(EXPECTED_TOOLS)`; `test_a_documented_tool_count_is_the_current_one_or_says_which_run_it_is_from` ist grün
    - Der `prepare_context`-Abschnitt nennt in allen drei Sprachen Talk-Digest und Mail-Zähler, die drei Kappungen und den Satz, dass kein Betreff und kein Mailinhalt im Standardbündel steht
    - Die deutsche Fassung trägt echte Umlaute, die französische ihre Akzente; keine Datei enthält Em-Dashes, En-Dashes oder Emojis
    - Die Stufenwörter in den Tabellen der Übersetzungen bleiben englisch
    - `uv run pytest -q` und `uv run pytest tests/contract -q` sind grün
  </acceptance_criteria>
  <done>Die drei READMEs sagen an allen vier Stellen dasselbe, und die Driftstelle, die kein Gate prüft, ist in derselben Task geschlossen worden wie die, die eines hat.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skriptliste zu Registry | Eine zweite Namensliste kann von der Registry abweichen, ohne dass ein Gate es merkt |
| Abnahmematrix zu Leser | Eine SKIP-Begründung, die nicht stimmt, verdeckt einen echten Fehler |
| Antwortschlüssel zu Modellverständnis | Ein Schlüssel mit zwei Bedeutungen wird auf einer Ebene falsch gelesen |
| Übersetzte Dokumentation zu Gate | `README.de.md` und `README.fr.md` werden von keinem Test geprüft |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-51 | Tampering | Ein registriertes Werkzeug fehlt in der Skriptliste und wird nie abgenommen | mitigate | Die erwartete Menge kommt aus `client.list_tools()`; die literale Liste entfällt und ein Prüfschritt behauptet ihr Verschwinden |
| T-11-52 | Repudiation | Eine SKIP-Zeile behauptet "keine Daten", obwohl der Aufruf fehlgeschlagen ist | mitigate | Die SKIP-Zweige prüfen den Rückgabewert von `call`; ein leerer Wert erzeugt keine SKIP-Zeile (IN-04) |
| T-11-53 | Spoofing | Die Abnahme ruft `fetch` mit einer geratenen Id und liest fremden Inhalt | mitigate | Jede Id kommt aus einem Lesevorgang desselben Laufs, nach der Regel, die schon in der Datei steht |
| T-11-54 | Tampering | `truncated` wird auf Eintragsebene als Seitenkappung gelesen und ein Modell paginiert im Kreis | mitigate | Umbenennung auf `preview_truncated`; die Antwortebene behält `truncated`, wo der Hinweistext und `next` hängen; ein Test belegt den gemeinsamen Fall |
| T-11-55 | Repudiation | Eine Zahl in einem Docstring widerspricht einem Gate derselben Datei | mitigate | IN-03: der Docstring verweist auf die Gate-Funktion statt eine Zahl zu nennen; ein Prüfschritt behauptet die Abwesenheit von "four variables" |
| T-11-56 | Information Disclosure | Die Übersetzungen versprechen etwas anderes als die englische Fassung, und kein Test merkt es | mitigate | Alle drei Dateien in derselben Task; ein Prüfschritt behauptet die acht Id-Präfixe, die Werkzeugzahl und die Abwesenheit von Em-Dashes in allen drei |
| T-11-SC | Tampering | Paketinstallation | mitigate | Keine neue Sprachabhängigkeit: `pyproject.toml` und `uv.lock` bleiben unangetastet, es gibt kein `uv add`, kein `pip install` und kein `npm install` in diesem Plan |
</threat_model>

<verification>
1. `uv run pytest -q` und `uv run pytest tests/contract -q` sind grün, inklusive der zwei Doku-Gates und der Annotations- und Werkzeugzahl-Tests.
2. `uv run ruff check .`, `uv run ruff format --check .`, `uv run pyright` und `uv run vulture src scripts vulture_whitelist.py` sind grün.
3. `uv run python scripts/acceptance_all_tools.py` gegen die laufende Topologie endet mit Exit-Code 0, und die Matrix trägt keine SKIP-Zeile neben einer FAIL-Zeile desselben Aufrufs.
4. `uv run python scripts/check_tool_budget.py` endet mit Exit-Code 0: dieser Plan ändert keine `Field`-Beschreibung.
5. `git diff --stat` zeigt keine Änderung an `pyproject.toml`, `uv.lock`, `appinfo/info.xml` oder `CHANGELOG.md`.
</verification>

<success_criteria>
- Das Abnahmeskript hat eine Wahrheit über die Werkzeugoberfläche und deckt beide neuen Id-Arten mit echten Ids ab (TOOL-15, inklusive des Tech-Debt-Eintrags "acceptance_all_tools-Zählung").
- Keine SKIP-Begründung im Abnahmeskript ist mehr falsch (IN-04).
- `truncated` hat je Ebene genau eine Bedeutung (IN-01), und der Docstring über die Umgebungsvariablen widerspricht keinem Gate mehr (IN-03).
- Alle drei READMEs nennen sieben Id-Arten, dieselbe Werkzeugzahl und dieselbe Bündelbeschreibung (TOOL-16, Befund WR-05 aus Phase 10).
</success_criteria>

<output>
Create `.planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-08-SUMMARY.md` when done.
Die Zusammenfassung nennt die Umbenennung `truncated` zu `preview_truncated` ausdrücklich als
Antwortformat-Änderung mit der betroffenen Ebene, die Liste der geänderten README-Stellen je
Sprache, und alle vier geschlossenen Info-Befunde mit ihrer Kennung, weil Plan 11-09 den
Changelog-Block von 0.1.8 daraus schreibt und IN-02 dort als letzter offener Befund geschlossen
wird.
</output>
