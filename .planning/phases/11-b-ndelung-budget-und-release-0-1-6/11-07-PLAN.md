---
phase: 11-b-ndelung-budget-und-release-0-1-6
plan: 07
type: execute
wave: 4
depends_on: [11-05]
files_modified:
  - src/mcp_connector/server/reg_mail.py
  - src/mcp_connector/server/reg_talk.py
  - src/mcp_connector/server/reg_tables.py
  - scripts/check_tool_budget.py
  - tests/contract/test_tool_surface.py
autonomous: true
requirements: [TOOL-15]

must_haves:
  truths:
    - "Alle fünf neuen Werkzeuge sind schema-diätet: jede Kürzung ist gemessen und keine verliert Information"
    - "tools/list bleibt unter einem Budget, das auf der Messung dieser Phase verankert ist, mit einer neuen Messzeile im Skript"
    - "Die Obergrenze je Werkzeug ist bewusst entschieden, mit Messzeile, statt aus Gewohnheit stehen zu bleiben"
    - "Die Annotationen sagen weiter die Wahrheit: drei lesend, zwei anlegend, und ein Contract-Test hält es"
    - "Die Werkzeugzahl 21 steht identisch in Registry und Contract-Tests"
  artifacts:
    - path: "src/mcp_connector/server/reg_mail.py"
      provides: "Gekürzte Field-Beschreibungen, insbesondere die Filtergrammatik, ohne Informationsverlust"
      contains: "mail_browse"
    - path: "scripts/check_tool_budget.py"
      provides: "Neue Messzeile mit Datum, Werkzeugzahl, Anlass, gemessenen Bytes und der Rechnung, plus das neu verankerte BUDGET_BYTES"
      contains: "Measurement 2026-08"
    - path: "tests/contract/test_tool_surface.py"
      provides: "Die Werkzeugzahl und die Annotationsmengen unverändert grün gegen die diätete Oberfläche"
      contains: "EXPECTED_TOOLS"
  key_links:
    - from: "scripts/check_tool_budget.py"
      to: "src/mcp_connector/server/"
      via: "das Gate misst die registrierte Oberfläche, die Diät passiert in den Field-Beschreibungen und Docstrings"
      pattern: "BUDGET_BYTES"
    - from: "src/mcp_connector/server/reg_mail.py"
      to: "README.md"
      via: "die Filtergrammatik steht in der README und im Tool-Docstring, nicht im Schema"
      pattern: "filter"
---

<objective>
TOOL-15, erste Hälfte: die fünf Werkzeuge des Meilensteins auf Schema-Diät setzen, danach messen,
danach das Gate auf die neue Messung verankern. In dieser Reihenfolge, nicht in einer anderen.

Purpose: Die Rechnung ist exakt und sie ist die eigentliche Aufgabe dieses Plans. Gemessen am
2026-08-24: **15736 Bytes bei 21 Werkzeugen**, Gate 18500. Die Regel "Messung plus 15 Prozent,
aufgerundet auf die nächsten 500" ergibt bei unveränderter Messung genau 18500, also exakt den
Zwischenstand, den Plan 10-06 ausdrücklich als Zwischenstand markiert hat. TOOL-15 erwartet 17500
bis 18000, und das ist nur erreichbar, wenn die Diät die Messung senkt: **mindestens 84 Bytes für
18000, mindestens 519 Bytes für 17500.** Verankert man mechanisch auf die alte Messung, schreibt
man dieselbe Zahl ein zweites Mal hin und die Aufgabe ist nicht erledigt.

Dazu kommt eine Gegenbewegung, die schon passiert ist: Plan 11-05 hat die Beschreibung von
`prepare_context` ehrlicher gemacht, und die Differenz steht in 11-05-SUMMARY. Sie arbeitet gegen
diese Zahl und muss gegengerechnet werden.

Die fünf Werkzeuge kosten heute zusammen 4463 Bytes und damit 28,4 Prozent der ganzen Oberfläche:
`mail_browse` 1377, `talk_browse` 886, `tables_create_row` 780, `tables_browse` 772, `talk_send`
648. `mail_browse` liegt 23 Bytes unter der Obergrenze je Werkzeug, und der teuerste Block darin
ist die Filtergrammatik im `Field`, deren richtiger Ort in derselben Datei schon benannt ist.

Output: gekürzte Registrierungen, eine neue Messzeile, ein verankertes `BUDGET_BYTES`, eine
bewusste Entscheidung über `MAX_TOOL_BYTES`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-RESEARCH.md
@.planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-PATTERNS.md
@.planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-05-SUMMARY.md

Die fünf geänderten Dateien:
@src/mcp_connector/server/reg_mail.py
@src/mcp_connector/server/reg_talk.py
@src/mcp_connector/server/reg_tables.py
@scripts/check_tool_budget.py
@tests/contract/test_tool_surface.py

Die Grenzen und das Vorbild:
@src/mcp_connector/server/__init__.py
@src/mcp_connector/server/reg_context.py
@README.md

<interfaces>
Gemessene Ist-Werte je Werkzeug (2026-08-24, kompaktes JSON, UTF-8, 15736 Bytes gesamt):
  1377 mail_browse            678 unified_search
  1351 calendar_create_event  648 talk_send
   951 calendar_list_events   644 notes_create
   924 search                 625 prepare_context
   886 talk_browse            538 files_list
   877 deck_create_card       501 files_upload
   780 tables_create_row      501 files_read
   772 tables_browse          487 contacts_search
   761 fetch                  478 notes_search
   736 deck_browse            339 notes_read
   703 files_search

Die Arithmetik der Verankerung (Regel: Messung mal 1,15, aufgerundet auf die nächsten 500):
  15736 (heute)      -> 18096,4 -> 18500   Zwischenstand, trifft die Erwartung NICHT
  15652              -> 17999,8 -> 18000   obere Kante der Erwartung
  15218 bis 15652    ->            18000   in der Erwartung
  14783 bis 15217    ->            17500   untere Kante der Erwartung
  14782 oder weniger ->            17000   unter der Erwartung

Aus scripts/check_tool_budget.py (122 Zeilen):
  BUDGET_BYTES = 18_500       Gesamtsumme, mit fünf Messzeilen darüber
  MAX_TOOL_BYTES = 1400       Obergrenze je Werkzeug, in Bytes seit 2026-08-21
  Der Messweg: Client(mcp, raise_exceptions=True).list_tools(),
      result.model_dump(by_alias=True, exclude_none=True, mode="json"),
      json.dumps(..., separators=(",", ":"), ensure_ascii=False),
      len(...encode("utf-8")); je Werkzeug dasselbe über das einzelne Tool-Dict
  Die Regeln der Datei, wörtlich einzuhalten:
      "Raising the number is allowed, but only together with a new measurement line above"
      "a tool that reaches it gets a shorter description and never a higher limit ...
       mail_browse was 1585 bytes when it was first written and it was cut, not exempted"

Aus src/mcp_connector/server/reg_mail.py (77 Zeilen):
  Der Modul-Docstring sagt selbst, wohin die Grammatik gehört: "The full filter grammar stands
  in the docstring of the tool and in the README instead of in the schema: every byte of a
  Field description is paid for in every tools/list of every session, and a grammar is read
  once."
  Der teuerste Block ist trotzdem noch im Field:
      filter: "Only level=messages: type:value conditions, space separated; types is, not,
               from, subject, tags, start, end; start/end take Unix seconds"
  Die vollständige Grammatik steht bereits in README.md ab Zeile 330.

Aus src/mcp_connector/server/reg_talk.py (75 Zeilen):
  talk_browse ist die kürzeste browse-Registrierung der Familie und damit die Vorlage
  talk_send steht hinter dem Admin-Schalter NC_MCP_TALK_SEND

Aus src/mcp_connector/server/__init__.py:
  READ_ONLY = ToolAnnotations(read_only_hint=True, open_world_hint=False)
  CREATE_ONLY setzt zusätzlich destructive_hint=False und idempotent_hint=False

Der Annotationsstand, verifiziert (11-RESEARCH.md, Ist-Zustand 6):
  tables_browse READ_ONLY, talk_browse READ_ONLY, mail_browse READ_ONLY,
  tables_create_row CREATE_ONLY, talk_send CREATE_ONLY
  Also drei lesend, zwei anlegend, wie TOOL-15 es verlangt; der Nachweis steht in
  test_every_tool_carries_honest_annotations gegen die eingefrorene Menge CREATE_TOOLS
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema-Diät der fünf Werkzeuge, jede Kürzung gemessen</name>
  <files>src/mcp_connector/server/reg_mail.py, src/mcp_connector/server/reg_talk.py, src/mcp_connector/server/reg_tables.py</files>
  <read_first>
    - src/mcp_connector/server/reg_mail.py (ganze Datei, 77 Zeilen: der Modul-Docstring mit dem Satz, wohin die Grammatik gehört, alle `Field`-Beschreibungen von `mail_browse`, der Tool-Docstring)
    - src/mcp_connector/server/reg_talk.py (ganze Datei, 75 Zeilen: `talk_browse` als kürzeste `browse`-Registrierung der Familie und damit Vorlage, `talk_send` mit dem Admin-Schalter)
    - src/mcp_connector/server/reg_tables.py (ganze Datei, 73 Zeilen: `tables_browse` und `tables_create_row`)
    - src/mcp_connector/server/reg_context.py (nach Plan 11-05, als Beispiel für eine Kürzung, die dieselbe Aussage kürzer macht)
    - README.md (ab Zeile 330: die vollständige Filtergrammatik, damit belegt ist, dass die Information beim Kürzen nicht verloren geht)
    - scripts/check_tool_budget.py (der Messweg, der hier für Einzelmessungen kopiert wird)
    - .planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-05-SUMMARY.md (die Bytedifferenz der neuen `prepare_context`-Beschreibung)
    - .planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-RESEARCH.md, Abschnitt "Budget-Gate, gemessen am 2026-08-24", Pitfall 2 und Pitfall 11
    - .planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-PATTERNS.md, Abschnitt "server/reg_*.py - Schema-Diät"
  </read_first>
  <action>
    Vor jeder Änderung eine Ausgangsmessung: die Bytegröße jedes der 21 Werkzeuge und die
    Gesamtsumme, mit **genau** dem Messweg aus `scripts/check_tool_budget.py`
    (`model_dump(by_alias=True, exclude_none=True, mode="json")`, `separators=(",", ":")`,
    `ensure_ascii=False`, `len(...encode("utf-8"))`), damit die Zahlen mit den Zahlen der Recherche
    vergleichbar bleiben. Diese Zahl ist der Nenner aller weiteren Aussagen dieses Plans.

    Die Reihenfolge der Schnitte, vom größten Hebel zum kleinsten:

    (1) `mail_browse`, 1377 Bytes, der größte der fünf und 23 Bytes unter der Obergrenze je
        Werkzeug. Der teuerste Block ist die `Field`-Beschreibung von `filter` mit der
        Typenliste. Der Modul-Docstring derselben Datei sagt, wohin eine Grammatik gehört, und die
        vollständige Grammatik steht bereits in `README.md` ab Zeile 330. Die `Field`-Beschreibung
        wird auf das reduziert, was ein Modell **im Schema** braucht, um den Parameter überhaupt
        richtig zu benutzen: dass er nur auf `level=messages` gilt und dass die Form `type:value`
        ist, mit dem Verweis, wo die Typen stehen. Die Typenliste selbst wandert in den
        Tool-Docstring, falls sie dort noch nicht vollständig steht. Kein Informationsverlust:
        nach der Änderung ist jede Typangabe im Tool-Docstring **und** in der README lesbar.
    (2) `talk_browse` (886), `tables_create_row` (780), `tables_browse` (772), `talk_send` (648):
        je `Field`-Beschreibung gegen die kürzeste Formulierung derselben Aussage prüfen.
        `talk_browse` ist die Vorlage der Familie, also ist dort der geringste Ertrag zu erwarten;
        `tables_create_row` und `tables_browse` sind die wahrscheinlicheren Kandidaten.

    Drei Verbote, alle drei sind Projektregeln und keine Vorlieben:
    - Eine Kürzung, die eine Information ersatzlos entfernt, ist verboten. Jede entfernte Angabe
      muss nach der Änderung im Tool-Docstring oder in der README lesbar sein, und die Prüfung
      dazu gehört in die Verifikation, nicht in ein gutes Gefühl.
    - Ein `Literal`-Enum bleibt ein `Literal`-Enum: der Modul-Docstring von `reg_mail.py` sagt,
      warum (das Modell sieht die gültigen Werte statt zu raten und für die Korrektur eine Runde
      zu bezahlen, D-06 und D-14). Ein Enum in einen freien String zu verwandeln, wäre eine
      Bytesparnis auf Kosten einer Fehlerrunde.
    - Leere Strings bleiben die Defaults: sonst erscheint ein `anyOf` aus String und Null im
      Schema, und das kostet mehr Bytes als jede Beschreibung, die man dafür kürzen könnte.

    Nach jedem Schnitt neu messen. Am Ende steht eine Tabelle mit Werkzeug, Bytes vorher, Bytes
    nachher und Differenz, plus die Gesamtsumme vorher und nachher. Diese Tabelle geht in die
    Zusammenfassung, weil Task 2 die Verankerung darauf stützt.

    Erreicht die Diät die Zielmarke nicht (mindestens 84 Bytes für 18000, mindestens 519 für
    17500), dann ist das Ergebnis **ehrlich als solches** zu dokumentieren und nicht durch eine
    Kürzung zu erkaufen, die Information verliert. Der Fall wird in Task 2 behandelt.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run pyright</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run python -c "
import asyncio, json
from mcp import Client
from mcp_connector.server import mcp

async def main():
    async with Client(mcp, raise_exceptions=True) as client:
        result = await client.list_tools()
    payload = result.model_dump(by_alias=True, exclude_none=True, mode='json')
    blob = json.dumps(payload, separators=(',', ':'), ensure_ascii=False)
    total = len(blob.encode('utf-8'))
    sizes = {t['name']: len(json.dumps(t, separators=(',', ':'), ensure_ascii=False).encode('utf-8')) for t in payload['tools']}
    print('total', total, 'tools', len(sizes))
    for name in ('mail_browse', 'talk_browse', 'tables_browse', 'tables_create_row', 'talk_send', 'prepare_context'):
        print(f'  {name}: {sizes[name]}')
    assert len(sizes) == 21, len(sizes)
    assert max(sizes.values()) <= 1400, sorted(sizes.items(), key=lambda kv: -kv[1])[:3]
    assert sizes['mail_browse'] < 1377, ('mail_browse must shrink', sizes['mail_browse'])
    assert total < 15736, ('the measurement has to fall', total)

asyncio.run(main())
"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run python -c "
import re
from pathlib import Path
from mcp_connector.server import reg_mail
readme = Path('README.md').read_text(encoding='utf-8')
src = Path(reg_mail.__file__).read_text(encoding='utf-8')
doc = reg_mail.mail_browse.__doc__ or ''
for kind in ('is', 'not', 'from', 'subject', 'tags', 'start', 'end'):
    token = f'{kind}:'
    assert token in doc or token in readme, f'{token} lost its place'
assert 'Literal[' in src, 'the level enum stays an enum'
print('no information was lost')
"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run pytest -q &amp;&amp; uv run pytest tests/contract -q</automated>
  </verify>
  <acceptance_criteria>
    - Die Gesamtmessung von `tools/list` liegt unter 15736 Bytes, und die Werkzeugzahl ist weiter 21
    - `mail_browse` ist kleiner als 1377 Bytes; kein Werkzeug liegt über 1400 Bytes
    - Jede der sieben Filtertypen (`is`, `not`, `from`, `subject`, `tags`, `start`, `end`) ist nach der Änderung im Tool-Docstring von `mail_browse` oder in `README.md` lesbar
    - `level` bleibt in allen betroffenen Registrierungen ein `Literal`-Enum, und leere Strings bleiben die Defaults
    - Eine Tabelle mit Werkzeug, Bytes vorher, Bytes nachher und Differenz je gekürztem Werkzeug plus Gesamtsumme vorher und nachher liegt vor
    - `uv run ruff check .`, `uv run ruff format --check .`, `uv run pyright` sind grün
    - `uv run pytest -q` und `uv run pytest tests/contract -q` sind grün: die Werkzeugzahl, die Annotationsmengen und der `prepare_context`-Vertrag sind unberührt
  </acceptance_criteria>
  <done>Die fünf Werkzeuge des Meilensteins sind kürzer, jede Kürzung ist gemessen, und keine Angabe ist verloren gegangen: sie steht im Docstring oder in der README.</done>
</task>

<task type="auto">
  <name>Task 2: Das Gate neu verankern, mit Messzeile und einer Entscheidung über die Obergrenze</name>
  <files>scripts/check_tool_budget.py, tests/contract/test_tool_surface.py</files>
  <read_first>
    - scripts/check_tool_budget.py (ganze Datei, 122 Zeilen: alle fünf bestehenden Messzeilen und ihre Form, der `Zwischenstand`-Absatz mit dem Verweis auf TOOL-15, die zwei Regelsätze über das Anheben und über die Obergrenze je Werkzeug, `BUDGET_BYTES`, `MAX_TOOL_BYTES`, der Messweg in `main`)
    - Die Messtabelle aus Task 1 (Bytes vorher, nachher, Differenz, Gesamtsumme)
    - .planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-05-SUMMARY.md (die Bytedifferenz der `prepare_context`-Beschreibung, die gegen die Diät zu rechnen ist)
    - tests/contract/test_tool_surface.py (`EXPECTED_TOOLS` mit 21 Namen, `CREATE_TOOLS` mit sechs Namen, `test_the_curated_set_is_complete...`, `test_every_tool_carries_honest_annotations`)
    - .planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-RESEARCH.md, Pitfall 2, Pitfall 11, Open Question 3, Annahme A2
  </read_first>
  <action>
    Eine neue Messzeile über `BUDGET_BYTES`, in **genau** der Form der fünf vorhandenen: Datum,
    Zahl der Werkzeuge, Anlass, gemessene Bytes, dann die Rechnung als eigene Zeile. Der Anlass
    heißt hier "Schema-Diät der fünf Werkzeuge des Meilensteins v1.2 plus die ehrlichere
    Beschreibung von `prepare_context`", und die Rechnung steht ausgeschrieben:
    Messung mal 1,15, aufgerundet auf die nächsten 500. Die alten Zeilen bleiben stehen, weil eine
    Regression nur zuordenbar ist, solange die Zahl lesbar ist, aus der sie regressiert ist.

    `BUDGET_BYTES` wird auf das Ergebnis dieser Rechnung gesetzt. Der `Zwischenstand`-Absatz, der
    heute auf TOOL-15 in Phase 11 verweist, wird zur erledigten Aussage umgeschrieben: er sagt,
    dass die Verankerung hier stattgefunden hat, mit welcher Messung, und dass das Gate damit kein
    Zwischenstand mehr ist. Ein Verweis auf eine Aufgabe, die erledigt ist, wäre in sechs Monaten
    eine falsche Fährte.

    Drei Fälle, und der Plan schreibt alle drei vor, damit die Ausführung nicht improvisiert:
    - Landet die Messung zwischen 14783 und 15652, ergibt die Rechnung 17500 oder 18000 und trifft
      die Erwartung von TOOL-15. Das ist der Normalfall.
    - Landet die Messung bei 14782 oder darunter, ergibt die Rechnung 17000. Das ist **besser** als
      die Erwartung und wird so gesetzt und begründet; eine Erwartung ist keine Untergrenze.
    - Bleibt die Messung über 15652, ergibt die Rechnung weiterhin 18500. Dann bleibt das Gate bei
      18500, die neue Messzeile sagt die neue Messung, und ein Satz sagt ausdrücklich, dass die
      Diät die Marke nicht erreicht hat und warum (welche Kürzung verboten war, weil sie
      Information verloren hätte). Eine Zahl zu erfinden, um eine Erwartung zu treffen, ist der
      einzige Ausgang, den dieser Plan verbietet.

    Danach die Entscheidung über `MAX_TOOL_BYTES`, ausdrücklich verlangt (Open Question 3): heute
    1400 mit 23 Bytes Luft über `mail_browse`, und das Skript nennt diese Grenze das eigentliche
    Gate. Liegt `mail_browse` nach der Diät klar darunter, wird die Grenze gesenkt, und zwar auf
    den neuen größten Wert plus dieselbe Logik wie beim Gesamtbudget (Messung plus Luft für
    Formulierung, auf eine runde Zahl), mit eigener Messzeile in der Form der zwei vorhandenen.
    Bleibt sie, sagt eine Messzeile, dass sie bewusst bleibt, und warum. Eine Anhebung ist
    verboten: die Datei sagt selbst, dass ein Werkzeug, das die Decke erreicht, eine kürzere
    Beschreibung bekommt und nie eine höhere Grenze.

    In `tests/contract/test_tool_surface.py` wird nichts an `EXPECTED_TOOLS` und `CREATE_TOOLS`
    geändert: die Werkzeugzahl bleibt 21, die Annotationen bleiben drei lesend und zwei anlegend,
    und beides ist schon durch Tests gedeckt. Was ergänzt wird, ist ein Test, der die **Konsistenz
    der Zahl** zwischen Registry und Gate behauptet: die Zahl der Werkzeuge, die
    `check_tool_budget.py` zählt, ist die Länge von `EXPECTED_TOOLS`. Damit fällt der Fall auf,
    in dem ein Werkzeug registriert, aber nie eingefroren wurde.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run pyright</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run python scripts/check_tool_budget.py</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run python -c "
import asyncio, json, math, re
from pathlib import Path
from mcp import Client
from mcp_connector.server import mcp
import importlib.util
spec = importlib.util.spec_from_file_location('budget', 'scripts/check_tool_budget.py')
budget = importlib.util.module_from_spec(spec); spec.loader.exec_module(budget)

async def main():
    async with Client(mcp, raise_exceptions=True) as client:
        result = await client.list_tools()
    payload = result.model_dump(by_alias=True, exclude_none=True, mode='json')
    total = len(json.dumps(payload, separators=(',', ':'), ensure_ascii=False).encode('utf-8'))
    expected = int(math.ceil(total * 1.15 / 500.0) * 500)
    assert budget.BUDGET_BYTES == expected, (total, expected, budget.BUDGET_BYTES)
    assert budget.MAX_TOOL_BYTES <= 1400, budget.MAX_TOOL_BYTES
    src = Path('scripts/check_tool_budget.py').read_text(encoding='utf-8')
    lines = [l for l in src.splitlines() if 'Measurement 20' in l]
    assert len(lines) >= 6, lines
    assert str(total) in src, 'the new measurement number has to appear in a measurement line'
    assert 'TOOL-15 in phase 11 re-anchors' not in src, 'the pending note has to become a done note'
    print('anchored at', budget.BUDGET_BYTES, 'from', total, 'per tool ceiling', budget.MAX_TOOL_BYTES)

asyncio.run(main())
"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run pytest -q &amp;&amp; uv run pytest tests/contract -q &amp;&amp; uv run vulture src scripts vulture_whitelist.py</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check_tool_budget.py` enthält mindestens sechs Messzeilen; die neue nennt Datum, Werkzeugzahl 21, den Anlass, die gemessenen Bytes und die Rechnung in der Form der fünf vorhandenen
    - `BUDGET_BYTES` ist genau `ceil(Messung * 1,15 / 500) * 500`, mit der heutigen Messung nachgerechnet
    - Die Zeichenkette "TOOL-15 in phase 11 re-anchors" steht nicht mehr in der Datei; an ihrer Stelle steht eine erledigte Aussage mit der Messung
    - `MAX_TOOL_BYTES` ist gesenkt oder ausdrücklich unverändert, in beiden Fällen mit eigener Messzeile; ein Wert über 1400 ist ausgeschlossen
    - Verankert die Rechnung weiterhin auf 18500, steht ein Satz in der Datei, der sagt, dass die Diät die Marke von TOOL-15 nicht erreicht hat, und welche Kürzung verboten war
    - `EXPECTED_TOOLS` hat weiter 21 Namen, `CREATE_TOOLS` weiter sechs, und `test_every_tool_carries_honest_annotations` ist grün
    - Ein neuer Contract-Test behauptet, dass die vom Gate gezählte Werkzeugzahl gleich `len(EXPECTED_TOOLS)` ist
    - `uv run python scripts/check_tool_budget.py` endet mit Exit-Code 0
    - `uv run pytest -q`, `uv run pytest tests/contract -q` und `uv run vulture src scripts vulture_whitelist.py` sind grün
  </acceptance_criteria>
  <done>Das Gate steht auf einer Messung dieser Phase, die Rechnung ist im Skript nachlesbar, die Obergrenze je Werkzeug ist eine Entscheidung mit Messzeile, und der Verweis auf die offene Aufgabe ist eine erledigte Aussage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Werkzeugbeschreibung zu Modellverhalten | Jede gekürzte Angabe, die nirgends sonst steht, ist eine Information, die ein Modell erraten muss |
| Gate-Zahl zu Regressionsschutz | Ein Budget über der Messung schützt nichts, eine erfundene Zahl schützt schlechter als keine |
| Registry zu eingefrorener Menge | Ein registriertes, nie eingefrorenes Werkzeug entzieht sich beiden Gates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-44 | Tampering | Eine Kürzung entfernt eine Angabe ersatzlos, und ein Modell rät danach den Parameterwert | mitigate | Jede entfernte Angabe muss im Tool-Docstring oder in `README.md` lesbar bleiben; ein Prüfschritt behauptet alle sieben Filtertypen |
| T-11-45 | Tampering | Ein `Literal`-Enum wird zu einem freien String, um Bytes zu sparen | mitigate | Ausdrücklich verboten, mit der Begründung aus dem Modul-Docstring (D-06, D-14); ein Prüfschritt behauptet `Literal[` in `reg_mail.py` |
| T-11-46 | Repudiation | Das Gate wird angehoben, ohne dass eine Messung es trägt | mitigate | `BUDGET_BYTES` wird gegen `ceil(Messung * 1,15 / 500) * 500` nachgerechnet; mindestens sechs Messzeilen und die neue Zahl müssen in der Datei stehen |
| T-11-47 | Repudiation | Eine Zahl wird erfunden, um die Erwartung von TOOL-15 zu treffen | mitigate | Der Plan schreibt alle drei möglichen Ausgänge vor, inklusive des ehrlichen "Marke nicht erreicht" mit Begründung in der Datei |
| T-11-48 | Elevation of Privilege | Die Obergrenze je Werkzeug wird angehoben, statt eine Beschreibung zu kürzen | mitigate | Ein Wert über 1400 ist ausgeschlossen und wird geprüft; die Datei begründet die Regel selbst |
| T-11-49 | Spoofing | Eine falsche Annotation lässt ein schreibendes Werkzeug als lesend erscheinen | mitigate | `CREATE_TOOLS` bleibt eingefroren, `test_every_tool_carries_honest_annotations` bleibt grün; die Diät berührt nur Beschreibungen, nie Annotationen |
| T-11-50 | Tampering | Ein Werkzeug wird registriert, aber nie in `EXPECTED_TOOLS` eingefroren | mitigate | Ein neuer Contract-Test behauptet die Gleichheit der vom Gate gezählten Zahl und `len(EXPECTED_TOOLS)` |
| T-11-SC | Tampering | Paketinstallation | mitigate | Keine neue Sprachabhängigkeit: `pyproject.toml` und `uv.lock` bleiben unangetastet, es gibt kein `uv add`, kein `pip install` und kein `npm install` in diesem Plan |
</threat_model>

<verification>
1. `uv run python scripts/check_tool_budget.py` endet mit Exit-Code 0 und meldet 21 Werkzeuge.
2. `BUDGET_BYTES` gleich `ceil(Messung * 1,15 / 500) * 500` ist nachgerechnet, und die Messung steht als neue Zeile in der Datei.
3. `uv run pytest -q` und `uv run pytest tests/contract -q` sind grün: Werkzeugzahl 21, sechs Schreibwege, `prepare_context`-Vertrag inklusive Talk und Mail.
4. `uv run ruff check .`, `uv run ruff format --check .`, `uv run pyright` und `uv run vulture src scripts vulture_whitelist.py` sind grün.
5. `git diff --stat` zeigt keine Änderung an `pyproject.toml`, `uv.lock`, `src/mcp_connector/tools/` oder `appinfo/info.xml`.
</verification>

<success_criteria>
- Alle fünf neuen Werkzeuge sind schema-diätet, jede Kürzung ist gemessen, und keine Information ist verloren (TOOL-15).
- `tools/list` bleibt unter einem Budget, das auf der Messung dieser Phase verankert ist, mit neuer Messzeile im Skript.
- Die Obergrenze je Werkzeug ist eine bewusste Entscheidung mit Messzeile und wurde nicht angehoben.
- Die Annotationen sagen weiter die Wahrheit (drei lesend, zwei anlegend), und die Werkzeugzahl 21 steht identisch in Registry und Contract-Tests.
</success_criteria>

<output>
Create `.planning/phases/11-b-ndelung-budget-und-release-0-1-6/11-07-SUMMARY.md` when done.
Die Zusammenfassung enthält die vollständige Vorher-Nachher-Tabelle je gekürztem Werkzeug mit
Differenz, die Gesamtsumme vorher und nachher, das neue `BUDGET_BYTES` mit der ausgeschriebenen
Rechnung, die Entscheidung über `MAX_TOOL_BYTES` mit Begründung, und für jeden nicht ausgeführten
Schnitt den Grund, weil Plan 11-09 den Changelog-Block von 0.1.8 daraus schreibt und Plan 11-08
die Werkzeugzahl in drei READMEs gegenprüft.
</output>
