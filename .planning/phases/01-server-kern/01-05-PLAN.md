---
phase: 01-server-kern
plan: 05
type: execute
wave: 4
depends_on: [01-03]
files_modified:
  - src/mcp_connector/paging.py
  - src/mcp_connector/nextcloud/clients/dav.py
  - src/mcp_connector/tools/files.py
  - src/mcp_connector/server/reg_files.py
  - tests/fixtures/webdav_search_207.xml
  - tests/fixtures/webdav_propfind_207.xml
  - tests/unit/test_paging.py
  - tests/unit/test_files_search.py
  - tests/unit/test_files_list.py
  - tests/integration/test_files_browse.py
autonomous: true
requirements: [TOOL-01, SRV-03, SRV-05]

must_haves:
  truths:
    - "Ein Nutzer kann Dateien und Ordner nach Namen suchen und bekommt Treffer mit Pfad, Typ, Groesse und fileid"
    - "Ein Nutzer kann den Inhalt eines Ordners listen und sieht Kinder ohne den Ordner selbst"
    - "Das Modell erfaehrt in der Antwort, dass nur Namen und nicht Inhalte durchsucht werden"
    - "Abgeschnittene Trefferlisten liefern ein Cursor-Handle, das ohne Server-State weitergelesen werden kann"
  artifacts:
    - path: "src/mcp_connector/paging.py"
      provides: "Zustandslose Cursor-Handles (base64url von kompaktem JSON)"
      contains: "def encode_cursor"
    - path: "src/mcp_connector/nextcloud/clients/dav.py"
      provides: "SEARCH basicsearch und PROPFIND Depth 1"
      contains: "basicsearch"
    - path: "tests/fixtures/webdav_search_207.xml"
      provides: "Echte 207-Multistatus-Antwort als Testfixture"
      contains: "multistatus"
  key_links:
    - from: "src/mcp_connector/nextcloud/clients/dav.py"
      to: "SEARCH /remote.php/dav/"
      via: "lxml-gebauter basicsearch-Body mit d:limit"
      pattern: "searchrequest"
    - from: "src/mcp_connector/tools/files.py"
      to: "src/mcp_connector/paging.py"
      via: "cursor-Parameter und next-Feld"
      pattern: "encode_cursor|decode_cursor"
---

<objective>
Die Datei-Vertikale vervollstaendigen: files_search (WebDAV SEARCH) und files_list (PROPFIND Depth 1), beide mit zustandsloser Pagination und ehrlicher Erwartungssteuerung.

Purpose: Lesen allein reicht nicht, das Modell muss finden und navigieren koennen. Die Cursor-Handles sind gleichzeitig der Beweis fuer SRV-05 auf Tool-Ebene.
Output: Zwei weitere Tools, ein Paging-Modul, Fixtures aus echten 207-Antworten, Unit- und Integrationstests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-server-kern/01-CONTEXT.md
@.planning/phases/01-server-kern/01-RESEARCH.md
@.planning/phases/01-server-kern/01-03-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cursor-Handles und DAV-SEARCH- beziehungsweise PROPFIND-Schicht</name>
  <files>src/mcp_connector/paging.py, src/mcp_connector/nextcloud/clients/dav.py, tests/fixtures/webdav_search_207.xml, tests/fixtures/webdav_propfind_207.xml, tests/unit/test_paging.py</files>
  <read_first>
    - .planning/phases/01-server-kern/01-RESEARCH.md Abschnitte "files_search (WebDAV SEARCH)", "files_list (PROPFIND)", "Pattern 4: Stateless Pagination ueber Handles", "Pitfall 5", "Anti-Patterns to Avoid"
    - src/mcp_connector/nextcloud/clients/dav.py und clients/xml.py (Stand aus Plan 02 und 03)
  </read_first>
  <behavior>
    - decode_cursor(encode_cursor({"o": 25, "q": "budget"})) ergibt genau das Ausgangsobjekt
    - decode_cursor("nicht-base64") erzeugt ToolError mit Hinweis, nie einen Crash
    - build_search_body escapt einen Suchbegriff mit & und &lt; korrekt und erzeugt genau ein d:limit mit nresults
    - Der SEARCH-Endpoint ist die DAV-Wurzel /remote.php/dav/, der Scope ist /files/&lt;user&gt; oder /files/&lt;user&gt;/&lt;unterordner&gt;
    - parse aus einer 207-Fixture liefert Pfad, displayname, contenttype, size, resourcetype und fileid pro Treffer
    - PROPFIND Depth 1 ueberspringt den ersten d:response (den Ordner selbst)
  </behavior>
  <action>
    paging.py anlegen: encode_cursor(dict) -> base64url von kompaktem JSON, decode_cursor(str) -> dict mit defensiver Pruefung (ToolError bei ungueltigem Handle). Kein Server-State, keine Signatur, Kommentar mit der Begruendung: das Handle enthaelt keine Geheimnisse und keine Autoritaet, Credentials kommen weiterhin aus dem Auth-Kanal.
    dav.py erweitern:
    - build_search_body(scope, term, limit, props) per lxml (etree.SubElement, niemals f-String) nach dem verifizierten basicsearch-Schema: d:searchrequest / d:basicsearch mit d:select/d:prop (displayname, getcontenttype, getlastmodified, getcontentlength, resourcetype, oc:fileid), d:from/d:scope mit d:href und d:depth infinity, d:where mit d:like auf displayname und Literal %term%, d:orderby leer, d:limit/d:nresults. Default-Limit explizit setzen (ohne d:limit deckelt Nextcloud auf 100). Keine Query-Baeume aus Nutzereingaben bauen (OPERATOR_LIMIT 100).
    - search(client, creds, scope, term, limit) -> SEARCH auf "&lt;base&gt;/remote.php/dav/" mit Content-Type text/xml, Antwort per xml.parse_multistatus, Mapping der Properties auf ein stabiles dict.
    - propfind_children(client, creds, path) -> PROPFIND mit Depth: 1 und dem verifizierten Property-Set inklusive oc:permissions und oc:size; erster d:response wird uebersprungen.
    Fixtures aus den Beispielantworten in 01-RESEARCH.md ableiten und als echte 207-XML-Dateien ablegen, inklusive eines Eintrags mit Sonderzeichen im Dateinamen.
    tests/unit/test_paging.py: Roundtrip, ungueltiges Handle, Sonderzeichen.
  </action>
  <verify>
    <automated>uv run pytest tests/unit -q && uv run ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - "uv run pytest tests/unit -q" endet mit Exit-Code 0
    - dav.py baut den SEARCH-Body ausschliesslich per lxml (grep -v '^\s*#' auf dav.py liefert keinen f-String mit "&lt;d:")
    - Der SEARCH-Request geht an /remote.php/dav/ und enthaelt d:limit mit nresults
    - Ein Unit-Test belegt korrektes Escaping fuer einen Suchbegriff mit & und &lt;
    - PROPFIND Depth 1 ueberspringt den Ordner selbst (Test gegen die Fixture)
  </acceptance_criteria>
  <done>Such- und Listen-Requests sind korrekt gebaut, escapt, gedeckelt und gegen echte Antworten geparst.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tools files_search und files_list mit ehrlicher Erwartungssteuerung</name>
  <files>src/mcp_connector/tools/files.py, src/mcp_connector/server/reg_files.py, tests/unit/test_files_search.py, tests/unit/test_files_list.py, tests/integration/test_files_browse.py</files>
  <read_first>
    - .planning/phases/01-server-kern/01-RESEARCH.md Abschnitte "files_search", "files_list", "Pitfall 5", "Schema-Diaet", "Tool-Registrierung mit Annotationen und Schema-Diaet"
    - src/mcp_connector/tools/files.py und server/reg_files.py (Stand aus Plan 03)
  </read_first>
  <behavior>
    - files_search("budget") liefert items mit path, name, kind (file oder folder), size, content_type, modified, id im Format file:&lt;fileid&gt;
    - Null Treffer liefern items: [] plus note: "matched on names only; contents are not indexed"
    - Auch bei Treffern enthaelt die Antwort dieses note-Feld, damit das Modell keine Inhaltssuche behauptet
    - Ein Limit ueber dem Maximum wird auf das Maximum reduziert statt abgelehnt
    - Abgeschnittene Ergebnisse liefern truncated: true plus next-Handle, und dasselbe Handle als cursor-Parameter setzt korrekt fort
    - files_list("/") liefert die Kinder des Wurzelordners, ein Pfad auf eine Datei liefert einen erklaerenden Fehler
    - 404 auf einen unbekannten Ordner liefert einen Fehler mit Pfad, 403 einen Rechte-Hinweis
  </behavior>
  <action>
    tools/files.py um async def search(clients, query, folder="/", limit=25, cursor=None) und async def list_dir(clients, path="/", limit=100, cursor=None) ergaenzen. Beide nutzen safe_path und paging. Scope fuer SEARCH aus dem folder-Parameter bauen ("/files/&lt;user&gt;" plus optionaler Unterordner), Ergebnisse ueber ids.encode_file kennzeichnen.
    server/reg_files.py um files_search und files_list ergaenzen, beide @mcp.tool(annotations=READ_ONLY, structured_output=False) plus @graceful. Docstrings einzeilig und mit dem entscheidenden Constraint darin: "Search files and folders by name (matches names, not file contents)." beziehungsweise "List the direct children of a folder." Parameter-Descriptions knapp, keine verschachtelten Modelle im Input (Schema-Diaet, keine $defs).
    Unit-Tests mit respx fuer alle Faelle aus dem behavior-Block, inklusive Cursor-Fortsetzung und ungueltigem Cursor.
    tests/integration/test_files_browse.py mit @pytest.mark.integration: legt per files_upload eine Datei mit eindeutigem Namen an, findet sie per files_search, sieht sie in files_list, und prueft, dass ein Suchbegriff, der nur im Dateiinhalt steht, KEINEN Treffer liefert (belegt Pitfall 5 als Verhalten, nicht als Bug).
  </action>
  <verify>
    <automated>uv run pytest tests/unit -q && uv run pytest tests/contract -q && uv run python scripts/check_tool_budget.py</automated>
  </verify>
  <acceptance_criteria>
    - "uv run pytest tests/unit -q" endet mit Exit-Code 0 und deckt fuer beide Tools Happy, leer, 4xx, 5xx, Limit-Deckelung, Cursor-Fortsetzung und ungueltigen Cursor ab
    - tools/list enthaelt files_search und files_list mit read_only_hint=True und ohne output_schema
    - Jede Suchantwort enthaelt das Feld note mit dem Hinweis auf die Namenssuche
    - "uv run python scripts/check_tool_budget.py" endet mit Exit-Code 0
    - "uv run pytest -m integration -q --collect-only" sammelt die neuen Integrationstests fehlerfrei
  </acceptance_criteria>
  <done>Vier Datei-Tools sind vollstaendig, token-schlank und mit zustandsloser Pagination ausgestattet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Suchbegriff zu WebDAV-XML | Modellgenerierter Text wird Teil eines XML-Bodys |
| Cursor-Handle zu Serverlogik | Client-gelieferter, opaker String steuert Fortsetzung |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-30 | Tampering | XML-Injection ueber Suchbegriffe im basicsearch-Body | mitigate | Body ausschliesslich per lxml (etree.SubElement) gebaut, Escaping automatisch; Unit-Test mit & und &lt; im Suchbegriff |
| T-01-31 | Denial of Service | XXE und Billion Laughs beim Parsen der 207-Antwort | mitigate | xml.hardened_parser (resolve_entities=False, no_network=True, huge_tree=False) fuer jede Antwort |
| T-01-32 | Elevation of Privilege | Path Traversal ueber folder- und path-Parameter | mitigate | safe_path() und Scope-Bau ausschliesslich unter /files/&lt;user&gt; aus dem Auth-Kanal, nie aus einem Parameter |
| T-01-33 | Tampering | Manipuliertes Cursor-Handle | mitigate | Handle enthaelt keine Autoritaet, nur Offset und Query; defensives Dekodieren mit ToolError, Credentials kommen weiterhin aus dem Auth-Kanal |
| T-01-34 | Denial of Service | Unbegrenzte Trefferlisten sprengen den Client-Kontext | mitigate | d:limit im Request, Limit-Deckelung im Tool, truncated-Marker plus Handle |
| T-01-35 | Information Disclosure | Credential-Leak ueber Logs | mitigate | Geerbt aus Plan 02 (maskiertes repr, from None, httpx-Logger auf WARNING) |
| T-01-36 | Tampering | Create-only-Schreibschutz | transfer | Dieser Plan enthaelt nur Lesepfade; die Kontrolle liegt in Plan 03 |
| T-01-37 | Spoofing | SSRF | accept | Kein URL-Parameter, Basis-URL aus Env; als Nicht-Ziel dokumentiert |
</threat_model>

<verification>
1. "uv run pytest -q" gruen, Budget-Gate gruen.
2. Escaping-Test und Cursor-Roundtrip belegt.
3. Contract-Test zeigt vier files_-Tools mit korrekten Annotationen.
</verification>

<success_criteria>
Suchen, Listen, Lesen und Anlegen von Dateien funktionieren aus einem MCP-Client, mit ehrlicher Erwartungssteuerung und ohne Server-State.
</success_criteria>

<output>
Create `.planning/phases/01-server-kern/01-05-SUMMARY.md` when done
</output>
