---
phase: 01-server-kern
plan: 13
type: execute
wave: 4
depends_on: [01-04, 01-12]
files_modified:
  - docs/contrib/227-pr-body.md
autonomous: false
requirements: [CONTRIB-01, SRV-01]

must_haves:
  truths:
    - "Der Fix-PR an nextcloud/context_agent#227 ist eingereicht und verlinkt"
    - "Der PR enthaelt genau eine funktionale Aenderung und keinen Scope darueber hinaus"
    - "Der Commit traegt einen Signed-off-by-Trailer (DCO-Praxis des Zielrepos) und keinen Claude-Trailer"
    - "Die Reproduktion ist im PR-Body als praezise Kommandofolge beschrieben, der automatisierte Regressionstest bleibt in unserem Repo"
  artifacts:
    - path: "docs/contrib/227-pr-body.md"
      provides: "PR-Text mit Problem, Ursache, Fix, Reproduktion und PR-URL"
      contains: "stateless_http"
  key_links:
    - from: "docs/contrib/227-pr-body.md"
      to: "tests/compat/legacy_client_check.py"
      via: "PR-Body verweist auf unseren Regressionstest als Belegquelle"
      pattern: "legacy_client_check"
---

<objective>
Den Ein-Zeilen-Fix fuer nextcloud/context_agent#227 als Pull Request einreichen: stateless_http konfigurierbar machen, Default sessionfaehig.

Purpose: CONTRIB-01 ist die Oekosystem-Flanke. Der Bug ist dieselbe Fehlerklasse, gegen die unser Client-Matrix-Test aus Plan 04 schuetzt, deshalb koennen wir ihn glaubwuerdig belegen. Ein Maintainer hat am 2026-08-14 auf das Issue reagiert, ein PR ist willkommen.
Output: Fork, Branch, DCO-signierter Commit, PR mit Reproduktionsanleitung, PR-URL dokumentiert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-server-kern/01-CONTEXT.md
@.planning/phases/01-server-kern/01-RESEARCH.md
@.planning/phases/01-server-kern/01-04-SUMMARY.md
@.planning/phases/01-server-kern/01-12-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fork, Ein-Zeilen-Fix mit DCO-Commit und PR-Body vorbereiten</name>
  <files>docs/contrib/227-pr-body.md</files>
  <read_first>
    - .planning/phases/01-server-kern/01-RESEARCH.md Abschnitt "Contribution-Fix #227 (CONTRIB-01)" komplett, plus "Statelessness und die #227-Fehlerklasse" und "Pitfall 1"
    - tests/compat/legacy_client_check.py (aus Plan 04, dient als Belegquelle)
  </read_first>
  <action>
    Vorpruefung mit gh: Issue-Status von nextcloud/context_agent#227 lesen, offene PRs auf Dopplung pruefen (falls inzwischen jemand denselben Fix eingereicht hat, KEINEN PR anlegen, sondern das in der SUMMARY vermerken und den Checkpoint entsprechend informieren).
    Arbeitsverzeichnis fuer den Fork ausserhalb unseres Repos anlegen (zum Beispiel unter dem Scratchpad oder C:\Users\Student\context_agent-fork), damit nichts in nextcloud-mcp-connector landet.
    "gh repo fork nextcloud/context_agent --clone" als street1983nk, Branch "fix/stateless-http-session-compat" anlegen.
    In ex_app/lib/main.py die eine Zeile "http_mcp_app = mcp.http_app("/", transport="http", stateless_http=True)" ersetzen durch die konfigurierbare Variante: ein os.getenv("MCP_STATELESS_HTTP", "0") mit Auswertung auf 1, true, yes, danach mcp.http_app("/", transport="http", stateless_http=_stateless), plus zwei Kommentarzeilen, die den Grund nennen (SDK>=1.28-Clients behalten die Session nach initialize und brechen bei stateless Transport mit "Session terminated" ab, siehe #227). Die Zielapp nutzt fastmcp 2.14.7, also die vorhandene http_app-Signatur beibehalten und NICHT auf mcp 2.x migrieren.
    Kein weiterer Scope: keine Formatierung fremder Zeilen, keine zusaetzlichen Dateien (damit greift das REUSE-Gate nicht, das nur neue Dateien betrifft), kein Test im fremden CI (die Matrix dort ist schwer und wuerde nur Rauschen erzeugen).
    Commit mit "git commit -s" (DCO ist gelebte Praxis im Zielrepo), Conventional-Commit-Titel, kein Co-Authored-By-Trailer. Branch in den Fork pushen.
    docs/contrib/227-pr-body.md in unserem Repo anlegen (englisch): Problem, Ursache (in fastmcp bewirkt stateless_http Wegwerf-Sessions und kostet beide Server-zu-Client-Kanaele), Fix (eine Zeile, per Env umschaltbar, Default sessionfaehig), Reproduktion als Kommandofolge inklusive eines Clients mit mcp>=1.29,<2 und der erwarteten Fehlermeldung "Session terminated", Verweis darauf, dass wir denselben Ablauf als Regressionstest in tests/compat/legacy_client_check.py automatisiert haben, sowie ein Satz zur Rueckwaertskompatibilitaet (wer stateless bewusst braucht, setzt MCP_STATELESS_HTTP=1).
  </action>
  <verify>
    <automated>test -f docs/contrib/227-pr-body.md && grep -c "legacy_client_check" docs/contrib/227-pr-body.md && python -c "import sys,io; t=io.open('docs/contrib/227-pr-body.md',encoding='utf-8').read(); sys.exit(1 if (chr(8212) in t or chr(8211) in t) else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - Der Fork-Branch enthaelt genau eine funktionale Aenderung in ex_app/lib/main.py (git diff --stat zeigt eine Datei)
    - "git log -1 --format=%B" im Fork enthaelt einen Signed-off-by-Trailer und keinen Co-Authored-By-Trailer
    - Der Branch ist zum Fork gepusht (gh api zeigt den Branch)
    - docs/contrib/227-pr-body.md existiert, ist englisch, ohne Em-Dashes, und verweist auf tests/compat/legacy_client_check.py
    - Falls bereits ein fremder PR denselben Fix enthaelt, ist das in der SUMMARY dokumentiert und kein Branch gepusht
  </acceptance_criteria>
  <done>Der Fix liegt einreichfertig im Fork, der PR-Text ist geschrieben und belegt.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner-Freigabe fuer die Einreichung</name>
  <read_first>
    - docs/contrib/227-pr-body.md
  </read_first>
  <what-built>
    Fork von nextcloud/context_agent unter street1983nk, Branch fix/stateless-http-session-compat mit einer einzigen funktionalen Aenderung in ex_app/lib/main.py, DCO-signiert. Der PR-Text liegt in docs/contrib/227-pr-body.md.
  </what-built>
  <how-to-verify>
    1. Diff pruefen: im Fork-Verzeichnis "git diff main...fix/stateless-http-session-compat" ausfuehren. Erwartung: genau eine geaenderte Datei, die Zeile mit stateless_http plus zwei Kommentarzeilen.
    2. docs/contrib/227-pr-body.md lesen: Ton, Sachlichkeit, keine Werbung fuer unser eigenes Projekt ueber den fachlichen Verweis hinaus.
    3. Bestaetigen, dass der PR unter dem GitHub-Konto street1983nk an nextcloud/context_agent gehen soll (ausgehende Kommunikation, Owner entscheidet).
  </how-to-verify>
  <acceptance_criteria>
    - Owner bestaetigt Diff und PR-Text
    - Owner gibt die Einreichung unter street1983nk explizit frei
  </acceptance_criteria>
  <action>
    Diff und PR-Text zur Pruefung vorlegen, auf die Freigabe warten und ohne Freigabe kein gh pr create ausfuehren. Aenderungswuensche am Text vor der Einreichung einarbeiten.
  </action>
  <done>Die Freigabe des Owners fuer die Einreichung liegt vor.</done>
  <resume-signal>Antworte "approved" oder benenne die gewuenschten Aenderungen am Text</resume-signal>
</task>

<task type="auto">
  <name>Task 3: PR einreichen und URL dokumentieren</name>
  <files>docs/contrib/227-pr-body.md</files>
  <read_first>
    - Freigabe aus Task 2
    - docs/contrib/227-pr-body.md
  </read_first>
  <action>
    Erst nach der Freigabe: "gh pr create --repo nextcloud/context_agent --head street1983nk:fix/stateless-http-session-compat --title" mit einem Conventional-Commit-artigen Titel und "--body-file" auf docs/contrib/227-pr-body.md.
    Danach "gh pr view &lt;nummer&gt; --repo nextcloud/context_agent --json url,state,title" ausfuehren und die URL samt Datum als Kopfzeile in docs/contrib/227-pr-body.md eintragen.
    Erwartungsmanagement in der SUMMARY notieren: das dortige CI faehrt eine schwere Matrix (server-versions master, stable33, stable32, stable31, llm2-App), lange Laufzeiten und flakige Jobs ohne Bezug zum Fix sind normal. Kein Nachfassen im Repo ohne Owner-Auftrag.
  </action>
  <verify>
    <automated>grep -c "https://github.com/nextcloud/context_agent/pull/" docs/contrib/227-pr-body.md</automated>
  </verify>
  <acceptance_criteria>
    - "gh pr view ... --json url,state" liefert state OPEN und eine URL unter github.com/nextcloud/context_agent/pull/
    - Die PR-URL steht mit Datum in docs/contrib/227-pr-body.md
    - Der PR referenziert Issue #227
  </acceptance_criteria>
  <done>CONTRIB-01 ist erfuellt: der PR ist eingereicht, verlinkt und dokumentiert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unser Arbeitsplatz zu fremdem Repository | Ausgehende, oeffentlich sichtbare Beitraege unter dem Konto des Owners |
| Fremder Code im lokalen Fork | Geklonter Code wird lokal bearbeitet, nicht ausgefuehrt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-88 | Information Disclosure | Interne Planung oder Secrets im PR-Text | mitigate | PR-Body wird vom Owner gelesen und freigegeben (Task 2), enthaelt nur fachliche Inhalte, keine Env-Werte, keine Roadmap |
| T-01-89 | Repudiation | Fehlender DCO-Nachweis blockiert den Merge | mitigate | git commit -s erzwingt Signed-off-by, Acceptance-Kriterium prueft den Trailer |
| T-01-90 | Tampering | Scope-Kriechen im fremden Repo | mitigate | Genau eine geaenderte Datei, Diff-Pruefung im Checkpoint, keine neuen Dateien (REUSE-Gate bleibt unberuehrt) |
| T-01-91 | Spoofing | Beitrag unter falschem Absender | mitigate | Ausschliesslich GitHub-Konto street1983nk, Freigabe des Owners vor dem Absenden, keine automatische Einreichung |
| T-01-92 | Tampering | Fremder Code wird lokal ausgefuehrt | accept | Der Fork wird nur editiert und gepusht, nicht installiert und nicht gestartet; Arbeitsverzeichnis liegt ausserhalb unseres Repos |
| T-01-93 | Elevation of Privilege | Credential-Handling, SSRF, Path Traversal, Create-only, XML-Injection | transfer | Dieser Plan aendert keinen Produktionscode unseres Servers; alle fuenf Kernkontrollen liegen in Plan 02, 03, 05, 07 und 08 |
</threat_model>

<verification>
1. Diff im Fork: genau eine Datei, eine funktionale Zeile plus Kommentar.
2. Signed-off-by vorhanden, kein Claude-Trailer.
3. PR offen, URL in docs/contrib/227-pr-body.md dokumentiert.
</verification>

<success_criteria>
Der PR an nextcloud/context_agent#227 ist mit minimalem Diff, DCO-Signatur und nachvollziehbarer Reproduktion eingereicht und die URL ist im Repo dokumentiert.
</success_criteria>

<output>
Create `.planning/phases/01-server-kern/01-13-SUMMARY.md` when done
</output>
