---
phase: 01-server-kern
plan: 14
type: execute
wave: 6
depends_on: [01-03, 01-05, 01-07, 01-08, 01-09, 01-11, 01-12]
files_modified:
  - tests/contract/test_tool_surface.py
  - tests/contract/test_no_destructive_calls.py
  - scripts/check_tool_budget.py
  - .github/workflows/ci.yml
  - README.md
  - docs/client-setup.md
autonomous: false
requirements: [TOOL-09, SRV-01, SRV-03, SRV-04, SRV-05]

must_haves:
  truths:
    - "tools/list zeigt genau die 15 kuratierten Tools mit korrekten Annotationen und ohne ueberfluessige Output-Schemas"
    - "Kein Tool kann loeschen, ueberschreiben oder Freigaben aendern, und das ist automatisiert geprueft"
    - "Das Token-Budget-Gate ist scharf gestellt (gemessen plus 15 Prozent) statt dekorativ"
    - "Kein Input-Schema enthaelt einen Nutzer-Parameter (kein Confused Deputy)"
    - "Ein Entwickler findet fuer stdio und HTTP eine Anleitung, die gegen einen echten Client verprobt ist"
  artifacts:
    - path: "tests/contract/test_no_destructive_calls.py"
      provides: "Grep-Test gegen destruktive HTTP-Methoden und Share-Aufrufe im Produktionscode"
      contains: "PROPPATCH"
    - path: "docs/client-setup.md"
      provides: "Setup fuer stdio (Claude Desktop) und HTTP mit Basic-Passthrough"
      contains: "NC_MCP_ALLOWED_HOSTS"
    - path: "scripts/check_tool_budget.py"
      provides: "Scharf gestelltes Budget-Gate mit gemessenem Wert"
      contains: "BUDGET_BYTES"
  key_links:
    - from: "tests/contract/test_tool_surface.py"
      to: "src/mcp_connector/server"
      via: "In-Memory-Client prueft die vollstaendige Registry"
      pattern: "list_tools"
    - from: ".github/workflows/ci.yml"
      to: "scripts/check_tool_budget.py"
      via: "Budget-Gate blockiert Regressionen"
      pattern: "check_tool_budget"
---

<objective>
Phasenabschluss: das Sicherheitsversprechen und die Tool-Oberflaeche automatisiert belegen, das Token-Budget scharf stellen, die Dokumentation fertigstellen und die Success Criteria der Phase gegen die laufende Docker-Nextcloud abnehmen.

Purpose: TOOL-09 ist ein Marketing-Versprechen, das ohne Test nur eine Behauptung ist. SRV-03 braucht einen gemessenen Budget-Wert, SRV-04 und SRV-05 brauchen einen Gesamtnachweis ueber alle 15 Tools.
Output: vollstaendiger Contract-Test, Destruktiv-Grep-Test, fixierter Budget-Wert, vulture-Durchlauf, finale README-Tabelle, Client-Setup-Doku, abgenommene Success Criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-server-kern/01-CONTEXT.md
@.planning/phases/01-server-kern/01-RESEARCH.md
@.planning/phases/01-server-kern/01-11-SUMMARY.md
@.planning/phases/01-server-kern/01-12-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Vollstaendiger Contract-Test und automatisierter Beweis der Schreibgrenzen</name>
  <files>tests/contract/test_tool_surface.py, tests/contract/test_no_destructive_calls.py</files>
  <read_first>
    - .planning/phases/01-server-kern/01-RESEARCH.md Abschnitte "Contract-Test: Annotationen, Namen, Schema-Diaet" (Code Example), "phase_requirements" Zeile TOOL-09, "Known Threat Patterns", "Anti-Patterns to Avoid"
    - tests/contract/test_tool_surface.py (Stand aus Plan 02)
    - src/mcp_connector/nextcloud/clients/ (alle Client-Module, um die Grep-Muster gegen echte Inhalte zu pruefen)
  </read_first>
  <behavior>
    - tools/list enthaelt genau diese 15 Namen: files_search, files_list, files_read, files_upload, calendar_list_events, calendar_create_event, notes_search, notes_read, notes_create, deck_browse, deck_create_card, contacts_search, unified_search, search, fetch
    - Die vier Create-Tools (files_upload, calendar_create_event, notes_create, deck_create_card) haben read_only_hint=False, destructive_hint=False, idempotent_hint=False
    - Alle anderen elf Tools haben read_only_hint=True
    - Alle 15 Tools haben open_world_hint=False
    - Ein output_schema existiert genau bei search und fetch
    - Kein Input-Schema enthaelt eine Property namens user, username, uid, userid oder owner
    - Jedes Tool hat eine nicht leere Description
    - Der Produktionscode enthaelt keine DELETE-, MOVE-, COPY- oder PROPPATCH-Anfrage und keinen Share-Endpoint
  </behavior>
  <action>
    tests/contract/test_tool_surface.py auf den vollstaendigen Umfang erweitern: EXPECTED-Set mit den 15 Namen (Mengenvergleich, damit ein zusaetzliches Tool auffaellt), CREATE_TOOLS-Set, STRUCTURED-Set, Annotationspruefungen, output_schema-Gegenprobe, Description-Pruefung und die Confused-Deputy-Pruefung ueber alle Input-Schemas (rekursiv, auch in $defs).
    tests/contract/test_no_destructive_calls.py anlegen: liest alle .py-Dateien unter src/mcp_connector, entfernt Kommentar- und Docstring-Zeilen, und prueft, dass keine der Zeichenfolgen "DELETE", "MOVE", "COPY", "PROPPATCH", "ocs/v2.php/apps/files_sharing" und ".delete(" vorkommt. Wichtig: die Filterung von Kommentaren ist Pflicht, sonst invalidiert eine erklaerende Kommentarzeile den Test (Grep-Gate-Hygiene). Der Test nennt bei Verstoss Datei und Zeile.
    Zusaetzlich einen Statelessness-Nachweis: pruefen, dass im Produktionscode kein Modul-globaler veraenderlicher Zustand fuer Sessions existiert (erlaubt bleiben der Event-Loop-Client-Pool und der Capabilities-Cache mit TTL, beide sind explizit als Latenz-Optimierung dokumentiert und werden namentlich als erlaubte Ausnahmen gelistet), und dass kein Tool ctx.elicit oder Resolve nutzt.
  </action>
  <verify>
    <automated>uv run pytest tests/contract -q && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - "uv run pytest tests/contract -q" endet mit Exit-Code 0
    - Der Mengenvergleich schlaegt nachweisbar fehl, wenn ein 16. Tool registriert wird (Gegenprobe im Test-Kommentar dokumentiert)
    - tests/contract/test_no_destructive_calls.py filtert Kommentare, bevor es zaehlt, und ist gruen
    - Die Confused-Deputy-Pruefung deckt alle 15 Input-Schemas ab
    - Kein Tool nutzt ctx.elicit oder Resolve
  </acceptance_criteria>
  <done>Das Sicherheitsversprechen und die kuratierte Oberflaeche sind automatisiert belegt, nicht behauptet.</done>
</task>

<task type="auto">
  <name>Task 2: Budget scharf stellen, Totcode entfernen, Dokumentation fertigstellen</name>
  <files>scripts/check_tool_budget.py, .github/workflows/ci.yml, README.md, docs/client-setup.md</files>
  <read_first>
    - .planning/phases/01-server-kern/01-RESEARCH.md Abschnitte "Token-Budget-Gate", "Open Questions" Punkt 4, "Assumptions Log" A2, "Project Constraints (from CLAUDE.md)"
    - README.md (Stand aus Plan 12), scripts/check_tool_budget.py (Stand aus Plan 01)
  </read_first>
  <action>
    Budget messen: "uv run python scripts/check_tool_budget.py" ausfuehren, die gemeldete Byte-Groesse notieren und BUDGET_BYTES auf gemessen plus 15 Prozent (aufgerundet auf 500 Bytes) setzen. Den gemessenen Ausgangswert samt Datum als Kommentar im Skript festhalten, damit spaetere Regressionen zuordenbar bleiben. Die Top-5-Ausgabe beibehalten.
    Version pruefen: "uv lock --upgrade-package mcp --dry-run" ausfuehren und in der SUMMARY notieren, ob eine neuere mcp-2.x-Version vorliegt (die Research ist bis 2026-08-28 fuer mcp gueltig). Kein Upgrade ohne gruene Suite.
    Totcode: "uv run --with vulture vulture src" ausfuehren, echte Funde entfernen, unvermeidbare Treffer in einer Whitelist-Datei begruenden.
    .github/workflows/ci.yml ergaenzen: ruff check und ruff format --check ueber das GANZE Repo (globale Regel), pytest tests/unit tests/contract, das Budget-Gate und vulture als eigener Schritt.
    README.md finalisieren: die Tool-Tabelle gegen die tatsaechliche Registry abgleichen (Namen, Permission-Level), einen Abschnitt "Known limitations" mit den ehrlichen Grenzen (Suche matcht Namen, nicht Inhalte; ein Konto ohne Kalender braucht occ dav:create-calendar; kein Loeschen und kein Ueberschreiben; Notes und Deck muessen installiert sein), sowie einen Hinweis auf docs/client-setup.md.
    docs/client-setup.md anlegen (englisch): stdio-Setup fuer Claude Desktop und Claude Code (Kommando nc-mcp, Env-Block mit NC_MCP_URL, NC_MCP_USER, NC_MCP_APP_PASSWORD, Hinweis auf App-Passwort statt Login-Passwort), HTTP-Setup (uvicorn-Kommando, NC_MCP_ALLOWED_HOSTS mit zwei Eintraegen pro Hostname, Basic-Passthrough per Authorization-Header, optionaler NC_MCP_STATIC_BEARER), die bekannten Stolperstellen (421 Misdirected Request bei fehlender Host-Allowlist, "Session terminated" bei stateless Transport, 429 durch Brute-Force-Schutz nach falschem App-Passwort). Die vollstaendige Client-Matrix inklusive ChatGPT, Cursor, Open WebUI und MUCGPT ist Phase 5 (EXAPP-05), hier nur die in Phase 1 verprobten Wege.
  </action>
  <verify>
    <automated>uv run python scripts/check_tool_budget.py && uv run ruff check . && uv run ruff format --check . && uv run pytest -q && python -c "import sys,io; t=io.open('README.md',encoding='utf-8').read()+io.open('docs/client-setup.md',encoding='utf-8').read(); sys.exit(1 if (chr(8212) in t or chr(8211) in t) else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - BUDGET_BYTES entspricht dem gemessenen Wert plus 15 Prozent und der Messwert steht mit Datum als Kommentar im Skript
    - "uv run python scripts/check_tool_budget.py" endet mit Exit-Code 0
    - "uv run --with vulture vulture src" meldet keine unbegruendeten Funde
    - .github/workflows/ci.yml enthaelt ruff, pytest, Budget-Gate und vulture
    - README.md-Tabelle stimmt mit den 15 registrierten Tool-Namen ueberein (automatisiert im Contract-Test aus Task 1 oder per Skript geprueft)
    - docs/client-setup.md existiert, englisch, ohne Em-Dashes, mit den drei Stolperstellen
  </acceptance_criteria>
  <done>Das Budget-Gate ist scharf, der Code ist aufgeraeumt, und die Dokumentation deckt die in dieser Phase verprobten Clients ab.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Abnahme der Phase-1-Success-Criteria gegen die echte Nextcloud</name>
  <read_first>
    - .planning/ROADMAP.md Abschnitt "Phase 1: Server-Kern" (die fuenf Success Criteria)
    - docs/client-setup.md
  </read_first>
  <what-built>
    15 Tools, stdio- und HTTP-Transport, Client-Matrix und Restart-Beweis, Create-only-Schreibpfade, Graceful Degradation, scharfes Budget-Gate, README mit Permission-Tabelle, App-ID-Freeze und der eingereichte context_agent-PR.
  </what-built>
  <how-to-verify>
    1. Docker Desktop starten, dann: docker compose -f compose.test.yml up -d --wait und bash scripts/bootstrap_test_nc.sh
    2. set -a && . ./.env.test && set +a
    3. uv run pytest -q (Unit und Contract), uv run pytest -m integration -q, uv run pytest -m matrix -q. Alle drei muessen gruen sein.
    4. Kriterium 1: nc-mcp per stdio in einem echten Client (Claude Desktop oder Claude Code) eintragen nach docs/client-setup.md und je einmal aufrufen: files_search, files_list, files_read, files_upload, calendar_list_events, calendar_create_event, notes_search, notes_read, notes_create, deck_browse, deck_create_card, contacts_search, unified_search, search, fetch.
    5. Kriterium 2: uvicorn starten, mit dem Client per HTTP verbinden, waehrend der Sitzung den Serverprozess neu starten und danach erneut ein Tool aufrufen.
    6. Kriterium 3: im Client die Tool-Liste ansehen (15 Tools, sinnvolle Beschreibungen) und die Permission-Tabelle im README dagegen halten.
    7. Kriterium 4: Notes-App per occ deaktivieren (occ app:disable notes), notes_search aufrufen, den Fehlertext lesen, App wieder aktivieren.
    8. Kriterium 5: docs/app-id-freeze.md und die PR-URL in docs/contrib/227-pr-body.md pruefen.
    9. Ergebnis melden: welche Kriterien erfuellt sind und was offen bleibt.
  </how-to-verify>
  <acceptance_criteria>
    - Alle drei Testlaeufe (Default, integration, matrix) enden mit Exit-Code 0
    - Alle 15 Tools wurden mindestens einmal aus einem echten Client aufgerufen und lieferten sinnvolle Antworten
    - Der Restart-Test im echten Client fuehrte zu keinem Verbindungsabbruch
    - Der Aufruf gegen die deaktivierte Notes-App lieferte den erklaerenden Fehlertext statt eines Crashes
    - Owner bestaetigt die fuenf Success Criteria der Phase oder benennt die offenen Punkte
  </acceptance_criteria>
  <action>
    Den Abnahmeablauf an den Owner geben, das Ergebnis je Success Criterion einsammeln und in der SUMMARY dokumentieren. Offene Punkte werden als Gaps fuer /gsd:plan-phase --gaps notiert.
  </action>
  <done>Die fuenf Success Criteria der Phase sind abgenommen oder die offenen Punkte sind benannt.</done>
  <resume-signal>Antworte "approved" oder liste die nicht erfuellten Kriterien mit Beobachtung</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gesamtsystem zu echtem MCP-Client | Erstmalige Nutzung durch einen fremden Host mit echten Credentials |
| Produktionscode zu Sicherheitsversprechen | Der Code muss halten, was README und Annotationen behaupten |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-94 | Tampering | Ein spaeterer Commit fuehrt eine destruktive Operation ein | mitigate | tests/contract/test_no_destructive_calls.py im CI, mit Kommentarfilterung, nennt Datei und Zeile |
| T-01-95 | Elevation of Privilege | Confused Deputy ueber einen neu eingefuehrten Nutzer-Parameter | mitigate | Contract-Test prueft rekursiv alle 15 Input-Schemas auf user, username, uid, userid, owner |
| T-01-96 | Information Disclosure | Credentials in Logs oder in der Doku | mitigate | docs/client-setup.md nennt nur Variablennamen, README enthaelt keine Werte; geerbte Maskierung aus Plan 02 wird im Abnahmelauf beobachtet |
| T-01-97 | Denial of Service | Token-Budget-Regression durch neue Tools oder lange Beschreibungen | mitigate | Budget-Gate scharf gestellt (gemessen plus 15 Prozent) und im CI blockierend |
| T-01-98 | Spoofing | SSRF | accept | Kein URL-holendes Tool in Phase 1; als Nicht-Ziel im README und in der SUMMARY dokumentiert |
| T-01-99 | Tampering | Create-only-Schreibschutz haelt in der Praxis nicht | mitigate | Abnahmelauf enthaelt den Integrationstest mit dem 412-Konflikt (aus Plan 03) sowie den Aufruf aus einem echten Client |
| T-01-100 | Tampering | Path Traversal und XML-Injection | mitigate | Kontrollen aus Plan 02, 05, 07 und 08 werden im Gesamtlauf mitgetestet (Unit- und Contract-Suite laufen komplett) |
| T-01-SC | Tampering | Abhaengigkeits-Upgrade kurz vor Phasenabschluss | mitigate | "uv lock --upgrade-package mcp --dry-run" nur pruefen und dokumentieren, kein Upgrade ohne gruene Suite; uv.lock bleibt committet, CI nutzt --frozen |
</threat_model>

<verification>
1. Unit-, Contract-, Integration- und Matrix-Suite gruen.
2. Budget-Gate mit gemessenem Wert gruen, vulture ohne unbegruendete Funde.
3. Destruktiv-Grep-Test und Confused-Deputy-Test gruen.
4. Abnahme der fuenf Success Criteria durch den Owner.
</verification>

<success_criteria>
Alle fuenf Success Criteria der Phase sind belegt: 15 Tools per stdio nutzbar, HTTP-Transport mit Client-Matrix und Restart-Beweis, korrekte Annotationen mit scharfem Budget-Gate und dokumentiertem Permission-Level, klarer Fehlertext bei fehlender App, App-ID eingefroren und der context_agent-PR eingereicht.
</success_criteria>

<output>
Create `.planning/phases/01-server-kern/01-14-SUMMARY.md` when done
</output>
