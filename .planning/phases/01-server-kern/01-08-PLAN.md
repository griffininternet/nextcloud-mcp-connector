---
phase: 01-server-kern
plan: 08
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - src/mcp_connector/nextcloud/clients/carddav.py
  - src/mcp_connector/tools/contacts.py
  - src/mcp_connector/server/reg_contacts.py
  - tests/fixtures/carddav_addressbooks_207.xml
  - tests/fixtures/carddav_report_207.xml
  - tests/unit/test_carddav_client.py
  - tests/unit/test_contacts_tools.py
  - tests/integration/test_contacts_search.py
autonomous: true
requirements: [TOOL-05]

must_haves:
  truths:
    - "Ein Nutzer kann Kontakte nach Name und Mailadresse suchen und bekommt Name, Mails, Telefonnummern und Organisation"
    - "Kontakte ohne die abgefragten Felder fuehren nie zu einem Crash"
    - "Hat das Konto kein Adressbuch, sagt das Tool das mit einem Loesungshinweis"
    - "Das Tool ist ausschliesslich lesend, es gibt keinen CardDAV-Schreibpfad in dieser Phase"
    - "D-07: contacts_search ist nutzbar und ausschliesslich lesend"
    - "D-17: vCard-Parsing laeuft ueber vobject, CardDAV-XML ueber lxml"
  artifacts:
    - path: "src/mcp_connector/nextcloud/clients/carddav.py"
      provides: "Adressbuch-Discovery und addressbook-query REPORT"
      contains: "addressbook-query"
    - path: "src/mcp_connector/tools/contacts.py"
      provides: "search() mit stabilen Antwortfeldern"
      contains: "full_name"
  key_links:
    - from: "src/mcp_connector/nextcloud/clients/carddav.py"
      to: "REPORT /remote.php/dav/addressbooks/users/<uid>/<addressbookUri>/"
      via: "c:prop-filter mit c:text-match und c:limit"
      pattern: "prop-filter"
    - from: "src/mcp_connector/tools/contacts.py"
      to: "vobject"
      via: "VCF wird geparst, nie per Regex"
      pattern: "vobject"
---

<objective>
Die Kontakte-Vertikale liefern: contacts_search als lesendes Tool ueber CardDAV mit serverseitigem Filter und Limit.

Purpose: Kontakte sind der letzte fehlende Lesekanal fuer den ChatGPT-Profil-Plan und fuer prepare_context in Phase 4. Der Pfad unterscheidet sich bewusst vom Kalender (users/-Segment), das ist eine typische Fehlerquelle.
Output: carddav-Client, contacts_search, Fixtures, Unit- und Integrationstests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-server-kern/01-CONTEXT.md
@.planning/phases/01-server-kern/01-RESEARCH.md
@.planning/phases/01-server-kern/01-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CardDAV-Client mit Discovery und addressbook-query</name>
  <files>src/mcp_connector/nextcloud/clients/carddav.py, tests/fixtures/carddav_addressbooks_207.xml, tests/fixtures/carddav_report_207.xml, tests/unit/test_carddav_client.py</files>
  <read_first>
    - .planning/phases/01-server-kern/01-RESEARCH.md Abschnitt "contacts_search (CardDAV REPORT addressbook-query)", "Pitfall 3", "Don't Hand-Roll"
    - src/mcp_connector/nextcloud/clients/xml.py, http.py, errors.py (Stand aus Plan 02)
    - src/mcp_connector/nextcloud/clients/caldav.py, falls aus Plan 07 bereits vorhanden (nur als Stilvorlage lesen, nicht aendern)
  </read_first>
  <behavior>
    - Discovery per PROPFIND auf addressbooks/users/&lt;uid&gt;/ mit Depth 1 (das users/-Segment existiert hier, anders als bei Kalendern)
    - Findet die Discovery kein Adressbuch, wird ein Fehler mit dem Hinweis auf occ dav:create-addressbook erzeugt
    - build_addressbook_query(term, limit) erzeugt genau ein c:filter mit test="anyof", je ein c:prop-filter fuer FN und EMAIL mit c:text-match match-type="contains" und collation="i;unicode-casemap", plus c:limit mit c:nresults
    - Ein Suchbegriff mit & und &lt; wird korrekt escapt
    - Das Parsen der 207-Fixture liefert pro Kontakt full_name, emails, phones, organization, addressbook, uid
    - Ein vCard ohne EMAIL, ohne TEL und ohne ORG wird ohne Exception verarbeitet
    - 404 auf ein unbekanntes Adressbuch und 5xx liefern die projektweit einheitlichen Fehlertexte
  </behavior>
  <action>
    carddav.py anlegen: discover_addressbooks(client, creds) per PROPFIND Depth 1 auf "&lt;base&gt;/remote.php/dav/addressbooks/users/&lt;user&gt;/" (Properties d:displayname, d:resourcetype), build_addressbook_query(term, limit) per lxml nach dem verifizierten Schema (nur EIN c:filter-Element ist erlaubt), query_contacts(client, creds, addressbook_uri, term, limit) per REPORT mit Depth 1 und Content-Type application/xml, sowie parse_contacts(multistatus) mit vobject fuer die c:address-data.
    Jeder Property-Zugriff defensiv (fehlende Felder sind der Normalfall), Multi-Value-Felder als Liste ausgeben.
    Fixtures aus den Beispielen in 01-RESEARCH.md ableiten: Adressbuch-Discovery mit zwei Adressbuechern, REPORT-Antwort mit drei Kontakten, davon einer minimal (nur FN) und einer mit mehreren Mails und Telefonnummern.
    tests/unit/test_carddav_client.py: alle Faelle aus dem behavior-Block.
  </action>
  <verify>
    <automated>uv run pytest tests/unit -q && uv run ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - "uv run pytest tests/unit/test_carddav_client.py -q" endet mit Exit-Code 0 und deckt mindestens 7 Faelle ab
    - Der Discovery-Pfad enthaelt addressbooks/users/ (Test prueft die URL)
    - Der Query-Body enthaelt genau ein c:filter mit test="anyof" und ein c:limit mit c:nresults
    - Ein Test belegt korrektes Escaping fuer einen Suchbegriff mit & und &lt;
    - Ein minimaler Kontakt (nur FN) wird ohne Exception geparst
    - Das Modul enthaelt keine der Methoden PUT, DELETE, MOVE oder PROPPATCH
  </acceptance_criteria>
  <done>Adressbuch-Discovery und serverseitig gefilterte Kontaktsuche sind korrekt gebaut und getestet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tool contacts_search</name>
  <files>src/mcp_connector/tools/contacts.py, src/mcp_connector/server/reg_contacts.py, tests/unit/test_contacts_tools.py, tests/integration/test_contacts_search.py</files>
  <read_first>
    - .planning/phases/01-server-kern/01-RESEARCH.md Abschnitte "contacts_search", "Schema-Diaet", "Tool-Registrierung mit Annotationen und Schema-Diaet"
    - src/mcp_connector/nextcloud/clients/carddav.py (aus Task 1), src/mcp_connector/server/__init__.py
  </read_first>
  <behavior>
    - contacts_search("meier") fragt alle Adressbuecher des Nutzers parallel ab und fuehrt die Treffer zusammen
    - Ein leerer Suchbegriff wird mit Hinweis abgelehnt, ohne Request
    - Ein Limit ueber dem Maximum wird auf das Maximum reduziert
    - Faellt ein Adressbuch aus, erscheint es im degraded-Feld und die uebrigen Treffer kommen trotzdem
    - Null Treffer liefern contacts: [] plus den abgefragten Begriff, kein Fehler
    - Die Antwort enthaelt keine Rohdaten der vCard, nur die stabilen Felder
  </behavior>
  <action>
    tools/contacts.py anlegen: async def search(clients, query, limit=25). Discovery, Fan-out per asyncio.gather(..., return_exceptions=True) mit hartem Timeout pro Adressbuch, degraded-Feld fuer Ausfaelle, kompakte Antwort mit full_name, emails, phones, organization, addressbook, uid.
    server/reg_contacts.py anlegen: contacts_search mit READ_ONLY, structured_output=False, @graceful. Docstring einzeilig: "Search the user's contacts by name or email address."
    tests/unit/test_contacts_tools.py mit respx: alle Faelle aus dem behavior-Block.
    tests/integration/test_contacts_search.py mit @pytest.mark.integration: der Test legt keinen Kontakt an (kein Schreibpfad in dieser Phase), sondern prueft gegen das per Bootstrap erzeugte Adressbuch, dass ein Suchbegriff ohne Treffer eine leere Liste liefert und dass der Aufruf ohne Adressbuch den erklaerenden Fehler erzeugt (bob hat kein Adressbuch, alice hat eines).
  </action>
  <verify>
    <automated>uv run pytest tests/unit -q && uv run pytest tests/contract -q && uv run python scripts/check_tool_budget.py</automated>
  </verify>
  <acceptance_criteria>
    - "uv run pytest tests/unit/test_contacts_tools.py -q" endet mit Exit-Code 0 und deckt mindestens 6 Faelle ab
    - tools/list enthaelt contacts_search mit read_only_hint=True, open_world_hint=False und ohne output_schema
    - Ein Test belegt das degraded-Feld bei einem ausgefallenen Adressbuch
    - "uv run python scripts/check_tool_budget.py" endet mit Exit-Code 0
    - "uv run pytest -m integration -q --collect-only" sammelt die Kontakte-Integrationstests fehlerfrei
  </acceptance_criteria>
  <done>Kontakte suchen funktioniert lesend, parallel und mit ehrlicher Teil-Degradierung.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Suchbegriff zu CardDAV-XML | Modellgenerierter Text wird Teil eines REPORT-Bodys |
| vCard-Inhalt zu Antwort | Fremde Kontaktdaten werden geparst und ausgegeben |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-53 | Tampering | Injection in CardDAV-XML ueber den Suchbegriff | mitigate | Body per lxml gebaut, Escaping automatisch, Test mit & und &lt; |
| T-01-54 | Denial of Service | XXE und Billion Laughs in der REPORT-Antwort | mitigate | xml.hardened_parser fuer alle Antworten |
| T-01-55 | Tampering | Fehlerhaftes vCard-Parsing per Regex | mitigate | vobject statt Eigenbau, defensiver Zugriff auf jedes Feld |
| T-01-56 | Information Disclosure | Kontaktdaten fremder Nutzer | mitigate | ACL-Durchsetzung serverseitig bei Nextcloud, Credentials aus dem Auth-Kanal, kein Nutzer-Parameter im Tool |
| T-01-57 | Tampering | Create-only-Schreibschutz | mitigate | Dieser Plan enthaelt keinen CardDAV-Schreibpfad; das Modul enthaelt nachweisbar kein PUT und kein DELETE |
| T-01-58 | Denial of Service | Sehr grosse Adressbuecher | mitigate | c:limit im Request, Limit-Deckelung im Tool, hartes Timeout pro Adressbuch |
| T-01-59 | Elevation of Privilege | Path Traversal ueber die addressbook-URI | mitigate | URI wird gegen die Discovery-Liste validiert, quote() beim Pfadbau |
| T-01-60 | Spoofing | SSRF | accept | Kein URL-Parameter, Basis-URL aus Env |
</threat_model>

<verification>
1. "uv run pytest -q" gruen, Budget-Gate gruen.
2. Escaping-Test und Minimal-vCard-Test belegt.
3. Grep-Gegenprobe: kein Schreibpfad im carddav-Modul.
</verification>

<success_criteria>
Kontakte lassen sich berechtigungstreu nach Name und Mailadresse durchsuchen, ohne dass ein Schreibpfad existiert.
</success_criteria>

<output>
Create `.planning/phases/01-server-kern/01-08-SUMMARY.md` when done
</output>
