---
phase: 01-server-kern
plan: 09
type: execute
wave: 4
depends_on: [01-06]
files_modified:
  - src/mcp_connector/nextcloud/clients/deck.py
  - src/mcp_connector/tools/deck.py
  - src/mcp_connector/server/reg_deck.py
  - tests/fixtures/deck_boards.json
  - tests/fixtures/deck_stacks.json
  - tests/unit/test_deck_client.py
  - tests/unit/test_deck_tools.py
  - tests/integration/test_deck_roundtrip.py
autonomous: true
requirements: [TOOL-04, SRV-04]

must_haves:
  truths:
    - "Ein Nutzer kann Deck-Boards, Stacks und Karten mit einem Tool und einem level-Parameter lesen"
    - "Ein Nutzer kann eine neue Karte in einem Stack anlegen"
    - "Ist die Deck-App nicht installiert oder fehlen Board-Rechte, erklaert das Tool das vorab"
    - "Karten einer Ebene kommen mit einem einzigen Request, ohne N+1-Aufrufe"
  artifacts:
    - path: "src/mcp_connector/nextcloud/clients/deck.py"
      provides: "Deck-REST v1.0 mit Pflichtheadern und eigenem Fehlerformat"
      contains: "apps/deck/api/v1.0"
    - path: "src/mcp_connector/tools/deck.py"
      provides: "browse() mit Ebenen-Parameter und create_card()"
      contains: "level"
  key_links:
    - from: "src/mcp_connector/tools/deck.py"
      to: "GET /boards/{boardId}/stacks"
      via: "Stacks-Antwort enthaelt die Karten, deshalb ein Request pro Board"
      pattern: "stacks"
    - from: "src/mcp_connector/tools/deck.py"
      to: "src/mcp_connector/nextcloud/capabilities.py"
      via: "deck_available und canCreateBoards werden vorab geprueft"
      pattern: "require_app|can_create_boards"
---

<objective>
Die Deck-Vertikale liefern: deck_browse als ein Tool mit Ebenen-Parameter (Boards, Stacks, Karten) und deck_create_card als Create-only-Schreibpfad.

Purpose: Deck ist die zweite JSON-Vertikale und Voraussetzung fuer die Karten-Aufloesung im ChatGPT-Profil. Ein Tool statt drei schuetzt Token-Budget und Client-Slots (D-06).
Output: deck-Client mit eigenem Fehlerformat, zwei Tools, Fixtures, Unit- und Integrationstests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-server-kern/01-CONTEXT.md
@.planning/phases/01-server-kern/01-RESEARCH.md
@.planning/phases/01-server-kern/01-06-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deck-Client mit Pflichtheadern und eigenem Fehlerformat</name>
  <files>src/mcp_connector/nextcloud/clients/deck.py, tests/fixtures/deck_boards.json, tests/fixtures/deck_stacks.json, tests/unit/test_deck_client.py</files>
  <read_first>
    - .planning/phases/01-server-kern/01-RESEARCH.md Abschnitte "deck_browse / deck_create_card (Deck REST v1.0)", "Pitfall 9", "Graceful Degradation"
    - src/mcp_connector/nextcloud/clients/ocs.py (parse_app_json aus Plan 06), errors.py
  </read_first>
  <behavior>
    - Jeder Deck-Request (auch GET) traegt "OCS-APIRequest: true" und "Content-Type: application/json"
    - Ein Fehler kommt als {"status": 400, "message": "..."} und wird per parse_app_json gelesen, nie per OCS-Envelope-Parser
    - get_boards liefert id, title, permissions aus der Fixture
    - get_stacks(board_id) liefert Stacks samt enthaltenen Karten aus einem Request
    - create_card(board_id, stack_id, payload) sendet POST auf /boards/{b}/stacks/{s}/cards mit type "plain"
    - Ein Titel ueber 255 Zeichen fuehrt zu einem Fehler mit Laengenhinweis, bevor der Request rausgeht
    - 403 "Permission denied" wird zu einem Rechte-Hinweis, 404 zu unbekanntem Board oder Stack
    - Eine HTML-Antwort (Loginseite) fuehrt zu einer erklaerenden Meldung, nicht zu einem KeyError
  </behavior>
  <action>
    clients/deck.py anlegen: Basis "&lt;base&gt;/index.php/apps/deck/api/v1.0". API-Version bewusst 1.0 (breiter kompatibel als 1.1, unsere Operationen brauchen 1.1 nicht). Funktionen get_boards(details=False), get_board(board_id), get_stacks(board_id), get_card(board_id, stack_id, card_id), create_card(board_id, stack_id, title, description=None, duedate=None, order=999). Titel-Laenge 255 lokal pruefen. Datumsformat ISO-8601. Fehlerbehandlung ueber parse_app_json, plus 401 ohne Retry und 429 mit Wartehinweis.
    Fixtures aus den Strukturen in 01-RESEARCH.md ableiten: deck_boards.json mit zwei Boards, deck_stacks.json mit zwei Stacks und drei Karten (eine ohne description, eine mit duedate).
    tests/unit/test_deck_client.py: alle Faelle aus dem behavior-Block, inklusive Header-Assertion fuer GET.
  </action>
  <verify>
    <automated>uv run pytest tests/unit -q && uv run ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - "uv run pytest tests/unit/test_deck_client.py -q" endet mit Exit-Code 0 und deckt mindestens 8 Faelle ab
    - Ein Test belegt beide Pflichtheader auf einem GET-Request
    - Der Titel-Laengencheck verhindert den Request (respx zaehlt 0 Calls)
    - Das Modul nutzt parse_app_json und nicht parse_ocs
    - Das Modul enthaelt keine DELETE- oder Update-Operationen
  </acceptance_criteria>
  <done>Deck-Requests sind korrekt beheadert, Fehler werden im richtigen Format gelesen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tools deck_browse und deck_create_card</name>
  <files>src/mcp_connector/tools/deck.py, src/mcp_connector/server/reg_deck.py, tests/unit/test_deck_tools.py, tests/integration/test_deck_roundtrip.py</files>
  <read_first>
    - .planning/phases/01-server-kern/01-RESEARCH.md Abschnitte "deck_browse / deck_create_card", "Graceful Degradation", "Anti-Patterns to Avoid" (ein Tool pro Deck-Ebene ist verboten), "Pattern 5: ID-Codec"
    - src/mcp_connector/nextcloud/clients/deck.py (aus Task 1), src/mcp_connector/nextcloud/capabilities.py, src/mcp_connector/ids.py
  </read_first>
  <behavior>
    - browse(level="boards") liefert Boards mit id und title
    - browse(level="stacks", board_id=X) liefert Stacks ohne Kartenlisten-Rauschen (nur Anzahl pro Stack)
    - browse(level="cards", board_id=X) liefert Karten mit id im Format card:&lt;boardId&gt;:&lt;stackId&gt;:&lt;cardId&gt;, title, stack, duedate, und braucht genau einen HTTP-Request
    - level="stacks" oder "cards" ohne board_id liefert einen Fehler, der den fehlenden Parameter nennt
    - Ein unbekannter level-Wert wird bereits vom Schema abgelehnt (Literal-Typ)
    - Ist Deck nicht installiert, liefern beide Tools den AppMissingError-Text ohne Deck-Request
    - Ist canCreateBoards false, erklaert deck_create_card das vorab, statt einen 403 zu provozieren
    - create_card liefert die id der neuen Karte im kanonischen langen Format
  </behavior>
  <action>
    tools/deck.py anlegen: async def browse(clients, level="boards", board_id=None, stack_id=None, limit=50) und async def create_card(clients, board_id, stack_id, title, description=None, duedate=None). Beide rufen zuerst capabilities.require_app("deck"). Bei level="cards" genau ein Request auf /boards/{id}/stacks, weil die Karten mitkommen (kein N+1). IDs ueber ids.encode_card bilden.
    server/reg_deck.py anlegen: deck_browse mit READ_ONLY, deck_create_card mit CREATE_ONLY, beide structured_output=False und @graceful. level als Literal["boards", "stacks", "cards"] typisieren (Enum im Schema, keine freie Zeichenkette). Docstrings einzeilig, englisch.
    tests/unit/test_deck_tools.py mit respx: alle Faelle aus dem behavior-Block, inklusive Zaehlung der HTTP-Calls bei level="cards".
    tests/integration/test_deck_roundtrip.py mit @pytest.mark.integration: ein Board per occ oder API-Vorbedingung vorausgesetzt (Bootstrap installiert Deck, der Test legt bei Bedarf ueber die API ein Board an oder ueberspringt sich mit klarer Meldung, wenn kein Board existiert), dann Karte anlegen und per browse(level="cards") wiederfinden.
  </action>
  <verify>
    <automated>uv run pytest tests/unit -q && uv run pytest tests/contract -q && uv run python scripts/check_tool_budget.py</automated>
  </verify>
  <acceptance_criteria>
    - "uv run pytest tests/unit/test_deck_tools.py -q" endet mit Exit-Code 0 und deckt mindestens 8 Faelle ab
    - Ein Test belegt genau einen HTTP-Request fuer level="cards"
    - tools/list enthaelt deck_browse (read_only_hint=True) und deck_create_card (read_only_hint=False, destructive_hint=False, idempotent_hint=False)
    - Das Input-Schema von deck_browse enthaelt eine Enum-Beschraenkung fuer level
    - Kein Deck-Request bei fehlender App (respx zaehlt 0 Calls)
    - "uv run python scripts/check_tool_budget.py" endet mit Exit-Code 0
  </acceptance_criteria>
  <done>Deck lesen und Karten anlegen funktioniert mit zwei Tools, ohne N+1 und mit vorab erklaerten Rechtegrenzen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tool-Parameter zu Deck-REST | Modellgenerierte Titel und Beschreibungen werden geschrieben |
| Deck-Antwort zu ID-Bildung | Board-, Stack- und Karten-IDs werden zu internen IDs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-61 | Information Disclosure | Credentials in Deck-Fehlermeldungen | mitigate | Nur message und hint, geerbte Maskierung aus Plan 02, kein Logging von Headern |
| T-01-62 | Tampering | Create-only-Verhalten bei deck_create_card | mitigate | Ausschliesslich POST auf cards, kein PUT, kein DELETE, keine Board- oder Stack-Aenderung; Annotationen sind dadurch ehrlich |
| T-01-63 | Elevation of Privilege | Fremde Board- oder Stack-IDs erraten | mitigate | Deck erzwingt die Rechte serverseitig, 403 wird als Rechte-Hinweis gemeldet, canCreateBoards wird vorab geprueft; kein Nutzer-Parameter im Tool |
| T-01-64 | Denial of Service | Grosse Boards mit vielen Karten | mitigate | limit-Parameter im Tool, ein Request pro Board, kompakte Antwortfelder |
| T-01-65 | Spoofing | SSRF | accept | Kein URL-Parameter, Basis-URL aus Env |
| T-01-66 | Tampering | Path Traversal und XML-Injection | transfer | Kein Dateipfad, kein XML in diesem Plan; Kontrollen bleiben in Plan 02 und 05 |
| T-01-67 | Tampering | Ueberlanger Titel provoziert Serverfehler | mitigate | Lokale 255-Zeichen-Pruefung vor dem Request |
</threat_model>

<verification>
1. "uv run pytest -q" gruen, Budget-Gate gruen.
2. Ein-Request-Nachweis fuer level="cards".
3. Contract-Test zeigt beide Deck-Tools mit korrekten Annotationen.
</verification>

<success_criteria>
Deck-Boards, Stacks und Karten sind mit einem Tool lesbar und neue Karten koennen angelegt werden, mit klaren Meldungen bei fehlender App oder fehlenden Rechten.
</success_criteria>

<output>
Create `.planning/phases/01-server-kern/01-09-SUMMARY.md` when done
</output>
