---
phase: 01-server-kern
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - .gitattributes
  - .gitignore
  - .github/workflows/ci.yml
  - src/mcp_connector/__init__.py
  - scripts/check_tool_budget.py
  - tests/conftest.py
  - tests/contract/test_tool_surface.py
  - tests/unit/test_project_layout.py
  - docs/dependency-audit.md
autonomous: false
requirements: [SRV-03]

must_haves:
  truths:
    - "Ein Entwickler kann uv sync ausfuehren und bekommt eine reproduzierbare, auditierte Umgebung"
    - "Der Contract-Test schlaegt rot fehl und beschreibt genau die fehlende Faehigkeit files_read"
    - "Das Token-Budget-Gate existiert als ausfuehrbares Skript und laeuft in CI"
    - "httpx2 ist nachweislich nur transitive Abhaengigkeit, nicht direkt gepinnt"
    - "D-02: PyPI-Name nextcloud-mcp-connector und CLI-Entry-Point nc-mcp sind in pyproject.toml verankert"
    - "D-14: Token-Budget-Check der tools/list-Antwort existiert als CI-Gate"
    - "D-19: Dependency-Pin ist mcp>=2.0,<3, der Fallback-Pin >=1.29,<2 ist dokumentiert"
    - "D-21: Test-Harness aus uv, pytest, respx und In-Memory-Client mit Markern integration und matrix"
  artifacts:
    - path: "pyproject.toml"
      provides: "Dependency-Pins, Console-Script nc-mcp, pytest-Marker, ruff-Konfiguration"
      contains: "mcp[cli]>=2.0,<3"
    - path: "tests/conftest.py"
      provides: "anyio_backend, Env-Fixtures, Integration-Skip-Guard"
      contains: "anyio_backend"
    - path: "tests/contract/test_tool_surface.py"
      provides: "RED-Test der Tool-Oberflaeche"
      contains: "files_read"
    - path: "scripts/check_tool_budget.py"
      provides: "CI-Gate fuer die Groesse der tools/list-Antwort"
      contains: "BUDGET_BYTES"
    - path: ".github/workflows/ci.yml"
      provides: "Lint-, Unit- und Budget-Job"
      contains: "ruff format --check"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/check_tool_budget.py"
      via: "uv run python scripts/check_tool_budget.py"
      pattern: "check_tool_budget"
    - from: "pyproject.toml"
      to: "mcp_connector.entry_stdio:main"
      via: "project.scripts nc-mcp"
      pattern: "nc-mcp"
---

<objective>
Projekt-Skelett, Toolchain und Test-Harness fuer den MCP-Server aufsetzen, inklusive des rot fehlschlagenden Contract-Tests, der die erste Faehigkeit (files_read) beschreibt.

Purpose: Ohne auditierte Dependency-Basis und ohne messbares Tool-Budget lassen sich SRV-03 und die Supply-Chain-Anforderung spaeter nicht belegen. Der RED-Test definiert das Ziel des Walking Skeleton (Plan 02) vorab.
Output: pyproject.toml mit exakten Pins, uv.lock, CI-Workflow, Test-Harness, RED-Contract-Test, Token-Budget-Gate, Dependency-Audit-Notiz.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-server-kern/01-CONTEXT.md
@.planning/phases/01-server-kern/01-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package-Legitimacy-Gate vor der ersten Installation</name>
  <read_first>
    - .planning/phases/01-server-kern/01-RESEARCH.md Abschnitt "Package Legitimacy Audit"
  </read_first>
  <what-built>
    Noch kein Code. Die Audit-Tabelle aus 01-RESEARCH.md liegt vor: 11 Pakete [OK], ein Paket [SUS] (httpx2, transitive Abhaengigkeit von mcp 2.0.0). slopcheck 0.6.1 lief am 2026-08-14.
  </what-built>
  <how-to-verify>
    1. Oeffne https://pypi.org/project/httpx2/ und pruefe: Author-Mail Tom Christie, Source github.com/pydantic/httpx2, Development Status 5 Production/Stable.
    2. Oeffne https://pypi.org/project/mcp/ und pruefe: Version 2.0.0, Source github.com/modelcontextprotocol/python-sdk.
    3. Bestaetige, dass die geplante pyproject.toml httpx2 NICHT als direkte Dependency listet (Dependencies laut Plan: mcp[cli]>=2.0,&lt;3, httpx>=0.28,&lt;0.29, lxml>=6.1,&lt;7, icalendar>=7.2,&lt;8, vobject>=0.9.9,&lt;1, pydantic>=2.12,&lt;3; dev: pytest, respx, ruff).
    4. Freigabe fuer die erste Installation via uv erteilen.
  </how-to-verify>
  <acceptance_criteria>
    - Owner bestaetigt httpx2 als legitim und transitive-only
    - Owner bestaetigt die Direct-Dependency-Liste ohne httpx2
  </acceptance_criteria>
  <action>
    Package-Legitimacy-Gate: die Pruefschritte an den Owner geben, auf die Freigabe warten und das Ergebnis in der SUMMARY festhalten. Ohne Freigabe wird kein uv add und kein uv sync ausgefuehrt.
  </action>
  <done>Der Owner hat die Direct-Dependency-Liste ohne httpx2 freigegeben.</done>
  <resume-signal>Antworte "approved" oder benenne Pakete, die ersetzt werden sollen</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Toolchain, Dependency-Pins und CI-Grundgeruest</name>
  <files>pyproject.toml, uv.lock, .gitattributes, .gitignore, .github/workflows/ci.yml, src/mcp_connector/__init__.py, docs/dependency-audit.md</files>
  <read_first>
    - .planning/phases/01-server-kern/01-RESEARCH.md Abschnitte "Standard Stack", "Package Legitimacy Audit", "GitHub Actions", "Environment Availability"
    - CLAUDE.md (Sprachregeln, ruff-Pflicht, Commit-Regeln)
    - .gitignore (bestehender Inhalt)
  </read_first>
  <action>
    pyproject.toml anlegen (kein uv init, das Repo existiert schon): [project] name "nextcloud-mcp-connector" (D-02), version "0.1.0", requires-python ">=3.13", license AGPL-3.0-or-later, description englisch.
    dependencies exakt: "mcp[cli]>=2.0,<3", "httpx>=0.28,<0.29", "lxml>=6.1,<7", "icalendar>=7.2,<8", "vobject>=0.9.9,<1", "pydantic>=2.12,<3". httpx2 NIEMALS direkt listen (kommt transitiv ueber mcp, [SUS]-Befund).
    [project.scripts] nc-mcp = "mcp_connector.entry_stdio:main" (D-02, Entry-Point wird in Plan 02 implementiert).
    [build-system] hatchling, [tool.hatch.build.targets.wheel] packages = ["src/mcp_connector"].
    [tool.pytest.ini_options]: testpaths = ["tests"], addopts = "-m 'not integration' -q", markers fuer "integration: braucht die Docker-Nextcloud" und "matrix: startet lokale Subprozesse ohne Nextcloud". Grund: die Docker-Linux-Engine laeuft auf diesem Host nicht, die Default-Suite muss ohne Docker gruen sein.
    [tool.ruff]: line-length 100, target-version py313, lint select mindestens E,F,I,UP,B.
    Dev-Dependencies ueber "uv add --dev pytest respx ruff" hinzufuegen, danach "uv sync" fuer uv.lock.
    .gitattributes anlegen mit "*.sh text eol=lf" und "*.yml text eol=lf" (Windows-Host, docker compose exec bricht sonst an \r).
    .gitignore ergaenzen: .venv/, __pycache__/, .pytest_cache/, .ruff_cache/, dist/, .env.test (Secrets nie im Repo, V14).
    src/mcp_connector/__init__.py mit __version__ = "0.1.0", keine Seiteneffekte, kein print (Pitfall 7: stdout ist im stdio-Modus die Leitung).
    .github/workflows/ci.yml mit Job "unit": actions/checkout@v5, astral-sh/setup-uv@v5 mit enable-cache, "uv sync --frozen", "uv run ruff check .", "uv run ruff format --check .", "uv run pytest tests/unit tests/contract", "uv run python scripts/check_tool_budget.py". Der Workflow ist bewusst rot, bis Plan 02 den Server-Layer liefert (RED-Phase).
    docs/dependency-audit.md schreiben: Audit-Tabelle aus 01-RESEARCH.md uebernehmen, Ausgabe von "uv tree --depth 2" einfuegen und die Feststellung dokumentieren, dass httpx2 ausschliesslich unter mcp haengt.
  </action>
  <verify>
    <automated>uv sync && uv run python -c "import mcp, httpx, lxml, icalendar, vobject, pydantic" && grep -v '^#' pyproject.toml | grep -c '"httpx2' | grep -qx 0 && uv run ruff check . && uv run ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - "uv sync" endet mit Exit-Code 0 und erzeugt uv.lock
    - "uv run python -c 'import mcp'" endet mit Exit-Code 0
    - pyproject.toml enthaelt httpx2 in keiner Zeile ausserhalb von Kommentaren (grep -v '^#' | grep -c '"httpx2' ergibt 0)
    - "uv tree --depth 2" zeigt httpx2 als Kind von mcp, nicht als Top-Level-Eintrag; die Ausgabe steht in docs/dependency-audit.md
    - "uv run ruff check ." und "uv run ruff format --check ." enden mit Exit-Code 0
    - pyproject.toml enthaelt addopts mit -m 'not integration' sowie die Marker integration und matrix
    - .gitattributes enthaelt "*.sh text eol=lf"
  </acceptance_criteria>
  <done>Reproduzierbare Umgebung mit auditierten Pins, CI-Workflow vorhanden, Secrets-Pfade ignoriert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test-Harness, RED-Contract-Test und Token-Budget-Gate</name>
  <files>tests/conftest.py, tests/contract/test_tool_surface.py, tests/unit/test_project_layout.py, scripts/check_tool_budget.py</files>
  <read_first>
    - .planning/phases/01-server-kern/01-RESEARCH.md Abschnitte "In-Memory-Client fuer Contract-Tests", "Contract-Test: Annotationen, Namen, Schema-Diaet", "Token-Budget-Gate", "Environment Availability"
    - pyproject.toml (nach Task 2)
  </read_first>
  <behavior>
    - tests/contract/test_tool_surface.py: schlaegt jetzt mit ModuleNotFoundError fuer mcp_connector.server fehl (RED) und wird in Plan 02 gruen
    - tests/unit/test_project_layout.py: gruen, prueft die Projekt-Invarianten (kein httpx2-Pin, addopts, Console-Script)
    - Integration-Tests werden ohne NC_MCP_URL automatisch uebersprungen, nie als Fehler gemeldet
  </behavior>
  <action>
    tests/conftest.py anlegen, ohne Import von mcp_connector-Modulen (sonst bricht die Collection in der RED-Phase):
    - Fixture "anyio_backend" -> "asyncio" (SDK-Doku-Muster, kein pytest-asyncio).
    - Fixture "nc_env" -> dict mit base_url "http://nc.test", user "alice", secret "app-password-test" (nur Strings, keine Projektklassen).
    - Fixture "live_env" -> liest NC_MCP_URL, NC_MCP_USER, NC_MCP_APP_PASSWORD aus os.environ.
    - pytest_collection_modifyitems bzw. autouse-Fixture: Tests mit Marker "integration" werden per pytest.skip uebersprungen, wenn NC_MCP_URL fehlt. Begruendung als Kommentar: Docker-Linux-Engine laeuft auf dem Entwicklungshost nicht.
    tests/contract/test_tool_surface.py anlegen: importiert "from mcp_connector.server import mcp" und "from mcp import Client", oeffnet "async with Client(mcp, raise_exceptions=True)", liest list_tools() und prueft fuer diese Phase: "files_read" ist vorhanden, dessen annotations.read_only_hint is True, annotations.open_world_hint is False, und output_schema is None (Schema-Diaet via structured_output=False). Marker @pytest.mark.anyio. Die vollstaendige 15-Tool-Pruefung folgt in Plan 14, hier bewusst nur die Skeleton-Faehigkeit.
    tests/unit/test_project_layout.py anlegen: liest pyproject.toml per tomllib und prueft: kein Eintrag beginnt mit "httpx2", addopts enthaelt "not integration", project.scripts enthaelt "nc-mcp", requires-python ist ">=3.13".
    scripts/check_tool_budget.py anlegen wie in 01-RESEARCH.md skizziert: BUDGET_BYTES = 24_000 mit Kommentar "Startwert, wird in Plan 14 auf gemessen plus 15 Prozent fixiert", In-Memory-Client, model_dump(by_alias=True, exclude_none=True, mode="json"), kompakte JSON-Serialisierung, Byte-Groesse plus Top-5-Tools ausgeben, Exit-Code 1 bei Ueberschreitung. Kein tiktoken.
  </action>
  <verify>
    <automated>uv run pytest tests/unit -q && uv run pytest tests/contract -q; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - "uv run pytest tests/unit -q" endet mit Exit-Code 0 und sammelt mindestens 4 Tests
    - "uv run pytest tests/contract -q" endet mit Exit-Code ungleich 0 und die Ausgabe nennt mcp_connector.server (definierter RED-Zustand)
    - "uv run pytest -q" laeuft ohne Docker, kein Test wartet auf einen Netzwerk-Timeout
    - scripts/check_tool_budget.py enthaelt BUDGET_BYTES und gibt bei Ueberschreitung Exit-Code 1 zurueck
    - tests/conftest.py importiert kein Modul aus mcp_connector
  </acceptance_criteria>
  <done>Der RED-Test beschreibt files_read exakt, die Unit-Suite ist gruen, das Budget-Gate ist in CI verdrahtet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI zu lokalem Environment | Fremder Code wird installiert und ausgefuehrt (Supply Chain) |
| Repository zu Oeffentlichkeit | Alles hier Angelegte wird spaeter public (Plan 12), Secrets duerfen nie einfliessen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | uv add / uv sync (PyPI) | mitigate | slopcheck-Audit aus 01-RESEARCH.md, blockierender Human-Checkpoint (Task 1) fuer httpx2 [SUS], uv.lock committen, "uv sync --frozen" in CI, httpx2 nie direkt pinnen |
| T-01-01 | Information Disclosure | Credential-Handling | mitigate | .env.test in .gitignore, keine Default-Credentials in pyproject/CI, CI nutzt keine Secrets in dieser Phase |
| T-01-02 | Spoofing | SSRF ueber Nextcloud-Basis-URL | transfer | In diesem Plan existiert kein HTTP-Client. Kontrolle liegt in Plan 02 (Basis-URL nur aus Env, kein URL-Parameter) |
| T-01-03 | Elevation of Privilege | Path Traversal in Datei-Tools | transfer | Kein Tool-Code in diesem Plan. Kontrolle in Plan 02 (Pfad-Guard) und Plan 03 (Upload) |
| T-01-04 | Tampering | Create-only-Schreibschutz | transfer | Kein Schreibpfad in diesem Plan. Kontrolle in Plan 03 (If-None-Match: *) |
| T-01-05 | Tampering | Injection in WebDAV-XML | transfer | Kein XML in diesem Plan. Kontrolle in Plan 02 (lxml-Builder, gehaerteter Parser) |
| T-01-06 | Denial of Service | Unbegrenzte tools/list-Antwort sprengt Client-Kontext | mitigate | scripts/check_tool_budget.py als CI-Gate ab Tag 1 |
</threat_model>

<verification>
1. "uv sync" und "uv run ruff check ." sind gruen.
2. "uv run pytest tests/unit -q" ist gruen, "uv run pytest tests/contract -q" ist definiert rot.
3. docs/dependency-audit.md belegt httpx2 als transitive Abhaengigkeit.
4. Kein print() im Paket, keine Secrets im Repo.
</verification>

<success_criteria>
Ein zweiter Claude-Executor kann ohne Rueckfragen "uv sync" ausfuehren, die Suite starten und aus dem roten Contract-Test ablesen, welche Faehigkeit Plan 02 liefern muss.
</success_criteria>

<output>
Create `.planning/phases/01-server-kern/01-01-SUMMARY.md` when done
</output>
