---
phase: 14-doku-reste-und-gate-entscheid
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.fr.md
  - README.de.md
  - CHANGELOG.md
  - appinfo/info.xml
autonomous: true
requirements: [DOC-01]

must_haves:
  truths:
    - "README.fr.md trägt kein 'confidemment' mehr und die Hauptüberschrift ist französisch statt englisch"
    - "README.de.md schreibt 'MCP Server' groß, wie die deutsche Store-Summary in info.xml"
    - "CHANGELOG.md trägt keine Linkdefinition ohne zugehörigen Abschnitt mehr"
    - "appinfo/info.xml behauptet nichts mehr über einen Ampersand, den keine der beiden Spendenadressen enthält"
  artifacts:
    - path: "README.fr.md"
      provides: "französische Fassung ohne Anglizismus in der Hauptüberschrift und ohne Scheinwort"
      contains: "MCP Connector pour Nextcloud"
    - path: "README.de.md"
      provides: "deutsche Fassung mit korrekter Großschreibung von MCP Server"
      contains: "MCP Server"
    - path: "CHANGELOG.md"
      provides: "Linkdefinitionen, die alle einen Abschnitt haben"
      contains: "[0.1.9]:"
    - path: "appinfo/info.xml"
      provides: "Kommentarblock der Spendenbuttons ohne veraltete Zustandsbehauptung"
      contains: "donation title=\"Donate with PayPal\""
  key_links:
    - from: "README.de.md"
      to: "appinfo/info.xml summary lang=\"de\""
      via: "dieselbe Schreibweise des Produktbegriffs in Doku und Store-Text"
      pattern: "MCP Server"
    - from: "CHANGELOG.md"
      to: "Keep a Changelog 1.1.0"
      via: "der Dateikopf beruft sich auf das Format, also darf keine Definition ins Leere zeigen"
      pattern: "keepachangelog"
---

<objective>
Die drei Sprach- und Formfehler aus 13-REVIEW.md schließen, die in Dateien liegen, die mit dem
nächsten Release in den Store fahren: die zwei französischen Reste und die deutsche
Kleinschreibung aus IN-05, die hängende `[Unreleased]`-Linkdefinition aus IN-06 und der
veraltete Ampersand-Kommentar aus IN-03.

Purpose: DOC-01a bis DOC-01c. Alle vier Dateien reisen im Store-Archiv oder erzeugen den
Store-Text, also ist jede Zeile hier öffentlich sichtbar. Der Fehler in einer
Linkdefinition und ein Kommentar, der einen Zustand beschreibt, den es nicht mehr gibt,
kosten den nächsten Leser Zeit und stellen die Sorgfalt der übrigen Aussagen in Frage.
Output: vier korrigierte Dateien, kein Versions-Bump, kein Changelog-Eintrag für 0.1.10
(der entsteht in Phase 15).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.3-phases/13-cimd-nachmessung-und-release-0-1-9/13-REVIEW.md
</context>

<constraints>
- KEIN Versions-Bump an irgendeiner der sechs Versionsstellen. Version bleibt 0.1.9, der Bump
  auf 0.1.10 gehört zu Phase 15 (EXAPP-10).
- KEIN Tag, KEIN Release, KEIN Changelog-Block 0.1.10 in diesem Plan.
- Deutsche Texte mit echten Umlauten (ä, ö, ü, ß), französische mit echten Akzenten und
  Cédille. Keine Em-Dashes (U+2014) und keine En-Dashes (U+2013) in irgendeiner Datei.
- Der Wortlaut des Enterprise-Abschnitts (Kürzung 55a5822, Kontakt admin@infranode.dev) wird
  inhaltlich NICHT angefasst; er ist die Nutzlast von Release 0.1.10.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Die drei Übersetzungsreste aus IN-05, geprüft gegen den Stand nach 55a5822</name>
  <files>README.fr.md, README.de.md</files>
  <read_first>
    - .planning/milestones/v1.3-phases/13-cimd-nachmessung-und-release-0-1-9/13-REVIEW.md, Abschnitt IN-05 (die drei benannten Stellen und der Fix-Vorschlag)
    - README.fr.md, Zeile 1-10 (Sprachleiste, Hinweis auf die maßgebliche englische Fassung, Hauptüberschrift auf Zeile 5)
    - README.fr.md, Zeile 270-280 und Zeile 524-532 (die beiden Fundstellen von "confidemment", einmal Prosa, einmal Tabellenzelle)
    - README.fr.md, Zeile 543-549 (der gekürzte Enterprise-Abschnitt, damit klar ist, dass dort nichts mehr zu tun ist)
    - README.de.md, Zeile 1-10 und Zeile 18-26 (die Hauptüberschrift "MCP Connector für Nextcloud" als Vorbild und die beiden Fundstellen "MCP server")
    - README.md, Zeile 5-8 und die englischen Gegenstücke der beiden "confidently"-Sätze (maßgebliche Fassung, an der sich die Übersetzung ausrichtet)
    - appinfo/info.xml, Zeile 18-20 (die drei Summaries: die deutsche schreibt "MCP Server", die französische ist bereits französisch)
  </read_first>
  <action>
    Drei Korrekturen, jede ein Wort oder eine Wortgruppe, keine Umformulierung eines Absatzes.

    1. README.fr.md Zeile 5: `# MCP Connector for Nextcloud` wird zu
       `# MCP Connector pour Nextcloud`. Der Produktname "MCP Connector" bleibt unverändert,
       nur die englische Präposition wird übersetzt, genau wie README.de.md es mit "für" tut.

    2. README.fr.md Zeile 274: "produit une réponse confidemment fausse" wird zu
       "produit une réponse fausse mais assurée". "confidemment" ist kein französisches Wort.

    3. README.fr.md Zeile 528 (Tabellenzelle der bekannten Grenzen): "Un fuseau deviné est une
       réponse confidemment fausse" wird zu "Un fuseau deviné est une réponse fausse mais
       assurée". Dieselbe Ersetzung wie oben, damit die Datei einen Begriff und nicht zwei
       trägt.

    4. README.de.md Zeile 7 und Zeile 22: "MCP server" wird zu "MCP Server". Im deutschen Satz
       ist das ein Substantiv, und die deutsche Store-Summary in appinfo/info.xml Zeile 19
       schreibt es bereits groß. Prüfe mit grep, dass es keine dritte Fundstelle gibt.

    Prüfe danach ausdrücklich per grep, ob DOC-01a noch weitere offene Stellen hat, und halte
    das Ergebnis für die SUMMARY fest: "confidemment" in README.fr.md und in appinfo/info.xml,
    sowie englische Präpositionen in französischen Überschriften (`^#` Zeilen von README.fr.md).
    Erwartung nach den Korrekturen: null Treffer. Die französische Description in
    appinfo/info.xml (ab Zeile 136) und die französische Summary (Zeile 20) sind nach heutigem
    Stand sauber; belege das mit dem Grep-Ergebnis statt mit einer Änderung.

    Der gekürzte Enterprise-Abschnitt in README.fr.md (Zeile 545 ff., Kontakt
    admin@infranode.dev) ist durch Commit 55a5822 bereits französisch und korrekt. Fasse ihn
    nicht an und dokumentiere in der SUMMARY, dass der ursprünglich in IN-05 gemeinte
    Enterprise-Wortlaut durch 55a5822 erledigt ist, mit dem Grep-Beleg daneben.
  </action>
  <verify>
    <automated>grep -n "confidemment" README.fr.md appinfo/info.xml; test $? -eq 1 && grep -qx "# MCP Connector pour Nextcloud" README.fr.md && ! grep -n "MCP server" README.de.md && ! grep -nE "^#.* (for|with|and|the) " README.fr.md && ! grep -n "—\|–" README.fr.md README.de.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "confidemment" README.fr.md appinfo/info.xml` gibt keine Zeile aus
    - `grep -c "réponse fausse mais assurée" README.fr.md` gibt 2
    - `grep -n "^# MCP Connector pour Nextcloud" README.fr.md` nennt Zeile 5, und `grep -nE "^#.* (for|with|and|the) " README.fr.md` gibt keine Zeile aus
    - `grep -n "MCP server" README.de.md` gibt keine Zeile aus, `grep -c "MCP Server" README.de.md` gibt mindestens 2
    - `grep -n "—\|–" README.fr.md README.de.md` gibt keine Zeile aus
    - `git diff --numstat README.fr.md README.de.md` nennt für beide Dateien gleich viele hinzugefügte wie entfernte Zeilen (nur Ersetzungen, keine neuen oder gelöschten Zeilen)
    - Die Zeile mit `admin@infranode.dev` in README.fr.md ist im Diff nicht enthalten
  </acceptance_criteria>
  <done>README.fr.md ist an den drei Stellen echtes Französisch, README.de.md schreibt MCP Server groß, der Enterprise-Wortlaut ist unangetastet, und der Grep-Beleg für die Restfreiheit von DOC-01a liegt für die SUMMARY bereit.</done>
</task>

<task type="auto">
  <name>Task 2: Hängende Linkdefinition und veralteter Ampersand-Kommentar</name>
  <files>CHANGELOG.md, appinfo/info.xml</files>
  <read_first>
    - .planning/milestones/v1.3-phases/13-cimd-nachmessung-und-release-0-1-9/13-REVIEW.md, Abschnitte IN-06 und IN-03
    - CHANGELOG.md, Zeile 1-12 (der Kopf beruft sich auf Keep a Changelog 1.1.0, danach beginnt sofort `## [0.1.9]`, es gibt keinen Unreleased-Abschnitt)
    - CHANGELOG.md, Zeile 476-484 (der Block der Linkdefinitionen, `[Unreleased]` auf Zeile 480, `[0.1.9]` auf Zeile 481)
    - appinfo/info.xml, Zeile 215-227 (der Kommentarblock der Spendenbuttons und die zwei Spendenelemente mit ihren heutigen URLs)
    - tests/unit/test_exapp_env_setup.py, Zeile 1953-1990 (Reichweite des Vokabular-Gates: CHANGELOG.md und die Manifest-Texte sind erfasst) und die Regel, dass ein XML-Kommentar keine Sequenz aus zwei Bindestrichen enthalten darf
  </read_first>
  <action>
    Zwei Korrekturen in zwei Dateien.

    1. CHANGELOG.md: Entferne die Linkdefinition `[Unreleased]: .../compare/v0.1.9...HEAD`
       (heute Zeile 480) vollständig. Gewählter Weg von den beiden, die DOC-01b zulässt
       ("entfernt oder wieder referenziert"): entfernen. Grund, der auch in die SUMMARY
       gehört: die Datei führt heute keinen `## [Unreleased]`-Abschnitt, und ein leerer
       Abschnitt wäre Prosa, die beim nächsten Release wieder aufgeräumt werden müsste. Die
       nutzersichtbaren Änderungen dieser Phase erscheinen in dem Block `## [0.1.10]`, den
       Phase 15 schreibt. Die Definition `[0.1.9]: .../compare/v0.1.8...v0.1.9` und alle
       älteren bleiben unverändert. Regel für Phase 15, ebenfalls in die SUMMARY: eine
       `[Unreleased]`-Definition entsteht erst wieder zusammen mit einem Abschnitt, der sie
       referenziert.

    2. appinfo/info.xml: Ersetze im Kommentarblock der Spendenbuttons die Trennzeile und die
       zwei Sätze über den Ampersand (heute Zeile 222 bis 224) durch eine Aussage, die den
       heutigen Zustand beschreibt: keine der beiden Adressen trägt heute einen Ampersand,
       und eine künftige Adresse mit einem Ampersand braucht die Entity `&amp;`, weil ein
       nackter Ampersand hier ein Parse-Fehler ist und der Store die Datei dann nie sieht.
       Die vorangehenden Sätze über Reihenfolge, secure-url und Provision bleiben Wort für
       Wort stehen. Keine Sequenz aus zwei Bindestrichen im Kommentar (info.xsd-Gate), und
       die vorhandene Einrückung mit `  - ` je Zeile beibehalten.

    Ändere weder eine Spenden-URL noch ein Element der Reihenfolge: die Elementfolge ist
    durch info.xsd bindend, und die PayPal-Adresse ist der Grund, aus dem Release 0.1.8
    existiert.
  </action>
  <verify>
    <automated>! grep -n "^\[Unreleased\]:" CHANGELOG.md && grep -qn "^\[0\.1\.9\]:" CHANGELOG.md && ! grep -n "The ampersand of the PayPal address" appinfo/info.xml && uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Unreleased" CHANGELOG.md` gibt keine Zeile aus
    - `grep -c "^\[0\.1\.9\]: https://github.com/street1983nk/nextcloud-mcp-connector/compare/v0.1.8...v0.1.9$" CHANGELOG.md` gibt 1
    - Jede verbleibende Linkdefinition in CHANGELOG.md hat einen Abschnitt: für jedes `^\[([0-9.]+)\]:` existiert ein `^## \[\1\]` (per grep beider Listen belegt, gleiche Menge an Versionen)
    - `grep -n "ampersand" appinfo/info.xml` gibt genau eine Zeilengruppe im Kommentar der Spendenbuttons aus und behauptet nicht, eine der Adressen enthalte einen Ampersand
    - `grep -n -- "--" appinfo/info.xml` gibt außerhalb der Kommentarbegrenzer `<!--` und `-->` keine Zeile aus
    - `grep -c "paypalme/KhaledCherifDev" appinfo/info.xml` gibt 1 und `grep -c "buy.stripe.com" appinfo/info.xml` gibt 1
    - `uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -q` endet mit Exit 0 (Manifest-Gate, XSD-Verträglichkeit und Vokabular-Gate über CHANGELOG.md)
    - `grep -c "0.1.9" appinfo/info.xml` ist unverändert; keine Versionsstelle wurde angefasst
  </acceptance_criteria>
  <done>CHANGELOG.md trägt nur noch Linkdefinitionen mit Abschnitt, der Kommentar in info.xml beschreibt den heutigen Zustand der Spendenadressen, und alle Gates über beide Dateien sind grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo-Text zu Store-Text | appinfo/info.xml und CHANGELOG.md reisen im Store-Archiv; eine falsche Aussage hier wird mit dem nächsten Release veröffentlicht und ist im Asset unveränderlich |
| Übersetzung zu maßgeblicher Fassung | README.md ist maßgeblich; eine Korrektur in der Übersetzung darf die Bedeutung nicht verschieben |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | README.fr.md, README.de.md | mitigate | Nur die drei in IN-05 benannten Wortstellen werden ersetzt; ein Akzeptanzkriterium verlangt gleich viele hinzugefügte wie entfernte Zeilen, sodass kein Absatz umformuliert wird |
| T-14-02 | Tampering | Enterprise-Abschnitt (Kürzung 55a5822) | mitigate | Der Abschnitt ist ausdrücklich außerhalb des Diffs; ein Akzeptanzkriterium prüft, dass die Zeile mit admin@infranode.dev nicht im Diff steht |
| T-14-03 | Repudiation | CHANGELOG.md | mitigate | Entfernt wird ausschließlich die Definition ohne Abschnitt; alle Versionsdefinitionen bleiben, und ein Kriterium belegt die Paarung von Definition und Abschnitt |
| T-14-04 | Tampering | appinfo/info.xml | mitigate | Nur ein Kommentar wird geändert, keine URL und keine Elementfolge; das Manifest-Gate (XSD-Verträglichkeit, keine Doppelbindestriche, Versionsstellen) läuft als automatisierte Prüfung |
| T-14-05 | Information Disclosure | Store-Texte | accept | Alle vier Dateien sind bereits öffentlich; die Änderungen sind Wortkorrekturen ohne neue Information |
| T-14-SC | Tampering | Paketinstallation | mitigate | Keine Paketinstallation in diesem Plan; alle Prüfungen laufen mit `uv run --no-sync`, also ohne Sync und ohne Auflösung |
</threat_model>

<verification>
- `grep -n "confidemment" README.fr.md appinfo/info.xml` leer, Hauptüberschrift französisch
- `grep -n "MCP server" README.de.md` leer
- `grep -n "Unreleased" CHANGELOG.md` leer, `[0.1.9]`-Definition unverändert vorhanden
- Kommentar in appinfo/info.xml behauptet keinen Ampersand in einer der beiden Adressen
- `uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -q` Exit 0
- Version bleibt an allen sechs Stellen 0.1.9, kein Tag entsteht
</verification>

<success_criteria>
Roadmap-Erfolgskriterium 1 ist erfüllt (echtes Französisch in README.fr.md, geprüft gegen den
Stand nach 55a5822, samt Grep-Beleg für die Restfreiheit) und Erfolgskriterium 2 ebenfalls
(keine hängende Linkdefinition, kein veralteter Ampersand-Kommentar).
</success_criteria>

<output>
Create `.planning/phases/14-doku-reste-und-gate-entscheid/14-01-SUMMARY.md` when done.
Nenne darin: die vier ersetzten Wortstellen, den Grep-Beleg, dass der Enterprise-Wortlaut aus
IN-05 durch 55a5822 bereits erledigt war, den gewählten Weg für DOC-01b (Definition entfernt,
weil kein Abschnitt existiert) samt der Regel für Phase 15, und die neue Formulierung des
Ampersand-Kommentars.
</output>
