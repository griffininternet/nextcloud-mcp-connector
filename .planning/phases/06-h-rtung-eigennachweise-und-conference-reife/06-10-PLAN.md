---
phase: 06-h-rtung-eigennachweise-und-conference-reife
plan: 10
type: execute
wave: 8
depends_on: ["06-09"]
files_modified:
  - docs/conference-demo.md
  - docs/conference-talk.md
  - .planning/phases/06-h-rtung-eigennachweise-und-conference-reife/06-10-MEASUREMENTS.md
autonomous: true
requirements: [CONF-01, CONF-02]

must_haves:
  truths:
    - "Ein Dritter kann die Demo-Strecke aus dem Drehbuch nachfahren, ohne das Repository zu kennen"
    - "Die Strecke deckt Verbindung, Werkzeugaufrufe, Per-User-Verwaltung und Widerruf gegen eine laufende Instanz ab"
    - "Das Drehbuch ist einmal vollstaendig durchgefahren und der Lauf ist belegt"
    - "Ein Lightning-Talk-Entwurf aus Folien und Sprechzettel liegt vor, Englisch, auf fuenf Minuten ausgelegt"
    - "Es wurde nichts eingereicht und niemand kontaktiert"
  artifacts:
    - path: "docs/conference-demo.md"
      provides: "Runbook mit Einmal-Aufbau, Drehbuch, Vorher-Checkliste und was bewusst nicht gezeigt wird"
    - path: "docs/conference-talk.md"
      provides: "Folienabfolge plus Sprechzettel, fuenf Minuten, vier Differenzierer"
    - path: ".planning/phases/06-h-rtung-eigennachweise-und-conference-reife/06-10-MEASUREMENTS.md"
      provides: "Beleg des einen vollstaendigen Durchlaufs mit Zeiten je Schritt"
  key_links:
    - from: "docs/conference-demo.md"
      to: "scripts/oauth_flow_check.py"
      via: "Das Drehbuch ruft die vorhandenen Skripte auf statt neue zu bauen"
      pattern: "oauth_flow_check"
    - from: "docs/conference-demo.md"
      to: ".planning/phases/06-h-rtung-eigennachweise-und-conference-reife/06-10-MEASUREMENTS.md"
      via: "Jeder Schritt des Drehbuchs hat eine Zeile im Durchlauf-Beleg"
      pattern: "Schritt"
---

<objective>
Das Conference-Material: eine nachfahrbare Demo-Strecke mit Drehbuch und ein
Lightning-Talk-Entwurf.

Purpose: Success Criterion 5 der Phase verlangt, dass ein DRITTER die Demo nachfahren kann.
Das ist der Unterschied zwischen einer Notiz und einem Runbook, und der Beweis ist ein
Durchlauf, nicht ein Versprechen. Die Demo muss dabei nichts neu bauen:
`scripts/oauth_flow_check.py` deckt Verbindung und Werkzeugaufruf ab,
`scripts/acceptance_all_tools.py` den Werkzeug-Durchlauf, und die Per-User-Seite samt
Widerruf steht seit Phase 4. Die Demo ist das Drehbuch um vorhandene Teile.
Fuer CONF-02 gilt eine harte Grenze aus der Recherche: der Call for Speakers der Nextcloud
Community Conference 2026 ist seit dem 03.08.2026 geschlossen. Dieser Plan liefert
ausschliesslich den ENTWURF. Jede Einreichung, jede Mail und jede Kontaktaufnahme ist
Owner-Sache und wird hier hoechstens als vorbereiteter Text abgelegt, nie gesendet.
Output: `docs/conference-demo.md`, `docs/conference-talk.md`, und ein Messprotokoll des einen
vollstaendigen Durchlaufs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-h-rtung-eigennachweise-und-conference-reife/06-CONTEXT.md
@.planning/phases/06-h-rtung-eigennachweise-und-conference-reife/06-RESEARCH.md
@.planning/phases/06-h-rtung-eigennachweise-und-conference-reife/06-PATTERNS.md
@.planning/phases/06-h-rtung-eigennachweise-und-conference-reife/06-07-SUMMARY.md
@.planning/phases/06-h-rtung-eigennachweise-und-conference-reife/06-09-SUMMARY.md
</context>

<interfaces>
Vorhandene Bausteine, die das Drehbuch benutzt statt sie zu ersetzen:
- `scripts/oauth_flow_check.py`: die sieben Schritte der Verbindungsstrecke, mit den
  Kopier-Kommandos im Docstring (Zeilen 11-24)
- `scripts/acceptance_all_tools.py`: der Durchlauf ueber alle Werkzeuge (Docstring Zeilen 1-27)
- `scripts/bootstrap_exapp.sh`: der Einmal-Aufbau der Topologie
- Die Per-User-Seite und der Widerruf unter `/connections` aus Phase 4
- Die laufende Instanz aus 06-07: `http://127.0.0.1:8081`, Nextcloud 34.0.3, Connector auf
  Repository-Stand, Nutzer `jane` mit zwei echten OAuth-Verbindungen, plus die in 06-09
  hinzugekommene CIMD-Verbindung

Formvorbilder:
- `docs/store-submission.md` als Runbook-Form: die Ueberschriftenfolge Stand, Artefakte,
  Einmal-Aufbau, Runbook, Vorher-Checkliste, "nicht noetig, haeufige Missverstaendnisse"
- `README.md` Statusabschnitt fuer Ton und Faktentreue des Talk-Entwurfs

Conference-Fakten, in der Recherche verifiziert: 19. bis 20.09.2026, CIC Berlin,
Lohmuehlenstrasse 65; Lightning Talk = FUENF Minuten (nicht zehn, das Format gibt es nicht);
CfP verlaengert bis 03.08.2026 und seither geschlossen; angenommene Sprecher liefern
Foliendraft bis 09.09.; Contributor Week 21. bis 25.09.

Die vier Differenzierer als Kern-Narrativ (aus CONTEXT.md): Store-Ein-Klick, spec-konformes
OAuth, Per-User-Verwaltung, kann konstruktionsbedingt nichts zerstoeren.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: docs/conference-demo.md als Runbook</name>
  <files>docs/conference-demo.md</files>
  <read_first>
    - docs/store-submission.md (vollstaendig; die Ueberschriftenfolge und der Ton sind die Vorlage)
    - scripts/oauth_flow_check.py (Docstring Zeilen 1-30 vollstaendig)
    - scripts/acceptance_all_tools.py (Docstring Zeilen 1-27)
    - scripts/bootstrap_exapp.sh (was es baut, registriert und setzt)
    - src/mcp_connector/exapp/ui/connections.py und src/mcp_connector/oauth/connections.py (was die Per-User-Seite kann: Pausieren, Freigeben, Verbindung beenden)
    - .planning/phases/06-h-rtung-eigennachweise-und-conference-reife/06-07-SUMMARY.md und 06-09-SUMMARY.md (der belegte Zustand der Instanz, den die Demo voraussetzt)
    - .planning/phases/06-h-rtung-eigennachweise-und-conference-reife/06-PATTERNS.md (§13 vollstaendig)
  </read_first>
  <action>
    Ein Runbook in Englisch, in der Ueberschriftenfolge von `docs/store-submission.md`,
    uebersetzt auf die Demo:
    - **Where this stands**: welche Instanz, welche Versionen, was belegt ist und wo der
      Beleg liegt (Verweise auf die Messdateien dieser Phase). Keine Behauptung ohne Fundstelle.
    - **What is shown**: die vier Stationen in der Reihenfolge, in der sie vorgefuehrt werden,
      jede mit dem Satz, welchen der vier Differenzierer sie belegt: Verbindung herstellen
      (spec-konformes OAuth), Werkzeuge aufrufen (was der Assistent kann), Per-User-Verwaltung
      (pausieren und freigeben), Widerruf (die Nutzerin beendet eine Verbindung und der
      Assistent verliert den Zugang sofort).
    - **One time setup**: der Aufbau der Topologie, mit dem `HP_SHARED_KEY`-Zuruecklesen und
      dem Neuerzeugen als getrennten Faellen, weil beide Verwechslungen in diesem Projekt
      schon Zeit gekostet haben. Alle Kommandos in Code-Bloecken, kopierbar, ohne Platzhalter,
      die man erst suchen muss.
    - **The script (run of show)**: das Drehbuch. Je Schritt: was gesagt wird (ein Satz), was
      getippt oder geklickt wird (Code-Block beziehungsweise Klickweg), was zu sehen sein muss
      (die Erwartung, an der man merkt, dass es klappt), und wie lange der Schritt dauert. Wo
      ein vorhandenes Skript den Schritt macht, wird es aufgerufen und nicht nachgebaut.
    - **Pre-demo checklist**: die Punkte, die man 10 Minuten vorher prueft. Insbesondere die
      drei, die in dieser Phase Zeit gekostet haben: laeuft die Instanz auf der erwarteten
      Version (`occ status`, nicht der Docker-Tag), traegt der Container das aktuelle Image
      (Digest), stehen die OAuth-Schalter im Ausgangszustand.
    - **What is deliberately not shown, and why**: die Ehrlichkeitsgrenzen. Der Punkt, den
      06-07 gemessen hat (Install- und Remove-Knopf, in dem Ausgang, der gemessen wurde), der
      Nextcloud-AIO-Fall als bewusst nicht abgedeckt, und dass CIMD Kontrolle ueber eine URL
      belegt und nicht die Identitaet eines Prozesses.
    - **Recovery**: was man tut, wenn ein Schritt live nicht funktioniert. Ein Runbook ohne
      Rueckfallweg ist ein Drehbuch fuer den guten Tag.

    Die Form ist Markdown im Repository, kein Praesentationswerkzeug und kein neues Paket
    (Package-Legitimacy-Gate wird dadurch nicht ausgeloest). Kopier-Texte immer im Code-Block,
    nie als Zitat. Keine Emojis, keine Em-Dashes.
  </action>
  <verify>
    <automated>test -s docs/conference-demo.md && grep -c "^## " docs/conference-demo.md && grep -n "oauth_flow_check" docs/conference-demo.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/conference-demo.md` enthaelt die sieben Abschnitte in der genannten Reihenfolge (Stand, was gezeigt wird, Einmal-Aufbau, Drehbuch, Vorher-Checkliste, was bewusst nicht gezeigt wird, Rueckfall)
    - Jede der vier Stationen (Verbindung, Werkzeugaufrufe, Per-User-Verwaltung, Widerruf) hat einen eigenen Drehbuch-Schritt mit Erwartung und Zeitangabe
    - Jeder Schritt, der ein Kommando braucht, hat es in einem Code-Block; `grep -c '^```' docs/conference-demo.md` ist grösser als 6
    - Das Drehbuch ruft `scripts/oauth_flow_check.py` und `scripts/acceptance_all_tools.py` auf; `grep -n "acceptance_all_tools" docs/conference-demo.md | wc -l` ist mindestens 1
    - Der Abschnitt "was bewusst nicht gezeigt wird" nennt den gemessenen Store-UI-Befund aus 06-07, den AIO-Fall und die CIMD-Ehrlichkeitsgrenze
    - Die Vorher-Checkliste nennt `occ status` und den Image-Digest und die drei OAuth-Schalter
    - `grep -n "—\|–" docs/conference-demo.md | wc -l` liefert 0; `grep -in "archiv" docs/conference-demo.md | wc -l` liefert 0
    - Kein Credential steht in der Datei, auch kein Beispiel-Token: `grep -inE "APP_SECRET=[a-f0-9]|HP_SHARED_KEY=[a-f0-9]|Bearer [A-Za-z0-9._-]{10,}" docs/conference-demo.md | wc -l` liefert 0
    - Kein neues Paket: `git diff --stat pyproject.toml uv.lock` ist leer
  </acceptance_criteria>
  <done>Das Runbook steht in der Form von docs/store-submission.md, deckt die vier Stationen ab, ruft vorhandene Skripte auf und nennt seine Grenzen.</done>
</task>

<task type="auto">
  <name>Task 2: Einmal vollstaendig durchfahren und belegen</name>
  <files>.planning/phases/06-h-rtung-eigennachweise-und-conference-reife/06-10-MEASUREMENTS.md, docs/conference-demo.md</files>
  <read_first>
    - docs/conference-demo.md (Stand nach Task 1, vollstaendig)
    - .planning/phases/06-h-rtung-eigennachweise-und-conference-reife/06-09-SUMMARY.md (der Zustand der Instanz und der Schalter nach der letzten Messung)
    - .planning/phases/06-h-rtung-eigennachweise-und-conference-reife/06-PATTERNS.md (§11, das Messdatei-Muster)
  </read_first>
  <action>
    Das Drehbuch einmal vollstaendig nachfahren, und zwar STRIKT so, wie es dasteht: kein
    Wissen aus dem Kopf, kein Schritt aus dem Gedaechtnis ergaenzt. Genau das ist der Test auf
    "ein Dritter kann es nachfahren". Jede Stelle, an der man nachdenken oder etwas
    nachschlagen musste, ist ein Fehler im Drehbuch und wird im Drehbuch behoben, nicht in der
    Messdatei entschuldigt.

    Das Protokoll entsteht nach dem Muster der Phase 5: Kopf mit Datum, Uhrzeit und Zeitzone,
    Topologie-Tabelle mit Versionen und Digests, dann eine Zeile je Drehbuch-Schritt mit
    gemessener Dauer und dem Ergebnis, und danach ein Abschnitt "Was am Drehbuch nicht
    gestimmt hat" mit den vorgenommenen Korrekturen.

    Der Widerruf-Schritt braucht einen echten Beleg, nicht eine Klickbestaetigung: nach dem
    Beenden der Verbindung wird derselbe Werkzeugaufruf noch einmal versucht und die Absage
    festgehalten. Ein Widerruf, dessen Wirkung man nicht sieht, ist die Station, die auf einer
    Buehne nichts beweist.

    Der Per-User-Schritt ebenso: pausieren, Werkzeugaufruf scheitert, freigeben,
    Werkzeugaufruf gelingt wieder, jeweils mit Rohbeleg.

    Die Gesamtdauer wird notiert. Ist sie deutlich laenger als das Zeitfenster, das das
    Drehbuch behauptet, wird das Drehbuch gekuerzt und nicht die Zeitangabe geschoenmalt.

    Am Ende wird der Instanzzustand belegt zurueckgestellt: die in der Demo beendete
    Verbindung ist beendet (das ist der gewollte Endzustand), die Schalter stehen im
    Ausgangszustand, und `occ app_api:app:list` meldet die App als `enabled`.
  </action>
  <verify>
    <automated>docker exec -u www-data nc-mcp-exapp-nc php occ app_api:app:list | grep -i "mcp_connector" && grep -c "^| " .planning/phases/06-h-rtung-eigennachweise-und-conference-reife/06-10-MEASUREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Die Messdatei hat eine Zeile je Drehbuch-Schritt mit gemessener Dauer und Ergebnis
    - Der Widerruf-Schritt belegt die Wirkung: derselbe Werkzeugaufruf nach dem Beenden wird abgelehnt, mit Rohbeleg
    - Der Per-User-Schritt belegt beide Richtungen: pausiert scheitert, freigegeben gelingt
    - Die Datei enthaelt den Abschnitt "Was am Drehbuch nicht gestimmt hat" und die Korrekturen sind in `docs/conference-demo.md` tatsaechlich vorgenommen (`git diff docs/conference-demo.md` zeigt sie)
    - Die notierte Gesamtdauer und die im Drehbuch behauptete Dauer weichen um nicht mehr als 20 Prozent ab, sonst ist das Drehbuch angepasst
    - Die Topologie-Tabelle nennt Versionen und Digests, keine Docker-Tags
    - Die Datei enthaelt kein Credential
    - `occ app_api:app:list` meldet die App nach dem Lauf als `enabled`; die drei OAuth-Schalter stehen belegt im Ausgangszustand
  </acceptance_criteria>
  <done>Das Drehbuch ist einmal wie geschrieben durchgefahren, die Stolperstellen sind im Drehbuch behoben, und Per-User-Schalter und Widerruf sind mit ihrer Wirkung belegt.</done>
</task>

<task type="auto">
  <name>Task 3: docs/conference-talk.md, Entwurf auf fuenf Minuten</name>
  <files>docs/conference-talk.md</files>
  <read_first>
    - README.md (Statusabschnitt; Ton und Faktentreue, "Fakten mit Fundstelle statt einer Phasennummer")
    - docs/conference-demo.md (Stand nach Task 2; der Talk verweist auf die Demo, wiederholt sie nicht)
    - .planning/phases/06-h-rtung-eigennachweise-und-conference-reife/06-CONTEXT.md (die vier Differenzierer und die Stilvorgaben)
    - .planning/phases/06-h-rtung-eigennachweise-und-conference-reife/06-RESEARCH.md (Open Question 2 mit den Conference-Fakten und dem geschlossenen CfP; Assumption A8)
    - .planning/phases/06-h-rtung-eigennachweise-und-conference-reife/06-07-SUMMARY.md und 06-09-SUMMARY.md (was belegt ist und daher behauptet werden darf)
  </read_first>
  <action>
    Ein Entwurf in Englisch, als Markdown im Repository, ohne Praesentationswerkzeug und ohne
    neues Paket. Aufbau:

    - **Kopfzeile mit dem Rahmen**: Format Lightning Talk, FUENF Minuten. Und der Stand, ohne
      Beschoenigung: der Call for Speakers der Nextcloud Community Conference 2026 ist seit
      dem 03.08.2026 geschlossen; angenommene Sprecher liefern Folien bis zum 09.09.; die
      Contributor Week vom 21. bis 25.09. ist ein anderes Format mit anderem Ton. Dieser
      Entwurf ist wiederverwendbares Material fuer Gespraeche vor Ort, fuer eine spaetere
      Einreichung und fuer den Prototype-Fund-Antrag. **Ob und wo eingereicht wird, entscheidet
      der Owner.** Dieser Plan reicht nichts ein und kontaktiert niemanden.
    - **Folienabfolge**, auf fuenf Minuten gerechnet: eine Titelfolie, eine Problemfolie, vier
      Folien fuer die vier Differenzierer (Store-Ein-Klick, spec-konformes OAuth,
      Per-User-Verwaltung, kann konstruktionsbedingt nichts zerstoeren), eine Demofolie mit
      dem Verweis auf `docs/conference-demo.md`, eine Abschlussfolie mit Repository und
      Lizenz. Je Folie: die Ueberschrift, hoechstens drei Stichpunkte, und darunter der
      **Sprechzettel** als ausformulierter Text mit einer Sekundenangabe. Die Summe der
      Sekundenangaben liegt unter 300.
    - **Faktentreue**: jede Behauptung ueber das Produkt haelt sich an das, was belegt ist. Die
      Ein-Klick-Folie sagt genau, was 06-07 gemessen hat, in dem Ausgang, der gemessen wurde.
      Die OAuth-Folie darf CIMD und die Loopback-Regel nennen, weil 06-09 sie live belegt hat.
      Die Zerstoerungsfolie stuetzt sich auf die Schreibgrenzen, die im Repo als Gate laufen.
      Keine Zahl ohne Fundstelle, kein Feature ohne Beleg.
    - **Stil**: keine Emojis, keine Em-Dashes, keine Superlative ohne Zahl. Der Ton ist der des
      Statusabschnitts der README: Fakten mit Fundstelle.
    - **Ein Abschnitt "If this becomes a 10 minute slot"** wird NICHT geschrieben: dieses
      Format gibt es nicht.

    Wenn ein vorbereiteter Einreichungstext sinnvoll erscheint, wird er als klar
    gekennzeichneter Entwurf am Ende der Datei abgelegt, mit dem Satz, dass der Owner sendet.
    Kein Formular wird ausgefuellt, keine Mail geschrieben, kein Kontakt angeschrieben.
  </action>
  <verify>
    <automated>test -s docs/conference-talk.md && grep -c "^## " docs/conference-talk.md</automated>
  </verify>
  <acceptance_criteria>
    - Die Datei nennt im Kopf das Format (Lightning Talk, fuenf Minuten) und den geschlossenen CfP mit Datum 03.08.2026
    - Die Datei sagt ausdruecklich, dass die Einreichung Owner-Entscheid ist und dass mit diesem Plan nichts eingereicht und niemand kontaktiert wurde
    - Es gibt Folien fuer alle vier Differenzierer, je mit hoechstens drei Stichpunkten und einem ausformulierten Sprechzettel
    - Jede Folie traegt eine Sekundenangabe, und die Summe ist kleiner als 300
    - `grep -in "10 minute\|ten minute" docs/conference-talk.md | wc -l` liefert 0
    - Die Ein-Klick-Aussage stimmt mit dem Befund aus 06-07 ueberein (woertlicher Vergleich mit der SUMMARY beziehungsweise der Messdatei)
    - `grep -n "—\|–" docs/conference-talk.md | wc -l` liefert 0; die Datei enthaelt keine Emojis
    - `grep -in "archiv" docs/conference-talk.md | wc -l` liefert 0
    - `git diff --stat pyproject.toml uv.lock` ist leer (kein Folien-Werkzeug installiert)
    - Kein Formular wurde abgesendet, keine Mail erstellt: die SUMMARY sagt es und nennt keinen gesendeten Text
  </acceptance_criteria>
  <done>Der Lightning-Talk-Entwurf liegt vor, auf fuenf Minuten gerechnet, faktentreu zu den Messungen dieser Phase, und ohne jede Einreichungsaktion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runbook -> fremder Nachfahrer | Wer das Drehbuch benutzt, hat unser Vorwissen nicht und tippt, was dort steht |
| Demo-Instanz -> Publikum | Auf einer Buehne ist der Bildschirminhalt oeffentlich |
| Conference-Material -> Oeffentlichkeit | Beide Dateien liegen im oeffentlichen Repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-63 | Information Disclosure | Credential in einem kopierbaren Kommando | mitigate | Kein Credential und kein Beispielwert in Code-Bloecken; die `HP_SHARED_KEY`-Zeilen erzeugen beziehungsweise lesen ihn, sie schreiben ihn nicht hin; Grep-Kriterium |
| T-06-64 | Information Disclosure | Bildschirminhalt der Demo zeigt fremde Daten | mitigate | Die Demo laeuft auf der Wegwerf-Instanz mit `jane` und der bekannten Fixture; das Drehbuch sagt ausdruecklich, dass keine Owner-Instanz benutzt wird |
| T-06-65 | Repudiation | Der Talk behauptet mehr als gemessen ist | mitigate | Jede Produktbehauptung wird gegen die Messdateien dieser Phase geprueft; die Ein-Klick-Folie folgt dem Ausgang aus 06-07 |
| T-06-66 | Tampering | Ungewollte Einreichung oder Kontaktaufnahme | mitigate | Der Plan reicht nichts ein und schreibt niemanden an; ein etwaiger Einreichungstext bleibt als gekennzeichneter Entwurf liegen, der Owner sendet |
| T-06-67 | Denial of Service | Ein Drehbuch, das auf der Buehne nicht funktioniert | mitigate | Ein vollstaendiger Durchlauf mit Zeiten je Schritt, Korrekturen im Drehbuch statt Entschuldigungen im Protokoll, plus ein Rueckfallabschnitt |
| T-06-68 | Denial of Service | Instanzzustand bleibt nach der Demo verstellt | mitigate | Endzustand belegt: beendete Verbindung ist beendet, Schalter im Ausgangszustand, App `enabled` |
| T-06-SC | Tampering | npm/pip/cargo installs (Folien-Toolchain) | mitigate | Folien und Sprechzettel sind Markdown im Repository. Wuerde ein Paket noetig, gilt das Package-Legitimacy-Gate voll (slopcheck, PyPI-JSON-API statt HTML-Projektseite, Owner-Freigabe); die Empfehlung der Recherche ist, die Frage nicht entstehen zu lassen. `pyproject.toml` und `uv.lock` bleiben unveraendert |
</threat_model>

<verification>
- `docs/conference-demo.md` und `docs/conference-talk.md` existieren, sind Englisch, ohne Emojis und ohne Em-Dashes
- Das Messprotokoll belegt einen vollstaendigen Durchlauf mit Zeiten je Schritt und die Wirkung von Per-User-Schalter und Widerruf
- `uv run pytest tests/unit -q` gruen (Text- und Vokabular-Gates)
- `git diff --stat pyproject.toml uv.lock` leer
- `occ app_api:app:list` meldet die App als `enabled`, die drei OAuth-Schalter stehen im Ausgangszustand
</verification>

<success_criteria>
- Die Demo-Strecke ist reproduzierbar und einmal wie geschrieben durchgefahren
- Verbindung, Werkzeugaufrufe, Per-User-Verwaltung und Widerruf sind je eine Station mit belegter Wirkung
- Der Lightning-Talk-Entwurf liegt als Folien plus Sprechzettel vor, fuenf Minuten, faktentreu
- Nichts wurde eingereicht und niemand kontaktiert
</success_criteria>

<output>
Create `.planning/phases/06-h-rtung-eigennachweise-und-conference-reife/06-10-SUMMARY.md` when done
</output>
