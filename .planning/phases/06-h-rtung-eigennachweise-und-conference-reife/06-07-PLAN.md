---
phase: 06-h-rtung-eigennachweise-und-conference-reife
plan: 07
type: execute
wave: 5
depends_on: ["06-06"]
files_modified:
  - compose.exapp.yml
  - .planning/phases/06-h-rtung-eigennachweise-und-conference-reife/06-07-MEASUREMENTS.md
  - docs/exapp-install.md
  - docs/contrib/app-api-971-comment.md
  - README.md
  - README.de.md
  - README.fr.md
  - appinfo/info.xml
autonomous: true
requirements: [EXAPP-06]

must_haves:
  truths:
    - "Die Messinstanz laeuft nachweislich auf 34.0.3, belegt durch occ status und nicht durch einen Docker-Tag"
    - "Der laufende Connector traegt den Stand des Repositories, nicht 0.1.1"
    - "Nutzer jane und die zwei bestehenden OAuth-Verbindungen haben das Upgrade ueberlebt"
    - "Es ist gemessen, OB die Store-Oberflaeche fuer diese ExApp einen Install- und einen Remove-Knopf zeigt"
    - "Doku, die drei READMEs und der Store-Text sagen danach genau das Gemessene"
  artifacts:
    - path: ".planning/phases/06-h-rtung-eigennachweise-und-conference-reife/06-07-MEASUREMENTS.md"
      provides: "Messprotokoll mit occ status als erster Zeile, Konto der Messung, Store-Cache-Schritt und Rohbelegen"
    - path: "docs/exapp-install.md"
      provides: "Der 34.0.3-Befund neben der historischen 34.0.2-Ursachenkette"
    - path: "docs/contrib/app-api-971-comment.md"
      provides: "Entwurf eines Upstream-Kommentars, nur im negativen Fall; Owner sendet"
  key_links:
    - from: "compose.exapp.yml"
      to: "nextcloud:34.0.3-apache"
      via: "Image-Pin statt gleitendem Tag 34-apache"
      pattern: "34.0.3-apache"
    - from: ".planning/phases/06-h-rtung-eigennachweise-und-conference-reife/06-07-MEASUREMENTS.md"
      to: "docs/exapp-install.md"
      via: "Jeder Doku-Satz zitiert eine Zeile des Protokolls"
      pattern: "34.0.3"
---

<objective>
Die Messtopologie auf 34.0.3 und auf den Repository-Stand bringen, und danach messen, ob die
Store-Oberflaeche einen Install- und einen Remove-Knopf fuer ExApps zeigt.

Purpose: Zwei Dinge tragen in diesem Moment alten Zustand und wuerden jede Messung dieser
Phase verfaelschen: die Instanz laeuft auf 34.0.2.1 hinter dem gleitenden Tag `34-apache`,
und der ExApp-Container laeuft mit Image `mcp_connector:0.1.1`, also ohne die
Teilregistrierung aus a80af0a und ohne alles aus 06-01 bis 06-06. Dieser Plan ist deshalb
die Voraussetzung aller folgenden Live-Messungen. Und EXAPP-06 selbst ist als FRAGE
formuliert, nicht als Bestaetigung: der Upstream-Fix ist als Merge und Milestone verifiziert,
aber ein Datei-fuer-Datei-Vergleich der `appstore`-App zwischen dem 34.0.2- und dem
34.0.3-Image zeigt ausserhalb der Uebersetzungen keinen Unterschied. Nur der Blick in die
laufende Oberflaeche entscheidet, und kein Doku- oder Store-Satz darf vor dieser Messung
geschrieben werden.
Output: `compose.exapp.yml` auf `nextcloud:34.0.3-apache` gepinnt, eine Messdatei nach dem
Muster der Phase 5, und der Doku-Nachlauf in genau dem Ausgang, der gemessen wurde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-h-rtung-eigennachweise-und-conference-reife/06-CONTEXT.md
@.planning/phases/06-h-rtung-eigennachweise-und-conference-reife/06-RESEARCH.md
@.planning/phases/06-h-rtung-eigennachweise-und-conference-reife/06-PATTERNS.md
@.planning/phases/06-h-rtung-eigennachweise-und-conference-reife/06-06-SUMMARY.md
</context>

<interfaces>
Der laufende Zustand, in der Recherche am 2026-08-20 gemessen (STATE.md weicht ab, dort steht
"vollstaendig entfernt"; die Messung gewinnt):

| Was | Wert |
|-----|------|
| Compose-Projekt | `nc-mcp-exapp`, Datei `compose.exapp.yml`, Port 8081 |
| Container | `nc-mcp-exapp-nc`, `-harp`, `-caddy`, `-registry`, `nc_app_mcp_connector` |
| Nextcloud | `nextcloud:34-apache`, `occ status` sagt 34.0.2.1 |
| ExApp | Image `127.0.0.1:5000/mcp_connector:0.1.1`, Repo steht auf 0.1.2 |
| Apps | `app_api` 34.0.0, `appstore` 1.0.0 |
| Inhalt | Nutzer `jane` (Jane Fischer), zwei echte OAuth-Verbindungen (Claude Desktop, Open WebUI ueber DCR und PKCE), Fixture aus 05-03 |
| Owner-Instanzen | `nc-mcp-test` und `findling-nextcloud`, unbeteiligt, muessen unberuehrt bleiben |
| Lokales Image | `nextcloud:34.0.3-apache` liegt vor, 2.21 GB, an `findling-nextcloud` als 34.0.3.2 verifiziert |

`HP_SHARED_KEY` lebt nur in der Umgebung der laufenden Container. Gegen bereits laufende
Container wird er ZURUECKGELESEN, nie neu erzeugt, sonst scheitert jeder `docker compose`-Aufruf
an der Interpolation und die Registrierung an `require_hex64`:
`export HP_SHARED_KEY=$(docker inspect nc-mcp-exapp-harp --format '{{range .Config.Env}}{{println .}}{{end}}' | grep '^HP_SHARED_KEY=' | cut -d= -f2)`
und in DERSELBEN Zeile weiterarbeiten.

`occ` steht in der Messdatei als Kuerzel fuer
`docker exec -u www-data nc-mcp-exapp-nc php occ`. Der Lasttest-Befund der Phase 5 gilt fort:
`docker exec` mit festem Containernamen statt `docker compose exec`, weil jeder
compose-Aufruf `HP_SHARED_KEY` in der Umgebung verlangt.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Volume-Sicherung, Upgrade auf 34.0.3, Rebuild auf Repo-Stand</name>
  <files>compose.exapp.yml, .planning/phases/06-h-rtung-eigennachweise-und-conference-reife/06-07-MEASUREMENTS.md</files>
  <read_first>
    - compose.exapp.yml (vollstaendig; Zeile 46 traegt den gleitenden Tag `nextcloud:34-apache`)
    - scripts/bootstrap_exapp.sh (vollstaendig; was es baut, registriert und setzt, insbesondere `NC_MCP_PUBLIC_URL`)
    - .planning/STATE.md (Abschnitt "Pending Todos", die zwei Absaetze zur ExApp-Topologie mit der Wiederanfahr-Prozedur im Wortlaut)
    - .planning/phases/06-h-rtung-eigennachweise-und-conference-reife/06-RESEARCH.md (Runtime State Inventory vollstaendig, Pitfall 6, Assumption A5 mit der Empfehlung Volume-Backup)
    - .planning/phases/06-h-rtung-eigennachweise-und-conference-reife/06-PATTERNS.md (§10 zum Compose-Pin, §11 zum Messdatei-Muster)
  </read_first>
  <action>
    Erst das Muster der Messdatei holen, sonst wird die Form neu erfunden. Die Dateien der
    Phasen 3 bis 5 sind mit Commit `d3eb627` geloescht und liegen nur in der Historie:
    `git show d3eb627^:.planning/phases/05-hardening-und-store-einreichung/05-07-MEASUREMENTS.md`
    und
    `git show d3eb627^:.planning/phases/05-hardening-und-store-einreichung/05-08-MEASUREMENTS.md`.

    Dann, in dieser Reihenfolge:

    1. `HP_SHARED_KEY` aus dem laufenden HaRP-Container zuruecklesen (Kommando im
       interfaces-Block) und in derselben Shell-Zeile weiterarbeiten.
    2. **Volume-Sicherung vor dem Upgrade.** Assumption A5 (In-Place-Upgrade innerhalb der
       Minor-Linie ohne Verlust von jane und den zwei Verbindungen) ist plausibel, aber auf
       dieser Topologie nicht gemessen. Gesichert werden
       `nc-mcp-exapp_nextcloud-exapp-data` und das ExApp-Volume
       `nc_app_mcp_connector_data` (letzteres traegt `oauth.sqlite3` mit den zwei
       Verbindungen), per Wegwerf-Container mit `tar` in eine Datei ausserhalb des
       Repositories. Der Pfad der Sicherung steht in der Messdatei, ihr Inhalt nicht.
    3. Vorzustand festhalten: `occ status`, `occ app:list` (Versionen von `app_api` und
       `appstore`), `occ app_api:app:list`, `docker inspect` auf den ExApp-Container fuer den
       Image-Digest, und die Zahl der Zeilen in `authorizations` sowie der Nutzername jane
       als Beweis, dass die Demo-Substanz vor dem Upgrade da war.
    4. `compose.exapp.yml` Zeile 46 von `nextcloud:34-apache` auf `nextcloud:34.0.3-apache`
       pinnen. Der gleitende Tag ist genau die Falle: das lokal vorhandene Image unter
       `34-apache` ist vom 05.08. und traegt 34.0.2.1, obwohl der Tag auf Docker Hub am
       17.08. aktualisiert wurde, und Docker zieht ohne `pull` nicht nach. Ein Kommentar an
       der Zeile sagt, warum hier eine Patch-Version steht und kein gleitender Tag.
    5. `docker compose -p nc-mcp-exapp -f compose.exapp.yml up -d --wait`, danach
       `occ upgrade` und `occ status`.
    6. Den ExApp-Container auf den Repository-Stand bringen: `bash scripts/bootstrap_exapp.sh`
       baut das Image neu und setzt `NC_MCP_PUBLIC_URL`. Ohne das Neubauen laeuft ein
       veraltetes Image, und CLIENT-04 wuerde die Teilregistrierung als kaputt messen
       (Pitfall 8).
    7. Nachzustand festhalten: `occ status` (muss 34.0.3.x sagen), `occ app_api:app:list`
       (Version und `enabled`), der neue Image-Digest, jane und die Zahl der Zeilen in
       `authorizations` unveraendert.

    Die Messdatei entsteht nach dem geholten Muster: Kopf mit Datum samt Uhrzeit und
    Zeitzone, Rechner, und dem Satz, dass alles aus einem Lauf stammt; dann die Tabelle
    "Topologie des Laufs" mit Compose-Datei, Projektname, Nextcloud-Image MIT Digest,
    Connector-Version MIT Image-Digest, `NC_MCP_PUBLIC_URL`, und der Zeile, dass
    `nc-mcp-test` und `findling-nextcloud` durchliefen und unberuehrt blieben, weil kein
    Kommando dieses Laufs sie nennt. Danach je Behauptung ein numerierter Abschnitt mit
    Rohbeleg. Kein Credential steht im Dokument, auch kein Wegwerf-Credential (T-05-39).

    Geht das Upgrade schief, wird aus der Sicherung zurueckgespielt und der Fehlschlag mit
    Rohbeleg in der Messdatei festgehalten; ein verlorener jane ist kein Zwischenstand, den
    man weglaesst.
  </action>
  <verify>
    <automated>docker exec -u www-data nc-mcp-exapp-nc php occ status | grep -E "versionstring|installed" && docker exec -u www-data nc-mcp-exapp-nc php occ app_api:app:list</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "34.0.3-apache" compose.exapp.yml | wc -l` liefert 1; `grep -n "nextcloud:34-apache" compose.exapp.yml | wc -l` liefert 0
    - `occ status` meldet eine `versionstring` mit `34.0.3` und `installed: true`; die Zeile steht als ERSTE Behauptung in der Messdatei
    - `occ app_api:app:list` meldet `mcp_connector` als `enabled` mit der Version aus `appinfo/info.xml`
    - Der Image-Digest des ExApp-Containers in der Messdatei unterscheidet sich vom Vorzustand (der Rebuild hat stattgefunden)
    - Nutzer `jane` existiert weiter (`occ user:list` enthaelt ihn) und die Zeilenzahl in `authorizations` ist gleich dem Vorzustand; beide Zahlen stehen mit Rohbeleg in der Messdatei
    - Die Messdatei nennt den Pfad der Volume-Sicherung, nicht deren Inhalt
    - Die Messdatei enthaelt kein Credential, auch keinen `HP_SHARED_KEY` und kein App-Passwort: `grep -inE "HP_SHARED_KEY=|APP_SECRET=|[a-z0-9]{5}-[a-z0-9]{5}-[a-z0-9]{5}" <Messdatei> | wc -l` liefert 0
    - Die Messdatei nennt `nc-mcp-test` und `findling-nextcloud` als unberuehrt; `docker ps` belegt, dass beide durchliefen
    - Die Topologie-Tabelle nennt Versionen, nicht Tags (Pitfall 6)
  </acceptance_criteria>
  <done>Die Messtopologie laeuft belegt auf 34.0.3 mit dem Repository-Stand des Connectors, jane und die zwei Verbindungen sind erhalten, und alles steht mit Rohbeleg in der Messdatei.</done>
</task>

<task type="auto">
  <name>Task 2: Der UI-Smoke, mit Cache-Schritt und Gegenprobe</name>
  <files>.planning/phases/06-h-rtung-eigennachweise-und-conference-reife/06-07-MEASUREMENTS.md</files>
  <read_first>
    - .planning/phases/06-h-rtung-eigennachweise-und-conference-reife/06-07-MEASUREMENTS.md (Stand nach Task 1)
    - docs/exapp-install.md (Zeilen 470-540; die Ursachenkette des 34.0.2-Befunds und die bereits vorhandene Vorwaerts-Zeile "Fixed upstream in Nextcloud 34.0.3")
    - .planning/phases/06-h-rtung-eigennachweise-und-conference-reife/06-RESEARCH.md (Pitfall 7 vollstaendig, Open Question 1 mit der md5-Gegenprobe, Assumption A6 zu app_api)
    - .planning/STATE.md (die Phase-05-Entscheidung zum AppAPI-Store-Cache: Ueberschreiben mit timestamp 0, nie Loeschen, sonst endet jedes folgende AppAPI-Kommando mit GenericFileException)
  </read_first>
  <action>
    Der Smoke laeuft in dieser Reihenfolge, und die Reihenfolge steht in der Messdatei:

    1. `occ upgrade` ist aus Task 1 erledigt; hier nur der Beleg, dass er lief.
    2. Den AppAPI-Store-Cache verwerfen, indem die Cache-Datei mit `timestamp 0`
       UEBERSCHRIEBEN wird, nie durch Loeschen: ein geloeschtes File laesst jedes folgende
       AppAPI-Kommando mit `GenericFileException` enden (Phase-05-Entscheidung).
    3. Gegenprobe VOR dem Blick in die Oberflaeche: `occ app_api:app:list` muss
       `mcp_connector` als `enabled` melden, waehrend gemessen wird. Sonst misst man eine
       leere Liste und nennt sie einen Frontend-Befund.
    4. Als ADMIN im Browser auf `/settings/apps` (der Zugang fuer Normalnutzer ist in NC 34
       bewusst entfernt, nextcloud/server#60495; unter welchem Konto gemessen wird, ist
       deshalb eine Pflichtangabe). Harter Reload. Der Browser-Schritt laeuft mit dem im
       Projekt vorhandenen Playwright-Muster; der Screenshot geht nach `docs/screenshots/`
       nur dann, wenn er ohne Credential und ohne fremde Inhalte auskommt, sonst bleibt er
       ausserhalb des Repositories und die Messdatei beschreibt ihn.
    5. Festhalten, was zu sehen ist, und zwar getrennt fuer beide Knoepfe: erscheint die
       ExApp in der Liste, gibt es einen Install-Knopf, gibt es einen Remove-Knopf. Ein
       "sieht gut aus" ist kein Befund; die Messdatei nennt den sichtbaren Text der
       Schaltflaechen.
    6. Bleibt die Oberflaeche leer, gehoert die md5-Gegenprobe der Recherche in die Datei
       (`apps/appstore` zwischen dem 34.0.2- und dem 34.0.3-Image, App-Version in beiden
       1.0.0, der einzige geaenderte Frontend-Chunk ohne `exapp`-Vorkommen), plus der zweite
       Versuch aus Assumption A6: ein `app_api`-Update, falls eines verfuegbar ist, und das
       Ergebnis danach.
    7. Das Unvorhergesagte bekommt einen eigenen Absatz, wie im Muster von 05-07.

    Beide Ausgaenge sind gueltige Ergebnisse dieses Plans. Der positive schliesst EXAPP-06 mit
    einer Bestaetigung, der negative mit einem belegten Negativbefund. Was NICHT passieren
    darf, ist ein Doku-Satz ohne Deckung: der Titelsatz "Fixed upstream in Nextcloud 34.0.3"
    steht heute schon in `docs/exapp-install.md` und ist bis zu dieser Messung unbelegt.
  </action>
  <verify>
    <automated>docker exec -u www-data nc-mcp-exapp-nc php occ app_api:app:list | grep -i "mcp_connector"</automated>
  </verify>
  <acceptance_criteria>
    - Die Messdatei nennt als erste Zeile des Abschnitts die `occ status`-Versionszeile, nicht den Docker-Tag
    - Die Messdatei nennt das Konto, unter dem gemessen wurde, und dass es ein Admin-Konto ist
    - Die Messdatei nennt den Cache-Schritt woertlich als Ueberschreiben mit `timestamp 0` und belegt, dass danach ein `occ app_api:app:list` weiter funktioniert (keine `GenericFileException`)
    - Die Gegenprobe `occ app_api:app:list` mit `mcp_connector [enabled]` steht mit Rohbeleg in der Datei, zeitlich VOR dem Oberflaechen-Blick
    - Der Befund ist fuer Install-Knopf UND Remove-Knopf getrennt beantwortet, jeweils mit dem sichtbaren Text der Schaltflaeche oder mit "nicht vorhanden"
    - Im negativen Fall enthaelt die Datei die md5-Gegenprobe und das Ergebnis des `app_api`-Zweitversuchs
    - Kein Screenshot im Repository enthaelt ein Credential oder fremde Nutzerinhalte
    - Die Datei enthaelt einen Absatz "Nicht vorhergesagt und darum hier festgehalten", auch wenn er nur einen Satz hat
  </acceptance_criteria>
  <done>Es ist mit Rohbeleg gemessen, ob die Store-Oberflaeche auf 34.0.3 einen Install- und einen Remove-Knopf fuer diese ExApp zeigt, unter benanntem Konto und nach dem Cache-Schritt.</done>
</task>

<task type="auto">
  <name>Task 3: Doku, drei READMEs und Store-Text sagen das Gemessene</name>
  <files>docs/exapp-install.md, README.md, README.de.md, README.fr.md, appinfo/info.xml, docs/contrib/app-api-971-comment.md</files>
  <read_first>
    - .planning/phases/06-h-rtung-eigennachweise-und-conference-reife/06-07-MEASUREMENTS.md (Stand nach Task 2, vollstaendig; jeder Satz dieses Tasks zitiert eine Zeile daraus)
    - docs/exapp-install.md (Zeilen 470-540 vollstaendig; der Abschnitt "Nextcloud 34 has no interface for installing or removing an ExApp" mit seiner Vorwaerts-Zeile)
    - README.md, README.de.md, README.fr.md (jeweils der Statusabschnitt und der Abschnitt zur Installation aus dem Store; die drei Dateien tragen identische Ueberschriftenfolgen)
    - appinfo/info.xml (Zeilen 10-60; die drei `<description>`-Bloecke en, de, fr, und der Kommentar Zeilen 12-25, der erklaert, auf welchem kleineren gemeinsamen Nenner sie stehen)
    - docs/contrib/ (der bestehende PR-Text als Formvorbild fuer einen Upstream-Beitrag)
    - .planning/phases/06-h-rtung-eigennachweise-und-conference-reife/06-PATTERNS.md (§12 vollstaendig, insbesondere die fuenf Eigenschaften des Musterabsatzes und die Trennung der Dreisprachigkeit)
  </read_first>
  <action>
    In `docs/exapp-install.md` wird der Abschnitt zum Store-UI-Befund so ergaenzt, dass die
    Historie stehenbleibt und der neue Befund datiert danebentritt. Die Struktur ist die des
    bestehenden Absatzes: Zustand im Titel, alter Befund datiert erhalten, was sich NICHT
    geaendert hat ausdruecklich benannt (occ bleibt auf jeder Version der zuverlaessige Weg),
    Roh-Zahlen im Code-Block.
    - Positiver Ausgang: der Titelsatz "Fixed upstream in Nextcloud 34.0.3" bekommt seinen
      Beleg ("verified on 34.0.3.x on 2026-08-XX: the Install button reads ... and the Remove
      button reads ..."), mit Verweis auf die Messdatei.
    - Negativer Ausgang: der Titelsatz wird ENTFERNT oder in eine Frage umgeschrieben, und
      der negative Befund tritt mit der md5-Gegenprobe daneben. Ein unbelegter "Fixed
      upstream"-Satz darf nicht stehenbleiben; das ist die Locked Decision "Doku sagt GENAU
      das Gemessene".

    Die drei READMEs und der Store-Text ziehen GEMEINSAM nach, weil sie nur gemeinsam
    nachgezogen werden duerfen. Betroffen ist genau die Aussage zur Installation per Klick:
    - Positiver Ausgang: die Ein-Klick-Formulierung darf woertlich wahr werden, mit der
      gemessenen Nextcloud-Version als Bedingung im Satz.
    - Negativer Ausgang: die vorsichtige Formulierung bleibt, ergaenzt um den datierten
      Stand. Kein Ein-Klick-Versprechen ohne Deckung.
    Der Store-Text sind die drei `<description>`-Bloecke in `appinfo/info.xml` und bleibt auf
    dem kleineren gemeinsamen Nenner der Instanz-Ansicht: Absaetze durch Leerzeilen, kein
    Backtick, keine Tabelle, kein Bild, keine Linie, kein HTML, weil dompurify dort alles
    andere entfernt statt es zu degradieren. Die deutsche und die franzoesische Fassung
    tragen echte Umlaute beziehungsweise Accents und Cedille und keine Em-Dashes.

    Nur im negativen Ausgang entsteht `docs/contrib/app-api-971-comment.md`: der Entwurf
    eines Kommentars an `nextcloud/app_api#971` mit dem gemessenen Befund, der Version, dem
    Konto, dem Cache-Schritt und der md5-Gegenprobe, im Ton der bestehenden Upstream-Texte
    dieses Projekts. Der Entwurf wird NICHT gesendet und nirgends gepostet: Einreichungen
    loest immer der Owner aus. Die Datei sagt das in ihrer ersten Zeile. Im positiven Ausgang
    entsteht sie nicht, und die SUMMARY sagt, warum nicht.

    Die Version im Manifest wird nicht angehoben und kein Release ausgeloest: ein
    Store-Release braucht Owner-Freigabe, und `release.yml` triggert auf `v*`-Tags.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_exapp_env_setup.py -q && python -c "import xml.etree.ElementTree as E; E.parse('appinfo/info.xml'); print('manifest ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Jeder neue Satz in `docs/exapp-install.md` zum 34.0.3-Verhalten laesst sich auf eine numerierte Behauptung der Messdatei zurueckfuehren; die Datei wird namentlich verlinkt
    - Im negativen Fall: `grep -n "Fixed upstream in Nextcloud 34.0.3" docs/exapp-install.md | wc -l` liefert 0
    - Im positiven Fall: derselbe Satz traegt eine Versionsangabe mit `34.0.3` und ein Datum
    - Die drei READMEs tragen weiter identische Ueberschriftenfolgen: ein Vergleich der `^#{1,3} `-Zeilen der drei Dateien ergibt dieselbe Anzahl und dieselbe Reihenfolge der Abschnitte
    - Die Aussage zur Klick-Installation ist in allen DREI READMEs und in allen DREI `<description>`-Bloecken inhaltlich gleich; keine Datei bleibt zurueck
    - `grep -n "—\|–" README.md README.de.md README.fr.md appinfo/info.xml docs/exapp-install.md | wc -l` liefert 0
    - Die deutsche Fassung enthaelt echte Umlaute (`grep -c "ä\|ö\|ü\|ß" README.de.md` ist grösser als 0) und keine ASCII-Ersatzformen wie `ae`/`oe`/`ue` in deutschen Woertern
    - `grep -in "archiv" README.md README.de.md README.fr.md appinfo/info.xml docs/exapp-install.md | wc -l` liefert 0 (Vokabular-Gate, gilt auch fuer das Manifest)
    - Die drei `<description>`-Bloecke enthalten keinen Backtick, kein `<`, keine Tabellenzeile und keine horizontale Linie
    - `grep -n "<version>" appinfo/info.xml` ist unveraendert; kein `v*`-Tag wurde erzeugt
    - Im negativen Fall existiert `docs/contrib/app-api-971-comment.md` und ihre erste Zeile sagt, dass der Owner sendet; im positiven Fall existiert die Datei nicht
    - `uv run pytest tests/unit -q` gruen (Text-, Vokabular- und Manifest-Gates)
  </acceptance_criteria>
  <done>Doku, die drei READMEs und die drei Store-Beschreibungen sagen genau den gemessenen Ausgang, gemeinsam nachgezogen; im negativen Fall liegt ein Upstream-Kommentar als Entwurf bereit, den der Owner sendet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Messlauf -> Owner-Instanzen | Auf demselben Docker-Host laufen zwei taeglich genutzte Instanzen; kein Kommando dieses Laufs darf sie beruehren |
| Messdatei -> oeffentliches Repository | `.planning/` ist oeffentlich (Owner-Entscheidung Phase 1), also ist jede Zeile der Messdatei ein oeffentliches Artefakt |
| Store-Text -> jeder Nextcloud-Admin | Die drei `<description>`-Bloecke sind der einzige Text, den ein Nutzer ohne Repository-Besuch sieht |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-41 | Information Disclosure | Messdatei mit Credential | mitigate | Kein Credential im Dokument, auch kein Wegwerf-Credential (T-05-39); Grep-Kriterium auf `HP_SHARED_KEY`, `APP_SECRET` und die App-Passwort-Form |
| T-06-42 | Denial of Service | Upgrade verliert jane und die zwei Verbindungen | mitigate | Volume-Sicherung vor dem Upgrade (Assumption A5), Vorzustand mit Zeilenzahl und Nutzernamen belegt, Rueckspielweg beschrieben |
| T-06-43 | Tampering | Falscher Befund durch gleitenden Tag | mitigate | Pin auf `nextcloud:34.0.3-apache`, `occ status` als erste Zeile der Messdatei, Versionen statt Tags in der Topologie-Tabelle |
| T-06-44 | Tampering | Falscher Befund durch Store-Cache oder falsches Konto | mitigate | Cache durch Ueberschreiben mit `timestamp 0` verwerfen, Messung als Admin, Gegenprobe `occ app_api:app:list` vor dem Blick |
| T-06-45 | Tampering | Messung gegen 0.1.1 statt Repo-Stand | mitigate | Rebuild ueber `scripts/bootstrap_exapp.sh`, Image-Digest vor und nach dem Rebuild in der Messdatei |
| T-06-46 | Repudiation | Ein Ein-Klick-Versprechen ohne Deckung | mitigate | Beide Ausgaenge sind geplant; im negativen Fall wird der unbelegte Titelsatz entfernt; jeder neue Satz zitiert eine numerierte Behauptung |
| T-06-47 | Tampering | Ungewolltes Store-Release | mitigate | Keine Versionsanhebung, kein `v*`-Tag; ein Release braucht Owner-Freigabe |
| T-06-48 | Denial of Service | Leeres Manifest-Element bricht den Store-Upload | mitigate | Manifest bleibt wohlgeformt und die bestehenden Manifest-Gates laufen mit |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | Kein Paket. Docker-Images kommen aus der lokalen Ablage beziehungsweise von `docker.io/library/nextcloud`, dessen Tag in der Recherche gegen die Docker-Hub-API verifiziert ist |
</threat_model>

<verification>
- `occ status` auf `nc-mcp-exapp-nc` meldet 34.0.3.x, `occ app_api:app:list` meldet `mcp_connector [enabled]` mit der Repo-Version
- `docker ps` belegt, dass `nc-mcp-test` und `findling-nextcloud` unberuehrt durchliefen
- `uv run pytest tests/unit -q` gruen (Text-, Vokabular- und Manifest-Gates)
- `uv run ruff check .` und `uv run ruff format --check .` gruen
- Die Messdatei traegt Topologie-Tabelle, numerierte Behauptungen mit Rohbeleg, Gegenproben und einen Absatz zum Unvorhergesagten
</verification>

<success_criteria>
- EXAPP-06 ist beantwortet, in dem Ausgang, der gemessen wurde, mit getrennter Aussage zu Install- und Remove-Knopf
- Die Messtopologie laeuft auf 34.0.3 mit dem Repository-Stand und hat jane samt beider Verbindungen behalten
- Doku, drei READMEs und drei Store-Beschreibungen sagen dasselbe und nur das Gemessene
- Im negativen Fall liegt der Upstream-Kommentar als Entwurf bereit; gesendet wird er nicht
</success_criteria>

<output>
Create `.planning/phases/06-h-rtung-eigennachweise-und-conference-reife/06-07-SUMMARY.md` when done
</output>
