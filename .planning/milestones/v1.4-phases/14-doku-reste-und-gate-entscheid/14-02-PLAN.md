---
phase: 14-doku-reste-und-gate-entscheid
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/store-submission.md
  - .planning/milestones/v1.3-phases/13-cimd-nachmessung-und-release-0-1-9/13-VERIFICATION.md
  - tests/unit/test_exapp_env_setup.py
autonomous: true
requirements: [DOC-01, DOC-02, SEC-03]

must_haves:
  truths:
    - "Die Nachweistabelle in docs/store-submission.md läuft in der Uhrzeit vorwärts, jede Zeile mit Zeitstempel steht hinter der früheren"
    - "Die 18:30Z-Zeile verweist auf den getaggten Commit 685295d korrekt zurück: eine Änderung hinter dem 22471c1 der 18:21Z-Zeile, gepusht vom git push dieses Schritts vor dem Tag"
    - "Keine bestehende Proof-Aussage wurde verfälscht: außer der einen präzisierten Zeile ist jede Tabellenzeile wortgleich zu vorher"
    - "Der Verweis auf die interne Planungsnotiz in docs/store-submission.md ist als interne Notiz gekennzeichnet, die verschwinden darf"
    - "Die abgelegte 13-VERIFICATION trägt eine datierte Nachtrags-Notiz mit dem Entfernungs-Commit f9b3d2d, und keine ihrer bestehenden Zeilen wurde geändert"
    - "Die Reichweite des Vokabular-Gates ist entschieden, im Gate kommentiert und an eine prüfbare Eigenschaft gebunden statt nur an Prosa"
  artifacts:
    - path: "docs/store-submission.md"
      provides: "chronologische Nachweistabelle mit präzisiertem Rückverweis der Tag-Zeile"
      contains: "685295d"
    - path: ".planning/milestones/v1.3-phases/13-cimd-nachmessung-und-release-0-1-9/13-VERIFICATION.md"
      provides: "datierter Nachtrag zur entfernten Datei, Befund unverändert"
      contains: "f9b3d2d"
    - path: "tests/unit/test_exapp_env_setup.py"
      provides: "dokumentierte und geprüfte Reichweitengrenze des Vokabular-Gates gegenüber .planning"
      contains: "def test_the_vocabulary_gate_stops_at_the_internal_planning_area"
  key_links:
    - from: "docs/store-submission.md 18:30Z-Zeile"
      to: "git tag v0.1.9 auf 685295d"
      via: "Rückverweis auf die 18:21Z-Zeile mit 22471c1, eine Änderung davor"
      pattern: "22471c1"
    - from: "tests/unit/test_exapp_env_setup.py Vokabular-Gate"
      to: "scripts/build_store_release.sh Mitgliederliste"
      via: "die Ausnahme hängt an der prüfbaren Eigenschaft, dass nichts aus .planning ins Store-Archiv reist"
      pattern: "archive_members"
---

<objective>
Die dokumentarischen Reste, die an Beweisdokumenten hängen: die Nachweistabelle von
docs/store-submission.md wird chronologisch, ihr Rückverweis auf den getaggten Commit wird
präzise, die abgelegte 13-VERIFICATION bekommt einen datierten Nachtrag statt einer
Umschreibung, und die Reichweite des Vokabular-Gates gegenüber `.planning` wird entschieden
statt offen gelassen.

Purpose: DOC-01d (IN-01, IN-02 und der verbliebene IN-07-Verweis), DOC-02 (W-2 aus dem
v1.3-Audit) und SEC-03 (I-2 aus dem v1.3-Audit). Alle drei berühren Dokumente, deren
einziger Wert ihre Belegbarkeit ist: eine nachträglich umgeschriebene Proof-Zeile wäre aus
einem Protokoll eine Behauptung, und ein Gate mit unentschiedener Reichweite ist ein Gate,
über dessen Aussage niemand verfügt.
Output: eine sortierte Nachweistabelle mit einer präzisierten Zeile, eine Nachtrags-Notiz im
Archivstand von Phase 13, und ein Vokabular-Gate, dessen Grenze im Docstring begründet und
von einem Test gehalten wird.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.3-phases/13-cimd-nachmessung-und-release-0-1-9/13-REVIEW.md
@.planning/milestones/v1.3-MILESTONE-AUDIT.md
</context>

<constraints>
- KEIN Versions-Bump, KEIN Tag, KEIN Release in diesem Plan. Version bleibt 0.1.9.
- Keine bestehende Proof-Aussage und kein bestehender Verifikationsbefund wird umgeschrieben.
  Präzisiert wird genau eine Zeile (die 18:30Z-Zeile), und zwar nur ihr Rückverweis; ergänzt
  wird ein Nachtrag als neuer Text. Alles andere bewegt sich, ohne sich zu ändern.
- Die Grenzwerte der Gates bleiben unverändert (18000 Bytes Budget, 1400 je Werkzeug, 21
  Werkzeuge). Die Wortliste des Vokabular-Gates bleibt an ihrer heutigen Stelle: zwei Orte mit
  demselben Wort wären zwei Wahrheiten.
- Deutsche Texte mit echten Umlauten, keine Em-Dashes (U+2014) und keine En-Dashes (U+2013).
  Der englische Text von docs/store-submission.md bleibt englisch.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Nachweistabelle chronologisch und der Rückverweis der Tag-Zeile präzise</name>
  <files>docs/store-submission.md</files>
  <read_first>
    - .planning/milestones/v1.3-phases/13-cimd-nachmessung-und-release-0-1-9/13-REVIEW.md, Abschnitte IN-01, IN-02 und IN-07 (die drei Befunde samt Fix-Vorschlag; IN-02 nennt die Formulierung fast wörtlich)
    - docs/store-submission.md, Zeile 74-79 (der Satz über die Beweisführung und der Tabellenkopf `| Date | Fact | Checked with |`)
    - docs/store-submission.md, Zeile 88-100 (die 0.1.2-Zeilen: 08:28Z und 08:30Z stehen hinter den 08:34Z- und 08:36Z-Zeilen)
    - docs/store-submission.md, Zeile 123-143 (die 0.1.8- und 0.1.9-Zeilen, mit den drei weiteren Stellen außerhalb der Uhr und den vier Rückverweisen "row above")
    - docs/store-submission.md, Zeile 8-13 (der Verweis auf `.planning/phases/05-store-research.md`, Befund IN-07)
    - tests/unit/test_exapp_env_setup.py, Zeile 1973-1978 (`VOCABULARY_EXCEPTION`: warum genau diese Datei ausgenommen ist, nämlich damit eine datierte Proof-Zeile nicht nachträglich umgeschrieben werden muss)
  </read_first>
  <action>
    Drei Eingriffe in eine Datei, und die Reihenfolge ist Absicht: zuerst sortieren, dann eine
    Zeile präzisieren, dann den Verweis kennzeichnen.

    (1) Sortieren, IN-01. Regel: jede Tabellenzeile mit einem Zeitstempel der Form
    `| YYYY-MM-DD HH:MMZ |` steht hinter jeder früheren. Zeilen mit reinem Datum ohne Uhrzeit
    (die 2026-08-19-Zeilen im oberen Teil) tragen keine Uhrzeit und bleiben, wo sie sind.
    Verschoben wird die ganze Zeile, unverändert, Zeichen für Zeichen. Vier Bewegungen, nach
    heutigen Zeilennummern:
      - Zeile 97 (08:28Z, Screenshot-URLs) und Zeile 98 (08:30Z, Manifest-Validierung) wandern
        gemeinsam und in dieser Reihenfolge vor Zeile 91 (08:33Z).
      - Zeile 128 (03:01Z, Listing 0.1.8) wandert hinter Zeile 132 (02:59Z) und ist damit die
        letzte Zeile des 0.1.8-Blocks.
      - Zeile 135 (18:13Z, sechs Gates 0.1.9) wandert vor Zeile 133 (18:14Z, Schritt 1).
      - Zeile 140 (18:46Z, Katalog-Listing 0.1.9) wandert hinter Zeile 143 (18:41Z, Tagliste)
        und ist damit die letzte Zeile des 0.1.9-Blocks.
    Gleiche Zeitstempel behalten ihre bisherige Reihenfolge zueinander. Prüfe danach die vier
    Rückverweise mit dem Wortlaut "row above" (heute Zeile 124, 127, 137 und 139): jeder muss
    weiter die Zeile treffen, die er meint. Nach den vier Bewegungen ist das so, aber lies die
    vier Stellen nach, statt es anzunehmen; wenn eine nicht mehr passt, ist das ein Befund, den
    du meldest, und keine Zeile, die du umschreibst.

    (2) Präzisieren, IN-02, genau eine Zeile: die 18:30Z-Zeile (heute Zeile 137). Der Rückverweis
    "points at `685295d`, the commit `main` already carried ... which the row above records at
    18:21Z" deckt den getaggten Commit nicht ab, denn die 18:21Z-Zeile zertifiziert `22471c1`.
    Nachgemessen und gültig: `git rev-list --count 22471c1..685295d` gibt 1, und `685295d` ist
    der Commit "docs(13-05): the proof line of the branch push, before any tag exists", also die
    Proof-Zeile selbst. Formuliere den Rückverweis auf genau diesen Sachverhalt um, im
    englischen Stil der Datei und ohne die übrigen Aussagen der Zeile anzutasten: der Tag zeigt
    auf `685295d`, eine Änderung hinter dem `22471c1`, den die Zeile darüber um 18:21Z
    zertifiziert, und `685295d` wurde von dem `git push origin main` dieses Schritts
    veröffentlicht, bevor der Tag entstand. Die Garantie "push before tag" bleibt damit stehen
    und stützt sich auf die Kommandospalte derselben Zeile. Nimm keine Zahl, keinen Zeitstempel
    und keinen Run-Namen aus der Zeile heraus.

    (3) Kennzeichnen, IN-07, Zeile 12: der Verweis "the store side facts are verified against
    the store source, see `.planning/phases/05-store-research.md`". Die Datei existiert heute,
    aber die Regel, die diese Phase in docs/oauth-setup.md selbst formuliert hat, verlangt einen
    selbsttragenden Beweis. Kennzeichne den Verweis als interne Notiz, die verschwinden darf,
    und benenne im selben Satz, dass jede Zeile der Nachweistabelle unten ihren eigenen Befehl
    oder ihre eigene URL trägt und deshalb nicht an dieser Notiz hängt. Entferne den Pfad nicht,
    denn er ist heute korrekt.

    Diese Datei ist die einzige mit einer Vokabular-Gate-Ausnahme, und ihre Begründung ist genau
    die Beweisrichtung, um die es hier geht: eine Zeile wird verschoben, nicht neu geschrieben.
  </action>
  <verify>
    <automated>grep -oE '^\| 2026-[0-9]{2}-[0-9]{2} [0-9]{2}:[0-9]{2}Z' docs/store-submission.md | sort -c && [ "$(diff <(git show HEAD:docs/store-submission.md | grep '^| 2026-' | sort) <(grep '^| 2026-' docs/store-submission.md | sort) | grep -c '^[<>]')" = "2" ] && grep -q "22471c1" docs/store-submission.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -oE '^\| 2026-[0-9]{2}-[0-9]{2} [0-9]{2}:[0-9]{2}Z' docs/store-submission.md | sort -c` gibt keine Meldung aus und endet mit Exit 0
    - `git show HEAD:docs/store-submission.md | grep -c '^| 2026-'` und `grep -c '^| 2026-' docs/store-submission.md` geben dieselbe Zahl: keine Zeile wurde gelöscht oder hinzugefügt
    - Der sortierte Vergleich beider Fassungen (`diff` über die sortierten Tabellenzeilen) nennt genau eine geänderte Zeile, und das ist die 18:30Z-Zeile: jede andere Proof-Aussage ist wortgleich
    - Die 18:30Z-Zeile enthält `685295d` und `22471c1` und sagt, dass `685295d` eine Änderung hinter dem `22471c1` der 18:21Z-Zeile ist und vom `git push origin main` dieses Schritts vor dem Tag veröffentlicht wurde
    - Die 18:30Z-Zeile enthält weiterhin `32883904698`, `47264` und `18:27Z`
    - `grep -c "row above" docs/store-submission.md` gibt 4, und für jeden Treffer ist die Zeile direkt darüber die gemeinte (nachgelesen und in der SUMMARY je Treffer benannt)
    - Zeile 8-13 kennzeichnet `.planning/phases/05-store-research.md` als interne Notiz, die verschwinden darf, und der Pfad steht weiter dort
    - `grep -n "—\|–" docs/store-submission.md` gibt keine Zeile aus
    - `git rev-list --count 22471c1..685295d` gibt 1 (die Grundlage der neuen Formulierung, im Lauf nachgeprüft)
  </acceptance_criteria>
  <done>Die Nachweistabelle läuft in der Uhrzeit vorwärts, genau eine Zeile wurde präzisiert und keine andere geändert, der Rückverweis trifft den getaggten Commit, und der Verweis auf die interne Notiz ist als solche gekennzeichnet.</done>
</task>

<task type="auto">
  <name>Task 2: Datierter Nachtrag im Archivstand der 13-VERIFICATION</name>
  <files>.planning/milestones/v1.3-phases/13-cimd-nachmessung-und-release-0-1-9/13-VERIFICATION.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md, DOC-02 (der Auftrag im Wortlaut: Nachtrag, kein Umschreiben)
    - .planning/milestones/v1.3-MILESTONE-AUDIT.md, Frontmatter tech_debt W-2 (die Formulierung des Audits: "war zum Prüfzeitpunkt korrekt")
    - .planning/milestones/v1.3-phases/13-cimd-nachmessung-und-release-0-1-9/13-VERIFICATION.md, Zeile 1-6 (Frontmatter mit `verified: 2026-08-25T21:15:00Z`), Zeile 34 (Truth 9), Zeile 48 (Artefakt-Tabelle) und Zeile 67 (Em-Dash-Prüfung), also die drei Stellen, die die Datei nennen, sowie das Dateiende
    - .planning/milestones/v1.3-phases/13-cimd-nachmessung-und-release-0-1-9/13-REVIEW.md, Abschnitte WR-03 und IN-04 (der Issue-Entwurf, um den es geht, und der zweite Befund an derselben Datei)
  </read_first>
  <action>
    Ergänze am Ende der Datei einen neuen Abschnitt, überschrieben als
    `## Nachtrag 2026-08-28 (Milestone v1.4, Phase 14, DOC-02)`. Nichts oberhalb wird
    angefasst: keine Tabellenzeile, kein Frontmatter-Feld, kein Score. Der Befund war zum
    Prüfzeitpunkt `2026-08-25T21:15:00Z` korrekt, und ein Verifikationsbericht, der später an
    die Welt angepasst wird, ist kein Bericht mehr.

    Inhalt des Nachtrags, in Prosa, deutsch, mit echten Umlauten:
      - `docs/contrib/enterprise-signals-issue.md` ist seit Commit `f9b3d2d` vom 2026-08-26
        nicht mehr im Repository ("docs: remove enterprise issue draft from repo per owner
        decision (D-07: no issue, no internals in repo)").
      - Die drei Stellen dieses Berichts, die die Datei als vorhanden nennen, sind Zeile 34
        (Truth 9), Zeile 48 (Artefakt-Tabelle) und Zeile 67 (Em-Dash-Prüfung). Alle drei waren
        am 2026-08-25 richtig und bleiben unverändert.
      - Was Truth 9 belegt, hängt nicht an der Datei: die drei WR-Fixes sind die Commits
        `c564a6b`, `9ac0a3c` und `f3faefd`, und die sind in der Historie nachweisbar,
        unabhängig davon, ob die vierte Datei heute noch existiert.
      - Der Befund IN-04 aus 13-REVIEW.md (die SSO-Aufzählung des Entwurfs wich vom README ab)
        ist mit derselben Entfernung gegenstandslos: die Datei, deren Aufzählung abwich, gibt
        es nicht mehr. Nenne dazu den Grep-Beleg, dass kein Pfad
        `docs/contrib/enterprise-signals-issue.md` mehr existiert.
      - Ein Satz, wer den Nachtrag geschrieben hat und warum: Phase 14 von v1.4, Anforderung
        DOC-02, Auslöser ist Tech-Debt-Punkt W-2 aus dem v1.3-Milestone-Audit.

    Schreibe nichts über zukünftige Releases und keine Bewertung der Owner-Entscheidung.
  </action>
  <verify>
    <automated>F=.planning/milestones/v1.3-phases/13-cimd-nachmessung-und-release-0-1-9/13-VERIFICATION.md; grep -q "f9b3d2d" "$F" && grep -q "2026-08-28" "$F" && grep -q "^## Nachtrag 2026-08-28" "$F" && [ "$(git diff --numstat -- "$F" | cut -f2)" = "0" ] && [ ! -f docs/contrib/enterprise-signals-issue.md ] && git log --oneline -1 f9b3d2d >/dev/null && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --numstat -- .planning/milestones/v1.3-phases/13-cimd-nachmessung-und-release-0-1-9/13-VERIFICATION.md` nennt 0 entfernte Zeilen: es wurde ausschließlich angefügt
    - Die Datei enthält `## Nachtrag 2026-08-28` und darin die Commit-Kennung `f9b3d2d` sowie das Datum 2026-08-26 der Entfernung
    - Der Nachtrag nennt die drei Fundstellen Zeile 34, Zeile 48 und Zeile 67 und sagt ausdrücklich, dass sie unverändert bleiben, weil sie zum Prüfzeitpunkt 2026-08-25T21:15:00Z korrekt waren
    - Der Nachtrag nennt die drei WR-Fix-Commits `c564a6b`, `9ac0a3c` und `f3faefd` als den Beleg, der ohne die entfernte Datei trägt
    - Der Nachtrag erklärt IN-04 als gegenstandslos und trägt den Grep-Beleg, dass `docs/contrib/enterprise-signals-issue.md` nicht existiert
    - `git log --oneline -1 f9b3d2d` nennt den Entfernungs-Commit (die Kennung im Nachtrag ist nachgeprüft und nicht abgeschrieben)
    - `grep -c "^| 9 |" <datei>` gibt 1, und die Zeile ist byte-gleich zu `git show HEAD:<datei>`
  </acceptance_criteria>
  <done>Der Archivstand von Phase 13 trägt einen datierten Nachtrag, der den Entfernungs-Commit nennt und IN-04 als gegenstandslos erklärt, und keine bestehende Zeile des Berichts wurde angefasst.</done>
</task>

<task type="auto">
  <name>Task 3: Reichweite des Vokabular-Gates entscheiden, begründen und prüfbar machen</name>
  <files>tests/unit/test_exapp_env_setup.py</files>
  <read_first>
    - .planning/REQUIREMENTS.md, SEC-03 (die beiden zugelassenen Wege und was in Kommentar und Summary stehen muss)
    - .planning/milestones/v1.3-MILESTONE-AUDIT.md, Frontmatter tech_debt I-2 (die heutige Lage: `.planning` trägt das Wort im tar-Kontext und liegt bewusst außerhalb der Reichweite)
    - tests/unit/test_exapp_env_setup.py, Zeile 1683-1690 (`FORBIDDEN_VOCABULARY`, die einzige Stelle der Wortliste)
    - tests/unit/test_exapp_env_setup.py, Zeile 1953-2032 (der Abschnittskommentar über die Reichweite, `PUBLIC_MARKDOWN`, `VOCABULARY_EXCEPTION`, `VERBATIM_ARCHIVE_TEXT`, `vocabulary_findings`, `public_markdown_pages` und `archive_members`)
    - tests/unit/test_exapp_env_setup.py, Zeile 2035-2118 (die drei bestehenden Tests der Reichweite samt Gegenprobe, als Stilvorbild für den neuen Test)
    - pyproject.toml, Zeile 50-66 (ruff: line-length 100, target py313, per-file-ignores für tests)
  </read_first>
  <action>
    Entschieden wird der zweite der beiden Wege, die SEC-03 zulässt: `.planning` bleibt außerhalb
    der Reichweite und wird als dokumentierte Ausnahme am Gate begründet. Die Reichweite wird
    NICHT ausgeweitet, und in `.planning` wird nichts bereinigt. Begründung, die in den
    Kommentar und in die SUMMARY gehört: die Regel adressiert, was der Store und ein Leser der
    Dokumentation dieses Repositories bekommt (die drei READMEs, der Changelog, alles unter
    `docs/`, die Manifest-Texte). `.planning` ist interner Planungsbereich, reist in keinem
    Store-Archiv mit, und die abgelegten Milestone-Dateien tragen das Wort in einem technischen
    tar-Kontext als datierten Messbefund; eine Bereinigung dieser Dateien würde ein Protokoll
    verfälschen, statt einen Text zu verbessern. Messung vom 2026-08-28 als Grundlage, ohne die
    Zahl in den Kommentar zu schreiben (sie veraltet dort still): 144 Treffer in 35
    Markdown-Dateien unter `.planning`, sämtlich in abgelegten Milestone- und Phasenständen.

    Zwei Änderungen in einer Datei, und die Wortliste bleibt an ihrer heutigen Stelle:

    1. Erweitere den Docstring von `public_markdown_pages()` um einen Absatz zu `.planning`,
       neben dem bereits vorhandenen Absatz, der `scripts/build_store_release.sh` und
       Nachbarn ausschließt. Der Absatz nennt die Abgrenzung ausdrücklich: Store- und
       Doku-Reichweite auf der einen Seite, interner Planungsbereich auf der anderen, samt der
       Begründung oben und dem Hinweis, dass die Entscheidung von Milestone v1.4 (SEC-03)
       stammt und der Test darunter ihre Grenze hält. Halte die 100-Spalten-Grenze ein.

    2. Ergänze einen Test `test_the_vocabulary_gate_stops_at_the_internal_planning_area`,
       direkt nach `test_the_widened_vocabulary_gate_reports_the_word_with_its_line`. Er bindet
       die Ausnahme an eine prüfbare Eigenschaft, in derselben Bauweise, in der
       `VOCABULARY_EXCEPTION` an `test_the_store_archive_carries_no_exempt_page` hängt, statt
       nur an einen Satz Prosa:
         - keine Seite aus `public_markdown_pages()` liegt unter `.planning` (`".planning" in
           page.parts`), sonst wäre die Grenze verschoben, ohne dass jemand die Entscheidung
           noch einmal getroffen hätte,
         - kein Mitglied aus `archive_members()` beginnt mit `.planning/`, also bekommt der
           Store nichts aus dem internen Bereich, was die Ausnahme rechtfertigt.
       Der Docstring des Tests trägt die Entscheidung und ihre Begründung in zwei bis vier
       Sätzen, damit ein rotes Ergebnis erklärt, was zu tun ist: nicht die Grenze wieder
       zurückschieben, sondern die Entscheidung neu treffen.

    Ändere `FORBIDDEN_VOCABULARY`, `PUBLIC_MARKDOWN`, `VOCABULARY_EXCEPTION`,
    `VERBATIM_ARCHIVE_TEXT` und die drei bestehenden Reichweiten-Tests nicht. Lege keine neue
    Datei und keine zweite Wortliste an.
  </action>
  <verify>
    <automated>uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -q && uv run --no-sync ruff check tests/unit/test_exapp_env_setup.py && uv run --no-sync ruff format --check tests/unit/test_exapp_env_setup.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_the_vocabulary_gate_stops_at_the_internal_planning_area" tests/unit/test_exapp_env_setup.py` gibt 1
    - Der neue Test prüft beide Eigenschaften: keine gedeckte Seite unter `.planning`, und kein Archivmitglied unter `.planning/` (beide Assertions vorhanden)
    - Der Docstring von `public_markdown_pages` nennt `.planning` und begründet die Abgrenzung zwischen Store-/Doku-Reichweite und internem Planungsbereich
    - `grep -c "FORBIDDEN_VOCABULARY = " tests/unit/test_exapp_env_setup.py` gibt 1: es gibt weiter genau eine Wortliste
    - `git diff tests/unit/test_exapp_env_setup.py` enthält keine Änderung an `PUBLIC_MARKDOWN`, `VOCABULARY_EXCEPTION`, `VERBATIM_ARCHIVE_TEXT` oder einem der drei bestehenden Reichweiten-Tests
    - `uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -q` endet mit Exit 0, und der neue Test erscheint als bestanden in der Sammlung (`-q` plus `-k internal_planning_area` nennt 1 passed)
    - `uv run --no-sync ruff check tests/unit/test_exapp_env_setup.py` und `uv run --no-sync ruff format --check tests/unit/test_exapp_env_setup.py` enden mit Exit 0
    - Kein Pfad unter `.planning` wurde geändert, um das Gate grün zu machen (`git status --short .planning` nennt nur die Dateien aus Task 2 und die Planungsdokumente dieser Phase)
  </acceptance_criteria>
  <done>Die Reichweite ist entschieden, im Docstring des Gates begründet und von einem Test gehalten, der die Grenze an eine prüfbare Eigenschaft bindet; die Wortliste liegt weiter an einer einzigen Stelle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Protokoll zu Leser | docs/store-submission.md und die abgelegte 13-VERIFICATION sind Beweisdokumente; ihre Aussagekraft hängt daran, dass sie nach dem Ereignis nicht mehr verändert werden |
| Store-/Doku-Reichweite zu internem Planungsbereich | Das Vokabular-Gate schützt, was veröffentlicht wird; `.planning` liegt jenseits dieser Grenze, und die Grenze muss benannt und prüfbar sein |
| Repository zu Store-Archiv | `scripts/build_store_release.sh` entscheidet, was der Store bekommt; nur daran darf eine Ausnahme hängen, nicht an einer Zusicherung im Text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06 | Tampering | docs/store-submission.md, Proof-Zeilen | mitigate | Zeilen werden verschoben, nicht neu geschrieben; ein Akzeptanzkriterium vergleicht die sortierten Tabellenzeilen vor und nach der Änderung und lässt genau eine geänderte Zeile zu |
| T-14-07 | Repudiation | 18:30Z-Zeile, Garantie "push before tag" | mitigate | Die neue Formulierung nennt `22471c1` und `685295d` samt ihrer Distanz; `git rev-list --count 22471c1..685295d` gibt 1 und wird im Lauf nachgeprüft, statt aus dem Review abgeschrieben zu werden |
| T-14-08 | Tampering | abgelegte 13-VERIFICATION.md | mitigate | Nur Anfügen: `git diff --numstat` muss 0 entfernte Zeilen zeigen, und Truth 9 wird byte-gleich gegen `git show HEAD:` geprüft |
| T-14-09 | Tampering | Vokabular-Gate | mitigate | Die Ausnahme wird eng gefasst (nur `.planning`), im Docstring begründet und an die prüfbare Eigenschaft gebunden, dass nichts aus `.planning` ins Store-Archiv reist; Wortliste, PUBLIC_MARKDOWN, VOCABULARY_EXCEPTION und die drei bestehenden Tests bleiben unverändert |
| T-14-10 | Elevation of Privilege | Gate-Reichweite | mitigate | Ein Test hält die Grenze: verschiebt sie jemand, wird der Lauf rot und der Docstring sagt, dass die Entscheidung neu zu treffen ist, statt die Grenze stillschweigend zurückzuschieben |
| T-14-11 | Information Disclosure | interne Planungsnotizen | accept | `.planning` ist per Owner-Entscheidung aus Phase 1 öffentlich (Repo-Sichtbarkeit option-a); der Verweis in docs/store-submission.md wird nur als interne Notiz gekennzeichnet, nicht entfernt |
| T-14-SC | Tampering | Paketinstallation | mitigate | Keine Paketinstallation in diesem Plan; alle Läufe mit `uv run --no-sync`, also ohne Sync und ohne Auflösung |
</threat_model>

<verification>
- `grep -oE '^\| 2026-[0-9]{2}-[0-9]{2} [0-9]{2}:[0-9]{2}Z' docs/store-submission.md | sort -c` Exit 0
- Sortierter Zeilenvergleich vor/nach: genau eine geänderte Tabellenzeile, keine gelöschte, keine neue
- Nachtrag in der abgelegten 13-VERIFICATION vorhanden, 0 entfernte Zeilen in `git diff --numstat`
- `uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -q` Exit 0, neuer Reichweiten-Test bestanden
- `uv run --no-sync ruff check` und `ruff format --check` über die geänderte Testdatei Exit 0
- Version bleibt 0.1.9, kein Tag entsteht
</verification>

<success_criteria>
Roadmap-Erfolgskriterium 3 ist erfüllt (Proof-Zeilen chronologisch, Rückverweis auf den
getaggten Commit, keine verfälschte Aussage), Erfolgskriterium 4 ist erfüllt (datierter
Nachtrag mit f9b3d2d, Befund unverändert) und Erfolgskriterium 5 ist erfüllt (Reichweite
einheitlich entschieden, als Kommentar am Gate begründet, Entscheidung im Plan- und
Summary-Text, Grenze von einem Test gehalten).
</success_criteria>

<output>
Create `.planning/phases/14-doku-reste-und-gate-entscheid/14-02-SUMMARY.md` when done.
Nenne darin: die vier Zeilenbewegungen und das Ergebnis des sortierten Vergleichs, den alten
und den neuen Wortlaut des Rückverweises der 18:30Z-Zeile samt der nachgemessenen
Commit-Distanz, die vier geprüften "row above"-Stellen, den Wortlaut des Nachtrags, und die
SEC-03-Entscheidung im Klartext: `.planning` bleibt außerhalb der Reichweite, begründet mit
der Abgrenzung Store-/Doku-Reichweite gegen internen Planungsbereich, mit der Messung 144
Treffer in 35 Dateien als Grundlage und dem neuen Test als Halteseil.
</output>
