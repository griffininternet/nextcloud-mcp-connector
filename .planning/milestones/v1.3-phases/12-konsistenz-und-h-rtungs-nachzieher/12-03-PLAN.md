---
phase: 12-konsistenz-und-h-rtungs-nachzieher
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp_connector/provider_map.py
  - tests/unit/test_tools_context.py
  - tests/unit/test_exapp_env_setup.py
autonomous: true
requirements: [SEC-02]

must_haves:
  truths:
    - "Jeder Eintrag in PROVIDER_KINDS trägt einen Verifikationskommentar mit Repository, Datei und Klasse, auch files und notes"
    - "Der Kommentar für files erklärt die Doppelspur aus Attribut und URL-Segment, der für notes erklärt die Ableitung aus der URL"
    - "Ein Regressionstest hält fest, dass tools/context.py die Nachrichtenebene von Mail nirgends im Code anfragt"
    - "Das Vokabular-Gate prüft zusätzlich zum Manifest die drei READMEs und CHANGELOG.md"
    - "docs/store-submission.md ist eine benannte interne Ausnahme mit Begründung, keine bereinigte Datei"
    - "Jedes der zwei neuen Gates hat eine Gegenprobe, die es rot sieht, und eine Behauptung, dass seine Dateiliste nicht leer ist"
    - "Die Wortliste des Vokabular-Gates steht an genau einer Stelle im Repo"
  artifacts:
    - path: "src/mcp_connector/provider_map.py"
      provides: "Verifikationskommentare für files und notes, am Release-Tag gelesen"
      contains: "FilesSearchProvider"
    - path: "tests/unit/test_tools_context.py"
      provides: "Quelltext-Gate zu T-11-29 plus Gegenprobe, neben dem bestehenden Verhaltenstest"
      contains: "level=\\\"messages\\\""
    - path: "tests/unit/test_exapp_env_setup.py"
      provides: "Vokabular-Gate über Manifest, drei READMEs und CHANGELOG.md, mit benannter Ausnahme und zwei Gegenproben"
      contains: "FORBIDDEN_VOCABULARY"
  key_links:
    - from: "src/mcp_connector/provider_map.py"
      to: "tests/unit/test_provider_map.py"
      via: "der Kommentar trägt den Beweis, der eingefrorene Halter begrenzt die Menge"
      pattern: "PROVIDER_KINDS"
    - from: "tests/unit/test_exapp_env_setup.py"
      to: "scripts/build_store_release.sh"
      via: "die Ausnahme wird prüfbar über eine positive Behauptung darüber, welche Dateien im Store-Archiv liegen"
      pattern: "build_store_release"
---

<objective>
SEC-02: die drei Nachzieher aus `11-SECURITY.md` werden von Prüfschritten zu dauerhaften Belegen.
(a) `PROVIDER_KINDS` trägt für jeden Eintrag den Verifikationskommentar mit Repository, Datei und
Klasse, heute fehlen `files` und `notes` (UF-1). (b) Das Quelltext-Gate aus T-11-29 wird ein
Regressionstest in der Suite statt einer Zeile in einem Ausführungsprotokoll (UF-2). (c) Das
Vokabular-Gate reicht über `appinfo/info.xml` hinaus und deckt die drei READMEs und `CHANGELOG.md`
mit ab (UF-3).

Purpose: Alle drei sind Regeln, die heute stimmen und nichts durchsetzt. Der Kommentar ist der
Beweis für eine Zuordnung, die niemand aus dem Namen der Provider-Id ableiten kann, und
`search-deck-card-board` ist der Präzedenzfall dafür, dass die naheliegende Id falsch ist. Ein
einmaliger Prüfschritt schützt genau eine Ausführung. Und ein Vokabular-Gate, das nur das Manifest
liest, während die Regel für jedes öffentliche Artefakt gilt, ist eine Regel mit einer Reichweite,
die kleiner ist als ihre Formulierung.

Output: zwei belegte Provider-Einträge, zwei neue Gates, je mit Gegenprobe, und eine Ausnahme, die
begründet und prüfbar ist statt bequem.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-konsistenz-und-h-rtungs-nachzieher/12-RESEARCH.md
@.planning/phases/12-konsistenz-und-h-rtungs-nachzieher/12-PATTERNS.md

Die drei geänderten Dateien:
@src/mcp_connector/provider_map.py
@tests/unit/test_tools_context.py
@tests/unit/test_exapp_env_setup.py

Die Vorlagen und Belege (nicht ändern):
@tests/unit/test_provider_map.py
@tests/contract/test_no_destructive_calls.py
@tests/contract/test_tool_surface.py
@src/mcp_connector/tools/context.py
@scripts/build_store_release.sh

<interfaces>
SEC-02(a). Kommentarformat der vier vorhandenen Einträge (provider_map.py:57-72): eine oder mehrere
`#`-Zeilen direkt über dem Eintrag, Form "Verified against nextcloud/&lt;repo&gt; &lt;pfad&gt;,
class &lt;Klasse&gt;: &lt;was der Code tut, das wir ausnutzen&gt;". Die zwei fehlenden Kommentare
gehören über `"files": "file"` (Zeile 55) und `"notes": "note"` (Zeile 56).

Belege, in der Recherche am Release-Tag gelesen (12-RESEARCH.md:172-186 und :415-431):
  files   nextcloud/server v34.0.0, apps/files/lib/Search/FilesSearchProvider.php
          class FilesSearchProvider implements IFilteringProvider, getId() liefert 'files'
          search() setzt addAttribute('fileId', ...) und addAttribute('path', ...)
          Link aus linkToRoute('files.View.showFile', ['fileid' =&gt; ...])
          Anschluss im eigenen Modul: _file_id (provider_map.py:176-188) liest attributes.fileId
          zuerst, sonst das /f/&lt;fileid&gt;-Segment der URL. Genau diese Doppelspur ist der Grund
          fuer den Kommentar.
  notes   nextcloud/notes v6.0.2, lib/AppInfo/SearchProvider.php
          class SearchProvider implements IProvider, getId() liefert Application::APP_ID
          Application::APP_ID = 'notes' (lib/AppInfo/Application.php)
          Der Provider setzt KEINE Attribute; Link ist
          linkToRouteAbsolute('notes.page.indexnote', ['id' =&gt; ...])
          Anschluss im eigenen Modul: provider_map.py:135-138 liest die Note-Id per
          _last_numeric_segment(url), weil es keine Attribute gibt.

Offener Punkt aus der Recherche (Secondary confidence): Application.php wurde auf `main` gelesen,
SearchProvider.php zusaetzlich am Tag v6.0.2. Die Konstante APP_ID ist am Tag zu bestaetigen,
bevor der Kommentar den Tag nennt.

SEC-02(b). Der einmalige Prueferschritt aus 11-05-SUMMARY.md behauptete unter anderem, dass
`level="messages"` in tools/context.py nicht vorkommt. Ist-Zustand, geprueft: das Wort "messages"
steht dort fuenfmal, ausschliesslich in Docstrings und Kommentaren (:36, :43, :348, :355, :593);
als Code stehen nur `level="accounts"` (:352) und `level="mailboxes"` (:367). Ein Gate auf die
exakte Zeichenkette `level="messages"` ist heute gruen und trifft keine Prosa.
  Nachbar und Stilvorbild:  tests/unit/test_tools_context.py:1815-1830
                            test_this_module_reads_no_content_of_its_own
  Zweites Vorbild:          tests/unit/test_tools_context.py:1807-1812
  Der Verhaltenstest dazu:  tests/unit/test_tools_context.py:1615-1645, bleibt unveraendert
                            (T-11-29: mail.of("messages") == [])

SEC-02(c). Ist-Zustand:
  tests/unit/test_exapp_env_setup.py:1683-1686   FORBIDDEN_VOCABULARY = "archiv" mit Erklaerung
  tests/unit/test_exapp_env_setup.py:1787-1789   angewendet in description_problems, nur auf den
                                                 Element-Text von appinfo/info.xml
  tests/unit/test_exapp_env_setup.py:1861-1874   zweiter, engerer Halter je Sprachvariante
  tests/unit/test_exapp_env_setup.py:1667-1672   die Gegenproben-Regel des Hauses, woertlich
  tests/unit/test_exapp_env_setup.py:1942-1950   die bestehende Gegenprobe als Vorlage

Trefferlage im ganzen Repo, neu ausgezaehlt (grep -ric "archiv"):
  docs/store-submission.md   10 Zeilen (47, 124, 125, 190, 204, 222, 244, 281, 318, 349)
  README.md, README.de.md, README.fr.md, CHANGELOG.md, alle anderen docs/*.md   0
Das erweiterte Gate ist bei Einfuehrung gruen. Die Gegenprobe muss daher aus einem konstruierten
Text kommen und nicht aus einer echten Datei (Pitfall 4).

Der Inhalt des Store-Archivs, gelesen in scripts/build_store_release.sh:42-46:
  appinfo/info.xml, CHANGELOG.md, LICENSE, README.md. Alle vier sind sauber.

Dateiliste als Muster fuer einen repo-weiten Markdown-Lauf: tests/contract/test_tool_surface.py:766
  for page in [*sorted(DOCS.glob("*.md")), README]:
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SEC-02(a): die zwei fehlenden Verifikationskommentare, am Release-Tag belegt</name>
  <files>src/mcp_connector/provider_map.py</files>
  <read_first>
    - src/mcp_connector/provider_map.py: der Modul-Docstring, die Tabelle `PROVIDER_KINDS` um Zeile 53 bis 73 mit allen vier vorhandenen Kommentaren als Formatvorbild, die Ableitung der Note-Id um Zeile 132 bis 156 und `_file_id` um Zeile 176 bis 188
    - tests/unit/test_provider_map.py, Zeile 78 bis 80 und 287 bis 299: der eingefrorene Halter der Menge und die Aussage, dass `spreed` nie eine Provider-Id war
    - .planning/phases/12-konsistenz-und-h-rtungs-nachzieher/12-RESEARCH.md, Abschnitt "SEC-02(a)" mit den beiden Belegzitaten und dem Secondary-Hinweis zu `Application.php`
    - .planning/phases/12-konsistenz-und-h-rtungs-nachzieher/12-PATTERNS.md, Abschnitt zu `provider_map.py`
  </read_first>
  <action>
    Erste Handlung: die eine Zeile bestätigen, die die Recherche nur auf `main` gelesen hat, mit
    einem Lesezugriff am Tag, zum Beispiel
    `curl -fsSL https://raw.githubusercontent.com/nextcloud/notes/v6.0.2/lib/AppInfo/Application.php`
    und darin `APP_ID`. Bestätigt sich `'notes'` am Tag, nennt der Kommentar den Tag. Ist der
    Netzzugriff nicht möglich, nennt der Kommentar nur die Datei aus `SearchProvider.php`, die am
    Tag gelesen wurde, und die SUMMARY protokolliert, welche Zeile an welchem Ref belegt ist. Ein
    Kommentar, der einen Tag behauptet, den niemand gelesen hat, wäre genau die Sorte Beleg, gegen
    die dieses Register gebaut ist.

    Dann zwei Kommentarblöcke, im Format der vier vorhandenen Einträge (Repository, Datei, Klasse,
    dann das Verhalten, das wir ausnutzen), jeweils direkt über dem Eintrag:

    (1) über `"files": "file"`: `nextcloud/server` am Tag `v34.0.0`,
        `apps/files/lib/Search/FilesSearchProvider.php`, Klasse `FilesSearchProvider implements
        IFilteringProvider`, `getId()` liefert `files`, und `search()` setzt die Attribute `fileId`
        und `path`, während der Link aus `linkToRoute('files.View.showFile', ['fileid' => ...])`
        kommt. Der Kommentar endet mit dem Anschluss an das eigene Modul: genau diese Doppelspur
        ist der Grund, warum `_file_id` zuerst `attributes.fileId` liest und erst dann auf das
        `/f/`-Segment der URL zurückfällt.

    (2) über `"notes": "note"`: `nextcloud/notes` am Tag `v6.0.2`,
        `lib/AppInfo/SearchProvider.php`, Klasse `SearchProvider implements IProvider`, `getId()`
        liefert `Application::APP_ID`, und diese Konstante ist `notes`. Entscheidend ist der zweite
        Satz: der Provider setzt keine Attribute, der Link ist
        `linkToRouteAbsolute('notes.page.indexnote', ['id' => ...])`, und genau deshalb liest das
        eigene Modul die Note-Id per `_last_numeric_segment(url)` statt aus einem Attribut. Damit
        erklärt der Kommentar die abweichende Ableitung und ist keine Fußnote.

    Beide Belege gelten für einen Tag und nicht für `main` oder `master`, damit der Kommentar so
    überprüfbar bleibt wie die vier bestehenden. `PROVIDER_KINDS` selbst wird nicht verändert:
    keine neue Id, keine entfernte Id, keine geänderte Zuordnung. `tests/unit/test_provider_map.py`
    bleibt unangetastet, der eingefrorene Halter dort begrenzt die Menge und der Kommentar trägt
    den Beweis.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run --no-sync ruff check . &amp;&amp; uv run --no-sync ruff format --check . &amp;&amp; uv run --no-sync pyright</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run --no-sync python -c "
import re
from pathlib import Path
from mcp_connector import provider_map

text = Path('src/mcp_connector/provider_map.py').read_text(encoding='utf-8')
block = text.split('PROVIDER_KINDS: Mapping[str, str] = {', 1)[1].split('}', 1)[0]
lines = block.splitlines()

pending: list[str] = []
documented: dict[str, str] = {}
for line in lines:
    stripped = line.strip()
    if stripped.startswith('#'):
        pending.append(stripped.lstrip('# ').strip())
        continue
    match = re.match(r'\"([a-z-]+)\":', stripped)
    if match:
        documented[match.group(1)] = ' '.join(pending)
        pending = []

assert set(documented) == set(provider_map.PROVIDER_KINDS), (sorted(documented), sorted(provider_map.PROVIDER_KINDS))
for name, comment in documented.items():
    assert comment, f'{name} carries no verification comment'
    assert 'Verified against nextcloud/' in comment, (name, comment)
for name in ('files', 'notes'):
    comment = documented[name]
    assert '.php' in comment, (name, 'the comment has to name the file')
    assert 'class ' in comment, (name, 'the comment has to name the class')
assert 'FilesSearchProvider' in documented['files'], documented['files']
assert 'fileId' in documented['files'], documented['files']
assert 'SearchProvider' in documented['notes'], documented['notes']
print('every provider entry carries its proof')
"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run --no-sync pytest tests/unit/test_provider_map.py -q &amp;&amp; uv run --no-sync pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - Jeder der sechs Einträge in `PROVIDER_KINDS` trägt einen Kommentar mit "Verified against nextcloud/", einem Dateipfad mit `.php` und einer Klassenangabe
    - Der `files`-Kommentar nennt `FilesSearchProvider`, `getId()`, die Attribute `fileId` und `path` und den Anschluss an `_file_id`
    - Der `notes`-Kommentar nennt `SearchProvider`, `Application::APP_ID`, die Abwesenheit von Attributen und den Anschluss an `_last_numeric_segment`
    - Beide Kommentare nennen einen Release-Tag und nicht `main` oder `master`; welche Zeile an welchem Ref bestätigt wurde, steht in der SUMMARY
    - `PROVIDER_KINDS` enthält dieselben sechs Ids mit denselben Zuordnungen wie vorher; `tests/unit/test_provider_map.py` ist unverändert und grün
    - `uv run --no-sync pytest -q`, `ruff check .` und `pyright` sind grün
  </acceptance_criteria>
  <done>Keine Zuordnung in der Provider-Tabelle steht mehr ohne ihren Beleg, und der Beleg erklärt jeweils die Ableitung im eigenen Modul.</done>
</task>

<task type="auto">
  <name>Task 2: SEC-02(b): aus dem einmaligen Prüfschritt zu T-11-29 wird ein Regressionstest mit Gegenprobe</name>
  <files>tests/unit/test_tools_context.py</files>
  <read_first>
    - tests/unit/test_tools_context.py: der Verhaltenstest zu T-11-29 um Zeile 1615 bis 1645, das Stilvorbild `test_no_sentence_of_this_module_frames_foreign_text_as_a_wish_of_the_user` um Zeile 1807 bis 1812 und der direkte Nachbar `test_this_module_reads_no_content_of_its_own` um Zeile 1815 bis 1830
    - src/mcp_connector/tools/context.py: die fünf Prosa-Nennungen von "messages" in Zeile 36, 43, 348, 355 und 593, und die zwei Code-Stellen `level="accounts"` (Zeile 352) und `level="mailboxes"` (Zeile 367)
    - tests/contract/test_no_destructive_calls.py, Zeile 287 bis 353 und 378 bis 392: die geteilte Prüffunktion, die Nicht-leer-Behauptung und die Gegenprobe als Muster
    - tests/unit/test_exapp_env_setup.py, Zeile 1667 bis 1672: die Gegenproben-Regel des Hauses im Wortlaut
    - .planning/phases/12-konsistenz-und-h-rtungs-nachzieher/12-RESEARCH.md, Abschnitt "SEC-02(b)" und Pitfall 4
  </read_first>
  <action>
    Neben dem bestehenden Verhaltenstest zu T-11-29 entsteht sein Quelltext-Partner, im Stil des
    direkten Nachbarn: Quelltext von `tools/context.py` lesen, Zeilen die mit `#` beginnen
    herausfiltern, dann behaupten. Die gesuchte Zeichenkette ist genau `level="messages"`. Der
    einfache Kommentarfilter genügt hier und die Begründung gehört in den Docstring: das Wort
    "messages" steht in `context.py` mehrfach in Prosa, aber nie in dieser Schreibweise, und die
    Prosa dort erklärt Entscheidungen und wird nicht gelöscht, um ein Gate grün zu bekommen.

    Bauform, damit die Gegenprobe möglich ist (das ist der eigentliche Punkt dieses Tasks): die
    Prüfung steht in einer kleinen Funktion, die **den Text als Parameter** nimmt und die Fundstellen
    mit Zeilennummer zurückgibt, nicht in einer Funktion, die selbst `read_text` aufruft. Das Gate
    liest die echte Datei und ruft diese Funktion; die Gegenprobe ruft dieselbe Funktion mit einem
    konstruierten String, der eine Zeile mit `level="messages"` als Code und eine zweite mit
    demselben Wort in einem Kommentar enthält, und erwartet genau einen Fund. Eine Gegenprobe, die
    die Prüfung nachbaut, beweist etwas über die Gegenprobe.

    Dazu die zweite Gegenprobe nach dem Muster `_source_files`: eine Behauptung, dass der gelesene
    Quelltext nicht leer ist und die erwartete Datei getroffen wurde (etwa dass er `mail_tools` und
    `level="mailboxes"` enthält). Ohne sie schützt ein Tippfehler im Pfad nichts, und das ist genau
    der Weg, auf dem ein bei Einführung grünes Gate wertlos wird.

    Der Docstring des Gates nennt die Bedrohungsnummer `T-11-29` und den Grund in einem Satz: die
    Nachrichtenebene von Mail ist der einzige Weg, auf dem Betreff oder Mailinhalt in das
    Kontextbündel gelangen könnten, und `prepare_context` fragt sie nicht an. Der bestehende
    Verhaltenstest um Zeile 1615 bis 1645 bleibt unverändert; die zwei Tests belegen dieselbe
    Zusage von zwei Seiten, das Verhalten und die Bauweise.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run --no-sync ruff check . &amp;&amp; uv run --no-sync ruff format --check . &amp;&amp; uv run --no-sync pyright</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run --no-sync pytest tests/unit/test_tools_context.py -q</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run --no-sync python -c "
from pathlib import Path
q = chr(34)
needle = 'level=' + q + 'messages' + q
text = Path('tests/unit/test_tools_context.py').read_text(encoding='utf-8')
assert 'T-11-29' in text, 'the gate names the threat it holds'
assert needle in text, 'the gate looks for the exact spelling'
assert 'context_tools.__file__' in text or 'context.py' in text, 'the gate reads the real module'
print('the source gate names its threat and its needle')
"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run --no-sync python -c "
import subprocess
from pathlib import Path

# The counter probe of the counter probe: break the production source on a copy, run the gate
# against it, and require a failure. A gate nobody has seen fail is not a gate.
q = chr(34)
source = Path('src/mcp_connector/tools/context.py')
original = source.read_text(encoding='utf-8')
broken = original.replace('level=' + q + 'mailboxes' + q, 'level=' + q + 'messages' + q, 1)
assert broken != original, 'the anchor line for the manipulation has to exist'
try:
    source.write_text(broken, encoding='utf-8')
    done = subprocess.run(
        ['uv', 'run', '--no-sync', 'pytest', 'tests/unit/test_tools_context.py', '-q', '-k', 'messages'],
        capture_output=True, text=True,
    )
    assert done.returncode != 0, done.stdout[-2000:]
finally:
    source.write_text(original, encoding='utf-8')
assert source.read_text(encoding='utf-8') == original, 'the production source is restored'
print('the gate goes red on a manipulated source, and the tree is clean again')
"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; git diff --exit-code -- src/mcp_connector/tools/context.py &amp;&amp; uv run --no-sync pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - Ein neuer Test in `tests/unit/test_tools_context.py` behauptet, dass `level="messages"` im Code von `tools/context.py` nicht vorkommt, und nennt `T-11-29` im Docstring
    - Die Prüfung steht in einer Funktion, die den Text als Parameter nimmt; das Gate und die Gegenprobe benutzen dieselbe Funktion
    - Eine Gegenprobe speist einen konstruierten Text mit einer Code-Zeile und einer Kommentar-Zeile und erwartet genau einen Fund
    - Eine zweite Behauptung stellt sicher, dass der gelesene Quelltext nicht leer ist und die erwartete Datei getroffen wurde
    - Der bestehende Verhaltenstest zu T-11-29 (um Zeile 1615 bis 1645) ist unverändert und grün
    - Der Lauf gegen eine manipulierte Kopie von `tools/context.py` schlägt fehl, und `git diff --exit-code -- src/mcp_connector/tools/context.py` ist danach sauber
    - `uv run --no-sync pytest -q`, `ruff check .` und `pyright` sind grün
  </acceptance_criteria>
  <done>Die Zusage aus T-11-29 wird bei jedem Testlauf geprüft statt einmal in einem Protokoll behauptet, und sie ist rot gesehen worden.</done>
</task>

<task type="auto">
  <name>Task 3: SEC-02(c): das Vokabular-Gate reicht bis zu den READMEs und zum Changelog, mit einer begründeten Ausnahme</name>
  <files>tests/unit/test_exapp_env_setup.py</files>
  <read_first>
    - tests/unit/test_exapp_env_setup.py: die Konstante `FORBIDDEN_VOCABULARY` samt Erklärung um Zeile 1683 bis 1686, die Anwendungsstelle in `description_problems` um Zeile 1787 bis 1789, der engere Halter je Sprachvariante um Zeile 1861 bis 1874, die Gegenproben-Regel des Hauses um Zeile 1667 bis 1672 und die bestehende Gegenprobe um Zeile 1942 bis 1950
    - tests/contract/test_tool_surface.py, Zeile 28 bis 35 und 752 bis 776: das Muster für einen repo-weiten Markdown-Lauf und die Konstanten `README` und `DOCS`
    - tests/contract/test_no_destructive_calls.py, Zeile 287 bis 292: die Nicht-leer-Behauptung als Muster
    - scripts/build_store_release.sh, Zeile 40 bis 50: welche vier Dateien in das Store-Archiv kopiert werden
    - docs/store-submission.md, die zehn Zeilen mit dem Wort (47, 124, 125, 190, 204, 222, 244, 281, 318, 349), insbesondere die zwei datierten Proof-Zeilen 124 und 125 und der Nextcloud-Klassenname in Zeile 281
    - .planning/phases/12-konsistenz-und-h-rtungs-nachzieher/12-RESEARCH.md, Abschnitt "SEC-02(c)", Pitfall 4 und Pitfall 6
  </read_first>
  <action>
    Ort und Zuschnitt sind hier entschieden und nicht dem Executor überlassen: die Wortliste bleibt,
    wo sie ist, und das Gate wächst um sie herum. `FORBIDDEN_VOCABULARY` steht weiter in
    `tests/unit/test_exapp_env_setup.py`, und die neue Markdown-Prüfung entsteht in derselben Datei.
    Begründung, die in den Docstring gehört: zwei Stellen mit demselben Wort wären zwei Wahrheiten,
    genau der Fehler, den dieses Projekt als IN-03 benannt und behoben hat, und ein Umzug der Liste
    in eine neue Datei würde am heute grünen Manifest-Gate bauen, ohne die Regel zu verbessern. Der
    Preis ist thematische Unschärfe im Dateinamen, und der ist billiger als eine zweite Wahrheit oder
    ein Umbau an einem grünen Sicherheits-Gate.

    (1) Eine Prüffunktion, die **einen Text und einen Namen als Parameter** nimmt und die
        Fundstellen mit Zeilennummer zurückgibt, damit ein Verstoß eine Einzeilenkorrektur ist und
        keine Suche im Baum. Groß- und Kleinschreibung wird wie bisher ignoriert
        (`casefold`).

    (2) Ein Gate, das diese Funktion über eine feste, benannte Dateiliste laufen lässt: `README.md`,
        `README.de.md`, `README.fr.md`, `CHANGELOG.md` und die übrigen `docs/*.md`. Ausdrücklich
        **nur** Markdown und das Manifest: `scripts/build_store_release.sh` und andere Skripte
        werden nicht geprüft, denn dort ist `ARCHIVE` ein Variablenname und `tar` das Werkzeug, das
        das Paket baut, und eine Prüfung darauf würde das Gate an einer Stelle rot machen, an der die
        Regel nie gemeint war.

    (3) Die Ausnahme: `docs/store-submission.md` wird namentlich ausgenommen, und die Begründung
        steht im Docstring des Gates, nicht in einem Kommentar am Rand: interne Release-Dokumentation,
        die nicht im Store-Archiv mitreist, mit zwei datierten Proof-Zeilen (124 und 125), die
        Protokoll sind und deren nachträgliche Umformulierung die Beweisrichtung umdrehen würde
        (T-11-63), und mit einem fremden Nextcloud-Klassennamen in Zeile 281, der nicht umbenennbar
        ist, ohne die Aussage falsch zu machen.

    (4) Die Ausnahme wird prüfbar statt bequem: eine positive Behauptung darüber, was im
        Store-Archiv liegt. Die Dateiliste dafür wird aus `scripts/build_store_release.sh` gelesen
        (die `cp`-Zeilen um Zeile 42 bis 46, heute `appinfo/info.xml`, `CHANGELOG.md`, `LICENSE`,
        `README.md`), und der Test behauptet, dass jede dieser Dateien sauber ist und dass
        `docs/store-submission.md` nicht darunter ist. Damit hängt die Ausnahme an einer
        überprüfbaren Eigenschaft und nicht an einer Zusicherung im Prosatext.

    (5) Zwei Gegenproben, weil das Gate bei Einführung grün ist (Pitfall 4). Erstens: die
        Prüffunktion bekommt einen konstruierten Text mit dem verbotenen Wort in wechselnder
        Schreibweise und muss ihn melden, mit der richtigen Zeilennummer. Zweitens: eine Behauptung,
        dass die geprüfte Dateiliste nicht leer ist und die vier Pflichtdateien enthält, nach dem
        Muster `assert files, f"no ... found under {...}"`. Die bestehende Gegenprobe am Manifest
        (Zeile 1942 bis 1950) bleibt unverändert.

    Die bestehende Anwendung auf den Element-Text von `appinfo/info.xml` und der engere Halter je
    Sprachvariante bleiben, wie sie sind. Dieses Gate wird erweitert, nicht ersetzt, und `CHANGELOG.md`
    ist ab jetzt mitgeschützt, was für Phase 13 relevant ist: der Block 0.1.9 entsteht dort unter
    dieser Regel.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run --no-sync ruff check . &amp;&amp; uv run --no-sync ruff format --check . &amp;&amp; uv run --no-sync pyright</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -q</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run --no-sync python -c "
from pathlib import Path
text = Path('tests/unit/test_exapp_env_setup.py').read_text(encoding='utf-8')
assert text.count('FORBIDDEN_VOCABULARY = ') == 1, 'the word list stays a single truth'
for needle in ('README.de.md', 'README.fr.md', 'CHANGELOG.md', 'store-submission.md', 'build_store_release'):
    assert needle in text, needle
other = [
    p for p in Path('tests').rglob('*.py')
    if p.name != 'test_exapp_env_setup.py' and 'FORBIDDEN_VOCABULARY = ' in p.read_text(encoding='utf-8')
]
assert other == [], other
print('one word list, wider reach, named exception')
"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run --no-sync python -c "
import subprocess
from pathlib import Path

# Counter probe on a real file: the gate has to go red when the word enters a covered page.
page = Path('README.md')
original = page.read_text(encoding='utf-8')
try:
    page.write_text(original + '\n\nThe Archive of your data stays untouched.\n', encoding='utf-8')
    done = subprocess.run(
        ['uv', 'run', '--no-sync', 'pytest', 'tests/unit/test_exapp_env_setup.py', '-q'],
        capture_output=True, text=True,
    )
    assert done.returncode != 0, done.stdout[-2000:]
finally:
    page.write_text(original, encoding='utf-8')
assert page.read_text(encoding='utf-8') == original, 'the readme is restored'
print('the widened gate goes red on a real page, and the tree is clean again')
"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; git diff --exit-code -- README.md README.de.md README.fr.md CHANGELOG.md docs/store-submission.md &amp;&amp; uv run --no-sync pytest -q &amp;&amp; uv run --no-sync pytest tests/contract -q</automated>
  </verify>
  <acceptance_criteria>
    - `FORBIDDEN_VOCABULARY` existiert genau einmal im ganzen Testbaum, weiter in `tests/unit/test_exapp_env_setup.py`
    - Das Gate prüft `README.md`, `README.de.md`, `README.fr.md`, `CHANGELOG.md` und die übrigen `docs/*.md`; Skripte und andere Nicht-Markdown-Dateien werden nicht geprüft
    - Jeder Fund nennt Datei und Zeilennummer
    - `docs/store-submission.md` ist namentlich ausgenommen, und der Docstring begründet die Ausnahme mit der internen Natur der Datei, den zwei datierten Proof-Zeilen und dem fremden Klassennamen
    - Eine positive Behauptung liest die vier Dateien des Store-Archivs aus `scripts/build_store_release.sh`, belegt ihre Sauberkeit und dass `docs/store-submission.md` nicht darunter ist
    - Eine Gegenprobe mit konstruiertem Text meldet das Wort in wechselnder Schreibweise mit der richtigen Zeilennummer; eine zweite Behauptung stellt sicher, dass die Dateiliste nicht leer ist und die vier Pflichtdateien enthält
    - Die bestehende Manifest-Gegenprobe (Zeile 1942 bis 1950) und der engere Halter je Sprachvariante sind unverändert und grün
    - Der Lauf gegen ein manipuliertes `README.md` schlägt fehl, und `git diff --exit-code` über die fünf Dokumente ist danach sauber
    - `docs/store-submission.md` ist nicht bearbeitet worden, keine Zeile mit einem Zeitstempel ist berührt
    - `uv run --no-sync pytest -q` und `uv run --no-sync pytest tests/contract -q` sind grün
  </acceptance_criteria>
  <done>Die Vokabular-Regel hat dieselbe Reichweite wie ihre Formulierung, ihre Ausnahme ist begründet und an eine prüfbare Eigenschaft gebunden, und das erweiterte Gate ist rot gesehen worden.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fremder Quelltext zu eigener Datenkarte | Die Zuordnung Provider-Id zu Ressourcenart ist eine Behauptung über Code, der nicht in diesem Repo liegt |
| Fremdtext zu Kontextbündel | `prepare_context` darf die Nachrichtenebene von Mail nie anfragen, sonst gelangt Fremdtext ohne Not in das Bündel (T-11-29) |
| Repo zu Öffentlichkeit | Store-Archiv und READMEs sind das öffentliche Gesicht; die Vokabular-Regel gilt für sie und nicht nur für das Manifest |
| Regel zu Durchsetzung | Ein einmaliger Prüfschritt und ein Kommentar ohne Gate sind Zusagen ohne Wächter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-09 | Spoofing | `provider_map.PROVIDER_KINDS`, Einträge ohne Beleg | mitigate | Verifikationskommentar mit Repository, Datei, Klasse und Verhalten für jeden Eintrag, am Release-Tag gelesen; ein Prüfschritt behauptet die Vollständigkeit über die Menge der Ids |
| T-12-10 | Information Disclosure | `tools/context.py`, Mail-Nachrichtenebene | mitigate | Quelltext-Gate auf `level="messages"` als Regressionstest neben dem bestehenden Verhaltenstest; der Lauf gegen eine manipulierte Kopie belegt, dass das Gate feuert |
| T-12-11 | Repudiation | Gate ohne Gegenprobe | mitigate | Jedes der zwei neuen Gates hat eine Gegenprobe über die geteilte Prüffunktion plus eine Nicht-leer-Behauptung über die geprüfte Menge |
| T-12-12 | Repudiation | `docs/store-submission.md`, datierte Proof-Zeilen | accept | Die Datei wird nicht bereinigt, sondern als benannte interne Ausnahme geführt: eine nachträgliche Umformulierung eines Protokolls würde die Beweisrichtung umdrehen (T-11-63, Pitfall 6). Die Ausnahme wird über eine positive Behauptung zum Inhalt des Store-Archivs prüfbar gemacht |
| T-12-13 | Tampering | Wortliste an zwei Stellen | mitigate | Die Liste bleibt an einer Stelle; ein Prüfschritt behauptet, dass `FORBIDDEN_VOCABULARY` im ganzen Testbaum genau einmal definiert ist |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | Keine Paketinstallation in diesem Plan: `pyproject.toml` und `uv.lock` bleiben unangetastet, es gibt kein `uv add`, kein `pip install` und kein `npm install`. Der einzige Netzzugriff ist ein lesender `curl` auf `raw.githubusercontent.com` an einem Release-Tag |
</threat_model>

<verification>
1. `uv run --no-sync pytest -q` und `uv run --no-sync pytest tests/contract -q` sind grün.
2. `uv run --no-sync ruff check .`, `ruff format --check .`, `pyright` und `vulture src scripts vulture_whitelist.py` sind grün.
3. Die zwei Gegenproben-Läufe gegen manipulierte Dateien schlagen fehl, und `git diff --exit-code` über `src/mcp_connector/tools/context.py`, `README.md`, `README.de.md`, `README.fr.md`, `CHANGELOG.md` und `docs/store-submission.md` ist danach sauber.
4. `grep -rc "FORBIDDEN_VOCABULARY = " tests/` findet die Definition genau einmal.
5. `git diff --stat` zeigt keine Änderung an `docs/store-submission.md`, `pyproject.toml`, `uv.lock`, `appinfo/info.xml`, `CHANGELOG.md` oder `scripts/check_tool_budget.py`.
</verification>

<success_criteria>
- Jeder Eintrag in `PROVIDER_KINDS` trägt den Verifikationskommentar mit Repository, Datei und Klasse, auch `files` und `notes` (SEC-02a, UF-1).
- Das Quelltext-Gate aus T-11-29 läuft als Regressionstest in der Suite und ist rot gesehen worden (SEC-02b, UF-2).
- Das Vokabular-Gate prüft über `appinfo/info.xml` hinaus die drei READMEs und `CHANGELOG.md`, und `docs/store-submission.md` ist als interne Ausnahme begründet und an eine prüfbare Eigenschaft gebunden (SEC-02c, UF-3).
- Die Wortliste steht an genau einer Stelle im Repo.
</success_criteria>

<output>
Create `.planning/phases/12-konsistenz-und-h-rtungs-nachzieher/12-03-SUMMARY.md` when done.
Die Zusammenfassung protokolliert, welche fremde Quelltextzeile an welchem Release-Tag bestätigt
wurde (insbesondere `Application::APP_ID` von `nextcloud/notes`), nennt die gewählte Option für den
Ort der Wortliste mit ihrer Begründung, und hält für Phase 13 fest, dass `CHANGELOG.md` ab jetzt
unter dem Vokabular-Gate steht, weil der Block 0.1.9 dort entsteht.
</output>
