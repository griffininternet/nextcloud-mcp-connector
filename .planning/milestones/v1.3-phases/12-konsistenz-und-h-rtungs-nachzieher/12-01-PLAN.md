---
phase: 12-konsistenz-und-h-rtungs-nachzieher
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp_connector/tools/talk.py
  - src/mcp_connector/tools/chatgpt.py
  - src/mcp_connector/server/reg_talk.py
  - tests/unit/test_talk_tools.py
autonomous: true
requirements: [TOOL-17]

must_haves:
  truths:
    - "Eine bei MAX_MESSAGE_BYTES gekappte Talk-Nachricht meldet sich als message_truncated und nicht mehr als truncated"
    - "Die Antwortebene von talk_browse behält truncated, und next steht daneben"
    - "fetch mit einer message-Id meldet eine gekappte Nachricht weiterhin als gekappt"
    - "Der Tool-Docstring von talk_browse nennt für jede der zwei Ebenen genau eine Bedeutung"
    - "Ein Test belegt den Fall, in dem Seite und Nachricht gleichzeitig gekappt sind, mit getrennten Behauptungen je Ebene"
    - "Die Werkzeugoberfläche bleibt bei 21 Werkzeugen unter 18000 Bytes und talk_browse unter 1400 Bytes, ohne Anhebung eines Gates"
  artifacts:
    - path: "src/mcp_connector/tools/talk.py"
      provides: "Eindeutige Benennung der Nachrichtenkappung auf Eintragsebene, mit Begründungskommentar"
      contains: "message_truncated"
    - path: "src/mcp_connector/tools/chatgpt.py"
      provides: "Der zweite Leser der Projektion zieht mit, damit fetch die Kappung nicht still verliert"
      contains: "message_truncated"
    - path: "src/mcp_connector/server/reg_talk.py"
      provides: "Tool-Docstring mit einer Bedeutung je Ebene plus Modul-Docstring-Absatz, der die Bytes rechtfertigt"
      contains: "message_truncated"
    - path: "tests/unit/test_talk_tools.py"
      provides: "Getrennte Abdeckung der zwei Ebenen plus der neue Test für den gemeinsamen Fall"
      contains: "message_truncated"
  key_links:
    - from: "src/mcp_connector/tools/talk.py"
      to: "src/mcp_connector/tools/chatgpt.py"
      via: "der Eintragsschlüssel der Projektion wird in _fetch_message gelesen und in metadata übersetzt"
      pattern: "message_truncated"
    - from: "src/mcp_connector/server/reg_talk.py"
      to: "scripts/check_tool_budget.py"
      via: "nur der Tool-Docstring reist in tools/list, deshalb wird nach dem Edit gemessen"
      pattern: "talk_browse"
---

<objective>
TOOL-17: der Schlüssel `truncated` in einer Antwort von `talk_browse(level="messages")` bedeutet
heute auf der Antwortebene "diese Seite wurde geschnitten, es gibt eventuell eine nächste" und auf
der Eintragsebene "diese Nachricht wurde bei `MAX_MESSAGE_BYTES` geschnitten". Die Eintragsebene
heißt nach diesem Plan `message_truncated`, die Antwortebene behält `truncated`, und der
Tool-Docstring nennt für jede Ebene genau eine Bedeutung.

Purpose: Talk ist die letzte Werkzeugfamilie, die dasselbe Wort auf zwei Ebenen benutzt. Mail hat
denselben Befund in Commit `53ba602` geschlossen (Review-Befund IN-01, dort
`preview_truncated`), und dieser Commit ist die vollständige Vorlage: Projektionsstelle mit
Begründungskommentar, Tool-Docstring, Modul-Docstring-Absatz über die Byte-Ausgabe, zwei Tests.
Ein Modell, das das Eintrags-Flag als Seiten-Flag liest, paginiert im Kreis und bezahlt dafür mehr
Roundtrips als die rund 53 Bytes, die der erklärende Satz kostet.

Output: eine Talk-Antwort, in der jeder Schlüssel genau eine Bedeutung hat, ein `fetch`, das die
Kappung weiterhin meldet, und eine Messung, die belegt, dass kein Gate angehoben werden musste.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-konsistenz-und-h-rtungs-nachzieher/12-RESEARCH.md
@.planning/phases/12-konsistenz-und-h-rtungs-nachzieher/12-PATTERNS.md

Die vier geänderten Dateien:
@src/mcp_connector/tools/talk.py
@src/mcp_connector/tools/chatgpt.py
@src/mcp_connector/server/reg_talk.py
@tests/unit/test_talk_tools.py

Die Vorlage, die kopiert wird (nicht ändern):
@src/mcp_connector/tools/mail.py
@src/mcp_connector/server/reg_mail.py
@tests/unit/test_mail_tools.py

<interfaces>
Fundstellen, alle im Arbeitsbaum gemessen (12-RESEARCH.md, Abschnitt TOOL-17):

  src/mcp_connector/tools/talk.py:525      entry["truncated"] = True         Eintragsebene, wird umbenannt
  src/mcp_connector/tools/talk.py:513-515  Docstring von _message            nennt den Feldnamen noch nicht
  src/mcp_connector/tools/talk.py:494-496  answer["truncated"] = True        Antwortebene, bleibt
  src/mcp_connector/tools/talk.py:697-703  _envelope, Konversationsebene     Antwortebene, bleibt
  src/mcp_connector/tools/talk.py:12-13    Modul-Docstring                   "Every cut is named in the answer"
  src/mcp_connector/tools/talk.py:107-110  _CURSOR_HINT                      spricht über die Konversationsliste, keine Pflichtaenderung
  src/mcp_connector/server/reg_talk.py:46-48  Tool-Docstring                 hier kommt der Satz mit den zwei Schluesseln hinein
  src/mcp_connector/server/reg_talk.py:40-43  cursor-Field-Description       bleibt woertlich, sie spricht ueber die Antwortebene
  src/mcp_connector/tools/chatgpt.py:653-654  Verbraucher                    if entry.get("truncated"): metadata["truncated"] = "true"

Der Satz aus dem Mail-Vorbild (server/reg_mail.py:76-81), 53 ASCII-Zeichen in der Talk-Fassung:
  "truncated: page cut; preview_truncated: preview cut."
  Talk-Entsprechung: "truncated: page cut; message_truncated: message cut."

Der Begründungskommentar, ausformuliert in 12-RESEARCH.md:372-379, gehört an talk.py:524-525:
  "``message_truncated`` and not ``truncated``, and the difference is the whole point: one level
   up, ``truncated`` means this window was cut and there is a ``next``, down here it means this
   message was cut at MAX_MESSAGE_BYTES. A cut window whose first entry carries a cut message
   sets both keys at once, so a model reading the entry level flag as a page flag pages in a
   circle (DF-11-01, the same finding IN-01 named for mail_browse)."

Review-Nummern: in Talk zitiert der Kommentar DF-11-01 und IN-01 (der Mail-Befund), nicht die
Requirement-Nummer. Die IN-Nummern in REQUIREMENTS.md sind gegen 11-REVIEW.md verschoben.

Messung heute (uv run --no-sync python scripts/check_tool_budget.py):
  tools/list 15657 Bytes, 21 Werkzeuge, Budget 18000
  talk_browse 858 Bytes, Ceiling MAX_TOOL_BYTES 1400, also 542 Bytes Luft
  Erwartung nach dem Edit: talk_browse rund 911, Oberflaeche rund 15710
  Keine Gegenkompression noetig, anders als bei mail_browse (dort waren nur 24 Bytes Luft)

Betroffene Tests:
  tests/unit/test_talk_tools.py:726   assert entry["truncated"] is True         wechselt den Schluessel
  tests/unit/test_talk_tools.py:730   Marker-Hygiene (ME-03)                    bleibt woertlich
  tests/unit/test_talk_tools.py:361, 530, 630, 655  Antwortebene                bleiben Waechter, unveraendert
  tests/unit/test_chatgpt_fetch.py:1302  metadata["truncated"] == "true"        bleibt woertlich, wird nur gruen, wenn chatgpt.py mitzieht
  tests/contract/test_tool_surface.py:327-351  talk_browse-Contract             prueft den Docstring-Wortlaut nicht

Nicht in diesem Plan: der Changelog-Block 0.1.9. Er gehoert zu Phase 13 (EXAPP-09, SC2). Die
Formataenderung wird stattdessen in der SUMMARY als Uebergabe an Phase 13 protokolliert.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Die Nachrichtenebene bekommt ihren eigenen Namen, und der zweite Leser zieht im gleichen Schritt mit</name>
  <files>src/mcp_connector/tools/talk.py, src/mcp_connector/tools/chatgpt.py, tests/unit/test_talk_tools.py</files>
  <read_first>
    - src/mcp_connector/tools/talk.py, ganze Datei: der Modul-Docstring um Zeile 10 bis 15, `_CURSOR_HINT` um Zeile 107, die Antwortebene in `_messages` um Zeile 481 bis 496, die Projektion `_message` mit ihrem Docstring um Zeile 505 bis 528, die öffentliche `one_message` um Zeile 531 bis 556 als Stilvorbild, und `_envelope` um Zeile 697 bis 703
    - src/mcp_connector/tools/mail.py, Zeile 470 bis 512: die Vorlage aus Commit `53ba602`, insbesondere der Begründungskommentar über `entry["preview_truncated"]` und der Docstring-Satz, der den Feldnamen ausdrücklich nennt
    - src/mcp_connector/tools/chatgpt.py, Zeile 600 bis 660: `_fetch_message` als Ganzes, der Aufruf von `one_message` als Zielmuster und die zwei Zeilen 653 und 654, die den Projektionsschlüssel lesen
    - tests/unit/test_talk_tools.py: der Kappungstest um Zeile 715 bis 732, die Antwortebenen-Wächter in Zeile 361, 530, 630 und 655
    - tests/unit/test_mail_tools.py, Zeile 501 bis 547: beide Tests des Vorbild-Commits, Struktur und Docstring-Stil
    - tests/unit/test_chatgpt_fetch.py um Zeile 1290 bis 1310: der Wächter, der rot wird, wenn chatgpt.py nicht mitzieht
    - .planning/phases/12-konsistenz-und-h-rtungs-nachzieher/12-RESEARCH.md, Abschnitt "TOOL-17" und Pitfall 1
    - .planning/phases/12-konsistenz-und-h-rtungs-nachzieher/12-PATTERNS.md, Abschnitte zu `tools/talk.py` und `tools/chatgpt.py`
  </read_first>
  <action>
    Drei Dateien, ein Schritt, weil die Projektion und ihr zweiter Leser in verschiedenen Dateien
    liegen und `entry.get(...)` nicht wirft (Pitfall 1).

    (1) `src/mcp_connector/tools/talk.py`, Projektionsstelle in `_message` (heute Zeile 524 bis
        525): der Eintragsschlüssel heißt `message_truncated`. Darüber steht der
        Begründungskommentar aus dem `<interfaces>`-Block, wörtlich in Struktur und Inhalt nach
        dem Mail-Vorbild `tools/mail.py:502-508`: er erklärt nicht die Umbenennung, sondern warum
        der alte Name nicht auflösbar war, nennt `MAX_MESSAGE_BYTES` als die Grenze der
        Eintragsebene und zitiert `DF-11-01` und `IN-01`.

    (2) Derselbe Datei, Docstring von `_message` (heute Zeile 513 bis 515): der Satz nennt den
        Feldnamen jetzt ausdrücklich, so wie `tools/mail.py:475-484` es tut. Die
        ME-03-Begründung bleibt erhalten: die Kappung ist ein Feld neben dem Text und niemals ein
        Marker im Text, und ein Chat ist der billigste Ort für einen eingeschmuggelten Marker,
        weil jeder Teilnehmer einer Konversation einen schreiben darf.

    (3) Derselbe Datei, Modul-Docstring (heute Zeile 12 bis 13, "Every cut is named in the answer,
        never silent"): er darf die zwei Namen jetzt nebeneinander nennen, weil ein Modul-Docstring
        keine `tools/list`-Bytes kostet. Die Antwortebenen in `_messages` (Zeile 494 bis 496) und
        in `_envelope` (Zeile 697 bis 703) bleiben unangetastet; `_CURSOR_HINT` (Zeile 107 bis 110)
        bleibt ebenfalls, weil er über die Konversationsliste spricht und nicht über die
        Nachrichtenseite. Wird er dennoch präzisiert, muss der Talk-Sinn erhalten bleiben: die
        Konversationsliste hat kein `next`.

    (4) `src/mcp_connector/tools/chatgpt.py`, Zeile 653: gelesen wird ab jetzt
        `entry.get("message_truncated")`. Die Zuweisung `metadata["truncated"] = "true"` in Zeile
        654 bleibt genau so, weil `metadata` eine Ebene ist und dort ein Wort eine Bedeutung hat.
        Ein kurzer Kommentar sagt, warum die zwei Namen hier auseinandergehen: die Projektion
        unterscheidet zwei Ebenen, `fetch` liefert genau eine.

    (5) `tests/unit/test_talk_tools.py`: der bestehende Kappungstest (um Zeile 725 bis 730)
        behauptet `entry["message_truncated"] is True` und zusätzlich `"truncated" not in entry`.
        Die vier Behauptungen zur Marker-Hygiene in derselben Stelle bleiben wörtlich, inklusive
        `MARKER not in entry["message"]` und der Prüfung gegen das Ersetzungszeichen an der
        Byte-Grenze.

    (6) Derselben Datei kommt ein neuer Test hinzu, Vorbild `tests/unit/test_mail_tools.py:519-547`
        in Struktur und Docstring-Stil: eine einzige Antwort, in der die Seite gekappt ist und der
        erste Eintrag eine gekappte Nachricht trägt. Behauptungen: `answer["truncated"] is True`,
        der dekodierte Cursor aus `answer["next"]` trägt die Talk-Form mit dem Konversations-Scope
        `"c"` (nicht `"m"` wie in Mail, siehe `tools/talk.py:496`), `"message_truncated" not in
        answer`, der gekappte Eintrag trägt `message_truncated is True` und kein `truncated` und
        kein `next`, und ein ungekappter Eintrag derselben Antwort trägt keinen der beiden
        Schlüssel. Das ist der Fall, der die Doppelbedeutung überhaupt erst zu einem Problem
        gemacht hat, und deshalb steht er in genau einem Test statt in zwei.

    Keine Änderung an `tests/unit/test_chatgpt_fetch.py`. Wird dieser Test rot, ist das der
    Beweis, dass Schritt (4) fehlt, und nicht die Aufforderung, den Test anzupassen.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run --no-sync ruff check . &amp;&amp; uv run --no-sync ruff format --check . &amp;&amp; uv run --no-sync pyright</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run --no-sync python -c "
from pathlib import Path
talk = Path('src/mcp_connector/tools/talk.py').read_text(encoding='utf-8')
code = [l for l in talk.splitlines() if not l.lstrip().startswith('#')]
entry_lines = [l for l in code if 'entry[' in l and 'truncated' in l]
assert entry_lines, 'the entry level assignment has to exist'
assert all('message_truncated' in l for l in entry_lines), entry_lines
answer_lines = [l for l in code if 'answer[' in l and 'truncated' in l]
assert answer_lines, 'the answer level assignments have to exist'
assert all('message_truncated' not in l for l in answer_lines), answer_lines
q = chr(34)
cg = Path('src/mcp_connector/tools/chatgpt.py').read_text(encoding='utf-8')
assert 'entry.get(' + q + 'message_truncated' + q + ')' in cg, 'the consumer reads the new key'
assert 'metadata[' + q + 'truncated' + q + ']' in cg, 'the fetch metadata keeps its single meaning'
print('one key one meaning in talk, and the consumer follows')
"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run --no-sync pytest tests/unit/test_talk_tools.py tests/unit/test_chatgpt_fetch.py -q</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run --no-sync pytest -q &amp;&amp; uv run --no-sync vulture src scripts vulture_whitelist.py</automated>
  </verify>
  <acceptance_criteria>
    - Jede `entry[...]`-Zuweisung in `tools/talk.py`, die das Wort `truncated` trägt, benutzt `message_truncated`; keine `answer[...]`-Zuweisung tut das
    - Über der Zuweisung steht ein Kommentar, der `MAX_MESSAGE_BYTES`, die Antwortebene und die Befundnummern `DF-11-01` und `IN-01` nennt
    - Der Docstring von `_message` nennt den Feldnamen ausdrücklich und behält die ME-03-Begründung
    - `tools/chatgpt.py` liest `entry.get("message_truncated")`; `metadata["truncated"] = "true"` ist unverändert
    - `tests/unit/test_talk_tools.py` behauptet an der Kappungsstelle `message_truncated is True` und `"truncated" not in entry`; die vier Marker-Hygiene-Behauptungen sind wörtlich erhalten
    - Ein neuer Test belegt eine Antwort mit gekappter Seite und gekappter Nachricht mit getrennten Behauptungen je Ebene, inklusive Cursor-Scope `"c"`
    - `tests/unit/test_chatgpt_fetch.py` ist unverändert und grün
    - Die Antwortebenen-Wächter in `tests/unit/test_talk_tools.py` (Zeile 361, 530, 630, 655) sind unverändert und grün
    - `uv run --no-sync pytest -q`, `ruff check .`, `ruff format --check .`, `pyright` und `vulture src scripts vulture_whitelist.py` sind grün
  </acceptance_criteria>
  <done>Eine gekappte Talk-Nachricht heißt `message_truncated`, eine gekappte Seite heißt `truncated`, `fetch` meldet die Kappung weiterhin, und der Fall, in dem beides gleichzeitig gilt, hat einen Test.</done>
</task>

<task type="auto">
  <name>Task 2: Der Tool-Docstring sagt die eine Bedeutung je Ebene, und die Messung belegt, dass kein Gate angehoben werden muss</name>
  <files>src/mcp_connector/server/reg_talk.py</files>
  <read_first>
    - src/mcp_connector/server/reg_talk.py, ganze Datei: der Modul-Docstring, die `level`-Enum, die `cursor`-Field-Description um Zeile 40 bis 43 und der Tool-Docstring um Zeile 46 bis 48
    - src/mcp_connector/server/reg_mail.py, Zeile 28 bis 82: der Modul-Docstring-Absatz, der die Byte-Ausgabe rechtfertigt, und der Tool-Docstring mit dem Satz über die zwei Schlüssel
    - scripts/check_tool_budget.py, die Kommentarblöcke über `BUDGET_BYTES` und `MAX_TOOL_BYTES`: die Regel, dass eine Anhebung nur zusammen mit einer neuen Messzeile erlaubt ist, und dass diese Phase nichts anhebt
    - tests/contract/test_tool_surface.py, Zeile 327 bis 351: der `talk_browse`-Contract, damit klar ist, was er prüft und was nicht (er prüft den Docstring-Wortlaut nicht)
    - .planning/phases/12-konsistenz-und-h-rtungs-nachzieher/12-RESEARCH.md, Abschnitt "Byte-Budget: Messung statt Vermutung"
    - .planning/phases/12-konsistenz-und-h-rtungs-nachzieher/12-PATTERNS.md, Abschnitt zu `server/reg_talk.py`
  </read_first>
  <action>
    Zwei Änderungen in einer Datei, davon kostet nur die erste Bytes in `tools/list`.

    (1) Der Tool-Docstring von `talk_browse` (heute Zeile 46 bis 48) bekommt den Satz nach dem
        Mail-Muster, der für jede Ebene genau eine Bedeutung nennt, in der Form
        "truncated: page cut; message_truncated: message cut." Der bestehende Text über die
        Reihenfolge (neueste zuerst, die nächste Seite läuft weiter in die Vergangenheit) bleibt.
        Eine Gegenkompression an anderer Stelle ist ausdrücklich nicht nötig und nicht erwünscht:
        `talk_browse` steht bei 858 von 1400 Bytes, anders als `mail_browse`, das im
        Vorbild-Edit nur 24 Bytes Luft hatte und deshalb seine Filterzeile im gleichen Schritt
        kürzen musste.

    (2) Der Modul-Docstring bekommt den Absatz, der die Byte-Ausgabe rechtfertigt, Vorlage
        `server/reg_mail.py:31-36`: der Docstring nennt beide Kappungsschlüssel, und das ist der
        eine Satz von ihm, der sich zurückkauft, weil ein Modell, das das Eintrags-Flag als
        Seiten-Flag liest, im Kreis paginiert und dafür mehr Roundtrips zahlt als die rund 53
        Bytes kosten. Der Nachsatz des Mail-Vorbilds zur Gegenkompression entfällt, und an seiner
        Stelle steht die gemessene Luft (858 von 1400, Oberfläche 15657 von 18000), damit der
        nächste Leser sieht, warum hier nichts gekürzt wurde.

    Nicht anfassen: die `cursor`-Field-Description in Zeile 40 bis 43. Sie spricht über die
    Antwortebene ("Next page handle from a truncated answer") und bleibt damit wörtlich richtig.
    `BUDGET_BYTES` und `MAX_TOOL_BYTES` in `scripts/check_tool_budget.py` bleiben unangetastet;
    dieser Plan fügt dort keine Messzeile hinzu und hebt keine Zahl an.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run --no-sync ruff check . &amp;&amp; uv run --no-sync ruff format --check . &amp;&amp; uv run --no-sync pyright</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run --no-sync python scripts/check_tool_budget.py</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run --no-sync python -c "
import asyncio, json
from mcp import Client
from mcp_connector.server import mcp

async def main():
    async with Client(mcp, raise_exceptions=True) as client:
        result = await client.list_tools()
    payload = result.model_dump(by_alias=True, exclude_none=True, mode='json')
    blob = json.dumps(payload, separators=(',', ':'), ensure_ascii=False)
    total = len(blob.encode('utf-8'))
    sizes = {
        tool['name']: len(json.dumps(tool, separators=(',', ':'), ensure_ascii=False).encode('utf-8'))
        for tool in payload['tools']
    }
    print('total', total, 'tools', len(sizes), 'talk_browse', sizes['talk_browse'])
    assert len(sizes) == 21, len(sizes)
    assert total &lt;= 18000, total
    assert sizes['talk_browse'] &lt;= 1400, sizes['talk_browse']
    doc = [t for t in payload['tools'] if t['name'] == 'talk_browse'][0]['description']
    assert 'message_truncated' in doc and 'truncated:' in doc, doc

asyncio.run(main())
"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run --no-sync git diff --stat -- scripts/check_tool_budget.py &amp;&amp; uv run --no-sync pytest tests/contract -q &amp;&amp; uv run --no-sync pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - Der Tool-Docstring von `talk_browse` nennt `truncated` und `message_truncated` mit je einer Bedeutung, in der Form des Mail-Vorbilds
    - Der Modul-Docstring von `reg_talk.py` begründet die Byte-Ausgabe und nennt die gemessene Luft statt eines Nachsatzes über Gegenkompression
    - Die `cursor`-Field-Description in `reg_talk.py` ist unverändert
    - `uv run --no-sync python scripts/check_tool_budget.py` endet mit Exit-Code 0
    - Die Einzelmessung zeigt 21 Werkzeuge, `talk_browse` unter 1400 Bytes und die Oberfläche unter 18000 Bytes; die gemessenen Werte stehen in der SUMMARY
    - `git diff` zeigt keine Änderung an `scripts/check_tool_budget.py`, weder an `BUDGET_BYTES` noch an `MAX_TOOL_BYTES`
    - `uv run --no-sync pytest tests/contract -q` und `uv run --no-sync pytest -q` sind grün
  </acceptance_criteria>
  <done>Das Modell liest im Tool-Docstring für jede Ebene genau eine Bedeutung, und die Messung belegt, dass der Satz aus der vorhandenen Luft bezahlt wurde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Projektion zu zweitem Leser | `tools/talk.py` baut den Eintrag, `tools/chatgpt.py` liest ihn; `entry.get(...)` wirft nicht, ein Bruch ist still |
| Antwortschlüssel zu Modellverständnis | Ein Wort mit zwei Bedeutungen in derselben Antwort wird auf einer Ebene falsch gelesen |
| Fremdtext zu Antwortstruktur | Eine Kappung, die als Marker im Text steht statt als Feld daneben, ist ein Einfallstor (ME-03) |
| Tool-Docstring zu Byte-Budget | Nur der Docstring in `reg_talk.py` reist in `tools/list`; jede Zeile dort ist Kontextkosten in jeder Sitzung jedes Clients |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | `tools/chatgpt.py::_fetch_message`, Zeile 653 | mitigate | Der Verbraucher wird im gleichen Task geändert wie die Projektion; `tests/unit/test_chatgpt_fetch.py:1302` bleibt wörtlich und ist der Wächter, ein Prüfschritt behauptet `entry.get("message_truncated")` im Quelltext |
| T-12-02 | Tampering | `talk_browse`-Antwort, Eintrags- gegen Antwortebene | mitigate | Eigener Schlüssel je Ebene plus ein Test, der den gemeinsamen Fall in einer einzigen Antwort mit getrennten Behauptungen belegt; ein Prüfschritt trennt `entry[...]` von `answer[...]` im Quelltext |
| T-12-03 | Tampering | Kappungsangabe innerhalb des Nachrichtentexts | accept | Bereits mitigiert und hier nur bewahrt: die vier Marker-Hygiene-Behauptungen an der Kappungsstelle bleiben wörtlich (ME-03), dieser Plan ändert die Bauweise der Kappung nicht |
| T-12-04 | Denial of Service | Werkzeugoberfläche, `tools/list` | mitigate | Messung statt Schätzung nach jedem Edit an `reg_talk.py`; `BUDGET_BYTES` 18000 und `MAX_TOOL_BYTES` 1400 bleiben unangetastet, ein Prüfschritt behauptet 21 Werkzeuge und beide Grenzen |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | Keine Paketinstallation in diesem Plan: `pyproject.toml` und `uv.lock` bleiben unangetastet, es gibt kein `uv add`, kein `pip install` und kein `npm install`. `12-RESEARCH.md` führt die Package-Legitimacy-Prüfung ausdrücklich als nicht anwendbar |
</threat_model>

<verification>
1. `uv run --no-sync pytest -q` und `uv run --no-sync pytest tests/contract -q` sind grün, `tests/unit/test_chatgpt_fetch.py` ohne eine einzige Änderung.
2. `uv run --no-sync ruff check .`, `ruff format --check .`, `pyright` und `vulture src scripts vulture_whitelist.py` sind grün.
3. `uv run --no-sync python scripts/check_tool_budget.py` endet mit Exit-Code 0; die Einzelmessung nennt die Bytes von `talk_browse` und die Gesamtgröße, beide Werte stehen in der SUMMARY.
4. `grep -rn '\["truncated"\]' src/mcp_connector/tools/talk.py` findet ausschließlich Antwortebenen-Zuweisungen.
5. `git diff --stat` zeigt keine Änderung an `scripts/check_tool_budget.py`, `CHANGELOG.md`, `appinfo/info.xml`, `pyproject.toml` oder `uv.lock`.
</verification>

<success_criteria>
- Die Nachrichtenebene von `talk_browse(level="messages")` heißt `message_truncated`, die Antwortebene behält `truncated` (TOOL-17, ROADMAP SC1).
- Der Tool-Docstring nennt für jede Ebene genau eine Bedeutung, und der Modul-Docstring begründet die Byte-Ausgabe.
- Tests decken beide Ebenen getrennt ab, inklusive des Falls, in dem beide gleichzeitig gelten.
- `fetch` mit einer `message`-Id meldet eine gekappte Nachricht weiterhin als gekappt.
- Kein Gate wurde angehoben: 21 Werkzeuge, `BUDGET_BYTES` 18000, `MAX_TOOL_BYTES` 1400 (ROADMAP SC5).
</success_criteria>

<output>
Create `.planning/phases/12-konsistenz-und-h-rtungs-nachzieher/12-01-SUMMARY.md` when done.
Die Zusammenfassung nennt die Umbenennung `truncated` zu `message_truncated` ausdrücklich als
Antwortformat-Änderung mit der betroffenen Ebene und dem betroffenen Werkzeug, und sie führt sie
als Übergabe an Phase 13 (EXAPP-09, Changelog-Block 0.1.9) auf, weil dieser Plan den
Changelog bewusst nicht anfasst. Ebenfalls in die Zusammenfassung: die gemessenen Bytes von
`talk_browse` und der Oberfläche vor und nach dem Edit.
</output>
