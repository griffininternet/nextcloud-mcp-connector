---
phase: 12-konsistenz-und-h-rtungs-nachzieher
plan: 04
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - src/mcp_connector/tools/talk.py
  - src/mcp_connector/tools/chatgpt.py
  - tests/contract/test_module_boundaries.py
  - tests/unit/test_provider_map.py
  - README.md
  - README.de.md
  - README.fr.md
autonomous: true
requirements: [TOOL-19]

must_haves:
  truths:
    - "Kein Modul unter src/mcp_connector ruft eine _-präfixte Funktion eines Tool-Moduls auf, das nicht es selbst ist"
    - "talk.one_room ist öffentlich und trägt die Sicherheitsbegründung von T-09-21 wörtlich weiter"
    - "Ein Gate in tests/contract wird rot, wenn ein Privat-Durchgriff zwischen Tool-Modulen zurückkehrt"
    - "Das Gate bleibt grün, obwohl tools/context.py private Fremdfunktionen in Prosa nennt"
    - "Das README-Beispiel für einen unbekannten Suchprovider nennt talk-conversations, eine echte und nie registrierte Provider-Id"
    - "Ein Test hält fest, dass jede in den READMEs genannte Provider-Id entweder registriert oder namentlich als echt und unregistriert geführt ist"
  artifacts:
    - path: "src/mcp_connector/tools/talk.py"
      provides: "Öffentliche Funktion one_room mit dem Aufrufer und der T-09-21-Begründung im Docstring"
      contains: "def one_room"
    - path: "src/mcp_connector/tools/chatgpt.py"
      provides: "Der Aufruf über die öffentliche Schnittstelle statt über den Privatnamen"
      contains: "talk_tools.one_room"
    - path: "tests/contract/test_module_boundaries.py"
      provides: "AST-Gate gegen Privat-Durchgriffe zwischen Tool-Modulen, plus Gegenprobe"
      contains: "ast.Attribute"
      min_lines: 60
    - path: "tests/unit/test_provider_map.py"
      provides: "Halter für die Provider-Ids in den drei READMEs, mit Gegenprobe"
      contains: "README"
    - path: "README.md"
      provides: "Beispielzeile mit einer echten, nie registrierten Provider-Id"
      contains: "talk-conversations"
  key_links:
    - from: "src/mcp_connector/tools/chatgpt.py"
      to: "src/mcp_connector/tools/talk.py"
      via: "der Aufruf läuft über die öffentliche one_room, wie er es für one_message schon tut"
      pattern: "talk_tools\\.one_room"
    - from: "tests/contract/test_module_boundaries.py"
      to: "src/mcp_connector/tools/"
      via: "das Gate löst die Import-Aliase der geprüften Datei auf und findet Attribute auf fremden Tool-Modulen"
      pattern: "ImportFrom"
    - from: "README.md"
      to: "tests/unit/test_provider_map.py"
      via: "die Beispiel-Provider-Id wird gegen PROVIDER_KINDS und eine benannte Liste echter unregistrierter Ids gehalten"
      pattern: "talk-conversations"
---

<objective>
TOOL-19: `tools/chatgpt.py` ruft heute `talk_tools._room`, also eine private Funktion eines fremden
Tool-Moduls. Nach diesem Plan heißt sie `one_room`, ist öffentlich, und ein Gate in
`tests/contract/` wird rot, wenn ein solcher Durchgriff zurückkehrt. Dazu die zweite Hälfte des
Requirements: das README-Beispiel für einen unbekannten Suchprovider nennt statt `spreed` die echte,
nie registrierte Provider-Id `talk-conversations`.

Purpose: Der Durchgriff ist nicht nur eine Stilfrage. Der Docstring von `_room` trägt die
Mitigation von T-09-21 (nie `GET /room/{token}` mit einem Token aus einer Modellantwort), und
`11-SECURITY.md` zitiert diese Zeilen als Beleg für T-11-12 und T-11-14. Eine private Funktion mit
einem Aufrufer aus einem anderen Modul ist die Konstruktion, bei der ein Refactoring in `talk.py`
diese Mitigation mitnimmt, ohne dass jemand es merkt. `talk.one_message` ist der Präzedenzfall im
gleichen Modul: öffentlich, weil `chatgpt.py` sie braucht, mit dem Aufrufer und dem
Sicherheitsgrund im Docstring. `spreed` war dagegen nie eine Provider-Id, und die drei READMEs sind
die letzte Stelle im Repo, an der sie noch steht.

Output: eine öffentliche Schnittstelle mit ihrer Begründung, ein Gate mit Gegenprobe, und ein
Beispiel, das wahr statt erfunden ist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-konsistenz-und-h-rtungs-nachzieher/12-RESEARCH.md
@.planning/phases/12-konsistenz-und-h-rtungs-nachzieher/12-PATTERNS.md
@.planning/phases/12-konsistenz-und-h-rtungs-nachzieher/12-01-SUMMARY.md

Die geänderten Dateien:
@src/mcp_connector/tools/talk.py
@src/mcp_connector/tools/chatgpt.py
@tests/unit/test_provider_map.py
@README.md
@README.de.md
@README.fr.md

Die Vorlagen (nicht ändern):
@tests/contract/test_no_destructive_calls.py
@src/mcp_connector/tools/context.py
@src/mcp_connector/provider_map.py

<interfaces>
Fundstellen (12-RESEARCH.md, Abschnitt TOOL-19; alle im Arbeitsbaum gelesen):

  src/mcp_connector/tools/talk.py:615      async def _room(clients, token, *, include_last_message)
  src/mcp_connector/tools/talk.py:615-626  Docstring mit der T-09-21-Begruendung, wandert woertlich mit
  src/mcp_connector/tools/talk.py:291      Aufrufer in send
  src/mcp_connector/tools/talk.py:481      Aufrufer in _messages
  src/mcp_connector/tools/talk.py:234      Prosa-Nennung: "(in :func:`_room`)"
  src/mcp_connector/tools/talk.py:531      one_message, der Praezedenzfall im gleichen Modul
  src/mcp_connector/tools/chatgpt.py:616   der Durchgriff
  src/mcp_connector/tools/chatgpt.py:621   talk_tools.one_message, das Zielmuster zwei Zeilen weiter

Bestandsaufnahme statt Vermutung (uv run --no-sync ruff check --select SLF src/, Lauf 2026-08-25):
  drei Treffer im ganzen Produktionsbaum
    oauth/provider.py:554   view._also nach copy.copy(self), eigenes Klassen-Internum
    oauth/provider.py:1544  self._provider._now(), eigenes Klassen-Internum
    tools/chatgpt.py:616    der einzige Durchgriff ueber eine Modulgrenze
  In tests/ 53 Treffer, alle legitime Quelltext-Gates.
Deshalb AST-Gate in tests/contract/ und NICHT ruff --select SLF: die Lint-Regel braeuchte zwei
noqa-Ausnahmen in oauth/provider.py plus per-file-ignores fuer tests/**, und sie prueft mehr als
das Requirement sagt. Diese Messung ist die Begruendung und gehoert in den Docstring des Gates.

Prosa-Nennungen, die das Gate NICHT treffen darf (tools/context.py):
  :340 mail_tools._mailboxes   :355 mail_tools._messages
  :394 mail_tools._special_role   :605 mcp_connector.tools.talk._conversation
Ein AST-Gate sieht sie nicht, weil ein Docstring und ein Kommentar keine Attribute-Knoten sind.
Das ist der Grund fuer die AST-Bauweise und nicht ein Nebeneffekt (Pitfall 2): die naheliegende
Reparatur eines roten Grep-Gates waere, die erklaerenden Saetze zu loeschen.

Alias-Muster im Baum, Grundlage der Auflösung (chatgpt.py:67-74 und Varianten):
  from . import talk as talk_tools
  from . import mail as mail_tools
  from ..tools import talk as talk_tools
Alle Formen resolven auf einen Modulstamm unter src/mcp_connector/tools/.

Die README-Zeile, drei Dateien, identischer Text:
  README.md:372, README.de.md:381, README.fr.md:389
  zu aendern ist genau "provider":"spreed" zu "provider":"talk-conversations"
  "kind":"url" und "resolvable":false bleiben stehen und werden damit wahr statt erfunden

Die Belege fuer die Ersatz-Id (tests/unit/test_provider_map.py, unveraendert gueltig):
  :287-299  test_the_provider_table_is_not_a_list_of_installed_apps
            friert die Menge ein und behauptet "talk-conversations" not in PROVIDER_KINDS
  :288      test_a_talk_conversation_hit_stays_a_url_because_a_conversation_is_no_document
            belegt fuer genau diese Id kind == "url" und canonical is False
  :78-80    haelt schriftlich fest, dass spreed nie eine Provider-Id war
  provider_map.py:42-43  Modul-Docstring: eine Konversation ist kein Dokument, talk_browse ist der
            Weg zu ihr, deshalb steht talk-conversations absichtlich nicht in der Tabelle

11-SECURITY.md zitiert den Docstring von _room. Dieses Dokument wird NICHT geaendert: es ist das
Protokoll einer abgeschlossenen Phase. Das Zitat bleibt wahr, weil der Text woertlich mitwandert.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aus _room wird one_room, und die Sicherheitsbegründung wandert wörtlich mit</name>
  <files>src/mcp_connector/tools/talk.py, src/mcp_connector/tools/chatgpt.py</files>
  <read_first>
    - src/mcp_connector/tools/talk.py: die Funktion `_room` mit ihrem vollständigen Docstring um Zeile 615 bis 640, die drei Aufrufstellen in Zeile 291 und 481, die Prosa-Nennung in Zeile 234 und die öffentliche `one_message` um Zeile 531 bis 556 als Vorbild für Sichtbarkeit und Docstring-Aufbau
    - src/mcp_connector/tools/chatgpt.py, Zeile 600 bis 630: der Durchgriff in Zeile 616 und das Zielmuster `talk_tools.one_message` in Zeile 621
    - vulture_whitelist.py: der Nachweis, dass `_room` dort nicht steht und eine öffentliche Funktion mit drei Aufrufern keinen Eintrag braucht
    - .planning/phases/12-konsistenz-und-h-rtungs-nachzieher/12-RESEARCH.md, Abschnitt TOOL-19 und Open Question 1
    - .planning/phases/12-konsistenz-und-h-rtungs-nachzieher/12-PATTERNS.md, Pattern 3 und der Abschnitt zu `tools/chatgpt.py`
  </read_first>
  <action>
    Der öffentliche Name ist festgelegt und wird nicht neu entschieden: `one_room`. Begründung, die
    in die SUMMARY gehört: das Modul hat mit `one_message` schon eine Konvention für "genau ein
    Objekt aus einer gelesenen Menge", und ein zweiter Namensstil im gleichen Modul wäre eine
    Konvention mit zwei Formen. `room_of` aus `11-REVIEW.md` IN-06 ist vertretbar, aber nicht
    konsistent.

    (1) `src/mcp_connector/tools/talk.py`: `_room` heißt `one_room`. Die Signatur bleibt Zeichen für
        Zeichen dieselbe, insbesondere `include_last_message` als keyword-only, damit die drei
        Aufrufstellen unverändert lesbar bleiben. Der Docstring wandert **wörtlich** mit: der
        Absatz über `GET /room/{token}`, den gezählten Brute-Force-Versuch gegen die Adresse dieses
        Containers, die 429 und den Satz, dass die Liste denselben einen Request kostet, keinen
        Token im Pfad trägt und die eigenen Daten des Kontos sind (T-09-21). Kein Umformulieren,
        kein Kürzen: `11-SECURITY.md` zitiert diese Zeilen als Beleg für T-11-12 und T-11-14, und
        das Zitat bleibt nur wahr, wenn der Text derselbe ist.

    (2) Nach dem Vorbild von `one_message` kommt ein Satz dazu, der sagt, warum die Funktion
        öffentlich ist: `tools/chatgpt.py` braucht sie für `fetch`, und ein privater Name mit einem
        Aufrufer aus einem anderen Modul ist genau die Konstruktion, in der ein Refactoring die
        Mitigation von T-09-21 mitnehmen kann, ohne dass ein Gate es merkt. Dieser Satz ist neu, die
        Begründung darunter ist alt.

    (3) Die zwei modulinternen Aufrufstellen (Zeile 291 in `send`, Zeile 481 in `_messages`) und die
        Prosa-Nennung in Zeile 234 (`(in :func:`_room`)`) ziehen auf den neuen Namen mit.

    (4) `src/mcp_connector/tools/chatgpt.py`, Zeile 616: der Aufruf lautet
        `await talk_tools.one_room(clients, token, include_last_message=False)`. Der Importblock
        bleibt unverändert. Damit stehen die zwei Aufrufe in `_fetch_message` im gleichen Stil, und
        Zeile 621 ist nicht mehr das Zielmuster, sondern der Nachbar.

    Nicht anfassen: `.planning/phases/11-*/11-SECURITY.md` und alle anderen Phasendokumente. Sie
    sind Protokolle abgeschlossener Phasen; ein Nachziehen von Zitaten dort wäre eine
    Rückschreibung.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run --no-sync ruff check . &amp;&amp; uv run --no-sync ruff format --check . &amp;&amp; uv run --no-sync pyright</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run --no-sync python -c "
import inspect
from pathlib import Path
from mcp_connector.tools import talk as talk_tools

assert hasattr(talk_tools, 'one_room'), 'the public name has to exist'
assert not hasattr(talk_tools, '_room'), 'the private name has to be gone'
signature = inspect.signature(talk_tools.one_room)
parameter = signature.parameters['include_last_message']
assert parameter.kind is inspect.Parameter.KEYWORD_ONLY, parameter.kind
doc = inspect.getdoc(talk_tools.one_room) or ''
for sentence in ('T-09-21', 'brute force', '429'):
    assert sentence in doc, sentence
assert 'chatgpt' in doc.lower() or 'fetch' in doc.lower(), 'the docstring names its caller'
source = Path('src/mcp_connector/tools/talk.py').read_text(encoding='utf-8')
assert '_room' not in source.replace('one_room', '').replace('get_rooms', ''), 'no private name left'
print('one_room is public and carries its reason')
"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run --no-sync python -c "
from pathlib import Path
cg = Path('src/mcp_connector/tools/chatgpt.py').read_text(encoding='utf-8')
assert 'talk_tools.one_room(' in cg, 'the caller uses the public interface'
assert 'talk_tools._room' not in cg, 'the private reach has to be gone'
assert 'talk_tools.one_message(' in cg, 'the neighbour call is untouched'
print('the caller goes through the front door')
"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run --no-sync ruff check --select SLF src/</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run --no-sync pytest -q &amp;&amp; uv run --no-sync vulture src scripts vulture_whitelist.py &amp;&amp; git diff --exit-code -- .planning/phases/11-b-ndelung-budget-und-release-0-1-6/</automated>
  </verify>
  <acceptance_criteria>
    - `talk_tools.one_room` existiert, `talk_tools._room` existiert nicht mehr, und `include_last_message` ist weiter keyword-only
    - Der Docstring von `one_room` enthält den T-09-21-Absatz wörtlich, inklusive der Aussagen über `GET /room/{token}`, den gezählten Versuch, die 429 und den einen Request der Liste
    - Der Docstring nennt zusätzlich den Aufrufer und den Grund für die Sichtbarkeit
    - `tools/chatgpt.py` ruft `talk_tools.one_room(...)`; der Import und der Aufruf von `one_message` sind unverändert
    - Die zwei modulinternen Aufrufstellen und die Prosa-Nennung in `tools/talk.py` nennen den neuen Namen
    - `uv run --no-sync ruff check --select SLF src/` meldet nur noch die zwei Treffer in `oauth/provider.py`
    - `uv run --no-sync vulture src scripts vulture_whitelist.py` ist grün, ohne neuen Whitelist-Eintrag
    - `git diff --exit-code` über die Phase-11-Dokumente ist sauber: kein Phasendokument wurde nachgezogen
    - `uv run --no-sync pytest -q` und `pyright` sind grün
  </acceptance_criteria>
  <done>Der letzte Privat-Durchgriff im Produktionsbaum ist eine öffentliche Schnittstelle, und die Sicherheitsbegründung, die an ihr hängt, steht unverändert an ihrem neuen Platz.</done>
</task>

<task type="auto">
  <name>Task 2: Ein AST-Gate hält die Modulgrenze, und eine Gegenprobe hat es rot gesehen</name>
  <files>tests/contract/test_module_boundaries.py</files>
  <read_first>
    - tests/contract/test_no_destructive_calls.py, ganze Datei: der Modul-Docstring-Stil, die Wurzelkonstante und `_source_files` mit ihrer Nicht-leer-Behauptung um Zeile 287 bis 292, die geteilte Prüffunktion `_violations` um Zeile 323 bis 353 mit ihrem Docstring-Satz über die Gegenprobe, das Gate selbst und die Gegenprobe um Zeile 378 bis 392
    - src/mcp_connector/tools/context.py, Zeile 330 bis 400 und 590 bis 610: die vier Prosa-Nennungen privater Fremdfunktionen, die das Gate nicht treffen darf
    - src/mcp_connector/tools/chatgpt.py, Zeile 58 bis 80: der Importblock mit den Alias-Formen, die das Gate auflösen muss
    - Weitere Alias-Formen im Baum: `grep -rn "^from .* import .* as .*_tools\|^from \.\. import\|^from \. import" src/mcp_connector/` als Bestandsaufnahme vor dem Schreiben
    - .github/workflows/ci.yml, Zeile 36: der Beleg, dass `tests/contract` ohne neuen Workflow-Schritt läuft
    - .planning/phases/12-konsistenz-und-h-rtungs-nachzieher/12-RESEARCH.md, Pattern 2, Open Question 2 und Pitfall 2
    - .planning/phases/12-konsistenz-und-h-rtungs-nachzieher/12-PATTERNS.md, Abschnitt zu `tests/contract/test_module_boundaries.py` und "No Analog Found"
  </read_first>
  <action>
    Neue Datei `tests/contract/test_module_boundaries.py`. Die Bauweise ist entschieden: AST, nicht
    `ruff --select SLF`. Die Begründung steht als Messung im Modul-Docstring: `SLF` findet in `src/`
    drei Treffer, davon zwei legitime Zugriffe auf eigene Klassen-Interna in `oauth/provider.py`,
    und in `tests/` 53 legitime Treffer, also kostet die Lint-Regel zwei `noqa`-Zeilen und ein
    `per-file-ignores`, und sie prüft mehr als die Regel dieses Requirements sagt.

    Aufbau, in der Reihenfolge des Vorbilds:

    (1) Modul-Docstring im Stil von `test_no_destructive_calls.py`: welche Zusage das Gate hält,
        warum sie ein Gate braucht und nicht Disziplin, warum AST und nicht Grep (die vier
        Prosa-Nennungen in `context.py` würden ein Grep-Gate sofort rot machen, und die naheliegende
        Reparatur wäre, die erklärenden Sätze zu löschen), und dass jeder Fund Datei und Zeile
        nennt, damit ein Verstoß eine Einzeilenkorrektur ist.

    (2) Konstanten: die Wurzel `src/mcp_connector` und das Verzeichnis `tools` darunter, beide über
        `Path(__file__).resolve().parents[2]`, wie im Vorbild. Dazu die Menge der Modulstämme unter
        `tools/`, aus dem Verzeichnis gelesen und nicht aufgezählt, plus eine Nicht-leer-Behauptung
        nach dem Muster von `_source_files`.

    (3) Eine Funktion, die für **einen Quelltext und einen Namen** (nicht für einen Pfad) die
        Fundstellen liefert. Sie parst mit `ast`, sammelt zuerst aus den `Import`- und
        `ImportFrom`-Knoten der Datei eine Abbildung von lokalem Namen auf den Modulstamm, den er
        bezeichnet, und berücksichtigt dabei alle im Baum vorkommenden Formen: `from . import talk
        as talk_tools`, `from . import talk`, `from ..tools import talk as talk_tools`,
        `from mcp_connector.tools import talk as ...` und `import mcp_connector.tools.talk as ...`.
        Aufgenommen wird ein Name nur, wenn sein Modulstamm eine `.py`-Datei unter `tools/`
        bezeichnet und nicht die gerade geprüfte Datei selbst ist. Danach läuft sie über die
        `ast.Attribute`-Knoten: ein Fund ist ein Attribut, dessen `value` ein `ast.Name` aus dieser
        Abbildung ist und dessen `attr` mit einem einzelnen Unterstrich beginnt. Dunder-Namen
        (`__file__`, `__name__`) sind kein Fund. Jeder Fund wird als `datei:zeile: alias.attr`
        formatiert.

        Diese Funktion wird von Gate **und** Gegenprobe benutzt. Eine Gegenprobe, die die Prüfung
        nachbaut, beweist etwas über die Gegenprobe (Docstring-Satz des Vorbilds).

    (4) Das Gate: über alle `.py`-Dateien unter `src/mcp_connector` laufen (nicht nur über
        `tools/`, denn ein Durchgriff aus `server/` oder `oauth/` auf ein Tool-Internum wäre
        derselbe Bruch), Fundstellen sammeln, `assert findings == []` mit einer Meldung, die alle
        Funde untereinander auflistet. Ein `self._x` und ein modulinterner `_helper()` sind
        konstruktionsbedingt kein Fund, weil sie kein Attribut auf einem Modul-Alias sind, und
        genau dieser Satz gehört als Kommentar dazu.

    (5) Erste Gegenprobe: eine konstruierte Quelle als String, die drei Dinge gleichzeitig enthält,
        nämlich einen Import in Alias-Form, einen echten Aufruf `talk_tools._room(...)` und
        dieselbe Zeichenkette einmal in einem Docstring und einmal in einem Kommentar. Erwartet
        wird **genau ein** Fund, mit der Zeilennummer des Aufrufs. Damit belegt eine einzige
        Behauptung beide Hälften: das Gate findet den Durchgriff, und es findet die Prosa nicht.

    (6) Zweite Gegenprobe: die echte `tools/context.py` durch dieselbe Funktion schicken und null
        Funde erwarten, mit einem Docstring, der sagt, dass diese Datei vier private
        Fremdfunktionen in Prosa nennt und deshalb die ehrliche Fixture für den Filter ist.

    (7) Dritte Behauptung: die Menge der geprüften Dateien ist nicht leer und enthält
        `tools/chatgpt.py` und `tools/context.py`, sonst schützt ein Tippfehler im Pfad nichts.

    Keine Änderung an `pyproject.toml`: kein `select = ["SLF"]`, kein `per-file-ignores`, keine
    `noqa`-Zeile in `oauth/provider.py`.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run --no-sync ruff check . &amp;&amp; uv run --no-sync ruff format --check . &amp;&amp; uv run --no-sync pyright</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run --no-sync pytest tests/contract/test_module_boundaries.py -q</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run --no-sync python -c "
from pathlib import Path
gate = Path('tests/contract/test_module_boundaries.py')
text = gate.read_text(encoding='utf-8')
assert 'ast.Attribute' in text, 'the check walks nodes, not strings'
assert 'ImportFrom' in text, 'the alias resolution reads the import nodes'
assert 'SLF' in text, 'the docstring carries the measurement that justifies the shape'
assert 'context.py' in text, 'the honest fixture is named'
assert text.count('def test_') &gt;= 3, 'gate plus two counter probes'
print('the gate is built the way the plan says')
"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run --no-sync python -c "
import subprocess
from pathlib import Path

# Counter probe on the real tree: put the private reach back and require a red gate.
source = Path('src/mcp_connector/tools/chatgpt.py')
original = source.read_text(encoding='utf-8')
broken = original.replace('talk_tools.one_room(', 'talk_tools._room(', 1)
assert broken != original, 'the anchor call has to exist'
try:
    source.write_text(broken, encoding='utf-8')
    done = subprocess.run(
        ['uv', 'run', '--no-sync', 'pytest', 'tests/contract/test_module_boundaries.py', '-q'],
        capture_output=True, text=True,
    )
    assert done.returncode != 0, done.stdout[-2000:]
    assert 'chatgpt.py' in done.stdout, done.stdout[-2000:]
finally:
    source.write_text(original, encoding='utf-8')
assert source.read_text(encoding='utf-8') == original, 'the production source is restored'
print('the gate goes red on a returning private reach, and names the file')
"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; git diff --exit-code -- src/mcp_connector/tools/chatgpt.py pyproject.toml &amp;&amp; uv run --no-sync pytest -q &amp;&amp; uv run --no-sync pytest tests/contract -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/contract/test_module_boundaries.py` existiert und ist über `pytest tests/contract` grün, ohne Änderung an `.github/workflows/ci.yml`
    - Die Prüfung läuft über `ast.Attribute`-Knoten und löst die Import-Aliase der geprüften Datei auf; alle im Baum vorkommenden Import-Formen sind behandelt
    - Das Gate deckt alle `.py`-Dateien unter `src/mcp_connector` ab, nicht nur `tools/`
    - Jeder Fund nennt Datei, Zeile und den Ausdruck; Dunder-Attribute sind kein Fund
    - Gate und Gegenproben benutzen dieselbe Prüffunktion, und diese nimmt Text und Namen als Parameter
    - Die erste Gegenprobe mit konstruierter Quelle erwartet genau einen Fund und belegt damit, dass Docstring und Kommentar nicht getroffen werden
    - Die zweite Gegenprobe schickt die echte `tools/context.py` durch die Prüfung und erwartet null Funde
    - Eine Behauptung stellt sicher, dass die Dateiliste nicht leer ist und `tools/chatgpt.py` und `tools/context.py` enthält
    - Der Lauf gegen ein manipuliertes `tools/chatgpt.py` schlägt fehl, die Fehlermeldung nennt `chatgpt.py`, und `git diff --exit-code` ist danach sauber
    - `pyproject.toml` ist unverändert: kein `SLF` im `select`, kein neues `per-file-ignores`, keine `noqa`-Zeile in `oauth/provider.py`
    - `uv run --no-sync pytest -q` und `pytest tests/contract -q` sind grün
  </acceptance_criteria>
  <done>Die Modulgrenze zwischen den Tool-Familien ist ein Gate und nicht mehr eine Absprache, und das Gate ist einmal rot gesehen worden, ohne einen erklärenden Satz zu kosten.</done>
</task>

<task type="auto">
  <name>Task 3: Das README-Beispiel nennt eine echte, nie registrierte Provider-Id, und ein Halter hält es fest</name>
  <files>README.md, README.de.md, README.fr.md, tests/unit/test_provider_map.py</files>
  <read_first>
    - README.md, Zeile 372, sowie README.de.md, Zeile 381, und README.fr.md, Zeile 389: die identische Beispielzeile mit `"provider":"spreed"`, samt der Sätze davor und danach, die die Zeile erklären
    - src/mcp_connector/provider_map.py: der Modul-Docstring um Zeile 42 bis 43 zur bewussten Abwesenheit von `talk-conversations`, `PROVIDER_KINDS` und die Rest-Kategorie darunter
    - tests/unit/test_provider_map.py, Zeile 78 bis 80 und 287 bis 299: die zwei Tests, die die Ersatz-Id als echt und nie registriert belegen
    - tests/contract/test_tool_surface.py, Zeile 752 bis 776: das Muster für einen Lauf über mehrere Markdown-Seiten mit einer sprechenden Fehlermeldung
    - .planning/phases/12-konsistenz-und-h-rtungs-nachzieher/12-RESEARCH.md, Abschnitt TOOL-19 (Das README-Beispiel) und Open Question 4
  </read_first>
  <action>
    (1) In allen drei READMEs wird in der Beispielzeile genau `"provider":"spreed"` zu
        `"provider":"talk-conversations"`. Sonst ändert sich an der Zeile nichts: `"kind":"url"`
        und `"resolvable":false` bleiben stehen und werden mit dieser Änderung wahr statt erfunden,
        belegt durch
        `test_a_talk_conversation_hit_stays_a_url_because_a_conversation_is_no_document`. Der
        URL-Teil `/index.php/call/abc123` bleibt, `abc123` erfüllt das Token-Alphabet. Umlaute und
        Akzente der umgebenden Prosa bleiben unangetastet, es sind drei Ein-Zeichenketten-Änderungen
        und keine Neuformulierung.

    (2) In `tests/unit/test_provider_map.py` kommt der Halter dazu, der genau diesen Fehler nicht
        wiederkehren lässt. Er ist bewusst klein: eine Funktion, die für **einen Text und einen
        Namen** alle Vorkommen von `"provider":"..."` einsammelt, plus ein Test, der sie über die
        drei READMEs laufen lässt und für jede gefundene Id verlangt, dass sie entweder in
        `PROVIDER_KINDS` steht oder in einer im Test benannten Liste echter, absichtlich nicht
        registrierter Ids (heute genau `talk-conversations`). Der Docstring sagt, warum diese Liste
        hier steht und nicht in `provider_map.py`: das Produktionsmodul führt eine
        Übersetzungstabelle und keine Liste von Ids, die es nicht übersetzt, und der Modul-Docstring
        dort erklärt die Abwesenheit von `talk-conversations` bereits in Prosa.

        Der Test nennt in seiner Fehlermeldung Datei und Id, weil ein Verstoß sonst eine Suche über
        drei Dateien wäre. Dazu die Nicht-leer-Behauptung: in jeder der drei Dateien wird
        mindestens eine Provider-Id gefunden, sonst wäre ein umformatiertes Beispiel ein stiller
        Verlust des Halters.

    (3) Gegenprobe im gleichen Stil wie im Rest des Repos: dieselbe Funktion bekommt einen
        konstruierten Text mit `"provider":"spreed"` und der Test verlangt einen Fund, der als
        unerklärt gemeldet wird. Ohne sie wäre der Halter bei Einführung grün, ohne je etwas
        geprüft zu haben.

    Keine weitere README-Änderung in diesem Plan. Die Tool-Tabellen sind nicht betroffen, die
    Werkzeugzahl bleibt 21, und `message_truncated` erscheint in keinem README-Beispiel, weil dort
    kein Talk-Antwortbeispiel steht (geprüft: `truncated` kommt in den READMEs nur im
    `files_list`-Beispiel vor).
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run --no-sync ruff check . &amp;&amp; uv run --no-sync ruff format --check . &amp;&amp; uv run --no-sync pyright</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run --no-sync python -c "
import re
from pathlib import Path
from mcp_connector import provider_map

q = chr(34)
pattern = q + 'provider' + q + ':' + q + '([a-z0-9-]+)' + q
allowed_unregistered = {'talk-conversations'}
for name in ('README.md', 'README.de.md', 'README.fr.md'):
    text = Path(name).read_text(encoding='utf-8')
    found = set(re.findall(pattern, text))
    assert found, (name, 'no provider id found, the example has been reformatted')
    assert 'spreed' not in found, (name, 'spreed was never a provider id')
    assert 'talk-conversations' in found, (name, 'the real unregistered id has to be named')
    unexplained = found - set(provider_map.PROVIDER_KINDS) - allowed_unregistered
    assert unexplained == set(), (name, unexplained)
    for forbidden in (chr(0x2014), chr(0x2013)):
        assert forbidden not in text, (name, 'no em dash and no en dash')
print('three readmes name provider ids that exist')
"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run --no-sync python -c "
from pathlib import Path
text = Path('tests/unit/test_provider_map.py').read_text(encoding='utf-8')
assert 'README' in text, 'the holder reads the readmes'
assert 'talk-conversations' in text
assert 'spreed' in text, 'the counter probe feeds the id that used to stand there'
print('the holder and its counter probe exist')
"</automated>
    <automated>cd /c/Users/Student/nextcloud-mcp-connector &amp;&amp; uv run --no-sync pytest tests/unit/test_provider_map.py -q &amp;&amp; uv run --no-sync pytest -q &amp;&amp; uv run --no-sync pytest tests/contract -q</automated>
  </verify>
  <acceptance_criteria>
    - In allen drei READMEs steht `"provider":"talk-conversations"`; `spreed` kommt in keinem der drei vor
    - `"kind":"url"`, `"resolvable":false` und der URL-Teil der Beispielzeile sind unverändert
    - Ein Test in `tests/unit/test_provider_map.py` prüft jede in den drei READMEs genannte Provider-Id gegen `PROVIDER_KINDS` plus eine benannte Liste echter unregistrierter Ids und nennt in der Fehlermeldung Datei und Id
    - Der Docstring des Tests begründet, warum die Liste im Test und nicht in `provider_map.py` steht
    - Eine Behauptung stellt sicher, dass in jeder der drei Dateien mindestens eine Provider-Id gefunden wird
    - Eine Gegenprobe speist derselben Funktion einen konstruierten Text mit `"provider":"spreed"` und erwartet einen unerklärten Fund
    - `tests/unit/test_provider_map.py` behält seine bestehenden Tests unverändert, insbesondere den eingefrorenen Mengen-Halter
    - Keine README enthält Em-Dashes oder En-Dashes; die deutschen Umlaute und französischen Akzente der umgebenden Prosa sind unverändert
    - `uv run --no-sync pytest -q` und `pytest tests/contract -q` sind grün
  </acceptance_criteria>
  <done>Das Beispiel für einen unbekannten Suchprovider ist wahr, und die Stelle, an der drei Releases lang eine erfundene Id stand, hat jetzt einen Halter mit Gegenprobe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Modellantwort zu Talk-Route | Ein Konversations-Token aus einer Modellantwort darf nie in den Pfad von `GET /room/{token}` gelangen (T-09-21) |
| Tool-Modul zu Tool-Modul | Ein privater Name ist die Zusage, dass niemand von außen daran hängt; ein Durchgriff bricht sie unsichtbar |
| Dokumentation zu Nutzer | Ein Beispiel mit einer erfundenen Id lehrt ein falsches Modell der Provider-Landschaft |
| Erklärende Prosa zu Gate | Ein Grep-Gate über Quelltext macht Dokumentation zum Kostenfaktor und lädt zum Löschen von Sätzen ein |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-14 | Elevation of Privilege | `tools/chatgpt.py` zu `tools/talk.py`, Privat-Durchgriff | mitigate | Öffentliche `one_room` mit dem Aufrufer im Docstring; ein AST-Gate in `tests/contract/` wird rot, wenn der Durchgriff zurückkehrt, und der Lauf gegen eine manipulierte Quelle belegt es |
| T-12-15 | Information Disclosure | Mitigation von T-09-21 im Docstring von `_room` | mitigate | Der Absatz wandert wörtlich mit; ein Prüfschritt behauptet `T-09-21`, `brute force` und `429` im Docstring der neuen Funktion, und die Phase-11-Dokumente bleiben unangetastet, damit ihr Zitat wahr bleibt |
| T-12-16 | Repudiation | Erklärende Prosa in `tools/context.py` | mitigate | AST statt Grep: Docstrings und Kommentare sind keine Attribute-Knoten. Eine Gegenprobe schickt die echte `context.py` durch die Prüfung und erwartet null Funde |
| T-12-17 | Spoofing | README-Beispiel mit `spreed` | mitigate | Ersetzung durch `talk-conversations`, belegt durch zwei bestehende Tests, plus ein Halter über die drei READMEs mit Gegenprobe |
| T-12-18 | Tampering | Lint-Ausnahmen als Kollateralschaden eines Gates | accept | `ruff --select SLF` wird bewusst nicht eingeführt: es kostet zwei `noqa`-Zeilen in `oauth/provider.py` und ein `per-file-ignores` für 53 legitime Treffer in `tests/`, und es prüft mehr als die Regel. Die Messung steht im Docstring des AST-Gates |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | Keine Paketinstallation in diesem Plan: `pyproject.toml` und `uv.lock` bleiben unangetastet, das Gate benutzt ausschließlich die Standardbibliothek (`ast`, `pathlib`) |
</threat_model>

<verification>
1. `uv run --no-sync pytest -q` und `uv run --no-sync pytest tests/contract -q` sind grün.
2. `uv run --no-sync ruff check .`, `ruff format --check .`, `pyright` und `vulture src scripts vulture_whitelist.py` sind grün, ohne neuen Whitelist-Eintrag.
3. `uv run --no-sync ruff check --select SLF src/` meldet genau die zwei Treffer in `oauth/provider.py` und keinen in `tools/`.
4. Die zwei Gegenproben-Läufe gegen manipulierte Quellen schlagen fehl, und `git diff --exit-code` über `src/mcp_connector/tools/chatgpt.py` ist danach sauber.
5. `git diff --stat` zeigt keine Änderung an `pyproject.toml`, `uv.lock`, `.github/workflows/ci.yml`, `CHANGELOG.md`, `appinfo/info.xml` oder an einem Dokument unter `.planning/phases/11-*`.
</verification>

<success_criteria>
- Kein Tool-Modul ruft eine `_`-präfixte Funktion eines fremden Tool-Moduls; `talk_tools._room` ist `talk.one_room` (TOOL-19, ROADMAP SC3).
- Ein Gate in `tests/contract/` wird rot, wenn ein Privat-Durchgriff zurückkehrt, und bleibt grün, obwohl `tools/context.py` private Fremdfunktionen in Prosa nennt.
- Die Sicherheitsbegründung von T-09-21 steht wörtlich am neuen Platz, und die zitierenden Phasendokumente sind unangetastet.
- Das README-Beispiel nennt in allen drei Sprachen `talk-conversations`, eine echte und nie registrierte Provider-Id, und ein Halter mit Gegenprobe hält das fest.
</success_criteria>

<output>
Create `.planning/phases/12-konsistenz-und-h-rtungs-nachzieher/12-04-SUMMARY.md` when done.
Die Zusammenfassung nennt die Namenswahl `one_room` mit ihrer Begründung, protokolliert den
`SLF`-Messwert vor und nach der Umbenennung als Beleg für die Vollständigkeit, und hält fest, dass
die Umbenennung eine rein interne Änderung ist: kein Werkzeugname, kein Antwortschlüssel und keine
Id-Form ändert sich, sie gehört also nicht in den Changelog von 0.1.9. Die README-Korrektur gehört
dagegen als Doku-Korrektur hinein und wird als solche an Phase 13 übergeben.
</output>
