---
phase: 13-cimd-nachmessung-und-release-0-1-9
plan: 03
type: execute
wave: 3
depends_on: ["13-01", "13-02"]
files_modified:
  - .planning/phases/13-cimd-nachmessung-und-release-0-1-9/13-MEASUREMENTS.md
  - docs/oauth-setup.md
autonomous: true
requirements: [EXAPP-08]

must_haves:
  truths:
    - "Die laufende Topologie führt den 0.1.9-Kandidaten, belegt durch Version UND Image-Digest"
    - "Ein CIMD-Client verbindet sich ohne Registrierung: client_id ist die https-Adresse seines Dokuments"
    - "Im Messfenster gibt es keinen POST /register, und die geschriebene clients-Zeile trägt client_secret_hash NULL plus gesetztes cimd_fetched_at"
    - "Mit abgeschaltetem Schalter wird die Dokumentadresse abgelehnt und kein Paket geht nach außen"
    - "Eine datierte, selbsttragende Proof-Zeile steht in docs/oauth-setup.md und verweist auf kein Nachbardokument"
    - "Kein Credential steht in einem Beweisdokument"
  artifacts:
    - path: ".planning/phases/13-cimd-nachmessung-und-release-0-1-9/13-MEASUREMENTS.md"
      provides: "Rohprotokoll mit Topologie-Tabelle, Rundlauf und Gegenprobe"
      min_lines: 80
    - path: "docs/oauth-setup.md"
      provides: "dauerhafte datierte CIMD-Proof-Zeile, toter Verweis geschlossen"
      contains: "Measured live on 2026-08"
  key_links:
    - from: "docs/oauth-setup.md"
      to: "nichts"
      via: "die neue Proof-Zeile trägt ohne Nachbardokument, kein .planning-Pfad bleibt in der Datei"
      pattern: "^(?!.*\\.planning).*$"
    - from: "13-MEASUREMENTS.md Topologie-Tabelle"
      to: "127.0.0.1:5000/mcp_connector:0.1.9"
      via: "Version und Manifest-Digest des gemessenen Images"
      pattern: "sha256:"
---

<objective>
Der CIMD-Weg wird gegen den 0.1.9-Kandidaten nachgemessen, nicht behauptet: ein E2E-Rundlauf
durch Caddy, HaRP und Nextcloud, eine Gegenprobe mit abgeschaltetem Schalter, ein Rohprotokoll
und eine dauerhafte Proof-Zeile.

Purpose: EXAPP-08 und der v1.1-Debt-Befund W-5. Der letzte Live-Beleg der Kette stammt vom
2026-08-20 gegen 0.1.2 und ist älter als die Review-Fixes `a47bb57` (`may_fetch=False` weg
von den heißen Pfaden) und `bd75cd8` (Allowlist VOR dem Fetch). Gemessen wird gegen den
Quellstand, der getaggt wird, und VOR dem Tag: der Tag hängt an einer Owner-Freigabe, die
zeitlich offen ist, und ein Beweis hinter dieser Freigabe wäre von ihr abhängig.
Output: `13-MEASUREMENTS.md` als Rohprotokoll, eine datierte Proof-Zeile in
`docs/oauth-setup.md`, ein geschlossener toter Verweis.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-cimd-nachmessung-und-release-0-1-9/13-CONTEXT.md
@.planning/phases/13-cimd-nachmessung-und-release-0-1-9/13-RESEARCH.md
@.planning/phases/13-cimd-nachmessung-und-release-0-1-9/13-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Topologie auf den 0.1.9-Kandidaten heben und protokollieren</name>
  <files>.planning/phases/13-cimd-nachmessung-und-release-0-1-9/13-MEASUREMENTS.md</files>
  <read_first>
    - scripts/bootstrap_exapp.sh, Zeile 1039-1041 (`app_version()` liest `<version>` aus appinfo/info.xml) und Zeile 1198-1215 (`ensure_exapp()`: kehrt zurück, sobald die App-Id in `occ app_api:app:list` vorkommt)
    - scripts/bootstrap_exapp.sh, Zeile 1085-1130 (`verify_image_digest`, Registry-Vergleich)
    - .planning/phases/13-cimd-nachmessung-und-release-0-1-9/13-RESEARCH.md, Abschnitte "Runtime State Inventory" und "Topologie auf den 0.1.9-Kandidaten bringen (Messweg A)"
    - .planning/phases/13-cimd-nachmessung-und-release-0-1-9/13-PATTERNS.md, Abschnitt zu `13-MEASUREMENTS.md` (Kopf-Pattern, drei Konventionen, Topologie-Tabelle)
    - docs/store-submission.md, Abschnitt "The update keeps the connections" (Verhalten von `unregister` ohne `--rm-data`)
  </read_first>
  <action>
    Rekonstruiere zuerst das Vorbild und lies es: `git show 02dd6e1^:.planning/phases/06-h-rtung-eigennachweise-und-conference-reife/06-09-MEASUREMENTS.md` in eine
    Scratchpad-Datei außerhalb des Repositories. Kopf-Pattern, die drei Konventionen und die
    Topologie-Tabelle werden von dort übernommen, nicht neu erfunden.

    Setze dann `occ` als Kürzel für `docker exec -u www-data nc-mcp-exapp-nc php occ`.
    Nicht `docker compose exec`: jeder compose-Aufruf gegen `compose.exapp.yml` verlangt
    `HP_SHARED_KEY` in der Umgebung. Falls compose gebraucht wird, exportiere den Wert aus
    `.env.exapp` und schreibe ihn nirgends hin.

    Hebe die Topologie auf den 0.1.9-Kandidaten:
    zuerst `occ app_api:app:unregister mcp_connector` OHNE `--rm-data`, damit Volume und
    Autorisierungen überleben, dann `bash scripts/bootstrap_exapp.sh`. Der Vorschritt ist
    Pflicht: `ensure_exapp()` prüft nur, ob die App-Id in `occ app_api:app:list` vorkommt,
    und meldet sonst `registered`, ohne das neue Image zu deployen. Gemessen würde dann
    weiter der heutige Mischstand (AppAPI meldet 0.1.7, der Container läuft auf dem
    ghcr-Image 0.1.6).

    Lege `13-MEASUREMENTS.md` an. Kopf: Titel, Datum des Laufs in UTC, und die drei
    Konventionen wörtlich übernommen: (1) `occ` steht für den `docker exec`-Aufruf,
    (2) kein Credential steht in diesem Dokument, auch kein Wegwerf-Credential, kein Token,
    kein Autorisierungscode, keine `code_challenge`, kein `state`, kein App-Passwort, gekürzte
    Werte erscheinen als `<gekürzt>`, (3) die Version einer Instanz ist immer die Zeile aus
    `occ status`, nie ein Docker-Tag, und für den Connector ist die Pflichtangabe Version UND
    Image-Digest.

    Erster Abschnitt ist die Topologie-Tabelle mit den Pflichtzeilen des Vorbilds:
    Compose-Datei und Projekt, Basis-Adresse `http://127.0.0.1:8081` (Caddy, nur Loopback),
    Nextcloud-Version aus `occ status`, Connector-Version aus `occ app_api:app:list`,
    Image-Referenz und Manifest-Digest aus
    `docker buildx imagetools inspect 127.0.0.1:5000/mcp_connector:0.1.9 --format '{{.Manifest.Digest}}'`,
    `RestartCount` und Health aus
    `docker inspect nc_app_mcp_connector --format '{{.Config.Image}} {{.RestartCount}} {{.State.Health.Status}}'`,
    die gemessene Client-Version aus `claude --version`, das Messkonto (`alice`, Fixture-Konto
    aus `scripts/bootstrap_exapp.sh`, nicht `jane`), und eine Zeile, dass die Owner-Instanzen
    `nc-mcp-test` und `findling-nextcloud` unberührt durchlaufen.

    Zweiter Abschnitt: die zwei Felder, ohne die kein Client den CIMD-Weg wählt. Frage als
    Überschrift mit Uhrzeit, Antwort in Fettschrift, dann der Rohbeleg aus
    `curl -sS http://127.0.0.1:8081/exapps/mcp_connector/.well-known/oauth-authorization-server | jq '{issuer, client_id_metadata_document_supported, token_endpoint_auth_methods_supported}'`.

    Fasse `compose.test.yml` nicht an und richte kein Kommando auf `nc-mcp-test` oder
    `findling-nextcloud`.
  </action>
  <verify>
    <automated>docker inspect nc_app_mcp_connector --format '{{range .Config.Env}}{{println .}}{{end}}' | grep -c '^APP_VERSION=0.1.9$'</automated>
  </verify>
  <acceptance_criteria>
    - `docker inspect nc_app_mcp_connector --format '{{range .Config.Env}}{{println .}}{{end}}' | grep -c '^APP_VERSION=0.1.9$'` gibt 1
    - `docker exec -u www-data nc-mcp-exapp-nc php occ app_api:app:list` nennt `mcp_connector` mit `0.1.9` und `enabled`
    - `docker inspect nc_app_mcp_connector --format '{{.Config.Image}}'` nennt `127.0.0.1:5000/mcp_connector:0.1.9`
    - `docker inspect nc_app_mcp_connector --format '{{.State.Health.Status}}'` gibt `healthy`
    - `curl -sS http://127.0.0.1:8081/exapps/mcp_connector/.well-known/oauth-authorization-server | jq -r .client_id_metadata_document_supported` gibt `true`
    - `13-MEASUREMENTS.md` enthält eine Zeile mit `sha256:` als Manifest-Digest des gemessenen Images
    - `13-MEASUREMENTS.md` enthält die drei Konventionen, darunter die Credential-Regel mit dem Platzhalter `<gekürzt>`
    - `docker ps --format '{{.Names}} {{.Status}}'` zeigt `nc-mcp-test` und `findling-nextcloud` weiter als Up
  </acceptance_criteria>
  <done>Die Topologie führt den 0.1.9-Kandidaten aus dem lokalen Registry, Version und Digest stehen im Protokoll, das AS-Dokument bewirbt beide Felder.</done>
</task>

<task type="auto">
  <name>Task 2: Der Rundlauf ohne Registrierung, mit zwei Belegen statt einem</name>
  <files>.planning/phases/13-cimd-nachmessung-und-release-0-1-9/13-MEASUREMENTS.md</files>
  <read_first>
    - src/mcp_connector/oauth/cimd.py: `is_cimd_client_id` (Zeile 121), `cache_lifetime` (Zeile 204), `fetch_document_and_lifetime` (Zeile 384), `validate_document` (Zeile 440); dort steht auch, dass ein geglückter Abruf nichts protokolliert
    - src/mcp_connector/oauth/provider.py, Zeile 340-390 und 510-540: wo `cimd_fetched_at` gesetzt wird und wo die Allowlist vor dem Fetch gefragt wird (`bd75cd8`)
    - src/mcp_connector/oauth/store.py, Zeile 170-200 und 255-275: das Schema der `clients`-Tabelle, insbesondere `client_secret_hash`, `cimd_fetched_at`, `cimd_expires_at`
    - scripts/oauth_flow_check.py, Funktion `sign_in`: der eine wiederverwendbare Browser-Schenkel; der Rest des Skripts führt über dynamische Registrierung und ist hier NICHT das Vorbild
    - .planning/phases/13-cimd-nachmessung-und-release-0-1-9/13-RESEARCH.md, Abschnitte "Die drei Messwege", "Der Vorlauf, den die Nachmessung wiederholen muss", Pitfalls 1, 2 und 4
    - docs/oauth-setup.md, Zeile 277-320: die bestehende Beweisführung von 2026-08-20 als Formvorbild
  </read_first>
  <action>
    Fahre Messweg A: der echte Client. Sichere vor dem ersten `claude`-Aufruf
    `C:\Users\Student\.claude.json` in eine Datei außerhalb des Repositories und außerhalb
    von `.planning`. Registriere den Server nur lokal und nur aus einem
    Scratchpad-Projektverzeichnis, nicht global und nicht im Repo:
    `claude mcp add --transport http ncmcp http://127.0.0.1:8081/exapps/mcp_connector/mcp -s local`.
    `claude mcp list` erzeugt den 401 und die zwei Discovery-Aufrufe, `claude mcp logout ncmcp`
    vor jedem Lauf, `claude mcp login ncmcp` verlangt ein Terminal. Ohne tty endet es mit
    `stdin isn't a terminal`; der Vorlauf löste das mit `CreatePseudoConsole` per `ctypes`,
    also mit Windows-Bordmitteln und ohne Paketinstallation. Der Treiber liegt im Scratchpad,
    nicht im Repository. Kein `uv add`, kein `pip install`, kein `npm install`.

    Fallback Messweg B, falls der Pseudo-Konsolen-Treiber im Ausführungsfenster nicht steht:
    ein HTTP-Treiber im Scratchpad, der `client_id` gleich
    `https://claude.ai/oauth/claude-code-client-metadata` sendet und den Browser-Schenkel über
    `oauth_flow_check.sign_in` führt. Baue nicht beide Wege parallel. Wird B gefahren, dann
    steht im Protokoll ein eigener, ausdrücklicher Satz, dass damit die Serverseite belegt ist
    und NICHT die Client-Wahl, weil der Treiber den CIMD-Weg vorgibt statt ihn wie der echte
    Client anhand von `client_id_metadata_document_supported` selbst zu wählen. Baue keine
    eigene Nextcloud-Login-Automatisierung: `sign_in` existiert, und ein Gate verbietet
    dasselbe unter `src/`.

    Lies den Loopback-Rückport aus der `/authorize`-Zeile, nimm ihn nicht an: Claude Code
    wählt auf Windows zufällig aus dem Fenster 39152 bis 49151, drei Läufe ergaben 45157,
    47608 und 41977. Alternativ `MCP_OAUTH_CALLBACK_PORT` setzen und im Protokoll als gesetzt
    kennzeichnen.

    Protokolliere den Rundlauf in `13-MEASUREMENTS.md`, ein Abschnitt je Frage, Uhrzeit in der
    Überschrift, Antwort in Fettschrift, dann der Rohbeleg. Diese Behauptungen brauchen je
    einen eigenen Beleg: die `/authorize`-Zeile trägt `client_id` als prozentkodierte
    https-Adresse und keinen Zufallsstring; im Messfenster gibt es keinen `POST /register`;
    die Consent-Seite nennt `claude.ai` als Host der Client-Id; der Code-Tausch am
    `/token`-Endpunkt antwortet 200; ein Werkzeugaufruf liefert echten Inhalt des angemeldeten
    Kontos, `files_list` auf `/` mit den Fixture-Markern.

    Ein geglückter CIMD-Abruf hinterlässt KEINE Logzeile, `oauth/cimd.py` protokolliert nur
    Absagen. Lege deshalb zwei Belege statt eines vor: (a) die geschriebene `clients`-Zeile
    mit `client_secret_hash` gleich NULL und gesetzten Frischespalten `cimd_fetched_at` und
    `cimd_expires_at`, gelesen aus `/nc_app_mcp_connector_data/oauth.sqlite3` im Container per
    `docker exec nc_app_mcp_connector`, mit dem Hinweis, dass das Fenster aus dem
    `Cache-Control` der Antwort kommt und auf 300 bis 3600 Sekunden gekappt wird; (b) derselbe
    Abruf aus dem laufenden Container heraus ausgeführt, über
    `fetch_document_and_lifetime` aus `src/mcp_connector/oauth/cimd.py`, mit Bytezahl und
    Lebensdauer.

    Kein Credential in das Protokoll: kein Token, kein Autorisierungscode, keine
    `code_challenge`, kein `state`, kein App-Passwort, auch kein Wegwerf-Wert. Wo eine
    Logzeile solche Werte trug, steht `<gekürzt>`.

    Beende die im Lauf erzeugte Verbindung anschließend über `/revoke` oder
    `occ mcp_connector:purge --force` und halte fremden Zustand unberührt.
  </action>
  <verify>
    <automated>grep -c "client_secret_hash" .planning/phases/13-cimd-nachmessung-und-release-0-1-9/13-MEASUREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `13-MEASUREMENTS.md` enthält die `/authorize`-Zeile mit `client_id` als prozentkodierter Adresse von `https://claude.ai/oauth/claude-code-client-metadata`
    - `13-MEASUREMENTS.md` belegt die Abwesenheit von `POST /register` im Messfenster mit dem Kommando, mit dem das Containerlog gelesen wurde, und mit dem Zeitfenster
    - `13-MEASUREMENTS.md` nennt die geschriebene `clients`-Zeile mit `client_secret_hash` NULL und gesetzten `cimd_fetched_at` sowie `cimd_expires_at`
    - `13-MEASUREMENTS.md` nennt den zweiten Beleg aus dem Container über `fetch_document_and_lifetime` mit Bytezahl und Lebensdauer
    - `13-MEASUREMENTS.md` nennt HTTP 200 am `/token`-Endpunkt und einen Werkzeugaufruf mit echtem Inhalt
    - Wenn Messweg B gefahren wurde, enthält `13-MEASUREMENTS.md` einen Satz mit den Worten Serverseite und nicht die Client-Wahl
    - `grep -nE '(code_verifier|code_challenge|Bearer [A-Za-z0-9]|state=[A-Za-z0-9]{8}|password)' .planning/phases/13-cimd-nachmessung-und-release-0-1-9/13-MEASUREMENTS.md` gibt keinen Treffer
    - `git status --short` nennt keinen neuen Treiber unter `scripts/`; der Treiber liegt außerhalb des Repositories
    - `git diff --numstat uv.lock pyproject.toml` gibt keine Zeile aus
  </acceptance_criteria>
  <done>Der Rundlauf ist protokolliert, die Identifikation lief über die Dokumentadresse, die Abwesenheit der Registrierung ist belegt und nicht behauptet, kein Credential steht im Dokument.</done>
</task>

<task type="auto">
  <name>Task 3: Gegenprobe mit abgeschaltetem Schalter, Proof-Zeile und der tote Verweis</name>
  <files>.planning/phases/13-cimd-nachmessung-und-release-0-1-9/13-MEASUREMENTS.md, docs/oauth-setup.md</files>
  <read_first>
    - docs/oauth-setup.md, Zeile 94-138: der Schalter `NC_MCP_OAUTH_CIMD`, sein Config-Key `oauth_cimd`, die Admin-Form in Administration settings, Security, MCP Connector, und der Satz, dass die sechs Felder erst nach disable und enable wirken
    - docs/oauth-setup.md, Zeile 277-320: die Proof-Zeile von 2026-08-20 als Formvorbild, die drei Gegenproben mit gezählten ausgehenden Sockets, und die toten Zeilen 314-315
    - docs/oauth-setup.md, Zeile 335-345: die Schaltertabelle mit dem beschriebenen Verhalten bei `NC_MCP_OAUTH_CIMD=0`
    - src/mcp_connector/exapp/config_values.py, Zeile 100-140: der Config-Key `oauth_cimd` und seine Abbildung auf `registry.ENV_CIMD`
    - tests/contract/test_tool_surface.py, Zeile 752-774: der Werkzeugzahl-Gate über `docs/*.md` und `README.md`, nicht rekursiv
    - tests/unit/test_exapp_env_setup.py, Zeile 1978 und 2009-2025: die Reichweite des Vokabular-Gates über `docs/**/*.md` mit der einzigen Ausnahme `docs/store-submission.md`
  </read_first>
  <action>
    Fahre genau eine Gegenprobe, die globale Regel verlangt mehr als den geglückten Pfad:
    schalte den CIMD-Weg über die Admin-Form ab, nicht über eine Neuregistrierung.
    `docker exec -u www-data nc-mcp-exapp-nc php occ app_api:app:config:set mcp_connector oauth_cimd --value 0`,
    dann `occ app_api:app:disable mcp_connector` und `occ app_api:app:enable mcp_connector`,
    weil die sechs Felder erst nach disable und enable wirken.

    Belege zweierlei: die Dokumentadresse wird abgelehnt (die abgewiesene Client-Seite ist
    `This link has expired`, dieselbe Seite, die eine unbekannte oder lange vergangene
    Registrierung sieht), und es geht kein Paket nach außen. Zähle die ausgehenden Sockets
    statt sie zu blocken, mit demselben Verfahren im positiven und im negativen Lauf: die
    Einträge mit Remote-Port 443 aus `/proc/net/tcp` und `/proc/net/tcp6` des Containers,
    gelesen per `docker exec nc_app_mcp_connector`, über ein benanntes Zeitfenster. Nenne
    beide Zahlen im Protokoll, die des positiven Laufs und die Null des negativen. Belege
    außerdem, dass das AS-Dokument die Fähigkeit in dieser Konfiguration nicht mehr bewirbt.

    Stelle danach den Ausgangszustand her:
    `occ app_api:app:config:delete mcp_connector oauth_cimd`, dann disable und enable, und
    belege mit einem erneuten Abruf des AS-Dokuments, dass
    `client_id_metadata_document_supported` wieder `true` ist. Eine Messung, die den Schalter
    umgelegt liegen lässt, verändert die Instanz hinter dem Rücken des nächsten Plans.

    Schreibe dann die dauerhafte Proof-Zeile in `docs/oauth-setup.md`, in das Kapitel
    "Client ID Metadata Documents", neben oder über den Absatz von 2026-08-20. Der alte
    Absatz wird nicht ersetzt, die 0.1.3-Aussage bleibt wahr. Form wie das Vorbild: fettes
    Datum plus Client-Version plus Instanz, dann ein Satz, der jede Behauptung des
    Erfolgskriteriums einzeln nennt, also Identifikation über die Dokumentadresse, kein
    `/register`, Consent nennt den Host, Token 200, echter Werkzeuginhalt. Nenne die
    gemessene Fassung 0.1.9 und dass die Messung gegen den Kandidaten aus dem lokalen
    Registry lief. Ergänze einen Satz zur Gegenprobe im Stil der drei bestehenden
    Aufzählungspunkte.

    Die Zeile muss selbsttragend sein: Datum, Befehl und Ergebnis stehen im Satz, und sie
    verweist auf kein Dokument unter `.planning/`, weil das Phasenverzeichnis beim
    Milestone-Abschluss entfernt wird. Schließe im selben Zug den toten Verweis: die
    heutigen Zeilen 314-315 zeigen auf
    `06-09-MEASUREMENTS.md` in einem mit `02dd6e1` gelöschten Phasenverzeichnis. Ersetze
    diese zwei Zeilen durch eine selbsttragende Aussage ohne Pfad. Nach diesem Task darf in
    `docs/oauth-setup.md` kein Pfadbestandteil `.planning` mehr vorkommen.

    Zwei Gates liegen auf dieser Datei: das Vokabular-Gate rekursiv über `docs/**/*.md`, also
    darf das Wort `archiv` nirgends im neuen Text stehen, in keiner Sprache und in keinem
    Kompositum; und der Werkzeugzahl-Gate über `docs/*.md` samt `README.md`, also braucht
    jede genannte Werkzeugzahl entweder den aktuellen Wert 21 oder den Halter-Pfad
    `tests/contract/test_tool_surface.py` in derselben Datei. Am einfachsten: nenne in der
    neuen Zeile keine Werkzeugzahl.
  </action>
  <verify>
    <automated>uv run --no-sync pytest tests/unit/test_exapp_env_setup.py tests/contract/test_tool_surface.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '\.planning' docs/oauth-setup.md` gibt keine Zeile aus
    - `grep -c '06-09-MEASUREMENTS' docs/oauth-setup.md` gibt 0
    - `docs/oauth-setup.md` enthält eine neue Zeile mit dem fetten Datum des Messtages und der Client-Version, und sie nennt 0.1.9
    - Die neue Zeile nennt alle fünf Behauptungen: Dokumentadresse, kein `/register`, Consent-Host, Token 200, echter Werkzeuginhalt
    - `13-MEASUREMENTS.md` nennt beide Socket-Zahlen, die des positiven Laufs und die Null des negativen, samt dem Kommando, mit dem gezählt wurde
    - `curl -sS http://127.0.0.1:8081/exapps/mcp_connector/.well-known/oauth-authorization-server | jq -r .client_id_metadata_document_supported` gibt nach dem Wiederherstellen wieder `true`
    - `docker exec -u www-data nc-mcp-exapp-nc php occ app_api:app:config:list mcp_connector` nennt keinen Wert `oauth_cimd`
    - `grep -in 'archiv' docs/oauth-setup.md` gibt keinen Treffer
    - `grep -c '—' docs/oauth-setup.md` gibt 0
    - `uv run --no-sync pytest tests/unit/test_exapp_env_setup.py tests/contract/test_tool_surface.py -q` endet mit Exit 0
  </acceptance_criteria>
  <done>Die Gegenprobe ist gemessen und zurückgenommen, die dauerhafte Proof-Zeile steht selbsttragend in der Doku, der tote Verweis existiert nicht mehr, beide Gates auf der Datei sind grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Externer Client zu ExApp | Ein unbekannter Client sendet eine https-Adresse, die der Server abruft; hier kreuzt untrusted Input |
| ExApp zu ausgehendem Netz | Der CIMD-Abruf ist der einzige ausgehende Request des Pfades; die Allowlist fragt vor dem Fetch |
| Messlauf zu Owner-Zustand | Der echte Client liest `C:\Users\Student\.claude.json`, und daneben laufen zwei fremde Instanzen |
| Messprotokoll zu öffentlichem Repo | `docs/oauth-setup.md` ist öffentlich, `13-MEASUREMENTS.md` liegt im Repo unter `.planning` und ist damit ebenfalls öffentlich |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-10 | Information Disclosure | 13-MEASUREMENTS.md, docs/oauth-setup.md | mitigate | Credential-Konvention aus 06-09 (T-05-39) als Konvention 2 im Dokumentkopf; gekürzte Werte als `<gekürzt>`; Akzeptanzkriterium ist ein grep nach `code_verifier`, `code_challenge`, `Bearer`, `state=` und `password` ohne Treffer |
| T-13-11 | Repudiation | Behauptung "ohne Registrierung" | mitigate | Zwei Belege: Abwesenheit von `POST /register` im benannten Logfenster UND die `clients`-Zeile mit `client_secret_hash` NULL plus gesetztem `cimd_fetched_at`; eine Registrierungszeile hat beides nicht |
| T-13-12 | Spoofing | Messweg B als Client-Beweis | mitigate | Wird B gefahren, verlangt ein Akzeptanzkriterium einen Satz im Protokoll, der die Serverseite als das Belegte und die Client-Wahl als das Nichtbelegte benennt |
| T-13-13 | Tampering | Fremde Instanzen und Owner-MCP-Zustand | mitigate | Nur die Topologie `nc-mcp-exapp` wird angesprochen; `claude mcp add` nur mit `-s local` aus einem Scratchpad-Projektverzeichnis; `C:\Users\Student\.claude.json` vorher außerhalb des Repos gesichert; `nc-mcp-test` und `findling-nextcloud` als Up geprüft |
| T-13-14 | Denial of Service | Volume und Autorisierungen der ExApp | mitigate | `occ app_api:app:unregister mcp_connector` ausdrücklich ohne `--rm-data`; der Schalter der Gegenprobe wird nach der Messung gelöscht und der Ausgangszustand belegt |
| T-13-15 | Tampering | Instanz bleibt in Messkonfiguration stehen | mitigate | `occ app_api:app:config:delete mcp_connector oauth_cimd` plus disable und enable, danach Beleg, dass das AS-Dokument die Fähigkeit wieder bewirbt; `config:list` nennt den Schlüssel nicht mehr |
| T-13-16 | Repudiation | Toter Verweis erzeugt einen unbelegbaren Beweis | mitigate | Die neue Proof-Zeile trägt Datum, Befehl und Ergebnis im Satz und verweist auf kein Nachbardokument; `grep -n '\.planning' docs/oauth-setup.md` muss leer sein |
| T-13-SC | Tampering | Messtreiber und Paketinstallation | mitigate | Pseudo-Konsole per Windows-Bordmittel `CreatePseudoConsole` über `ctypes`, kein `uv add`, `pip install` oder `npm install`; der Treiber liegt im Scratchpad und nicht unter `scripts/`; `git diff --numstat uv.lock pyproject.toml` bleibt leer |
</threat_model>

<verification>
- `docker inspect nc_app_mcp_connector` nennt `APP_VERSION=0.1.9` und das Registry-Image
- `13-MEASUREMENTS.md` trägt Topologie-Tabelle mit Digest, Rundlauf und Gegenprobe
- `docs/oauth-setup.md` trägt die neue datierte Zeile und keinen `.planning`-Pfad mehr
- `uv run --no-sync pytest tests/unit/test_exapp_env_setup.py tests/contract/test_tool_surface.py -q` grün
- Kein Credential-Muster im Protokoll
</verification>

<success_criteria>
Roadmap-Erfolgskriterium 1 ist erfüllt: ein E2E-Lauf gegen die laufende Topologie zeigt, dass
ein CIMD-Client sich weiterhin ohne Registrierung verbindet, `client_id` ist die https-Adresse
seines Dokuments, und die Proof-Zeile mit Datum, Befehl und Ergebnis steht in `docs/oauth-setup.md`
sowie als Rohprotokoll in `13-MEASUREMENTS.md`, nicht in einer Zusammenfassung. Der
v1.1-Debt-Befund W-5 ist damit geschlossen: gemessen wurde eine Fassung nach `a47bb57` und
`bd75cd8`.
</success_criteria>

<output>
Create `.planning/phases/13-cimd-nachmessung-und-release-0-1-9/13-03-SUMMARY.md` when done.
Nenne darin ausdrücklich, welcher Messweg gefahren wurde (A oder B) und welchen Digest das
gemessene Image trug.
</output>
