---
phase: 13-cimd-nachmessung-und-release-0-1-9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/mcp_connector/__init__.py
  - appinfo/info.xml
  - uv.lock
  - README.md
  - README.de.md
  - README.fr.md
  - CHANGELOG.md
autonomous: true
requirements: [EXAPP-09]

must_haves:
  truths:
    - "Die Zeichenkette 0.1.9 steht als dieselbe Version an allen fünf Runbook-Stellen plus uv.lock"
    - "Der Changelog-Block 0.1.9 nennt message_truncated ausdrücklich als Formatänderung"
    - "Der Changelog-Block 0.1.9 nennt die README-Provider-Korrektur talk-conversations als Doku-Korrektur"
    - "Nach diesem Plan existiert kein Tag v0.1.9"
  artifacts:
    - path: "pyproject.toml"
      provides: "Paketversion 0.1.9"
      contains: "version = \"0.1.9\""
    - path: "src/mcp_connector/__init__.py"
      provides: "__version__ 0.1.9"
      contains: "__version__ = \"0.1.9\""
    - path: "appinfo/info.xml"
      provides: "Manifest-Version und image-tag 0.1.9"
      contains: "<version>0.1.9</version>"
    - path: "CHANGELOG.md"
      provides: "Release-Notes-Block 0.1.9 plus zwei Link-Referenzen"
      contains: "## [0.1.9]"
  key_links:
    - from: "appinfo/info.xml"
      to: "src/mcp_connector/__init__.py"
      via: "Versions-Gleichheit, gehalten von tests/unit/test_exapp_env_setup.py"
      pattern: "0\\.1\\.9"
    - from: "appinfo/info.xml <image-tag>"
      to: "appinfo/info.xml <version>"
      via: "image-tag folgt der Version, gehalten vom selben Gate"
      pattern: "<image-tag>0\\.1\\.9</image-tag>"
    - from: "CHANGELOG.md Link-Referenzen"
      to: "GitHub-Compare-Bereich v0.1.8...v0.1.9"
      via: "Markdown-Referenzdefinition am Dateiende"
      pattern: "compare/v0\\.1\\.8\\.\\.\\.v0\\.1\\.9"
---

<objective>
Dieser Plan hebt die Version des Repositories auf 0.1.9 und schreibt den Release-Notes-Block
dazu. Er erzeugt keinen Tag und stößt nichts an.

Purpose: Runbook Schritt 1 und 2 aus `docs/store-submission.md`. Der 0.1.9-Kandidat, gegen
den Plan 13-03 den CIMD-Weg nachmisst, entsteht aus `appinfo/info.xml`: `bootstrap_exapp.sh`
liest die Version dort (`app_version()`, Zeile 1039-1041) und baut daraus das Image-Tag.
Ohne diesen Plan gibt es keinen 0.1.9-Kandidaten.
Output: sechs geänderte Versionsstellen, ein Changelog-Block, zwei Link-Referenzen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-cimd-nachmessung-und-release-0-1-9/13-CONTEXT.md
@.planning/phases/13-cimd-nachmessung-und-release-0-1-9/13-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sechs Versionsstellen auf 0.1.9</name>
  <files>pyproject.toml, src/mcp_connector/__init__.py, appinfo/info.xml, uv.lock, README.md, README.de.md, README.fr.md</files>
  <read_first>
    - pyproject.toml (Zeile 1-3, `version = "0.1.8"`)
    - src/mcp_connector/__init__.py (Zeile 7, `__version__ = "0.1.8"`)
    - appinfo/info.xml (Zeile 171 `<version>`, Zeile 245 `<image-tag>`)
    - uv.lock (Zeile 468-475, der Block mit `name = "nextcloud-mcp-connector"`)
    - README.md (Zeile 25-27, Abschnitt `## Status`)
    - README.de.md (Zeile 27-29, Abschnitt `## Status`)
    - README.fr.md (Zeile 29-31, Abschnitt `## Statut`)
    - docs/store-submission.md, Runbook Schritt 1 (Zeile 174-187): dort stehen die fünf Stellen wörtlich
    - tests/unit/test_exapp_env_setup.py, Zeile 143-180: der Gate, der Manifest-Version gegen `__version__` und `image-tag` gegen `<version>` hält
  </read_first>
  <action>
    Ersetze die Zeichenkette 0.1.8 durch 0.1.9 an genau sechs Stellen und an keiner weiteren:
    `pyproject.toml` Zeile 3 (`version = "0.1.9"`), `src/mcp_connector/__init__.py` Zeile 7
    (`__version__ = "0.1.9"`), `appinfo/info.xml` Zeile 171 (`<version>0.1.9</version>`),
    `appinfo/info.xml` Zeile 245 (`<image-tag>0.1.9</image-tag>`), die drei
    README-Statuszeilen (`Version 0.1.9.` als Zeilenanfang in README.md, README.de.md und
    README.fr.md) und die `version`-Zeile des Pakets `nextcloud-mcp-connector` in `uv.lock`
    (Zeile 472).

    Die uv.lock-Zeile ist die sechste Stelle und wird bewusst mitgezogen: Commit `8392680`
    hat sie beim 0.1.8-Bump ebenso geändert (`uv.lock | 2 +-`), obwohl kein Gate und kein
    Runbook-Schritt sie hält. Fasse sie per Texteditierung an, rufe NICHT `uv lock` und
    NICHT `uv sync` auf: ein Lock-Lauf würde Dependency-Zeilen bewegen. Kein `uv add`, kein
    `pip install`, kein Dependency-Block in `pyproject.toml` oder `uv.lock`.

    In den drei READMEs ändert sich ausschließlich die Versionszahl der Statuszeile. Die
    Zahl 21 in den Formulierungen "All 21 tools", "Alle 21 Tools" und "Les 21 outils" bleibt
    unangetastet: sie steht unter dem Gate `tests/contract/test_tool_surface.py` und in
    dieser Phase wird kein Werkzeug angefasst. Keine Em-Dashes, echte Umlaute in der
    deutschen und echte Accents in der französischen Fassung.

    In `appinfo/info.xml` werden `<summary>` (Zeile 18-20), die `<description>`-Blöcke und
    `<environment-variables>` in diesem Task NICHT angefasst. Ein leer gemachtes Element
    lässt den Store-Upload später mit HTTP 500 sterben.

    Erzeuge keinen Tag. `git tag` bleibt in diesem Plan unbenutzt.
  </action>
  <verify>
    <automated>uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^version = "0.1.9"' pyproject.toml` gibt 1
    - `grep -c '^__version__ = "0.1.9"' src/mcp_connector/__init__.py` gibt 1
    - `grep -o '<version>[^<]*' appinfo/info.xml` gibt genau `<version>0.1.9`
    - `grep -o '<image-tag>[^<]*' appinfo/info.xml` gibt genau `<image-tag>0.1.9`
    - `grep -n '^Version 0\.1\.9\.' README.md README.de.md README.fr.md` gibt drei Treffer, einen je Datei
    - `grep -rn '0\.1\.8' pyproject.toml src/mcp_connector/__init__.py` gibt keinen Treffer
    - `git diff --numstat uv.lock` gibt genau `1	1	uv.lock`
    - `git diff --numstat pyproject.toml` gibt genau `1	1	pyproject.toml`
    - `git diff -- pyproject.toml uv.lock` enthält keine Zeile mit `dependencies`, `requires-dist` oder einem Paketnamen außer `nextcloud-mcp-connector`
    - `uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -q` endet mit Exit 0
    - `git tag --list v0.1.9` gibt keine Zeile aus
  </acceptance_criteria>
  <done>Sechs Stellen tragen dieselbe Zeichenkette 0.1.9, das Manifest-Gate ist grün, kein Tag existiert.</done>
</task>

<task type="auto">
  <name>Task 2: Changelog-Block 0.1.9 mit message_truncated als Formatänderung</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md, Zeile 1-13 (Kopf, SPDX-Kommentar, Keep-a-Changelog-Absatz, dann direkt `## [0.1.8] - 2026-08-25`)
    - CHANGELOG.md, Zeile 12-95 (der ganze 0.1.8-Block; besonders `### Changed` Zeile 66-74, der `preview_truncated`-Eintrag, und Zeile 84-85 zur Store-Manifest-Lesezeit)
    - CHANGELOG.md, Zeile 97-101 (Block 0.1.7, die genauere Vorlage für ein Pflege-Release)
    - CHANGELOG.md, Zeile 121-124 (Store-Text-Änderung als eigener Eintrag) und die letzten zehn Zeilen (Link-Referenzen, Ist-Zustand `[Unreleased]: ...compare/v0.1.8...HEAD`)
    - .planning/phases/12-konsistenz-und-h-rtungs-nachzieher/12-01-SUMMARY.md (Übergabe zum Schlüssel `message_truncated`, inklusive Wortlaut-Vorlage)
    - .planning/phases/12-konsistenz-und-h-rtungs-nachzieher/12-04-SUMMARY.md (Übergabe zur README-Provider-Korrektur, mit Formulierungsvorschlag)
    - .planning/phases/12-konsistenz-und-h-rtungs-nachzieher/12-VERIFICATION.md (was Phase 12 wirklich geliefert hat)
    - .planning/phases/13-cimd-nachmessung-und-release-0-1-9/13-RESEARCH.md, Abschnitt "Der Changelog-Block 0.1.9: was hineingehört, was nicht" (die Ja/Nein-Tabelle)
  </read_first>
  <action>
    Setze einen neuen Block `## [0.1.9] - 2026-08-25` direkt oberhalb von
    `## [0.1.8] - 2026-08-25` (heute Zeile 12) ein. Ein `## [Unreleased]`-Abschnitt existiert
    nicht und wird auch nicht angelegt, nur die gleichnamige Link-Referenz am Dateiende.
    Das Datum ist der Kalendertag des Schreibens; Plan 13-05 prüft es vor dem Tag noch
    einmal nach und korrigiert es, falls der Tag an einem anderen Tag entsteht.

    Der Block beginnt mit einem Einleitungsabsatz aus Prosa ohne Aufzählung, im Ton von
    `## [0.1.7]`: dieses Release ist ein Pflege-Release, keine neue Werkzeugfamilie, ein
    umbenannter Antwortschlüssel, eine Doku-Korrektur, ein neuer Abschnitt im Store-Text.

    Danach genau diese Rubriken und Einträge:

    `### Changed` mit dem Eintrag zur Formatänderung, gebaut nach der Vorlage des
    0.1.8-Eintrags zu `preview_truncated` (CHANGELOG.md:68-74): in einer Antwort von
    `talk_browse(level="messages")` heißt der Schlüssel `truncated` eines einzelnen
    Eintrags jetzt `message_truncated`, die Antwortebene behält `truncated` unverändert,
    und ein Client mit persistierter Werkzeugliste sieht den neuen Docstring erst nach
    seiner nächsten Auffrischung. Nenne ausdrücklich, dass ein Leser des alten
    Schlüssels nachgezogen werden muss.

    `### Fixed` mit der Doku-Korrektur nach dem Formulierungsvorschlag aus 12-04-SUMMARY.md:
    das `unified_search`-Beispiel in den READMEs nannte `spreed`, was nie eine Provider-Id
    war; es nennt jetzt `talk-conversations`.

    NICHT in den Block aufnehmen (Begründung je Zeile in der Ja/Nein-Tabelle der Research):
    die Ersetzung von `_ID_KIND` durch `ids.encode_mail` samt der neuen Absagen in
    `ids.parse`, die Umbenennung `talk_tools._room` nach `talk_tools.one_room`, und die
    neuen Gates aus Phase 12. Keine dieser Änderungen ist nutzersichtbar.

    Am Dateiende: schreibe die Referenz `[Unreleased]` auf
    `https://github.com/street1983nk/nextcloud-mcp-connector/compare/v0.1.9...HEAD` um und
    ergänze darüber `[0.1.9]:` mit
    `https://github.com/street1983nk/nextcloud-mcp-connector/compare/v0.1.8...v0.1.9`.

    Sprache Englisch wie die übrigen Blöcke. Keine Em-Dashes. Seit Phase 12 liegt
    `CHANGELOG.md` im Vokabular-Gate: das Wort `archiv` darf im neuen Block in keiner
    Sprache und in keinem Kompositum vorkommen.

    Der Eintrag zum Enterprise-Abschnitt gehört NICHT in diesen Task; Plan 13-02 setzt ihn
    unter `### Added` in denselben Block, sobald der Text existiert.
  </action>
  <verify>
    <automated>uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -k "vocabulary" -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '^## \[' CHANGELOG.md | head -2` zeigt `## [0.1.9]` vor `## [0.1.8] - 2026-08-25`
    - `grep -c 'message_truncated' CHANGELOG.md` gibt mindestens 1, und der Treffer liegt im 0.1.9-Block oberhalb der Zeile mit `## [0.1.8]`
    - `grep -c 'talk-conversations' CHANGELOG.md` gibt mindestens 1
    - `grep -c 'talk_browse' CHANGELOG.md` gibt mindestens 1 im 0.1.9-Block
    - `grep -c 'compare/v0.1.9...HEAD' CHANGELOG.md` gibt 1
    - `grep -c 'compare/v0.1.8...v0.1.9' CHANGELOG.md` gibt 1
    - Der 0.1.9-Block nennt weder `_ID_KIND` noch `one_room` noch `test_module_boundaries`
    - `grep -in 'archiv' CHANGELOG.md` gibt keinen Treffer
    - `grep -c '—' CHANGELOG.md` gibt 0
    - `uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -k "vocabulary" -q` endet mit Exit 0
  </acceptance_criteria>
  <done>Der 0.1.9-Block steht über dem 0.1.8-Block, nennt die Formatänderung und die Doku-Korrektur, die zwei Link-Referenzen stimmen, das Vokabular-Gate ist grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Arbeitsbaum zu Git-Tag | Ein Tag ist der Auslöser des Release-Workflows und irreversibel; dieser Plan darf die Grenze nicht überschreiten |
| Repo-Version zu veröffentlichtem Artefakt | Was hier als Zeichenkette steht, reist unveränderlich im signierten Tarball mit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | Die sechs Versionsstellen | mitigate | Ein Akzeptanzkriterium je Stelle mit exaktem grep-Muster; `tests/unit/test_exapp_env_setup.py` hält Manifest gegen `__version__` und `image-tag` gegen `<version>`; `release.yml` prüft die Gleichheit ein zweites Mal beim Tag |
| T-13-02 | Denial of Service | Git-Tag | mitigate | Dieser Plan erzeugt keinen Tag und behauptet als Akzeptanzkriterium, dass `git tag --list v0.1.9` leer ist; der Tag entsteht in Plan 13-05 nach Owner-Freigabe (D-01) |
| T-13-03 | Repudiation | CHANGELOG.md | mitigate | Nur die zwei Einträge, die die Phase-12-SUMMARYs als nutzersichtbar übergeben haben; drei intern gebliebene Änderungen sind namentlich ausgeschlossen und werden per Akzeptanzkriterium geprüft |
| T-13-04 | Denial of Service | appinfo/info.xml | mitigate | `<summary>`, `<description>` und `<environment-variables>` bleiben in diesem Plan unangetastet; ein leer gemachtes Element beantwortet den Store-Upload mit HTTP 500 |
| T-13-SC | Tampering | uv.lock, pyproject.toml Dependencies | mitigate | Kein `uv add`, `uv lock`, `uv sync`, `pip install` oder `npm install`; `git diff --numstat` erlaubt genau eine geänderte Zeile je Datei, und der Diff darf keinen Fremdpaketnamen enthalten |
</threat_model>

<verification>
- Alle sechs Versionsstellen tragen dieselbe Zeichenkette (grep je Stelle)
- `uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -q` grün (Manifest-Gate und Vokabular-Gate)
- `git tag --list v0.1.9` leer
- `git diff --stat` nennt genau die acht Dateien dieses Plans
</verification>

<success_criteria>
Roadmap-Erfolgskriterium 2 ist zur Hälfte erfüllt: die Version 0.1.9 steht als derselbe
String an allen fünf Runbook-Stellen (plus uv.lock als sechster), und der Changelog-Block
0.1.9 nennt `message_truncated` ausdrücklich als Formatänderung sowie die
README-Provider-Korrektur als Doku-Korrektur. Kein Tag existiert.
</success_criteria>

<output>
Create `.planning/phases/13-cimd-nachmessung-und-release-0-1-9/13-01-SUMMARY.md` when done.
Nenne darin ausdrücklich, ob das Changelog-Datum 2026-08-25 lautet, damit Plan 13-05 es vor
dem Tag prüfen kann.
</output>
