---
phase: 13-cimd-nachmessung-und-release-0-1-9
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - README.md
  - README.de.md
  - README.fr.md
  - appinfo/info.xml
  - docs/contrib/enterprise-signals-issue.md
  - CHANGELOG.md
autonomous: true
requirements: [EXAPP-09]

must_haves:
  truths:
    - "Die drei READMEs tragen einen Abschnitt Enterprise mit den drei Bausteinen Audit-Log, Gruppen-Policies und SSO"
    - "Jede der vier Fassungen sagt ausdrücklich, dass heute keiner der drei Bausteine existiert"
    - "Keine Fassung nennt einen Preis oder ein Datum"
    - "Die Store-Beschreibung trägt denselben Hinweis in EN, DE und FR"
    - "Der Issue-Entwurf liegt als Datei im Repo und ist nicht veröffentlicht"
  artifacts:
    - path: "README.md"
      provides: "Abschnitt ## Enterprise (EN), Langfassung"
      contains: "## Enterprise"
    - path: "README.de.md"
      provides: "Abschnitt ## Enterprise (DE)"
      contains: "## Enterprise"
    - path: "README.fr.md"
      provides: "Abschnitt ## Enterprise (FR)"
      contains: "## Enterprise"
    - path: "appinfo/info.xml"
      provides: "Enterprise-Kurzfassung in den drei description-Blöcken"
      contains: "k.cherif@outlook.de"
    - path: "docs/contrib/enterprise-signals-issue.md"
      provides: "Issue-Entwurf mit Titel, Body und Go-Kriterium, nicht veröffentlicht"
      min_lines: 25
  key_links:
    - from: "appinfo/info.xml <description>"
      to: "Nextcloud App Store Katalog"
      via: "Manifest wird ausschließlich beim Release-Upload gelesen (D-06)"
      pattern: "k\\.cherif@outlook\\.de"
    - from: "docs/contrib/enterprise-signals-issue.md"
      to: "Owner-Entscheidung"
      via: "Satz, dass die Veröffentlichung eine Owner-Entscheidung ist (D-07)"
      pattern: "owner decision"
---

<objective>
Der Enterprise-Fake-Door aus dem ISV-Vorhaben: vier Texte und ein Issue-Entwurf. Kein
Enterprise-Feature wird gebaut.

Purpose: D-05, D-06 und D-07 aus 13-CONTEXT.md. Der Store liest das Manifest ausschließlich
beim Release-Upload, eine Beschreibungsänderung ohne Release ist unsichtbar (das kostete die
Versionen 0.1.5 und 0.1.6). Deshalb fährt der Text mit 0.1.9 mit und nicht später.
Output: `## Enterprise` in drei READMEs, ein Absatz in drei Manifest-Beschreibungen, ein
Issue-Entwurf als Datei, eine Changelog-Zeile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-cimd-nachmessung-und-release-0-1-9/13-CONTEXT.md
@.planning/phases/13-cimd-nachmessung-und-release-0-1-9/13-PATTERNS.md
@.planning/phases/13-cimd-nachmessung-und-release-0-1-9/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Abschnitt Enterprise in den drei READMEs</name>
  <files>README.md, README.de.md, README.fr.md</files>
  <read_first>
    - README.md, Zeile 481-511 (`## Known limitations`) und Zeile 512 (`## Development`): Ton, Einleitungs-Pattern und Ehrlichkeits-Pattern der Analogstelle
    - README.de.md, Zeile 495-526 (`## Bekannte Einschränkungen`) und Zeile 527 (`## Entwicklung`)
    - README.fr.md, Zeile 511-544 (`## Limitations connues`) und Zeile 545 (`## Développement`)
    - C:/Users/Student/scripts/docs/specs/ideation-isv-monetarisierung-2026-08-25/concept-brief-1-connector-enterprise.md (der Schnitt Connector Enterprise: welche drei Bausteine, welcher Nutzen)
    - C:/Users/Student/scripts/docs/specs/ideation-isv-monetarisierung-2026-08-25/validation-plan.md (Fake-Door-Methode, Go-Kriterium)
    - .planning/phases/13-cimd-nachmessung-und-release-0-1-9/13-PATTERNS.md, Abschnitt zum Enterprise-Abschnitt (Pflichtteile und Platzierung)
  </read_first>
  <action>
    Setze in jeder der drei READMEs einen neuen Abschnitt `## Enterprise` ein, jeweils direkt
    nach dem Grenzen-Abschnitt und vor dem Entwicklungs-Abschnitt: in README.md nach
    `## Known limitations` und vor `## Development`, in README.de.md nach
    `## Bekannte Einschränkungen` und vor `## Entwicklung`, in README.fr.md nach
    `## Limitations connues` und vor `## Développement`. Die Überschrift heißt in allen drei
    Fassungen `## Enterprise`, das Wort ist in allen drei Sprachen dasselbe.

    Inhaltliche Pflichtteile in jeder Fassung (D-05):
    - die drei Bausteine namentlich: ein Audit-Log (audit log / Audit-Log / journal d'audit),
      Gruppen-Policies (group policies / Gruppen-Policies / politiques de groupe) und eine
      SSO- beziehungsweise IdP-Anbindung (SSO / IdP)
    - das Wort planned in der englischen, geplant in der deutschen und prévu (mit Accent) in
      der französischen Fassung
    - einen ausdrücklichen eigenen Satz, dass heute keiner der drei Bausteine existiert und
      dass dieser Abschnitt ein Vorhaben beschreibt, kein Feature
    - die Kontaktadresse `k.cherif@outlook.de` als Adresse für Interessensmeldungen; sie
      steht bereits als `<author mail=...>` im Manifest, ist im öffentlichen Artefakt also
      nichts Neues

    Verboten in jeder Fassung: ein Preis, eine Preisspanne, eine Währung, ein Lieferdatum,
    eine Versionszusage, das Wort kostenlos oder free als Abgrenzung zu einem Preis. Kein
    Superlativ, keine Zusage in der Zukunftsform, die mehr behauptet als ein Vorhaben.

    Ton und Aufbau spiegeln die Analogstelle: erst ein Absatz, der die Erwartung setzt, dann
    die drei Bausteine als Liste. Übernimm das Ehrlichkeits-Pattern des Grenzen-Abschnitts,
    also die Grenze benennen statt sie zu beschönigen. Zwei bis vier Absätze plus Liste,
    nicht mehr; die Langfassung gehört hierhin, die Kurzfassung in Task 2.

    Sprachregeln: die deutsche Fassung mit echten Umlauten, die französische mit Accents und
    mit dem Leerzeichen vor dem Doppelpunkt wie in den vorhandenen Abschnitten. Keine
    Em-Dashes in keiner Fassung. Die drei READMEs sind strukturell deckungsgleich, der
    Abschnitt entsteht dreimal parallel und sagt dreimal dasselbe.

    Fasse in diesem Task keine andere Zeile der READMEs an. Insbesondere bleibt die
    Statuszeile aus Plan 13-01 unverändert und die Zahl 21 in den Werkzeug-Formulierungen
    unangetastet: sie steht unter dem Gate `tests/contract/test_tool_surface.py`, das
    `README.md` mitliest.
  </action>
  <verify>
    <automated>uv run --no-sync pytest tests/unit/test_exapp_env_setup.py tests/contract/test_tool_surface.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Enterprise' README.md README.de.md README.fr.md` gibt je 1
    - In jeder der drei Dateien steht die Zeilennummer von `## Enterprise` zwischen der des Grenzen-Abschnitts und der des Entwicklungs-Abschnitts (`grep -n '^## '` je Datei belegt die Reihenfolge)
    - `grep -c 'k.cherif@outlook.de' README.md README.de.md README.fr.md` gibt je mindestens 1
    - README.md enthält im Enterprise-Abschnitt das Wort `planned`, README.de.md `geplant`, README.fr.md `prévu` mit Accent
    - Jeder der drei Abschnitte nennt alle drei Bausteine: ein grep nach dem jeweiligen Wort für Audit-Log, Gruppen-Policies und SSO findet in jedem Abschnitt je einen Treffer
    - `grep -cE '(EUR|€|\$|USD|/month|pro Monat|par mois)' README.md README.de.md README.fr.md` gibt je 0
    - `grep -c '—' README.md README.de.md README.fr.md` gibt je 0
    - `grep -n 'Version 0\.1\.9\.' README.md README.de.md README.fr.md` gibt weiterhin drei Treffer
    - `uv run --no-sync pytest tests/unit/test_exapp_env_setup.py tests/contract/test_tool_surface.py -q` endet mit Exit 0
  </acceptance_criteria>
  <done>Die drei READMEs tragen denselben Enterprise-Abschnitt in ihrer Sprache, ehrlich als Vorhaben formuliert, ohne Preis, mit Kontaktadresse; Vokabular-Gate und Werkzeugzahl-Gate sind grün.</done>
</task>

<task type="auto">
  <name>Task 2: Enterprise-Kurzfassung in den drei description-Blöcken des Manifests</name>
  <files>appinfo/info.xml</files>
  <read_first>
    - appinfo/info.xml, Zeile 70-77 (`### Under your control` und der folgende `### Resources`): Kürze, Fettschrift-Form, Listenform
    - appinfo/info.xml, Zeile 111-118 (`### In Ihrer Hand` bis `### Weiterführendes`)
    - appinfo/info.xml, Zeile 154-161 (`### Sous votre contrôle` bis `### Pour aller plus loin`), inklusive der Leerzeichen vor dem Doppelpunkt
    - appinfo/info.xml, Zeile 18-20 (`<summary>`): wird nicht angefasst
    - tests/unit/test_exapp_env_setup.py, Funktion `description_problems`: die harten Regeln für diesen Text
    - tests/unit/test_exapp_env_setup.py, Funktion `variable_problems`: warum `<environment-variables>` nicht angefasst wird
    - docs/store-submission.md, Zeile 200-203 (Runbook Schritt 3 nennt die Beschreibungsregeln wörtlich) und der Abschnitt zum Cache-Verhalten
  </read_first>
  <action>
    Setze in jeden der drei `<description>`-CDATA-Blöcke einen kurzen Enterprise-Abschnitt
    ein, jeweils zwischen den Kontroll-Abschnitt und den Link-Abschnitt: in der englischen
    Fassung zwischen `### Under your control` und `### Resources`, in der deutschen zwischen
    `### In Ihrer Hand` und `### Weiterführendes`, in der französischen zwischen
    `### Sous votre contrôle` und `### Pour aller plus loin`.

    Die Kurzfassung sagt dasselbe wie der README-Abschnitt aus Task 1, in zwei bis drei
    Sätzen: die drei Bausteine (Audit-Log, Gruppen-Policies, SSO beziehungsweise IdP) sind
    als kommerzielles Add-on geplant, heute existiert keiner davon, und wer Interesse hat,
    schreibt an `k.cherif@outlook.de`. Kein Preis, kein Datum, keine Zusage.

    Harte Regeln des Gates `description_problems`, die dieser Text nicht brechen darf: kein
    Backtick, kein senkrechter Strich auch nicht in Prosa, kein Bild, keine horizontale Linie
    aus drei Bindestrichen allein auf einer Zeile, kein HTML-Element, mindestens zwei durch
    Leerzeilen getrennte Absätze je Sprache, und das Wort `archiv` nirgends im Elementtext.
    Erlaubt sind Überschriften, Fettschrift, Kursiv, Links, Listen und Blockzitate. Die
    Instanz-Ansicht rendert mit `breaks: false`: Absätze entstehen durch Leerzeilen, ein
    einzelner Zeilenumbruch verschwindet.

    Nicht anfassen: `<summary>` (Zeile 18-20), `<version>`, `<image-tag>`,
    `<environment-variables>` und jedes andere Element. Ein leer gemachtes Element
    beantwortet den Store-Upload mit HTTP 500 ohne verwertbare Meldung. Der Enterprise-Text
    liegt ausschließlich in `<description>`.

    Französische Fassung mit Accents und Leerzeichen vor dem Doppelpunkt wie die
    Nachbarabschnitte, deutsche Fassung mit echten Umlauten, keine Em-Dashes.
  </action>
  <verify>
    <automated>uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'k.cherif@outlook.de' appinfo/info.xml` gibt mindestens 4 (drei Beschreibungen plus die vorhandene `<author mail=...>`-Zeile)
    - `grep -o '<version>[^<]*' appinfo/info.xml` gibt weiterhin `<version>0.1.9`, `grep -o '<image-tag>[^<]*' appinfo/info.xml` weiterhin `<image-tag>0.1.9`
    - `git diff -- appinfo/info.xml` zeigt keine geänderte Zeile innerhalb von `<summary>` oder `<environment-variables>`
    - Der neue Text enthält kein Backtick, keinen senkrechten Strich, kein `<` und kein `>` als Markup, keine Zeile aus nur drei Bindestrichen
    - `grep -in 'archiv' appinfo/info.xml` gibt keinen Treffer im Elementtext der drei Beschreibungen
    - `uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -q` endet mit Exit 0, das deckt `description_problems`, `variable_problems` und das Vokabular-Gate ab
  </acceptance_criteria>
  <done>Die drei Store-Beschreibungen tragen denselben Enterprise-Hinweis, das Beschreibungs-Gate und das Variablen-Gate sind grün, keine andere Manifest-Zeile ist berührt.</done>
</task>

<task type="auto">
  <name>Task 3: Issue-Entwurf als Datei und eine Changelog-Zeile</name>
  <files>docs/contrib/enterprise-signals-issue.md, CHANGELOG.md</files>
  <read_first>
    - docs/contrib/227-pr-body.md, Zeile 1-12: das Kopf-Pattern aus HTML-Kommentar mit Metadaten und die Zeile, die Metadaten von Fremdtext trennt
    - C:/Users/Student/scripts/docs/specs/ideation-isv-monetarisierung-2026-08-25/validation-plan.md: Fake-Door Schritt 2, Titelvorgabe, Go/No-Go-Kriterien
    - CHANGELOG.md, Zeile 121-124: Store-Text-Änderung als eigener Changelog-Eintrag (Präzedenz 0.1.5 bis 0.1.7)
    - CHANGELOG.md, der in Plan 13-01 geschriebene 0.1.9-Block
    - tests/unit/test_exapp_env_setup.py, Zeile 2009-2025: warum `docs/contrib/**` im Vokabular-Gate liegt und warum `docs/contrib/227-pr-body.md` nicht verschoben werden darf
  </read_first>
  <action>
    Lege `docs/contrib/enterprise-signals-issue.md` an. Kopf ist ein HTML-Kommentar nach dem
    Muster von `docs/contrib/227-pr-body.md` mit den Metadaten
    `Target repo: street1983nk/nextcloud-mcp-connector`,
    `Title: Enterprise features: what would your org need before allowing MCP access?` und,
    statt einer `Submitted:`-Zeile, der Zeile
    `Not published. Publishing this issue is an owner decision (D-07).` Danach die Zeile, die
    Metadaten von Fremdtext trennt, und darunter der Body, wie er auf GitHub gerendert würde.

    Der Body fragt, was eine Organisation braucht, bevor sie MCP-Zugriff auf ihre Nextcloud
    erlaubt, und nennt die drei geplanten Bausteine (Audit-Log, Gruppen-Policies, SSO
    beziehungsweise IdP) als Ausgangspunkt, ohne sie als vorhanden darzustellen. Body auf
    Englisch wie das Analogon.

    In den Metadatenkopf gehört außerdem das Go-Kriterium, damit später messbar ist, wofür
    der Entwurf da war: mindestens fünf qualifizierte Organisations-Signale mit über 100
    Nutzern innerhalb von sechs Wochen, oder ein Ankerkunde mit Pilotwunsch.

    Kein `gh issue create`, kein `gh api`, kein Veröffentlichungsschritt in dieser Phase.
    Verschiebe, benenne und ändere `docs/contrib/227-pr-body.md` nicht: ein Selbsttest des
    Vokabular-Gates behauptet ausdrücklich die Existenz dieser Datei als Beleg, dass der
    Walk rekursiv ist.

    Ergänze anschließend im 0.1.9-Block von `CHANGELOG.md` eine Rubrik `### Added` mit genau
    einer Zeile zum Enterprise-Abschnitt, nach dem Muster der Store-Text-Einträge der
    Blöcke 0.1.5 bis 0.1.7: die READMEs und die Store-Beschreibung nennen jetzt einen
    geplanten kommerziellen Zusatz mit Audit-Log, Gruppen-Policies und SSO, heute existiert
    keiner davon, und die Beschreibung wird mit diesem Release sichtbar, weil der Store das
    Manifest nur beim Release-Upload liest. Kein Preis in der Changelog-Zeile.

    `docs/contrib/**` liegt in der rekursiven Reichweite des Vokabular-Gates: das Wort
    `archiv` darf in der neuen Datei nicht vorkommen. `docs/contrib/` liegt dagegen NICHT in
    der Reichweite des Werkzeugzahl-Gates, das nur `docs/*.md` auf oberster Ebene und
    `README.md` liest; eine Werkzeugzahl braucht in dieser Datei also keinen Halter-Pfad,
    nenne sie aber ohnehin nicht.
  </action>
  <verify>
    <automated>uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/contrib/enterprise-signals-issue.md` ist wahr
    - `grep -c 'Enterprise features: what would your org need before allowing MCP access?' docs/contrib/enterprise-signals-issue.md` gibt 1
    - `grep -c 'Not published' docs/contrib/enterprise-signals-issue.md` gibt 1 und die Zeile nennt `owner decision`
    - Die Datei nennt das Go-Kriterium mit der Zahl fünf und der Zahl 100 sowie den Zeitraum von sechs Wochen
    - `git status --short docs/contrib/227-pr-body.md` gibt keine Zeile aus
    - `grep -c '^### Added' CHANGELOG.md` gibt mindestens 1, und der Treffer liegt im 0.1.9-Block oberhalb der Zeile mit `## [0.1.8]`
    - Die neue Changelog-Zeile nennt Enterprise und nennt keine Währung und keinen Preis
    - `grep -in 'archiv' docs/contrib/enterprise-signals-issue.md` gibt keinen Treffer
    - `git log --oneline -1 --grep='gh issue create'` findet nichts und die Shell-Historie dieses Plans enthält kein `gh issue`
    - `uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -q` endet mit Exit 0
  </acceptance_criteria>
  <done>Der Issue-Entwurf liegt als Datei im Repo, benennt die Veröffentlichung als Owner-Entscheidung und trägt das Go-Kriterium; der 0.1.9-Block nennt den Enterprise-Abschnitt in einer Zeile; nichts wurde veröffentlicht.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo-Text zu Store-Katalog | Der `<description>`-Text wird beim Release-Upload gelesen und steht danach jahrelang unverändert im Katalog |
| Entwurf im Repo zu veröffentlichtem GitHub-Issue | Die Veröffentlichung ist eine Owner-Handlung, kein Schritt dieses Plans |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Spoofing | Enterprise-Text in vier Fassungen | mitigate | Jede Fassung trägt einen eigenen Satz, dass heute keiner der drei Bausteine existiert, plus das Wort planned / geplant / prévu; Akzeptanzkriterium verbietet Währung, Preis und Lieferdatum per grep (D-05, D-08) |
| T-13-06 | Denial of Service | appinfo/info.xml Store-Upload | mitigate | Nur `<description>`-Elementtext wird geändert; `<summary>` und `<environment-variables>` bleiben unberührt und werden per `git diff`-Kriterium geprüft; `variable_problems` hält die bekannte 500er-Ursache |
| T-13-07 | Tampering | Rendering der Store-Beschreibung | mitigate | `description_problems` läuft vor dem Push; kein Backtick, kein senkrechter Strich, kein HTML, keine horizontale Linie, mindestens zwei Absätze je Sprache, Absätze durch Leerzeilen |
| T-13-08 | Repudiation | docs/contrib/enterprise-signals-issue.md | mitigate | Kein `gh issue create` in dieser Phase; die Datei nennt die Veröffentlichung ausdrücklich als Owner-Entscheidung (D-07) |
| T-13-09 | Tampering | docs/contrib/227-pr-body.md | mitigate | Datei wird nicht verschoben, umbenannt oder geändert; ein Selbsttest des Vokabular-Gates hängt an ihrer Existenz; per `git status`-Kriterium geprüft |
| T-13-SC | Tampering | Paketinstallation | accept | Dieser Plan ändert ausschließlich Markdown und XML-Elementtext; kein Paketmanager wird aufgerufen, es gibt keinen Installationskandidaten |
</threat_model>

<verification>
- `uv run --no-sync pytest tests/unit/test_exapp_env_setup.py tests/contract/test_tool_surface.py -q` grün
- `## Enterprise` in allen drei READMEs an der richtigen Stelle
- `k.cherif@outlook.de` in allen vier Fassungen, kein Preis in keiner
- Issue-Entwurf existiert, ist nicht veröffentlicht, `docs/contrib/227-pr-body.md` unberührt
</verification>

<success_criteria>
D-05, D-06 und D-07 sind geliefert: vier Fake-Door-Texte, die ein Vorhaben beschreiben und
kein Feature versprechen, plus ein Issue-Entwurf, dessen Veröffentlichung dem Owner
vorbehalten bleibt. D-08 ist gewahrt: kein Audit-Log, keine Policy, kein SSO wurde gebaut.
</success_criteria>

<output>
Create `.planning/phases/13-cimd-nachmessung-und-release-0-1-9/13-02-SUMMARY.md` when done.
</output>
