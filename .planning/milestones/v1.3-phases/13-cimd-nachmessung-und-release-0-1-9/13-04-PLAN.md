---
phase: 13-cimd-nachmessung-und-release-0-1-9
plan: 04
type: execute
wave: 4
depends_on: ["13-03"]
files_modified:
  - docs/store-submission.md
autonomous: true
requirements: [EXAPP-09]

must_haves:
  truths:
    - "Alle sechs Gates aus Runbook-Schritt 3 laufen lokal grün, das Vokabular-Gate in seiner Phase-12-Reichweite eingeschlossen"
    - "Das gebaute Store-Archiv hat genau einen Top-Level-Ordner und trägt die README-Statuszeile 0.1.9"
    - "Der Archiv-Probelauf ist im Nachweis ausdrücklich als Strukturprüfung benannt"
    - "Die Runbook-Schritte 1 bis 3 tragen je eine Proof-Zeile mit Datum, Behauptung und Befehl"
    - "Kein Tag v0.1.9 existiert"
  artifacts:
    - path: "docs/store-submission.md"
      provides: "Proof-Zeilen der Schritte 1 bis 3 für 0.1.9"
      contains: "0.1.9"
  key_links:
    - from: "dist/mcp_connector-0.1.9.tar.gz"
      to: "README.md Statuszeile"
      via: "das Archiv trägt README.md mit, die Statuszeile reist unveränderlich mit"
      pattern: "^Version 0\\.1\\.9\\."
    - from: "docs/store-submission.md"
      to: "Vokabular-Gate"
      via: "einzige Datei mit Ausnahme, damit eine datierte Proof-Zeile nicht nachträglich umgeschrieben werden muss"
      pattern: "VOCABULARY_EXCEPTION"
---

<objective>
Runbook Schritt 3: die sechs Gates lokal grün, der Archiv-Probelauf als Strukturprüfung, und
die Proof-Zeilen der Schritte 1 bis 3. Dieser Plan ist der letzte, der ohne Owner-Freigabe
vollständig abschließbar ist.

Purpose: D-04 verlangt die Gates auf der v1.2-Messung und das Vokabular-Gate in der neuen
Phase-12-Reichweite lokal VOR dem Push. Pitfall 7 verlangt den Beleg, dass die
README-Statuszeile diesmal im Archiv steht: bei 0.1.8 lag der Fix hinter dem Tag, und weil
das Asset unveränderlich ist, blieb die Zeile 0.1.7 veröffentlicht.
Output: sechs grüne Gates, ein geprüftes Archiv in `dist/` (nicht committet), drei
Proof-Zeilen in `docs/store-submission.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-cimd-nachmessung-und-release-0-1-9/13-CONTEXT.md
@.planning/phases/13-cimd-nachmessung-und-release-0-1-9/13-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Die sechs Gates in der Runbook-Reihenfolge und der Archiv-Probelauf</name>
  <files>dist/ (gitignored, nicht committen)</files>
  <read_first>
    - docs/store-submission.md, Zeile 191-203 (Runbook Schritt 3: die sechs Kommandos wörtlich und die Beschreibungsregeln)
    - docs/store-submission.md, Zeile 123-124 (die 0.1.8-Proof-Zeilen zu Gates und Archiv-Probelauf als Formvorbild)
    - scripts/build_store_release.sh, Zeile 15-30 (der Kommentar "diagnosis only" und der Verweis auf Schritt 6) sowie die Mitgliederliste des Archivs
    - scripts/check_tool_budget.py (der Werkzeug-Oberflächen-Gate und seine Grenzwerte)
    - .planning/phases/13-cimd-nachmessung-und-release-0-1-9/13-RESEARCH.md, Abschnitt "Die sechs Gates aus Runbook-Schritt 3" und Pitfall 7
  </read_first>
  <action>
    Führe die sechs Gates in genau der Reihenfolge aus, die Runbook Schritt 3 nennt, jedes mit
    dem Präfix `uv run --no-sync`: `pytest -q`, `ruff check .`, `ruff format --check .`,
    `pyright`, `vulture src scripts vulture_whitelist.py`, und
    `python scripts/check_tool_budget.py`. Halte jede Ausgabe fest, die die Proof-Zeile
    braucht: die Zahl der bestandenen und deselektierten Tests, die Zahl der formatierten
    Dateien, und die Messung der Werkzeugoberfläche.

    Erwartungswert der letzten Messung: 15711 Bytes über 21 Werkzeuge gegen ein Budget von
    18000. Die Zahl darf sich in dieser Phase nicht ändern, weil kein Werkzeug und kein
    Docstring angefasst wurde. Weicht sie ab, ist das ein Befund und kein Rundungsfehler: dann
    stoppe und nenne die Abweichung, statt die Grenzwerte anzuheben. Die Gates bleiben auf der
    v1.2-Messung (BUDGET_BYTES 18000, MAX_TOOL_BYTES 1400, 21 Werkzeuge), eine Anhebung ist
    ausdrücklich Out of Scope (D-04).

    Baue danach das Store-Archiv als Strukturprüfung: `bash scripts/build_store_release.sh`,
    dann `tar -tzf dist/mcp_connector-0.1.9.tar.gz`. Erwartet wird genau ein Top-Level-Ordner
    `mcp_connector/`. Die vom Skript ausgegebene Signatur ist Diagnose und wird nirgends
    eingereicht: signiert wird in Plan 13-06 ausschließlich das heruntergeladene Asset, weil
    `tar.gz` nicht bytereproduzierbar ist (bei 0.1.8: 45710 Bytes lokal gegen 45546
    veröffentlicht, verschiedene sha256).

    Prüfe im gebauten Archiv ausdrücklich die fünfte Versionsstelle, denn nur `README.md`
    reist mit und kein Gate hält sie:
    `tar -xzOf dist/mcp_connector-0.1.9.tar.gz mcp_connector/README.md | grep -n '^Version 0\.1\.9\.'`
    muss einen Treffer geben.

    Committe nichts aus `dist/`; das Verzeichnis ist gitignored und hält bereits die
    Archive früherer Versionen.
  </action>
  <verify>
    <automated>uv run --no-sync pytest -q && uv run --no-sync ruff check . && uv run --no-sync ruff format --check . && uv run --no-sync pyright && uv run --no-sync vulture src scripts vulture_whitelist.py && uv run --no-sync python scripts/check_tool_budget.py</automated>
  </verify>
  <acceptance_criteria>
    - Jedes der sechs Kommandos endet mit Exit 0, und die Ausgaben von `pytest -q`, `ruff format --check .` und `check_tool_budget.py` sind mit ihren Zahlen notiert
    - `uv run --no-sync python scripts/check_tool_budget.py` nennt 21 Werkzeuge und ein Budget von 18000
    - `tar -tzf dist/mcp_connector-0.1.9.tar.gz | cut -d/ -f1 | sort -u` gibt genau eine Zeile, `mcp_connector`
    - `tar -xzOf dist/mcp_connector-0.1.9.tar.gz mcp_connector/README.md | grep -c '^Version 0\.1\.9\.'` gibt 1
    - `git status --short dist` gibt keine Zeile aus
    - `git tag --list v0.1.9` gibt keine Zeile aus
  </acceptance_criteria>
  <done>Sechs Gates grün, das Archiv hat einen Top-Level-Ordner und trägt die Statuszeile 0.1.9, nichts aus dist ist committet, kein Tag existiert.</done>
</task>

<task type="auto">
  <name>Task 2: Proof-Zeilen der Runbook-Schritte 1 bis 3</name>
  <files>docs/store-submission.md</files>
  <read_first>
    - docs/store-submission.md, Zeile 76-79 (der Satz "Every line was measured, not assumed. No fact without its check." und der Tabellenkopf `| Date | Fact | Checked with |`)
    - docs/store-submission.md, Zeile 123-132 (die zehn 0.1.8-Zeilen als Formvorbild) und Zeile 134 (`### The update keeps the connections`, die Grenze, vor der die neuen Zeilen stehen)
    - docs/store-submission.md, Zeile 174-203 (Runbook Schritte 1 bis 3, damit jede Behauptung ihren Schritt trifft)
    - tests/unit/test_exapp_env_setup.py, Zeile 1978 (`VOCABULARY_EXCEPTION`) samt Gate-Docstring: warum genau diese Datei ausgenommen ist
  </read_first>
  <action>
    Schreibe drei neue Zeilen in die Nachweistabelle, direkt nach der letzten 0.1.8-Zeile
    (heute Zeile 132) und vor `### The update keeps the connections`. Form wie die Vorbilder:
    Datum in UTC mit `Z`, dann die Behauptung im Präsens, dann der Befehl, mit dem sie
    geprüft wurde. Sprache Englisch wie die übrige Datei.

    Zeile zu Schritt 1: die sechs Versionsstellen tragen 0.1.9, mit den drei
    README-Statuszeilen ausdrücklich genannt, geprüft per grep je Stelle plus dem
    Manifest-Gate; nenne auch, dass `uv.lock` mitgezogen wurde.

    Zeile zu Schritt 2: der Changelog-Block 0.1.9 nennt `message_truncated` als
    Formatänderung, die README-Provider-Korrektur als Doku-Korrektur und den
    Enterprise-Abschnitt als neuen Store-Text; die zwei Link-Referenzen am Dateiende stehen.

    Zeile zu Schritt 3: alle sechs Gates sind lokal grün, mit den gemessenen Zahlen aus
    Task 1 (bestandene und deselektierte Tests, formatierte Dateien, keine Lint- und
    Typbefunde, kein toter Code, Werkzeugoberfläche in Bytes über 21 Werkzeuge gegen 18000),
    und das Store-Archiv hat genau einen Top-Level-Ordner und trägt die Statuszeile 0.1.9.
    Diese Zeile muss den Satz enthalten, dass der Archiv-Probelauf eine Strukturprüfung ist
    und das lokal gebaute Archiv nicht das Artefakt, das signiert wird. Nenne keine Signatur
    und keine Bytezahl als eingereichten Wert.

    Schreibe keine Zeile für ein Ereignis, das noch nicht eingetreten ist. Die Schritte 4
    bis 8 bekommen ihre Zeilen in den Plänen 13-05 und 13-06, nachdem sie passiert sind:
    eine vorab geschriebene Proof-Zeile dreht die Beweisrichtung um und macht aus einem
    Protokoll eine Behauptung. Diese Datei ist die einzige mit einer Vokabular-Gate-Ausnahme,
    und genau diese Beweisrichtung ist ihre Begründung.

    Behaupte am Ende dieses Tasks ausdrücklich, dass noch kein Tag existiert.
  </action>
  <verify>
    <automated>uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `docs/store-submission.md` enthält drei neue Tabellenzeilen, jede beginnend mit einem Datum der Form `| YYYY-MM-DD HH:MMZ |`
    - Die drei Zeilen stehen zwischen der letzten 0.1.8-Zeile und der Überschrift `### The update keeps the connections` (`grep -n` belegt die Reihenfolge)
    - Jede der drei Zeilen nennt `0.1.9` und trägt in der dritten Spalte mindestens ein Kommando
    - Die Zeile zu Schritt 3 enthält die Worte structure check und sagt, dass das lokal gebaute Archiv nicht das signierte Artefakt ist
    - `docs/store-submission.md` enthält keine Zeile zu den Schritten 4 bis 8 für 0.1.9
    - `git tag --list v0.1.9` gibt keine Zeile aus
    - `uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -q` endet mit Exit 0
  </acceptance_criteria>
  <done>Drei datierte Proof-Zeilen belegen die Schritte 1 bis 3, der Probelauf ist als Strukturprüfung benannt, keine Zeile läuft ihrem Ereignis voraus, kein Tag existiert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lokal gebautes Archiv zu veröffentlichtem Asset | Zwei verschiedene Bytefolgen, die leicht verwechselt werden; nur die zweite darf signiert werden |
| Nachweistabelle zu Leser | Die Tabelle ist der einzige Beleg für die Sorgfalt eines Releases; ihre Richtung ist Ereignis, dann Zeile |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-17 | Tampering | dist/mcp_connector-0.1.9.tar.gz | mitigate | Der Probelauf ist in der Proof-Zeile ausdrücklich als Strukturprüfung benannt; die vom Skript ausgegebene Signatur ist Diagnose; das Signieren findet ausschließlich in Plan 13-06 über das heruntergeladene Asset statt |
| T-13-18 | Repudiation | docs/store-submission.md | mitigate | Nur Zeilen für eingetretene Ereignisse; Form ist Datum in UTC plus Behauptung plus Befehl; ein Akzeptanzkriterium verbietet Zeilen zu den Schritten 4 bis 8 in diesem Plan |
| T-13-19 | Denial of Service | Git-Tag | mitigate | Dieser Plan erzeugt keinen Tag und behauptet als Akzeptanzkriterium in beiden Tasks, dass `git tag --list v0.1.9` leer ist |
| T-13-20 | Tampering | Gate-Grenzwerte | mitigate | Die Grenzwerte bleiben auf der v1.2-Messung (D-04); weicht die Werkzeugoberfläche ab, stoppt der Task und meldet den Befund statt das Budget anzuheben |
| T-13-21 | Information Disclosure | dist/ im Arbeitsbaum | accept | `dist/` ist gitignored und hält nur gebaute Archive aus öffentlichem Quellcode; ein Akzeptanzkriterium prüft, dass nichts daraus committet wird |
| T-13-SC | Tampering | Paketinstallation | mitigate | Alle Gate-Läufe mit `uv run --no-sync`, also ohne Sync und ohne Auflösung; kein `uv add`, `pip install` oder `npm install` |
</threat_model>

<verification>
- Sechs Gates mit Exit 0, Zahlen notiert
- Archiv mit genau einem Top-Level-Ordner und Statuszeile 0.1.9 darin
- Drei Proof-Zeilen an der richtigen Stelle, keine Zeile für die Schritte 4 bis 8
- `git tag --list v0.1.9` leer
</verification>

<success_criteria>
Roadmap-Erfolgskriterium 3 ist zur Hälfte erfüllt: alle Gates laufen lokal grün, das
Vokabular-Gate in seiner neuen Reichweite aus Phase 12 eingeschlossen, und kein Tag existiert.
Die Phase ist damit ohne Owner-Freigabe bis Runbook-Schritt 3 vollständig abgeschlossen.
</success_criteria>

<output>
Create `.planning/phases/13-cimd-nachmessung-und-release-0-1-9/13-04-SUMMARY.md` when done.
Nenne darin die gemessenen Gate-Zahlen und die Bytegröße des lokal gebauten Archivs, damit
Plan 13-06 die Differenz zum veröffentlichten Asset belegen kann.
</output>
